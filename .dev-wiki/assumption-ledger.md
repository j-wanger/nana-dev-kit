# Assumption Ledger

<!-- Schema + validator: scripts/check-assumption-ledger.sh (the `## Ledger schema` block) is THE single
     source of truth for this format. APPEND-ONLY: one `## Phase` block per phase, newest at the bottom;
     never rewrite a prior block except to fill a blank `revisit-status:` at debrief. The dev-plan
     assumption gate APPENDS a block when positions are taken; dev-debrief FILLS revisit-status at close. -->

## Phase 80 — Assumption-Surfacer Completeness Screen
- date: 2026-06-09
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Forced accept/reject/don't-know verdicts engage cognition rather than producing faster rubber-stamps"
- A2 | cost: high | position: reject | revisit-status: held | "The agent-CHOSEN assumption set can be trusted to be complete enough to gate on"
- A3 | cost: medium | position: accept | revisit-status: held | "The ledger's revisit-status will be filled (not write-only), given a debrief forcing-function"
- A4 | cost: medium | position: accept | revisit-status: held | "nana-dev-kit is the right substrate to build and dogfood this gate"

## Phase 82 — QA & Verification Sweep (ultracode)
- date: 2026-06-09
- all_accept: false
- A1 | cost: high | position: don't-know | revisit-status: bit | "The seven repo-centric audit areas cover the silent-breakage surface that matters (maintainer named a missing axis: utilization/dead-weight, e.g. the barely-used MCP memory server)"
- A2 | cost: high | position: reject | revisit-status: held | "In-kit subagent context leak is acceptable for QA verification given an executed-command evidence standard plus orchestrator re-execution of clean rows"
- A3 | cost: medium | position: accept | revisit-status: held | "templates/ is the right direction-of-authority default and the planning-time 6-file resync overwrote no unbackported ~/.claude hot-fix"
- A4 | cost: medium | position: accept | revisit-status: held | "The pre-registered fix boundary (kit-managed + non-frozen + S/M + test-covered blast radius; coverage exception: writing the missing S/M functional test IS the fix; >10 confirmed defects stops the phase) is the right autonomy contract"
- A5 | cost: high | position: accept | revisit-status: held | "REVISES A1: eight areas — adding a deterministic usage/utilization audit whose under-use findings feed the parked Phase-79 prune-on-value item — cover the silent-breakage-and-dead-weight surface"
- A6 | cost: high | position: accept | revisit-status: held | "REVISES A2: subagents demoted to candidate-generators with orchestrator-executed deterministic commands as the SOLE verdict evidence (clean and defect-found alike) makes verification sound despite the leak"

## Phase 83 — Prune-on-Value Subtraction
- date: 2026-06-09
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Installed deregistration is mechanically safe: a cut hook's settings entry can be removed from live ~/.claude + consuming-project settings.json without corrupting the hooks shape (sandbox-first rehearsal + survivor functional smoke), despite no deregistration mechanism existing today"
- A2 | cost: high | position: accept | revisit-status: bit | "The Phase-82 usage zeros measure absent demand, not the dormancy eras — the window is sound post-enforcement-restoration, with per-candidate couldnt-fire/didnt-fire arming as the plumbing check"
- A3 | cost: high | position: reject | revisit-status: held | "The three liveness-grep roots (repo, ~/.claude, edge-screener) are the COMPLETE installed surface — maintainer rejected: other installs may exist"
- A4 | cost: medium | position: don't-know | revisit-status: held | "The reinforcement arming test is decidable: a seeded near-duplicate on the live install's actual dedup path settles couldnt-fire vs didnt-fire despite the unknown 0/55 base rate"
- A5 | cost: medium | position: don't-know | revisit-status: held | "The kit's own 55 memory entries are real voluntary-layer use, scoping candidate 3 to scaffold-shipping only — undefendable (access counts ~0, reinforcement never fired); DOWN-SCOPED: plan stays agnostic, kit-side memory-layer value question deferred to Blockers, must-revisit"
- A6 | cost: high | position: accept | revisit-status: held | "REVISES A3: the installed surface is mechanically DISCOVERED at T1 (kit-marker scan over this machine: .nana-dev-kit-path, .claude/hooks kit scripts, .nana/ dirs, shell-profile exports) and every cut's liveness grep runs over all discovered roots; residual: other machines out of reach"
- A7 | cost: medium | position: accept | revisit-status: held | "REVISES A4: defended with code+venv evidence — live install lacks fastembed and storage.py only reinforces at cosine >0.90 (unreachable without embeddings; word-overlap path only warns), so candidate 2's zero is largely pre-classified couldnt-fire → keep/harden semantics, never a demand-evidence cut"

## Phase 84 — Hook & Registration Hygiene
- date: 2026-06-09
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: bit | "A success/failure signal for the two dormant hooks is recoverable — present in the current PostToolUse event or reconstructible from verifiable state (git state / tool_response.stderr); capture-first T2 with three pre-declared evidence-forced branches (remap / trigger-redesign / file-upstream+disable-at-boundary)"
- A2 | cost: high | position: don't-know | revisit-status: open | "Ghost global registrations are not load-bearing after matrix+checkpoint — REVISED with evidence (ghosts ARE the only wiring for 8 hooks in 3 roots: stock-screener, ai-game, fate; edge-analyst+edge-screener 11/11 local) — still don't-know on the no-invisible-dependency-class claim; DEFERRED: no live settings execution rides on this gate; remediate-then-deregister prepared as checkpoint default, execution only on explicit checkpoint approval or deferred to a follow-up round"
- A3 | cost: medium | position: accept | revisit-status: held | "Exporting CLAUDE_PROJECT_DIR=$WORK_DIR in the eval harness reproduces the platform guarantee at hook invocation; unset-variant test covers the hooks' :-. fallback branch as the hedge"
- A4 | cost: low | position: accept | revisit-status: held | "The four Phase-82 pinned repros still reproduce at HEAD — VERIFIED at gate time (all three sandbox repros rc=1/dormant + eval-runner grep CLAUDE_PROJECT_DIR = 0), dissolved from assumption to fact"
- A5 | cost: medium | position: accept | revisit-status: held | "The eval surface absorbs the woken hooks: ~6 affected corpus scenarios updated, legacy exit_code fallback retained, 52-scenario denominator unchanged, every flip explained in eval-diff.md; mass unexplained flips = hard stop"

## Phase 85 — Install-Gap Fix + Edge-Screener Dogfood
- date: 2026-06-10
- all_accept: false
- A1 | cost: high | position: don't-know | revisit-status: held | "Incident 5's causal path was the Phase-82 drift-guided resync and py-init/ts-init recursive copies are sound, so install.sh-global + checker fixes cover ALL real shipping paths — DEFERRED (Phase-84 A2 pattern): T1 inventory completes BEFORE checkpoint 1, nothing live touched on this assumption's authority, spec conditional clause expands scope on refutation"
- A2 | cost: high | position: accept | revisit-status: bit | "Duplicate hook entries across edge-screener settings.json + settings.local.json double-fire — accepted as working model WITH mandatory pre-checkpoint-2 empirical verification (real piped event in sandbox) and STOP-and-re-present if contradicted"
- A3 | cost: medium | position: don't-know | revisit-status: held | "Presence of session-start.sh at ~/.claude justifies shipping/currency-checking its .d/ there — DOWN-SCOPED with evidence (copy is registration-dead: zero references in ~/.claude/settings.json, all live registrations project-local, positive control passed): installer invariant + checker cells proceed independent of the copy's fate; ship-vs-dispose of dead ~/.claude project-scope copies decided explicitly at checkpoint 1 with the full inventory"
- A4 | cost: medium | position: accept | revisit-status: held | "The hook_dirs schema migration's consumer set is exactly 4 files — defended with exhaustive repo-wide grep (install.sh:72, test_registration.sh:62, test_hook_firing_coverage.sh:40, harness-audit.sh:385; no hook_dirs name collision; all 4 covered by make test)"

## Phase 86 — Ceremony Lift Measurement
- date: 2026-06-10
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "Stage-1 evidence (admissibility-ruled demand table + cost table) suffices for an informed HUMAN disposition at the hard checkpoint — ACCEPTED AT ROUND 2 after maintainer rejected the reversible-disposition down-scope: stage 1 mints NO verdicts, the closed keep/trim/cut/ambiguous enum is the maintainer's menu, and the agent-counterfactual residual (would the implementing agent have caught it without the reviewer?) rides as an explicit caveat column absorbed by the maintainer"
- A2 | cost: high | position: accept | revisit-status: bit | "Pre-fix states are recoverable for enough historical reviewer findings (git ancestors / transcript-extracted file state) that the review step's rows do not all downgrade to ambiguous; pinned downgrade direction handles any shortfall honestly rather than minting evidence"
- A3 | cost: medium | position: accept | revisit-status: held | "Per-step ceremony cost is deterministically extractable from session transcripts — DEFENDED with spike evidence at gate time (one real 3.2M session: 565 usage-bearing messages, 299,574 output tokens summed, Skill invocations identifiable by name, per-message timestamps); heuristic session→phase and step-boundary attribution gated by a positive control on a known-composition session before any cost row counts"
- A4 | cost: high | position: accept | revisit-status: held | "In-kit retrospective tabulation of historical FACTS is outside the Ph80 leak class (which poisoned clean-context agent-BEHAVIOR measurement), provided the pre-registered corpus (fixed phase window + mechanical enumeration of ALL reviewer/spec/debrief dispatches incl. zero-catch denominators) closes the prior-driven row-selection channel"
- A5 | cost: low | position: accept | revisit-status: held | "At least one ceremony step is decidable from stage-1 evidence alone — the screen narrows the stage-2 question (debrief's consumption-only evidence class and review's Phase-85 marginal catch already point in opposite directions); if false, the cost table + keep-by-immateriality branch are still valid cheap outcomes"

## Phase 87 — Ceremony Stage-2 Episode Contrast
- date: 2026-06-10
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "The Phase-85 enforcement state is reproducible into the setup commit so hooks FIRE in both clones (gitignored settings.local.json + untracked .claude/enforce force-added per provisioning manifest) — guarded by a HEU-012 scratch-clone hook-fire probe (real event piped, exit 2 asserted) before any arm tokens; registered-but-broken arm-B treatment = the episode measures nothing"
- A2 | cost: high | position: accept | revisit-status: bit | "A canned-gate ceremony arm is representative enough for the spec-generation disposition (the primary customer runs /spec --internal with NO user gate); a tie/loss carries the bundle-attribution caveat rather than reading as decapitated-ceremony evidence; claim ceiling bars verdicts on gate-dependent steps"
- A3 | cost: high | position: accept | revisit-status: held | "Cutting both clones from a CHILD setup SHA (368e056 + parity setup commit) is faithful to the frozen 'twin worktrees from the same git state' — the freeze fixes same-state, not the literal SHA; deviation re-presented explicitly at the setup HARD checkpoint for the record"
- A4 | cost: medium | position: accept | revisit-status: held | "Two same-model budget-capped sessions are drivable non-interactively under the closed gate-response policy — ACCEPTED AT ROUND 2 as SPIKE-DEFENDED after initial don't-know: a scratch drivability spike (gate-bearing session driven to stop point) runs BEFORE the setup checkpoint, STOP after 2 mechanism attempts; arm DNF is a recorded result row, never a quiet re-run"
- A5 | cost: medium | position: accept | revisit-status: held | "Seeding the settings.local.json single-registration invariant into edge-screener's dev-wiki is an acceptable checkpoint-gated write (true documentation, Ph85-verified) and the ceremony arm can plausibly surface it via dev-plan's dev-wiki reading; seed rejected at checkpoint = frozen control unrunnable = STOP, no unilateral substitute"

## Phase 88 — Trim Follow-On Round
- date: 2026-06-11
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Basename-normalized deregistration + ghost sweep over ALL discovered surfaces (~/.claude settings.json + every discovered root's settings.json AND gitignored settings.local.json) makes each cut's installed-surface removal mechanically safe — sandbox rehearsal with positive control + survivor-fire assertion before any live edit; wrong = the 5th registered-but-dormant instance, self-inflicted"
- A2 | cost: high | position: accept | revisit-status: held | "The three routed stage-2 files (run-exit-criteria.sh c2 function, check-instrument.sh, check-ship-table.sh) are severable from the freeze — SPIKE-DEFENDED after initial don't-know: verified no standing check pins the checker scripts' own bytes (no self-hash, no test reference, no MANIFEST row); T2 OPENS with a scratch-worktree demonstration (edit → both runners + make test green) before any live edit, failure = STOP/file/drop the strand"
- A3 | cost: medium | position: accept | revisit-status: held | "enforcement.log is reconstructable into admissible block→follow-through demand evidence — ATTEMPT+FALLBACK after initial don't-know: JSON-lines with ISO ts, schema_version-1 records carry a phase field (provenance filtering feasible for recent records); the phase never hinges on success, undecidable-on-this-evidence is the pre-stated landing, never argued from the raw 385 count"
- A4 | cost: high | position: reject | revisit-status: held | "Edge-screener's zero voluntary memory-tool use measures absent DEMAND for the kit-side memory MCP layer — REJECTED by maintainer: the zero is not demand evidence; the memory-layer disposition is REMOVED from Phase-88 scope and defers to a future round with better evidence (Blockers filing re-trigger updated; 20-surface enumeration defers with it)"
- A5 | cost: medium | position: accept | revisit-status: held | "active-knowledge.md's ~15 reader surfaces degrade gracefully on file-absence so the ride-along trim is a clean single-commit deletion — verified at T1 with the enumeration committed as an eval/trim-round/ artifact; if verification fails the row's mechanism changes (stub-write or same-commit reader cleanups), shape not direction"
- A6 | cost: high | position: reject | revisit-status: held | "Trimming the memory bridge (the layer's main writer) while the layer disposition is deferred is acceptable if the demand filing records the trim date (post-trim zeros = couldnt-write) — REJECTED by maintainer: defer the bridge trim too; ALL memory-bridge/harvest writers (debrief memory-harvest, dev-plan Step 15a-bis) stay alive until the layer demand question settles; strand 2 narrows to working-knowledge seeding + journal prose"

## Phase 89 — Post-Trim Dogfood & Demand-Evidence Round
- date: 2026-06-11
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "At least one Phase-89 session reaches a trigger-relevant state (planning/recovery decision with pinned decisions in scope) so the window strand gets PROBATIVE exposure — plan shapes session 1 as a real /dev-plan Phase-10 planning session in edge-screener; reachability records make failure visible (non-probative attestations), never silent"
- A2 | cost: high | position: accept | revisit-status: held | "Memory-tool calls and their provocation are observable/attributable from session artifacts via deterministic orchestrator-executed rules — ACCEPTED AS DEFENDED after initial don't-know: live probe evidence at gate time (memory MCP is user-scope registered in ~/.claude.json so edge-screener sessions inherit it; this session's own memory_store call verified present in the transcript JSONL as a mcp__memory__ tool_use block with input + timestamps); caveat recorded: a headless agent may fail to FIND the deferred tool (scratch probe declared it unavailable rather than searching) — demand-relevant context the evidence file must note"
- A3 | cost: high | position: accept | revisit-status: held | "The checkpoint-gated edge-screener re-sync (template-sourced --project-local + basename-normalized jq deregistration of the cut detect-loop) leaves its harness WORKING — rehearsal on a copy of the ACTUAL settings.local.json, timestamped backup + tested restore, post-resync currency assertion + survivor smoke before sessions count"
- A4 | cost: medium | position: accept | revisit-status: bit | "Edge-screener's own Phase-10 agenda (PIT-survivorship branch coverage; planning input ready, zero open tasks) sustains ≥3 real-work sessions — if it runs dry, STOP at checkpoint; any downgrade to scripted harness-exercise needs explicit approval and is recorded as weaker evidence"
- A5 | cost: medium | position: accept | revisit-status: held | "Headless maintainer-driven sessions are acceptable provenance for demand evidence (Phase-85 precedent), with the driver AND whose-agenda stated in every session block's honesty note — if the future prune round disputes headless provenance, the admissibility pin marks headless rows separately rather than discarding the round"

## Phase 90 — Fable-5 Distillation
- date: 2026-06-13
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: open | "Clean-context forcing functions (Track B2 framing subagent) recover fable's reframe-DEPTH despite Ph80 in-kit leak + Ph47 self-dialogue-negative — ACCEPTED PILOT-GATED: B2 built but NOT wired; the test is the consuming-project pilot (eval/fable-distill/pilot-protocol.md), unresolved until it runs. If false: the B2 centerpiece is comply-in-form relocated into a subagent"
- A2 | cost: medium | position: accept | revisit-status: held | "The 5 codifiable lines are genuinely absent from the prior soul AND being checkable are executed not rubber-stamped — absence confirmed via on-disk diff before edit; one line (#2 confirmed/inferred) shipped vibey, caught+fixed by the review gate (finding 3 → 'Mark inferred claims as inferred'), the rest name a pointable artifact"
- A3 | cost: medium | position: accept | revisit-status: held | "Ships on JUDGMENT not measurement — interaction quality unmeasurable in-kit (Ph59, Ph80) — HELD: exit = applied+parity+self-consistent, no A/B; the B2 pilot is the one real future measurement"
- A4 | cost: high | position: accept | revisit-status: held | "Cleared to edit global ~/.claude/rules/nana-soul.md directly (identity-level, all-projects blast radius) — HELD: edited + parity-mirrored to templates, maintainer reviews at delivery gate"

## Phase 91 — Consuming-Project Memory E2E + Recovery Continuation
- date: 2026-06-14
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Memory MCP is fixed-and-kept NOW (fix-then-judge), not cut first — maintainer ACCEPT: Track 1 repairs the consumer E2E break so Phase 92 judges demand on a WORKING layer (couldnt-fire is inadmissible prune evidence); if false, Track 1 activates a layer Phase 92 then removes"
- A2 | cost: high | position: accept | revisit-status: held | "The PYTHONPATH=~/.claude env on the MCP registration makes the server START in a LIVE consumer Claude Code session — maintainer ACCEPT, but only the Python import-chain was validated this session, NOT a fired MCP launch; the FIRST Track-1 task is verify-by-firing memory_search in a real consumer before anything builds on it (HEU-012). HELD: T5 verify-by-firing PASSED — a consumer-context memory_search returned without error and <project>/.memory/memory.db was created; the import-chain-only caveat is discharged"
- A3 | cost: medium | position: accept | revisit-status: held | "Consumers need WITHIN-project continuity → per-project .memory/ DBs are correct, not a shared/global store — maintainer ACCEPT: the fix ships a per-project topology + .memory/ gitignore; if false, memory cannot surface knowledge across projects and the baked-in topology is wrong"
- A4 | cost: high | position: don't-know | revisit-status: open | "Phase-90's codifiable behaviors LANDED is a real Phase-90 EFFECT, not the user's native planning culture — maintainer DON'T-KNOW; undefendable (no pre-90 opus baseline; the eval showed signal-watch fable exhibits the 5 natively). RESOLVED BY DOWN-SCOPE: the plan DROPS the 'Track A effective' claim; Track 2 treats recovery as UNPROVEN and the B2 pilot/baseline IS the measurement. Deferred → Blockers, revisit at debrief"

## Phase 92 — Strategic Inflection Review & Roadmap Re-sequencing
- date: 2026-06-18
- all_accept: false
- A1 | cost: high | position: reject | revisit-status: held | "nana-dev-kit is primarily a single-maintainer research instrument and serving external consuming projects is no longer a goal to optimize for — maintainer REJECTED: it IS a product for consumers, so the last ~10 days' inward measurement/hygiene growth is mission-creep to correct and the under-served consumer surface (install.sh re-sync, consumer memory firing, scaffold currency) is the primary work; the plan re-sequences around the product frame"
- A2 | cost: high | position: accept | revisit-status: held | "The amplifier-null (4 clean nulls Ph70/71/77/78 + 1 instrument-dead Ph80; the Ph61 runtime-retrieval A/B nulls) is decisive enough to shrink the knowledge/memory/re-presentation layer — ACCEPTED WITH the couldnt-fire correction: the Ph89 memory demand-zero was measured on memory BROKEN until Ph91, so it is inadmissible as cut evidence; one clean re-measure on re-synced WORKING memory MUST precede any memory-layer cut ([[HEU-012]])"
- A3 | cost: high | position: accept | revisit-status: held | "The honest reading of the kit's own evidence is wind-down, NOT the review synthesis's 'refocus' — which smuggled in two unmeasured build commitments (a proprietary/post-cutoff retrieval R&D track with ZERO lift evidence + a linter-rewrite on a never-observed defect); burden-of-proof-on-the-feature bars BOTH until measured, and the Ph87 'determinism beat the LLM reviewers' keep-evidence is a binary-file-excluded-from-diff confound not a det-over-neural proof"
- A4 | cost: medium | position: accept | revisit-status: held | "The det-vs-LLM boundary principles (LLM only for irreducible semantic synthesis; the final integrity gate is deterministic and asserts the observable artifact not the LLM's narration; fail-polarity by cost-of-error; curate the hot-cache over bolting on retrieval) are durable and adopted as a MAINTENANCE GUIDELINE — explicitly NOT a mandate to rewrite the working linter-skills (dev-check/wiki-health/wiki-registry), which stay deferred until a real mis-read is observed (their hypothesized failure mode is unobserved and the synthesis inflated their size ~40%)"
- A5 | cost: medium | position: accept | revisit-status: held | "The dependency order is install.sh-resync (Phase 93) then consumer memory re-measure (Phase 94) then memory-layer shrink (Phase 95, supersedes the old phase-92-memory-layer-prune spec): you cannot re-measure consumer memory demand until the Ph91 fix is re-synced INTO the stale consumers, and you cannot honestly cut the layer until that re-measure lands"

## Phase 93 — install.sh Idempotent Update / Consuming-Project Re-sync Mode
- date: 2026-06-18
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "Automated basename-normalized deregistration of cut hooks can be made safe enough to ship in the installer — maintainer ACCEPT behind --dry-run + timestamped backup + tested restore + survivor smoke + sandbox rehearsal; because this ships the destructive op the kit has been burned by 5x, Phase 93 is BUILD + SANDBOX-VERIFY ONLY (controls-first: a seeded cut-hook + a seeded duplicate registration must be caught before the path is trusted) and NEVER runs against a live consumer this phase. If a safety rail is insufficient the seeded-control sandbox catches it before any live use"
- A2 | cost: high | position: accept | revisit-status: held | "Build-only (zero live consumer writes) is the right Phase-93 scope — maintainer ACCEPT: build + heavily sandbox-test the capability, touch no external repo; live application (signal-watch onboarding + the 4 staged-gate backfills) defers to a gated follow-on. Drops the blast radius from 5 external repos to kit-code+tests, and does NOT block Phase 94 (consumer memory already FIRES globally via the Ph91 PYTHONPATH env, independent of the hook re-sync)"
- A3 | cost: medium | position: accept | revisit-status: held | "Extend install.sh with an idempotent --update mode (reuse cp/register/ship_hook_dirs, single source) rather than a separate scripts/resync.sh — subtraction test; the re-sync IS install logic with reconciliation + dedupe + dereg semantics added"
- A4 | cost: medium | position: accept | revisit-status: held | "Hook-refresh must be DECOUPLED from arming: --update refreshes/reconciles project-local hooks WITHOUT touching .claude/enforce (the 4 staged consumers were deliberately left un-armed in Phase 91; a plain --project-local re-run would arm them mid-work). Arming stays an explicit opt-in (--arm or manual marker). If false, re-sync silently arms enforcement in consumers"
- A5 | cost: medium | position: accept | revisit-status: held | "The consumer drift classes are enumerable + fixture-reproducible (signal-watch no-hooks; staged 17+detect-loop-ghost; Phase-79 hand-patched duplicate registration) so a controls-first mktemp sandbox harness IS the test for the mode without any live consumer; if a real consumer holds an unmodeled drift class, the deferred live-application follow-on catches it (dry-run first)"

## Phase 94 — Clean Consumer Memory Re-measure
- date: 2026-06-20
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "The memory MCP layer fires end-to-end in a consumer cwd (a store persists to <consumer>/.memory/memory.db and a search retrieves it, driven as Claude Code launches it) so a retrospective zero is genuine no-use, not couldnt-fire — ACCEPT; already corroborated by live consumer DBs holding real rows (aml-substrate 44, aml-casework 20). T1 verify-by-firing is the controls-first confirm (a broken-config run MUST classify COULDNT-FIRE). If false, the whole tally is inadmissible (Ph89 redux) and Phase 95 stays blocked"
- A2 | cost: high | position: don't-know | revisit-status: open | "A spontaneous-demand FLOOR on an unannounced layer is an adequate evidence basis for the Phase-95 cut decision — maintainer DON'T-KNOW. RESOLVED BY DOWN-SCOPE + SUBSTRATE EXPANSION: Phase 94 no longer rests on the floor alone — per A3 it ALSO measures the coerced demand directly (aml-substrate rules+hooks; aml-casework rules-only), the actual cut target, which is NON-ZERO (the floor reading reversed). Phase 94 files floor + coerced with an explicit NO-SUFFICIENCY caveat and routes the final sufficiency-for-cut judgment to Phase 95 (the disposition authority). Deferred -> Blockers"
- A3 | cost: medium | position: accept | revisit-status: held | "signal-watch n=1 as the substrate — maintainer REVISED to n=3: directed adding aml-substrate + aml-casework. Done; the expansion is load-bearing — it turned an n=1 spontaneous-floor (~0) into a 3-consumer machinery-gradient natural experiment (none / rules / rules+hooks -> 0 / 20 / 44 persisted rows) that REVERSED the 'consumer demand is zero' reading. n=3 forced-not-chosen on the live set (edge-screener dormant since ~06-11). If false, the contrast doesn't generalize beyond these three AML/agent consumers"
- A4 | cost: medium | position: accept | revisit-status: held | "The consumers' post-2026-06-14 (post-PYTHONPATH-fix) sessions ran on the working global memory layer, forming an admissible retrospective window (pre-fix excluded as couldnt-fire) — ACCEPT; T1 firing + the live DB rows corroborate the layer was live across the window. If false, the tally mixes admissible and broken-memory sessions"

## Phase 95 — Memory-Layer Disposition (Reconcile-and-Close)
- date: 2026-06-20
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Phase 95 is reconcile-dispose-then-close — adjudicate EVERY open memory obligation to a recorded evidence-cited closed-enum verdict, success = nothing left silently open, NOT a shrink/cut hunt — maintainer ACCEPT (Phase 94 REVERSED the demand-zero premise; the memory layer mostly KEEPS). If false (the kit-side writers/machinery are still a live cut target), the phase under-scopes a subtraction sub-hunt"
- A2 | cost: high | position: accept | revisit-status: held | "The enforce-memory disposition (keep|redesign|retire) is decided at a HARD maintainer checkpoint on a deterministic firing-distribution audit (allow/block ratio + block->real-memory_search follow-through vs ritual marker-touch), lean redesign-if-spike-passes-else-keep — maintainer ACCEPT. Redesign = assert a REAL in-session memory_search event (det-vs-llm Principle 2), GATED on a feasibility spike (can a PreToolUse hook read the session transcript JSONL?). If false (spike fails or the audit shows pure ritual), the menu narrows to keep-or-retire"
- A3 | cost: medium | position: reject | revisit-status: held | "Writer verdicts (bridge-writer, harvest-writer) require a FRESH kit-side store-retrieval audit — maintainer REJECTED: the Phase-94 consumer reversal + the writers' cheapness/liveness affirms KEEP without an audit (keep-by-affirmation). Evidence-split asymmetry holds — consumer evidence may KEEP a kit-side writer (the safe, no-collateral direction) but may NOT CUT one; the 0-reinforcements/access_count~0 signal is couldnt-fire (fastembed-gated), not demand-zero. If false (a future kit-side audit shows write-heavy never-retrieved spam), a writer cut needs its own kit-side evidence in a later round, out of scope here"
- A4 | cost: medium | position: accept | revisit-status: held | "The cut-execution rails are CONDITIONAL — verdict-table + per-row evidence citations + admissibility discipline ALWAYS present, but the heavy rails (basename-normalized deregistration, survivor functional smoke, unreachable-installs finding) arm ONLY if a destructive verdict fires (the subtraction test applied to the spec itself) — maintainer ACCEPT. The only destructive path left is enforce-memory retire (low-hazard: removing a BLOCKING hook un-blocks, no store backup, no self-lockout). If false (a memory-mcp-layer cut emerges), its full removal-set + dereg + skill-layer-caller discipline arms"

## Phase 96 — Consumer Re-sync Rollout (consolidate-to-local + live --update across all 7)
- date: 2026-06-21
- all_accept: false
- A1 | cost: high | position: reject | revisit-status: held | "Extend --update IN-PLACE (two-file-aware reconcile, each kit hook left in the settings file it lives in) — maintainer REJECTED in favor of CONSOLIDATE-to-local: a one-shot install.sh --migrate-to-local moves settings.json-resident kit-hook registrations into gitignored settings.local.json (register current kit set into local + basename-deregister kit-managed + cut hooks from settings.json), then the existing single-file --update serves all 7 unchanged. Rationale: one canonical topology + simpler --update + kit hooks become gitignored/personal; accepts a visible git diff on 3 committed settings.json (solo repos, no kit-less collaborators). If false, the seeded-control sandbox catches a bad migration before any live use"
- A2 | cost: high | position: accept | revisit-status: held | "The destructive migration/dereg rails must cover BOTH settings files — timestamped backup of settings.json AND settings.local.json before any removal, survivor functional smoke (a kept enforce hook fires allow+block) after, revert-on-failure — because the cut now touches the COMMITTED settings.json (the existing --update rail backed up only settings.local.json). If false, a failed Group-B migration can't auto-revert the committed file"
- A3 | cost: medium | position: accept | revisit-status: held | "Controls-first: new mktemp fixtures reproduce the settings.json-topology + post-migration idempotency classes, and seeded controls (a detect-loop registration in settings.json the migration MUST remove; settings.json kit-regs that MUST relocate to local) gate the build — clean-on-seed = instrument-dead -> STOP before any live consumer (HEU-012)"
- A4 | cost: high | position: accept | revisit-status: held | "Roll out all 7 consumers LIVE this phase (signal-watch onboard, edge-screener, aml-substrate via --update; edge-analyst, ai-game, fate, aml-casework via --migrate-to-local then --update) behind dry-run-first + per-consumer maintainer review + git-snapshot of dirty trees (ai-game's 73 uncommitted .claude files committed/stashed FIRST) + the two-file backup + survivor smoke + revert-on-failure — maintainer ACCEPT. A consumer revealing a drift class not modeled by the fixtures STOPs that consumer for re-modeling. If false, a live consumer's settings corrupt despite the rails"
- A5 | cost: medium | position: reject | revisit-status: held | "Arming stays fully decoupled (no consumer armed) — maintainer REJECTED: arm signal-watch + aml-casework this phase; ai-game + fate stay UNARMED; the 3 already-armed (edge-screener, edge-analyst, aml-substrate) stay armed (--update never touches .claude/enforce without --arm). If false, arming signal-watch (the Ph94 spontaneous-floor consumer) or aml-casework changes their behavior in a way the maintainer didn't intend"

## Phase 97 — Frontier Positioning Sweep
- date: 2026-06-21
- all_accept: false
- A1 | cost: high | position: reject | revisit-status: held | "A lift-sensitive shrink target remains for external signal to move (after Phase-95 closed memory→KEEP) — maintainer REJECTED: layer-shrink is the wrong target. REFRAMED to PRODUCT-POSITIONING — the sweep reads whether the kit's bet (deterministic spine + lifecycle ceremony) is DIFFERENTIATED / COMMODITIZED / DIVERGENT vs the frontier; shrink becomes a downstream CONSEQUENCE of a COMMODITIZED verdict, not the primary axis. If false (positioning is also unanswerable from public signal), T4 returns INCONCLUSIVE with named resolving-evidence rather than a retrofitted call — HELD: the positioning reframe held and produced a clean mechanical verdict (INCONCLUSIVE-forced-under-observed, differentiated-leaning); the reframe did not bit"
- A2 | cost: high | position: reject | revisit-status: held | "GitHub/OSS repos + public discourse is a good-enough proxy for 'the frontier' — maintainer REJECTED: the labs ARE the frontier. REWEIGHTED — published lab artifacts (Anthropic agent SDK / cookbooks / Claude Code itself, OpenAI Agents SDK, Google ADK, eng blogs, papers) are PRIMARY signal; OSS repos (stars/trending: aider, opencode, OpenHands, LangGraph, CrewAI…) drop to SECONDARY corroboration. Honesty bound stated in the pre-registration: internal lab harnesses stay unseen — we read DECLARED direction (what labs productize), which is a strong but not complete proxy — HELD: the labs WERE the frontier; lab artifacts carried the verdict (14/14 primary sources), OSS corroborated DIFFERENTIATED as secondary; did not bit"
- A3 | cost: medium | position: don't-know | revisit-status: held | "A one-shot sweep's signal half-life is long enough to be decision-useful vs needing the standing pipeline now — DOWN-SCOPED: no standing pipeline this phase regardless; T4 assesses signal half-life and RECOMMENDS a pipeline cadence (or none) for rung B, routed to a T4 deliverable (verdict.md), not Blockers. Resolves at T4, not deferred-open — HELD/RESOLVED at T4: value-capturing core layer is a slow (quarters-scale) signal → recommended QUARTERLY core-primitive watch for rung B (not a standing high-frequency pipeline)"

## Phase 98 — Frontier Watch (rung-B)
- date: 2026-06-21
- all_accept: false
- A1 | cost: high | position: reject | revisit-status: held | "The narrow watched-surface set + a per-cycle scope-completeness scan is sufficient to OBSERVE a core-primitive (B1/B4) commoditization — maintainer REJECTED: widen it. RESOLVED — the watch uses a BROAD source list (Anthropic/OpenAI/Google/MS/MCP release+changelog+blog surfaces + OSS bellwethers, secondary) scanned cheaply for the narrow value-capture-passing tripwire signal; breadth replaces narrowness as the blind-spot guard, backed by a monthly deep re-derivation. If false (a core transition arrives via a channel not in the broad list), the monthly deep re-derivation + scope-completeness scan are the backstop"
- A2 | cost: high | position: reject | revisit-status: held | "A quarterly cadence is the right watch frequency (the Ph97 verdict's quarters-scale half-life read) — maintainer REJECTED: things change too fast, weekly if not daily. RESOLVED — TWO-SPEED design splitting frequency by COST: a WEEKLY broad-source/narrow-signal fast scan (primary, cheap, high signal-to-noise — won't fire on attach-surface churn) + a MONTHLY deep full re-derivation backstop + on-hit escalation to the frozen rule. Honors the Ph97 half-life finding per-tier (fast cadence on the cheap scan, rare on the expensive re-derivation) while giving the maintainer's speed. If false (the weekly scan burns cost / causes alarm fatigue on the fast layer that does not move the verdict), the narrow-signal filter + monthly-only deep tier are the mitigations; revisit cadence at the first monthly backstop"
- A3 | cost: medium | position: accept | revisit-status: held | "On a tripwire firing, re-running the FROZEN pre-registration.md (600e1c9f) still yields a decision-useful verdict despite its known over-conservatism (the Ph97 B5 zero-tolerance bite) — maintainer ACCEPT. Anti-retrofit holds (cannot swap the rule after seeing it bite); the B1/B4 COMMODITIZED tripwire rides the K_low path not the B5 contest clause so the bite does not affect it, and a B5 fire resolves via the charter's pre-stated outcomes (both -> DIFFERENTIATED). If false (escalation re-emits FORCED-INCONCLUSIVE noise), a fresh simplified instrument is a later-round decision, out of scope here"

## Phase 99 — Direction Dashboard (render-only, dashboard-first)
- date: 2026-06-22
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "A render-only dashboard relieves the throughput pain — the bottleneck is PROCESSING dense design-direction text, not the latency of deciding (read the visual, answer the gate in-session); interactive click-to-decide is a separate later pillar. If false (the real bottleneck is reading-then-typing round-trip), this under-delivers and re-scopes toward a browser->session channel"
- A2 | cost: high | position: don't-know | revisit-status: open | "A one-shot generated HTML file suffices over an always-on watching server — DOWN-SCOPED to the generator this phase (it is a strict subset: a future live server reuses the same render function on file-change, so the choice costs nothing toward a server later); the live-server question is filed to Blockers with a concrete re-trigger (regenerate-by-hand becomes a friction bottleneck)"
- A3 | cost: medium | position: accept | revisit-status: held | "The /dev-plan direction gate (approach + options + cost-sorted assumptions) is the right first surface to render — higher value and self-dogfooding vs a general project-state/roadmap dashboard, which becomes orienting context AROUND the brief"
- A4 | cost: medium | position: accept | revisit-status: held | "A small /dev-plan integration is in-scope — dev-plan emits a structured direction-brief artifact at the gate (one new persisted file + a generate call) so options/approach render as visuals, not just chat prose; if the integration proves non-trivial it down-scopes to rendering existing dev-wiki files only"

## Phase 100 — Contract-vs-Spec Fidelity Screen (rung-C pillar-1 measurement)
- date: 2026-06-22
- all_accept: false
- A1 | cost: high | position: don't-know | revisit-status: held | "A new contract artifact earns its complexity over reusing success:/spec (subtraction-test fork) — DON'T-KNOW, CONVERTED to the phase's measurement target: a pre-registered 3-arm fidelity screen on real opencode workers (contract vs spec vs bare-baseline) decides it empirically, not by reasoning (maintainer: 'need to run actual test scenarios with opencode workers using different contract vs spec variants'). Resolves at the verdict. If false (screen inconclusive / arms equal), the contract artifact does NOT ship and the deliverable collapses to runner-only; the bare-baseline guards a trivial-task null"
- A2 | cost: high | position: don't-know | revisit-status: held | "The fidelity spine is deterministic-only — DON'T-KNOW, CONVERTED to measurement via R1: every run scored TWICE (deterministic guardrails AND a calibrated neural judge); deterministic-vs-judge agreement measures whether a judge catches fidelity violations the guardrails miss. If false (judge consistently catches what guardrails miss), the spine needs an explicit separately-marked neural layer — recorded by the verdict, not pre-built"
- A3 | cost: high | position: reject | revisit-status: held | "Validate against SEEDED worker outputs, no real worker this phase — REJECTED: a real opencode worker IS the instrument (seeded outputs can't answer contract-vs-spec). ABSORBED: a minimal slice of pillar-2 (real worker execution via opencode run --pure) merges in for the measurement only; the 24/7 fleet stays a later phase. If false (real runs too noisy/costly to score n-fold), fall back to smaller n + seeded controls for the deterministic check, but the screen needs SOME real runs to be non-vacuous"
- A4 | cost: medium | position: accept | revisit-status: held | "Pillar 1 ships to the kit — ACCEPT, scoped to the deterministic fidelity-CHECK (dual-use: scoring instrument + product, needed under EITHER A1 outcome so building it is no precommitment). The contract-SCHEMA shape is deferred to the verdict (R3). The opencode harness + runs + results are measurement apparatus -> gitignored companion/research/. If false (the check has no shippable form independent of the schema), the defer-the-schema split collapses and the phase becomes measurement-only (Option B')"
- R1 | cost: high | position: reject | revisit-status: held | "Deterministic-only fidelity scoring — REJECTED in favor of deterministic + a MARKED neural-judge arm. Every run scored by the deterministic check AND a calibrated, exemplar-anchored judge (cross-model to avoid self-grading bias, Ph44-50 method); the task corpus MUST include a subjective quality dimension so the two scorers CAN disagree (else the judge arm is vacuous). If false (judge un-calibratable / always agrees), the judge arm returns a clean 'deterministic suffices' and is dropped"
- R2 | cost: high | position: accept | revisit-status: held | "3-arm screen {contract, spec, bare-baseline} + a frozen pre-registered decision rule (contract must beat spec by a pre-stated margin on the fidelity metric to justify a contract artifact) — ACCEPT. The bare-baseline is the amplifier-null guard (caught every prior null Ph70/71/77/78/80); the freeze is anti-retrofit (Ph87/97 three-tier authority). If false (3 arms x n too costly), drop n before dropping the baseline arm"
- R3 | cost: medium | position: accept | revisit-status: held | "Ship the deterministic fidelity-CHECK to scripts/ + make + tests/ (instrument now, product later); DEFER the contract-schema shape to the verdict (ship in-phase if conclusive, else route to a build phase) — ACCEPT. Reconciles A4-ship with A1-don't-know. If false (the verdict needs the schema to exist to be testable), ship a provisional schema marked draft and refine at the verdict"

## Phase 101 — Harder-corpus contract screen on edge-screener
- date: 2026-06-22
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Tasks calibratable to the DISCRIMINATION BAND via a MANDATORY pilot (keep only tasks where bare fails-but-not-floored) before freezing; no in-band task -> informative null ('no discriminating harder corpus constructible for this worker'), not a forced verdict — maintainer ACCEPT. If false (the pilot itself can't locate the band), the null IS the result"
- A2 | cost: high | position: reject | revisit-status: held | "Bug-injection-repair construction — maintainer REJECTED in favor of FROM-SCRATCH STUB (more genuinely 'actual code development'): stub a real integrity-critical FUNCTION (single function, one strong namable invariant — decision-lag k->k+1, cost application, PIT survivorship), worker re-implements; bare/spec/contract differ only in whether the integrity invariant is named (test HELD-OUT). Floor risk (from-scratch is hard) mitigated by FUNCTION-granularity stubs + the A1 pilot discarding floored tasks. If false (all stub-tasks floor), the phase reports the null"
- A3 | cost: medium | position: accept | revisit-status: held | "Worker develops + is scored in a real per-run edge-screener copy (reuse .venv so deps are ready; opencode runs pytest there), heavier than Phase 100 -> smaller corpus (3-4 tasks x n=3-5); feasibility verified in the pilot — maintainer ACCEPT. If false (opencode can't run/test in edge-screener), re-scope the workspace before the full run"
- A4 | cost: medium | position: accept | revisit-status: held | "edge-screener is the right base (real, genuinely hard, un-foolable integrity tests, maintainer's domain; worker-in-edge-screener gives automatic nana-isolation) — maintainer ACCEPT. If false (a blocker emerges), fall back to a different real codebase or synthetic-but-hard tasks"

## Phase 102 — Pillar 2: contract-driven downstream-worker loop
- date: 2026-06-22
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "FEEDBACK RICHNESS is the experimental variable: the loop feeds back the failing guardrail's assertion error + counterexample (NOT pass/fail), because single-shot WITH the invariant named already floored — the worker needs to know WHY/where it broke. ACCEPT. Shapes: the loop's power to recover the decision-lag floor lives here; if rich feedback STILL doesn't recover it, the finding is 'the floor is implementation-difficulty, not feedback-starvation' (informative either way). If false (feedback too noisy / unactionable), the loop arms collapse toward the single-shot floor"
- A2 | cost: high | position: accept | revisit-status: held | "The worker iterates against the contract's VISIBLE guardrails; a SEPARATE held-out integrity test validates the converged impl (gaming = loop-pass but held-out-fail). ACCEPT. Shapes: makes the loop meaningful (something to iterate against) AND detects iterate-to-green gaming (the Ph101 ceiling confound, now by design) — the visible/held-out split is the instrument's integrity. If false (no clean split for a task), that task drops to floor-recovery only, no gaming measurement"
- A3 | cost: medium | position: accept | revisit-status: held | "Arms = single-shot (Ph101 baseline) / contract-loop / bare-loop (loop on functional tests, no named invariant). ACCEPT. Shapes: single-shot isolates the LOOP's value, bare-loop isolates the CONTRACT-invariant's value within the loop; corpus focuses on decision-lag (the floor) + >=1 in-band task; measures floor-recovery rate, iterations-to-converge, gaming rate. If false (bare-loop redundant), drop it; cost is the main risk"
- A4 | cost: medium | position: accept | revisit-status: held | "Worker substrate = opencode loop (opencode run --continue/--session, accumulating context — the proven Ph100/101 worker) over the earlier reuse-Agent/Workflow lean (opencode is now proven). ACCEPT. Shapes: reuses the edge-screener harness; capped iterations (~5); byte-frozen + controls-first. If false (--continue doesn't iterate usefully / context bloats), fall back to fresh-runs-with-accumulated-feedback or a Claude Agent loop"

## Phase 103 — Gaming-rate vs visible-gate-strength
- date: 2026-06-22
- all_accept: false
- A1 | cost: high | position: don't-know | revisit-status: bit | "ELICITABILITY (the T0 weakest): a deliberately weakened gate WILL elicit >=1 silent-wrong run from the real opencode/big-pickle worker. DON'T-KNOW — maintainer correctly declines to pre-judge an empirical outcome (this is what the phase measures). DEFERRED, not a silent pass: resolved by the T2 make-or-break calibration pilot BEFORE the freeze; the plan does NOT bank on it — if false (~0 gaming at every gate strength) the pre-registered INFORMATIVE NULL stands (worker competence, not the gate, carries truthfulness; delegation robust to gate looseness, rhyming with the amplifier-nulls Ph59/80) and the maintainer adjudicates the null's acceptability at the DELIVERY gate. Revisit at debrief"
- A2 | cost: high | position: don't-know | revisit-status: held | "REALISTIC WEAK GATE: a weak-but-plausible gate (a human pinning fewer example loci, not an obvious strawman) is constructible along the lag-coverage axis. DON'T-KNOW. DEFERRED: realism judged concretely when the actual rungs are built in T1 (a one-example-locus gate is a plausible under-specified contract); the plan does NOT bank on it — if false the verdict NARROWS the claim to 'the constructed weak gate' + flags the realism limit regardless. Revisit at debrief"
- A3 | cost: medium | position: don't-know | revisit-status: held | "SILENT-WRONG SEPARABLE: silent-wrong vs honest-flagged-wrong is mechanically pinnable from the transcript/converged code (a marker on the visibly-UNCONSTRAINED locus in the worker's own vocabulary, NOT held-out names which the leak guard guarantees absent). DON'T-KNOW. DEFERRED: the T2 pilot tests separability on real transcripts; the plan does NOT bank on it — if false the headline degrades gracefully to the raw under-spec rate (visible-pass AND held-out-fail), taxonomy descriptive-only. Revisit at debrief"

## Phase 104 — Emerging-Agent-Tooling Landscape Survey
- date: 2026-06-22
- all_accept: false
- A1 | cost: high | position: don't-know | revisit-status: held | "The survey surfaces GENUINELY NOVEL signal beyond parametric recall (the post-cutoff sweet spot Ph59 left untested; cutoff Jan-2026, today Jun-2026) — DON'T-KNOW, converted to a CONTROL not a bet: a blind baseline (the map I'd write from parametric memory, committed BEFORE any search) makes the phase's value the measured DELTA; a ~0 delta is the pre-registered INFORMATIVE NULL ('the trend map is recoverable from parametric knowledge' — Ph59-consistent), adjudicated at the delivery gate. The blind baseline doubles as the anti-retrofit seal (priors fixed before search → can't relabel known repos as freshly 'discovered'). RESOLVED at debrief → TRUE: the value-delta is LARGE (skills/plugins economy, openclaw ~380k runtime, agent-security scanners, code-KG-MCP — all genuinely-new AND ground-truth-verified ~65/65); first empirical demonstration of Ph59's post-cutoff retrieval exception. Held (favorable). Near-miss recorded: real 2026 star magnitudes were nearly discarded as hallucinated, caught by orchestrator ground-truth verification ([[HEU-012]])"
- A2 | cost: high | position: accept | revisit-status: held | "LANDSCAPE-AWARENESS is the right commitment level — the deliverable is an annotated trend map (clusters, momentum, appearing/fading), NOT an adopt-ranked list (adopt-scout) and NOT a positioning verdict (the Ph97 angle the maintainer is reacting against). Maintainer CHOSE this lens over adopt-scout / threat-refresh. If false (he actually wants 'what to take'), the output re-ranks by kit-fit in a follow-on adopt-scout phase"
- A3 | cost: medium | position: accept | revisit-status: held | "ONE-SHOT first, not a standing watch — do the discovery once, see the map's value, then decide separately whether a recurring trend-scan is worth standing up (Ph98 already over-built a mis-aimed watch). Maintainer ACCEPT. If false (the map is only useful kept-fresh), a recurring scan is a gated follow-on, costing nothing toward it now"
- A4 | cost: high | position: accept | revisit-status: held | "Map unit = BOTH LAYERS (pattern/capability clusters as the spine + a repo-evidence table appendix) — maintainer REVISED my pattern-clusters-only rec to BOTH. Clusters give durable orientation; the repo-evidence appendix keeps every cluster claim cited (source + date + traction) so the map is auditable, not vibes. If false (the appendix buries the orientation signal), the clusters stay primary and the table demotes to a reference"
- A5 | cost: medium | position: accept | revisit-status: held | "LIGHTWEIGHT gitignored method-note serves as the spec (Ph97 precedent — the apparatus is private), NO byte-freeze — there is no scored pass/fail verdict to retrofit, so the heavy Ph97/98 freeze earns nothing; the blind baseline (A1) IS the anti-retrofit discipline that matters here. Controls = blind-baseline delta + a recency positive-control (a known-established repo MUST NOT classify as 'emerging', or the trending filter is dead — [[HEU-012]]). If false (a verdict-shaped claim creeps in), the method-note gets byte-frozen before the synthesis"

## Phase 105 — Code-Retrieval Adopt-Scout (code-KG / tool-reach)
- date: 2026-06-23
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "Scope = adopt-scout aimed at Category 2 (code-KG / codebase-retrieval as MCP); maintainer chose it OVER my Category-1 (skill-file scanner) rec (an override, but he accepted the Cat-2 direction). Shapes: the whole investigation targets code-structure retrieval. RESOLVED — the grounding sweep + pre-mortem showed Cat-2 is informationally DOMINATED by in-context AST + the mypy already installed in the substrate; my 'already-covered' prior was vindicated by evidence, not assertion"
- A2 | cost: high | position: accept | revisit-status: held | "The honest adopt-scout deliverable is a MEASUREMENT SCREEN, not a desk-eval or a direct adopt-spike — both adversarial steelmen (ADOPT/NOT-ADOPT) converged on 'fund a screen, not the adoption'; NULL-is-valid pre-accepted (phase value = the measured delta). Shapes: precludes plausibility-only adoption. The screen itself was then found non-viable (A4)"
- A3 | cost: medium | position: accept | revisit-status: held | "Substrate = aml-substrate; maintainer chose it OVER my edge-screener rec (accepted the substrate). Shapes: a real AML-domain consumer. VERIFIED (HEU-012, orchestrator-run): 26K first-party LOC (not the 1.79M incl .venv), mypy/dmypy installed, _ARCHITECTURE.md leaks dependency rows — the facts that made the code-KG screen non-viable"
- A4 | cost: high | position: reject | revisit-status: bit | "REJECTED assumption: the code-KG-vs-grep screen is a viable instrument. The pre-mortem showed it is RIGGED — the pre-built index is an answer-cache for the scored transitive-closure relations (PASS baked in); the positive control is NON-CONSTRUCTIBLE (a 0.13s stdlib-ast BFS recovers callers); the syntactic KG is dominated by installed mypy. Maintainer REFRAMED to 'are agents under-using already-present type-aware tooling?' (tool-reach) over bank-null / redesign / large-substrate. Shapes: moved the object from external-infra adoption to a harness affordance"
- A5 | cost: high | position: reject | revisit-status: bit | "REJECTED assumption (→ BANK THE NULL): the tool-reach reframe yields a genuine REACH-GAP. The harden + make-or-break corpus probe showed otherwise — the FAIR Rung-1 existence-only test predicts DEGENERATE (naming the tool over-coaches = Ph78/Ph80 leak); the dispatch-hard corpus is only PARTIAL (~1-2 cross-module cases; registry dispatch both mypy-unresolvable AND prose-leaked); this is the amplifier-null hypothesis class terminated 4x here (Ph59/70/77/78). Terminal: maintainer banked the null 2026-06-23. [[decision:code-retrieval-adopt-scout]]"

## Phase 106 — Project-State Dashboard + Act-from-Page Decision Gate
- date: 2026-06-23
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "The act-from-page round-trip (ephemeral server + served form + background-watch + ingest) actually REMOVES decision-moment friction rather than SWAPPING click-chips-in-terminal for spin-up-server/switch-tab/fill-form/poll-file — the weakest assumption (presentation/friction value is UNMEASURABLE in-kit, Ph59/80 carve-out; ships on the maintainer's stated need not evidence; Ph99 ranked this BELOW the direction-render with no new evidence the ordering flipped). Accept ONLY because cheaply reversible: AskUserQuestion stays the default AND fail-open fallback, opt-in via a .dev-wiki/act-from-page marker (absence = today's path, server never spawns), retire = rm marker with zero gate-semantics change. Shapes: the whole feature is marker-gated + fail-open so a thumbs-down after one real use costs nothing to retire"
- A2 | cost: medium | position: accept | revisit-status: held | "A thin STATUS digest + the DIRECTION pane (Architecture/Decisions panes CUT) is the right monitoring floor — the friction is the decision-MOMENT (Ph99: in-session text streams too fast to steer), not re-reading _ARCHITECTURE.md / 200+ decision articles at one's own pace. Cutting honors the subtraction test (those panes are the highest-LOC, lowest-evidence, fully-unmeasurable additions). Shapes: drops ~2 panes + a YAML/inline-list parser + sortable grid + freshness badges → keeps the generator thin and the phase to one L task; re-add only on reported real friction"
- A3 | cost: medium | position: accept | revisit-status: held | "ONE deterministic validate-decision-response.py, RUN by BOTH the server POST path and the dev-plan ingest (the orchestrator runs it, never eyeballs nonce/coverage/position), is the correct boundary — an LLM-prose ingest would be the neural-judge-at-boundary the kit rejects and would silently drift from the server schema. Shapes: T1 is the FIRST task (the contract everything depends on); the server imports it, the companion instructs the orchestrator to RUN it, all roundtrip controls invoke it — one artifact, real diff"
- A4 | cost: high | position: accept | revisit-status: held | "The orchestrator-side wait is a run_in_background watcher / Monitor that re-wakes + self-releases on a deadline/sentinel, NEVER a foreground sleep (BLOCKED in this env — a foreground until-sleep loop would self-brick every future gate, the Ph82 self-lock class); the SERVER owns the single timeout (writes a {status:timeout} sentinel + exits), eliminating the dual-deadline-coupling hang the kit's own working-knowledge warns against. Shapes: the integration waits on ONE file condition and branches on contents (valid decision vs timeout sentinel); the orchestrator deadline is a pure backstop strictly > the server watchdog"
- A5 | cost: medium | position: accept | revisit-status: held | "Structural staleness defense (gitignore the response file + unconditional pre-launch rm + 128-bit nonce + watcher predicate requiring existence AND fresh-nonce + consume-once + nonce OPTIONAL only at render/ingest legacy-skip) prevents a stale/foreign/replayed decision from driving the wrong phase's gate. Original design's defense was procedural prose + the file un-gitignored (a fresh clone or leftover wakes the watcher on iteration 1) — making it structural is belt-and-suspenders, all low-cost, all tested (R4). Shapes: adds the .gitignore edit, the fresh-nonce predicate, consume-once rename, and the legacy-nonce-skip so existing nonce-less briefs render + fall open cleanly"
- A6 | cost: high | position: accept | revisit-status: held | "Localhost-CSRF defense via Origin==own-origin + Host allowlist (127.0.0.1/localhost:port) + Content-Length cap + reject-chunked + single-accept latch is sufficient for a loopback single-user write-endpoint open ~15-30 min — no other browser page can forge a same-Origin decision; nonce entropy alone is grindable given unlimited bad POSTs, but header checks are free, entropy-independent, and standard localhost mitigation. Shapes: adds the Origin/Host/Content-Length/chunked-reject guards + the single-accept latch to do_POST, each with an S4-tier reject control"

## Phase 107 — Decisioning Cockpit (dashboard-as-primary gate)
- date: 2026-06-23
- all_accept: true
- A1 | cost: high | position: accept | revisit-status: held | "WEAKEST (rank 1): a well-designed cockpit (tabbed Status|Decide|Workflow, per-option REASONING+CONSEQUENCES laid out for comparison, a loud stale-brief guard) actually REMOVES the maintainer's decision friction rather than swapping one friction for another. Maintainer DIRECTED this (found the Ph106 dashboard 'not usable at all'). UNMEASURABLE in-kit (Ph59/80 carve-out) → accepted on STATED NEED, ships on his judgment at the delivery gate; CHEAPLY REVERSIBLE (the static page + AskUserQuestion fallback survive any revert; revert the render, keep the schema + stale-guard). Shapes: tests assert MECHANICS only (option-scoped reasoning/consequences container, stale-banner-no-form), the aesthetic is judged at delivery. If false (a prettier surface still isn't usable): the deeper gap was elsewhere — revert render, keep schema/guard"
- A2 | cost: high | position: accept | revisit-status: held | "DASHBOARD-AS-PRIMARY (maintainer's fork choice over on-demand+freshness-fix): the ephemeral decision-server serves the LIVE decision at every /dev-plan gate by DEFAULT; AskUserQuestion becomes the fail-open FALLBACK — a FLIP of Ph106's AskUserQuestion-default/opt-in. ACCEPTED, gated on the HARD constraint: dashboard-as-default MUST fail open to AskUserQuestion on EVERY failure branch (spawn fail, render/import error, validator error, timeout sentinel, headless/no-browser). The default flips; the fallback never disappears + 'is the fallback reachable' is a TEST not a claim (HEU-012/Ph82 don't-self-brick-the-gate). If false (a branch can't fall open): that branch is fixed before ship; the flip does not land otherwise"
- A3 | cost: high | position: accept | revisit-status: held | "NATIVE WORKFLOW RE-RENDER (maintainer's fork choice over iframe): refactor generate-workflow.py to emit content FRAGMENTS the cockpit restyles into the shared visual system; its standalone main()→docs/workflow.html keeps building. Assumes a surgical seam leaves the standalone output stable. SHIP-BLOCKING fallback: iframe was LOCKED OUT at the direction gate, so if the seam forces a deep restyle that risks the standalone output → STOP + ESCALATE to the maintainer, never ship iframe autonomously. If false: escalate (do not silently downgrade to the rejected alternative)"
- A4 | cost: medium | position: accept | revisit-status: held | "Per-option reasoning + consequences are HAND-AUTHORED at the gate (the orchestrator + the dev-plan direction-brief companion) — there is no honest auto-derivation source. Shapes: the brief schema gains optional reasoning/consequences; the dev-plan companion authors them when emitting the gate brief. If false (maintainer expected auto-derivation): surface that no honest auto-source exists and keep authored content"
- A5 | cost: medium | position: accept | revisit-status: held | "The existing 128-bit nonce + a PHASE-MATCH stale guard prevent wrong-phase ingest (the Ph106 silent-Phase-103 root cause): brief carries phase-id + nonce; the deterministic validator (run by BOTH server-accept AND planner-ingest, never eyeballed) rejects a response whose phase/nonce ≠ the live gate; the served page renders a loud banner + NO submittable form on a phase-mismatch/malformed/empty brief; consume-once + pre-launch rm + fresh-nonce watcher predicate. If false: tighten the validator (the single schema authority), not a parallel check"
- A6 | cost: medium | position: accept | revisit-status: held | "Dashboard-primary is an INTERACTIVE-SESSION feature; headless/autonomous runs keep the existing path (no human at a served page to block on). The companion keys cockpit-vs-existing on the same non-interactive signal the orchestrator already uses (best-effort prose discipline). HARD BACKSTOP: if a headless run spawns the server anyway, the server-owned timeout writes the sentinel + the gate falls open (A2) → a bounded wait, never a hang. If false: detect non-interactive up front + skip the server"

## Phase 108 — GUI Dev-Harness v1 Thin Slice
- date: 2026-06-26
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "WEAKEST (T0 rank 1): Pi's tool_call extension hook is an un-bypassable in-process gate that can deny ({block:true}) / modify (event.input) every tool call incl. built-ins (read/write/edit/bash) BEFORE side effects — verified as architectural INFERENCE (the hook exists + a real pi-permission-system extension uses it), NOT a Pi security guarantee. ACCEPT: Pi-primary committed; un-bypassability is engine-spike checkpoint #1 (deny a bash rm → no side effect; a same-name-tool shadow AND an unregistered tool cannot suppress the host gate). If false → STOP-and-escalate: swap primary to the Claude Agent SDK (vendor-guaranteed canUseTool) or build-the-loop on Vercel AI SDK, behind the same app-owned engine-adapter interface."
- A2 | cost: high | position: accept | revisit-status: held | "The existing Python MCP memory server runs UNCHANGED under the new app via MCP stdio (ports as-is, the 'spine') — a mount, not a rewrite. ACCEPT: a write→restart→read round-trip is a v1 exit criterion that proves it early. If false → the defect is at the host's MCP-client mount, not the server; scope a mount-fix sub-task, do NOT rewrite the server."
- A3 | cost: high | position: don't-know | revisit-status: held | "keyring-rs (Rust crate) gives OS-keychain access under Tauri on macOS with the keys unreachable by the agent's file/shell tools. DON'T-KNOW — maintainer cannot pre-evaluate the Tauri-integration + agent-deny specifics without trying it. DEFERRED (not a silent pass): T2 is an EARLY keyring-rs spike BEFORE the key-custody rail is built on it; routed to phase Blockers; revisit-status: open (must-revisit at debrief). Partial defense: keyring-rs is a mature crate wrapping the macOS Keychain. If false (spike fails) → key custody moves to a separate non-agent-reachable credential helper, NEVER plaintext (the spec's stated fallback)."
- A4 | cost: medium | position: accept | revisit-status: held | "Pi's MIT core (agent loop + tool_call hook = the gate primitive) stays available under MIT despite Earendil's VC-backed open-core roadmap (RFC-0015 reserves future Fair Source/proprietary layers + a hosted platform). ACCEPT: mitigation = pin a Pi version + the engine-neutral app-owned adapter interface (Claude SDK / Vercel AI SDK) is the escape hatch if the core ever closes. If false → pin-and-fork the last MIT version, or switch the primary adapter."

## Phase 109 — Felt-Quality Surface + Branding
- date: 2026-06-26
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "WEAKEST (T0 rank 1): assistant-ui's custom runtime binds cleanly to the existing engine-neutral event reduction (applyEngineEvent/reduceEngineEvents) WITHOUT reshaping Ph108's tested engine types. ACCEPT: front-loaded as integration spike T1, before any view is built on it. The spec notes assistant-ui rides the Vercel AI-SDK data-stream → the clean path may be feeding it AI-SDK-shaped events. If false (the binding forces reshaping the tested engine layer) → STOP, fall to AI-SDK-data-stream-shape binding; NEVER reshape the proven engine-neutral types. RESOLVED HELD (T1 spike, 2026-06-26): useExternalStoreRuntime + a custom convertMessage feeds our own message model with ZERO engine-type reshape (EngineEvent/NormalizedToolCall/SurfaceMessage untouched); no fallback needed. The #1 integration detail surfaced: applyEngineEvent mutates, so the streaming hook MUST clone-on-event or React won't re-render."
- A2 | cost: high | position: reject | revisit-status: held | "REJECTED my context-blind-v1 recommendation. Maintainer DIRECTED minimum context-assembly INTO Phase 109 (AskUserQuestion 2026-06-26): the app assembles the active workspace's AGENTS.md/CLAUDE.md + .claude/rules/*.md into the engine's per-turn system context (the porting-matrix 'context loader → rebuilt as app code', NOT in Ph108 — the app had MCP memory but no project-file auto-load), so the first dogfood isn't project-blind. Shapes: adds T2 (app/src/context/**); the 'drivable' bar now includes project-instruction auto-load; loud-on-missing (never silently context-blank). Scope-guard-compliant (used every turn) + sanctioned by the spec's porting matrix. Resolved by revision — not re-surfaced (the directive was unambiguous)."
- A3 | cost: medium | position: accept | revisit-status: held | "Felt-quality exit criteria are MECHANICS-only (diff renders pre/post bytes, gate-hold surfaces a blocking dialog, streaming renders incrementally, revert→pre-edit bytes, inert-render preserved); joy/control ships on the maintainer's judgment at the delivery gate (Ph59/80 carve-out; the joy/control north star is his to judge directly — no honest in-kit felt measure exists). If false (maintainer wants a measurable felt bar) → surface that no in-kit measure exists, keep it judgment-based."
- A4 | cost: medium | position: accept | revisit-status: held | "The architecture spec (specs/gui-harness-architecture.md Rev 2, nana:approved) is Phase 109's UMBRELLA spec (Ph108 precedent) — its Surface section + Success Vision (5 axes) + Exit criterion #1 govern this slice; no separate Phase-109 spec generated. If false (maintainer wants a dedicated phase spec) → generate one via /spec --internal."
- A5 | cost: medium | position: accept | revisit-status: held | "Minimum-first depth (maintainer accepted): axes 4/1/5/2 in v1 (streaming chat, gate-confirm+diff, artifact preview, one-action revert); DEFER the command palette + rich keyboard-shortcut layer (axis 3) until dogfood proves weekly use (the spec's own scope guard). If false (axis 3 is load-bearing for daily use) → add a minimum palette in a follow-on."

## Phase 110 — Dogfood UX Pass (Tool-Call Visibility + Artifact Fidelity + Branding)
- date: 2026-06-27
- all_accept: false
- A1 | cost: high | position: accept | revisit-status: held | "WEAKEST (T0 rank 1): Pi POPULATES real tool args + output on a channel the adapter can read — type defs show ToolExecutionStartEvent.args / ToolExecutionEndEvent.result / tool_result.content+details (types.d.ts:549-570), but the LIVE local-model path delivering them is UNVERIFIED (Ph109 dogfood proved tool calls + the gate fire live, NOT that these payloads populate). ACCEPT: T1 is a FRONT-LOADED spike that confirms the real source before T2-T8 build on it; fallback = capture the rich result via Pi's tool_result EXTENSION hook (definitely fires, vs the under-documented subscribe stream). If false (no channel delivers it) → STOP-and-escalate; the headline fix can't render empty fields. revisit-status: open (confirm at debrief the spike held). Shapes: T1 front-loads the source-of-truth spike + a pure mapPiEvent seam."
- A2 | cost: high | position: accept | revisit-status: held | "Threading real tool output to the UI adds untrusted content (bash stdout, file bytes) to the renderer — the GUI XSS/exfil surface the terminal never had. ACCEPT as a HARD constraint: every new field routes through redactSecrets() and stays on the inert React-string path (never raw HTML); the inert-render + key-store tests stay green AND gain assertions for the new args/output fields; output is size-capped at the boundary. T3 is the security-bearing task. If false (a field bypasses redact/inert) → that path is fixed before the field ships. Shapes: T3 extends the inert-render rail to the enriched fields + adds the redact+truncate seam."
- A3 | cost: high | position: accept | revisit-status: held | "The typed artifacts PANEL (edit→diff, test→results) needs Pi's typed `details` to populate on the LOCAL-model path — genuinely unverified (same spike as A1). ACCEPT: T1's spike checks details-on-local; if absent, the panel DEGRADES to a generic terminal/text artifact for ALL tools (still real output, just untyped) rather than deferring the whole panel. Maintainer chose the fuller scope (panel in Ph110) over inline-only. If false (details never populate locally) → ship the generic-text panel, defer typed diff/test views to 111. revisit-status: open (confirm details-on-local at debrief). Shapes: T5 router has a details-absent fallback branch; the panel ships either way."
- A4 | cost: medium | position: accept | revisit-status: held | "ROUTING LOCATION (maintainer don't-know → DEFENDED to accept, not a silent pass): typed tool→view routing (bash→terminal, edit→diff, test→results) lives in the UI layer keyed on tool NAME; the engine-neutral EngineEvent/SurfaceToolCall carries args/output/details OPAQUELY and stays minimal. Defense: routing-in-adapter would push UI presentation vocabulary (view kinds) BACKWARDS across the Ph108 invariant ([[engine-adapter-in-process-gate]] — no engine-specific types leak the boundary) AND duplicate the mapping across BOTH the Pi and Vercel adapters (state-loader coupling warning). Low-blast + reversible (a small UI module). If maintainer prefers to defer → route A4 to Blockers. Shapes: T5 is a pure UI-side mapToArtifact(toolName, result, details); the engine layer stays additive-only."
