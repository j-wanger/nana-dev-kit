# Current State: nana-dev-kit

> Last updated: 2026-06-30 by /dev-debrief (Phase 115 — Conversation memory (persist + restore the chat thread): DELIVERED — 5/5 tasks [x]; app suite 343→374 green + npm run build + cargo check exit 0; a 2-finder adversarial pre-commit review (orchestrator-verified) caught+fixed 6 confirmed findings incl. 1 HIGH (saveConversation byte-backstop could prune a non-empty thread to [] then overwrite good history = silent total loss); redact-at-the-PERSISTENCE-boundary (the in-memory store holds RAW tool args) + per-workspace localStorage keyed by ready.workspaceRoot, display-only restore/swap (zero engine sends) — which also fixes the Ph114 stale cross-workspace leak; decision [[conversation-memory-persistence]] high; ledger Phase-115 A1/A2/A3/A4 held, A5 BIT). Prior: 2026-06-30 by /dev-debrief (Phase 114 — Pi as the default daily engine (good tools) + workspace picker: DELIVERED — 6/6 tasks [x]; app suite 343/343 + npm run build + cargo check exit 0; a 3-finder adversarial pre-commit review caught+fixed 6 confirmed findings incl. 1 HIGH (ancestor-blind secret-deny on the now-active recursive grep/find/ls); Rust-atomic workspace picker landed TIGHTER than planned (the webview gets NO dialog capability at all); decision [[pi-default-engine]] high; ledger Phase-114 A1/A3/A4 held, A2 BIT). Prior: 2026-06-30 by /dev-plan (Phase 114 — Pi as the default daily engine (good tools) + workspace picker: PLANNED, 6 tasks (one L spike T1, then S/M/M/S/M), 1 decision [[pi-default-engine]] high — the dogfood "the tools are awful" complaint is an ACCIDENTAL-DEFAULT bug: the agent runs on the Vercel adapter (2 hand-rolled tools, uncapped bash, whole-file write) but Vercel is the default purely by a Ph109 bring-up artifact (main.ts:20 NANA_ENGINE ?? 'vercel'); the spec + all 3 Ph108 decisions name Pi PRIMARY and Pi already ships the rich paginated/capped suite (read/grep/find/ls/surgical-edit/spilling-bash) that the host gate already covers (Ph112 T1/A4). Make Pi the default + harden (thread maxTokens past 2048 → NANA_MAX_TOKENS ~8192; activate dormant grep/find/ls via the tools allowlist, fallback customTools; PROVE the live round-trip) + a Rust-atomic workspace picker (native dialog → re-spawn the sidecar with the chosen NANA_WORKSPACE; webview never supplies the root; dialog-only capability). T1 FRONT-LOADS a live Pi drive with a hard STOP-and-escalate (A1, the key risk — the live round-trip was never GUI-verified). Vercel stays the NANA_ENGINE=vercel fallback; #2 conversation memory → Ph115. Ledger Phase-114 all_accept:true (A1-A4). Rides umbrella spec specs/gui-harness-architecture.md). Prior: 2026-06-27 by /dev-plan (Phase 113 — Felt-Quality Build — Axis 3: Command Palette + Keyboard Control: PLANNED, 6 tasks (all M, zero L), 1 decision [[axis3-command-palette]] high — build axis 3 of the five north-star control/joy axes ("every action reachable by button, shortcut, or palette search", the LONE unbuilt axis; 1/2/4/5 shipped Ph109-111): a small PURE command registry + a custom hand-rolled Cmd+K command palette + a keyboard shortcut layer dispatching EXISTING actions (stop/approve/deny/revert/focus-composer) + the one missing action (new-conversation); NO cmdk dep (owned not adopted); mechanics-only tests, felt-quality on maintainer judgment; NO new privileged host path — a Ph112-style no-bypass invariant guards the engine-host HostInbound union (unchanged or +1 benign reset); gate verdict-loop core + inert-render + strict CSP UNCHANGED. Ledger Phase-113 all_accept:true (A1-A4). Rides umbrella spec specs/gui-harness-architecture.md). Prior: 2026-06-27 by /dev-debrief (Phase 112 — OS-sandbox bash filesystem isolation: BUILT + adversarially reviewed, 6/6 tasks [x]; a per-command macOS seatbelt (`/usr/bin/sandbox-exec`) enforcement layer BELOW the Ph111 string-gate at the bash execution site for BOTH adapters via ONE chokepoint (NEW `app/src/gate/sandbox/{seatbelt,approved-writes}.ts`) — closes the Ph111 string-gating residual (`python3 -c`/`node -e`/`base64`/env indirection evade). T1 spike proved A1 inheritance / A4 Pi-seam-binds + gate-sees-original / A5 strict-viable HELD; A2 BIT-partially (caches → integrity allowlist). CORRECTIONS: the Pi seam is `customTools`+`spawnHook` NOT `baseToolsOverride` (the gate sees the ORIGINAL command); strict drops unix-socket (loopback-IP only); `sandbox-exec` absolute path; `canonicalizePath` basename+reject-`/`. C1-preserve via per-command `extraWrites`; the gate verdict-loop core + `types.ts` UNCHANGED. An adversarial PoC review caught 2 real whole-FS-grant bugs 264 tests missed + 3 hardenings → fixed +10 tests. app suite 201→274 green incl. live e2e; tsc + `npm run build` + `cargo check` exit 0. READY FOR COMPLETION, delivery=pending — gate flips post-commit. Decision [[os-sandbox-bash-fs-isolation]] high (the named follow-on of [[host-gate-out-of-workspace-hardening]]). Prior: 2026-06-27 by /dev-plan (Phase 112 — OS-sandbox bash filesystem isolation: PLANNED, 6 tasks (L/M/S/M/M/M — one L spike), 1 decision [[os-sandbox-bash-fs-isolation]] high — the named follow-on of [[host-gate-out-of-workspace-hardening]] closing the documented Ph111 residual (string-gating arbitrary bash is incomplete by nature: python3 -c / node -e / base64 / env indirection evade). Per-command OS-sandbox (macOS seatbelt via sandbox-exec) BELOW the string-gate, at the bash execution site for BOTH adapters through ONE chokepoint (new pure app/src/gate/sandbox/seatbelt.ts); Pi v0.80.2 exports baseToolsOverride/BashSpawnHook/BashOperations → substitute sandbox-wrapped bash WITHOUT forking Pi; Vercel's execSync wrapped directly; NO types.ts change; gate verdict-loop core UNCHANGED. T1 = the FRONT-LOADED de-risking spike (A1 inheritance / A4 Pi-seam-binds + original-command-visible / A5 strict-viability; STOP-escalate → integrity-only if it fails). A3 C1-PRESERVE (approved out-of-ws write threads into extraWrites), A5 STRICT. Ledger Phase-112 all_accept:false (A3/A5 rejects resolved by revision). Prior: 2026-06-27 by /dev-debrief (Phase 111 — Typed Artifact Fidelity + host-gate out-of-workspace hardening: BUILT + adversarially reviewed, 5/5 tasks [x] + 3 review fixes; A1 RESOLVED HELD (typed details.diff on the LOCAL subscribe path; the header-less +/-/space diff format makes the typed path strictly beat Ph110's looksLikeDiff heuristic); a SECURITY scope-amendment folded in [[host-gate-out-of-workspace-hardening]] high — bash + alt-named write coverage, gate verdict-loop core UNCHANGED, honest string-gating residual → OS-sandbox Phase 112; app suite 157→201 green incl. all live e2e, tsc + npm run build + cargo check exit 0; READY FOR COMPLETION, delivery=pending — gate flips post-commit). Prior: 2026-06-27 by /dev-plan (Phase 111 — Typed Artifact Fidelity (typed-details structured diff): PLANNED, 5 tasks, 1 decision [[typed-artifact-fidelity]] high — the de-risked Ph110 follow-on; thread Pi's typed details additively through the engine-neutral layers so the artifact panel renders a STRUCTURED canonical diff instead of Ph110's looksLikeDiff shape-heuristic; test panel DESCOPED; scope = typed-diff thread + pin the flaky provider-roundtrip e2e). Prior: 2026-06-27 by /dev-plan (Phase 110 — Dogfood UX Pass (Tool-Call Visibility + Artifact Fidelity + Branding): PLANNED, 8 tasks, 1 decision [[tool-call-visibility-thread]] high — a single vertical thread source→surface that connects real Pi tool args/output/typed details to the Ph109 UI components already built but starved of data; closes the #1 dogfood gap (allowed tool calls render NAME-ONLY)). Prior: 2026-06-26 by /dev-debrief (Phase 109 — Felt-Quality Surface + Branding: BUILT + DELIVERY ACCEPTED — 6/6 tasks [x] (T6 webview↔engine bridge discovered mid-phase, maintainer-approved); app tests 63→108 green, tsc+vite+cargo check all exit 0; READY FOR COMPLETION, delivery gate flips after the commit lands. The drivable assistant-ui surface that makes Ph108's rails felt (axes 4/1/5/2) + the minimum context-assembly seam + the Rust-spawned Node-sidecar bridge that makes the harness genuinely drivable. A1 spike resolved CLEAN (zero engine reshape). Decisions [[felt-quality-surface]] + [[webview-engine-bridge]] + [[gate-confirm-approve-loop]] all high). Prior: 2026-06-26 by /dev-plan (Phase 109 — Felt-Quality Surface + Branding: PLANNED, 5 tasks, decision [[felt-quality-surface]] high; build the drivable assistant-ui + AI Elements surface that makes Ph108's rails felt + minimum context-assembly seam). Prior: 2026-06-26 by /dev-debrief (Phase 108 — GUI Dev-Harness v1 Thin Slice: BUILT, 8/8 tasks [x], 14/14 machine-checkable exit criteria proven, delivery gate PENDING; THE PIVOT — re-platform nana-dev-kit into a GUI-primary, model-agnostic desktop dev harness per the approved spec specs/gui-harness-architecture.md; embed a model-agnostic agent engine IN-PROCESS behind an app-owned EngineAdapter interface, the security gate at the in-process tool-call site (Pi primary / Claude SDK 2nd / Vercel fallback); decision [[engine-adapter-in-process-gate]] high). Prior: 2026-06-23 by /dev-plan (Phase 106 — Project-State Dashboard + Act-from-Page Decision Gate: PLANNED, 7 tasks; extend Ph99's direction dashboard into an on-demand project-state monitor + an opt-in act-from-page ephemeral-server decision gate; decision [[project-state-dashboard]] high). Prior: 2026-06-23 by /dev-plan (Phase 105 — Code-Retrieval Adopt-Scout (code-KG / tool-reach): PLANNING-STAGE INFORMATIVE NULL — do NOT adopt; resolved via 3 controls-first adversarial workflows BEFORE any measurement run; SHIPS NOTHING; decision [[code-retrieval-adopt-scout]] high). Prior: 2026-06-22 by /dev-plan+implement (Phase 102 — Pillar 2: contract-driven downstream-worker LOOP DELIVERED 5/5; VERDICT: a contract-governed LOOP RECOVERS the Ph101 implementation-difficulty floor VIA FEEDBACK [decision-lag bare-loop 3/3 + contract-loop 3/3 vs single-shot 0/15 + best-of-N 0/3 → feedback=+1.0 SUPPORTED; invariant-naming nearly inert, only sped convergence ~1 iter; gaming 0/3 truthful]; pit "gaming 3/3" was a frozen-held-out artifact [orthogonal before-baseline case; post-hoc invariant-focused = 6/6 recovery], reported NOT retrofitted; gaming detectable-in-principle but not exhibited by opencode/big-pickle; an adversarial-refutation pass found+closed a held-out leak+under-pinning pre-freeze, and an apparatus cleanup() rmtree bug run concurrently with the runner deleted+restored edge-screener [intact, src checksum 931c0caef3742029 identical, .git restored, bug fixed, all work serialized]; frozen sha-intact, ships NOTHING make test PASS eval 50/50 drift 0; apparatus gitignored companion/research/contract-loop/). Prior: 2026-06-22 by /dev-plan+implement (Phase 101 — Harder-corpus contract-vs-spec fidelity screen [edge-screener] DELIVERED 5/5; VERDICT: corpus DISCRIMINATED [Ph100 ceiling broken — all 3 tasks in-band, bare fails the integrity assertion 3/3]; A1 det-primary DIFFERENTIATED — contract HELPS on pit [det 1.00 vs 0.00 bare/spec], genuine FLOOR on decision-lag [naming the invariant ≠ implementing the subtle pending-lag], leverage = a held-out-test WORDING artifact; A2 neither-scorer-sole-arbiter [det caught the decision-lag bug the judge MISSED → det-primary vindicated; judge caught the leverage wording-artifact det got wrong]; contracts add real fidelity TASK-SPECIFICALLY on hard delegation [refines the Ph100 null, not a blanket "contracts work"]; ships NOTHING, frozen sha-intact, edge-screener src untouched, make test PASS eval 50/50; gate Phase-101 all_accept:false [A1 accept, A2 reject→from-scratch stub, A3/A4 accept]). Prior: 2026-06-22 by /dev-plan+implement (Phase 100 — Contract-vs-Spec Fidelity Screen DELIVERED, 5/5 tasks; VERDICT = AMPLIFIER-NULL → collapse-to-runner-only + deterministic-suffices; 60 opencode runs, det+judge HONORED=1.00 everywhere; contract adds no fidelity over bare → ship check-fidelity.py runner, no contract schema; ceiling-saturated corpus → harder-corpus screen routed forward; make test PASS, eval 50/50, drift 0; apparatus gitignored companion/research/contract-screen/). Prior: 2026-06-22 by /dev-debrief (Phase 99 — Direction Dashboard: DELIVERED + ACCEPTED — 4/4 tasks; work commit 46473a7; render-only direction-gate dashboard, 4th docs/ HTML generator; review gate CLEAN + 1 LOW fixed inline). Prior: 2026-06-21 by /dev-debrief (Phase 98 — Frontier Watch (rung-B): DELIVERED + ACCEPTED — 3/3 tasks; work commit fb82864; phase active→completed, Delivery gate flipped. Stood up the Ph97 rung-B charter as a re-runnable, frozen, controls-validated TWO-SPEED tripwire watch in gitignored companion/research/watch/ — WEEKLY broad-source/narrow-signal fast scan + MONTHLY deep re-derivation backstop + on-hit escalation to the frozen pre-registration.md 600e1c9f. watch-charter.md frozen (SHA256 .frozen 8ec4af7b); run-watch.sh --selftest PASS [both seeded tripwires FIRE, both false-positive controls QUIET, charter-drift guard exit 3]; dated Blockers tripwire as the committed fallback (the /schedule weekly+monthly trigger is OFFERED, maintainer to confirm). A macOS awk `\|`-as-alternation classifier bug was found AND fixed inside the apparatus — caught by the controls-first seeded-positives (the QUIET controls had passed for the WRONG reason). ZERO shipped-kit change (git diff = .dev-wiki/ + active-phase.md + 2 articles); make test PASS [incl. install-update 55/55], drift 0, companion/ untracked.)

## Recommended Next Action

**Phase 116 — YouTube→wiki grounded acquisition core (rung-B foundation): BUILT 2026-06-30, 6/6 tasks [x], READY FOR COMPLETION, delivery gate PENDING. Standard ceremony. Commit the built work — that flips the delivery gate (D3) — then `/dev-plan` Phase 117.** Built the acquisition + grounded-extraction CORE of the Phase-97 rung-B standing research pipeline, specialized to YouTube, as LOCAL gitignored Python apparatus under `companion/research/youtube/` (10 modules + 6 test files + README + requirements + venv) — NOT a shipped kit skill (honors the Ph97 "research apparatus stays local, never shipped" precedent); the continuous half (RSS / scheduler / dedup-watermark / Telegram digest) is deferred to nanaclaw. A manual-trigger acquire → chunk → extract → ground+consolidate loop feeding a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills; extraction-by-selection (verbatim `source_quote` + ≤40-word minimal claim). LOAD-BEARING INVARIANT = the grounding rail (T4 CHECKPOINT, PASSED): every `source_quote` mechanically verified ∈ its chunk before it can enter a concept file; a planted fabricated quote is REJECTED; the calibration probe ABORTS on broken normalization; C2 merge keeps quotes IMMUTABLE + re-verifies the MERGED set. **T6 ran a 2-round adversarial review** (a 5-lens finder workflow, 22 findings / 5 HIGH, then a focused re-attack; every finding orchestrator-verified by running the actual code per [[HEU-012]]) → 5 HIGH + meaningful MED/LOW fixed inline with 16 regression tests (DISCOVERY/SECURITY escape hatch). The re-attack CAUGHT the first punctuation-strip fix re-opening the anti-fabrication invariant to cross-sentence splices → REVERTED to casefold + curly/dash-fold only. **Health:** NEW companion-local pytest suite 0→53 green (hermetic — lazy/injected deps, no network); separate from the shipped kit's `make test` (unchanged); `git check-ignore companion/research/youtube` confirms nothing shippable. Direction gate closed: ledger Phase-116 all_accept:false (A1/A2 accept, A3 reject→Standard; `--gate 116` exit 0). Spec `specs/phase-116-youtube-grounded-acquisition.md` (nana:approved). Decision [[youtube-grounded-acquisition]] (high). **NEXT ACTION:** commit → `/dev-plan` Phase 117 (candidates: the LIVE multi-video first-run validation [deferred manual-drive, residential IP — measures the captioned/dead-letter rate A2 + per-video cost + real-content grounding] / rung-B continuous half in nanaclaw / whisper.cpp fallback if dead-letter rate high / the still-open pre-pivot GUI candidates — renderer-trust hardening, blocking out-of-ws READ, multi-workspace localStorage LRU). Phase 115 (conversation memory) is DELIVERED + committed (a485706).

## Active Phase

**[[phase-116-youtube-grounded-acquisition|Phase 116: YouTube→wiki grounded acquisition core (rung-B foundation)]]** (status: active — PLANNED 2026-06-30, 0/6 tasks). Builds the acquisition + grounded-extraction CORE of the Phase-97 rung-B standing research pipeline, specialized to YouTube, as a manual-trigger Python apparatus under gitignored `companion/research/youtube/` — turning one Pi/agent-setup video into a grounded, provenance-carrying concept set (each concept tied to a verbatim, mechanically-verified transcript quote + timestamp), written ready to feed a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills. NOT a shipped kit skill (the local-apparatus home, per the Ph97 "research apparatus stays local, never shipped" precedent); the continuous half (RSS / scheduler / dedup-watermark / Telegram digest) is deferred to nanaclaw. Loop: acquire (captioned-first; uncaptioned → dead-letter) → chunk (~1000-tok / 15% overlap, segment→time map) → extract (Claude structured, extraction-by-selection: verbatim `source_quote` + ≤40-word claim) → ground + consolidate. LOAD-BEARING INVARIANT = the grounding rail (T4): every `source_quote` mechanically verified ∈ its chunk before it can enter a concept file; a planted fabricated quote is REJECTED; the C1 normalization-symmetry calibration probe ABORTS on broken normalization (else a silent drop reads as "thin video"); C2 merge keeps quotes IMMUTABLE + re-verifies the MERGED set. 8 adversarial-review-caught constraints (C1-C4/E1-E2/S1-S2). Decision [[youtube-grounded-acquisition]] (high); spec `specs/phase-116-youtube-grounded-acquisition.md` (nana:approved — a SEPARATE phase contract, different domain than the GUI umbrella spec); ledger Phase-116 all_accept:false (A1/A2 accept, A3 reject→Standard ceremony).

Entry criteria: MET (the maintainer wants established Pi/agent-setup patterns from YouTube; reframed against the Ph97 rung ladder as the rung-B FOUNDATION increment, built local-gitignored per the Ph97 precedent. Direction gate closed: A1 accept (grounding+selection+wiki-review sufficient, no AML-tier reconciliation), A2 accept (captioned-first, whisper deferred), A3 reject→Standard ceremony; `--gate 116` exit 0.)
Exit criteria: acquire dead-letters an empty-200 + a sub-200-word fixture (above-floor success control passes); chunk produces >1 overlapping ~1000-token window with the segment→time map and no dropped text; extract (mocked) yields a schema-valid concept (verbatim `source_quote` + ≤40-word claim + timestamp), skips a malformed payload, the pre-flight cost ceiling aborts; ground REJECTS a planted fabricated quote + PASSES a genuine one + the calibration probe ABORTS on broken normalization + consolidate dedups overlapping-chunk duplicates into one concept with ≥1 immutable grounded quote + a re-verified merged set; end-to-end on fixtures writes a concept file with frontmatter provenance + a structured immutable `verified` field, neutralizes `[[`/leading-`---`/backticks in quotes, the regression gate refuses a degraded clobber without `--force` (positive write control passes), a full run drops a completion marker; README documents the manual trigger + the provenance-preserving `wiki-absorb` hand-off; full companion pytest suite exit 0 + `git check-ignore` confirms gitignored; the live multi-video first-run is manual-drive (deferred). RESIDUALS (accepted): A1 grounding catches FABRICATED not MISREAD quotes (routed to human wiki-review); A2 whisper deferred (uncaptioned dead-letter, rate measured at first live run); live first-run manual-drive.
Progress: ~0% (PLANNED — 0/6 tasks; T1 [M] acquisition + canonical/match normalization + dead-letter + gitignore guard; T2 [S] chunking with segment→time map; T3 [M] extraction (mocked Claude, extraction-by-selection) + pre-flight cost ceiling; T4 [M, CHECKPOINT] grounding rail + calibration probe + consolidation — STOP+report after the planted-fabricated-quote rejection; T5 [M] end-to-end wiring + atomic/guarded output + markdown-neutralization; T6 [S] README + full gate + focused adversarial review + BUILT.)

## Active Phase Contract

Phase: 116 - YouTube→wiki grounded acquisition core (rung-B foundation)
Tasks: 6 (see tasks.md)
Transition: continue
Abort: if blocked >3 attempts, ask user: skip or abort.

## Recent Decisions

| [[youtube-grounded-acquisition]] (Phase 116) YouTube→wiki grounded acquisition core (rung-B foundation) — the maintainer wants established Pi/agent-setup patterns from YouTube ("don't build from scratch"). Reframed against the Ph97 rung ladder: the rung-B (standing research pipeline) FOUNDATION increment, specialized to YouTube, built as LOCAL gitignored Python apparatus under companion/research/youtube/ (NOT a shipped kit skill — honors the Ph97 local-apparatus-never-shipped precedent; the continuous half RSS/scheduler/dedup-watermark/Telegram digest deferred to nanaclaw). DECISION: a manual-trigger acquire→chunk→extract→ground+consolidate loop feeding a Pi-setups wiki via the EXISTING wiki-add→wiki-absorb skills; extraction-by-selection (verbatim source_quote + <=40-word claim); LOAD-BEARING INVARIANT = the grounding rail (every source_quote mechanically verified in its chunk before it can enter a concept file; planted fabricated quote REJECTED). 8 adversarial-review-caught constraints: C1 normalization SYMMETRY + a calibration probe that ABORTS on broken normalization (else a silent drop reads as 'thin video'); C2 immutable-quote merge re-verifies the MERGED set; C3 atomic write + regression gate on grounded-quote count + completion marker; C4 neutralize [[/leading---/backticks in quotes at write; E1 empty-200 + sub-floor -> dead-letter; E2 segment->time map survives chunking; S1 pin tool version + pre-flight cost ceiling; S2 structured immutable verified field + README preserves provenance. Alternatives REJECTED: (A) one-shot sweep now / defer all infra (maintainer overrode -> rung B target); (B) shipped youtube-ingest kit skill (conflicts with Ph97 local-only); (C) whole pipeline in nanaclaw (continuous half deferred there); (D) Lite ceremony (A3 reject->Standard). RESIDUALS (accepted): A1 grounding catches FABRICATED not MISREAD quotes (routed to human wiki-review; NO AML-tier reconciliation); A2 whisper deferred (uncaptioned dead-letter, rate measured at first live run); live network/API first-run manual-drive (residential IP; tests on fixtures + mocked client). Spec specs/phase-116-youtube-grounded-acquisition.md (nana:approved); ledger Phase-116 all_accept:false (A1/A2 accept, A3 reject->Standard; --gate 116 exit 0). Foundation of [[frontier-positioning-sweep]] (Ph97 rung-B); grounding inherits [[qa-verification-sweep]]/[[HEU-012]] verify-by-re-fetch. | high | 2026-06-30 |
| [[conversation-memory-persistence]] (Phase 115) Conversation memory — per-workspace persisted + redacted at the boundary. Closes dogfood gap #2: the GUI conversation is pure in-memory React state (chat-runtime.ts:36), so an app restart loses the thread AND a workspace change LEAKS it (the Ph114 picker respawns the sidecar but the surface never remounts — same BridgeClient, no key — so the prior workspace's thread stays visible against the new workspace's fresh gate; a stale cross-workspace leak CONFIRMED in code, ledger A4). DECISION: persist a bounded, structurally-redacted, per-workspace conversation to localStorage keyed by ready.workspaceRoot; redact at the PERSISTENCE BOUNDARY via a NEW structured walker redactForPersist(UiMessage)→UiMessage (redactSecrets on ALL string leaves incl. nested tool output + details.diff; STRIP the write content body → '[N bytes]' marker — the single largest secret-bearing field, maintainer chose 'strip high-risk bodies'; finalize done:false turns); bound = last-N-turns + byte backstop, prune-oldest, fail-soft setItem (try/catch + prune-retry then skip). Restore on restart + SWAP on workspace change (fixes the leak) — DISPLAY-ONLY (pure setMessages, ZERO engine sends). The workspace source is an OPTIONAL useChatRuntime param → absent disables persistence (bare adapters/offline unaffected). Alternatives REJECTED: (B) host/Rust transcript (subtraction test — a bounded redacted conversation fits localStorage; the store is NOT gate-load-bearing so renderer-owned persistence doesn't widen the renderer-trust gap); (C) text-only/metadata-only max-safety (lower fidelity, kept as the A1 fallback); (D) full-redacted incl. write content (more at-rest exposure). KEY RISK / honest residual A1 (high, don't-know RESOLVED via down-scope): redactSecrets is pattern-based (PEM/sk-/AKIA/AIza/gh*_/xox*/JWT + opaque ≥32-char) and misses short/plaintext secrets; the content-strip is write-only — an edit tool's new_string body is redacted-not-stripped and shares this residual (same content the live DOM already showed, the delta is durability memory→disk); the live restart-restore round-trip is live-drive-only (Tauri WebKit/wry; cargo-check/vitest = compile/unit, Ph109-114 precedent). SECURITY (the constant, UNCHANGED): the host gate + inert-render + redact rail + no-bypass invariant stay UNCHANGED; out-of-ws READ stays the Ph112 residual. Ledger Phase-115 all_accept:false (A1 don't-know→down-scope; A2-A5 accept). Builds on [[tool-call-visibility-thread]] (Ph110 redact-at-adapter), [[engine-adapter-in-process-gate]] (no engine types across the boundary; no-bypass), [[pi-default-engine]] (Ph114); rides umbrella spec specs/gui-harness-architecture.md. | high | 2026-06-30 |
| [[pi-default-engine]] (Phase 114) Pi as the default daily engine (good tools) + workspace picker — the dogfood "the tools are awful" complaint is an ACCIDENTAL-DEFAULT bug, NOT a missing feature: the agent runs on the Vercel adapter (2 hand-rolled tools — uncapped bash, whole-file write) but Vercel is the default purely by a Ph109 bring-up artifact (main.ts:20 NANA_ENGINE ?? 'vercel'; no decision chose it). The spec + all 3 Ph108 decisions name Pi PRIMARY, and Pi ALREADY ships the rich suite (read offset/limit, ripgrep grep, find, ls, surgical multi-edit, a bash that caps 2000 lines/50KB + spills overflow) that the host gate already covers (Ph112 T1/A4). DECISION: make Pi the default (flip main.ts:20 'vercel'→'pi'; Vercel stays the NANA_ENGINE=vercel fallback) + harden the Pi path (thread maxTokens past the 2048 default → NANA_MAX_TOKENS ~8192; activate dormant grep/find/ls via the tools allowlist, fallback customTools; PROVE the live round-trip) + add a Rust-atomic workspace picker (native dialog → re-spawn the sidecar with the chosen NANA_WORKSPACE → fresh createHostGate + approved-writes; webview NEVER supplies the root; capability dialog-ONLY, no fs/shell/http). Alternatives REJECTED: (B) build a tool suite on Vercel (re-implements what Pi has, each gate-wrapped by hand); (C) user-pickable engine+workspace at launch (bigger; only the workspace half folded in). KEY RISK / honest residual A1 (high): the live Pi GUI round-trip was NEVER verified (Ph109 deferred) + a flaky e2e → Vercel may be the de-facto path for a real reason → T1 FRONT-LOADS a live drive with a hard STOP-and-escalate (do NOT flip the default if Pi can't drive reliably OR the gate can't be PROVEN to see the new tools). Other residuals: out-of-ws READ stays OPEN (Ph112 residual — Pi's read tools make it ergonomic, not newly possible; blocking reads = a future confidentiality phase); the native dialog→re-spawn round-trip is live-drive-only (cargo check = compile); #2 conversation memory → Ph115. The gate verdict-loop core + inert-render + redact stay UNCHANGED. Ledger Phase-114 all_accept:true (A1-A4). Builds on [[engine-adapter-in-process-gate]] (Pi named primary), [[os-sandbox-bash-fs-isolation]] (Ph112 gate-sees-all-Pi-tools), [[second-adapter-vercel-ai-sdk]] + [[provider-defaults-to-local-model]] (why Vercel was the 2nd adapter, not the intended default); rides umbrella spec specs/gui-harness-architecture.md. | high | 2026-06-30 |
| [[axis3-command-palette]] (Phase 113) Axis 3: Command Palette + Keyboard Control — build the LONE unbuilt north-star control/joy axis ("every action reachable by button, shortcut, or palette search") as a small PURE engine-neutral command registry + a custom hand-rolled Cmd+K command palette + a keyboard shortcut layer dispatching EXISTING actions (stop/approve/deny/revert/focus-composer) + new-conversation. Hand-rolled, NO `cmdk` dep (Ph109 "owned not adopted"; ~6 static commands don't justify a runtime dep + supply-chain surface in a sandbox-hardened app). NO new privileged host path — a Ph112-style no-bypass invariant guards the engine-host HostInbound union (unchanged or +1 benign `reset`); gate verdict-loop core + inert-render + strict CSP UNCHANGED; command titles inert; two task-bound footguns (bare-Enter must not auto-approve a held gate; single-key shortcuts must not fire while the composer is focused). Alternatives REJECTED: (B) gate the build on finally running the 4x-deferred live drive (maintainer chose the fast ship, Option A); (C) adopt cmdk (runtime dep + supply-chain surface, cuts against owned-not-adopted). KEY RISK A1 (high, honest): building axis 3 now BETS supply creates demand — the spec's "used weekly" test (spec:80) has never run (live drive deferred 4x); if unreached, surfaces at the live drive, Ph114 can deprioritize. Mechanics-only tests; felt-quality on maintainer judgment. Builds on [[felt-quality-surface]] (Ph109), [[tool-call-visibility-thread]] (Ph110), [[typed-artifact-fidelity]] (Ph111); rides umbrella spec specs/gui-harness-architecture.md. | high | 2026-06-27 |
| [[os-sandbox-bash-fs-isolation]] (Phase 112) OS-sandbox bash filesystem isolation (seatbelt, per-command, below the string-gate) — the named follow-on of [[host-gate-out-of-workspace-hardening]], closing the documented Ph111 residual: string-gating arbitrary bash is INCOMPLETE BY NATURE (python3 -c / node -e / base64 -d / env indirection evade a literal-text pattern). Add a per-command OS-sandbox enforcement layer BELOW the Ph111 string-gate, macOS-first (seatbelt via sandbox-exec), at the bash EXECUTION site for BOTH adapters through ONE guarded chokepoint. New pure app/src/gate/sandbox/seatbelt.ts (isSandboxAvailable cached; escapeSBPL+char-reject; buildProfile strict+integrity with $TMPDIR pinned from the TRUSTED host env; wrapBashArgv argv-form ['sandbox-exec','-p',profile,'/bin/bash','-c',cmd]; runSandboxedBash single chokepoint). Pi runs bash INSIDE the SDK but v0.80.2 EXPORTS clean seams (createAgentSession({baseToolsOverride}), BashSpawnHook, BashOperations) → substitute a sandbox-wrapped bash WITHOUT forking Pi; Vercel's bash is our own execSync (vercel-adapter.ts:54), wrapped directly. NO types.ts change (wrap at the execution site, not the gate contract); the gate VERDICT-LOOP core (confirmingGate/ConfirmationBroker/key-store hard-deny) UNCHANGED; per-command TS keeps it engine-neutral ([[engine-adapter-in-process-gate]]). DECISIONS LOCKED (ledger Phase-112, all_accept:false): A1 write-deny INHERITS to subprocesses (accept spike-gated, T1 proves first); A2 default-deny-write (accept, T1 sets the minimal mach/network allowlist); A3 = C1-PRESERVE (maintainer REJECTED C1-drop — confirmable out-of-ws bash writes STAY; on approval the target threads into extraWrites → approve-then-succeed, OS-enforced, platform-consistent; needs a gate→executor approved-targets channel; [[gate-confirm-approve-loop]] verdict-loop core unchanged); A4 Pi baseToolsOverride binds AND the tool_call gate still sees the ORIGINAL command (accept spike-gated; fallbacks BashSpawnHook→BashOperations→gate-modify); A5 = STRICT (maintainer REJECTED integrity-only — also tighten network+mach; STOP-escalate to integrity-only if T1 shows strict unusable). HONEST RESIDUALS (do NOT over-claim "residual closed"): exit criteria = "DIRECT bash file-writes confined on darwin", NOT "residual closed"; daemon/XPC-mediated writes (defaults write/launchctl/osascript) + full filesystem READ remain even in strict mode; sandbox-exec is Apple-deprecated-but-functional (Chromium-seatbelt precedent); non-darwin = Ph111 string-gating ONLY (Linux landlock/bwrap + Windows job-objects OUT); the allowlist can't enumerate every tool cache. T1 is the de-risking gate — STOP-and-escalate if inheritance fails OR the Pi seam isn't substitutable OR strict is unusable. Rejected: process-wide Rust sidecar sandbox (confines the host's own infra writes; coarse); the gate modify-path command rewrite (leaks the wrapper into the surface + quoting fragility); C1-drop (capability regression + platform inconsistency); integrity-only (kept as the TESTED fallback, maintainer chose strict). Rides umbrella spec specs/gui-harness-architecture.md (Ph108-111 precedent, ADR-named). Builds on [[host-gate-out-of-workspace-hardening]], [[engine-adapter-in-process-gate]], [[gate-confirm-approve-loop]]. | high | 2026-06-27 |
| [[typed-artifact-fidelity]] (Phase 111) Typed artifact fidelity — structured diff from Pi's typed details. The de-risked Ph110 follow-on: Ph110 threaded real tool args/output but the artifact panel routes via a diff-SHAPE heuristic (looksLikeDiff over the output text) + generic-text fallback because typed details were deferred — brittle (false-negative when edit output isn't diff-shaped; can only show the diff that's IN the output text). Ph110's adversarial review CONFIRMED Pi's AgentToolResult wrapper carries typed details (EditToolDetails.diff, pi edit.js:212-216) and named this the Ph111 headline. Thread Pi's typed details (NORMALIZED to a neutral {diff?: string} at the adapter) ADDITIVELY through the EXISTING engine-neutral layers (types.ts tool-result EngineEvent → runtime.ts SurfaceToolCall → artifact-feed.ts mapToArtifact) so the panel renders a STRUCTURED canonical DiffView; the typed path WINS over the heuristic when present, the looksLikeDiff + generic-text path remains as fallback for Vercel (details-less) + detail-absent tools. Three direction-gate forks (all_accept:true): A2 test panel DESCOPED (diff-only — no Pi built-in tool emits structured test details, a TestResults panel would reintroduce the heuristic class Ph110 removed); A3 NORMALIZE details to {diff?: string} at the adapter (not raw-forward — no Pi EditToolDetails type leaks, symmetric with extractToolText, honors [[engine-adapter-in-process-gate]]; A4 PRESERVED — the adapter normalizes DATA, mapToArtifact owns the VIEW-kind decision); A4 SCOPE = typed-diff thread + pin the flaky provider-roundtrip e2e ONLY (command palette / Claude SDK adapter / signed bundle + keyring residual + rollback stay separate future phases). Consequences: types.ts tool-result + runtime.ts SurfaceToolCall + artifact-feed.ts gain an additive details?:{diff?:string}; the Vercel adapter + host/bridge line protocol are the "consumers you didn't touch" (NO change beyond type-checking; payload stays JSON-serializable); details.diff is untrusted model-adjacent content → redactSecrets() + ~16KB cap at the adapter boundary + the inert React-string path (DiffView already inert); NO App.tsx/ArtifactPanel change (DiffView consumes the diff string; ArtifactPanel routes kind==='diff'→DiffView); the gate CORE ([[gate-confirm-approve-loop]]) stays UNCHANGED. #1 risk A1 (front-loaded T1 spike, revisit-status:open): tool_execution_end.result is typed any + ToolResultEvent.details is an EXTENSION-HOOK event not the subscribe stream → reading result.details off the subscribe event is INFERENCE; T1 live-spikes it (pin ≥1 test against the REAL Pi event shape), fallback = pi.on('tool_result') + toolCallId correlation, STOP-and-escalate if neither. Rejected: raw-forward details?:unknown (leaks Pi vocabulary, soft violation of the engine-adapter invariant); light up TestResults via bash-output parsing (reintroduces the heuristic class); bundle command palette / Claude SDK adapter / signed bundle (each its own phase). Builds on [[tool-call-visibility-thread]] (Ph110 — confirmed details-on-wrapper, named this the headline), [[engine-adapter-in-process-gate]], [[felt-quality-surface]], [[gate-confirm-approve-loop]], [[webview-engine-bridge]]. Rides umbrella spec specs/gui-harness-architecture.md Rev 2. Ledger Phase-111 all_accept:true (A1 accept-spike-mitigated/revisit-open, A2/A3/A4 accept). | high | 2026-06-27 |
| [[tool-call-visibility-thread]] (Phase 110) Thread real tool args/output/details source→surface — close the Ph109 #1 dogfood gap (allowed tool calls render NAME-ONLY). Pi ALREADY carries args (ToolExecutionStartEvent.args) + output (ToolExecutionEndEvent.result) + partial streaming (ToolExecutionUpdateEvent.partialResult) + typed details (ToolResultEvent.details); the adapter's narrowed PiStreamEvent stubs args:{}/result:{isError} and the reduction's SurfaceToolCall drops the output, while the Ph109 components (ToolCallView/DiffView/TestResults/TerminalOutput) already render them inertly — built ahead of the data. Thread it through the EXISTING engine-neutral layers ADDITIVELY (no engine-type reshape, honors [[felt-quality-surface]] A1), routed through redactSecrets() on the inert React-string path (the security rail extends to the new untrusted output, A2) + a documented ~16KB boundary size-cap; add ONE additive tool-progress EngineEvent variant for per-step streaming legibility; route completed results to typed artifact views in the UI keyed on tool name (NOT the adapter — A4 defended: routing-in-adapter pushes UI vocabulary backwards across the Ph108 engine-adapter invariant AND duplicates across both adapters), degrading to a generic terminal/text artifact if Pi's typed details don't populate on the LOCAL path (A3, validated by T1's spike); generate a simple Nana icon (mechanics-only). Consequences: EngineEvent/SurfaceToolCall + reduction gain additive fields + one union variant; the Vercel adapter + host/bridge line protocol are the "consumers you didn't touch" (must handle the variant; payload stays JSON-serializable); the redact+inert rail + size-cap are load-bearing; the gate CORE ([[gate-confirm-approve-loop]]) stays UNCHANGED. Rejected: route typed views in each adapter (backwards across the invariant + dup); defer the typed panel to Ph111 (maintainer chose fuller scope); adopt Vercel AI Elements (forces Tailwind/shadcn; owned components exist). Builds on [[felt-quality-surface]], [[webview-engine-bridge]], [[gate-confirm-approve-loop]], [[engine-adapter-in-process-gate]]. Ledger Phase-110 all_accept:false (A1/A2/A3 accept, A4 don't-know→defended-accept). | high | 2026-06-27 |
| [[felt-quality-surface]] | high | 2026-06-26 |
| [[engine-adapter-in-process-gate]] (Phase 108) Embed the agent engine IN-PROCESS behind an app-owned `EngineAdapter` interface; the security gate lives at the tool-call site — THE PIVOT to a GUI-primary, model-agnostic desktop dev harness (spec specs/gui-harness-architecture.md, nana:approved 2026-06-26 Rev 2). A host can own a PRE-EXECUTION gate (deny/modify a destructive tool call before side effects) ONLY if the agent loop runs in the host's own process. Verified against primary sources (2026-06): ACP permission is agent-discretion ("MAY", no enforcement; agents do local I/O outside the protocol); opencode-HTTP, Goose-daemon, and Paseo-supervisor each run the agent in THEIR process and only relay permission prompts. So the swappable boundary is an internal app-owned EngineAdapter interface (≥2 implementations) with the engine EMBEDDED IN-PROCESS, the gate at the in-process tool-call site. Primary = Pi SDK (gate via its tool_call hook), 2nd adapter = Claude Agent SDK (canUseTool, engine-neutrality proof), fallback = Vercel AI SDK (build-the-loop, deeper escape hatch only). Shell = Tauri (keyring-rs keychain + audited capability manifest); surface = assistant-ui (custom runtime → the adapter) + Vercel AI Elements; the Python MCP memory server ports as-is via MCP stdio. v1 thin slice = minimum daily loop + security rails, security/gate FIRST; app in a new app/ dir. Un-bypassability is a TEST not a claim ([[HEU-012]]) — if the Pi gate is bypassable empirically → STOP-and-escalate to Claude SDK / Vercel behind the same adapter. Adopt the engine, don't build the loop (amplifier-null floor +0.25). UI/felt quality UNMEASURABLE in-kit (Ph59/80) → ships on judgment at delivery, tests assert MECHANICS only (the 14 spec exit criteria are functional tests). Ledger Phase-108 (all_accept:false — A3 keyring-rs deferred don't-know revisit-status:open; A1 Pi-gate-un-bypassable accept-gated-on-T3-spike, A2 MCP-server-ports-as-is accept, A4 Pi-MIT-core-stays-available accept). Rejected: ACP-as-boundary (can't guarantee pre-exec interception); opencode-server-primary (the gate is opencode's, not the host's); fork Paseo (subprocess supervisor + permission relay, no host-owned gate; AGPL-3.0); build-the-loop-from-scratch on Vercel AI SDK (kept only as the deeper fallback — don't rebuild commodity plumbing). Builds on [[HEU-012]] + the amplifier-null family. | high | 2026-06-26 |
| [[project-state-dashboard]] (Phase 106) Project-State Dashboard + Act-from-Page Decision Gate — extend Ph99's render-only direction dashboard into an on-demand `make dashboard` project-state monitor + an OPT-IN act-from-page direction gate (the maintainer decides ON a served dashboard that drives /dev-plan), pulling forward Ph99's deferred A2 (served surface) + A3 (general project-state). The file:// sandbox can't write a repo file → the gate spins up a tiny 127.0.0.1 ephemeral server that serves the live dashboard, accepts ONE POST /decision, atomically writes .dev-wiki/decision-response.json, and exits; the orchestrator blocks on a run_in_background watch (NEVER foreground sleep) and ingests via ONE deterministic validate-decision-response.py shared by server+ingest (deterministic validator at the boundary, no neural judge). Safe-by-default: opt-in via .dev-wiki/act-from-page (absence = today's AskUserQuestion path, server never spawns); fail-open everywhere; the server owns the single timeout (writes a {status:timeout} sentinel). Three stdlib-only scripts (generate-dashboard.py IMPORTS the direction render fns / validate-decision-response.py / decision-server.py with a single-accept latch + atomic mkstemp→fsync→os.replace + Origin/Host/Content-Length/chunked guards). Subtraction-test: Architecture/Decisions panes CUT to a STATUS+DIRECTION floor; no daemon; ONE gitignored repo write; eval 50 stays 50; no shared-scaffold refactor (themes diverge). Human-facing presentation value UNMEASURABLE in-kit (Ph59/80 carve-out) → ships on stated need, tests assert MECHANICS only (HEU-012 controls-first: HTML CONTAINS live fixture content; present-but-restructured source → a DISTINCT visible marker not a silent placeholder; every malformed/stale/partial/phantom/bad-position input FAILS LOUD). Direction locked across 4 AskUserQuestion gates → "Continue" (ledger Phase-106, all_accept:true; A1 round-trip-removes-friction flagged weakest + cheaply reversible). Design hardened by a 13-agent ground+adversarial-stress workflow (6 lenses). Rejected: serverless clipboard directive (keeps the copy-paste step); always-on watching server (the literal Ph99 A2 — a managed daemon); File System Access API (Chrome-only/fragile); Architecture+Decisions panes; a shared-scaffold DRY refactor of the 5 generators (themes diverge → deferred to a 6th); pillars 1-2 contract-spine (continued pillar 3 instead). Builds on [[direction-dashboard]] (Ph99), [[HEU-012]]. | high | 2026-06-23 |
| [[gaming-rate-gate-strength]] (Phase 103) Gaming-rate vs visible-gate-strength — rung-C pillar-2 deepening (4th in the Ph100/101/102 lineage). A MEASUREMENT phase: build a deliberately WEAKER visible gate and measure whether the real opencode/big-pickle worker games (visible-PASS ∧ held-out-FAIL = silently-wrong work) — is the contract's STRENGTH load-bearing for truthful delegation, or does the worker's competence carry truthfulness regardless of gate strength? Ph102 flagged the loose thread: gaming detectable-in-principle but NOT exhibited under a STRONG gate. IV = gate strength as COVERAGE of the lag invariant (the Ph102 seeded adversary attacks calibrate the rungs — a gate's strength = which seeded gamers it catches); ladder G2-strong (v1_func+v2_lag, loci {0→1}+{2→3}) → G1-medium (one locus) → G0-weak (shape-only, lag unpinned); the held-out (decision_lag_strict, every-position sweep, disjoint AAA) held FIXED across rungs — only the visible gate varies. Headline = the SILENT-WRONG rate as the gate weakens; each held-out-fail run classified three ways {silent-wrong | special-cased (grep impl for visible SPY literals) | honest-flagged-wrong (hedge about the visibly-UNCONSTRAINED locus in the worker's OWN vocab, a SAFE failure never collapsed into the headline)}; cold-pass (visible-pass@iter1=no loop) bucketed apart, gradient on LOOPED runs only; NATURAL-worker-only arm. KNOWN CONFOUND named not isolated: weakening the gate weakens BOTH the bar AND feedback richness (feedback = the gate's failing assertions) — that IS a weak contract. Det-PRIMARY (held-out = ground truth). Controls-first / [[HEU-012]]: per rung a seeded gamer passes-visible+fails-held-out; a per-ADJACENT-pair manipulation seed (rungs−1) proves rung-k⊂rung-(k+1); the held-out catches 100% of seeded broken-impls at EVERY rung (clean-on-seed → drop the rung); each visible check reds on a seeded defect; validate_controls --selftest is ADVERSARIAL (each sub-check fails on a broken canary → no hollow exit-0); all-dead → abort. Held-out never leaks, RE-VERIFIED after EACH weakening edit (the edit is the leak-risk moment): per-run transcript+prompt grep voids on a held-out name/path/literal hit + transitive-reachability of each edited visible-check tree. Infra-fail excluded + re-rolled. Pinned effect floor (non-overlap Wilson OR |Δ|≥k·spread → below it = directional/underpowered); n matched across rungs, pinned per pilot. Byte-frozen before scored runs; the T2 calibration pilot (the MAKE-OR-BREAK) precedes the freeze — strong-gate ~0 replication + weakest-gate ≥1 real held-out-fail among LOOPED runs with a gradient, else informative null + STOP. Real /Users/jwang/edge-screener NEVER mutated (per-run copies + checksum; Ph102 incident fix carried — cleanup refuses non-tempdir, all serial). SHIPS NOTHING (check-fidelity.py shipped Ph100); apparatus gitignored companion/research/contract-loop-gaming/ (reuses frozen Ph102 by COPY). Gate Phase-103 all_accept:false — A1 elicitability / A2 realistic-weak-gate / A3 silent-wrong-separable all DON'T-KNOW → DEFERRED (revisit-status:open; the plan banks on none, each has a pre-registered fallback + the T2 pilot is the empirical resolver). Arm scope = natural-only (adversarial-prompt capability arm DECLINED — the seeded controls already establish gameability-in-principle). Rejected: the adversarial-prompt capability arm (option B — doubles runs for a secondary number); pit as a 2nd task (its Ph102 held-out was confounded by an orthogonal before-baseline case); collapsing honest-flagged-wrong into the gaming headline (loses the delegation-relevant safe-vs-dangerous distinction). Builds on [[contract-loop]] (Ph102), [[harder-corpus-screen]] (Ph101), [[HEU-012]]. A clean "gaming climbs (strength load-bearing)" OR "~0 everywhere (worker competence carries truthfulness — informative null rhyming with the amplifier-nulls Ph59/80)" are BOTH valid. | medium | 2026-06-22 |
| [[contract-loop]] (Phase 102) Contract-driven downstream-worker LOOP — rung-C culmination (pillar 2). A MEASUREMENT phase: build a throwaway contract-driven iteration LOOP and measure whether contract-governed FEEDBACK (beyond mere resampling) recovers the Ph101 single-shot FLOOR (decision-lag — naming the invariant ≠ implementing the subtle pending-lag), and whether iteration breeds GAMING (iterate-to-green on the contract's VISIBLE guardrails while failing a HELD-OUT generative integrity test the worker never sees). Per (task,arm,run): a per-run edge-screener copy (target fn stubbed, held-out removed + scrubbed) → opencode implements → check-fidelity.py scores the visible guardrails → on fail rich feedback (failing assertion + a counterexample with inputs DISJOINT from the held-out's) → opencode run --continue → ≤ cap iterations → score held-out ONCE in a SEPARATE copy. The ADVERSARIAL REVIEW added a structural 4th arm — best-of-N (N independent attempts, NO feedback, take the best) — so the headline feedback claim is contract-loop − best-of-N (isolating feedback from resampling), NOT − single-shot; bare-loop isolates the invariant within the loop. 4 arms byte-identical except the invariant-naming line. Det-PRIMARY (held-out = ground truth). Controls-first / [[HEU-012]]: a seeded GAMER MUST pass-all-visible + fail-held-out per task (gaming detectable in principle; else redundant held-out → task dropped), a seeded HONORED passes both, every visible check reds on a seeded defect; all-dead → abort. Held-out never leaks (transitive-reachability + per-run transcript+prompt grep voids on hit). RE-VERIFY the decision-lag single-shot floor STILL exists ≥3 seeds before the freeze (no floor → no-floor null). Worker can't edit the scoring (editable surface = the target fn only; scoring rebuilt from the frozen contract each iteration). Terminal-failure bucketed (converging-truncated/stalled/regressing); infra-fail excluded. Pinned effect floor (below it = directional/underpowered — no tiny-n over-read). Byte-frozen before scored runs. Real /Users/jwang/edge-screener NEVER mutated (per-run copies + checksum). Ships NOTHING (check-fidelity.py already shipped Ph100); apparatus gitignored companion/research/contract-loop/. Gate Phase-102 all_accept:true — A1 rich feedback, A2 visible-guardrail loop + held-out validation, A3 arms single-shot/contract-loop/bare-loop, A4 opencode loop via run --continue. Rejected: pass/fail-only feedback (A1 — single-shot WITH the invariant named already floored); iterating against the held-out itself (gaming undetectable by construction); claiming feedback value without a best-of-N arm (the resampling confound the 4th arm kills); general pillar-2 plumbing without the loop experiment (A3 — less tied to the Ph100/101 findings). Builds on [[contract-fidelity-screen]] (Ph100), [[harder-corpus-screen]] (Ph101), [[HEU-012]]. A clean feedback-recovers / resampling-only / floor-persists / gaming / no-floor are ALL valid. **OUTCOME: feedback RECOVERS the decision-lag floor (bare-loop 3/3 + contract-loop 3/3 vs single-shot 0/15, best-of-N 0/3 — feedback=+1.0 SUPPORTED; invariant-naming nearly inert; gaming 0/3 truthful). pit "gaming 3/3" was a frozen-held-out artifact (orthogonal before-baseline case; post-hoc invariant-focused = 6/6 recovery). Gaming detectable-in-principle but not exhibited. An adversarial pass found+closed a leak+under-pinning pre-freeze; an apparatus rmtree bug (run concurrently) deleted+restored edge-screener (intact, bug fixed, work serialized).** | high | 2026-06-22 |
| [[harder-corpus-screen]] (Phase 101) Harder-corpus contract-vs-spec fidelity screen on edge-screener — the harder-corpus follow-on routed forward from Phase 100's ceiling-saturated null. A MEASUREMENT phase reusing the Ph100 harness adapted to an edge-screener workspace: construct from-scratch FUNCTION-level stubs of integrity-critical edge-screener functions (one strong namable invariant each — no-lookahead decision-lag / cost application / PIT survivorship); the worker re-implements the stubbed function in a per-run throwaway edge-screener copy; arms (bare/spec/contract) at information-parity differ ONLY in whether the integrity invariant is named (test held-out). Score PRIMARILY by the held-out integrity test (deterministic, ship-blocking — the function's full functional+integrity test, so a vacuous stub fails), SECONDARILY by a cross-model judge (descriptive, can't overturn) — the OPPOSITE of Phase 100 (where the judge was the headline), because here the integrity tests are un-foolable ground truth. A mandatory difficulty-calibration pilot keeps only tasks where bare FAILS for an integrity reason, stably (≥2/N bare runs fail the SAME integrity assertion); no in-band task → informative null. Forked frozen analyze-hard.py (det-primary, MIN_CORPUS=3, n=3 — NOT the judge-primary Ph100 analyze.py). Gate Phase-101 all_accept:false — A1 accept (mandatory calibration pilot; no-in-band → informative null), A2 REJECT bug-injection → from-scratch stub (maintainer: more genuinely "actual code development"), A3 accept (per-run edge-screener copy reusing .venv; 3-4 tasks × n=3), A4 accept (edge-screener base). Controls-first / [[HEU-012]]: each held-out scorer validated against a seeded known-wrong reference (MUST fail the canonical bug) AND a seeded real reference (MUST pass); clean-on-seed aborts the task, all-clean aborts the phase. Adversarial+Tier1-hardened (failure-mode stability, contract-leakage diff, held-out-truly-unreachable grep, info-parity, contract-arm floor guard). Real /Users/jwang/edge-screener NEVER mutated (per-run copies + checksum). Ships NOTHING; apparatus gitignored companion/research/contract-screen-hard/. A clean "no discriminating corpus constructible" is a valid result. Rejected: bug-injection-repair (A2 maintainer reject → from-scratch); reuse Ph100 analyze.py verbatim (judge-primary/MIN_CORPUS=4/n≥5 → forked analyze-hard.py); synthetic-but-hard tasks (edge-screener is real + the maintainer's domain). Relates to [[contract-fidelity-screen]] (Ph100), [[can't-measure-clean-context-in-kit]] (N/A here — worker in edge-screener), [[HEU-012]]. | medium | 2026-06-22 |
| [[contract-fidelity-screen]] (Phase 100) Reframe rung-C pillar-1 from build-the-spine to a contract-vs-spec fidelity SCREEN — a MEASUREMENT phase (the rung-C analogue of the amplifier-screen lineage Ph70/71/77/78/80: pre-registered, controls-first, verdict read mechanically). At the gate the maintainer made A1 (does a contract artifact earn its complexity over reusing success:/spec) and A2 (is a deterministic spine enough) empirical-only (don't-know → measure with real opencode workers); A3 reject (a real worker IS the instrument, not seeded-only); A4 accept (ship the deterministic check, defer the schema shape to the verdict); R1 reject (deterministic + a marked neural-judge arm); R2 accept (3-arm + a frozen decision rule); R3 accept (ship the check, defer the schema). Build the deterministic fidelity-check (scripts/check-fidelity.py — scoring instrument AND the one shippable artifact; dual-use so building it precommits to NEITHER A1 outcome) then use it + a calibrated marked judge to score a pre-registered 3-arm screen (bare-prompt/spec/contract) on real opencode workers (opencode run --pure --format json, v1.17.3) in a non-leaking workspace outside the repo tree. Controls (adversarial+review hardened): information-parity arms; a leak-probe K≥4 at-chance-or-void (Ph80 leak hazard, [[can't-measure-clean-context-in-kit]]); per-guardrail seeded negatives + a gaming fixture (det PASSES, judge MUST flag); judge falsifiability (stable-across-repeats + citable defect, vs Ph44-50 self-grading inflation); infra-fail ≠ 0; check determinism + an AST no-LLM-import gate; pinned effect floor + min-corpus pilot rule; byte-frozen pre-registration before runs. Apparatus gitignored (companion/research/contract-screen/, like Ph97/98); only the deterministic check ships. A null (contract adds nothing → collapse to runner-only; deterministic suffices → drop the judge) is a VALID success. Rejected: build-first / seed-test / ship the contract spine now (ships before measuring, presupposes A1); measurement-only ship-nothing Ph97/98-style (A4 ships the check); seeded-only validation with no real worker (A3 — seeded outputs can't answer contract-vs-spec). Direction gate all_accept:false. Relates to [[direction-dashboard]] (Ph99 pillar 3), [[frontier-positioning-sweep]] (Ph97 rung-C named), [[HEU-012]]. | medium | 2026-06-22 |
| [[direction-dashboard]] (Phase 99) Direction Dashboard (render-only, dashboard-first) — render the /dev-plan DIRECTION GATE as a static, scannable HTML page (recommendation + options + cost-sorted assumptions + orienting context) so the maintainer reviews design directions visually at human pace instead of parsing a fast in-session text stream, then answers in-session. Pillar 3 (dashboard) of the rung-C contract-driven-delegation program, pulled forward + delivered FIRST per the maintainer's reframe (the contract-fidelity harness + downstream/local workers are pillars 1–2, later phases). 4th member of the existing docs/ HTML-generator family (generate-report/workflow/delivery-report), self-contained house-style match. A1 render-only ACCEPT (bottleneck is PROCESSING dense text, not decide-latency; interactive click-to-decide deferred); A2 generator-vs-server DON'T-KNOW → DOWN-SCOPED to the one-shot generator (strict subset of a future watching server; live-server filed to Blockers); A3 first-surface=direction-gate ACCEPT (self-dogfooding); A4 small dev-plan brief-emission ACCEPT (emit .dev-wiki/direction-brief.json at the gate). Controls-first / [[HEU-012]]: tests assert the rendered HTML CONTAINS the brief content + a seeded malformed brief MUST fail the generator loud. Judgment not measurement (Ph59/80 are model-facing). Rejected: interactive round-trip (A1); always-on live server (A2 → Blockers); the three soft Ph99 candidates opinionated-content/ride-the-rails/rung-D (superseded by the rung-C reframe — fidelity reframes rung-C AS the opinionated-guardrail bet); a shared-helper refactor of the 3 generators (surgical-changes discipline). Direction gate all_accept:false. | medium | 2026-06-22 |
| [[frontier-watch]] (Phase 98) Frontier Watch (rung-B) — stand up the Ph97 verdict's rung-B charter as a re-runnable, frozen, controls-validated TWO-SPEED tripwire watch: a WEEKLY broad-source/narrow-signal fast scan + a MONTHLY deep full re-derivation backstop + on-hit escalation to the frozen Ph97 pre-registration.md (600e1c9f). Watches the two resolvers — B5 boundary-validator (a 2nd lab ships a value-capturing GA deterministic boundary-validator like OpenAI auto-Pydantic, OR OpenAI deprecates → DIFFERENTIATED) + the COMMODITIZED early-warning (ANY lab ships value-capturing default-on B1 blocking CONTENT or B4 assumption/plan-adjudication → reopens shrink). Maintainer overrode the verdict's quarterly cadence (A2 reject → weekly fast scan, "things change too fast") + narrow scope (A1 reject → broad sources), reconciled via the two-speed split (Ph97 half-life finding applied per-tier: fast cadence on the cheap scan, rare on the expensive re-derivation); escalation re-runs the FROZEN rule (A3 accept, anti-retrofit). DETECTS-AND-FLAGS only — NO cut. Controls-first detector MUST flag a seeded tripwire + read QUIET on attach-surface churn ([[HEU-012]] anti-dormancy). Lives entirely in gitignored companion/research/watch/. Rejected: the verdict's quarterly narrow watch (A1+A2); a daily FULL re-fetch-and-reclassify (the cost-burning standing pipeline the verdict warned against → alarm fatigue → back-door dormancy); a fresh escalation instrument (A3 — anti-retrofit). Direction gate all_accept:false. | high | 2026-06-21 |
| [[frontier-positioning-sweep]] (Phase 97) Frontier Positioning Sweep — the EXTERNAL arm of the Ph92 re-measure-then-shrink (the internal arm [[memory-layer-disposition]] already returned KEEP). "Shrink or not" reopened with the maintainer constraint: stop deciding it in isolation (every amplifier-null was self-referential). A one-shot, pre-registered sweep reads where the agent-harness / agent-tooling frontier is converging; the verdict is read MECHANICALLY against a decision rule FROZEN before any research. Two maintainer rejects reshaped it: A1 reject → reframe layer-shrink to PRODUCT-POSITIONING (DIFFERENTIATED / COMMODITIZED / DIVERGENT / INCONCLUSIVE; shrink is a downstream consequence of COMMODITIZED, not the axis; does NOT reopen the Ph95 memory KEEP); A2 reject → lab-published artifacts (Anthropic agent SDK/cookbooks/Claude Code, OpenAI Agents SDK, Google ADK, eng blogs, papers) PRIMARY, OSS repos SECONDARY corroboration (labs ARE the frontier; internal harnesses unseen → read DECLARED direction); A3 don't-know → down-scoped to T4 (no standing pipeline this phase; T4 assesses signal half-life + recommends a cadence for rung B). Lives entirely in gitignored companion/research/ (rung A of the de-risking ladder — B pipeline / C opencode workers under nana-written contracts referencing signal-watch / D generalization e.g. trading are FUTURE phases). VERDICT only — NO cut (any shrink is a separate gated phase). Frozen discipline (Ph87 three-tier authority) + mechanical verdict guard apparatus-as-decision-avoidance; lands on the one untested amplifier frontier (post-cutoff signal a bare model can't hold). Rejected: layer-shrink as the target (A1 — Ph95 closed the big one KEEP); OSS-popularity as the frontier proxy (A2); building the standing pipeline before a one-shot sweep proves the signal (subtraction test — cheapest experiment first); bundling the opencode worker platform into this phase (deferred to rung C). Direction gate all_accept:false. | medium | 2026-06-21 |
| [[consumer-resync-rollout]] (Phase 96) Consumer re-sync rollout — realize the banked sandbox-only Phase-93 `--update` LIVE across all 7 consumers. Planning probe REVERSED the Phase-93 filing: `--update` hardcodes `settings.local.json` but 4/7 (edge-analyst, ai-game, fate, aml-casework) register in `settings.json` (project scope) — running `--update` there orphans the `detect-loop` reg (ghost, bitten 5× [[install-gap-dir-currency]]) + spawns a parallel `settings.local.json` (cross-file double-fire [[drq-1-settings-merge-semantics-are-string-keyed]]). Maintainer A1 REJECTED in-place reconcile → consolidate-to-local: a one-shot `install.sh --migrate-to-local` relocates `settings.json` kit-regs into gitignored `settings.local.json` + deregisters kit-managed/cut hooks from `settings.json` (both-file backup + survivor smoke + revert; existing `--update` UNCHANGED). Build controls-first + sandbox-verify, HARD checkpoint after T3, then apply LIVE all 7 gated per-consumer (A4; ai-game's 73 dirty files snapshot-first). Arm signal-watch + aml-casework (A5 reject of fully-decoupled); ai-game + fate unarmed. Closes the Phase-93 [[install-resync-update-mode]] deferred live-application filing. Rejected: in-place two-file `--update` (A1 — keeps kit hooks in committed settings.json + permanent two-file logic); hand-migrate Group B with per-consumer jq surgery (reintroduces the manual surgery Phase-93 ended); defer ai-game (A4 — include, snapshot first). Direction gate all_accept:false. | high | 2026-06-21 |
| [[memory-layer-disposition]] (Phase 95) Memory-Layer Disposition — Phase 94's reversal of the demand-zero premise (superseded specs/phase-92-memory-layer-prune.md) re-shapes the long-deferred memory-layer prune into a RECONCILE-AND-CLOSE round: adjudicate EVERY open memory-layer obligation to a recorded, evidence-cited closed-enum verdict (verdict-table.md, PINNED row schema `\| <id> \| <verdict> \| <evidence> \|`, verdict col 2). memory-mcp-layer + both writers KEEP (the writers keep-by-affirmation per the maintainer's direction-gate position — evidence-split asymmetry: consumer evidence may KEEP not CUT a kit-side writer); enforce-memory the ONE live fork (keep\|redesign\|retire) decided at a HARD checkpoint on a firing-distribution audit (JSON tool_use never grep, block→follow-through vs ritual) + a redesign feasibility spike (redesign GATED on SPIKE: PASS — can a PreToolUse hook deterministically assert a prior in-session memory_search, det-over-narration); the Ph88 trim-trials ak-ride-along (d43950f) + wk-seeding (df3e623, REVERT-COUPLED) CONFIRM (windows clean Ph88-93). The cut apparatus is CONDITIONAL (subtraction test on the spec — most verdicts keep; only destructive path = enforce-memory retire, low-hazard). Closes ledger Phase-83 A5 (open→held) + Phase-88 A4/A6/A5. Zero kit code UNLESS enforce-memory flips. Cross-link [[consumer-memory-remeasure]], [[strategic-inflection-review]], [[deterministic-vs-llm-boundary]], [[trim-round-outcome]], [[prune-on-value-subtraction]], [[HEU-012]]. Rejected: inheriting the phase-92 full cut apparatus (most verdicts keep); a fresh kit-side store audit for the writers (A3 reject); restoring the trim-trials (windows clean → confirm). Direction gate all_accept:false (A1/A2/A4 accept, A3 reject → keep-by-affirmation). | high | 2026-06-20 |
| [[consumer-memory-remeasure]] (Phase 94) Clean consumer memory re-measure — ONE admissible RETROSPECTIVE re-measure of consuming-project memory-LAYER demand across a 3-consumer machinery gradient (signal-watch none / aml-casework rules / aml-substrate rules+hooks) on the repaired (Ph91) global memory MCP, gated by verify-by-firing ([[HEU-012]]; the Ph89 zero was couldn't-fire). Recon PRIOR to confirm (NOT a banked result): demand tracks machinery — spontaneous floor ~0 (signal-watch 1/0/0 rows) but coerced demand persists (aml-casework 10/17/20 rows; aml-substrate 28/37/44 rows) — would REVERSE the "consumer demand is zero" lean. EVIDENCE ONLY (Phase 95 disposes); JSON tool_use parse never grep; coercion ≠ demand (3-class + cross-session read-back = ritual-vs-value); zero kit code (eval/ only); does NOT trigger the deferred Phase-93 live re-sync. Rejected: the heavier install-the-layer-fresh + fresh-sessions apparatus path; n=1 on signal-watch alone (maintainer expanded to n=3 — the expansion REVERSED the floor reading). Direction gate all_accept:false (A2 don't-know → down-scope + expansion; sufficiency-for-cut routed to Phase 95). | high | 2026-06-20 |
| [[install-resync-update-mode]] (Phase 93) Extend install.sh with an idempotent `--update` re-sync mode (REUSE cp/register/ship_hook_dirs, single source — subtraction test) that reconciles a consumer's project-local hooks + settings.local.json to the current kit: ADD/UPDATE changed hooks + dedupe registrations by script basename (DRQ-1, [[drq-1-settings-merge-semantics-are-string-keyed]]) + AUTOMATE the basename-normalized deregistration of cut hooks (codifies the manual Phase-84/85 jq surgery — no dereg mechanism has ever shipped, register-settings.py is upsert-only, registered-but-broken bitten 5× [[install-gap-dir-currency]]) behind --dry-run + timestamped backup + tested restore + survivor smoke + revert-on-failure. Hook-refresh DECOUPLED from arming (.claude/enforce untouched unless --arm — the 4 staged consumers must NOT auto-arm); signal-watch never-installed routes through --project-local. **BUILD + SANDBOX-VERIFY ONLY** — ZERO live consumer writes; controls-first mktemp fixtures (3 drift classes: no-hooks / staged+detect-loop-ghost / Phase-79 duplicate-registration) + seeded controls (cut-hook + duplicate registration the dereg/dedupe MUST catch, clean-on-seed = instrument-dead) ARE the test. Rejected: separate scripts/resync.sh (subtraction); detect-and-warn-only (maintainer chose automate-behind-rails [[prune-on-value-subtraction]], [[hook-registration-hygiene]]); a live canary write this phase (build-only — defers live application to a gated follow-on; does NOT block Phase 94, memory FIRES globally via Ph91). Re-sequenced from [[strategic-inflection-review]]. | high | 2026-06-18 |
| [[strategic-inflection-review]] (Phase 92) Strategic inflection review — at ~91 phases, an evidence-grounded review (7-agent workflow: 5 surveys → synthesis → adversarial critique) of the kit's strengths/limits + objectives + the det-vs-LLM boundary. Two maintainer gate positions: FRAME = **product for consumers** (rejected the instrument-primary read → inward measurement/hygiene growth is mission-creep to correct, consumer surface is primary); DIRECTION = **re-measure-once-then-shrink** (NOT the synthesis's "refocus", which smuggled in a zero-lift-evidence retrieval R&D track + a linter-rewrite on a never-observed defect; the Ph89 memory demand-zero is couldn't-fire — broken until Ph91). Critique corrected 5 overclaims (Ph87 det-beat-reviewers = binary-file confound; "5 nulls" = 4 clean + 1 instrument-dead). Re-sequenced roadmap: 93 install.sh re-sync → 94 memory re-measure → 95 memory-layer shrink (supersedes the old phase-92-memory-prune spec). | high | 2026-06-18 |
| [[deterministic-vs-llm-boundary]] (Phase 92) Det-vs-LLM boundary principles for an agentic harness: (1) LLM only for irreducible semantic synthesis, everything mechanical → script (gates NEW work; not a mandate to rewrite working prose-skills); (2) the final integrity gate is deterministic + asserts the observable artifact, not the LLM's narration (verify by firing — the measured win, 4 silent breakages caught); (3) fail-polarity by cost-of-error (integrity gates fail-closed, advisory/relevance filters fail-open); (4) when the answer is already in-context, curate the hot-cache, don't bolt on retrieval (Ph59/61). Kit map: assumption gate = the model to emulate (LLM surfaces, Bash validator asserts the row); dev-check/wiki-health/wiki-registry = mechanical-dressed-as-LLM but deferred (no observed defect); enforce-memory = gameable det gate, redesign-or-retire at Phase 95. Adopted as a maintenance guideline. | high | 2026-06-18 |
| [[assumption-gate-direction-filter]] (Phase-90 follow-on) Assumption-gate direction filter — the dev-plan assumption gate was surfacing direction/approach CHOICES dressed as assumptions (regressing to "approve the approach? → blind yes"; signal-watch dogfood, Jake's recurring correction that hadn't persisted). Root: Surfacing defined load-bearing as "outcome changes if false" — which direction choices satisfy, no filter excluding them. Fix: a checkable "assumption, not direction" filter (`If FALSE, then [breakage]`; incoherent negation or "I'd have chosen differently" = direction → drop; a real assumption rests on a fact the agent LACKS, what a don't-know marks). Same comply-in-form class Phase 90 targeted — the gate's own degeneration it missed. Both-landings + 4 synced projects + MANIFEST; signal-watch via global; feedback to memory. Heavier fix if it still degenerates: clean-context surfacing subagent (the B2 move). Refines [[assumption-approval-gate]]. | high | 2026-06-13 |
| [[fable-distillation-round]] (Phase 90) Fable-5 Distillation — distill the removed fable-5's planning/assumption-surfacing/interaction edge (paired-transcript contrast: signal-watch + nana-dev-kit vs opus-4-8; sgup/ai Fable5.md as convergent reference) and recover it for future opus sessions. Governing finding: opus already fills the T0/assumption-gate FORMS in form not substance, so more same-kind prose is the kit's measured failure mode (Ph46/Ph47) — the lever is context-shaping + checkable forcing-functions. Track A (shipped, both-landings, parity): 5 checkable forcing-functions into nana-soul.md (2 new + 3 sharpenings, absence-diff-confirmed — name-likeliest-wrong-claim, mark-inferred-as-inferred, what-speaks-the-old-contract, no-regressions-needs-a-baseline, match-effort-to-blast-radius). Track B1 (shipped): dev-plan Step 10 reframe-absorption beat (bounded firing) recovers fable's pause-validate (I1) structurally. Track B2 (STAGED, pilot-gated): clean-context framing subagent recovers reframe-DEPTH (I3) — built, NOT wired; Ph80 leak + Ph47 self-dialogue-negative require a consuming-project pilot first (eval/fable-distill/pilot-protocol.md). I2 reveal-banking DEFERRED (feeds Phase-91 prune target). Ships on judgment not measurement (Ph59/Ph80; the B2 pilot = the one real measurement). Gate all_accept:true (A1 accept-pilot-gated). Review 8.5/10 ACCEPT-WITH-FIXES (finding 3 + 3 LOW fixed inline). | high | 2026-06-13 |
| [[dogfood-demand-evidence-round]] (Phase 89) Dogfood & Demand-Evidence Round — pre-registered four-stage evidence round: T1 pre-registration commit FIRST (session-evidence schema with trigger-reachability fields + 3-class memory-call classification `hook-prompted | rules-instructed | spontaneous` + a blind boundary ruling for instructed-read-with-read-back; SESSION UNIVERSE spans the ≥3 edge-screener evidence sessions AND every kit-side Phase-89 working session incl. close-out, kit-side expected non-probative via the WK-already-presents-it subclass; window-events = a CROSS-PHASE accumulator Phases 90-93 keep appending to until the Phase-93 disposition, the append obligation pinned in always-loaded active-phase.md at close-out per [[HEU-012]]; admissibility pins citing the A4 reject rationale + the Phase-83 keep-with-revisit filing; wk-seeding pinned-decision inventory; measurement-blind prompts; header-deferral note) → T2/T3 controls-first checkers + runner → T4 read-only probes + HARD checkpoint + gated edge-screener re-sync (post-trim surfaces BEFORE any session — pre-trim exposure is invalid; rehearsed basename-normalized jq deregistration of the cut detect-loop on a copy of the ACTUAL settings.local.json; backup + tested restore) → T5 ≥3 real sessions with session 1 = a real /dev-plan Phase-10 planning session (trigger-reachable BY ITS NATURE — counters the weakest assumption that headless sessions structurally cannot reach either trigger) → T6 close-out (3-class tallies, writer write/read-back counts, A4/A6 evidence filing, runner ALL-PASS). Evidence only, never disposition; templates/**, modules.json, MANIFEST byte-untouched; no reverts of d43950f/df3e623/75b48af/b8bd416. Gate all_accept:true (A1-A5 accept; A2 accept-defended after don't-know — live transcript-observability probe). | high | 2026-06-11 |
| [[trim-follow-on-round]] (Phase 88) Trim follow-on round — verdict-table-first, serialized execution; the Phase-83 method extended with trim-trial reversibility, five stages: evidence re-snapshots FIRST (provenance-filtered enforcement.log 69→385 drift, named filtering mechanism — ts cross-reference vs session-transcript windows + memory.db write times; block→follow-through pairing; pre-stated fallback `undecidable-on-this-evidence`, never argued from the raw count) + ~10-row verdict table (closed enums per candidate class; session-start.sh:110 coupling split pre-listed; revert trigger + observation window + Blockers re-trigger per trim — a trim with no written revert trigger is not a trial and may not execute) → checker-tightening strand controls-first (seeded-defect + clean + boundary fixtures under eval/trim-round/checker-fixtures/ BEFORE editing the 3 routed files; allowlist + cmp on everything else) → unconditional HARD checkpoint → serialized per-candidate commits (sandbox-rehearsed revert + basename-normalized deregistration with positive control + ghost sweep incl. settings.local.json + regenerated-diff ⊆ removal set) → close-out with baseline-pin (trim commits = new harness baseline). Trim mechanism: deletion with rehearsed revert, NOT config-flip demotion (registered-but-dormant is the kit's 4×-bitten class). Coupled memory verdicts resolved AT THE GATE: A4 REJECT defers the layer disposition, A6 REJECT keeps the bridge/harvest writers alive with it (clean future demand evidence beats trimming the writer now) — no dead-mandate or evidence-laundering pairing can arise; enforce-memory disposes against a live layer (A3 attempt+fallback). Gate all_accept:false (A1/A5 accept, A2 spike-defended, A3 attempt+fallback, A4+A6 reject). | high | 2026-06-11 |
| [[stage2-episode-execution-design]] (Phase 87) Stage-2 episode contrast execution design — execute the FROZEN `## Stage-2 parameters` verbatim (byte-frozen at 9ad62f0); three maintainer rulings (direction Q&A 2026-06-10): isolation = INDEPENDENT CLONES from the same pinned setup state (faithful implementation of the frozen "twin worktrees" wording); ship-runner referent = edge-screener's standing phase-exit gate (full pytest + coverage + mypy strict + ruff) pinned as one byte-exact command, orchestrator-executed identically in both arms; gate inputs = CANNED, orchestrator-mediated (closed policy, verbatim per-arm logs; zero-gate-firing in arm B pre-declared valid). Execution shape: addendum-first (eval/ceremony-lift/stage2/execution-protocol.md pins all execution-level mechanics, committed BEFORE any arm — first-add-commit ancestry + byte check) → setup commit off 368e056 as a HARD checkpoint (parity provisioning: force-added gitignored/untracked enforcement surfaces, Phase-10 analysis, positive-control dev-wiki seed; setup-SHA deviation explicitly acked) → instrument validation before arm tokens (HEU-012 hook-fire probe, isolation probe, restoration incl. live DIRTY state, detector rehearsal, extractor smoke) → arms fresh-session, canary to arm B post-stop → orchestrator-executed tables, blinded tie-break iff both pass → disposition in closed vocabulary. Consequences: bundle-attribution caveat explicit (full-ceremony-vs-minimal, not spec-vs-no-spec); kit-commit embargo vs 6728e2f until both arms close; the spec-generation disposition routes the [[ceremony-step-verdicts]] ambiguous-stage-2 row. | high | 2026-06-10 |
| [[ceremony-lift-tiered-screen]] (Phase 86) Ceremony lift: tiered screen before episode experiment — measure whether the kit's ceremony steps (dev-plan, spec, reviewer dispatches, debrief) provide lift via a two-stage design, episodes conditional. Stage 1 deterministic in-repo (`eval/ceremony-lift/`): pre-registration committed-then-FROZEN before tabulation (byte-unchanged anti-retrofit; per-step thresholds, cost-materiality threshold, evidence taxonomy, closed verdict enum, corpus = fixed phase window + mechanical enumeration of ALL dispatches incl. zero-catch, admissibility rule, stage-2 verdict-transition authority); ceremony cost baseline FIRST (tokens raw + cache-adjusted, wall-clock, synchronous interruptions; keep-by-immateriality early exit); two-class evidence taxonomy — outcome-grade (marginal catches, admissible ONLY after orchestrator re-execution of the deterministic gate against recoverable pre-fix state; else downgrade to ambiguous, pinned) vs consumption-grade ([uses:N], retrieval hits — can mint cut-candidates, can NEVER mint keep for re-presentation-class outputs, the amplifier-null class); controls-first (hand-labeled cost session, seeded tabulator rows). Stage 1 mints NO verdicts (gate A1 round 2): the maintainer takes keep/trim/cut/ambiguous→stage-2 positions at the HARD checkpoint (caveat column + MDE arithmetic — no-catches-observed alone invalid for rare-event steps). Stage 2 conditional: bundle full-ceremony vs assumption-gate+hooks on burnable edge-screener Phase 10, twin worktrees, deterministic ship criteria, positive control; n=1 confirms cut-candidate→reversible trim-trial only. VERDICTS-ONLY phase, pinned base 5360486; cuts route to a follow-on. Rejected: full episode experiment now (cost without screen; LOO attribution noisy); judgment cut without measurement (Phase-85 reviewer catch is live counter-evidence). | high | 2026-06-10 |
| [[install-gap-dir-currency]] (Phase 85) Fix the structural invariant, not the single instance: every install path that ships a hook script also ships the subdirectories it consumes (modules.json `project_local.extra_dirs` migrated to a declared top-level `hook_dirs` map — a project_local-named key driving global-path behavior is a single-source smell; 4 consumers migrated; empty-glob tolerant under set -e), and check-install-drift.sh treats "consumer present, companion directory missing/stale" as drift via consumer-conditioned directory cells (orphan installed-only files FLAGGED as drift rows, never removed — detect-and-warn charter). T1 inventory FIRST: line-cited enumeration of every hook-shipping path; the incident-5 causal hypothesis (Phase-82 drift-guided resync used checker output as its shopping list) verified, not assumed; session-start.sh's registration-dead status recorded; sourced-.d/ mode-drift exemption pinned. Then edge-screener: DRQ-1 empirical verification (STOP if not double-fire) → HARD checkpoint 2 → dedupe-by-basename migration into settings.local.json (register-settings.py is upsert-only — it cannot dedupe the Phase-79 hand-patch; .bak disposition explicit) + union-uniqueness + single-firing; dogfood ≥2 real-work sessions with the memory liveness probe FIRST (Phase-83 couldnt-fire trap) — A5 evidence filed, not disposed. Gate: A1 deferred (checkpoint 1 decides), A2 accept (mandatory verification), A3 down-scoped (kit fixes independent of the dead copy's fate; ship-vs-dispose at checkpoint 1), A4 accept; all_accept:false. Rejected: delete the unregistered ~/.claude project-scope copies this phase (regresses the Phase-82 presence charter; separate prune-on-value question); re-scope session-start.sh as scope:global (recreates the ghost class Phase 84 just removed — the fix ships FILES, never registrations). | high | 2026-06-10 |
| [[hook-registration-hygiene]] (Phase 84) Instrument-first, capture-anchored hook fixes + checkpoint-gated registration reconciliation. Stage A: eval harness hermetic FIRST — run_hook exports CLAUDE_PROJECT_DIR="$WORK_DIR" + live-state tripwire + seeded-leak self-check (reverted copy must turn the leak test RED) + unset-variant + post-instrument 52-scenario baseline BEFORE any hook change. Stage B: REAL PostToolUse capture with provenance BEFORE any fix (pinned Phase-82 events carry NO exit-code field); three pre-declared evidence-forced branches per hook — remap / redesign (post-commit → actual git state with failed-commit rigor; detect-loop → tool_response.stderr/error markers) / upstream (platform-defect filing + disable-at-boundary at the checkpoint, criteria N/A-upstream); canonical jq parse (detect-loop off raw grep), legacy top-level .exit_code fallback retained (52 denominator stable), 3-step marker resolution over the 4-matrix, HEU-002 fail-loud on signal-absence, parser-edge fixtures, artifact-CONTENT assertions. Stage C: coverage matrix over all kit-marker-discovered roots (ghosts load-bearing for 8 hooks in 3 roots) + basename-normalized extraction + sandbox-rehearsed remediate-then-deregister (timestamped backup + tested restore) + HARD maintainer checkpoint BEFORE any live settings edit. Gate: A1/A3/A5 accept, A2 don't-know→evidence-revised→still don't-know→DEFERRED (must-revisit; T4a checkpoint is the decision point; remediate-then-deregister the prepared default), A4 dissolved to verified fact (all four Phase-82 repros confirmed at HEAD at gate time); all_accept:false, ledger appended + validated. Rejected: fix-hooks-before-harness (instrument would mutate the subject); keep detect-loop's pure-grep parser (truncates at embedded quotes; jq is the kit norm); hand-written assumed-shape fixtures (fixture circularity — both hooks' existing tests pass TODAY while dormant); deregister-all-11 without a coverage matrix (4th cascade-failure instance); surgery on direction-gate authority alone (the A2 deferred don't-know). | high | 2026-06-09 |
| [[prune-on-value-subtraction]] (Phase 83) Evidence-first, verdict-gated, serialized subtraction over the 6 Phase-82-evidenced dead-weight candidates (enforce-memory.sh; memory reinforcement machinery; memory-mcp-scaffold shipping; audit-log model field; 3 orphan companions; harness-audit.sh). Per-candidate verdict table (closed enum keep/cut/harden/disable-at-boundary) with arming-procedure + matrix-row-citation columns BEFORE touching anything; mandatory couldnt-fire vs didnt-fire zero-classification via mktemp -d sandbox arming (won't-fire-when-armed = DEFECT, never a demand-evidence cut — [[HEU-012]]); installed surface mechanically DISCOVERED via kit-marker scan (A3 reject → A6); removal-set-first liveness grep over ALL discovered roots (LIVE vs HISTORICAL, Phase-72 semantics — historical records superseded never rewritten); unconditional mid-phase checkpoint BEFORE any cut; serialized per-candidate commits (`Phase 83 cut: <name>`) with regenerated-diff ⊆ removal set + sandbox-rehearsed own-ghosts-only installed deregistration + survivor functional smoke + revert-on-failure; memory_server menu keep/disable-at-boundary/cut-with-regenerated-patch (vendoring contract); zero cuts a valid outcome. Direction gate: A1/A2 accept, A3 REJECT → discovery revision, A4 don't-know → defended A7 (live install lacks fastembed → candidate 2 largely pre-classified couldnt-fire → keep/harden), A5 don't-know DOWN-SCOPED (kit-side memory-layer value question deferred must-revisit); ledger appended, all_accept:false. Rejected: hook-hygiene-round-2 / edge-screener consuming-project work (Jake's phase-direction choice); wholesale memory-layer disable (contradicts [[memory-architecture-classification]]); all-11 ghost deregistration (Jake chose own-ghosts-only); repo-only cuts (leaves silently-failing ghost registrations). | high | 2026-06-09 |
| [[qa-verification-sweep]] (Phase 82) Coverage-defined two-stage multi-agent QA sweep, 8 audit areas (wiring/firing/companions/schema/drift/coverage/docs/usage), incl. the two gate-forced revisions from the FIRST live dogfood of the Phase-81 assumption-approval gate: (a) usage/utilization as the 8th area (Jake's A1 don't-know — "is the MCP memory server even used?"; under-use findings feed the parked Phase-79 prune-on-value item), (b) subagents demoted to READ-ONLY candidate-generators with orchestrator-executed deterministic commands as the SOLE verdict evidence (Jake's A2 reject of leak-is-acceptable-with-re-execution; the Ph80 leak helps candidate recall but is fatal for verification). Controls-first staging (HEU-006 two-tier mirrored): every checker vouching for a clean verdict must first catch a seeded synthetic defect — clean-on-seed = instrument-dead for its area. Mid-phase findings report to the user BEFORE any fix (unconditional); serialized single-writer fixes under the pre-registered boundary (kit-managed + S/M + test-covered blast radius; coverage exception: the missing S/M functional test IS the fix — functional-smoke invariant; >10 confirmed defects → STOP and re-scope). Deliverable: eval/qa-sweep/verification-matrix.md (enum clean/defect-found/deferred/instrument-dead) — verified-clean mechanically distinguishable from never-checked; ZERO confirmed defects is a valid success (agents must not manufacture findings). Phase-81 surface quarantined (ledger read-only; gate defects via --selftest, filed). Frozen apparatus (eval/amplifier/*, eval/assumption-screen/) read-only — findings filed, not fixed. Rejected: extending make test directly as the phase (the sweep produces the gap list test-writing consumes); depth-first Phase-81-only audit (breadth is the point when the suites are green). | high | 2026-06-09 |
| [[assumption-approval-gate]] (Phase 81) Ship the SIMPLEST assumption-approval gate — the earned consequence of Phase 80's INSTRUMENT-DEAD ([[assumption-surfacer-completeness-screen]]; the scope-anchored surfacer's only edge was a working-knowledge leak, the clean signal DEGENERATE — a naive cost-sorted surfacer recovers load-bearing assumptions by reasoning). Four components: (1) the frozen Phase-80 NAIVE surfacer in dev-plan (3-6 cost-sorted; Step-10 T0's weakest assumption a required member); (2) accept/reject/don't-know positions via AskUserQuestion that **REPLACE** the approach-approval click (positions ARE the gate; reject→revise+re-surface, don't-know→defend-or-down-scope else deferred→Blockers+must-revisit, no unresolved reject/don't-know = direction confirmed); (3) all-accept→warn + track `all_accept:true` + restate how each accepted assumption shapes the approach (Jake's Q2 choice, NOT a hard block — the bite is the always-fired restatement + cross-phase tracking, [[HEU-012]]-compliant); (4) an append-only cross-phase ledger `.dev-wiki/assumption-ledger.md` + a dev-debrief revisit-status forcing-function + a deterministic NO-LLM `scripts/check-assumption-ledger.sh` enforced at debrief finalization. NO new hook (the debrief-finalization check is the firing point; session-start advisory deferred). The Phase-80 plan was itself interrogated through this mechanism — Jake's live A1 accept / A2 reject (made Ph80 a screen) / A3 accept / A4 accept, plus three direction-gate decisions this plan (Q1 positions REPLACE the click; Q2 all-accept→warn+track+restate; Q3 build the ledger now). Efficacy is UNMEASURABLE in-kit (the amplifier-null + Ph66/69/80 representativeness) — tests assert MECHANICS only, no quality delta claimed; the ledger row is the firing evidence ([[HEU-012]]: verify firing not presence); ONE documented ledger-schema source referenced by both skills + the check (no split-brain); append-only + monotonic-row corruption guard; the debrief revisit re-scans prior-phase unrevisited rows (later-phase bite caught — the cascade pattern). Rejected: the elaborate scope-anchored machinery (leak artifact); a HARD all-accept block (Jake chose warn+track+restate); a session-start advisory hook (deferred — subtraction test); auto-mutating a linked decision's confidence (debrief SURFACES the suggestion, the maintainer decides). | high | 2026-06-09 |
| [[assumption-surfacer-completeness-screen]] (Phase 80) Reframe the dev-plan human gate from approving the APPROACH (a conclusion the maintainer can't evaluate → blind-yes) to taking accept/reject/don't-know positions on the plan's load-bearing ASSUMPTIONS. The plan was interrogated LIVE via that mechanism (gate eating its own dogfood): Jake A1 accept / **A2 REJECT** ("can't trust the agent-CHOSEN assumption set — solve set-completeness before building anything") / A3 accept / A4 accept. A2 ⇒ Phase 80 is a SCREEN, not a build: validate a scope-anchored surfacer before the gate/ledger/all-accept block (Phase 81). An agent-internal adversarial review (6/10) found the CRITICAL hole — author-selected ground truth is CIRCULAR (a corpus holds only assumptions someone already noticed; blind exactly where A2 lives). Reframe: does the scope+framing surfacer BEAT a blind (outcome-unaware) baseline at recovering assumptions the project's history later PROVED load-bearing (ground truth = outcome-determined + mechanically extracted, NOT author-selected)? Structurally the amplifier's harness-vs-baseline question; `surfacer ≈ baseline` is a legitimate 5th-amplifier-null TERMINATE (pre-registered). Lead with a cheap circularity-escape SPIKE (T1) as a hard gate (INSTRUMENT-DEAD stops the phase for one task). Two-track verdict (scope completeness+recovery / framing recovery-only weaker), cost-sort-adversarial control (burial relocates to the cost-sort otherwise), variance-derived bar (not the inherited 4/5), entity-presence scoring (a require-ERE measures vocabulary not recovery). Cuts the handover's smoothness signature-detector (re-imports Ph66/69 representativeness + gameable) and the rewarded exogenous interrogator (deferred; surfacing still runs clean-context). Repo-only `eval/assumption-screen/` (amplifier program closed — new line). Labeling fork (mechanical vs Jake-adjudicates-delta) resolved by deferral (spike tests mechanical; adjudicate is fallback). Jake: "yes lets go" on the revised spike-first design. | low→pending | 2026-06-09 |
| [[harness-hygiene]] (Phase 79) Bundle two kit-hardening fixes into one "harness hygiene" phase (Jake chose bundled via AskUserQuestion). (A) Hook path resolution: the kit registers hooks with bare relative `.claude/hooks/X.sh`; Claude Code does NOT guarantee CWD=project-root, so consuming projects (edge-screener Stop-hook dogfood) hit `No such file or directory` though the scripts exist. Fix at source — register-settings.py + template emit `${CLAUDE_PROJECT_DIR}/.claude/hooks/X.sh` (project) / `$HOME/.claude/hooks/X.sh` (global) AND hook scripts `cd "${CLAUDE_PROJECT_DIR:-.}"` (command-fix alone leaves them found-but-inert); the unconfirmable live check becomes a committed hermetic wrong-CWD test. (B) working-knowledge size cap: the curator caps entry-count(100)/lines(210) not SIZE, so 4 amplifier mega-entries (~11k chars/24% of a 45k file) trip the context warning — compress them → 1 dense entry + dev-wiki pointers + a NON-DESTRUCTIVE per-entry char-cap advisory in wk-prune.sh (warn, never truncate/evict). Restores [[decision:memory-architecture-classification]] (terse always-loaded layer, detail in dev-wiki). 4th dogfood→harden fix (Ph74/75/76). Rejected: prune hooks/entries on value grounds (subtraction framing — deferred, hardens not shrinks); auto-truncate over-cap entries (destructive); defer A2 (Jake approved full). | high | 2026-06-04 |
| [[skill-crystallization-headroom-screen]] (Phase 78) Before building any capability→skill route (crystallize a phase's tooling into a reusable project-level skill), run a cheap, deterministic, verifier-independent SCREEN: does a candidate tooling artifact embed correctness a bare model CANNOT re-derive even given the RECOVERABLE CORPUS (interface + call sites + task) a non-crystallized consuming project would have, but not the implementation or its tests? Successor to the decision-retention line (Ph70/71/77 all null) — tests the capability/correctness boundary, the avenue the campaign repeatedly pointed at. OFF = clean subagent given only the recoverable corpus, NOT a lossy brief (review C2); scoring runs the artifact's HIDDEN spec-implied tests (NO-LLM `check.sh`, n=5/threshold=4 cloned from anchor-screen). Guards: `.offleak` leak-check (C4/M5); spec-implied = entailed-by-corpus with a QUOTED sentence per test (C3, mechanizes the partition, kills the Ph77 cherry-pick hazard); candidate correctness LOCATED+QUOTED from the test file not asserted in prose (C1 — `check-install-drift.sh` has NO /var canonical-path fix, that was Ph76 session-start.sh); 4 controls negative/positive-unknowable/recoverable-fully-specified/.offleak gate it (M3), any violation ⇒ INSTRUMENT-DEAD; UNSTABLE never votes toward TERMINATE (M2); pre-registration ancestor-guarded. Honest prior SPLIT: general tooling DEGENERATE, domain tooling (edge-screener PIT-survivorship, silently-wrong-on-re-derivation) HAS-HEADROOM — the split is the finding. Verdict gates the build (TERMINATE→don't; HAS-HEADROOM→build, scoped, + router reframe: skill vs regression-test/lint vessel). Repo-only `eval/amplifier/skill-screen/`, frozen. Design hardened by an agent-internal adversarial review BEFORE pre-registration. Jake chose "plan Phase 78 as that screen" after rejecting the report's two-domain specialization (thought-exercise only) and the low-prior process-retention sliver. | high | 2026-06-04 |
| [[cross-session-retention-headroom-screen]] (Phase 77) Run the amplifier program's last untested regime (cross-session decision/process retention) on the real READ-ONLY edge-screener substrate via an AUDIT-GATED ablation. The cheap T1 residual-audit gate (dev-wiki decisions over edge-screener real history minus everything recoverable from source tree + tests + `git log -p` commit bodies; each keyed on a pre-registered discriminating token ABSENT from OFF inputs AND still resolving at HEAD AND pinned to its terminal value) short-circuits to TERMINATE/INCONCLUSIVE before any expensive OFF/ON run. T2/T3 controlled ablation (bare-OFF / padded-OFF [Phase-59 length control] / ON / positive-control; n=5; frozen `retention-screen/` consensus-by-clause checker, NO LLM in scoring; pinned n≥3 floor committed BEFORE the residual count is consulted; ancestor-guarded pre-registration) runs ONLY on a residual ≥ 3. Honest prior: degenerate (git-log+code+tests already carry the decisions), one regime past Ph70/71. Positive control gates the null (INSTRUMENT-DEAD blocks a false TERMINATE); a null residual ≠ substrate worthless (its value may have precipitated into git via debrief discipline — caveat recorded). Repo-only `eval/amplifier/xsession-screen/`, frozen on completion; edge-screener byte-identical before/after. Rejected: (a) synthetic Ph71-clone (re-imports the self-measurement confound Phase 73 escaped); (b) naturalistic-only (no counterfactual — folded in as the residual pre-screen); (c) full ablation regardless of the pre-screen (risks confirming a foregone degenerate). Jake chose "audit-gated ablation" at the direction gate. | high | 2026-06-04 |
| [[installed-copy-drift-guard]] (Phase 76) Detect when the maintainer's installed `~/.claude` has drifted from the kit source (`templates/.claude`) — the kit develops in templates/ but RUNS from ~/.claude (bit twice: Phase-73 curator gap, Phase-75 stale debrief). DETECT-AND-WARN over symlink (`--link` rejected — live-WIP-breakage risk; copy-install safety preferred). Scope (A) maintainer-side; (B) consuming-project drift deferred. Deterministic `scripts/check-install-drift.sh` (kit-managed copy-verbatim files from modules.json minus a pinned bounded-count exclusion allow-list) → session-start advisory gated to the kit repo + `install.sh --status`. Fail-open, noise-scoped. Jake-approved ("A only"). | medium | 2026-05-31 |
| [[delivery-commit-verification]] (Phase 75) Detector-first fix for the accepted-but-uncommitted divergence the edge-screener dogfood exposed: a fail-open `session-start.sh` divergence detector (PRIMARY — fires independent of agent adherence; the skill-text delivery-flow D3 commit step was itself skipped, so more skill-text repeats the failure) + D3 commit self-assert + gate-after-commit ordering (SECONDARY). Deterministic-validator-at-the-boundary per the posture; reframed at the direction gate from "guarantee the commit fires" to "make gate-state diverging from git-state impossible to ignore". YAGNI-defers Stop-time catch + state-store drift + eval scenario. Jake-approved ("yes"). Shipped + pushed `add2ee7`, reviewer 9/10. | high | 2026-05-31 |
| [[harden-consuming-project-scaffold]] (Phase 74) Fix the 4 consuming-project scaffold defects surfaced by the edge-screener dogfood AT THE SOURCE: py-review Stop hook prompt→gated command (the loop fix), py-init recursive hook copy (curator gap), pyproject ruff/mypy out-of-box guards, AGENTS.md domain-neutralize. Deferred #5 (dev-init/dev-plan CWD-coupling) — skill-architecture, YAGNI. modules.json single-source + regenerate settings; kit make test is the gate. Jake-approved ("yes, do Phase 74 now"). | high | 2026-05-30 |
| [[cross-session-substrate-stock-screener]] (Phase 73) Cross-session-persistence substrate = a new EXTERNAL stock-screener project (`/Users/jwang/edge-screener`), scaffolded with the real harness so there is a genuine harness to measure, built across many real sessions — removes the self-measurement confound of P70/71. Jake-chosen at the `/dev-plan` direction fork (AskUserQuestion 2026-05-30: Build a real substrate → cross-session persistence → stock screener, defaults approved). Honest goal = un-foolable backtest apparatus not guaranteed alpha; cross-session measurement DEFERRED (can't measure recovery on session 1); data = free public (yfinance/Stooq) with loud survivorship/point-in-time guards. nana-dev-kit Phase 73 is thin: bootstrap substrate + record deferred-measurement design, then return to NONE. Screener Phase 1 = "Validated backtest ruler, before any edge search" (mirrors Phase 68). | high | 2026-05-30 |
| [[cash-compaction-recovery-subtraction]] (Phase 72) Cash the P70/71 conclusion — subtract the dead `.session-anchor` recovery machinery (post-compact.sh read-branch + .gitignore entry). Confirm-truly-dead first (exhaustive grep: only those 2 live refs; all else historical records). Subtraction over construction (the recovery pathway was measured headroom-free in P70/71 — wiring up a writer would add machinery the campaign proved inert). Historical [[hook-reconciliation]] superseded, NOT rewritten. gap 4.1 DEFERRED YAGNI. make test/eval green at unchanged surface (no registration/settings/README drift). Jake-chosen (Engineering→Tight subtraction, AUQ 2026-05-30). | high | 2026-05-30 |
| [[cross-boundary-retention-headroom-screen]] (Phase 71) Execute the Phase-70 surviving long-horizon/multi-turn process-retention avenue as a cross-boundary retention headroom SCREEN (the multi-turn analog of the anchor screen; Jake-chosen via AskUserQuestion 2026-05-30). SYNTHETIC boundary, model-authored (not hand-truncated) OFF residual + an isolated in-context `[HARNESS STATE]` increment (defeats both the OFF-starvation confound and the eval/comparison A-vs-C isolation scar). Differential two-n=5-batch verdict (DEGENERATE/HAS-HEADROOM/INERT/UNSTABLE; distinct INERT same-clause + distinct INERT-MACHINERY ladder rung); `check.sh` run_check/aggregate/verify_pins ported VERBATIM, diff_verdict new; NO LLM in scoring. A pinned motivated-vs-arbitrary rationale-probe litmus + human authorization gate CONTINUE (guards re-declaring the Phase-70 unknown-facts result in multi-turn clothing). Bare-derivation gate disqualifies model-default candidates first; pre-registration committed SEPARATELY before verdicts (anti-retrofit ancestor guard). Machinery FROZEN (the subject of measurement). Spec nana:approved; adversarially reviewed. | medium | 2026-05-30 |
| [[amplifier-anchor-headroom-screen]] (Phase 70) **RESULT: PROGRAM-VERDICT: TERMINATE.** Screened anchor EXISTENCE (inverting the Phase-69 order — anchor selection is upstream of measurement; predicate-repair-first was structurally blocked on the degenerate OFF ≥ ON anchor). Controls validated the instrument (neg→DEGENERATE, pos→HAS-HEADROOM, middle→STABLE); then all 4 candidates (structuring, UBO multiply-and-sum, sanctions transliteration, engineered-favorable EU AMLR €10k cap) screened **DEGENERATE 5/5** — opus does the AML reasoning unprompted; headroom appeared ONLY on the positive control's *fictional* fact. Per the pre-registered ladder (even engineered-favorable DEGENERATE ⇒ TERMINATE) the single-decision-anchor measurement line is closed. Finding: single-decision harness headroom lives only in *unknown facts* (a retrieval problem), extending the Phase-59 lesson. Surviving avenues: retrieval-on-proprietary + long-horizon (both with decidable-when). NO harness-value claim. User chose Approach A (AskUserQuestion 2026-05-30); all 5 adversarial-review fixes adopted (4/10→fixed). | high | 2026-05-30 |
| [[amplifier-representativeness-audit]] (Phase 69) A read-only recon ran the Phase-68 ruler over all 8 real consuming-project transcripts and FALSIFIED the handover's live-run plan: 8/8 `surfaced=false`, AUQ-scoped hits=0 (decision surfaces in prose not AskUserQuestion — v1 predicate is a structural false-negative), anchor DEGENERATE (look-ahead handled unprompted even in the harness-OFF baseline ⇒ no headroom). User chose Approach A (AskUserQuestion): turn the recon into the deliverable — NO live run, audit-only, do NOT patch the emitter. Ship a re-runnable `measurability-gate.sh` (NOT-MEASURABLE now; the Phase-70 trigger) + honest apparatus disposition (ruler is NOT yet a validated gate ⇒ comparison stays tombstoned, reasoning calibration-only, binary corpus sole gate). | high | 2026-05-29 |
| [[amplifier-measurement-instrument]] (Phase 68) Build Frontier 0 (the measurement instrument / ruler) BEFORE any cognitive frontier — the handover's dependency order was backwards and the escalation-classifier (Frontier 1) is unfalsifiable without a ruler. A deterministic READ-ONLY emitter at `eval/amplifier/emit-proxy-vector.*` ingests a REAL transcript jsonl (substrate, NOT enforcement.log — closes the Phase-66 schema-gap + clears the Phase-65 corpus-duplication bar) and emits a 4-group proxy-vector; validated by a positive/negative CONTROL pair (differential-flip), NO LLM judge (PARK if it can't pass deterministically). Instrument-validation (Phase 68, planted ground truth, n=1) is separated from harness-measurement (Phase 69, real consuming-project, n>1). | high | 2026-05-29 |
| [[hook-firing-coverage-gate]] (Phase 67) Make the functional-smoke invariant machine-checkable — a RED-first meta-test enforcing coverage over the UNION of `.hooks[]` (predicate `(.type//"command")!="prompt"`, NOT literal `command` which matches zero) + `extra_dirs/*.sh` curators; un-gameable via pinned exemption allow-list + denominator floor + negative control; invocation-anchored coverage (bare grep is empirically useless). Fix the GENERATOR, not the 3 known holes. Tests only. | high | 2026-05-29 |
| [[park-enforcement-scorer-signal-insufficient]] (Phase 66) The signal-richness gate FAILED (11 new-format records, 1 hook, 0 blocks, all self-traffic) — park the scorer behind a runnable probe-trigger, NOT a calendar note; 2 of 4 blockers are structural (representativeness: kit's own log ≠ consuming-project work; schema-gap: record = decision, not agent action). Redirect to the audit-log subtraction-test disposition. NO scorer code. | high | 2026-05-29 |
| [[audit-log-disposition]] (Phase 66) KEEP + harden audit-log — it functions + is eval-covered; `.nana/audit.jsonl` is a human-facing forensic trail (which model edited which file when), NOT code-dead deadweight; hardened raw-printf `$FILE_PATH`→`jq --arg` (fixed latent JSONL-injection) + added the functional-smoke the eval lacked. | high | 2026-05-29 |
| [[instrument-not-score-enforcement-firing-substrate]] (Phase 65) Build the enforcement-firing SUBSTRATE, not a scorer — a scored fixture-replay duplicates the corpus `lifecycle` category; the missing ingredient is real trace provenance, so instrument the 6 lifecycle hooks (fail-open) and defer the scorer. | high | 2026-05-29 |
| [[cut-heuristic-scoring-keep-articles]] Cut the never-fired scoring MACHINERY (matcher/judge/counter/lifecycle/dashboard + dev-plan Step 13 sub-items 6-7 + dev-debrief Step 7 heuristic-capture); KEEP the 17 articles + SCHEMA.md (test fixtures + eval/reasoning corpus). | high | 2026-05-29 |

## Blockers and Open Questions

- [planning: Phase-115 — A1 at-rest redaction completeness, revisit-status: held] Persisting the conversation to localStorage is a memory→disk regression vs today's volatile-only display: `redactSecrets` is pattern-based (PEM/sk-/AKIA/AIza/gh*_/xox*/JWT prefixes + opaque ≥32-char high-entropy), so a short/non-prefixed token, or a plaintext password in a written file's `content` arg, would persist DURABLY. RESOLVED THIS SESSION by down-scope (gate protocol): the maintainer chose 'strip high-risk bodies' — `redactForPersist` persists the structured thread but DROPS the write `content` body (keeps path + `'[N bytes]'` marker, the single largest secret-bearing field). RESIDUAL (accepted): short/plaintext secrets in bash commands / tool output that `redactSecrets` misses + the content-strip is write-only (an edit tool's `new_string` body is redacted-not-stripped and shares this residual) — same content the live DOM already showed, the delta is durability. **Confirm the residual is acceptable at the delivery gate** (T5 documents it inline + in the phase article; the fallback if unacceptable = persist text+tool-metadata only, the rejected max-safety option). (filed 2026-06-30)
- [planning: Phase-115 — display-only restore/swap is the no-bypass invariant] Restoring/swapping a saved thread must be DISPLAY-ONLY — pure `setMessages`, ZERO engine sends (no `sendPrompt`/`respondGate`/`engine_send`) — or a restored thread with tool calls could re-trigger them or desync the freshly-reset gate after a respawn. T4 pins a no-bypass test asserting the swap/restore issues zero engine sends. Confirmed at planning: the store is plain `useState` (chat-runtime.ts:36) fed only by onNew/commitEvent, so a swap is pure setMessages. (filed 2026-06-30)
- [planning: Phase-115 — workspace-keyed restore + the stale cross-workspace LEAK fix] The Ph114 respawn does NOT remount the surface (same `BridgeClient`, `App.tsx` never re-calls setBridge, `HarnessSurface` has no `key`), so today a workspace change LEAVES the prior workspace's thread visible against the new workspace's fresh gate — a stale cross-workspace LEAK (CONFIRMED in code), not a loss. So restore must trigger on `onWorkspace` (`currentWorkspace` is null until first ready, NOT at mount) and SWAP per workspace key. Phase 115 ALSO fixes this leak; success = thread SWAPS on workspace change + restart RESTORES. Knowledge gap (live-drive-only): the localStorage restart-restore round-trip is unverified at cargo-check/vitest level (Tauri WebKit/wry runtime; Ph109-114 native-runtime precedent) → maintainer live-drive confirms. (filed 2026-06-30)
- [open: Phase-113 — A1 the "supply creates demand" bet, revisit-status: open] Axis 3 (the command palette + keyboard control) is being BUILT ahead of the dogfood evidence the spec's scope-guard requires: `specs/gui-harness-architecture.md:80` gates every ported feature on a "used weekly" test, and [[felt-quality-surface]] A5 deferred the palette "until dogfood proves weekly use" — but the live window-drive that would surface that demand has been DEFERRED every phase 109-112. The direction gate RESOLVED this as Option A (build the palette as the bet that makes the daily loop reach-for-able), NOT Option B (gate the build on the live drive). The bet is the basis, not the evidence (stated honestly; decision [[axis3-command-palette]] Consequences). If the palette isn't reached for, that surfaces at the still-deferred live drive and Ph114 can deprioritize. Revisit at the live window-drive / Ph114. (raised 2026-06-27)
- [open: Phase-112 — T1 de-risking gate, revisit-status: open] The four planning forks are LOCKED at the assumption gate (decision [[os-sandbox-bash-fs-isolation]]): attach seam = Pi `baseToolsOverride` (fallbacks BashSpawnHook→BashOperations→gate-modify, A4); wrap layer = per-command TS chokepoint (`runSandboxedBash`, NOT the Rust sidecar — engine-neutral, blast radius small, A3/A4); profile/confirm-loop tension = C1-PRESERVE (approved out-of-ws write threads into `extraWrites`, A3 — needs the gate→executor approved-targets channel, T4); cross-platform = Ph111 string-gating remains the off-darwin fallback (Linux landlock/bwrap + Windows job-objects OUT → Ph113). The REMAINING risk is EMPIRICAL, front-loaded into T1: (A1) seatbelt write-deny INHERITS to subprocesses; (A4) Pi `baseToolsOverride` binds AND the tool_call gate still sees the ORIGINAL command (not the `sandbox-exec` wrapper); (A5) strict network/mach is viable for a basic shell + DNS. **STOP-and-escalate** if inheritance fails OR the Pi seam isn't substitutable through the fallbacks OR strict is unusable → pivot / fall back to integrity-only, do NOT build on a false foundation. `sandbox-exec` confirmed functional on this darwin box (exit 0). The Python MCP memory server is a SEPARATE process, NOT under the bash sandbox (its ~/.claude writes are unaffected). Ledger Phase-112 A1/A4/A5 revisit-status: open (confirm the T1 spike held at debrief). (raised 2026-06-27)
- [planning: Phase-111 — A1 typed-`details`-on-subscribe reachability, revisit-status: open] Phase 111 renders a structured diff from Pi's typed `details.diff`, but `tool_execution_end.result` is typed `any` and the strongly-typed `ToolResultEvent` (carrying `.details`) is an EXTENSION-HOOK event, NOT the subscribe stream — so reading `result.details` off the subscribe event is INFERENCE, not live-verified (Ph110's review confirmed `.content`+`.details` are ON the wrapper, but read it via the wrapper-string path, not `.details` typed). **T1 is a FRONT-LOADED live spike** (exact Ph110-T1 pattern — the wrapper-shape bug the all-string fixtures masked was caught this way); pin ≥1 test against the REAL Pi event shape. **Fallback** = register `pi.on('tool_result')` alongside the existing gate hook + correlate by `toolCallId`. **If details on NEITHER subscribe NOR a reliably-correlatable hook → STOP-and-escalate** (pivot Ph111 to a different dogfood gap, e.g. command palette), do NOT render empty. Ledger Phase-111 A1 revisit-status: open (confirm the spike held at debrief). (filed 2026-06-27)
- [deferred → future phases: Ph110 discovered residuals NOT taken by Ph111] Phase 111 takes only the typed-diff thread + pin-the-flaky-e2e from Ph110's "Discovered → Phase 111" list; the rest stay separate gated phases (A4 focused-scope fork): (a) **structured TestResults** — unreachable until a test-runner tool with typed `details` exists (no Pi built-in tool emits structured test details; bash-output parsing would reintroduce the heuristic class Ph110 removed); (b) **signed/notarized desktop bundle + the Phase-108 A3 keyring residual + one-click rollback** (a distribution concern, not a felt-quality render); (c) **command palette (axis 3)**; (d) **Claude Agent SDK adapter (key-gated)**. Re-trigger: pull any one forward at the next `/dev-plan`. (filed 2026-06-27)
- [deferred don't-know: Phase-108 A3 — keyring-rs + Tauri + agent-deny on macOS, revisit-status: open] Does keyring-rs (Rust crate wrapping the macOS Keychain) give OS-keychain store/retrieve under Tauri WITH the keys unreachable by the agent's file/shell tools? The maintainer could not pre-evaluate the Tauri-integration + agent-deny specifics without trying it → DEFERRED (not a silent pass). **T2 is an EARLY keyring-rs spike, BEFORE the key-custody rail is built on it** (a store→retrieve round-trip returns the secret AND the agent's file-read tool is denied the key-store path — `app/tests/security/key-store-deny`). Partial defense: keyring-rs is a mature crate. **If the spike fails → key custody moves to a separate non-agent-reachable credential helper, NEVER plaintext** (the spec's stated fallback). Revisit at the Phase-108 debrief. Ledger Phase-108 A3 revisit-status: open. (filed 2026-06-26)
- [open: Phase-108 — Pi `tool_call` hook un-bypassability is EMPIRICAL] Pi's `tool_call` extension hook as an un-bypassable in-process gate is an architectural INFERENCE (the hook exists + a real pi-permission-system extension uses it), NOT a Pi security guarantee. T3 is the make-or-break spike: deny a seeded `bash rm` (no side effect) AND a same-name shadow tool + an unregistered tool cannot suppress the host gate (`app/tests/security/{destructive-gate,gate-bypass-resistance}`). **If un-bypassable fails empirically → STOP-and-escalate** (swap primary to the Claude Agent SDK's vendor-guaranteed `canUseTool`, or build-the-loop on Vercel AI SDK, behind the same app-owned adapter). Ledger Phase-108 A1. (filed 2026-06-26)
- [open: Phase-108 A4 — Pi open-core risk] Earendil is VC-backed; RFC-0015 reserves future Fair Source/proprietary layers + a hosted platform. The MIT core (agent loop + `tool_call` gate primitive) is assumed to stay MIT. Mitigation = pin a Pi version + the engine-neutral app-owned adapter (Claude SDK / Vercel AI SDK) is the escape hatch. **If the core ever closes → pin-and-fork the last MIT version, or switch the primary adapter.** Re-trigger: a Pi release that gates the loop/hook behind a non-MIT license. (filed 2026-06-26)
- [deferred: Phase-99 A2 — direction dashboard live-server vs one-shot generator] The direction dashboard ships as a one-shot generator (`make direction` → `docs/direction.html`); the maintainer's A2 was DON'T-KNOW (generator vs an always-on watching server), DOWN-SCOPED to the generator — a strict subset: a future live server reuses `scripts/generate-direction.py`'s render function on file-change, so the choice costs nothing toward it. **Re-trigger:** regenerating-by-hand becomes a friction bottleneck (the dashboard is consulted often enough that a manual `make direction` after each dev-wiki/brief change is annoying) → build a watching local server that re-renders on change. Same class as deciding interactivity (Phase-99 A1: click-to-decide is the other deferred pillar). Ledger Phase-99 A2 revisit-status: open. (filed 2026-06-22)
- [RESOLVED 2026-06-22: check-tests-were-run make-test false-positive] The Phase-85 dogfood filing (the Stop hook false-blocking the kit's own non-pytest sessions) is RESOLVED by [[post-phase-99-cttwr-harden]]: the hook now accepts `make test`/`make eval` (flag/var tolerant, command-anchored) alongside `pytest`. An adversarial workflow also caught + fixed 4 latent false-block classes (jq line-tolerance, `\.py$` extension, legacy path/command split, make-flags). 18/18 controls-first; live-confirmed QUIET this session. (resolved 2026-06-22)
- [harden-candidate: jq line-tolerance across transcript-parsing hooks] check-tests-were-run was found to false-block when a malformed/truncated transcript JSONL line aborts a naive `jq -r 'filter' "$TRANSCRIPT"` (dropping every later event). Fixed there with `jq -rR 'fromjson? | …'`. **GENERAL:** any other hook that parses the transcript the same way shares this bug. Re-trigger: next hook-hardening round — audit `~/.claude`/templates hooks for non-tolerant transcript jq and apply `-R 'fromjson?'`. (filed 2026-06-22)
- [follow-up: consumer check-tests-were-run currency] All 7 consumers carry this hook but are pytest-only (no Makefile), so the make-test fix gives them no functional benefit; the jq-tolerance/`.py$`/command-scoping fixes are minor robustness gains. signal-watch's copy is also separately divergent (pre-existing). NOT propagated (surgical/blast-radius). Re-trigger: a periodic consumer-currency sweep, or a consumer adopts a Makefile test target. Propagate via `install.sh --update` per consumer (dry-run first). (filed 2026-06-22)
- [tripwire: Phase-98 rung-B frontier watch — WEEKLY + MONTHLY cadence] The two-speed watch is STOOD-UP but not yet fired (baseline = the Ph97 read). **Anti-dormancy obligation ([[HEU-012]] — a built-but-never-run watch is false security):** run the WEEKLY fast scan (`bash companion/research/watch/run-watch.sh <cycle.tsv>`) — first cycle due ~2026-06-28 — and the MONTHLY deep re-derivation — first due ~2026-07-21. On `TRIPWIRE-FIRED`, escalate per charter §4 (re-run the frozen `companion/research/pre-registration.md` 600e1c9f). This dated entry is the committed fallback trigger; a `/schedule` weekly+monthly reminder routine is OFFERED on top (maintainer to confirm at delivery). See `companion/research/watch/README.md`. (filed 2026-06-21)
- [planning: Phase-97→98 companion VC-durability — NOW ACTIVE] `companion/` is gitignored, so the kit repo does NOT track the research/watch history (pre-registration, findings, verdict, watch-charter, cycle logs). rung B has now LANDED (Phase 98 — the watch accumulates cycle history over time), so this question is live: decide local version control for `companion/` — a nested git repo vs loose gitignored files vs a sibling local repo. Same class as the Phase-96 consumer VC-durability item below. The watch deliberately keeps `companion/` gitignored (apparatus is private); VC is a separate decision — decide before the watch accrues many cycles. (raised 2026-06-21, updated Ph98 2026-06-21)
- [deferred: Phase-96 consumer VC-durability follow-on] The live consolidation is WORKING-TREE-only for the 3 git-tracked consumers (edge-analyst + ai-game committed `.claude/settings.json`; fate staged). Runtime is correct now (Claude Code reads the working tree), but HEAD/index still registers `detect-loop` + the old kit set, and the relocated kit regs live in gitignored `settings.local.json` (not under VC) — a `git checkout`/`reset --hard`/`clean`/fresh-clone reverts them; ai-game's committed settings.json also retains a duplicate `scan-secrets.sh`. **To make durable (per-consumer, each repo's own lifecycle):** commit a kit-clean `settings.json` + the `detect-loop.sh` deletion (`settings.local.json` stays gitignored — kit hooks are personal, re-created by `install.sh --update`); EXCLUDE the untracked `.claude/.migrate-backup.<ts>/` cruft (holds detect-loop.sh); fate's commit hook must be satisfied/bypassed; ai-game's 73 unrelated dirty files handled separately. Maintainer DEFERRED at the Phase-96 delivery gate (runtime correct regardless). Evidence: `eval/consumer-resync/rollout-evidence.md` "Version-control durability". (filed 2026-06-21)
- [RESOLVED by self-resync 2026-06-21] nana-dev-kit's OWN `.claude/hooks/` re-synced to current templates via the new `install.sh --migrate-to-local` (self-dogfood; gitignored .claude → no git impact): enforce-memory is now the Phase-95 REDESIGNED hook (the gameable `.memory-consulted` marker is gone), session-start + check-tests-were-run refreshed, zero hook-file drift, 17 regs consolidated to settings.local.json, check-install-drift --consumer CLEAN. Verified-by-firing (HEU-012): the redesigned hook correctly gated (blocked a stale search, allowed a fresh one). **FIXED 2026-06-21** — the freshness anchor is now PER-SESSION keyed (`~/.claude/.session-start-ts-<session_id>`, written by session-start.sh from the SessionStart event's `session_id`), with a fallback to the global `~/.claude/.session-start-ts` when no keyed file exists (never stricter than before). A CONCURRENT session can no longer advance another session's bound; `--resume` re-fires SessionStart with the SAME session_id so the keyed bound still advances on resume (freshness preserved). Both hooks edited (templates + live + global, byte-synced); 4 new keyed-anchor tests in test_tooluse_hooks.sh (25/25); make test PASS, make eval 50/50, drift CLEAN; verified-by-firing live (fallback path allows). Original finding: the GLOBAL `~/.claude/.session-start-ts` was advanced ~2h past a genuine earlier-this-session memory_search by a concurrent/resumed session-start, falsely blocking a real search. (Original: nana-dev-kit's installed enforce-memory was the pre-Phase-95 marker. resolved 2026-06-21)
- [SUPERSEDED — was: follow-on enforce-memory marker drift] This session was blocked until `.claude/.memory-consulted` was touched — the Phase-95 enforce-memory redesign (assert a real `memory_search`, ts >= session-start) shipped to `templates/` + `~/.claude` but the kit's own project-local `.claude/hooks/enforce-memory.sh` still uses the gameable marker. A self-drift; re-sync nana-dev-kit's installed copy (and `session-start.sh:110`'s vestigial `.memory-consulted` clear is load-bearing again until then). (filed 2026-06-21)

 - [RESOLVED by Phase 95 — Memory-Layer Disposition (verdict table: eval/memory-disposition/verdict-table.md)] The reconcile-and-dispose round closed every open memory-layer obligation: **memory-mcp-layer KEEP** (Phase-94 reversal — coerced demand value-bearing 2/3 consumers); **bridge-writer + harvest-writer KEEP-by-affirmation** (ledger Phase-95 A3; evidence-split asymmetry); **enforce-memory REDESIGNED** (the gameable agent-touched `.claude/.memory-consulted` marker replaced by a transcript assertion of a real memory_search with ts >= ~/.claude/.session-start-ts — det-vs-LLM Principle 2 + per-session freshness; firing audit ~55% per-episode value + 35-70% per-bite band; spike PASS; tests + eval 50/50 + drift 0; supersedes the Ph88 keep); **Ph88 trim-trials ak-ride-along (d43950f) + wk-seeding (df3e623) CONFIRMED** permanent (windows clean, zero triggers — window-events Phase 95 section). This RESOLVES the deferred entries below: Phase-88 A4/A6 (memory-layer + writers — now KEEP); Phase-83 A5 don't-know (ledger flipped open->held — layer earns its keep) + the Phase-83 enforce-memory keep-with-revisit (audit done, redesigned); the two trim-trial re-trigger entries; and the enforce-memory resume-artifact harden-candidate (the redesign's session-start-ts freshness bound fixes the resume stale-pass). Supersedes specs/phase-92-memory-layer-prune.md. (resolved 2026-06-21)

- [evidence-filed: Phase-94 memory-layer RE-MEASURE — Phase-95 input] The clean re-measure on WORKING memory is filed: `eval/memory-remeasure/memory-demand-remeasure.md` (verify-by-firing `VERDICT: FIRES`, repair-commit `318e9b6` window). RESOLVES the prior "re-measure on the now-working layer" backlog item (the Ph89/Ph83 consumer zeros were couldn't-fire, inadmissible). **Finding REVERSES the "consumer demand is zero" prior:** a 3-consumer machinery gradient on working memory — signal-watch (no rules/hooks) 1 search/0 stores/0 rows ≈ spontaneous FLOOR; aml-casework (rules) 10/17/20 rows, 10/10 cross-session read-back; aml-substrate (rules+hooks) 28/37/44 rows, 25 read-back. The coerced/announced layer (the Phase-95 cut target) is in active, **value-bearing** (cross-session read-back, not ritual) use in 2 of 3 live consumers. EVIDENCE ONLY — disposition is Phase 95, which must reconcile floor + coerced + read-back (the A2 sufficiency don't-know routes here; ledger Phase-94 A2 revisit-status: open). Likely points KEEP/refine, not shrink. (filed 2026-06-20)
- [RESOLVED by Phase 96 — Consumer Re-sync Rollout 2026-06-21 (eval/consumer-resync/rollout-evidence.md + run-exit-criteria.sh ALL-PASS)] All 7 consumers applied LIVE and verified: signal-watch onboarded+armed, edge-screener + aml-substrate refreshed (--update), edge-analyst/ai-game/fate/aml-casework consolidated off settings.json onto settings.local.json (--migrate-to-local then --update) with detect-loop deregistered (no ghost) and each kit hook registered exactly once (no DRQ-1 double-fire); drift CLEAN on all 7; armed={signal-watch,edge-screener,aml-substrate,edge-analyst,aml-casework}, unarmed={ai-game,fate} per A5. Group-B carry .migrate-backup.<ts>/ rollback dirs; pre-state WIP-committed per consumer (fate staged-not-committed, recoverable). The original deferred filing follows (DONE): `install.sh --update` was BUILT + SANDBOX-VERIFIED only (Phase 93 close 2026-06-18): the ADD/UPDATE + dedupe-by-basename + automated cut-hook dereg (backup → survivor smoke → revert-on-failure) path passed `tests/test_install_update.sh` (36 assertions incl. seeded cut-hook + duplicate controls, controls-first) but was NEVER run against a live consumer — ZERO consumer repos touched (`git diff` is kit-only). The destructive dereg is proven in mktemp sandboxes only. **Apply live as a gated follow-on, dry-run-first, per consumer:**
  1. signal-watch — onboard (zero kit hooks today): `cd signal-watch && ~/nana-dev-kit/install.sh --update --dry-run` then `--update` (routes through the full ADD path). Do NOT `--arm` unless Jake wants enforcement there.
  2. edge-screener / edge-analyst / ai-game / fate — backfill the current kit set + dereg the lingering Phase-88 `detect-loop` (the 17/18/19-hook spread): per consumer `--update --dry-run` (review the diff: adds enforce-assumption-gate where missing, deregisters detect-loop, dedupes any Phase-79 duplicate), then `--update`. The timestamped `.claude/.dereg-backup.<ts>/` is the rollback.
  - **Arm separately:** `--update` is decoupled from arming — `.claude/enforce` is left untouched. These 4 consumers were deliberately left un-armed (Phase 91); arm only on explicit per-consumer decision via `--update --arm` (or a manual `touch .claude/enforce`).
  - Pre-flight each with `~/nana-dev-kit/scripts/check-install-drift.sh --consumer <root>` (read-only) to see the drift before reconciling. (built+verified 2026-06-18; live application deferred)
- [deferred don't-know: Phase-91 A4, must-revisit] Whether Phase-90's codifiable behaviors "landed" is a real Phase-90 EFFECT (vs the user's native planning culture) is undefendable — no pre-90 opus baseline, and the dogfood eval found signal-watch fable exhibits the 5 behaviors natively. Plan DOWN-SCOPED to drop the "Track A effective" claim; Track 2 recovery treated as UNPROVEN. Revisit when a measure exists: the B2 pilot's controlled question-invalidation comparison, or a pre-90 harness-snapshot baseline re-plan. Ledger row A4 revisit-status: open. (filed 2026-06-14)
- [Phase-91 finding — reframes the Phase-92 memory blockers below] Root cause of "zero memory use in consumers" CONFIRMED this session: the MCP server is launched with the PROJECT cwd, but `memory_server` resolves only from a cwd that CONTAINS the package — only nana-dev-kit does (it is the kit's source repo). Every consumer → ModuleNotFoundError → server never starts → hook-forced memory_search fails → DB never created. So the Phase-83/89 "zero use" (below) is COULDN'T-FIRE, not demand — the prior `import exit 0` liveness probe gave false confidence (it tested import from a package-bearing cwd, not the live MCP launch in a consumer cwd). Phase-92's prune CANNOT judge demand until Track 1's PYTHONPATH fix + verify-by-firing lands. (filed 2026-06-14)
- [pending: Phase-90 B2 framing-subagent pilot] The clean-context framing subagent (`eval/fable-distill/framing-subagent-prompt.md`) is BUILT but NOT wired into dev-plan — deployment is gated on the pre-registered pilot in `eval/fable-distill/pilot-protocol.md` passing in a CONSUMING project (signal-watch, no Ph80 leak): ≥2/3 question-invalidation lift, zero leak-tells, no dilution regression. Until then the inline T0 + Track-B1 reframe-absorption beat are the shipped reframe mechanism. A negative pilot is a valid outcome (bounds where context-shaping recovers model-intrinsic behavior). (filed 2026-06-13)
- [resolved-into-spec: Phase-92 DRQ-1/2/3 → Phase-95 spec] The three Phase-92 planning DRQs (MCP-deregistration runtime behavior; other-consumers' CWD-resolved store disposition on a layer cut; file-substrate vs memory_store routing coverage) are subsumed by the nana:approved `specs/phase-95-memory-layer-disposition.md`: the layer KEEPS (Phase-94 reversal), so the cut-disposition DRQs become conditional-only and are governed by the spec's destructive-verdict constraints (`unreachable-installs:` over all kit-marker-discovered roots, ghost-registration class, Phase-84 method); the redesign feasibility (DRQ-1-adjacent) is the spike. Live again only if a destructive verdict fires at the enforce-memory checkpoint. (resolved 2026-06-20)
- [evidence-filed: Phase-89 A4/A6 memory-layer demand] Clean pre-registered demand evidence now exists: eval/dogfood-round/evidence/memory-demand.md — edge-screener tallies ALL-ZERO on a liveness-probed live layer with a THREE-WAY-clean zero (no hook coercion: enforce-memory fired-allow throughout; rules-instructed session-start search skipped — voluntary-skip pattern; no spontaneous use), and the pinned multi-session CONTINUITY case (the layer's design purpose) arose naturally and was served entirely by the .dev-wiki file substrate. Kit-side separately: writer LIVE (2 bridge stores, 1 same-session read-back), 1 hook-prompted block→COMPLY follow-through (first post-restoration Phase-83 distribution datapoint). Writer steps unreached in es sessions (session 1 stopped at its direction gate pre-15a-bis) — recorded as unreached, not declined. Disposition belongs to the future prune round (A4/A6 ledger rows). (filed 2026-06-11)
- [noise-candidate: py-review Stop-block on docs-only diffs — Phase 89 dogfood] In 2 of 3 evidence sessions, py-review.sh BLOCKED at Stop on diffs containing zero Python changes (markdown/state only), forcing a review cycle that correctly concluded "no code changes" each time. Demonstrated bite (one extra Stop cycle per session), correct outcome, wasted work. Harden candidate: skip when the session diff contains no .py files. Evidence: eval/dogfood-round/evidence/sessions.md S1/S2 rows. Re-trigger: next hook-hardening round. (filed 2026-06-11)
- [pending: edge-screener Phase-10 direction gate] Phase-89 session 1 ran a real /dev-plan in edge-screener, discovered its named candidate work was already shipped (a6effcb, via the Phase-87 episode arm A), reframed a retroactive Phase-10 plan, and STOPPED at the direction gate with 3 assumptions awaiting Jake's positions (headless sessions don't self-answer maintainer gates). Edge-screener lifecycle item, not the kit's: close it in an edge-screener session whenever Jake next works there. (raised 2026-06-11)
- [trim-trial: ak-ride-along — Phase 88, commit d43950f] The active-knowledge re-presentation machinery (dev-plan 15f-bis/ter, wiki-query 8a second writer, debrief Step 9/19 readers, dev-check S10) removed as a REVERSIBLE trim-trial. Revert = `git revert d43950f` (rehearsed, eval/trim-round/rehearsals/ak-ride-along.log). Trigger: a post-compaction recovery or planning decision demonstrably wrong for lack of phase-pinned knowledge. Window: through Phase 93 close. Re-trigger: at the Phase-93 debrief, take restore-or-confirm disposition. (filed 2026-06-11)
- [trim-trial: wk-seeding — Phase 88, commit df3e623] Debrief-side working-knowledge seeding removed as a REVERSIBLE trim-trial; wiki-query is now the sole WK writer; curator untouched, entries decay in place. COUPLED at the revert layer with ak-ride-along (the real debrief writer was the Step-19 carry-forward, removed at d43950f — full restore needs BOTH reverts; joint attribution acknowledged). Trigger: re-deriving a decision working-knowledge previously pinned, ≥2 times. Window: through Phase 93 close. Re-trigger: Phase-93 debrief disposition. (filed 2026-06-11)
- [deferred: Phase-88 gate A4/A6 rejects — memory-layer + bridge/harvest writers] The kit-side memory-MCP-layer demand disposition was REMOVED from Phase-88 scope at the direction gate (A4 reject: the edge-screener dogfood zero is NOT demand evidence) and the memory-bridge/harvest writer trims (debrief memory-harvest, dev-plan 15a-bis) were deferred with it (A6 reject: keep the writers alive so the future round gets clean demand evidence). The 20-surface reference enumeration defers too. Re-trigger: a future prune round with demand evidence beyond the dogfood zero. Ledger rows A4/A6. (filed 2026-06-11)
- [harden-candidate: enforce-memory resume-artifact — Phase 88 snapshot finding] Session-RESUME clears `.claude/.memory-consulted` though the session already searched (verified live twice in Phase 88's own session; 3 of 6 post-restoration marker-touch compliances trace to this class). Candidate: marker semantics distinguish resume from fresh session. Evidence: eval/trim-round/evidence/enforce-memory-snapshot.md. Re-trigger: next hook-hardening round. (filed 2026-06-11)

- [held: Phase-86 WK seeding] 3 cross-phase facts held until Phase-86 verdicts land (cache-adjusted token attribution ~10x overstatement; "no catches observed" invalid for rare-event subtraction / underpowered verdict-menu pattern; correlational on/off comparisons invalid on selection-biased corpora) — working-knowledge is itself under measurement this phase and over its size advisory. Re-offer at Phase-86 debrief; sources in [[ceremony-lift-tiered-screen]] + spec. (held 2026-06-10)
- [deferred: Phase-85 dogfood] check-tests-were-run.sh false-positive class CONFIRMED in real consuming-project use: Read calls on .py files trigger the "tests not run" Stop nudge on read-only analysis work — the nudge has demonstrated bite (the session re-ran the full 390-test suite to satisfy it, 3 Stop cycles; eval/install-gap/dogfood-evidence.md session 2). Harden candidate: trigger should key on Edit/Write of code files, not Reads. Re-trigger: next prune-on-value / hook-hardening round. (raised 2026-06-10)
- [evidence-filed: Phase-83 A5 must-revisit] Consuming-project memory-layer DEMAND evidence now exists: liveness-probed live layer (import exit 0), yet across 2 real edge-screener work sessions + 1 probe session ZERO voluntary memory-tool use; DB never created (row count 0). Evidence: eval/install-gap/dogfood-evidence.md. The A5 disposition (cut/keep) belongs to the next prune-on-value round. (filed 2026-06-10) — **Phase-88 gate outcome (2026-06-11): A4 REJECT** — the dogfood zero was ruled NOT demand evidence; the layer disposition is removed from Phase-88 scope and deferred to a future round with better evidence (the 20-surface reference enumeration and the vendoring-contract menu defer with it). Per the paired A6 reject, the memory-bridge/harvest writers stay alive until that round so it gets clean demand evidence.
- [deferred: Phase-84 A2 don't-know, must-revisit] Ghost deregistration safety (no-invisible-dependency-class) undefendable pre-matrix; gate evidence: ghosts load-bearing for 8 hooks in 3 roots. No live settings execution on direction-gate authority; T4a hard checkpoint decides (remediate-then-deregister default); ledger row A2 revisit-status: open. (raised 2026-06-09)
- [deferred: Phase-83 keep-with-revisit] enforce-memory kept on couldnt-fire evidence (lifetime zero = dormancy; confirmed firing live in Phase-83's own session, 69 records). The DEMAND question (does Jake want the memory nudge?) is now answerable with real data: revisit at the next prune round with the post-restoration firing distribution (allow/block ratio, block follow-through). (raised 2026-06-09)
- [deferred: Phase-83 stale-WK-entry] The working-knowledge entry "enforce-memory is OPT-IN (separate .claude/enforce-memory, NOT shipped...)" is contradicted by modules.json L39, which DECLARES ~/.claude/enforce-memory as a shipped marker (install.sh created it 2026-05-28). Supersede the entry (never rewrite history) at next curation. (raised 2026-06-09)
- [deferred: Phase-83 user-owned-ref] ~/.claude/rules/dev-wiki-hooks.md (user-owned, outside the kit's atomic boundary) references "/dev-harness H6" — no such skill exists; harness-audit.sh is now wired as `make audit`. Jake to update the rules file manually. (raised 2026-06-09)
- [deferred: Phase-83 A5 don't-know, must-revisit] Does the kit-side memory MCP layer itself earn its keep? Down-scoped out of Phase 83 (candidate 3 is scaffold-shipping only). Evidence pointing down: access counts ~0, reinforcement never fired, consuming project never calls it; evidence pointing up: harness design routes session-start search + bridge stores through it. Ledger row A5 revisit-status: open. Re-trigger: Phase-83 close-out filing review, or a prune-on-value round 2. (raised 2026-06-09) — **Phase-88 gate outcome (2026-06-11): A4 REJECT** — disposition NOT taken in Phase 88; the dogfood zero is not demand evidence; deferred to a future round needing better evidence. A6 REJECT keeps the bridge/harvest writers alive until then (no evidence-laundering: "no writes" after removing the writer would be manufactured). Phase-88 T6 files the updated layer+bridge deferral with the A4/A6 gate citations.
- [deferred: Phase-82 subtraction-review — usage area] Dead-weight list feeding the parked Phase-79 prune-on-value item, with utilization evidence in `eval/qa-sweep/` (matrix `usage` row + repro-runs.log): enforce-memory.sh (ZERO lifetime firings anywhere), memory reinforcement machinery (0 reinforcements across 55 entries), memory MCP unused in the consuming project (edge-screener never calls memory tools), audit-log model field always "unknown" (CLAUDE_MODEL never set), 3 orphan companions (dev-wiki/stale-queue-spec.md, knowledge-wiki/registry-schema.md, knowledge-wiki/session-context.md — pinned exemptions in test_companions.sh Direction C), harness-audit.sh (unwired). Re-trigger: a prune-on-value subtraction phase. (raised 2026-06-09)
- [deferred: Phase-82 firing] 4 confirmed-by-repro lower-severity hook candidates left unfixed by the pre-registered boundary: post-commit + detect-loop top-level exit-code dormancy, post-commit/detect-loop HOME-only marker resolution, test-harness CLAUDE_PROJECT_DIR sandbox escape. Evidence: eval/qa-sweep/repro-runs.log. Re-trigger: next hook-touching phase. (raised 2026-06-09) — **RESOLVED Phase 84 (2026-06-10)**: harness escape fixed (T1, seeded-leak control); post-commit redesigned + marker resolution fixed (T3, pinned repros 48/56 pass); detect-loop = upstream platform defect, see the Phase-84 platform filing below. SUPERSESSION NOTE: Phase-82/83 `make eval` greens were measured on the leaky instrument — the post-fix baseline showed ZERO flips (eval/hook-hygiene/eval-diff.md), so the leak was not load-bearing for any prior verdict; recorded, not rewritten.
- [deferred: Phase-82 drift] Ghost GLOBAL registrations: ~/.claude/settings.json still registers 11 project-scoped hooks from the pre-Phase-79 global-install era. Their runtime copies are now refreshed AND drift-compared (checker pass 2b), so they run current code — but deregistering them changes live wiring in every project (a maintainer call, not a QA fix). Also filed: installed-only skill residue inventory; checker-header memory_server omission note; dead settings.json EXCLUDE entry. (raised 2026-06-09) — **RESOLVED Phase 84 (2026-06-10)** for the 11 ghosts: maintainer approved the T4a checkpoint package; 6 roots remediated (copies synced incl. session-start.d, 11/11 project-local registration), 11 ghosts deregistered, survivor smoke green, end-state == modules.json scope:global set (eval/hook-hygiene/coverage-matrix.md + rehearsal.log; backup retained). The 3 minor sub-filings (skill residue, checker header, EXCLUDE entry) remain open.
- [deferred: Phase-84 platform] PostToolUse delivers NO failure signal for Bash: no exit-code field in tool_response, and NO event delivery at all for failing tool calls (two live probes; eval/hook-hygiene/capture-diagnosis.md). detect-loop.sh's consecutive-failure counter is structurally unimplementable hook-side. RESOLVED-BY-CUT (Phase 88, commit 75b48af, T4-approved): hook + registration + 2 eval scenarios removed, session-start.sh:110 split, own installed surfaces deregistered (rehearsals/detect-loop.log). Re-trigger STANDS: a platform release adding failure events or an exit-code field → restore via git revert 75b48af. (raised 2026-06-10)
- [deferred: Phase-84 install-gap] No install path ships hooks/session-start.d/ to ~/.claude (modules.json declares it only under project_local.extra_dirs; install.sh + check-install-drift.sh have zero references) — this made the Phase-82 refresh of session-start.sh BREAK every SessionStart machine-wide (registered+present+current-but-broken, 5th instance; repaired live 2026-06-10, matrix addendum). Fix install.sh extra_dirs handling + drift-checker directory currency. Re-trigger: next install/drift-touching phase. (raised 2026-06-10) — ADDRESSED by Phase 85 (in progress 2026-06-10)
- [deferred: Phase-82 misc] (1) generate-delivery-report.py functional smoke needs a stubbed `make` (M design); (2) wk-prune.sh cites an absent spec-precedence rule (low); (3) INSTRUMENT HAZARD: .dev-wiki/enforcement.log has no run-provenance separation — audit/test pipes into template hooks with CWD=repo write real records (observed 18:24-18:27Z scope-check contamination during this phase's own audit); any future log-based evidence must account for this. (raised 2026-06-09)
- [planning: Phase-82 knowledge gap] The agentic-engineering-wiki index references curated articles (verifier-quality, multi-agent-orchestration, agent-evaluation-and-testing) that do NOT exist as files on disk — only raw/ sources + knowledge.db. A wiki-health candidate, OUT of Phase-82 scope (the sweep audits the kit, not the knowledge wiki). Re-trigger: next wiki-health/wiki-reorg run, or a wiki-query that 404s on one of the three. (raised 2026-06-09)
- [planning: Phase-81 knowledge gap] The **accretion/budget-class residual** (where the Phase-80 naive surfacer cleanly MISSED — the line-cap class) is genuinely UNMEASURED. A clean test needs a CONSUMING-project context (the in-kit always-loaded `working-knowledge.md` leaks the answers — Ph66/69/80 representativeness). Deferred per the `eval/assumption-screen/screen-record.md` (a separate user call). Re-trigger: an accretion/budget assumption bites in real use AND a clean consuming-project substrate is available to measure it. (raised 2026-06-09)
- [planning: Phase-81 open risk] Whether the **debrief-finalization deterministic check is sufficient mechanical bite** for the (judgment-laden) revisit-status, vs a needed session-start advisory backstop. The advisory is deferred (subtraction test; the debrief-finalization check is the chosen firing point). Re-trigger: debrief is skipped in real use, leaving `unrevisited` rows undetected ([[HEU-012]] firing point) → add the deferred session-start advisory. (raised 2026-06-09)
- [deferred: Phase-79] Two items scoped OUT of harness hygiene ([[harness-hygiene]]): (1) **prune-on-value** — the subtraction framing (which of the 17 hooks / 96 working-knowledge entries actually earn their keep?) surfaced in T0 but was set aside; this phase HARDENED the existing layer, it did not shrink-by-deletion. Re-trigger: the harness stays heavy / a hook is found never-firing. (2) **edge-screener re-sync** — its `settings.json` was hand-patched to `${CLAUDE_PROJECT_DIR}` this session (interim); after this kit fix lands, re-run `install.sh` there so the durable template-sourced fix replaces the hand-patch (its `.claude/settings.json.bak` is the rollback). Also: a consuming session that resolves the project dir ITSELF wrongly is a Claude Code config issue the hermetic test cannot cover (documented known limit). (raised 2026-06-04)
- [deferred: Phase-76] Two items scoped OUT of the installed-copy-drift guard ([[installed-copy-drift-guard]]), recorded for re-trigger: (1) **(B) consuming-project drift** — `templates/.claude` vs a *consuming project's* `.claude/` (not the maintainer's `~/.claude`). A different problem: projects deliberately pin a kit version at scaffold time, so divergence is expected, not drift; detecting "meaningful" drift there needs a version/provenance model the kit doesn't have. Harder + currently unevidenced. YAGNI until it bites. Re-trigger: a consuming project is silently broken by a kit upgrade it never took. (2) **`install.sh --link` symlink dev-mode** — considered and REJECTED, not merely deferred: symlinking `~/.claude` → `templates/.claude` live means a broken WIP edit to e.g. `session-start.sh` breaks *every* session in *every* project at once; the copy-install model gives a stable last-good checkpoint and the drift is the price of that safety. The Phase-76 detector preserves the safety AND catches the forget — strictly better than `--link`. Re-trigger: the copy/resync cycle becomes a genuine friction bottleneck for kit development. (raised 2026-05-31)
- [deferred: Phase-75] Three items scoped OUT of the delivery-commit-verification fix ([[delivery-commit-verification]]), recorded for re-trigger: (1) **same-session Stop-time catch** in `session-stop.sh` — session-start (compaction-surviving) is the load-bearing detector point; a Stop nudge the just-failed agent generates for itself is marginal → add only if divergence recurs same-session; (2) **broader state-store drift** — in edge-screener `active-phase.md` never advanced to Phase 3 (the rules / git / dev-wiki stores drifted apart); separate symptom, YAGNI; (3) **lifecycle eval scenario** for the divergence detector — the `test_harden` firing test is the gate, eval is nice-to-have. Re-trigger: any of the three recurs in real consuming-project use. (raised 2026-05-31)
- [deferred: Phase-74 #5] dev-init/dev-plan resolve the project via CWD git-root, so they cannot be invoked to target a SIBLING project from another repo's session (forced the manual dev-init drive when bootstrapping edge-screener in Phase 73). DEFERRED — a skill-architecture change (would need a target-path arg), tangential to scaffold CONTENT, and the manual drive worked. Re-trigger: it recurs, or a workflow needs cross-project skill invocation. (raised 2026-05-30)
- [ADDRESSED by Phase-77] The exact OFF/ON cross-session recovery protocol (does a FRESH session WITH the persisted harness files recover decisions a bare fresh session loses?) was DEFERRED at Phase 73 until the `/Users/jwang/edge-screener` substrate accrued real multi-session decision history. Decidable-when NOW GREEN: edge-screener has 9 completed phases, 14 decision articles, 11 journals across 3 dates, 28 commits. **Phase 77 ([[cross-session-retention-headroom-screen]], spec nana:approved 2026-06-04) executes it** as an AUDIT-GATED ABLATION — resolving both sub-questions Jake left open at Phase 73: the controlled-vs-naturalistic fork is decided as BOTH (the cheap naturalistic residual-audit is the GATE; the controlled OFF/ON ablation runs only on a residual ≥ 3). Pre-registration follows the apparatus (campaign discipline). Resolved-when: Phase 77 writes a closed-vocabulary `^PROGRAM-VERDICT`. (raised 2026-05-30, addressed 2026-06-04)
- RESOLVED (Phase-71 planning, 2026-05-30): the 3 Phase-71 design questions (SUBSTRATE — synthesize vs forced-`/compact`; DETERMINISM — `check.sh` port + single-output checkability; CONFOUND — isolate the harness-state increment from the native summary) are resolved by `specs/phase-71-cross-boundary-retention-screen.md` (nana:approved): SYNTHETIC boundary with a MODEL-AUTHORED OFF residual + an isolated in-context `[HARNESS STATE]` increment; `check.sh` `run_check`/`aggregate`/`verify_pins` ported verbatim with `diff_verdict` new; `assert-off-on-isolation.sh` enforces ON = OFF + appended block byte-for-byte. (resolved 2026-05-30)
- [open: Phase-71 knowledge gap] Whether a model-authored compaction summary naturally DROPS a counter-default decision (a lossy boundary the screen can run on) or PRESERVES it (summary-robust) — determines whether the screen returns a candidate-level verdict or `TERMINATE-by-summary-robustness`. Resolved empirically at T2 (summaries generated + leak-checked) / T4 (lossy-boundary count). (raised 2026-05-30)
- [open: Phase-71 knowledge gap] Whether the pinned rationale-probe litmus cleanly separates MOTIVATED from ARBITRARY in practice, or real project decisions blur it (partially derivable) — and how a partial (2–3/5) probe result is recorded so it cannot quietly upgrade an arbitrary candidate toward CONTINUE. Resolved at T2 (probe runs + classification). (raised 2026-05-30)
- [Phase-70 ADDRESSES the anchor-existence item] Of the Phase-69 deferred set, Phase 70 now executes the **anchor-headroom screen** — "a valid non-commodity anchor passing the OFF-baseline headroom screen" — as the upstream go/no-go. The REMAINING items stay DEFERRED + GATED (predicate repair without raw-text collapse, the live off/on run n>1, Frontier 1): predicate-repair + live-run are unblocked only by a Phase-70 `PROGRAM-VERDICT: CONTINUE` (≥1 natural anchor HAS-HEADROOM) AND then `measurability-gate.sh` flipping MEASURABLE. A `PARKED`/`TERMINATE` verdict re-gates them on new priors / a multi-turn substrate (the deferred long-horizon class). (raised 2026-05-29, reframed 2026-05-30)
- RESOLVED (Phase 68): the 3 Phase-68 planning questions — exact transcript schema (T1 GO, no logging-hook pivot), redirect proxy computability (shipped as a structural `unsolicited_human_turns` proxy), deterministic keyword detector sufficiency (passed its planted control). The Phase-69 recon then surfaced that the detector, while correct on planted fixtures, is non-representative on REAL data — now the Phase-69 work. (resolved 2026-05-29)
- [Phase-66 scorer PARKED] The signal-richness gate was checked 2026-05-29 and returned **NOT-SCOREABLE**: only 11 new-format (`schema_version=1`) records in `enforcement.log` — all from one hook (dev-wiki-scope-check), ZERO action-changing `block` firings, all `phase:"65"` intra-phase self-traffic (the 253 enforce-loop + 23 block records are pre-instrumentation legacy format). Unbuildable for 4 reasons, **2 structural**: (1) volume; (2) skew; (3) **representativeness** — the log lives in the kit's OWN `.dev-wiki`, sampling the maintainer editing the kit, never the consuming-project agentic work enforcement governs; (4) **schema-gap** — the record captures the hook's DECISION, not the agent's subsequent ACTION (no firing→response link). `decidable-when`: re-run `scripts/signal-richness-probe.sh` (the committed gate) → SCOREABLE, AND the two structural blockers resolved first (real consuming-project provenance + a record schema that captures agent-response). Not a calendar note — a runnable predicate. See [[park-enforcement-scorer-signal-insufficient]]. (parked 2026-05-29)
- RESOLVED (Phase 64): the 2 Phase-64 design Qs (which working-knowledge entries describe removed-machinery-as-operational; whether Memory Harvest shares counter-write code with Heuristic Capture) — both resolved during Phase 64 execution (semantic prune via curator dogfood; Memory Harvest confirmed separate). (resolved 2026-05-29)
- RESOLVED (Phase 63): cut-vs-fix for the never-fired cognitive system → CUT the scoring machinery (DEADWEIGHT: 18 counters at 0/0, git -S empty ~13 phases), keep the 17 articles as fixtures. Executed in Phase 64. (resolved 2026-05-29)

- ~~Phase 58 residual research delta is +0.5 composite at n=1 (at significance threshold, topic-favorable). Kept Step 2.7 (Checkpoint 2). Phase 59 resolves the keep/trim/cut call.~~ **RESOLVED 2026-05-28 → CUT.** Strengthened with 3 new topics (13 paired runs at n≥3): poor −1.0 real harm, rich 0.0/−0.4 variance-dominated; the +0.5 sits inside the noise band. Feature removed; dev-plan reverts to Phase-55 behavior. (resolved 2026-05-28)

- OPEN: Active research's value on genuinely novel / post-training-cutoff / proprietary topics (weak parametric knowledge — research's theoretical sweet spot) is UNTESTED. Only well-documented domains were measured. A deliberate keep-for-novel-topics-only is a separate user call. (raised 2026-05-28) — Phase 61 reconfirmed: the weak-parametric + properly-absorbed + covered sweet spot remains unmeasured (would need an absorb pipeline + a non-commodity corpus first).

- OPEN: D2 re-test trigger (deferred, concrete numeric trigger not a present feature): if the MCP memory store ever grows past the hot-cache 100-entry cap with valuable DISTINCT entries, re-run the D2 A/B (memory_search as overflow recall for the evicted tail). As it stands the store (20 entries) is a strict subset of the hot cache. (raised 2026-05-29)

- Haiku/judge inter-run variance: mean ranges 2.97-4.85; recurred in Phase 59 (rich-topic spread 0.79–1.19, both variance-dominated at n=5). Cross-model judge / judge re-calibration remains a standing lever (deferred). (raised 2026-05-27, reconfirmed 2026-05-28)
- ~~Memory venv broken (recurring Phases 56-58): `make test` halted at `test_memory.sh`.~~ **RESOLVED 2026-05-28.** Root cause was twofold: (1) the optional `sqlite-vec` dep was absent from the (healthy, uv-built, Py3.13) venv — the `libpython3.11.dylib` symptom was stale from an older venv; (2) `test_memory.sh` forced `_vec_available=True` and hard-crashed instead of skipping when the *optional* dep was missing. Fixed both: installed `sqlite-vec==0.1.9` locally (full 11/11 run), AND guarded the 4 vec-requiring tests behind a one-time probe so they skip cleanly (FTS5-only 7/7) when sqlite-vec is absent — `make test` can no longer halt on a missing optional dep. (resolved 2026-05-28)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| `companion/research/youtube/**` (**NEW, gitignored, local-only — NEVER shipped; Phase 116 — YouTube→wiki grounded acquisition core (rung-B foundation); BUILT + adversarially reviewed**): 10 Python modules + 6 test files + README + requirements + a companion-local venv (uv; youtube-transcript-api 1.2.4 / yt-dlp 2026.6.9 / anthropic 0.115.0) — a manual-trigger acquire → chunk → extract → ground+consolidate loop that turns one Pi/agent-setup video into a grounded, provenance-carrying concept set (each concept tied to a verbatim, mechanically-verified transcript quote + timestamp), written to feed a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills. `acquire.py`+`normalize.py` (captioned-first VTT parse + WORD_FLOOR=200 dead-letter + entity-decode×2/NFC/rolling-caption-dup canonical norm; match-norm = casefold + curly/dash-fold), `chunk.py` (~1000-word/15%-overlap windows carrying the segment→time map), `extract.py`+`schema.py` (mocked-Claude `complete_fn` DI seam, extraction-by-selection, frozen immutable `Concept`, pre-flight cost ceiling), `ground.py`+`consolidate.py` (the LOAD-BEARING grounding rail — symmetric substring verify, calibration probe loud-abort on broken normalization, immutable-quote merge re-verifies the merged set), `ingest.py` (CLI) + `write.py` (atomic temp+`os.replace`, regression gate on grounded-quote count, markdown-neutralization, completion marker). NOT a shipped kit skill (the Ph97 local-apparatus-never-shipped precedent); the continuous half (RSS/scheduler/digest) deferred to nanaclaw. A 2-round adversarial review (5-lens finder + focused re-attack; 22 findings/5 HIGH; orchestrator-verified per [[HEU-012]]) → 5 HIGH + MED/LOW fixed inline with 16 regression tests; the re-attack caught the punctuation-strip fix re-opening cross-sentence splices → reverted to casefold + curly/dash-fold. RESIDUALS (accepted): A1 grounding catches FABRICATED not MISREAD quotes (routed to human wiki-review); A2 whisper deferred (uncaptioned dead-letter, rate at first live run); live multi-video first-run manual-drive (residential IP). NEW companion pytest suite 0→53 green (hermetic); `git check-ignore companion/research/youtube` confirms nothing shippable. NO shipped-kit change (`app/`/`templates/`/`install.sh`/`make test` untouched). [[youtube-grounded-acquisition]] (high). | 2026-06-30 |
| `app/src/ui/conversation-store.ts` (NEW) + `app/src/ui/chat-runtime.ts` + `app/src/App.tsx` + `app/tests/ui/{conversation-store,conversation-persist,workspace-swap}.test.ts` (**Phase 115 — Conversation memory: persist + restore the chat thread; BUILT + adversarially reviewed**): a bounded, structurally-redacted, PER-WORKSPACE conversation persisted to **localStorage** (keyed by `ready.workspaceRoot` via `bridge.onWorkspace`); restore on restart + **SWAP** on workspace change (also fixes the Ph114 stale cross-workspace leak — the respawn never remounts the surface). NEW pure module `conversation-store.ts` = `redactForPersist(UiMessage)→UiMessage` (redacts ALL string leaves incl. nested tool output + `details.diff`; STRIPS the write `content` body → `'[N bytes]'`; finalizes `done:false` turns + clamps tool `called`→`done`) + `save`/`load`/`clear` (shape-guarded parse, bound = last-N-turns + byte backstop prune-oldest, fail-soft `setItem` with prune-retry-then-SKIP). `useChatRuntime` gained an OPTIONAL workspace-source 2nd param (absent → persistence disabled; bare adapters/offline path unaffected) + persist-on-settle + first-ready restore + onWorkspace swap + clear-on-newConversation. Restore/swap is DISPLAY-ONLY (pure `setMessages`, ZERO engine sends — locked by a no-bypass test). The host gate + line protocol UNCHANGED (no engine types across the boundary). A 2-finder adversarial review (orchestrator-verified) caught + fixed 6 confirmed findings incl. **1 HIGH** (byte-backstop could prune a non-empty thread to `[]` then overwrite good history = silent total loss). HONEST RESIDUALS (A1, accepted): `redactSecrets` is pattern-based and misses short/plaintext secrets; the content-strip is write-only (edit `new_string` redacted-not-stripped); no cross-workspace LRU; the live restart-restore round-trip is live-drive-only (Tauri). app suite 343→374 green (+31); tsc + `npm run build` + `cargo check` exit 0. [[conversation-memory-persistence]] (high). | 2026-06-30 |
| `app/src/gate/sandbox/{seatbelt,approved-writes}.ts` (NEW) + `app/src/engine/{pi/pi-adapter.ts,vercel/vercel-adapter.ts}` + `app/src/gate/host-gate.ts` + `app/src/host/engine-host.ts` + `app/tests/{sandbox/seatbelt-spike,adapters/pi-sandbox,adapters/vercel-sandbox,gate/sandbox-approved-writes,gate/sandbox-no-bypass}.test.ts` (**Phase 112 — OS-sandbox bash filesystem isolation; BUILT + adversarially reviewed**): a per-command macOS seatbelt (`/usr/bin/sandbox-exec`) enforcement layer BELOW the Ph111 string-gate, at the bash EXECUTION site for BOTH adapters through ONE chokepoint — closes the documented Ph111 residual (string-gating arbitrary bash is incomplete by nature: `python3 -c`/`node -e`/`base64`/env indirection evade). NEW pure `gate/sandbox/seatbelt.ts` (`buildProfile` strict+integrity, `$TMPDIR` pinned from the TRUSTED host env, `escapeSBPL`+char-reject, `wrapBashArgv` argv-form, `runSandboxedBash` single chokepoint, `isSandboxAvailable` cached) + `approved-writes.ts` (the C1 per-command registry). Pi wired via `customTools`+`spawnHook` (NOT `baseToolsOverride` — the synchronous hook is DOWNSTREAM of `pi.on('tool_call')` so the gate sees the ORIGINAL command, P6 source-trace + live probe); Vercel `runBash` routed through the chokepoint; no-op off-darwin (the Ph111 string-gate remains). `host-gate.ts` exported `bashOutOfWorkspaceWriteTargets` + extracted `isOutsidePath`; `engine-host.ts` `onApprove` records approved out-of-ws bash writes (C1-preserve via per-command `extraWrites`, consume-once). NO `types.ts`/engine-contract change; the gate VERDICT-LOOP core ([[gate-confirm-approve-loop]]) UNCHANGED. T1 spike (49 tests) proved A1 inheritance / A4 Pi-seam + gate-sees-original / A5 strict-viable HELD; A2 BIT-partially (caches → integrity allowlist). An adversarial PoC review caught 2 confirmed whole-FS-grant bugs (canonicalize off-by-one `/w`→`/`; subpath-`/` over-grant) the 264 tests missed + 3 hardenings → +10 tests. HONEST RESIDUALS → Phase 113: cross-platform (Linux landlock/bwrap + Windows job-objects), confidentiality (deny-read) + daemon residuals, Vercel output redaction, `sandbox-exec` deprecation. app suite 201→274 green incl. live e2e; tsc + `npm run build` + `cargo check` exit 0. [[os-sandbox-bash-fs-isolation]] (high). | 2026-06-27 |
| `app/src/gate/host-gate.ts` + `app/src/engine/{types.ts,pi/pi-adapter.ts}` + `app/src/ui/{runtime,artifact-feed}.ts` + `app/tests/{gate/host-gate-coverage,adapters/pi-event-mapping,ui/artifact-feed,ui/typed-diff-pipeline,e2e/typed-details-roundtrip,e2e/provider-roundtrip}.test.ts` (**Phase 111 — Typed Artifact Fidelity + host-gate out-of-workspace hardening; BUILT + adversarially reviewed**): two threads in one session. (1) The typed-diff thread — `pi-adapter.extractToolDetails` normalizes Pi's `EditToolDetails.diff` to a neutral `{diff?: string}` (redact+cap), `types.ts` `tool-result` EngineEvent + `runtime.ts` `SurfaceToolCall` gain additive optional `details?:{diff?:string}` (JSON-serializable, Vercel/noop/host untouched), `artifact-feed.toArtifact` routes the typed-diff branch FIRST (wins over Ph110's `looksLikeDiff` heuristic — Pi's header-less `+`/`-`/space diff format would MISS the heuristic). A1 RESOLVED HELD via a live local-model edit (the `pi.on('tool_result')` fallback NOT needed). (2) The SECURITY fold-in (ledger A5, maintainer-directed) — `host-gate.ts` base-policy COVERAGE extended: default-branch `PATH_ARG_KEYS` `isOutsideWorkspace` check (alt-named write tools) + bash-branch `extractBashWriteTargets` (redirect/`tee`/`cp`/`mv`/`dd` out-of-workspace targets) + a `DEV_SINK` exemption (`2>/dev/null` not over-blocked); confirmable not hard-deny; gate VERDICT-LOOP core ([[gate-confirm-approve-loop]]/key-store hard-deny) UNCHANGED. Closed the demonstrated + common bash/alt-name vectors deterministically (`tests/gate/host-gate-coverage.test.ts`, 24 cases) + corrected the Ph108 "empirically un-bypassable" over-claim. An adversarial finder×refuter review caught a `/dev/null` over-block + a `>|` force-clobber evasion the 24 gate tests missed. HONEST RESIDUAL: string-gating bash incomplete by nature → OS-sandbox follow-on (Phase 112). app suite 157→201 green incl. all live e2e; tsc + `npm run build` + `cargo check` exit 0. [[typed-artifact-fidelity]] (high) + [[host-gate-out-of-workspace-hardening]] (high). | 2026-06-27 |
| `app/src/engine/{pi/pi-adapter.ts,types.ts}` + `app/src/ui/{runtime,chat-binding,chat-runtime,artifact-feed}.ts` + `{tool-call-view,artifacts}.tsx` + `App.tsx` + `app/src/security/redact.ts` + `app/src-tauri/icons/**` + `app/assets/icon-source.png` (**Phase 110 — Dogfood UX Pass: tool-call visibility + artifact fidelity + branding; BUILT + adversarially reviewed**): a single vertical thread source→surface threads real Pi tool args/output through the EXISTING engine-neutral layers ADDITIVELY (no engine-type reshape) into the Ph109 components that were built ahead of the data. `mapPiStreamEvent` pure seam + `extractToolText()` (normalizes Pi's `AgentToolResult` WRAPPER `{content:[{text}],details}`→text at the adapter boundary, redacts-then-caps ~16KB); additive `SurfaceToolCall` args?/output?/isError? + reduction; NEW additive `tool-progress` EngineEvent variant (`tool_execution_update.partialResult` in-flight streaming); NEW pure `ui/artifact-feed.ts` router (edit→diff/bash→terminal, generic-text fallback per A3); `ArtifactPanel` in `App.tsx` (`useChatRuntime`→`{runtime,artifacts}`); `redact.ts` reworked (specific AWS/GCP-`AIza`/JWT patterns + a hash-exempting base64 catch-all); regenerated Nana icon set. The gate CORE ([[gate-confirm-approve-loop]]/key-store hard-deny) UNCHANGED. An adversarial pre-commit review (6 finders × 2 refuters; 15 raised → 6 confirmed → all fixed) caught the HIGH wrapper-shape + redaction gaps the 142 string-fixture tests MASKED. app suite 108→142→157 green; tsc+`npm run build`+`cargo check` exit 0; ledger A1/A3 RESOLVED (Pi populates in wrapper form, typed `details` confirmed-available → Ph111). [[tool-call-visibility-thread]] (high). | 2026-06-27 |
| `app/src/{host,gate/confirm,context,ui}/**` + `app/src-tauri/src/lib.rs` + `app/src/App.tsx`+`styles.css` (**NEW, Phase 109 — the drivable felt-quality surface**): `host/` (engine-HOST sidecar `engine-host.ts`+`main.ts` over a JSON line protocol — prompt/gate-verdict/revert/interrupt in; engine-event/gate-pending/revert-result/ready/error out), `gate/confirm/` (`broker.ts`+`confirming-gate.ts`+`diff.ts` — real human-confirm approve-loop on the existing `setToolCallGate` seam; only the literal "requires explicit human confirmation" marker is confirmable, key-store denials stay hard-denied), `context/assembly.ts` (workspace AGENTS.md/CLAUDE.md + `.claude/rules/*.md` → per-turn system context, loud-on-missing), `ui/` (`Thread.tsx`, `chat-runtime.ts`, `chat-binding.ts`, `tool-call-view.tsx`, `gate-confirm.tsx`, `diff-view.tsx`, `artifacts.tsx`, `engine-bridge.ts` BridgeClient-as-EngineAdapter), `lib.rs` (Rust spawns the node sidecar + `engine_send` command + stdout→`host-message` events → webview manifest stays `core:default` + CSP `connect-src 'self'`). assistant-ui (`@assistant-ui/react`) bound with ZERO engine reshape; esbuild ESM host bundle (`build:host`, `npm run app`). app tests 63→108 green; tsc+vite+cargo check exit 0. [[felt-quality-surface]] + [[webview-engine-bridge]] + [[gate-confirm-approve-loop]] (all high). LIVE end-to-end round-trip UNVERIFIED (maintainer's deferred launch check). | 2026-06-26 |
| `app/**` (**NEW top-level dir, Phase 108 — THE PIVOT**): Tauri Rust shell `app/src-tauri/` (+ standalone `keyhelper/` crate for keyring custody) + TS/React surface `app/src/` — `engine/` (EngineAdapter + canonical types + Noop/Pi/Vercel adapters + normalize + shared event-queue), `gate/` (host-gate + Pi gate-bridge + checkpoint/external-mod rails), `memory/` (MCP stdio mount of the UNTOUCHED Python memory server), `security/` (secret-deny + redact + keychain accessor), `fs/` (agent-file-read tool), `ui/` (inert renderer + engine→surface runtime + strict CSP), `control/` (spend ceiling + hard interrupt); 15 test files `app/tests/`. The GUI-primary, model-agnostic desktop dev harness: engine embedded IN-PROCESS behind the app-owned adapter, security gate at the tool-call site. 63/63 tests green (incl. live); `cargo build` exit 0 (23.5MB). [[engine-adapter-in-process-gate]] (high), [[provider-defaults-to-local-model]] (high), [[second-adapter-vercel-ai-sdk]] (high). | 2026-06-26 |
| `companion/research/watch/` (**gitignored, local-only — NEVER shipped**): `watch-charter.md` (the FROZEN instrument / spec for this private phase), `.frozen` (SHA256 attestation 8ec4af7b — anti-retrofit seal), `run-watch.sh` (controls-first fast-scan detector: classifies each tripwire MET/UNMET vs the charter delta rule → `WATCH: QUIET \| TRIPWIRE-FIRED \| ESCALATE`; `--selftest` + charter-drift guard), `fixtures/**` (2 seeded tripwires + 2 false-positive controls), `README.md` | NEW (Phase 98) — Frontier Watch, rung B of the companion de-risking ladder. The standing **two-speed tripwire watch** that keeps the Ph97 differentiated-leaning verdict fresh: WEEKLY broad-source/narrow-signal fast scan + MONTHLY deep re-derivation backstop + on-hit escalation to the frozen `pre-registration.md` 600e1c9f. `--selftest` PASS (both seeded tripwires FIRE, both controls QUIET, drift-guard exit 3). DETECTS-AND-FLAGS only — NO cut. Dated Blockers tripwire = the committed scheduling fallback (`/schedule` offered, maintainer to confirm). A macOS awk `\|`-alternation classifier bug was found + fixed here (caught by the seeded-positives — clean-on-seed, [[HEU-012]]). ZERO shippable-kit change; `git ls-files companion/` empty. [[frontier-watch]] (high). | 2026-06-21 |
| `companion/research/` (**gitignored, local-only — NEVER shipped**): `pre-registration.md` (the FROZEN decision instrument; serves the spec function for this private phase), `.frozen` (SHA256 freeze attestation seal), `sweep-findings.md` + `convergence-map.md` (cited evidence + rubric classification), `verdict.md` (mechanical verdict + half-life→cadence) | NEW (Phase 97) — Frontier Positioning Sweep, rung A of the de-risking ladder. The EXTERNAL arm of the Ph92 re-measure: a pre-registered one-shot read of where the agent-harness frontier is converging, read MECHANICALLY against a rule FROZEN before any research. **VERDICT = INCONCLUSIVE-forced-under-observed (differentiated-leaning)** — K_low=0 / K_high=1 (B5 boundary-validator CONTESTED, sole FOR = OpenAI Pydantic tool-validation); CORE (B1 blocking gates + B4 assumption gate) UNCOMMODITIZED; INCONCLUSIVE FORCED by the rigorous rule's any-contest-bars-DIFFERENTIATED clause on one non-core primitive (realized rigor/legibility cost). NET: external frontier gives NO case to shrink the bet; both arms of the Ph92 re-measure now agree (with Phase-95 memory KEEP). Freeze = SHA256 attestation (git ancestry can't guard a gitignored file). Analogous to the `eval/` measurement programs but GITIGNORED + never installed. ZERO shippable-kit change. [[frontier-positioning-sweep]] (high). | 2026-06-21 |
| `templates/.claude/hooks/{enforce-memory,block-dangerous-bash,session-start}.sh` + `scripts/check-install-drift.sh` (`--consumer` content-currency) + `tests/test_tooluse_hooks.sh` (43) | Post-Phase-96 hook hardening (2026-06-21): enforce-memory freshness anchor per-`session_id` keyed (global fallback); block-dangerous-bash matches dangerous TARGETS precisely (absolute/home/parent/cwd — relative subpaths allowed; reversed-flag false-negative closed); check-install-drift --consumer flags `stale: <hook>` on content drift (was registration-only). Synced to templates + global ~/.claude + all 7 consumers via `install.sh --update`. [[post-phase-96-hook-hardening]] (high). | 2026-06-21 |
| `install.sh` (`--migrate-to-local`) + `scripts/check-install-drift.sh` (`--consumer` settings.json-topology detect + global-hook cut-exclusion) + `tests/test_install_update.sh` (54) + `eval/consumer-resync/` (`rollout-evidence.md`, `run-exit-criteria.sh`) | NEW (Phase 96) — consumer re-sync rollout: `--migrate-to-local` consolidates `settings.json`-topology consumers onto gitignored `settings.local.json` (relocate kit regs + dereg kit/cut hooks from settings.json + **ship hook files**; both-file backup → survivor smoke → revert; `--update` UNCHANGED). Applied LIVE to all 7 consumers — working-tree state: 17-hook kit set in settings.local only, settings.json kit-clean, detect-loop ghost-free, drift CLEAN; armed={signal-watch,edge-screener,aml-substrate,edge-analyst,aml-casework}. `run-exit-criteria.sh` = NO-LLM live close-out verifier (asserts no DRQ-1 dup). Adversarial review (28 candidates) → 3 code fixes + the **version-control-durability caveat** for the 3 git-tracked consumers (working-tree-only; committing per-consumer = the durability follow-up). [[consumer-resync-rollout]] (high). | 2026-06-21 |
| `eval/memory-disposition/` (`verdict-table.md`, `run-exit-criteria.sh` +`--selftest`, `tally-firing.py`, `spike-probe.sh`, `enforce-memory-audit.md`, `redesign-spike.md`) + `templates/.claude/hooks/enforce-memory.sh` (REDESIGNED) | NEW (Phase 95) — memory-layer disposition: verdict table (memory-mcp-layer + writers KEEP; enforce-memory REDESIGNED; trim-trials CONFIRMED), column-2-anchored runner with controls-first `--selftest`. `tally-firing.py` = enforce-memory firing-distribution audit (`.dev-wiki/enforcement.log` + transcript correlation, JSON `tool_use` never grep, SAME-SESSION attribution + window BAND + episode view after the adversarial review caught window-gaming; ~55% per-episode value). `spike-probe.sh` + `redesign-spike.md` = the redesign feasibility spike (PreToolUse `transcript_path` confirmed, 64ms, SPIKE: PASS). The hook now asserts a real `memory_search` (ts >= `~/.claude/.session-start-ts`) instead of the gameable marker — jq-only, fail-open; 5 paired tests + 3 firing-log tests updated; eval scenarios moved to the transcript contract (denominator unchanged). [[memory-layer-disposition]] (high). | 2026-06-21 |
| `eval/memory-remeasure/` (`verify-firing.sh`, `tally-demand.py`, `fixtures/**`, `memory-demand-remeasure.md`) | NEW (Phase 94) — clean RETROSPECTIVE consumer memory-LAYER demand re-measure on the WORKING layer (Ph91 repair), EVIDENCE ONLY. `verify-firing.sh` = HARD admissibility gate (`VERDICT: FIRES` in a consumer cwd; broken-config control COULDN'T-FIRE). `tally-demand.py` = JSON `tool_use` parse (never grep; reads `<session-uuid>/subagents/agent-*.jsonl` separately from the top-level session file), 3-class taxonomy + cross-session read-back + attempted-vs-satisfied, selftest + verify-ingest controls. Evidence file (Ph89 schema, EVIDENCE-ONLY + NO-SUFFICIENCY, repair-commit `318e9b6` window) documents the machinery gradient that **REVERSES** the "consumer demand is zero" prior: persisted DB rows signal-watch 0 (floor) / aml-casework 20 / aml-substrate 44; the coerced layer is in value-bearing read-back use in 2 of 3 consumers. NOT in make test/eval (phase-evidence, not registered). Phase 95 disposes. [[consumer-memory-remeasure]] (high). | 2026-06-20 |
| `install.sh` (`--update [--arm]`) + `scripts/register-settings.py` (`--dedupe`, `deregister`) + `scripts/check-install-drift.sh` (`--consumer`) + `modules.json` (`cut_hooks`) + `tests/test_install_update.sh` + `eval/install-update/` | NEW (Phase 93) — idempotent consumer re-sync: `--update` reconciles project-local hooks + settings.local.json to the current kit (ADD/UPDATE + dedupe-by-basename + automated cut-hook dereg behind timestamped backup → survivor smoke → revert-on-failure; arming DECOUPLED, `.claude/enforce` untouched unless `--arm`). First deregistration mechanism the kit ships (register-settings.py was upsert-only); two-tier cut handling (LIBERAL warn / CONSERVATIVE `.cut_hooks` removal). 37-assertion controls-first harness (seeded cut-hook + duplicate-registration MUST be caught); programmatic fixtures from live modules.json. BUILD + SANDBOX-VERIFY ONLY — ZERO live consumer writes (live application = gated follow-on, Blockers). [[install-resync-update-mode]] + [[deregistration-as-register-settings-subcommands]] (both high). | 2026-06-18 |
| `.dev-wiki/articles/decisions/strategic-inflection-review.md` + `deterministic-vs-llm-boundary.md` + `articles/phases/phase-92-strategic-inflection-review.md` | NEW (Phase 92) — the inflection review's durable output: the **product-for-consumers** frame + **re-measure-once-then-shrink** direction (5 synthesis overclaims corrected by the workflow's adversarial critique — incl. the Ph87 det-beat-reviewers binary-file confound) + the 4 deterministic-vs-LLM boundary principles & kit map (adopted as a maintenance guideline, linter-determinization deferred-until-observed-defect). Re-sequenced roadmap 93→94→95. [[strategic-inflection-review]] + [[deterministic-vs-llm-boundary]] (both high). | 2026-06-18 |
| `templates/.claude/hooks/enforce-assumption-gate.sh` + `tests/test_enforce_assumption_gate.sh` | NEW (Phase 91, Track 3) — the 18th project-scoped hook: PreToolUse Write\|Edit\|MultiEdit, mirrors enforce-spec, BLOCKS implementation writes (exit 2) when the active phase has no valid assumption-ledger block (`check-assumption-ledger.sh --gate` ONLY — whole-file `--schema` false-locks properly-gated projects with prior-block drift, verified on aml-substrate). Both-landings (templates + ~/.claude); 7 firing cases. The gate is now hook-bound, not prose-only. [[assumption-gate-hook-binding]] (high). | 2026-06-14 |
| `scripts/register-settings.py` (cmd_mcp env) + `modules.json` (mcp `env`) | NEW (Phase 91, Track 1) — the memory MCP registration gained an `env` `{PYTHONPATH: $HOME/.claude}`; `cmd_mcp` now emits env (was command/args/cwd only). Root cause: `python -m memory_server` resolves the package only from a package-bearing cwd (only the kit) → server DEAD in every consumer (rc=1 from a consumer cwd, rc=0 with PYTHONPATH). Landed in BOTH `~/.claude/settings.json` AND the authoritative `~/.claude.json`; verified by FIRING a consumer-context memory_search (DB created). [[memory-mcp-consumer-e2e-fix]] (high). | 2026-06-14 |
| `eval/fable-distill/` | (Phase 90) — fable-5 distillation: `findings.md` (paired-transcript contrast + codifiable-vs-intrinsic split + sgup/ai Fable5.md convergence), `framing-subagent-prompt.md` + `pilot-protocol.md` (Track B2 clean-context framing subagent, STAGED + pilot-gated — NOT wired). Track A (nana-soul.md +5 forcing-functions) + Track B1 (dev-plan reframe beat) live in the global+template copies. [[fable-distillation-round]] (high). | 2026-06-13 |
| `eval/dogfood-round/` | NEW (Phase 89) — pre-registered demand-evidence apparatus: `pre-registration.md` committed FIRST (45460bc, append-only Addendum 1) + 7 deterministic checkers with seeded-failure selftests, `evidence/` (header, liveness-probe, sessions [3 real headless edge-screener sessions], `window-events.md` CROSS-PHASE accumulator through Phase 93, `memory-demand.md` A4/A6 evidence — es tallies three-way-clean ZERO, kit-side writer LIVE, prompts/), `rehearsals/`, `run-exit-criteria.sh` ALL-PASS 10/10. Evidence only, never disposition. | 2026-06-11 |
| `eval/trim-round/` | NEW (Phase 88) — trim-round durable record: verdict table (10 rows, phase-base SHA header, executed rows with revert SHAs) + `check-verdict-table.sh`, evidence re-snapshots (enforce-memory provenance-filtered log + follow-through reconstruction SUCCEEDED; active-knowledge reader enumeration), `checker-fixtures/` + `run-seeded-controls.sh` (14/14) + `check-stage2-allowlist.sh` + `check-ghost-registrations.sh`, `rehearsals/` (per-candidate revert SHAs), `run-exit-criteria.sh` ALL-PASS. Outcome: [[trim-round-outcome]] (high). | 2026-06-11 |
| `eval/ceremony-lift/stage2/` | NEW (Phase 87) — the stage-2 episode contrast's frozen durable record: `spike/` (drivability, pty mechanics), `execution-protocol.md` (addendum, committed before any arm; first-add-commit ancestry + byte check), `amendments/001-003` (pre-unblinding deviation mechanism), checkers + 18 seeded controls (`check-instrument.sh`, `check-ship-table.sh`, `check-cost-table.sh`, `check-claim-ceiling.sh`, `check-substrate-intact.sh`), `instrument-record.md` (pre-arm 10/10 + full 29/29; canary CLEAN, positive control both-arms), `arm-records/` (B FINISHED 996s / A DNF 2,106s), `ship-table.md` + `cost-table.md` + `results.md` (claim ceiling verbatim; arm B ship-blocked, arm A shipped a6effcb), `run-exit-criteria.sh` (8/8 RUN-STATUS LIVE). Disposition in the phase article + [[stage2-episode-outcome]] (high). | 2026-06-10 |
| `eval/ceremony-lift/` | Phase 86 stage-1 screen (19 files, verdicts-only): byte-FROZEN `pre-registration.md` (9ad62f0), cost extractor + 13-check control, cost table (ceremony ~66% adj tokens / ~64% wall over 12 sessions), blind-controlled evidence table + re-execution log (1 outcome-grade / 10 ambiguous-downgrade / 8 consumption-capped / 25 zero-catch), `check-verdicts-only.sh`, `run-exit-criteria.sh` 8/8. Verdicts: [[ceremony-step-verdicts]] (high). | 2026-06-10 |
| `eval/install-gap/` + `tests/test_install_drift_dircurrency.sh` | Phase 85 — install-path inventory (causal-path confirmed), DRQ-1 string-keyed-dedupe verification, dogfood evidence (A5 zero-demand filed), 9/9 exit criteria; modules.json `hook_dirs` map + install.sh `ship_hook_dirs` + checker directory cells (7 seeded controls, 26th make-test script). [[install-gap-dir-currency]] (high). | 2026-06-10 |
| `eval/hook-hygiene/` + Phase-84 test scripts | Phase 84 — PostToolUse capture diagnosis (NO exit-code field; no failure-event delivery), post-commit redesign, 11-ghost deregistration record, 10/10 exit criteria; `tests/test_eval_hermeticity.sh` + `test_fixture_provenance.sh` + `test_lifecycle_hooks_firing.sh` + real-event fixtures (23rd-25th make-test scripts; hermetic eval-runner). [[hook-registration-hygiene]] (high). | 2026-06-10 |
| `eval/prune-on-value/` | Phase 83 — 6-row verdict table (2 cuts / 2 keeps / 2 hardens), liveness greps over 7 discovered roots, couldnt-fire arming evidence, 10/10 exit criteria; `make audit` wired. [[prune-on-value-subtraction]] (high). | 2026-06-09 |
| `eval/qa-sweep/` | Phase 82 — 8-area verification matrix (orchestrator-only evidence standard), 58 candidates → 35 fixes, 10/10 exit criteria. [[qa-verification-sweep]] (high). | 2026-06-09 |
| `scripts/check-assumption-ledger.sh` + `.dev-wiki/assumption-ledger.md` + dev-plan/dev-debrief gate companions | Phase 81 — the assumption-approval gate: NO-LLM ledger validator (4 modes + `--selftest`; 20th make-test script), append-only cross-phase ledger, debrief revisit forcing-function. [[assumption-approval-gate]] (high). | 2026-06-09 |
| `eval/assumption-screen/` + `eval/amplifier/` (anchor-screen, retention-screen, xsession-screen, skill-screen, emitter + measurability-gate) | Phases 68-80 frozen measurement apparatus (repo-only, NOT in install.sh/make test/make eval): the amplifier program's 5 nulls + the Ph80 INSTRUMENT-DEAD surfacer screen. Records: each tree's `screen-record.md` `^PROGRAM-VERDICT`. | 2026-06-09 |
| `scripts/check-install-drift.sh` + `templates/.claude/hooks/session-start.sh` | Phase 76 (+82/85 extensions) — installed-copy-drift comparator (consumer-conditioned directory cells, pass 2b presence-comparison) + `[nana:drift]` / `[nana:recovery]` session-start advisories. [[installed-copy-drift-guard]] (high). | 2026-06-10 |
| `/Users/jwang/edge-screener` (EXTERNAL repo) | The consuming-project substrate (Phase 73): real stock-screener with the full harness; Phase-85 single-registration template-sourced install; Phase-87 episode substrate (p87-setup 4ed8071, twin clones) + arm A's Phase-10 work shipped a6effcb (394 tests / 94.58% cov). | 2026-06-10 |
| `modules.json` | Single canonical scope-tagged `hooks` array (17 project + 1 global; +enforce-assumption-gate Phase 91, detect-loop cut Phase 88) + top-level `hook_dirs` map + mcp block with an `env` `{PYTHONPATH}` (Phase 91) — hook + MCP source of truth; drives install.sh, register-settings.py, drift comparator, tests | 2026-06-14 |

## Session Journal (last 5)

- [2026-06-30] [[2026-06-30-phase-116-youtube-grounded-acquisition-built|Phase 116 — YouTube→wiki grounded acquisition core (rung-B foundation): BUILT (6/6 tasks, READY FOR COMPLETION)]] -- Built the acquisition + grounded-extraction CORE of the Phase-97 rung-B standing research pipeline, specialized to YouTube, as LOCAL gitignored Python apparatus under `companion/research/youtube/` (10 modules + 6 test files + README + venv) — NOT a shipped kit skill (the Ph97 local-apparatus-never-shipped precedent); the continuous half (RSS/scheduler/digest) deferred to nanaclaw. A manual-trigger acquire→chunk→extract→ground+consolidate loop turning one Pi/agent-setup video into a grounded, provenance-carrying concept set (verbatim quote + timestamp), feeding a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills. **LOAD-BEARING = the grounding rail (T4 CHECKPOINT, PASSED):** every `source_quote` mechanically verified ∈ its chunk before it enters a concept file (planted fabricated quote REJECTED); the calibration probe loud-aborts on broken normalization; the immutable-quote merge re-verifies the merged set. **T6 ran a 2-round adversarial review** (5-lens finder workflow, 22 findings/5 HIGH, then a focused re-attack; every finding orchestrator-verified by running the actual code per [[HEU-012]]) → 5 HIGH + MED/LOW fixed inline with 16 regression tests (DISCOVERY/SECURITY escape hatch): casefold so the probe survives non-ASCII, tilde-fence-safe/never-raises parser, refuse-to-clobber hand-edited files, per-quote timestamps from the segment→time map, the vacuous E2 boundary test. The re-attack CAUGHT the first punctuation-strip fix re-opening the anti-fabrication invariant to cross-sentence splices → REVERTED to casefold + curly/dash-fold only. **Health:** NEW companion pytest suite 0→53 green (hermetic — lazy/injected deps, no network); separate from the shipped kit's `make test` (unchanged); `git check-ignore companion/research/youtube` confirms nothing shippable. NO shipped-kit change (`app/`/`templates/`/`install.sh` untouched). Ledger Phase-116: A1/A2/A3 held (all_accept:false — A3 reject→Standard, vindicated by the 5 HIGH the Standard review caught). Residuals: A1 grounding catches FABRICATED not MISREAD (human wiki-review); A2 whisper deferred; live first-run manual-drive. Decision [[youtube-grounded-acquisition]] (high). Escape hatches: DISCOVERY/SECURITY (22 review findings folded into T6, not new tasks); USER OVERRIDE ×3 (maintainer chose rung-B over the agent's rung-A, Standard over the agent's Lite, and "continue with all tasks in session"). Retro: not triggered (91 completed, 91 % 5 ≠ 0).
- [2026-06-30] [[2026-06-30-phase-115-conversation-memory-delivered|Phase 115 — Conversation memory (persist + restore the chat thread): DELIVERED (5/5 tasks)]] -- Closed dogfood gap #2: the GUI conversation was pure in-memory React state, so an app restart lost the thread AND a workspace change LEAKED it. Persist a bounded, structurally-redacted, PER-WORKSPACE conversation to **localStorage** (keyed by `ready.workspaceRoot`); restore on restart + **SWAP** on workspace change — the swap also fixes the Ph114 stale cross-workspace leak (the respawn never remounts the surface: same `BridgeClient`, no `key`). **The approach reviewer corrected the FACTUAL PREMISE** before any code — restart/workspace-change does not LOSE the thread, it LEAKS it — reframing success to 'thread SWAPS on workspace change' (ledger A4 strengthened). NEW pure module `app/src/ui/conversation-store.ts` (`redactForPersist` on ALL string leaves incl. nested output + `details.diff`, STRIP write `content` body → `'[N bytes]'`; bound + prune + fail-soft localStorage); `useChatRuntime` gained an OPTIONAL workspace source; restore/swap DISPLAY-ONLY (zero engine sends). A1 don't-know RESOLVED via the 'strip high-risk bodies' down-scope. **A 2-finder adversarial review (orchestrator-verified each finding against the code) caught + fixed 6 — incl. 1 HIGH:** `saveConversation`'s byte-backstop could prune a NON-empty thread to `[]` then `setItem("[]")` overwrote good history (silent total loss) → keep ≥1 message + SKIP the write at the last message + fail-soft wrap. Plus MED multi-workspace quota (→256KB + documented no-LRU residual), LOW tool-status finalize, LOW `details` redact, LOW shape-guard per-element, LOW empty-root. **Health:** app suite 343→374 (+31); tsc + npm run build + cargo check exit 0; gate/inert/redact/no-bypass rails unbroken. Ledger Phase-115: A1/A2/A3/A4 held, **A5 BIT** (the byte-backstop data-loss bug — the detect-after backstop working). Residuals: A1 at-rest (short/plaintext secrets `redactSecrets` misses; content-strip write-only); no cross-workspace LRU; live restart-restore live-drive-only. Decision [[conversation-memory-persistence]] (high). Escape hatch: DISCOVERY/SECURITY (the 6 review fixes folded into T5, not new tasks). Retro (90 completed, triggered): Dims 1-2 none, Dim 3 low (the factual-premise reframe = the approach gate working) → no systemic issue; the adversarial review has now caught a HIGH the suite missed on 5 consecutive security/cross-boundary phases (Ph110/111/112/114/115) — keep it.
- [2026-06-30] [[2026-06-30-phase-114-pi-default-engine-workspace-picker-delivered|Phase 114 — Pi as the default engine (good tools) + Rust-atomic workspace picker: DELIVERED (6/6 tasks)]] -- Resumed at T3 and carried the phase to delivered. Fixed the dogfood "the tools are awful" at its ROOT — an accidental default: the agent ran on the Vercel adapter (uncapped bash + whole-file write) only via a Ph109 bring-up artifact, while the spec names Pi primary and Pi already ships the rich capped suite the gate covers. **Make Pi the default** (T1 make-or-break PASSED — live e2e green, gate intercepts every tool; flipped vercel→pi; threaded NANA_MAX_TOKENS past 2048; activated grep/find/ls) + a **Rust-atomic workspace picker** (T3–T5): `pick_workspace` opens the native dialog FROM RUST so the webview gets **NO dialog capability at all** (DISCOVERY, verified against v2.tauri.app/docs.rs — tighter than the planned grant); `changeWorkspace()` kills+respawns the sidecar with NANA_WORKSPACE (fresh process ⇒ fresh gate + approved-writes); the header shows the active workspace + project-blind state (widened `ready` carries root+sources, never the systemContext contents); a "Change workspace…" palette command. **T6 adversarial review (3 finders, orchestrator-verified each finding against the code) caught + fixed 6 — incl. 1 HIGH:** the secret deny-list was ancestor-blind, so the now-active recursive grep/find/ls (not seatbelt-confined like bash) reached ~/.ssh / ~/.aws via an ancestor search root → `pathReachesDeniedPath` (ancestor-aware). Plus MED zombie-reap, MED changeWorkspace re-entrancy guard, LOW atomic stdin/child swap, LOW respawn current_dir, LOW App .catch. **Health:** app suite 318→343; tsc + npm run build + cargo check exit 0; gate/inert/redact/visibility rails 99/99 no regression. Ledger Phase-114: A1/A3/A4 held, **A2 BIT** (the ancestor-blind secret-read hole — the detect-after backstop working). Residuals: out-of-ws READ stays the Ph112 residual; native dialog→respawn round-trip live-drive-only; #2 conversation memory → Ph115; a pre-existing renderer-trust gap (engine_send gate-verdict auto-approve) flagged for a future phase. Decision [[pi-default-engine]] (high, updated DELIVERED). Escape hatches: SECURITY/DISCOVERY (the 6 review fixes; the Rust-atomic picker tighter than planned).
- [2026-06-27] [[2026-06-27-phase-112-os-sandbox-bash-fs-isolation|Phase 112 — OS-sandbox bash filesystem isolation: BUILT + adversarially reviewed (6/6 tasks, READY FOR COMPLETION)]] -- The named follow-on of [[host-gate-out-of-workspace-hardening]], planned + implemented + reviewed in one session. Closes the documented Ph111 residual (string-gating arbitrary bash is INCOMPLETE BY NATURE — `python3 -c`/`node -e`/`base64`/env indirection evade): a per-command macOS seatbelt (`/usr/bin/sandbox-exec`) enforcement layer BELOW the Ph111 string-gate, at the bash EXECUTION site for BOTH adapters through ONE chokepoint (NEW pure `app/src/gate/sandbox/{seatbelt,approved-writes}.ts`); no-op off-darwin (the Ph111 string-gate remains). **T1 spike (49 tests) proved A1 (write-deny INHERITS to subprocesses) / A4 (Pi seam binds + the gate sees the ORIGINAL command) / A5 (strict viable — only 2 mach names) HELD; A2 BIT-partially** (npm/pip/cargo caches denied at first → fixed by allowing `~/.npm`/`~/.cache`/`~/.cargo` in the integrity profile; strict omits them by design). The `defaults write` positive-control ESCAPED (the documented residual). **CORRECTIONS vs plan:** the Pi seam is `createAgentSession({customTools:[createBashToolDefinition(cwd,{spawnHook})]})` — NOT `baseToolsOverride` (that's on the low-level `AgentSession` ctor + replaces the whole base set); the synchronous `spawnHook` is downstream of `pi.on('tool_call')`, so the gate + Ph110/111 visibility see the ORIGINAL command, not the `sandbox-exec` wrapper (P6 source-trace + live probe). strict DROPS the unix-socket allowance (loopback-IP only — closes a Docker-daemon-socket exfil + DNS leak); `/usr/bin/sandbox-exec` absolute path (PATH-shadow); `canonicalizePath`=`basename(abs)` + `assertSafeProfilePath` rejects `/`. **C1-PRESERVE:** approval threads the target into per-command `extraWrites` (consume-once, command-keyed; engine-host `onApprove` records, the executor consumes); the gate VERDICT-LOOP core + `types.ts` UNCHANGED. **The adversarial PoC review** (6 finders × 2 refuters running real `sandbox-exec` attacks) caught 2 CONFIRMED whole-FS-grant bugs (canonicalize off-by-one `/w`→`/`; subpath-`/` over-grant) the 264 passing tests MISSED + 3 hardenings (PATH-shadow, strict unix-socket exfil, hardlink) → all fixed, +10 regression tests. **Health:** app suite 201→274 (+73: sandbox-spike 49, pi-sandbox 6, vercel-sandbox 2, sandbox-approved-writes 8, sandbox-no-bypass 8); tsc + `npm run build` + `cargo check` all exit 0; all live e2e green under the sandbox; no regressions. **HONEST RESIDUALS → Phase 113:** cross-platform (Linux landlock/bwrap + Windows job-objects); confidentiality (deny-read) + daemon residuals (`defaults`/`launchctl`/`osascript`, loopback local-service); Vercel bash output redaction+cap (a pre-existing asymmetry vs Pi); `sandbox-exec` deprecation durability. Ledger Phase-112: A1/A4/A5 HELD, A2 bit, A3 held (`--revisit 112` exit 0). Escape hatches: DISCOVERY (the Pi seam is `customTools` not `baseToolsOverride`, corrected before integration); SECURITY (2 whole-FS-grant bugs fixed inline pre-commit); USER OVERRIDE (A3 C1-preserve over C1-drop, A5 strict over integrity-only — both at the direction gate). Delivery accepted per the Ph109/110/111 precedent (live window-drive = the maintainer's deferred call); gate flips after commit (D3). Decision [[os-sandbox-bash-fs-isolation]] (high, Consequences updated BUILT). Retro (90 completed, triggered): Dims 1-2 none, Dim 3 low (2 direction-gate reframes = the assumption gate working) → no systemic issue; carry the empirical-PoC adversarial review forward (now caught real bugs in Ph110/111/112 that the passing suites missed).
- [2026-06-27] [[2026-06-27-phase-111-typed-artifact-fidelity|Phase 111 — Typed Artifact Fidelity + host-gate out-of-workspace hardening: BUILT + adversarially reviewed (5/5 tasks + 3 review fixes, READY FOR COMPLETION)]] -- The de-risked Ph110 follow-on, planned + implemented + reviewed in one long session, then pivoted on a SECURITY finding. **The typed-diff thread:** Pi's typed `details` (normalized to a neutral `{diff?: string}` at the adapter via `extractToolDetails`) threaded ADDITIVELY through `types.ts` tool-result EngineEvent → `runtime.ts` SurfaceToolCall → `artifact-feed.ts` toArtifact, so an edit renders a STRUCTURED canonical DiffView (the typed path WINS over Ph110's `looksLikeDiff` heuristic). **A1 RESOLVED HELD via the T1 live spike** — `tool_execution_end.result.details.diff` IS on the LOCAL subscribe path (a live local edit emitted `{diff:'-1 hello world / +1 goodbye world / 2 second line'}` and really edited the file; the `pi.on('tool_result')` fallback was NOT needed). KEY FINDING: Pi's `EditToolDetails.diff` is a line-number-prefixed `+`/`-`/space format with NO `---`/`+++`/`@@` header → Ph110's heuristic MISSES it, so the typed path is strictly BETTER, not just a refactor; DiffView's line-classing still renders it. TestResults stayed descoped (A2 — no Pi tool emits structured test details). **THE SESSION PIVOT — a SECURITY finding:** capturing the regression baseline exposed a REAL host-gate out-of-workspace gap — the boundary was enforced ONLY for write/edit with an `args.path` key, while bash file-writes (`echo>`/`tee`/`cp`/`mv`/`dd`) and alt-named write tools BYPASSED it (deterministically confirmed at the gate, no model; **Ph108's "empirically un-bypassable" was happy-path-only**). The flaky `provider-roundtrip` e2e was intermittently catching it; the as-planned T4 ("soften the assertion") would have MASKED it. Maintainer-directed (AskUserQuestion) "fold a gate fix into Phase 111" (ledger A5, SECURITY escape hatch) → T4 reshaped "pin the flaky e2e" → "CLOSE the gate gap," reordered FIRST: [[host-gate-out-of-workspace-hardening]] extends base-policy COVERAGE (default-branch `PATH_ARG_KEYS` check + bash-branch `extractBashWriteTargets` + a `DEV_SINK` exemption so `2>/dev/null` isn't over-blocked), confirmable not hard-deny, gate VERDICT-LOOP core ([[gate-confirm-approve-loop]]) UNCHANGED. **An adversarial finder×refuter review** (6 finders × 2 refuters; 8 raised → 2 confirmed + 1 self-verified → fixed, 6 dismissed) caught a `/dev/null` over-block (alert-fatigue regression) + a `>|` force-clobber write-evasion the 24 passing gate tests MISSED — both fixed pre-commit. HONEST RESIDUAL: string-gating bash is incomplete by nature (`python -c`/`node -e`/base64/env indirection evade) → the COMPLETE fix is OS-sandboxing bash's fs to the workspace, routed to Phase 112. **Health:** app suite 157→201 (+44; all live e2e green incl. the out-of-workspace e2e that FAILED at this session's baseline); tsc + `npm run build` + `cargo check` all exit 0; the confirming-gate + Vercel 2nd-adapter gate tests stayed green (no over-block). Ledger Phase-111: A1 HELD-resolved, A2/A3/A4 held, A5 SECURITY-amendment revisit-open (residual accepted at the delivery gate; `--revisit 111` exit 0). Escape hatches: SECURITY (gate fold-in + Ph108 over-claim correction), USER OVERRIDE (maintainer-directed fold-in), DISCOVERY (Pi's header-less diff format shaped T3; the `/dev/null` over-block + `>|` evasion found in review). Delivery accepted per Ph109/110 precedent (live window-drive = the maintainer's deferred call); gate flips after commit (D3). Decisions [[typed-artifact-fidelity]] (high, updated BUILT) + [[host-gate-out-of-workspace-hardening]] (high, NEW). Retro (90 completed, triggered): Dims 1-3 all low (the convenient-fixture-masks-a-gap class caught pre-commit; the Ph108 over-claim correction; the maintainer-directed fold-in) → no systemic issue.
## Cross-References

- **Phase 116 outcome** ([[youtube-grounded-acquisition]], high; status active, **READY FOR COMPLETION**, delivery=pending — gate flips after commit): the Ph97 rung-B FOUNDATION increment — the acquisition + grounded-extraction CORE of a standing research pipeline, specialized to YouTube, built as LOCAL gitignored Python apparatus under `companion/research/youtube/` (10 modules + 6 test files + README + venv), manual-trigger, feeding a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills. NOT a shipped kit skill (the Ph97 local-apparatus-never-shipped precedent, [[frontier-positioning-sweep]]); the continuous half (RSS/scheduler/dedup-watermark/Telegram digest) is deferred to nanaclaw. Loop: acquire (captioned-first VTT, WORD_FLOOR=200 dead-letter, above-floor success control) → chunk (~1000-word/15%-overlap, segment→time map, no-drop) → extract (mocked-Claude `complete_fn` DI seam, extraction-by-selection = verbatim `source_quote` + ≤40-word claim + timestamp, frozen immutable `Concept`, pre-flight cost ceiling) → ground+consolidate. **THE LOAD-BEARING PROOF (T4 CHECKPOINT, PASSED):** grounding is a DETERMINISTIC gate not an LLM judgment — every `source_quote` mechanically verified ∈ its chunk via symmetric `match_normalize` substring before it can enter a concept file; a planted fabricated quote is REJECTED; the calibration probe perturbs a verbatim span and `CalibrationError` loud-aborts on broken normalization (else a silent drop reads as "thin video"); consolidation keeps quotes IMMUTABLE + re-verifies the MERGED set. **T6 2-round adversarial review** (5-lens finder workflow — grounding-symmetry/markdown-injection/atomic-clobber/test-hermeticity/provenance-immutability, 22 findings/5 HIGH — then a focused re-attack; every finding orchestrator-verified by RUNNING the actual code, subagent prose = candidate-only per [[HEU-012]]/[[qa-verification-sweep]]) → 5 HIGH + meaningful MED/LOW fixed inline with 16 regression tests (DISCOVERY/SECURITY escape hatch): casefold so the probe survives non-ASCII (German ß aborted the whole video); tilde-fence-safe neutralize + line-anchored/last-block/never-raises parse (a quote ending `~~~json` forged the fence → parse crash); refuse-to-clobber unparseable/hand-edited files; per-quote timestamps from the segment→time map (was the chunk start, ~minutes off); the vacuous E2 boundary test (passed at zero overlap). The re-attack CAUGHT the first punctuation-strip fix re-opening the anti-fabrication invariant to cross-sentence splices → REVERTED to casefold + curly/dash-fold only (a manual-track internal-comma-drop is now a SAFE false-negative). HONEST RESIDUALS (accepted): A1 — grounding catches FABRICATED quotes, not a real quote MISREAD into a wrong claim nor a faithful sub-span TRUNCATION; routed to the human wiki-absorb review as the second gate (extraction-by-selection + MIN_QUOTE_WORDS shrink the surface); NO AML-tier reconciliation. A2 — whisper.cpp deferred; uncaptioned → dead-letter; rate measured at the first live run. Live network/API multi-video first-run is manual-drive (residential IP, real tokens; deterministic tests use fixtures + a mocked client). NEW companion pytest suite 0→53 green (hermetic — lazy/injected deps, no network); separate from the shipped kit's `make test` (unchanged); `git check-ignore companion/research/youtube` confirms nothing shippable — ZERO shipped-kit change (`app/`/`templates/`/`install.sh`/security/engine surface untouched). Ledger Phase-116 (all_accept:false — A1/A2 accept, A3 reject→Standard ceremony, VINDICATED: the Standard review caught 5 HIGH; `--gate 116` exit 0; revisit A1/A2/A3 held). USER OVERRIDE ×3 at the direction gate: maintainer chose rung-B over the agent's rung-A recommendation, Standard over the agent's Lite, and "continue with all tasks in session". Discovered → Phase 117: the LIVE multi-video first-run validation (deferred manual-drive — measures the captioned/dead-letter rate A2 + per-video token cost + real-content grounding) / rung-B continuous half in nanaclaw / whisper.cpp fallback if dead-letter rate high / a deterministic wiki-absorb provenance-preservation check (S2, currently instruction-only) / the still-open pre-pivot GUI candidates (renderer-trust hardening, blocking out-of-ws READ, multi-workspace localStorage LRU).
- **Phase 115 outcome** ([[conversation-memory-persistence]], high; status active, **READY FOR COMPLETION**, delivery=pending — gate flips after commit): closed dogfood gap #2 — persist a bounded, structurally-redacted, PER-WORKSPACE conversation to **localStorage** keyed by `ready.workspaceRoot` (via `bridge.onWorkspace`); restore on restart + **SWAP** on workspace change, which ALSO fixes the Ph114 stale cross-workspace leak (the respawn kills+respawns the sidecar but never remounts the surface — same `BridgeClient`, `setBridge` once, no `key` — so the prior workspace's thread stayed visible against the new workspace's fresh gate; CONFIRMED in code by the approach reviewer = a LEAK not a loss, reframing success to 'thread SWAPS on change'). NEW pure module `app/src/ui/conversation-store.ts` — `redactForPersist` walks ALL string leaves (incl. nested tool output per `runtime.ts:21` `unknown`, `details.diff`) + STRIPS the write `content` body → `'[N bytes]'` (the A1 'strip high-risk bodies' down-scope) + finalizes `done:false` turns + clamps tool `called`→`done`; `save`/`load`/`clear` with a shape-guarded parse, bound = last-N-turns + byte backstop prune-oldest, fail-soft `setItem` (prune-retry then SKIP, never clobber). `useChatRuntime` gained an OPTIONAL workspace source (absent → persistence disabled; bare adapters/offline unaffected); `App.tsx` passes the bridge's workspace interface. Restore/swap is DISPLAY-ONLY (pure `setMessages`, ZERO engine sends — locked by a no-bypass test); the host gate + inert-render + redact rail + no-bypass invariant + line protocol UNCHANGED (no engine types across the boundary, [[engine-adapter-in-process-gate]]; builds on [[tool-call-visibility-thread]] Ph110 redact-at-adapter, [[pi-default-engine]] Ph114). A 2-finder adversarial review (orchestrator-verified each finding against the code, subagent prose = candidate-only [[qa-verification-sweep]]) caught + fixed 6 confirmed — **1 HIGH** (`saveConversation`'s byte-backstop `while (kept.length > 0 …)` could prune a NON-empty thread to `[]` when one message exceeded `MAX_BYTES`, then `setItem("[]")` overwrote good history = silent total loss; fixed: keep ≥1 message + SKIP the write at the last message + fail-soft wrap) + MED multi-workspace quota (→256KB, documented no-cross-workspace-LRU residual) + 4 LOW (tool-status finalize, `details` redact, per-element shape guard, empty-root). HONEST RESIDUALS (do NOT over-claim): A1 at-rest — `redactSecrets` is pattern-based and misses short/plaintext secrets, the content-strip is write-only (edit `new_string` redacted-not-stripped) — same content the live DOM already showed, the delta is durability (memory→disk); no cross-workspace LRU past ~15-20 large active workspaces (fail-soft skip); the live restart-restore + native swap round-trip is live-drive-only (Tauri runtime; cargo-check/vitest = compile/unit, Ph109-114 precedent). app suite 343→374 green (+31: conversation-store 22, conversation-persist 5, workspace-swap 4); tsc + `npm run build` + `cargo check` exit 0. Ledger Phase-115 (all_accept:false — A1 don't-know RESOLVED via down-scope; A1/A2/A3/A4 held, A5 BIT; `--revisit 115` exit 0). Discovered → Phase 116: a dedicated live window-drive validation phase (the deferred round-trips) / renderer-trust hardening (Ph108/109 `engine_send{gate-verdict}` auto-approve) / blocking out-of-workspace READ (Ph112 confidentiality residual) / multi-workspace localStorage LRU eviction.
- **Phase 112 outcome** ([[os-sandbox-bash-fs-isolation]], high, Consequences updated BUILT; the named follow-on of [[host-gate-out-of-workspace-hardening]]; status active, **READY FOR COMPLETION**, delivery=pending — gate flips after commit): the security residual's real fix — a per-command macOS seatbelt (`/usr/bin/sandbox-exec`) enforcement layer BELOW the Ph111 string-gate, at the bash EXECUTION site for BOTH adapters through ONE chokepoint (NEW pure `app/src/gate/sandbox/{seatbelt,approved-writes}.ts`), closing the documented Ph111 residual (string-gating arbitrary bash is INCOMPLETE BY NATURE — `python3 -c`/`node -e`/`base64`/env indirection evade). NO `types.ts`/engine-contract change; the gate VERDICT-LOOP core ([[gate-confirm-approve-loop]]) UNCHANGED; per-command TS keeps it engine-neutral ([[engine-adapter-in-process-gate]]). **T1 spike PROVED A1 (write-deny INHERITS to subprocesses) / A4 (Pi seam binds + the gate sees the ORIGINAL command) / A5 (strict viable — only 2 mach names) HELD; A2 BIT-partially** (npm/pip/cargo caches denied at first → fixed by allowing `~/.npm`/`~/.cache`/`~/.cargo` in the integrity profile; strict omits them by design). **CORRECTIONS:** the Pi seam is `createAgentSession({customTools:[createBashToolDefinition(cwd,{spawnHook})]})` NOT `baseToolsOverride` (the synchronous `spawnHook` is downstream of `pi.on('tool_call')` so the gate + Ph110/111 visibility see the original command, P6 source-trace + live probe); strict DROPS the unix-socket allowance (loopback-IP only — closes a Docker-daemon-socket exfil + DNS leak); `/usr/bin/sandbox-exec` absolute path (PATH-shadow); `canonicalizePath`=`basename(abs)` + `assertSafeProfilePath` rejects `/`. **C1-PRESERVE** via per-command `extraWrites` (consume-once; engine-host `onApprove` records, the executor consumes). An adversarial PoC review (6 finders × 2 refuters running real `sandbox-exec` attacks) caught 2 CONFIRMED whole-FS-grant bugs (canonicalize off-by-one `/w`→`/`; subpath-`/` over-grant) the 264 passing tests MISSED + 3 hardenings → all fixed, +10 regression tests. HONEST RESIDUALS (do NOT over-claim "residual closed"): daemon/XPC writes (`defaults`/`launchctl`/`osascript`) + full filesystem READ remain even in strict; `sandbox-exec` is Apple-deprecated-but-functional; non-darwin = Ph111 string-gating ONLY. app suite 201→274 green incl. live e2e; tsc + `npm run build` + `cargo check` all exit 0. Ledger Phase-112 (all_accept:false — A1/A4/A5 HELD, A2 bit, A3/A5 reject-resolved-by-revision HELD; `--revisit 112` exit 0). Discovered → Phase 113: cross-platform sandbox (Linux landlock/bwrap + Windows job-objects) / confidentiality (deny-read) + daemon residuals (loopback local-service) / Vercel bash output redaction+cap (a PRE-EXISTING asymmetry vs Pi) / `sandbox-exec` deprecation durability / carried — signed+notarized bundle + A3 keyring residual + rollback, command palette (axis 3), Claude Agent SDK adapter (key-gated), the deferred live window-drive felt-quality check.
- **Phase 111 outcome** ([[typed-artifact-fidelity]], high, updated BUILT + DELIVERED; + [[host-gate-out-of-workspace-hardening]] high, NEW; status active, **READY FOR COMPLETION**, delivery=pending — gate flips after commit): the de-risked Ph110 follow-on threaded Pi's typed `details` (normalized to a neutral `{diff?: string}` at the adapter) ADDITIVELY through the EXISTING engine-neutral layers — `types.ts` tool-result EngineEvent → `runtime.ts` SurfaceToolCall → `artifact-feed.ts` toArtifact — so an edit renders a STRUCTURED canonical DiffView; the typed path WINS over Ph110's `looksLikeDiff` heuristic. **A1 RESOLVED HELD via the T1 live spike** — `tool_execution_end.result.details.diff` IS populated on the LOCAL subscribe path (a live local-model edit emitted `{diff:'-1 hello world / +1 goodbye world / 2 second line'}` and really edited the file; the `pi.on('tool_result')` fallback was NOT needed). KEY FINDING: Pi's `EditToolDetails.diff` is a line-number-prefixed `+`/`-`/space format with NO `---`/`+++`/`@@` header → Ph110's heuristic MISSES it, so the typed path is strictly BETTER (not just a refactor); DiffView's line-classing still renders it. TestResults stayed descoped (A2 — no Pi tool emits structured test details; still built-ahead-of-data). **SECURITY PIVOT:** the regression baseline exposed a REAL host-gate out-of-workspace coverage gap — `createHostGate` enforced the boundary ONLY for write/edit with an `args.path` key; bash file-writes (`echo>`/`tee`/`cp`/`mv`/`dd`) + alt-named write tools BYPASSED it (deterministically confirmed at the gate, no model; **Ph108's "empirically un-bypassable" was happy-path-only**). Maintainer-directed fold-in (ledger A5, SECURITY escape hatch) → [[host-gate-out-of-workspace-hardening]] extends base-policy COVERAGE (default-branch `PATH_ARG_KEYS` check + bash-branch `extractBashWriteTargets` + a `DEV_SINK` exemption), confirmable not hard-deny, gate VERDICT-LOOP core ([[gate-confirm-approve-loop]]) UNCHANGED. An adversarial finder×refuter review (6 finders × 2 refuters; 8 raised → 2 confirmed + 1 self-verified → fixed) caught a `/dev/null` over-block + a `>|` force-clobber write-evasion the 24 passing gate tests MISSED. HONEST RESIDUAL: string-gating bash is incomplete by nature (`python -c`/`node -e`/base64/env evade) → the COMPLETE fix is OS-sandbox bash-fs isolation, routed to Phase 112. HIGHEST blast radius `app/src/engine/types.ts` (`tool-result` variant + additive optional `details?: { diff?: string }`) stayed additive-clean + JSON-serializable; Vercel/noop/host untouched. app suite 157→201 green incl. all live e2e; tsc + `npm run build` + `cargo check` all exit 0. Ledger Phase-111 (all_accept:true — A1 HELD-resolved, A2/A3/A4 held, A5 revisit-open; `--revisit 111` exit 0). Discovered → Phase 112: OS-sandbox bash-fs isolation (the security residual's real fix) / structured TestResults (needs a test-runner tool) / signed+notarized bundle + A3 keyring residual + rollback / command palette (axis 3) / Claude Agent SDK adapter (key-gated) / the deferred live window-drive felt-quality check.
- **Phase 110 outcome** ([[tool-call-visibility-thread]], high, updated BUILT; status active, **READY FOR COMPLETION**, delivery accepted per Ph109 precedent — gate flips after commit): the first dogfood-driven UX pass closed Ph109's live-drive #1 gap (allowed tool calls rendered NAME-ONLY). A single vertical thread source→surface threaded real Pi tool args/output through the EXISTING engine-neutral layers ADDITIVELY (no engine-type reshape, honors [[felt-quality-surface]] A1) into the Ph109 components that were built ahead of the data: T1 `mapPiStreamEvent` seam → T2 additive `SurfaceToolCall`+reduction → T3 inline render via `redactSecrets` on the inert path → T4 additive `tool-progress` streaming → T5 pure `ui/artifact-feed.ts` router → T6 `ArtifactPanel` in App.tsx → T7 Nana icon → T8 e2e+gate. **THE SESSION HEADLINE — an adversarial pre-commit review** (6 finders × 2 refuters; 15 raised → 6 confirmed → all fixed, 9 refuted) caught real defects the 142 string-fixture tests MASKED: (HIGH) Pi's live `result`/`partialResult` are `AgentToolResult` WRAPPER objects (`{content:[{text}],details}`), NOT strings → `extractToolText()` normalizes at the adapter boundary (else the live app renders JSON noise, the 16KB cap never fires, edit→diff never routes); (HIGH/MED) `redact.ts` reworked to catch AWS/GCP-`AIza`/JWT shapes reachable via gate-allowed `bash cat ~/.aws/credentials`/`env` while EXEMPTING git-SHA/sha256 digests (both pinned in `tests/security/redact.test.ts`); (LOW) `looksLikeDiff` tightened. **Resolves ledger A1/A3** (was open) — Pi populates args/output in wrapper form + typed `details` (`edit.details.diff`) confirmed-available → a real structured DiffView/TestResults is the **Ph111 headline** (de-risked). The gate CORE ([[gate-confirm-approve-loop]], key-store hard-deny) UNCHANGED; inert-render/CSP/capability-guard/gate-confirm NO regression. app suite 108→142 (8 tasks) →157 (6 review fixes); tsc + `npm run build` + `cargo check` all exit 0; 1 flaky `provider-roundtrip` live e2e (model non-determinism, →Ph111). Ledger Phase-110 A1→held, A2→held, A3→held, A4→held (`--revisit 110` exit 0). DISCOVERY escape hatch: the 2 HIGH + the security gap fixed in-phase as delivery hardening. Discovered → Phase 111: typed test/diff-from-`details` panel (headline) / signed+notarized bundle + A3 keyring residual + one-click rollback / command palette (axis 3) / Claude Agent SDK adapter (key-gated) / pin the flaky live e2e.
- **Phase 109 outcome** ([[felt-quality-surface]], high, updated BUILT; + [[webview-engine-bridge]] high + [[gate-confirm-approve-loop]] high, both NEW; status active, **READY FOR COMPLETION**, delivery accepted, gate flips after commit): the drivable felt-quality slice of THE PIVOT — Ph108's placeholder `App.tsx` (`<h1>`) became a real assistant-ui surface wiring the 5-axis thesis (axes 4 streaming chat / 1 gate-hold confirm+diff / 5 artifact preview / 2 one-action revert; axis 3 command palette deferred A5) to the existing engine-neutral reduction, plus a minimum context-assembly seam (workspace AGENTS.md/CLAUDE.md + `.claude/rules/*.md` → per-turn system context, loud-on-missing; `SendPromptOptions.systemContext` additive — Vercel native `system:`, Pi `<project-context>` preamble). **A1 spike resolved CLEAN** — `useExternalStoreRuntime` + a custom `convertMessage` bound with ZERO engine-type reshape; the AI-SDK-data-stream fallback was never needed; AI Elements skipped → custom owned components. **T3 discovered** the gate only AUTO-DENIED destructive actions with no held-call resolution path → a host-owned `ConfirmationBroker` + `confirmingGate` (on the existing `setToolCallGate` seam) turns a confirmable deny into an unresolved Promise the adapters already await + surfaces it to the UI; SECURITY-CRITICAL — only the literal "requires explicit human confirmation" marker is confirmable, key-store/secret denials stay HARD-denied, tested against the REAL gate (constraint #4 relaxed UI-only→UI+host broker, gate CORE unchanged). **T6 discovered** Ph108's "daily loop" had only ever run in Node tests — the Node-only engine was unreachable from the browser webview → a Rust-spawned Node engine-host sidecar + JSON line protocol + `BridgeClient`-as-`EngineAdapter` makes the harness genuinely drivable; Rust does the spawning so the webview manifest stays `core:default` + CSP `connect-src 'self'` (inert-render guard unbroadened); esbuild **ESM** host bundle (CJS failed — pi-coding-agent export-map-only, `ERR_PACKAGE_PATH_NOT_EXPORTED`). app tests 63→108 green (+45), tsc+vite+cargo check all exit 0, ESM host starts clean, 1 intermittent FLAKY live-model e2e under concurrency. Ledger Phase-109 all `held` (A1 open→held: spike clean; A2-A5 held; none bit; `--revisit 109` exit 0). The #1 assistant-ui integration detail: `applyEngineEvent` mutates → the streaming hook MUST clone-on-event or React won't re-render. **UNVERIFIED:** the LIVE end-to-end window round-trip (webview→Rust spawn→node sidecar→local model→back) — each layer tested independently, not the assembled live drive; likeliest failure = the Rust spawn + `NANA_ENGINE_HOST_JS` env-path under real `tauri dev`; maintainer's deferred launch check. Discovered → Phase 110+: live-drive verification / real app icon + branding / richer artifact pipeline (live tool-results → typed views) / signed+notarized bundle folding the host-sidecar bundling + the A3 keyring residual / pin the flaky e2e / daily-driver dogfood (now unblocked) / command palette (axis 3) / Claude Agent SDK adapter (key-gated).
- **Phase 108 outcome** ([[engine-adapter-in-process-gate]], high, CONFIRMED; + [[provider-defaults-to-local-model]] high, [[second-adapter-vercel-ai-sdk]] high — both NEW; status active, **delivery gate PENDING**): THE PIVOT BUILT — a new top-level `app/` dir holds a Tauri (Rust) desktop shell embedding a model-agnostic engine IN-PROCESS behind the app-owned `EngineAdapter`, the security gate at the in-process tool-call site (built security/gate FIRST). All 8 tasks [x]; 14/14 machine-checkable exit criteria proven; 63/63 tests green incl. live; `tsc`+`vite build`+`cargo build` all exit 0 (Tauri binary 23.5MB). **A1 (Pi gate un-bypassable) EMPIRICALLY validated** — a real local model attempted `bash rm` + an out-of-workspace write through BOTH adapters; the in-process gate denied both before execution. **A2 (MCP server ports as-is) validated** — the round-trip failure was a MOUNT bug (FastMCP `structuredContent.result` list-wrap; the mount unwraps it), server UNTOUCHED. **A3 (keyring-rs) resolved→accept** — keyring-rs 3.6.3 round-trips the real macOS Keychain via a standalone `keyhelper` crate; residual = signed-bundle entitlements (bundle-time). Ledger Phase-108 A1/A2/A3/A4 all `held` (none bit). Two deviations within the spec's approved engine set: provider default = a LOCAL OpenAI-compatible backend (subscription-OAuth ruled OUT — prohibited + metered, no Max-quota benefit); second adapter = Vercel AI SDK (USER OVERRIDE — the Claude Agent SDK is API-key-only, can't drive local; engine-neutrality proven LIVE, one gate two engines). Rust toolchain 1.96.0 installed (DEPENDENCY escape hatch). Discovered (Phase 109+): Claude Agent SDK adapter (key-gated) / felt-quality pass / signed bundle + auto-update + rollback / live-repo dogfood (now unblocked). The Python MCP memory server is UNTOUCHED (mounted as-is).
- **Phase 103 outcome** ([[gaming-rate-gate-strength]], high; delivered, delivery gate pending): the rung-C pillar-2 deepening that closed the Ph102 loose thread (gaming detectable-in-principle but NOT exhibited). **INFORMATIVE NULL + REFRAME — loop-gaming does NOT occur; gate strength IS load-bearing for truthful delegation by FORCING THE CORRECTIVE LOOP, not by preventing in-loop gaming.** A deliberately weaker visible gate (ladder G2-strong{v1_func,v2_lag} → G1-medium{v1_func} → G0-weak{v0_shape}) over the FIXED Ph102 held-out, natural `opencode/big-pickle` worker. Make-or-break pilot (3 gates × n=3, 9 runs, 0 infra, leak-clean): 3/3 consistent at every gate — g2-strong + g1-medium **truthful 3/3** (converged-via-loop, held-out PASS; shown a failing lag locus the worker generalizes to a correct one-day lag even from ONE locus); g0-weak **cold-pass 3/3** (held-out FAIL; the shape-only gate is satisfied by the worker's wrong first impl → no loop fires). **Loop-gaming rate = 0/6** (Wilson 95% [0, 0.39]); silent-wrong is one-shot cold-pass only, at g0. The null is **STRUCTURAL not underpowered**: no gate the worker both loops against AND games exists (no intermediate gaming sweet spot). The contract earns its keep by GUARANTEEING the worker is SHOWN a concrete failure; once shown, deterministic feedback recovers truthfulness regardless of how few loci are pinned. Sharpens [[contract-loop]] (Ph102) + rhymes with the amplifier-nulls (Ph59/80). Per the pre-registered checkpoint (cold-pass-dominated weakest rung → STOP), the byte-freeze (T3) + full campaign (T4) were NOT run — by design; maintainer chose close-out. T1 earned a 4-lens adversarial review (found + fixed 2 HIGH [docstring→feedback leak; provably-non-nested ladder] + 2 MEDIUM + 1 LOW before any opencode run). Ledger Phase-103: A1 bit, A2/A3 held (A3 caveat: a naive marker grep false-positives on codebase identifiers like `SPY_TR_CAVEAT` → scope to the worker's own prose). Untested (declined, reaffirmed post-pilot): the capability bound — whether the worker CAN game when TOLD to optimize the checks. SHIPS NOTHING (make test PASS, eval 50/50, drift 0; edge-screener `931c0caef3742029` unchanged); apparatus gitignored `companion/research/contract-loop-gaming/`.
- **Phase 102 outcome** ([[contract-loop]], high; delivered + accepted 2026-06-22, plan f56038c / close-out 709ecf9): the rung-C pillar-2 culmination — **a contract-governed LOOP RECOVERS the Ph101 implementation-difficulty floor VIA FEEDBACK, not resampling and not naming the invariant.** decision-lag: single-shot 0/15, best-of-N 0/3, bare-loop 3/3, contract-loop 3/3 → feedback = bare−bestofN = +1.0 SUPPORTED; invariant-in-loop = contract−bare = +0.0 (naming nearly inert, only sped convergence ~1 iter); gaming 0/3. pit (secondary) CONFOUNDED (frozen "gaming 3/3" an instrument artifact from an orthogonal before-baseline case; post-hoc invariant-focused re-score = 6/6 recovery — reported VERBATIM not retrofitted). Gaming detectable-in-principle but NOT exhibited — the Phase-103 target. INCIDENT (owned): an apparatus `cleanup()` rmtree bug run CONCURRENTLY with the runner deleted + then restored `/Users/jwang/edge-screener` (intact, checksum `931c0caef3742029`, 9/9 tests; bug fixed — cleanup refuses non-tempdir; all later work serial). SHIPS NOTHING; apparatus gitignored `companion/research/contract-loop/`.
- **Phase 98 outcome** ([[frontier-watch]], high; implementation complete, delivery gate pending): the standing rung-B early-warning that keeps the Ph97 differentiated-leaning verdict fresh — a re-runnable, frozen, controls-validated **two-speed tripwire watch** in gitignored `companion/research/watch/`: WEEKLY broad-source/narrow-signal fast scan (primary) + MONTHLY deep full re-derivation backstop + on-hit escalation to the frozen `pre-registration.md` 600e1c9f. Two tripwires: (A) **B5 boundary-validator** (a 2nd primary lab ships a value-capturing GA deterministic boundary-validator, OR OpenAI's regresses → either → DIFFERENTIATED); (B) the **COMMODITIZED early-warning** (ANY lab ships value-capturing, default-on B1 blocking CONTENT or B4 assumption/plan-adjudication → CORE toward COMMODITIZED → reopens shrink). `watch-charter.md` frozen (SHA256 `.frozen` 8ec4af7b, anti-retrofit seal — VOIDs a fired verdict on any post-freeze edit to the frozen §2/§4/§6); `run-watch.sh --selftest` PASS (both seeded tripwires FIRE, both false-positive controls QUIET, charter-drift guard exit 3 on tamper). DETECTS-AND-FLAGS only — NO cut; on a B1/B4 fire shrink reopens via a fresh full frozen-instrument re-sweep, on a B5 fire the verdict resolves DIFFERENTIATED. Maintainer overrode the verdict's quarterly cadence (→ weekly, A2 reject — "things change too fast") + narrow scope (→ broad sources, A1 reject); reconciled via the two-speed split (fast cadence on the cheap scan, rare re-derivation — Ph97 half-life applied per-tier; A3 accept → re-run the FROZEN rule on escalation). DID NOT re-run the sweep (Ph97 read 2026-06-21 = baseline); a **dated Blockers tripwire is the committed fallback** (the `/schedule` weekly+monthly trigger is OFFERED, maintainer to confirm — the open question). Live HEU-012 re-confirmation: a macOS awk `\|`-as-alternation classifier bug (use `[|]` bracket class) was caught ONLY by the controls-first seeded-positives — the QUIET false-positive controls had passed for the WRONG reason (a broken classifier never fires, so it looks clean). ZERO shipped-kit change; make test PASS, drift 0, `git ls-files companion/` empty. Soft direction candidates (Phase-99+): opinionated-content investment over plumbing / "ride the rails" adopt-standard-plumbing / rung-D AML-or-trading generalization — all per the Ph97 convergence-map.
- **Phase 97 outcome** ([[frontier-positioning-sweep]], high; implementation complete, delivery gate pending): the EXTERNAL arm of the Ph92 re-measure-then-shrink — a pre-registered, one-shot sweep of where the agent-harness / agent-tooling frontier is converging, read MECHANICALLY against a rule FROZEN before any research (lab artifacts PRIMARY, OSS SECONDARY), lives entirely in gitignored `companion/research/`. **VERDICT = INCONCLUSIVE — forced-under-observed (differentiated-leaning)**: the OUTCOME label is INCONCLUSIVE (of record), kept SEPARATE from the differentiated-leaning substance (anti-retrofit; independently confirmed). K_low=0 (zero COMMODITIZED primitives), K_high=1 (B5 boundary-validator CONTESTED — OpenAI's value-capturing auto-Pydantic tool-arg validation sole FOR, below the ≥2-labs-or-Anthropic+1 bar); CORE (B1 blocking gates + B4 assumption gate) UNCOMMODITIZED; B2/B3 NOT-COMMODITIZED; 12/14 THIN; 3 labs affirmatively leave the discipline layer to tooling; DIVERGENT=0; OSS corroborates DIFFERENTIATED. INCONCLUSIVE is FORCED by the rigorous rule's zero-tolerance "any contest bars DIFFERENTIATED" clause on ONE non-core primitive — the realized rigor/legibility cost flagged at freeze (the maintainer-rejected simpler rule returns a clean DIFFERENTIATED; recorded, not edited). **NET: the external frontier gives NO case to shrink the bet — every GA "blocking" surface ships the hook/intercept surface but leaves the opinionated block CONTENT to the consumer (value-capture test held); combined with the Phase-95 internal memory KEEP, both arms of the Ph92 re-measure now AGREE.** New reusable method: SHA256 ATTESTATION freeze for a gitignored apparatus (git first-add-commit ancestry can't guard an untracked file; `.frozen` snapshots the hash, T4 re-verifies, VOID on mismatch). A3 resolved: QUARTERLY core-primitive watch for rung B (not a standing pipeline). Two cross-project lessons (→ /wiki-capture candidates): (1) an over-hardened FROZEN decision rule has a realized rigor/legibility cost (over-conservatism on a non-core contest); (2) an agent hallucinated a verbatim citation caught by the re-fetch audit — reinforces verify-by-re-fetch + a dedicated citation-reality auditor for any deep-research workflow. ZERO shippable-kit change; make test PASS; drift 0. Independent cold re-derivation CONFIRMED (freeze MATCH, retrofit-check CLEAN). Decision article Outcome appended; ledger Phase-97 A1/A2/A3 open→held.
- Phases 1-103: **102 completed** (Phases 97-103 are the rung-A→rung-C de-risking + contract-driven-delegation arc — Ph97 frontier sweep, Ph98 frontier watch, Ph99 dashboard, Ph100 fidelity check, Ph101 harder-corpus, Ph102 contract-loop feedback-recovers-the-floor, Ph103 gate-strength-forces-the-loop; Phase 96 delivered + accepted 2026-06-21, 973e78e; gate flipped 64c017c, pushed). Post-delivery follow-on hardened 3 hooks (enforce-memory per-session anchor, block-dangerous-bash precise targets, session-start keyed write) + added check-install-drift --consumer content-currency, propagated to all 7 consumers ([[post-phase-96-hook-hardening]], high; commits 54d91e0/3fa2f0b/55ab629/4b1a502). Phase 91 made the memory MCP layer FIRE in consumers + bound the assumption gate with a hook; Phase 92 re-sequenced the roadmap around the product-for-consumers frame; Phase 93 shipped the first deregistration mechanism + idempotent `--update` re-sync (build-only); Phase 94 re-measured consumer memory demand (EVIDENCE that reversed the demand-zero prior); Phase 95 disposed the memory layer (KEEP + enforce-memory REDESIGNED + trim-trials CONFIRMED); Phase 96 applied the re-sync LIVE to all 7 consumers (consolidate-to-local + `--migrate-to-local`), closing the Phase-93 deferred application.
- **Phase 96 outcome** ([[consumer-resync-rollout]], high): the live consumer re-sync the product-for-consumers arc pointed at. The planning probe reversed the Phase-93 premise (4/7 consumers register kit hooks in git-tracked `settings.json`, not `settings.local.json`) → maintainer chose **consolidate-to-local**: `install.sh --migrate-to-local` relocates `settings.json` kit regs onto gitignored `settings.local.json` + deregisters kit/cut hooks (both-file backup → survivor smoke → revert; ships hook files; `--update` UNCHANGED). Applied LIVE to all 7 behind a HARD checkpoint; working-tree end state on all 7 = 17-hook kit set in settings.local only, settings.json kit-clean, detect-loop ghost-free, drift CLEAN, single-fire (each hook in one file); armed +signal-watch +aml-casework. Ultracode adversarial review (28 candidates → REVISE) fixed 3 code defects inline + corrected overstated claims; the HIGH miss = **version-control durability** (the 3 git-tracked consumers are consolidated working-tree-only; committing per-consumer makes it durable — a maintainer follow-up). Known limitation: dereg is basename-keyed (a custom hook sharing a kit basename would be treated as kit-managed). Evidence: `eval/consumer-resync/rollout-evidence.md` + `run-exit-criteria.sh` ALL-PASS.
- **Phase 95 outcome** ([[memory-layer-disposition]], high): reconcile-and-dispose-then-close — adjudicated every open memory-layer obligation to a recorded, evidence-cited verdict (Phase 94 reversed the demand-zero premise → adjudication not a cut). memory-mcp-layer + bridge-writer + harvest-writer KEEP; **enforce-memory REDESIGNED** (the gameable agent-touched `.claude/.memory-consulted` existence check replaced by a transcript assertion of a real `memory_search` with `ts >= ~/.claude/.session-start-ts` — det-vs-LLM Principle 2 + per-session freshness; jq-only, fail-open; survivor-smoke PASS; supersedes the Ph88 keep); ak-ride-along (`d43950f`) + wk-seeding (`df3e623`) trim-trials CONFIRMED. Firing audit ~55% per-episode value (the ultracode adversarial pass corrected a window-gamed 71% to an honest 35-70% band + surfaced the stale-pass that shaped the freshness bound); spike PASS. Ledger Phase-83 A5 `open->held`; Phase-88 A4/A6 + both trim re-triggers + the enforce-memory resume-artifact harden-candidate resolved. Supersedes `specs/phase-92-memory-layer-prune.md`. Honest limit: the redesign is a fail-open nudge (one real search satisfies it). The memory-layer "shrink" thread is now SPENT (KEEP).
- **Phase 94 outcome** ([[consumer-memory-remeasure]], high): ONE clean admissible RETROSPECTIVE re-measure of consumer memory-LAYER demand on the working (Ph91) layer, gated by verify-by-firing (`VERDICT: FIRES`; broken-config control COULDN'T-FIRE; window pinned to repair-commit `318e9b6`). JSON `tool_use` tally (never grep — deferred-tool catalog over-counts a grep ~18x; subagent transcripts read from separate `<session-uuid>/subagents/agent-*.jsonl` files). **REVERSES the "consumer demand is zero" prior** (the Ph89/Ph83 consumer zeros were couldn't-fire on broken memory, inadmissible): a 3-consumer machinery gradient — signal-watch (no rules/hooks) 0 persisted rows ≈ spontaneous FLOOR; aml-casework (rules) 20 rows, 10/10 cross-session read-back; aml-substrate (rules+hooks) 44 rows, 25 read-back — the coerced/announced layer (the Phase-95 cut target) is in active, value-bearing (not ritual) use in 2 of 3 live consumers. EVIDENCE ONLY — Phase 95 disposes (must reconcile floor + coerced + read-back; likely KEEP/refine not shrink). Live kit-side datapoint: enforce-memory FIRED on the orchestrator this session (Phase-95 redesign-or-retire input). Review gate 7/10 revise → 1 HIGH (subagent-transcript files missed) + 3 LOW fixed inline; load-bearing parts re-verified SOUND. Evidence: `eval/memory-remeasure/memory-demand-remeasure.md`. Resolved the Blockers re-measure backlog item.
- **Phase 93 outcome** ([[install-resync-update-mode]], high; impl [[deregistration-as-register-settings-subcommands]], high): `install.sh --update` reconciles a consumer's project-local hooks + settings.local.json to the current kit (ADD/UPDATE + dedupe-by-basename per DRQ-1 + automated cut-hook dereg behind timestamped backup → survivor smoke → revert-on-failure; arming DECOUPLED). First dereg mechanism the kit ships (register-settings.py was upsert-only → new `--dedupe`/`deregister` subcommands, basename-normalized; two-tier LIBERAL-warn / CONSERVATIVE-`.cut_hooks`-remove). Controls-first 37-assertion harness (seeded cut-hook + dup registration caught); `check-install-drift.sh --consumer` = read-only complement. BUILD + SANDBOX-VERIFY ONLY — ZERO live consumer writes; live application = gated follow-on (Blockers, dry-run-first, arm-separately). Review gate 8/10 revise → MEDIUM (malformed-settings half-sync) fixed inline. **Trim-trial windows (ak-ride-along/wk-seeding) closed CLEAN — zero triggers Phases 88-93 — recommend CONFIRM at Phase 95.**
- **Phase 92 outcome** ([[strategic-inflection-review]], high; [[deterministic-vs-llm-boundary]], high): an ultracode 7-agent workflow (5 surveys → synthesis → adversarial critique) grounded the inflection review in the kit's own measured evidence. Two gate positions: FRAME = **product for consumers** (A1 reject of instrument-primary → inward measurement/hygiene growth is creep to correct, consumer surface primary); DIRECTION = **re-measure-once-then-shrink** (the synthesis's "refocus" smuggled in 2 unmeasured builds — a zero-lift-evidence retrieval R&D track + a linter-rewrite on a never-observed defect; the Ph89 memory zero is couldn't-fire). The critique corrected 5 overclaims (Ph87 det-beat-reviewers = binary-file confound; "5 nulls" = 4 clean + 1 instrument-dead; linter-skills size-inflated ~40%). Re-sequenced: Phase 93 install.sh re-sync → 94 consumer memory re-measure → 95 memory-layer shrink (supersedes `specs/phase-92-memory-layer-prune.md`). Det-vs-LLM = 4 principles adopted as a maintenance guideline (assumption gate = the model to emulate; enforce-memory redesign-or-retire at Phase 95).
- **Phase 91 outcome** ([[memory-mcp-consumer-e2e-fix]], high; [[assumption-gate-hook-binding]], high): Track 1 fixed the memory consumer E2E break (`-m memory_server` resolves only from a package-bearing cwd → dead in every consumer; PYTHONPATH=$HOME/.claude env on the registration, landed in ~/.claude/settings.json + ~/.claude.json, verified by FIRING a consumer memory_search — DB created) so Phase 92's prune judges a WORKING layer (couldn't-fire is inadmissible prune evidence). Track 3 bound the assumption gate with `enforce-assumption-gate.sh` (PreToolUse, `--gate`-only after whole-file `--schema` false-locked aml-substrate), armed in kit + aml-substrate, STAGED in 4 consumers, signal-watch deferred (needs install.sh --project-local). 18 project hooks total / 17 in modules.json hooks array (1 global). USER OVERRIDE: maintainer-directed consumer propagation. Open: A4 don't-know (Phase-90 recovery additivity — revisit at the B2 pilot); the unbuilt idempotent install.sh re-sync mode; DRQ-1 (multi-PreToolUse-hook stderr surfacing).
- **Phase 89 outcome** ([[dogfood-demand-evidence-round]], high): pre-registered four-stage evidence round executed clean — 3 real headless edge-screener sessions on the post-trim re-synced harness (detect-loop deregistered there; ed7503d in its repo); A4/A6 demand evidence filed in Blockers (`eval/dogfood-round/evidence/memory-demand.md`: es tallies three-way-clean ZERO on a liveness-probed live layer; kit-side writer LIVE; the continuity case — the layer's design purpose — served entirely by the .dev-wiki file substrate); trim-trial windows accruing in `evidence/window-events.md` with the Phases-90-93 per-session append obligation pinned in active-phase.md; disposition authority unchanged (trims = Phase-93 debrief; memory layer = future prune round). Ledger A4 BIT (stale-premise class). Defect filings: py-review docs-only Stop-block (noise-candidate), edge-screener Phase-10 gate pending.
- **Phase 88 outcome** ([[trim-round-outcome]], high; method [[trim-follow-on-round]], high): 2 trim-trials SHIPPED (ak-ride-along d43950f — active-knowledge re-presentation machinery incl. the wiki-query Step-8a second writer; wk-seeding df3e623 — EXECUTION-CORRECTED: the real debrief WK writer was the Step-19 carry-forward removed at d43950f, trials REVERT-COUPLED), windows through Phase 93 with restore-or-confirm at the Phase-93 debrief; detect-loop CUT 75b48af (impossibility, never demand; re-trigger = platform failure events); check-tests-were-run HARDENED b8bd416; 3 stage-2 checker tightenings 6677157 (Phase-87 verdicts stand as recorded); enforce-memory KEEP on real follow-through evidence; loader-writer-heft + journal-prose DROPPED at checkpoint. Eval 50; hooks 16; tests 27 scripts.
- **Phase 87 outcome:** spec-generation = **UNDECIDABLE** ([[stage2-episode-outcome]], high; design [[stage2-episode-execution-design]], high) — the episode did not resolve the Phase-86 ambiguous-stage-2 verdict in either direction: arm B (minimal) won the pinned changed-lines tie-break but was SHIP-BLOCKED by the orchestrator validity sweep (modified golden-master fixture proved load-bearing; restore-baseline drift failure reproduced both directions); arm A (full ceremony) DNF'd on the done-sentinel while passing the frozen triple (394/94.58) with zero validity violations → shipped to edge-screener a6effcb via the separate ship checkpoint. Canary CLEAN; positive control non-discriminating (both arms surfaced). Caveats: bundle attribution, canned gates (DNF partly gate-cadence artifact), n=1. The queued trim follow-on round proceeds on stage-1 evidence alone.
- **Phase 86 outcome:** first measured ceremony-step disposition ([[ceremony-step-verdicts]], high; apparatus [[ceremony-lift-tiered-screen]], high): cost ~66% adj tokens / ~64% wall (12 sessions); verdicts dev-plan=trim, spec=ambiguous-stage-2 (→ Phase 87), approach-rev=keep, plan-rev=keep-by-immateriality, review-gate=underpowered-keep-direction, debrief-capture=trim; the review gate convicted taxonomy-widening against the FROZEN pre-registration (A2 bit, STOP fired + accepted).
- Recent phase outcomes (full detail in journals + phase articles): Phase 85 install-gap fix + dogfood ([[install-gap-dir-currency]]); Phase 84 hook hygiene + 11-ghost deregistration ([[hook-registration-hygiene]]); Phase 83 prune-on-value 2 cuts/2 keeps/2 hardens ([[prune-on-value-subtraction]]); Phase 82 QA sweep 58→35 fixed ([[qa-verification-sweep]]); Phase 81 assumption gate ([[assumption-approval-gate]]); Phase 80 INSTRUMENT-DEAD surfacer screen = 5th amplifier-null ([[assumption-surfacer-completeness-screen]]).
- Amplifier program (closed): 5 nulls across Ph70/71/77/78/80 — harness headroom does NOT live in re-presenting decisions, tooling-correctness, or assumption-surfacing the model can recover; surviving avenue = genuinely proprietary/post-cutoff retrieval. Records under `eval/amplifier/` + `eval/assumption-screen/`.
- Roadmap: [[roadmap-gap-analysis|Engineering Gap Analysis]] -- 1 OPEN gap (4.1 language-agnostic core, DEFERRED YAGNI). [[roadmap-cognitive-enhancement|Cognitive Enhancement Roadmap]] -- 7/7 complete.
- Release: v0.5.0 tagged and pushed. Benchmark: FTS5 recall@5 91.0%, hybrid/turn ~95% estimated (LongMemEval-S).
- Knowledge wiki: wiki/ (cognitive-patterns) -- 11 heuristics + 5 IRON RULES + 4 domain pattern articles.
- Retro (Phase-93 debrief, 75 completed): no systemic issue — the one low-signal recurring class is registered-but-broken / ghost-registration (bitten 5× Ph79/82/84/85/88), exactly what Phase 93 mechanizes away (dereg-behind-rails + `check-install-drift.sh --consumer`); 0 decision reversals; 0 user corrections this window (gate all-accept A1-A5). Residual: the dereg primitive is sandbox-only — the gated live-application follow-on (Blockers) closes the loop. Prior retros: Phases 85-89 (Stop-hook noise on non-code work — Ph85 ctw + Ph89 py-review docs-only, filed harden candidates); 81-85 instrument-validity dominant.
