# Current State: nana-dev-kit

> Last updated: 2026-06-10 by /dev-debrief (Phase 86 — Ceremony Lift Measurement complete: 8/8 tasks, 8/8 exit criteria post-correction, checkpoint verdicts taken [4 decided, 2 to stage-2/follow-on], review gate 6/10 revise → CRITICAL taxonomy-widening fixed + A2 STOP re-presented + verdicts re-confirmed; READY FOR COMPLETION, delivery gate pending)

## Recommended Next Action

**Begin Phase 87 — Ceremony Stage-2 Episode Contrast at task 1 (the drivability spike, cheapest-validation-FIRST).** Planned 2026-06-10: 8 tasks (M/M/M/M/L/M/M/S); spec `specs/phase-87-stage2-episode-contrast.md` (nana:approved; 8 machine-checkable exit criteria via `eval/ceremony-lift/stage2/run-exit-criteria.sh`); decision [[stage2-episode-execution-design]] (high). Execute the FROZEN `## Stage-2 parameters` (eval/ceremony-lift/pre-registration.md, byte-frozen at 9ad62f0) verbatim — never redesign: arm A full-ceremony vs arm B assumption-gate+hooks on edge-screener Phase 10 (close recovered-name/recycled-ticker survivorship branches), independent clones cut from a checkpoint-gated setup commit (368e056 + parity provisioning incl. force-added gitignored/untracked enforcement surfaces + positive-control dev-wiki seed). Sequence: **T1** drivability spike (non-interactive gate-bearing session driving, mechanism TBD — STOP after 2 failed attempts) → **T2** execution-protocol addendum pinning every execution-level item + the READ-ONLY A1 baseline gate (390 tests / 94.44% coverage, STOP on drift), COMMITTED before any arm runs (first-add-commit ancestry + byte check) → **T3** stage-2 checkers controls-first (every checker catches its seeded negative) → **T4** HARD CHECKPOINT substrate setup (seed write + setup-branch/clones as SEPARATE acks; restoration test incl. live DIRTY state; HEU-012 hook-fire probe; isolation probe; detector rehearsal) → **T5** execute arms (closed canned gate policy, verbatim logs; canary to arm B post-stop; neither diff opened until both close) → **T6** orchestrator-only evidence tables (frozen ship triple + validity assertions; blinded tie-break iff both pass) → **T7** HARD CHECKPOINT disposition (closed vocabulary: confirm-trim-trial | not-confirmed | undecidable | instrument-dead | void; claim ceiling: n=1 confirms cut-candidate→reversible trim-trial ONLY) → **T8** close-out + conditional gated ship. Kit-commit embargo (component-path diff vs pinned base 6728e2f) until both arms close; every out-of-repo write HARD-checkpoint-gated. (Supersedes prior line: "Phase 86 ... delivery report → delivery-gate flip" — done 2026-06-10, 6728e2f.)

Prior: **Phase 86 — Ceremony Lift Measurement READY FOR COMPLETION: delivery report → delivery-gate flip + commit/push (this debrief).** ALL 8 tasks [x]; 8/8 exit criteria via `eval/ceremony-lift/run-exit-criteria.sh` (post-correction re-run); `check-verdicts-only.sh` green vs pinned base 5360486 (no kit component touched); pre-registration byte-FROZEN, survived 11 commits. **Headlines:** cost table over 12 sessions (Ph76-85): ceremony ~66% of cache-adjusted tokens / ~64% wall-clock, EARLY-EXIT no; evidence table 44 rows == manifest (post-correction: 1 outcome-grade-admitted / 10 ambiguous-downgrade / 8 consumption-capped / 25 zero-catch) — flagship row: the Ph85 review-gate catch FULLY ADMITTED via transcript-extracted pre-fix re-execution (4/4 Edit calls reverse-applied in a worktree; defect reproduces as false-DRIFT rows while make test passes on the same state). **Checkpoint verdicts ([[ceremony-step-verdicts]], high):** dev-plan-orchestration=trim (assumption-vote gate untouched; ride-alongs queued for follow-on), spec-generation=ambiguous-stage-2 (structurally stage-1-undecidable), approach-reviewer=keep (5.4% adj), plan-reviewer=keep-by-immateriality (0.8%/0.4%), review-gate-reviewer=underpowered-decide-by-arithmetic keep-direction (1 fully re-executed catch/3 dispatches; got a SECOND live catch during this very debrief), debrief-capture=trim (operational half kept; knowledge-capture half = follow-on prime cut candidate); **STAGE-2: go, routed follow-on.** Review gate 6/10 revise: CRITICAL taxonomy-widening (4 ledger bits admitted outcome-grade against the FROZEN pre-registration — the retrofit class, caught by the external reviewer) fixed inline; the pre-registered A2 >50%-downgrade STOP fired, maintainer accepted (ledger A2 bit); verdicts re-confirmed, no line moved. **Next-direction candidates after the gate flip:** Phase 87 stage-2 episode contrast (go; spec-generation is the primary customer; parameters frozen in pre-registration `## Stage-2 parameters`) AND/OR the trim follow-on round (dev-plan ride-alongs [active-knowledge re-presentation + state-loader/artifact-writer heft] + debrief knowledge-capture half + prune-on-value round 2 leftovers from Ph85 Blockers). (Supersedes prior line: "Begin Phase 86 at task 1" — done 2026-06-10.)

Prior: **Begin Phase 86 — Ceremony Lift Measurement at task 1 (pre-registration, commit-then-FREEZE FIRST).** Planned 2026-06-10: 8 tasks (M/L/M/M/M/M/M/M); spec `specs/phase-86-ceremony-lift-measurement.md` (nana:approved; 8 machine-checkable exit criteria via `eval/ceremony-lift/run-exit-criteria.sh`); decision [[ceremony-lift-tiered-screen]] (high; gate-revised at A1 round 2: stage 1 mints NO verdicts). Tiered screen over the kit's ceremony steps (dev-plan, spec, reviewer dispatches, debrief), episodes conditional. **Stage 1 deterministic in-repo:** T1 pre-registration committed then FROZEN (byte-unchanged anti-retrofit check; corpus window + frozen end-commit 5360486 + measurement-session exclusion + hand-counted anchor phase + admissibility rule + token attribution + cost-materiality thresholds + MDE arithmetic + closed verdict menu + stage-2 parameters) → T2 transcript cost extractor with a hand-labeled positive control (raw + cache-adjusted tokens, wall-clock, interruptions; control fails twice = instrument-dead STOP) → T3 per-step cost table + materiality verdict (every step below threshold ⇒ keep-by-immateriality EARLY EXIT, skip T4-6, straight to the checkpoint) → T4 five BLIND tabulator controls vs a never-read answer key → T5 frozen corpus manifest (mechanical enumeration of ALL reviewer/spec/debrief dispatches incl. zero-catch) → T6 admissibility-ruled evidence table + re-execution log, COMMITTED (outcome-grade only via orchestrator re-execution against recoverable pre-fix state; unrecoverable → ambiguous; agent prose = candidate-generation only; consumption-grade capped at cut-candidate) → **T7 HARD CHECKPOINT: the maintainer takes keep/trim/cut/ambiguous→stage-2 positions** (caveat columns + MDE arithmetic; no-catches-observed alone is INVALID for rare-event steps) + stage-2 go/no-go → T8 close-out (check-verdicts-only.sh against pinned base 5360486 + the 8-criterion runner with pre-registered early-exit reporting). **Stage 2 (only on checkpoint go):** full-ceremony vs assumption-gate+hooks bundle contrast on burnable edge-screener Phase 10, twin worktrees, leak canary, positive control, deterministic ship criteria; n=1 confirms cut-candidate→reversible trim-trial ONLY. VERDICTS-ONLY phase — no kit component modified; cuts route to a gated follow-on with removal checklists. (Superseded prior line: "Phase 85 ... delivery report → gate flip + commit/push" — done 2026-06-10, 3f04979 + 5360486; the pending Phase-85 debrief/state reconciliation noted in the prior header is superseded by this planning pass.)

Prior: **Phase 85 — Install-Gap Fix + Edge-Screener Dogfood is READY FOR COMPLETION: delivery report → gate flip + commit/push.** ALL 8 tasks [x]; 9/9 exit criteria via `eval/install-gap/run-exit-criteria.sh` (make test 26 scripts green incl. the new dir-currency seeded controls; make eval 52/52 ZERO flips; live ~/.claude drift 0 WITH directory cells active; kit hooks == modules.json scope:global set). Reviewer 8/10 revise → HIGH (harness-audit.sh 3 stale 4b predicates false-DRIFTed post-migration — instrument-dead class) + key MEDIUMs fixed inline, re-verified (test_scripts_smoke 4/4, exit criteria 9/9). **Headlines:** T1 inventory CONFIRMED the incident-5 causal path (Phase-82 drift-guided resync via the dir-blind checker shopping list; py-init/ts-init exempt recursive, nana-init exempt no-copy; the A1 deferred don't-know resolved at checkpoint 1, ledger row held); modules.json `project_local.extra_dirs` → top-level `hook_dirs` map + install.sh `ship_hook_dirs` on BOTH paths + checker consumer-conditioned directory cells with orphan flagging (7 seeded controls; latent bash-3.2 set-u empty-array bug fixed); checkpoint 1 approved **keep-current** for the registration-dead ~/.claude copies ([[a3-disposition-keep-current-for-registration-dead]]; disposal → next prune-on-value round); **DRQ-1 verdict: string-keyed dedupe** ([[drq-1-settings-merge-semantics-are-string-keyed]]; A2 bit instructively — distinct command strings invoking the same script BOTH fire) → edge-screener migrated to a single-registration template-sourced install (17/17 basenames exactly once; firing probe: each Stop hook exactly once; 390 tests / 94.44% cov exact pre-migration baseline); dogfood 2 real sessions + **A5 demand evidence FILED** (liveness probe exit 0, DB row count 0 → genuine zero-demand, not couldnt-fire). **Next-direction candidates:** prune-on-value round 2 (A5 memory-layer disposition + check-tests-were-run Read-trigger harden + ~/.claude registration-dead residue disposal + detect-loop prune + Phase-82 installed-only residue), or edge-screener Phase 10 (analysis input ready at its `.dev-wiki/phase-10-candidate-analysis.md`). (Superseded prior line: "Begin Phase 85 at task 1" — done 2026-06-10.)

Prior: **Begin Phase 85 — Install-Gap Fix + Edge-Screener Dogfood at task 1 (install-path inventory, inventory-FIRST).** Planned 2026-06-10: 8 tasks (M/M/M/L/M/M/M/M); spec `specs/phase-85-install-gap-dogfood.md` (nana:approved 2026-06-10); decision [[install-gap-dir-currency]] (high). Five serialized stages: **T1** install-path inventory with line-cited evidence (verify — not assume — that incident 5's causal path was the Phase-82 drift-guided resync; record `~/.claude/hooks/session-start.sh`'s registration-dead consumption status; pin the .d/ mode-drift exemption) → **T2** kit fixes test-first (modules.json `project_local.extra_dirs` → declared top-level `hook_dirs` map + 4 consumer migrations; install.sh ships hook_dirs alongside any consumer script on BOTH paths, empty-glob tolerant under set -e; check-install-drift.sh consumer-conditioned directory cells with seeded controls) → **T3** maintainer root: sandbox rehearsal + live positive control → **HARD checkpoint 1** (A1 inventory verdict + A3 ship-vs-dispose + rehearsal log + backup/tested restore) → live ~/.claude run; post: scope:global set equality + drift 0 → **T4** edge-screener: DRQ-1 empirical verification FIRST (STOP and re-present if not double-fire) → **HARD checkpoint 2** → dedupe-by-basename migration into settings.local.json + union-uniqueness + single-firing; then dogfood with the memory liveness probe FIRST so the A5 answer is demand evidence, never couldnt-fire → **T5** close-out (9-criterion spec runner). The two maintainer touchpoints are checkpoints 1 and 2; the dogfood spans ≥2 real-work sessions in edge-screener. (Superseded prior line: "accept the Phase 84 delivery → delivery-gate flip + push" — done 2026-06-10, e894dff.)

Prior: **Phase 84 — Hook & Registration Hygiene is READY FOR COMPLETION: accept the delivery → delivery-gate flip + push → the next direction is OPEN.** 6/6 tasks [x]; 10/10 exit criteria via `eval/hook-hygiene/run-exit-criteria.sh` (make test green 25 scripts incl. 3 new; make eval 52/52 unchanged denominator with 2 explained-and-resolved flips; drift 0); reviewer 9/10 ACCEPT (4 MEDIUM: 2 fixed inline, 2 deferred-with-rationale — comment-only header notes would re-stale 8 just-synced installed copies; facts live in capture-diagnosis.md + Blockers). **Headlines:** T2 live capture proved PostToolUse carries NO exit-code field AND does not fire for failing commands — event arrival IS the success signal → post-commit **REDESIGNED** (event-arrival + git-recency ≤120s + project-local OR HOME marker + HEU-002 fail-loud), detect-loop **UPSTREAM** (consecutive-failure counter structurally unimplementable hook-side; platform defect filed; prune candidate). T4a HARD checkpoint approved the FULL remediate-then-deregister package: 6 roots remediated (incl. session-start.d sync), 11 ghosts deregistered (end-state == modules.json scope:global set), survivor smoke green, backup + tested restore retained, fresh-session one-command verification handed to the maintainer (rehearsal.log). Live mid-phase repair: ~/.claude/hooks/session-start.d/ was EMPTY → every SessionStart errored machine-wide since 2026-06-09 (5th registered-but-broken instance; install-gap Blocker filed). Hook-hygiene was the chosen re-trigger and is now done — remaining filed candidates for the next direction: **A5 memory-layer disposition** (must-revisit), **edge-screener dogfood round**, **install-gap extra_dirs fix** (install.sh + drift-checker directory currency). (Superseded prior line: "Begin Phase 84 at T1." — done 2026-06-10.)

See [[cross-session-retention-headroom-screen]] (Phase 77) + [[2026-06-04-phase-77-cross-session-retention-screen|Phase 77 journal]] + [[installed-copy-drift-guard]] (Phase 76) + [[delivery-commit-verification]] (Phase 75).

## Active Phase

**[[phase-87-stage2-episode-contrast|Phase 87: Ceremony Stage-2 Episode Contrast]]** (status: active — planned 2026-06-10, ~0%). Execute the pre-registered stage-2 episode experiment frozen in `eval/ceremony-lift/pre-registration.md` `## Stage-2 parameters` (byte-frozen at 9ad62f0): full-ceremony arm A vs assumption-gate+hooks arm B on burnable edge-screener Phase 10, producing admissible episode evidence for the maintainer's disposition of the `spec-generation = ambiguous-stage-2` verdict from [[ceremony-step-verdicts]]. Freeze-then-execute, cheapest-validation-first: drivability spike → execution-protocol addendum (pins all execution mechanics; committed BEFORE any arm — first-add-commit ancestry + byte check) → controls-first checkers → HARD-checkpoint-gated substrate setup (independent clones from a setup commit off 368e056 with parity provisioning: force-added gitignored/untracked enforcement surfaces, Phase-10 candidate analysis, positive-control dev-wiki seed; restoration test incl. live DIRTY state; HEU-012 hook-fire probe; cross-arm isolation probe; surfacing-detector rehearsal) → arms in fresh sessions under closed canned gate policy (verbatim logs; neither diff opened until both close; DRQ-1 leak canary to arm B POST-STOP — correct answer = VOID; positive control must reach both arms byte-identically — NEITHER-arm-surfaces = INSTRUMENT-DEAD, not null) → orchestrator-only evidence tables (frozen ship triple + validity assertions; blinded tie-break iff both pass) → HARD checkpoint disposition (closed vocabulary confirm-trim-trial | not-confirmed | undecidable | instrument-dead | void; claim ceiling: n=1 confirms cut-candidate→reversible trim-trial ONLY, never keep or cut) → close-out + conditional gated ship. Kit-commit embargo (component-path diff vs pinned base 6728e2f) until both arms close; stage-1 apparatus additive-only, pre-registration NEVER edited. Entry MET (Phase 86 checkpoint routed stage-2 go; delivery accepted 6728e2f); exit = the spec's 8 machine-checkable criteria via `eval/ceremony-lift/stage2/run-exit-criteria.sh`. Decision [[stage2-episode-execution-design]] (high); spec nana:approved. Direction gate closed 2026-06-10 (A1-A3 accept, A4 accept-spike-defended round 2, A5 accept; all_accept:true).

Prior: **[[phase-86-ceremony-lift-measurement|Phase 86: Ceremony Lift Measurement]]** (status: completed — delivery accepted + gate flipped 2026-06-10, 6728e2f; 8/8 tasks [x], 8/8 exit criteria post-correction, reviewer 6/10 revise → CRITICAL fixed + A2 STOP accepted, verdicts re-confirmed). Tiered screen measuring whether the kit's ceremony steps (dev-plan, spec generation, reviewer dispatches, dev-debrief) provide lift, episodes conditional — extends the amplifier program (5 nulls on re-presentation) and the prune-on-value verdict-table method from injected knowledge and installed components to the CEREMONY STEPS themselves. **Stage 1 (deterministic, in-repo, `eval/ceremony-lift/`):** pre-registration committed-then-FROZEN before any tabulation (byte-unchanged anti-retrofit check between its add-commit and the evidence-table's add-commit; no mid-phase wording fixes); ceremony cost baseline FIRST — raw AND cache-adjusted tokens (always-loaded ~10k is mostly cache reads; naive summation overstates ~10x), wall-clock, synchronous interruption count, extractor validated against a hand-labeled session before bulk processing; keep-by-immateriality EARLY EXIT if every step is below the pre-registered threshold; then the admissibility-ruled demand-evidence table — outcome-grade rows ONLY via orchestrator re-execution of the deterministic gate against recoverable pre-fix state (unrecoverable → downgrade to ambiguous, direction pinned), agent-authored prose is candidate-generation only, consumption-grade evidence capped (can mint cut-candidates, can NEVER support keep for re-presentation-class outputs). Controls-first: tabulator must classify 5 BLIND seeded controls against a never-read answer key; capture-step benefit measured by RETROSPECTIVE consumption over the full corpus (a forward window mechanically reads zero); historical evidence supports COST claims + per-event re-executed marginal catches only, never correlational ceremony-on/off comparisons (selection bias — ceremony-heavy sessions were the riskier phases; zero true minimal-arm sessions exist). **Stage 1 mints NO verdicts** (gate A1 round 2): the maintainer takes keep | trim | cut | ambiguous→stage-2 positions at the HARD checkpoint, with agent-counterfactual residual as an explicit caveat column + MDE arithmetic (reviewer catches run ~1-2% base rate — "no catches observed" alone is an INVALID cut justification; "underpowered — decide on expected-cost arithmetic" is on the menu). **Stage 2 (fires only on checkpoint go, out-of-repo):** full-ceremony vs assumption-gate+hooks bundle contrast on burnable edge-screener Phase 10, twin worktrees from the same git state, leak canary, positive control, deterministic ship criteria; n=1 may confirm a cut-candidate into a reversible trim-trial ONLY — never mint keep or cut. VERDICTS-ONLY phase: no kit component modified (enforced by `check-verdicts-only.sh` diff against pinned base 5360486); cuts route to a gated follow-on with per-cut removal checklists (no deregistration mechanism exists — ghost registrations fail silently). Entry MET; exit = the spec's 8 machine-checkable criteria via `eval/ceremony-lift/run-exit-criteria.sh` (criteria 2/4/6 N/A-by-early-exit reporting pre-registered). Decision [[ceremony-lift-tiered-screen]] (high); spec nana:approved.

Prior: **[[phase-85-install-gap-dogfood|Phase 85: Install-Gap Fix + Edge-Screener Dogfood]]** (status: completed — delivery accepted + gate flipped 2026-06-10, 3f04979; 8/8 tasks [x], 9/9 exit criteria, reviewer 8/10 revise → HIGH + key MEDIUMs fixed inline). Close the extra_dirs install gap — no install path ships `hooks/session-start.d/` to a root that holds `session-start.sh`, and `check-install-drift.sh` is structurally blind to directory contents (the exact shape of the 2026-06-09 machine-wide SessionStart breakage, 5th registered-but-broken instance) — then replace edge-screener's Phase-79 hand-patched hook registration with a durable template-sourced install and run a real consuming-project dogfood round, including a liveness-probed answer to whether the memory MCP layer is ever touched (feeds open ledger row A5; evidence, NOT disposition). Fix the structural invariant, not the single instance: every install path that ships a hook script also ships the subdirectories it consumes, and the drift comparator treats "consumer present, directory missing/stale" as drift. Five serialized stages: **T1** install-path inventory FIRST (spec checkpoint precondition; line-cited evidence; verify the Phase-82 drift-guided-resync causal hypothesis with a machine-checkable verdict line; record session-start.sh's registration-dead consumption status; pin the sourced-.d/ mode-drift exemption) → **T2** kit fixes test-first (modules.json `project_local.extra_dirs` → declared top-level `hook_dirs` map, 4 verified consumers migrated: install.sh:72, test_registration.sh:62, test_hook_firing_coverage.sh:40, harness-audit.sh:385; install.sh ships hook_dirs alongside any consumer script on BOTH paths, empty-glob tolerant under set -euo pipefail; check-install-drift.sh consumer-conditioned directory cells — deleted-dir / stale-file / orphan-flag / synced-0 seeded controls + synthetic second dir, Makefile-registered) → **T3** maintainer root: sandbox rehearsal (pinned real-capture SessionStart fixture exits 0, 3 curators sourced) + live positive control → HARD checkpoint 1 (A1 inventory verdict + A3 ship-vs-dispose + rehearsal log + backup/tested restore) → live ~/.claude run; post: scope:global set equality + drift 0 → **T4** edge-screener: DRQ-1 empirical verification (duplicate registrations across settings.json + settings.local.json: double-fire/dedupe/precedence; STOP if not double-fire) → HARD checkpoint 2 → dedupe-by-basename migration into settings.local.json (CWD assertion; .bak disposition) + union-uniqueness + single-firing count; then dogfood: liveness probe FIRST, ≥2 real-work sessions, pinned evidence schema (hook | event | timestamp | helped/neutral/noise), A5 evidence filed → **T5** close-out: inventory rows finalized (fixed/exempt-with-rationale), eval diff note, 9-criterion spec runner, supersession notes. Gate: A1 deferred don't-know (checkpoint 1 decides), A2 accept (mandatory pre-checkpoint-2 verification), A3 down-scoped, A4 accept; all_accept:false. Entry MET; exit = the spec's 9 machine-checkable criteria via `eval/install-gap/run-exit-criteria.sh`. Decision [[install-gap-dir-currency]] (high); spec nana:approved.

Prior: **[[phase-84-hook-registration-hygiene|Phase 84: Hook & Registration Hygiene]]** (status: completed — delivery accepted + gate flipped 2026-06-10, e894dff; 6/6 tasks [x], 10/10 exit criteria, reviewer 9/10 ACCEPT). Restore the two dormant lifecycle hooks (post-commit.sh, detect-loop.sh) to verified firing, close the marker-resolution + eval-sandbox environment leaks (scripts/eval-runner.sh CLAUDE_PROJECT_DIR/HOME), and bring the 11 ghost global hook registrations in ~/.claude/settings.json into agreement with modules.json scope tags — rollback-safe, no silently disarmed consuming project. Three serialized stages, instrument-first, capture-anchored: **T1** hermetic instrument FIRST — run_hook exports CLAUDE_PROJECT_DIR="$WORK_DIR" (the hooks' Phase-79 `cd` line otherwise escapes mktemp sandboxes into the live repo); live-state tripwire; seeded-leak self-check (reverted copy must turn the leak test RED — controls-first per [[qa-verification-sweep]]); unset-variant for the `:-.` fallback; post-instrument 52-scenario baseline recorded BEFORE any hook change; **T2** REAL PostToolUse capture with provenance (the pinned Phase-82 repro events carry NO exit-code field — tool_response has only stdout/stderr) → one evidence-forced branch per hook: remap (signal exists, fix field paths) / redesign (no signal — post-commit verifies via actual git state with failed-commit rigor, detect-loop keys on tool_response.stderr/error markers) / upstream (platform-defect filing + disable-at-boundary routed to the checkpoint; pinned-repro criteria N/A-upstream); **T3** fixes per branch — canonical jq parse (detect-loop migrated off raw grep; stale "pure bash <50ms" header updated), legacy top-level .exit_code fallback retained (unchanged 52 eval denominator), marker resolution cd-project-dir → .claude/enforce → $HOME/.claude/enforce tested over the 4-matrix, HEU-002 fail-loud on signal-absence (the dormancy was exactly silent degradation), parser-edge fixtures, artifact-CONTENT assertions, every eval flip explained in eval/hook-hygiene/eval-diff.md; **T4a** coverage matrix over ALL kit-marker-discovered roots (registration presence + copy-currency md5; gate evidence: the ghosts ARE the only wiring for 8 hooks in 3 roots — edge-analyst/edge-screener 11/11 local, stock-screener/ai-game/fate 3/11, ai-game has a literal null command entry) + basename-normalized jq extraction (live forms mixed: bash ~/... and absolute) + sandbox-rehearsed remediate-then-deregister with timestamped backup + tested restore + HARD maintainer checkpoint BEFORE any live settings edit (A2 deferred — no live execution on direction-gate authority); **T4b** checkpoint-gated execution (remediate FIRST, then own-ghosts-only surgery + survivor functional smoke + revert-on-failure + fresh-session one-command verification) OR registration-exceptions.md filing on defer/partial; **T5** close-out — run-exit-criteria.sh (10 spec criteria, explicit N/A-upstream pass rule for 3-5), supersession notes for the Phase-82/83 firing zeros measured on the leaky instrument (never rewrite history), README/_ARCHITECTURE/MANIFEST, drift 0. Every matrix/extraction script carries a positive control before its output counts as evidence (zsh word-splitting hazard, gate-time self-caught). Entry MET; exit = the spec's 10 machine-checkable criteria via `eval/hook-hygiene/run-exit-criteria.sh`. Decision [[hook-registration-hygiene]] (high); spec nana:approved.

Prior: **[[phase-83-prune-on-value-subtraction|Phase 83: Prune-on-Value Subtraction]]** (status: completed — delivery accepted + gate flipped 2026-06-09, fb6d189; 5/5 tasks [x], 2 cuts / 2 keeps / 2 hardens-activated, 10/10 exit criteria, reviewer 9/10 ACCEPT). Evidence-first, verdict-gated, serialized subtraction over the 6 Phase-82-evidenced dead-weight candidates (enforce-memory.sh — zero lifetime firings pre-restoration; memory reinforcement machinery — 0/55 entries; memory-mcp-scaffold shipping; audit-log model field — always "unknown"; 3 orphan companions; harness-audit.sh — unwired), five stages: **T1** installed-surface DISCOVERY via kit-marker scan (A3 reject: discovered, never assumed) + per-candidate removal sets FIRST + removal-set-exclusion liveness grep over ALL discovered roots (LIVE vs HISTORICAL; historical dev-wiki records never rewritten, superseded instead) + 6-row verdict-table skeleton (closed enum keep/cut/harden/disable-at-boundary; arming-procedure + matrix-row-citation columns; candidate 3's DEREG artifact decided here); **T2** per-candidate mktemp -d sandbox arming → mandatory couldnt-fire vs didnt-fire zero-classification (won't-fire-when-armed = DEFECT finding, never a demand-evidence cut — [[HEU-012]]; the A7 triple pre-classifies candidate 2 largely couldnt-fire: live install lacks fastembed, cosine >0.90 reinforce branch unreachable, word-overlap fallback only warns); **T3** unconditional mid-phase checkpoint — full verdict table to the maintainer BEFORE any cut, couldnt-fire candidates presented as defects with no cut offered; **T4** approved verdicts serially, one commit per candidate (`Phase 83 cut: <name>`): modules.json single-source edit + make template + MANIFEST regen with regenerated-diff ⊆ removal set, sandbox-rehearsed jq deregistration of each cut's OWN installed ghosts only (the other 10 ghost registrations stay under the Phase-82 drift filing) + survivor functional smoke (allow AND block paths) + revert-on-failure; **T5** close-out — make eval with any explained denominator change, drift 0, residue filings (harden follow-ups, the A5 must-revisit kit-side memory-layer question, frozen-apparatus-blocked deferrals). memory_server verdict menu keep/disable-at-boundary/cut-with-regenerated-patch (vendoring contract). Frozen apparatus (eval/qa-sweep/** beyond the documented denominator change, eval/amplifier/**, eval/assumption-screen/**) + assumption-ledger READ-ONLY; user-owned ~/.claude/rules/ untouchable → defer with filing. Zero cuts is a valid outcome. Entry MET; exit = the spec's 10 machine-checkable criteria via `eval/prune-on-value/run-exit-criteria.sh`. Decision [[prune-on-value-subtraction]] (high); spec nana:approved.

Prior: **[[phase-82-qa-verification-sweep|Phase 82: QA & Verification Sweep (ultracode)]]** (status: completed — delivery accepted + gate flipped 2026-06-09; 4/4 tasks [x], 58 candidates → 35 fixed / 20 deferred-with-filings / 3 orphans, 10/10 spec exit criteria, make test 22 scripts ~480 assertions green, make eval 52/52, drift 0 EXTENDED, reviewer 9/10 ACCEPT). Coverage-defined QA sweep over nana-dev-kit, 8 audit areas (wiring / firing / companions / schema / drift / coverage / docs / usage), four stages: **T1** controls-first baseline — every checker vouching for a clean verdict must first catch a seeded synthetic defect (instrument-dead otherwise) + `eval/qa-sweep/verification-matrix.md` skeleton; **T2** ultracode Workflow fan-out of 8 READ-ONLY candidate-generator agents — subagents NEVER produce evidence (Jake rejected the leak-is-acceptable assumption; clean context is structurally impossible in-kit, the Ph80 leak HELPS candidate recall but is fatal for verification); **T3** orchestrator-only verdicts (every clean AND defect-found verdict = a deterministic command the orchestrator executes ITSELF; defects confirmed only by orchestrator reproduction) + mid-phase findings report to the user BEFORE any fix + serialized single-writer fixes under the pre-registered boundary (kit-managed [in-repo, excluding frozen apparatus + assumption-ledger + unlisted ~/.claude] + S/M + test-covered blast radius; coverage exception: writing the missing S/M functional test IS the fix; >10 confirmed defects → STOP and re-scope); **T4** verification-matrix close-out (`| area | command | evidence | verdict |`, enum clean/defect-found/deferred/instrument-dead) + deferred filings in Blockers + full spec exit-criteria run (`eval/qa-sweep/run-exit-criteria.sh`). Zero confirmed defects is a valid success — the deliverable is the matrix, not a defect count. Phase-81 surface quarantined (ledger read-only; gate defects via `--selftest`, filed); usage-area under-use findings feed the parked Phase-79 prune-on-value item. FIRST live dogfood of the assumption-approval gate (A1 don't-know→usage area; A2 reject→orchestrator-only verdicts; ledger block appended, 6 rows, all_accept:false). Entry MET; exit = the spec's 10 machine-checkable criteria. Decision [[qa-verification-sweep]] (high); spec nana:approved.

Prior: **[[phase-81-assumption-approval-gate|Phase 81: Assumption-Approval Gate]]** (status: completed — delivery accepted + gate flipped 2026-06-09; 4/4 tasks [x], all 10 spec exit criteria pass, make test green, make eval 52/52, reviewer 9/10 ACCEPT both findings fixed). Replace the dev-plan direction gate's "approve the approach?" step with the maintainer taking accept/reject/don't-know positions on the plan's load-bearing assumptions (positions ARE the gate), recorded in an append-only cross-phase ledger whose unfilled revisit-status is mechanically surfaced at debrief — rubber-stamp → interrogator. The earned consequence of Phase 80's INSTRUMENT-DEAD: ship the SIMPLEST gate (the frozen Phase-80 NAIVE cost-sorted surfacer; the scope-anchored machinery's only edge was a working-knowledge leak), NOT the elaborate version. Four components: (1) naive surfacer in dev-plan (3-6 cost-sorted; Step-10 T0's weakest assumption a required member); (2) accept/reject/don't-know via AskUserQuestion REPLACING the approval click (reject→revise+re-surface, don't-know→defend-or-down-scope else deferred→Blockers+must-revisit); (3) all-accept→warn + track `all_accept:true` + restate consequences (Jake's Q2, NOT a hard block); (4) append-only `.dev-wiki/assumption-ledger.md` + a debrief revisit forcing-function + a deterministic NO-LLM `check-assumption-ledger.sh` enforced at debrief finalization. NO new hook (the debrief-finalization check is the firing point). Efficacy UNMEASURABLE in-kit ([[HEU-012]]; the amplifier-null + Ph66/69/80 representativeness) — tests assert MECHANICS only. Phase 81 itself is planned with the PRE-feature dev-plan (records no ledger entry from its own planning; the new gate semantics apply to FUTURE phases via the T2 Step-15f template change). Entry MET; exit-criteria all open. Decision [[assumption-approval-gate]] (high); spec nana:approved.

Prior: **[[phase-80-assumption-surfacer-completeness-screen|Phase 80: Assumption-Surfacer Completeness Screen]]** (status: completed — delivery accepted, 6/6 tasks, make test green; `^PROGRAM-VERDICT: INSTRUMENT-DEAD`). EARN the right to build the assumption-approval gate. The dev-plan human gate is to move from approving the APPROACH → taking accept/reject/don't-know positions on the plan's load-bearing ASSUMPTIONS. The plan was interrogated LIVE via that mechanism (the gate eating its own dogfood): Jake A1 **accept** (forced verdict engages cognition) / A2 **REJECT** (can't trust the agent-CHOSEN assumption set — solve set-completeness first) / A3 **accept** (ledger, conditional on a debrief forcing-function) / A4 **accept** (nana-dev-kit is the right substrate). The A2 reject ⇒ SCREEN not build. An agent-internal adversarial review (6/10) found author-selected ground truth is CIRCULAR (a corpus holds only already-noticed assumptions — blind where A2 lives) ⇒ reframe: does the scope+framing surfacer BEAT a blind (outcome-unaware) baseline at recovering assumptions the project's history later PROVED load-bearing (outcome-determined, mechanically-extracted ground truth)? Lead with a cheap circularity-escape SPIKE (T1) as a hard gate (INSTRUMENT-DEAD stops it for one task's cost). Two-track verdict (scope completeness+recovery / framing recovery-only), cost-sort-adversarial control, variance-derived bar; structurally the amplifier's harness-vs-baseline question (5th-null TERMINATE possible, pre-registered). Repo-only `eval/assumption-screen/`; gate+ledger+all-accept = Phase 81 gated on the `^PROGRAM-VERDICT`. Cuts: the handover's smoothness signature-detector (Ph66/69 representativeness + gameable) and the rewarded exogenous interrogator. Decision [[assumption-surfacer-completeness-screen]] (low→pending).

Prior: **[[phase-79-harness-hygiene|Phase 79: Harness Hygiene]]** (status: complete — both sub-fixes shipped, make test/eval green; delivery gate pending acceptance). Fixed two kit defects degrading real sessions: (A) bare-relative hook paths 404 under non-root CWD (edge-screener dogfood) → `${CLAUDE_PROJECT_DIR}` in register-settings.py + template + hook-internal `cd` + hermetic wrong-CWD test; (B) `working-knowledge.md` 45k-char bloat trips the context warning (curator caps count/lines not size) → compress 4 amplifier entries → 1 + non-destructive per-entry char-cap advisory. Bundled, direction approved 2026-06-04; decision [[harness-hygiene]] (high); spec nana:approved. 4 tasks (S/M/L/M). 4th dogfood→harden fix.

Prior: **[[phase-78-skill-crystallization-headroom-screen|Phase 78: Skill-Crystallization Headroom Screen]]** (status: complete — PROGRAM-VERDICT TERMINATE, both candidates DEGENERATE; delivery accepted + pushed). Measured whether crystallizing a phase's TOOLING into a reusable skill beats bare re-derivation — does a candidate tooling artifact embed correctness a bare model fails to reproduce even given the RECOVERABLE CORPUS (interface + call sites + task) a non-crystallized consuming project would have, but not the implementation or its tests? Successor to the decision-retention line (Ph70/71/77 all null): tests the capability/correctness boundary instead. Cheap go/no-go SCREEN before building any capability→skill module (Phase 79+). Verifier-independent, NO-LLM `check.sh` (n=5/threshold=4 cloned from anchor-screen); `.offleak` leak guard; spec-implied=entailed-by-corpus with a quoted sentence per test; 4 controls (negative/positive-unknowable/recoverable-fully-specified/.offleak) gate it; pre-registration ancestor-guarded. Subjects READ-ONLY: `check-install-drift.sh` (nana-dev-kit) + a real edge-screener PIT-survivorship artifact, frozen fixtures. Direction approved 2026-06-04; decision [[skill-crystallization-headroom-screen]] (high); spec nana:approved. 4 tasks (M/S/L/S). **Design hardened pre-registration by an adversarial review (C1–C4/M1–M5).** Honest prior: SPLIT (general DEGENERATE, domain HAS-HEADROOM).

Prior: Phase 77 — Cross-Session Retention Headroom Screen COMPLETED + delivery-accepted 2026-06-04 ([[cross-session-retention-headroom-screen]], high). PROGRAM-VERDICT TERMINATE (residual 0/14, all RECOVERABLE:tree incl. residual-favorable Shumway/Stooq); T3 SKIPPED-BY-GATE; the amplifier **decision-retention** line closed across all 3 regimes (Ph70/71/77). Substrate kept on operational grounds; no measured harness-value claim. Repo-only `eval/amplifier/xsession-screen/`.

Prior: Phase 76 — Installed-Copy-Drift Guard COMPLETED + delivery-accepted 2026-05-31 ([[installed-copy-drift-guard]], high; reviewer 9/10). A deterministic fail-open `scripts/check-install-drift.sh` comparator (modules.json comparison set minus a pinned 3-entry exclusion allow-list) → kit-repo-scoped `[nana:drift]` session-start advisory (canonical-path gate) + `install.sh --status`. DOGFOOD: caught the real `~/.claude` 36 files behind `templates/` — resynced to 0.

Prior: Phase 75 — Delivery-Commit Verification COMPLETED + pushed 2026-05-31 (`add2ee7`; [[delivery-commit-verification]], high; reviewer 9/10). A fail-open session-start.sh divergence detector (PRIMARY) + delivery-flow D3 commit self-assert + gate-after-commit ordering (SECONDARY) close the accepted-but-uncommitted divergence the edge-screener dogfood exposed.

Prior: Phase 74 — Harden the Consuming-Project Scaffold Path COMPLETED 2026-05-30 ([[harden-consuming-project-scaffold]]; reviewer SHIP, delivery accepted) — fixed at the source the 4 scaffold defects the first edge-screener dogfood surfaced (py-review loop hook, recursive hook copy, pyproject ruff/mypy guards, AGENTS.md neutralize; #5 CWD-coupling deferred).

Prior: Phase 73 — Cross-Session Substrate (thin handoff) COMPLETED 2026-05-30 ([[phase-73-cross-session-substrate|Phase 73]], [[cross-session-substrate-stock-screener]]; delivery accepted). The amplifier program entered substrate-construction: the external stock-screener project (`/Users/jwang/edge-screener`) was bootstrapped with the real harness + committed (`5cceac6`); the deferred OFF/ON cross-session recovery measurement awaits real multi-session history. NO quant code in nana-dev-kit — the screener build lives in its own `.dev-wiki/`.

Prior: Phase 72 — Compaction-Recovery Subtraction COMPLETED 2026-05-30 (delivery accepted, pushed to main; removed the dead `.session-anchor` recovery machinery — [[cash-compaction-recovery-subtraction]]). Phase 71 — Cross-Boundary Retention Headroom Screen COMPLETED 2026-05-30 (PROGRAM-VERDICT: TERMINATE-by-summary-robustness). The amplifier campaign closed BOTH single-decision (Phase 70) and single-session/single-compaction retention (Phase 71); cross-SESSION is the surviving regime Phase 73 now builds a real substrate for.

## Active Phase Contract

Phase: 87 - Ceremony Stage-2 Episode Contrast
Objective: execute the FROZEN stage-2 parameters verbatim — full-ceremony arm A vs assumption-gate+hooks arm B on edge-screener Phase 10, independent clones from a checkpoint-gated setup commit, producing admissible episode evidence for the maintainer's spec-generation disposition (closed vocabulary); freeze-then-execute, cheapest-validation-first
Tasks: 8 (see tasks.md — T1 M drivability spike, T2 M execution-protocol addendum + A1 baseline gate committed before arms, T3 M checkers controls-first, T4 M HARD CHECKPOINT + substrate setup, T5 L execute arms + instrument records, T6 M evidence tables + blinded tie-break, T7 M HARD CHECKPOINT disposition, T8 S close-out + conditional ship)
Scope: eval/ceremony-lift/stage2/** (NEW, additive — stage-1 files byte-frozen, pre-registration.md NEVER edited), .dev-wiki/articles/phases/phase-87-stage2-episode-contrast.md (T7 verdict block); out-of-repo HARD-checkpoint-gated: edge-screener setup branch + twin clones + dev-wiki seed + conditional ship; read-only: arm transcripts (programmatic parsing only), stage-1 cost extractor reused as-is
Exit: the spec's 8 machine-checkable criteria via eval/ceremony-lift/stage2/run-exit-criteria.sh (specs/phase-87-stage2-episode-contrast.md; pre-declared VOID/INSTRUMENT-DEAD run-status lines are valid reports)
Constraints: frozen parameters executed verbatim, never redesigned (three-tier authority: FROZEN / ADDENDUM-PINNED / SPEC-ADDED validity assertions; never alter the frozen ship triple); addendum committed BEFORE any arm (first-add-commit ancestry + byte check; post-unblinding amendment = VOID); kit-commit embargo until both arms close (component-path diff vs pinned base 6728e2f); orchestrator-only evidence (every verdict cell = orchestrator-executed deterministic command; agent prose = candidate generation); canary correct answer = VOID; positive control NEITHER-arm = INSTRUMENT-DEAD; claim ceiling enforced deterministically (n=1 confirms cut-candidate→reversible trim-trial ONLY; disposition vocabulary closed); every out-of-repo write its own HARD checkpoint
Transition: continue
Abort: if blocked >3 attempts, ask user: skip or abort; STOP after 2 failed spike mechanism attempts (T1); STOP and re-present on A1 baseline drift (390/94.44); STOP on restoration failure, isolation-probe failure after 2 addendum fixes; canary contamination = VOID; dual-arm positive-control miss = INSTRUMENT-DEAD; post-unblinding apparatus defect = VOID; checkpoint rejected → STOP and re-present

## Recent Decisions

| [[stage2-episode-execution-design]] (Phase 87) Stage-2 episode contrast execution design — execute the FROZEN `## Stage-2 parameters` verbatim (byte-frozen at 9ad62f0); three maintainer rulings (direction Q&A 2026-06-10): isolation = INDEPENDENT CLONES from the same pinned setup state (faithful implementation of the frozen "twin worktrees" wording); ship-runner referent = edge-screener's standing phase-exit gate (full pytest + coverage + mypy strict + ruff) pinned as one byte-exact command, orchestrator-executed identically in both arms; gate inputs = CANNED, orchestrator-mediated (closed policy, verbatim per-arm logs; zero-gate-firing in arm B pre-declared valid). Execution shape: addendum-first (eval/ceremony-lift/stage2/execution-protocol.md pins all execution-level mechanics, committed BEFORE any arm — first-add-commit ancestry + byte check) → setup commit off 368e056 as a HARD checkpoint (parity provisioning: force-added gitignored/untracked enforcement surfaces, Phase-10 analysis, positive-control dev-wiki seed; setup-SHA deviation explicitly acked) → instrument validation before arm tokens (HEU-012 hook-fire probe, isolation probe, restoration incl. live DIRTY state, detector rehearsal, extractor smoke) → arms fresh-session, canary to arm B post-stop → orchestrator-executed tables, blinded tie-break iff both pass → disposition in closed vocabulary. Consequences: bundle-attribution caveat explicit (full-ceremony-vs-minimal, not spec-vs-no-spec); kit-commit embargo vs 6728e2f until both arms close; the spec-generation disposition routes the [[ceremony-step-verdicts]] ambiguous-stage-2 row. | high | 2026-06-10 |
| [[ceremony-lift-tiered-screen]] (Phase 86) Ceremony lift: tiered screen before episode experiment — measure whether the kit's ceremony steps (dev-plan, spec, reviewer dispatches, debrief) provide lift via a two-stage design, episodes conditional. Stage 1 deterministic in-repo (`eval/ceremony-lift/`): pre-registration committed-then-FROZEN before tabulation (byte-unchanged anti-retrofit; per-step thresholds, cost-materiality threshold, evidence taxonomy, closed verdict enum, corpus = fixed phase window + mechanical enumeration of ALL dispatches incl. zero-catch, admissibility rule, stage-2 verdict-transition authority); ceremony cost baseline FIRST (tokens raw + cache-adjusted, wall-clock, synchronous interruptions; keep-by-immateriality early exit); two-class evidence taxonomy — outcome-grade (marginal catches, admissible ONLY after orchestrator re-execution of the deterministic gate against recoverable pre-fix state; else downgrade to ambiguous, pinned) vs consumption-grade ([uses:N], retrieval hits — can mint cut-candidates, can NEVER mint keep for re-presentation-class outputs, the amplifier-null class); controls-first (hand-labeled cost session, seeded tabulator rows). Stage 1 mints NO verdicts (gate A1 round 2): the maintainer takes keep/trim/cut/ambiguous→stage-2 positions at the HARD checkpoint (caveat column + MDE arithmetic — no-catches-observed alone invalid for rare-event steps). Stage 2 conditional: bundle full-ceremony vs assumption-gate+hooks on burnable edge-screener Phase 10, twin worktrees, deterministic ship criteria, positive control; n=1 confirms cut-candidate→reversible trim-trial only. VERDICTS-ONLY phase, pinned base 5360486; cuts route to a follow-on. Rejected: full episode experiment now (cost without screen; LOO attribution noisy); judgment cut without measurement (Phase-85 reviewer catch is live counter-evidence). | high | 2026-06-10 |
| [[install-gap-dir-currency]] (Phase 85) Fix the structural invariant, not the single instance: every install path that ships a hook script also ships the subdirectories it consumes (modules.json `project_local.extra_dirs` migrated to a declared top-level `hook_dirs` map — a project_local-named key driving global-path behavior is a single-source smell; 4 consumers migrated; empty-glob tolerant under set -e), and check-install-drift.sh treats "consumer present, companion directory missing/stale" as drift via consumer-conditioned directory cells (orphan installed-only files FLAGGED as drift rows, never removed — detect-and-warn charter). T1 inventory FIRST: line-cited enumeration of every hook-shipping path; the incident-5 causal hypothesis (Phase-82 drift-guided resync used checker output as its shopping list) verified, not assumed; session-start.sh's registration-dead status recorded; sourced-.d/ mode-drift exemption pinned. Then edge-screener: DRQ-1 empirical verification (STOP if not double-fire) → HARD checkpoint 2 → dedupe-by-basename migration into settings.local.json (register-settings.py is upsert-only — it cannot dedupe the Phase-79 hand-patch; .bak disposition explicit) + union-uniqueness + single-firing; dogfood ≥2 real-work sessions with the memory liveness probe FIRST (Phase-83 couldnt-fire trap) — A5 evidence filed, not disposed. Gate: A1 deferred (checkpoint 1 decides), A2 accept (mandatory verification), A3 down-scoped (kit fixes independent of the dead copy's fate; ship-vs-dispose at checkpoint 1), A4 accept; all_accept:false. Rejected: delete the unregistered ~/.claude project-scope copies this phase (regresses the Phase-82 presence charter; separate prune-on-value question); re-scope session-start.sh as scope:global (recreates the ghost class Phase 84 just removed — the fix ships FILES, never registrations). | high | 2026-06-10 |
| [[hook-registration-hygiene]] (Phase 84) Instrument-first, capture-anchored hook fixes + checkpoint-gated registration reconciliation. Stage A: eval harness hermetic FIRST — run_hook exports CLAUDE_PROJECT_DIR="$WORK_DIR" + live-state tripwire + seeded-leak self-check (reverted copy must turn the leak test RED) + unset-variant + post-instrument 52-scenario baseline BEFORE any hook change. Stage B: REAL PostToolUse capture with provenance BEFORE any fix (pinned Phase-82 events carry NO exit-code field); three pre-declared evidence-forced branches per hook — remap / redesign (post-commit → actual git state with failed-commit rigor; detect-loop → tool_response.stderr/error markers) / upstream (platform-defect filing + disable-at-boundary at the checkpoint, criteria N/A-upstream); canonical jq parse (detect-loop off raw grep), legacy top-level .exit_code fallback retained (52 denominator stable), 3-step marker resolution over the 4-matrix, HEU-002 fail-loud on signal-absence, parser-edge fixtures, artifact-CONTENT assertions. Stage C: coverage matrix over all kit-marker-discovered roots (ghosts load-bearing for 8 hooks in 3 roots) + basename-normalized extraction + sandbox-rehearsed remediate-then-deregister (timestamped backup + tested restore) + HARD maintainer checkpoint BEFORE any live settings edit. Gate: A1/A3/A5 accept, A2 don't-know→evidence-revised→still don't-know→DEFERRED (must-revisit; T4a checkpoint is the decision point; remediate-then-deregister the prepared default), A4 dissolved to verified fact (all four Phase-82 repros confirmed at HEAD at gate time); all_accept:false, ledger appended + validated. Rejected: fix-hooks-before-harness (instrument would mutate the subject); keep detect-loop's pure-grep parser (truncates at embedded quotes; jq is the kit norm); hand-written assumed-shape fixtures (fixture circularity — both hooks' existing tests pass TODAY while dormant); deregister-all-11 without a coverage matrix (4th cascade-failure instance); surgery on direction-gate authority alone (the A2 deferred don't-know). | high | 2026-06-09 |
| [[prune-on-value-subtraction]] (Phase 83) Evidence-first, verdict-gated, serialized subtraction over the 6 Phase-82-evidenced dead-weight candidates (enforce-memory.sh; memory reinforcement machinery; memory-mcp-scaffold shipping; audit-log model field; 3 orphan companions; harness-audit.sh). Per-candidate verdict table (closed enum keep/cut/harden/disable-at-boundary) with arming-procedure + matrix-row-citation columns BEFORE touching anything; mandatory couldnt-fire vs didnt-fire zero-classification via mktemp -d sandbox arming (won't-fire-when-armed = DEFECT, never a demand-evidence cut — [[HEU-012]]); installed surface mechanically DISCOVERED via kit-marker scan (A3 reject → A6); removal-set-first liveness grep over ALL discovered roots (LIVE vs HISTORICAL, Phase-72 semantics — historical records superseded never rewritten); unconditional mid-phase checkpoint BEFORE any cut; serialized per-candidate commits (`Phase 83 cut: <name>`) with regenerated-diff ⊆ removal set + sandbox-rehearsed own-ghosts-only installed deregistration + survivor functional smoke + revert-on-failure; memory_server menu keep/disable-at-boundary/cut-with-regenerated-patch (vendoring contract); zero cuts a valid outcome. Direction gate: A1/A2 accept, A3 REJECT → discovery revision, A4 don't-know → defended A7 (live install lacks fastembed → candidate 2 largely pre-classified couldnt-fire → keep/harden), A5 don't-know DOWN-SCOPED (kit-side memory-layer value question deferred must-revisit); ledger appended, all_accept:false. Rejected: hook-hygiene-round-2 / edge-screener consuming-project work (Jake's phase-direction choice); wholesale memory-layer disable (contradicts [[memory-architecture-classification]]); all-11 ghost deregistration (Jake chose own-ghosts-only); repo-only cuts (leaves silently-failing ghost registrations). | high | 2026-06-09 |
| [[qa-verification-sweep]] (Phase 82) Coverage-defined two-stage multi-agent QA sweep, 8 audit areas (wiring/firing/companions/schema/drift/coverage/docs/usage), incl. the two gate-forced revisions from the FIRST live dogfood of the Phase-81 assumption-approval gate: (a) usage/utilization as the 8th area (Jake's A1 don't-know — "is the MCP memory server even used?"; under-use findings feed the parked Phase-79 prune-on-value item), (b) subagents demoted to READ-ONLY candidate-generators with orchestrator-executed deterministic commands as the SOLE verdict evidence (Jake's A2 reject of leak-is-acceptable-with-re-execution; the Ph80 leak helps candidate recall but is fatal for verification). Controls-first staging (HEU-006 two-tier mirrored): every checker vouching for a clean verdict must first catch a seeded synthetic defect — clean-on-seed = instrument-dead for its area. Mid-phase findings report to the user BEFORE any fix (unconditional); serialized single-writer fixes under the pre-registered boundary (kit-managed + S/M + test-covered blast radius; coverage exception: the missing S/M functional test IS the fix — functional-smoke invariant; >10 confirmed defects → STOP and re-scope). Deliverable: eval/qa-sweep/verification-matrix.md (enum clean/defect-found/deferred/instrument-dead) — verified-clean mechanically distinguishable from never-checked; ZERO confirmed defects is a valid success (agents must not manufacture findings). Phase-81 surface quarantined (ledger read-only; gate defects via --selftest, filed). Frozen apparatus (eval/amplifier/*, eval/assumption-screen/) read-only — findings filed, not fixed. Rejected: extending make test directly as the phase (the sweep produces the gap list test-writing consumes); depth-first Phase-81-only audit (breadth is the point when the suites are green). | high | 2026-06-09 |
| [[assumption-approval-gate]] (Phase 81) Ship the SIMPLEST assumption-approval gate — the earned consequence of Phase 80's INSTRUMENT-DEAD ([[assumption-surfacer-completeness-screen]]; the scope-anchored surfacer's only edge was a working-knowledge leak, the clean signal DEGENERATE — a naive cost-sorted surfacer recovers load-bearing assumptions by reasoning). Four components: (1) the frozen Phase-80 NAIVE surfacer in dev-plan (3-6 cost-sorted; Step-10 T0's weakest assumption a required member); (2) accept/reject/don't-know positions via AskUserQuestion that **REPLACE** the approach-approval click (positions ARE the gate; reject→revise+re-surface, don't-know→defend-or-down-scope else deferred→Blockers+must-revisit, no unresolved reject/don't-know = direction confirmed); (3) all-accept→warn + track `all_accept:true` + restate how each accepted assumption shapes the approach (Jake's Q2 choice, NOT a hard block — the bite is the always-fired restatement + cross-phase tracking, [[HEU-012]]-compliant); (4) an append-only cross-phase ledger `.dev-wiki/assumption-ledger.md` + a dev-debrief revisit-status forcing-function + a deterministic NO-LLM `scripts/check-assumption-ledger.sh` enforced at debrief finalization. NO new hook (the debrief-finalization check is the firing point; session-start advisory deferred). The Phase-80 plan was itself interrogated through this mechanism — Jake's live A1 accept / A2 reject (made Ph80 a screen) / A3 accept / A4 accept, plus three direction-gate decisions this plan (Q1 positions REPLACE the click; Q2 all-accept→warn+track+restate; Q3 build the ledger now). Efficacy is UNMEASURABLE in-kit (the amplifier-null + Ph66/69/80 representativeness) — tests assert MECHANICS only, no quality delta claimed; the ledger row is the firing evidence ([[HEU-012]]: verify firing not presence); ONE documented ledger-schema source referenced by both skills + the check (no split-brain); append-only + monotonic-row corruption guard; the debrief revisit re-scans prior-phase unrevisited rows (later-phase bite caught — the cascade pattern). Rejected: the elaborate scope-anchored machinery (leak artifact); a HARD all-accept block (Jake chose warn+track+restate); a session-start advisory hook (deferred — subtraction test); auto-mutating a linked decision's confidence (debrief SURFACES the suggestion, the maintainer decides). | high | 2026-06-09 |
| [[assumption-surfacer-completeness-screen]] (Phase 80) Reframe the dev-plan human gate from approving the APPROACH (a conclusion the maintainer can't evaluate → blind-yes) to taking accept/reject/don't-know positions on the plan's load-bearing ASSUMPTIONS. The plan was interrogated LIVE via that mechanism (gate eating its own dogfood): Jake A1 accept / **A2 REJECT** ("can't trust the agent-CHOSEN assumption set — solve set-completeness before building anything") / A3 accept / A4 accept. A2 ⇒ Phase 80 is a SCREEN, not a build: validate a scope-anchored surfacer before the gate/ledger/all-accept block (Phase 81). An agent-internal adversarial review (6/10) found the CRITICAL hole — author-selected ground truth is CIRCULAR (a corpus holds only assumptions someone already noticed; blind exactly where A2 lives). Reframe: does the scope+framing surfacer BEAT a blind (outcome-unaware) baseline at recovering assumptions the project's history later PROVED load-bearing (ground truth = outcome-determined + mechanically extracted, NOT author-selected)? Structurally the amplifier's harness-vs-baseline question; `surfacer ≈ baseline` is a legitimate 5th-amplifier-null TERMINATE (pre-registered). Lead with a cheap circularity-escape SPIKE (T1) as a hard gate (INSTRUMENT-DEAD stops the phase for one task). Two-track verdict (scope completeness+recovery / framing recovery-only weaker), cost-sort-adversarial control (burial relocates to the cost-sort otherwise), variance-derived bar (not the inherited 4/5), entity-presence scoring (a require-ERE measures vocabulary not recovery). Cuts the handover's smoothness signature-detector (re-imports Ph66/69 representativeness + gameable) and the rewarded exogenous interrogator (deferred; surfacing still runs clean-context). Repo-only `eval/assumption-screen/` (amplifier program closed — new line). Labeling fork (mechanical vs Jake-adjudicates-delta) resolved by deferral (spike tests mechanical; adjudicate is fallback). Jake: "yes lets go" on the revised spike-first design. | low→pending | 2026-06-09 |
| [[harness-hygiene]] (Phase 79) Bundle two kit-hardening fixes into one "harness hygiene" phase (Jake chose bundled via AskUserQuestion). (A) Hook path resolution: the kit registers hooks with bare relative `.claude/hooks/X.sh`; Claude Code does NOT guarantee CWD=project-root, so consuming projects (edge-screener Stop-hook dogfood) hit `No such file or directory` though the scripts exist. Fix at source — register-settings.py + template emit `${CLAUDE_PROJECT_DIR}/.claude/hooks/X.sh` (project) / `$HOME/.claude/hooks/X.sh` (global) AND hook scripts `cd "${CLAUDE_PROJECT_DIR:-.}"` (command-fix alone leaves them found-but-inert); the unconfirmable live check becomes a committed hermetic wrong-CWD test. (B) working-knowledge size cap: the curator caps entry-count(100)/lines(210) not SIZE, so 4 amplifier mega-entries (~11k chars/24% of a 45k file) trip the context warning — compress them → 1 dense entry + dev-wiki pointers + a NON-DESTRUCTIVE per-entry char-cap advisory in wk-prune.sh (warn, never truncate/evict). Restores [[decision:memory-architecture-classification]] (terse always-loaded layer, detail in dev-wiki). 4th dogfood→harden fix (Ph74/75/76). Rejected: prune hooks/entries on value grounds (subtraction framing — deferred, hardens not shrinks); auto-truncate over-cap entries (destructive); defer A2 (Jake approved full). | high | 2026-06-04 |
| [[skill-crystallization-headroom-screen]] (Phase 78) Before building any capability→skill route (crystallize a phase's tooling into a reusable project-level skill), run a cheap, deterministic, verifier-independent SCREEN: does a candidate tooling artifact embed correctness a bare model CANNOT re-derive even given the RECOVERABLE CORPUS (interface + call sites + task) a non-crystallized consuming project would have, but not the implementation or its tests? Successor to the decision-retention line (Ph70/71/77 all null) — tests the capability/correctness boundary, the avenue the campaign repeatedly pointed at. OFF = clean subagent given only the recoverable corpus, NOT a lossy brief (review C2); scoring runs the artifact's HIDDEN spec-implied tests (NO-LLM `check.sh`, n=5/threshold=4 cloned from anchor-screen). Guards: `.offleak` leak-check (C4/M5); spec-implied = entailed-by-corpus with a QUOTED sentence per test (C3, mechanizes the partition, kills the Ph77 cherry-pick hazard); candidate correctness LOCATED+QUOTED from the test file not asserted in prose (C1 — `check-install-drift.sh` has NO /var canonical-path fix, that was Ph76 session-start.sh); 4 controls negative/positive-unknowable/recoverable-fully-specified/.offleak gate it (M3), any violation ⇒ INSTRUMENT-DEAD; UNSTABLE never votes toward TERMINATE (M2); pre-registration ancestor-guarded. Honest prior SPLIT: general tooling DEGENERATE, domain tooling (edge-screener PIT-survivorship, silently-wrong-on-re-derivation) HAS-HEADROOM — the split is the finding. Verdict gates the build (TERMINATE→don't; HAS-HEADROOM→build, scoped, + router reframe: skill vs regression-test/lint vessel). Repo-only `eval/amplifier/skill-screen/`, frozen. Design hardened by an agent-internal adversarial review BEFORE pre-registration. Jake chose "plan Phase 78 as that screen" after rejecting the report's two-domain specialization (thought-exercise only) and the low-prior process-retention sliver. | high | 2026-06-04 |
| [[cross-session-retention-headroom-screen]] (Phase 77) Run the amplifier program's last untested regime (cross-session decision/process retention) on the real READ-ONLY edge-screener substrate via an AUDIT-GATED ablation. The cheap T1 residual-audit gate (dev-wiki decisions over edge-screener real history minus everything recoverable from source tree + tests + `git log -p` commit bodies; each keyed on a pre-registered discriminating token ABSENT from OFF inputs AND still resolving at HEAD AND pinned to its terminal value) short-circuits to TERMINATE/INCONCLUSIVE before any expensive OFF/ON run. T2/T3 controlled ablation (bare-OFF / padded-OFF [Phase-59 length control] / ON / positive-control; n=5; frozen `retention-screen/` consensus-by-clause checker, NO LLM in scoring; pinned n≥3 floor committed BEFORE the residual count is consulted; ancestor-guarded pre-registration) runs ONLY on a residual ≥ 3. Honest prior: degenerate (git-log+code+tests already carry the decisions), one regime past Ph70/71. Positive control gates the null (INSTRUMENT-DEAD blocks a false TERMINATE); a null residual ≠ substrate worthless (its value may have precipitated into git via debrief discipline — caveat recorded). Repo-only `eval/amplifier/xsession-screen/`, frozen on completion; edge-screener byte-identical before/after. Rejected: (a) synthetic Ph71-clone (re-imports the self-measurement confound Phase 73 escaped); (b) naturalistic-only (no counterfactual — folded in as the residual pre-screen); (c) full ablation regardless of the pre-screen (risks confirming a foregone degenerate). Jake chose "audit-gated ablation" at the direction gate. | high | 2026-06-04 |
| [[installed-copy-drift-guard]] (Phase 76) Detect when the maintainer's installed `~/.claude` has drifted from the kit source (`templates/.claude`) — the kit develops in templates/ but RUNS from ~/.claude (bit twice: Phase-73 curator gap, Phase-75 stale debrief). DETECT-AND-WARN over symlink (`--link` rejected — live-WIP-breakage risk; copy-install safety preferred). Scope (A) maintainer-side; (B) consuming-project drift deferred. Deterministic `scripts/check-install-drift.sh` (kit-managed copy-verbatim files from modules.json minus a pinned bounded-count exclusion allow-list) → session-start advisory gated to the kit repo + `install.sh --status`. Fail-open, noise-scoped. Jake-approved ("A only"). | medium | 2026-05-31 |
| [[delivery-commit-verification]] (Phase 75) Detector-first fix for the accepted-but-uncommitted divergence the edge-screener dogfood exposed: a fail-open `session-start.sh` divergence detector (PRIMARY — fires independent of agent adherence; the skill-text delivery-flow D3 commit step was itself skipped, so more skill-text repeats the failure) + D3 commit self-assert + gate-after-commit ordering (SECONDARY). Deterministic-validator-at-the-boundary per the posture; reframed at the direction gate from "guarantee the commit fires" to "make gate-state diverging from git-state impossible to ignore". YAGNI-defers Stop-time catch + state-store drift + eval scenario. Jake-approved ("yes"). Shipped + pushed `add2ee7`, reviewer 9/10. | high | 2026-05-31 |
| [[harden-consuming-project-scaffold]] (Phase 74) Fix the 4 consuming-project scaffold defects surfaced by the edge-screener dogfood AT THE SOURCE: py-review Stop hook prompt→gated command (the loop fix), py-init recursive hook copy (curator gap), pyproject ruff/mypy out-of-box guards, AGENTS.md domain-neutralize. Deferred #5 (dev-init/dev-plan CWD-coupling) — skill-architecture, YAGNI. modules.json single-source + regenerate settings; kit make test is the gate. Jake-approved ("yes, do Phase 74 now"). | high | 2026-05-30 |
| [[cross-session-substrate-stock-screener]] (Phase 73) Cross-session-persistence substrate = a new EXTERNAL stock-screener project (`/Users/jwang/edge-screener`), scaffolded with the real harness so there is a genuine harness to measure, built across many real sessions — removes the self-measurement confound of P70/71. Jake-chosen at the `/dev-plan` direction fork (AskUserQuestion 2026-05-30: Build a real substrate → cross-session persistence → stock screener, defaults approved). Honest goal = un-foolable backtest apparatus not guaranteed alpha; cross-session measurement DEFERRED (can't measure recovery on session 1); data = free public (yfinance/Stooq) with loud survivorship/point-in-time guards. nana-dev-kit Phase 73 is thin: bootstrap substrate + record deferred-measurement design, then return to NONE. Screener Phase 1 = "Validated backtest ruler, before any edge search" (mirrors Phase 68). | high | 2026-05-30 |
| [[cash-compaction-recovery-subtraction]] (Phase 72) Cash the P70/71 conclusion — subtract the dead `.session-anchor` recovery machinery (post-compact.sh read-branch + .gitignore entry). Confirm-truly-dead first (exhaustive grep: only those 2 live refs; all else historical records). Subtraction over construction (the recovery pathway was measured headroom-free in P70/71 — wiring up a writer would add machinery the campaign proved inert). Historical [[hook-reconciliation]] superseded, NOT rewritten. gap 4.1 DEFERRED YAGNI. make test/eval green at unchanged surface (no registration/settings/README drift). Jake-chosen (Engineering→Tight subtraction, AUQ 2026-05-30). | high | 2026-05-30 |
| [[cross-boundary-retention-headroom-screen]] (Phase 71) Execute the Phase-70 surviving long-horizon/multi-turn process-retention avenue as a cross-boundary retention headroom SCREEN (the multi-turn analog of the anchor screen; Jake-chosen via AskUserQuestion 2026-05-30). SYNTHETIC boundary, model-authored (not hand-truncated) OFF residual + an isolated in-context `[HARNESS STATE]` increment (defeats both the OFF-starvation confound and the eval/comparison A-vs-C isolation scar). Differential two-n=5-batch verdict (DEGENERATE/HAS-HEADROOM/INERT/UNSTABLE; distinct INERT same-clause + distinct INERT-MACHINERY ladder rung); `check.sh` run_check/aggregate/verify_pins ported VERBATIM, diff_verdict new; NO LLM in scoring. A pinned motivated-vs-arbitrary rationale-probe litmus + human authorization gate CONTINUE (guards re-declaring the Phase-70 unknown-facts result in multi-turn clothing). Bare-derivation gate disqualifies model-default candidates first; pre-registration committed SEPARATELY before verdicts (anti-retrofit ancestor guard). Machinery FROZEN (the subject of measurement). Spec nana:approved; adversarially reviewed. | medium | 2026-05-30 |
| [[amplifier-anchor-headroom-screen]] (Phase 70) **RESULT: PROGRAM-VERDICT: TERMINATE.** Screened anchor EXISTENCE (inverting the Phase-69 order — anchor selection is upstream of measurement; predicate-repair-first was structurally blocked on the degenerate OFF ≥ ON anchor). Controls validated the instrument (neg→DEGENERATE, pos→HAS-HEADROOM, middle→STABLE); then all 4 candidates (structuring, UBO multiply-and-sum, sanctions transliteration, engineered-favorable EU AMLR €10k cap) screened **DEGENERATE 5/5** — opus does the AML reasoning unprompted; headroom appeared ONLY on the positive control's *fictional* fact. Per the pre-registered ladder (even engineered-favorable DEGENERATE ⇒ TERMINATE) the single-decision-anchor measurement line is closed. Finding: single-decision harness headroom lives only in *unknown facts* (a retrieval problem), extending the Phase-59 lesson. Surviving avenues: retrieval-on-proprietary + long-horizon (both with decidable-when). NO harness-value claim. User chose Approach A (AskUserQuestion 2026-05-30); all 5 adversarial-review fixes adopted (4/10→fixed). | high | 2026-05-30 |
| [[amplifier-representativeness-audit]] (Phase 69) A read-only recon ran the Phase-68 ruler over all 8 real consuming-project transcripts and FALSIFIED the handover's live-run plan: 8/8 `surfaced=false`, AUQ-scoped hits=0 (decision surfaces in prose not AskUserQuestion — v1 predicate is a structural false-negative), anchor DEGENERATE (look-ahead handled unprompted even in the harness-OFF baseline ⇒ no headroom). User chose Approach A (AskUserQuestion): turn the recon into the deliverable — NO live run, audit-only, do NOT patch the emitter. Ship a re-runnable `measurability-gate.sh` (NOT-MEASURABLE now; the Phase-70 trigger) + honest apparatus disposition (ruler is NOT yet a validated gate ⇒ comparison stays tombstoned, reasoning calibration-only, binary corpus sole gate). | high | 2026-05-29 |
| [[amplifier-measurement-instrument]] (Phase 68) Build Frontier 0 (the measurement instrument / ruler) BEFORE any cognitive frontier — the handover's dependency order was backwards and the escalation-classifier (Frontier 1) is unfalsifiable without a ruler. A deterministic READ-ONLY emitter at `eval/amplifier/emit-proxy-vector.*` ingests a REAL transcript jsonl (substrate, NOT enforcement.log — closes the Phase-66 schema-gap + clears the Phase-65 corpus-duplication bar) and emits a 4-group proxy-vector; validated by a positive/negative CONTROL pair (differential-flip), NO LLM judge (PARK if it can't pass deterministically). Instrument-validation (Phase 68, planted ground truth, n=1) is separated from harness-measurement (Phase 69, real consuming-project, n>1). | high | 2026-05-29 |
| [[hook-firing-coverage-gate]] (Phase 67) Make the functional-smoke invariant machine-checkable — a RED-first meta-test enforcing coverage over the UNION of `.hooks[]` (predicate `(.type//"command")!="prompt"`, NOT literal `command` which matches zero) + `extra_dirs/*.sh` curators; un-gameable via pinned exemption allow-list + denominator floor + negative control; invocation-anchored coverage (bare grep is empirically useless). Fix the GENERATOR, not the 3 known holes. Tests only. | high | 2026-05-29 |
| [[park-enforcement-scorer-signal-insufficient]] (Phase 66) The signal-richness gate FAILED (11 new-format records, 1 hook, 0 blocks, all self-traffic) — park the scorer behind a runnable probe-trigger, NOT a calendar note; 2 of 4 blockers are structural (representativeness: kit's own log ≠ consuming-project work; schema-gap: record = decision, not agent action). Redirect to the audit-log subtraction-test disposition. NO scorer code. | high | 2026-05-29 |
| [[audit-log-disposition]] (Phase 66) KEEP + harden audit-log — it functions + is eval-covered; `.nana/audit.jsonl` is a human-facing forensic trail (which model edited which file when), NOT code-dead deadweight; hardened raw-printf `$FILE_PATH`→`jq --arg` (fixed latent JSONL-injection) + added the functional-smoke the eval lacked. | high | 2026-05-29 |
| [[instrument-not-score-enforcement-firing-substrate]] (Phase 65) Build the enforcement-firing SUBSTRATE, not a scorer — a scored fixture-replay duplicates the corpus `lifecycle` category; the missing ingredient is real trace provenance, so instrument the 6 lifecycle hooks (fail-open) and defer the scorer. | high | 2026-05-29 |
| [[cut-heuristic-scoring-keep-articles]] Cut the never-fired scoring MACHINERY (matcher/judge/counter/lifecycle/dashboard + dev-plan Step 13 sub-items 6-7 + dev-debrief Step 7 heuristic-capture); KEEP the 17 articles + SCHEMA.md (test fixtures + eval/reasoning corpus). | high | 2026-05-29 |

## Blockers and Open Questions
- [held: Phase-86 WK seeding] 3 cross-phase facts held until Phase-86 verdicts land (cache-adjusted token attribution ~10x overstatement; "no catches observed" invalid for rare-event subtraction / underpowered verdict-menu pattern; correlational on/off comparisons invalid on selection-biased corpora) — working-knowledge is itself under measurement this phase and over its size advisory. Re-offer at Phase-86 debrief; sources in [[ceremony-lift-tiered-screen]] + spec. (held 2026-06-10)
- [deferred: Phase-85 dogfood] check-tests-were-run.sh false-positive class CONFIRMED in real consuming-project use: Read calls on .py files trigger the "tests not run" Stop nudge on read-only analysis work — the nudge has demonstrated bite (the session re-ran the full 390-test suite to satisfy it, 3 Stop cycles; eval/install-gap/dogfood-evidence.md session 2). Harden candidate: trigger should key on Edit/Write of code files, not Reads. Re-trigger: next prune-on-value / hook-hardening round. (raised 2026-06-10)
- [evidence-filed: Phase-83 A5 must-revisit] Consuming-project memory-layer DEMAND evidence now exists: liveness-probed live layer (import exit 0), yet across 2 real edge-screener work sessions + 1 probe session ZERO voluntary memory-tool use; DB never created (row count 0). Evidence: eval/install-gap/dogfood-evidence.md. The A5 disposition (cut/keep) belongs to the next prune-on-value round. (filed 2026-06-10)
- [deferred: Phase-84 A2 don't-know, must-revisit] Ghost deregistration safety (no-invisible-dependency-class) undefendable pre-matrix; gate evidence: ghosts load-bearing for 8 hooks in 3 roots. No live settings execution on direction-gate authority; T4a hard checkpoint decides (remediate-then-deregister default); ledger row A2 revisit-status: open. (raised 2026-06-09)
- [deferred: Phase-83 keep-with-revisit] enforce-memory kept on couldnt-fire evidence (lifetime zero = dormancy; confirmed firing live in Phase-83's own session, 69 records). The DEMAND question (does Jake want the memory nudge?) is now answerable with real data: revisit at the next prune round with the post-restoration firing distribution (allow/block ratio, block follow-through). (raised 2026-06-09)
- [deferred: Phase-83 stale-WK-entry] The working-knowledge entry "enforce-memory is OPT-IN (separate .claude/enforce-memory, NOT shipped...)" is contradicted by modules.json L39, which DECLARES ~/.claude/enforce-memory as a shipped marker (install.sh created it 2026-05-28). Supersede the entry (never rewrite history) at next curation. (raised 2026-06-09)
- [deferred: Phase-83 user-owned-ref] ~/.claude/rules/dev-wiki-hooks.md (user-owned, outside the kit's atomic boundary) references "/dev-harness H6" — no such skill exists; harness-audit.sh is now wired as `make audit`. Jake to update the rules file manually. (raised 2026-06-09)
- [deferred: Phase-83 A5 don't-know, must-revisit] Does the kit-side memory MCP layer itself earn its keep? Down-scoped out of Phase 83 (candidate 3 is scaffold-shipping only). Evidence pointing down: access counts ~0, reinforcement never fired, consuming project never calls it; evidence pointing up: harness design routes session-start search + bridge stores through it. Ledger row A5 revisit-status: open. Re-trigger: Phase-83 close-out filing review, or a prune-on-value round 2. (raised 2026-06-09)
- [deferred: Phase-82 subtraction-review — usage area] Dead-weight list feeding the parked Phase-79 prune-on-value item, with utilization evidence in `eval/qa-sweep/` (matrix `usage` row + repro-runs.log): enforce-memory.sh (ZERO lifetime firings anywhere), memory reinforcement machinery (0 reinforcements across 55 entries), memory MCP unused in the consuming project (edge-screener never calls memory tools), audit-log model field always "unknown" (CLAUDE_MODEL never set), 3 orphan companions (dev-wiki/stale-queue-spec.md, knowledge-wiki/registry-schema.md, knowledge-wiki/session-context.md — pinned exemptions in test_companions.sh Direction C), harness-audit.sh (unwired). Re-trigger: a prune-on-value subtraction phase. (raised 2026-06-09)
- [deferred: Phase-82 firing] 4 confirmed-by-repro lower-severity hook candidates left unfixed by the pre-registered boundary: post-commit + detect-loop top-level exit-code dormancy, post-commit/detect-loop HOME-only marker resolution, test-harness CLAUDE_PROJECT_DIR sandbox escape. Evidence: eval/qa-sweep/repro-runs.log. Re-trigger: next hook-touching phase. (raised 2026-06-09) — **RESOLVED Phase 84 (2026-06-10)**: harness escape fixed (T1, seeded-leak control); post-commit redesigned + marker resolution fixed (T3, pinned repros 48/56 pass); detect-loop = upstream platform defect, see the Phase-84 platform filing below. SUPERSESSION NOTE: Phase-82/83 `make eval` greens were measured on the leaky instrument — the post-fix baseline showed ZERO flips (eval/hook-hygiene/eval-diff.md), so the leak was not load-bearing for any prior verdict; recorded, not rewritten.
- [deferred: Phase-82 drift] Ghost GLOBAL registrations: ~/.claude/settings.json still registers 11 project-scoped hooks from the pre-Phase-79 global-install era. Their runtime copies are now refreshed AND drift-compared (checker pass 2b), so they run current code — but deregistering them changes live wiring in every project (a maintainer call, not a QA fix). Also filed: installed-only skill residue inventory; checker-header memory_server omission note; dead settings.json EXCLUDE entry. (raised 2026-06-09) — **RESOLVED Phase 84 (2026-06-10)** for the 11 ghosts: maintainer approved the T4a checkpoint package; 6 roots remediated (copies synced incl. session-start.d, 11/11 project-local registration), 11 ghosts deregistered, survivor smoke green, end-state == modules.json scope:global set (eval/hook-hygiene/coverage-matrix.md + rehearsal.log; backup retained). The 3 minor sub-filings (skill residue, checker header, EXCLUDE entry) remain open.
- [deferred: Phase-84 platform] PostToolUse delivers NO failure signal for Bash: no exit-code field in tool_response, and NO event delivery at all for failing tool calls (two live probes; eval/hook-hygiene/capture-diagnosis.md). detect-loop.sh's consecutive-failure counter is structurally unimplementable hook-side — hook left shipped + registered but cannot fire on the real shape (legacy path non-regression-tested); prune-on-value candidate next round. Re-trigger: a platform release adding failure events or an exit-code field. (raised 2026-06-10)
- [deferred: Phase-84 install-gap] No install path ships hooks/session-start.d/ to ~/.claude (modules.json declares it only under project_local.extra_dirs; install.sh + check-install-drift.sh have zero references) — this made the Phase-82 refresh of session-start.sh BREAK every SessionStart machine-wide (registered+present+current-but-broken, 5th instance; repaired live 2026-06-10, matrix addendum). Fix install.sh extra_dirs handling + drift-checker directory currency. Re-trigger: next install/drift-touching phase. (raised 2026-06-10) — ADDRESSED by Phase 85 (in progress 2026-06-10)
- [deferred: Phase-82 misc] (1) generate-delivery-report.py functional smoke needs a stubbed `make` (M design); (2) wk-prune.sh cites an absent spec-precedence rule (low); (3) INSTRUMENT HAZARD: .dev-wiki/enforcement.log has no run-provenance separation — audit/test pipes into template hooks with CWD=repo write real records (observed 18:24-18:27Z scope-check contamination during this phase's own audit); any future log-based evidence must account for this. (raised 2026-06-09)
- [planning: Phase-82 knowledge gap] The agentic-engineering-wiki index references curated articles (verifier-quality, multi-agent-orchestration, agent-evaluation-and-testing) that do NOT exist as files on disk — only raw/ sources + knowledge.db. A wiki-health candidate, OUT of Phase-82 scope (the sweep audits the kit, not the knowledge wiki). Re-trigger: next wiki-health/wiki-reorg run, or a wiki-query that 404s on one of the three. (raised 2026-06-09)
- [planning: Phase-81 knowledge gap] The **accretion/budget-class residual** (where the Phase-80 naive surfacer cleanly MISSED — the line-cap class) is genuinely UNMEASURED. A clean test needs a CONSUMING-project context (the in-kit always-loaded `working-knowledge.md` leaks the answers — Ph66/69/80 representativeness). Deferred per the `eval/assumption-screen/screen-record.md` (a separate user call). Re-trigger: an accretion/budget assumption bites in real use AND a clean consuming-project substrate is available to measure it. (raised 2026-06-09)
- [planning: Phase-81 open risk] Whether the **debrief-finalization deterministic check is sufficient mechanical bite** for the (judgment-laden) revisit-status, vs a needed session-start advisory backstop. The advisory is deferred (subtraction test; the debrief-finalization check is the chosen firing point). Re-trigger: debrief is skipped in real use, leaving `unrevisited` rows undetected ([[HEU-012]] firing point) → add the deferred session-start advisory. (raised 2026-06-09)
- [deferred: Phase-79] Two items scoped OUT of harness hygiene ([[harness-hygiene]]): (1) **prune-on-value** — the subtraction framing (which of the 17 hooks / 96 working-knowledge entries actually earn their keep?) surfaced in T0 but was set aside; this phase HARDENED the existing layer, it did not shrink-by-deletion. Re-trigger: the harness stays heavy / a hook is found never-firing. (2) **edge-screener re-sync** — its `settings.json` was hand-patched to `${CLAUDE_PROJECT_DIR}` this session (interim); after this kit fix lands, re-run `install.sh` there so the durable template-sourced fix replaces the hand-patch (its `.claude/settings.json.bak` is the rollback). Also: a consuming session that resolves the project dir ITSELF wrongly is a Claude Code config issue the hermetic test cannot cover (documented known limit). (raised 2026-06-04)
- [deferred: Phase-76] Two items scoped OUT of the installed-copy-drift guard ([[installed-copy-drift-guard]]), recorded for re-trigger: (1) **(B) consuming-project drift** — `templates/.claude` vs a *consuming project's* `.claude/` (not the maintainer's `~/.claude`). A different problem: projects deliberately pin a kit version at scaffold time, so divergence is expected, not drift; detecting "meaningful" drift there needs a version/provenance model the kit doesn't have. Harder + currently unevidenced. YAGNI until it bites. Re-trigger: a consuming project is silently broken by a kit upgrade it never took. (2) **`install.sh --link` symlink dev-mode** — considered and REJECTED, not merely deferred: symlinking `~/.claude` → `templates/.claude` live means a broken WIP edit to e.g. `session-start.sh` breaks *every* session in *every* project at once; the copy-install model gives a stable last-good checkpoint and the drift is the price of that safety. The Phase-76 detector preserves the safety AND catches the forget — strictly better than `--link`. Re-trigger: the copy/resync cycle becomes a genuine friction bottleneck for kit development. (raised 2026-05-31)
- [deferred: Phase-75] Three items scoped OUT of the delivery-commit-verification fix ([[delivery-commit-verification]]), recorded for re-trigger: (1) **same-session Stop-time catch** in `session-stop.sh` — session-start (compaction-surviving) is the load-bearing detector point; a Stop nudge the just-failed agent generates for itself is marginal → add only if divergence recurs same-session; (2) **broader state-store drift** — in edge-screener `active-phase.md` never advanced to Phase 3 (the rules / git / dev-wiki stores drifted apart); separate symptom, YAGNI; (3) **lifecycle eval scenario** for the divergence detector — the `test_harden` firing test is the gate, eval is nice-to-have. Re-trigger: any of the three recurs in real consuming-project use. (raised 2026-05-31)
- [deferred: Phase-74 #5] dev-init/dev-plan resolve the project via CWD git-root, so they cannot be invoked to target a SIBLING project from another repo's session (forced the manual dev-init drive when bootstrapping edge-screener in Phase 73). DEFERRED — a skill-architecture change (would need a target-path arg), tangential to scaffold CONTENT, and the manual drive worked. Re-trigger: it recurs, or a workflow needs cross-project skill invocation. (raised 2026-05-30)
- [ADDRESSED by Phase-77] The exact OFF/ON cross-session recovery protocol (does a FRESH session WITH the persisted harness files recover decisions a bare fresh session loses?) was DEFERRED at Phase 73 until the `/Users/jwang/edge-screener` substrate accrued real multi-session decision history. Decidable-when NOW GREEN: edge-screener has 9 completed phases, 14 decision articles, 11 journals across 3 dates, 28 commits. **Phase 77 ([[cross-session-retention-headroom-screen]], spec nana:approved 2026-06-04) executes it** as an AUDIT-GATED ABLATION — resolving both sub-questions Jake left open at Phase 73: the controlled-vs-naturalistic fork is decided as BOTH (the cheap naturalistic residual-audit is the GATE; the controlled OFF/ON ablation runs only on a residual ≥ 3). Pre-registration follows the apparatus (campaign discipline). Resolved-when: Phase 77 writes a closed-vocabulary `^PROGRAM-VERDICT`. (raised 2026-05-30, addressed 2026-06-04)
- RESOLVED (Phase-71 planning, 2026-05-30): the 3 Phase-71 design questions (SUBSTRATE — synthesize vs forced-`/compact`; DETERMINISM — `check.sh` port + single-output checkability; CONFOUND — isolate the harness-state increment from the native summary) are resolved by `specs/phase-71-cross-boundary-retention-screen.md` (nana:approved): SYNTHETIC boundary with a MODEL-AUTHORED OFF residual + an isolated in-context `[HARNESS STATE]` increment; `check.sh` `run_check`/`aggregate`/`verify_pins` ported verbatim with `diff_verdict` new; `assert-off-on-isolation.sh` enforces ON = OFF + appended block byte-for-byte. (resolved 2026-05-30)
- [open: Phase-71 knowledge gap] Whether a model-authored compaction summary naturally DROPS a counter-default decision (a lossy boundary the screen can run on) or PRESERVES it (summary-robust) — determines whether the screen returns a candidate-level verdict or `TERMINATE-by-summary-robustness`. Resolved empirically at T2 (summaries generated + leak-checked) / T4 (lossy-boundary count). (raised 2026-05-30)
- [open: Phase-71 knowledge gap] Whether the pinned rationale-probe litmus cleanly separates MOTIVATED from ARBITRARY in practice, or real project decisions blur it (partially derivable) — and how a partial (2–3/5) probe result is recorded so it cannot quietly upgrade an arbitrary candidate toward CONTINUE. Resolved at T2 (probe runs + classification). (raised 2026-05-30)
- [Phase-70 ADDRESSES the anchor-existence item] Of the Phase-69 deferred set, Phase 70 now executes the **anchor-headroom screen** — "a valid non-commodity anchor passing the OFF-baseline headroom screen" — as the upstream go/no-go. The REMAINING items stay DEFERRED + GATED (predicate repair without raw-text collapse, the live off/on run n>1, Frontier 1): predicate-repair + live-run are unblocked only by a Phase-70 `PROGRAM-VERDICT: CONTINUE` (≥1 natural anchor HAS-HEADROOM) AND then `measurability-gate.sh` flipping MEASURABLE. A `PARKED`/`TERMINATE` verdict re-gates them on new priors / a multi-turn substrate (the deferred long-horizon class). (raised 2026-05-29, reframed 2026-05-30)
- RESOLVED (Phase 68): the 3 Phase-68 planning questions — exact transcript schema (T1 GO, no logging-hook pivot), redirect proxy computability (shipped as a structural `unsolicited_human_turns` proxy), deterministic keyword detector sufficiency (passed its planted control). The Phase-69 recon then surfaced that the detector, while correct on planted fixtures, is non-representative on REAL data — now the Phase-69 work. (resolved 2026-05-29)
- [Phase-66 scorer PARKED] The signal-richness gate was checked 2026-05-29 and returned **NOT-SCOREABLE**: only 11 new-format (`schema_version=1`) records in `enforcement.log` — all from one hook (dev-wiki-scope-check), ZERO action-changing `block` firings, all `phase:"65"` intra-phase self-traffic (the 253 enforce-loop + 23 block records are pre-instrumentation legacy format). Unbuildable for 4 reasons, **2 structural**: (1) volume; (2) skew; (3) **representativeness** — the log lives in the kit's OWN `.dev-wiki`, sampling the maintainer editing the kit, never the consuming-project agentic work enforcement governs; (4) **schema-gap** — the record captures the hook's DECISION, not the agent's subsequent ACTION (no firing→response link). `decidable-when`: re-run `scripts/signal-richness-probe.sh` (the committed gate) → SCOREABLE, AND the two structural blockers resolved first (real consuming-project provenance + a record schema that captures agent-response). Not a calendar note — a runnable predicate. See [[park-enforcement-scorer-signal-insufficient]]. (parked 2026-05-29)
- RESOLVED (Phase 64): the 2 Phase-64 design Qs (which working-knowledge entries describe removed-machinery-as-operational; whether Memory Harvest shares counter-write code with Heuristic Capture) — both resolved during Phase 64 execution (semantic prune via curator dogfood; Memory Harvest confirmed separate). (resolved 2026-05-29)
- RESOLVED (Phase 63): cut-vs-fix for the never-fired cognitive system → CUT the scoring machinery (DEADWEIGHT: 18 counters at 0/0, git -S empty ~13 phases), keep the 17 articles as fixtures. Executed in Phase 64. (resolved 2026-05-29)

- ~~Phase 58 residual research delta is +0.5 composite at n=1 (at significance threshold, topic-favorable). Kept Step 2.7 (Checkpoint 2). Phase 59 resolves the keep/trim/cut call.~~ **RESOLVED 2026-05-28 → CUT.** Strengthened with 3 new topics (13 paired runs at n≥3): poor −1.0 real harm, rich 0.0/−0.4 variance-dominated; the +0.5 sits inside the noise band. Feature removed; dev-plan reverts to Phase-55 behavior. (resolved 2026-05-28)

- OPEN: Active research's value on genuinely novel / post-training-cutoff / proprietary topics (weak parametric knowledge — research's theoretical sweet spot) is UNTESTED. Only well-documented domains were measured. A deliberate keep-for-novel-topics-only is a separate user call. (raised 2026-05-28) — Phase 61 reconfirmed: the weak-parametric + properly-absorbed + covered sweet spot remains unmeasured (would need an absorb pipeline + a non-commodity corpus first).

- OPEN: D2 re-test trigger (deferred, concrete numeric trigger not a present feature): if the MCP memory store ever grows past the hot-cache 100-entry cap with valuable DISTINCT entries, re-run the D2 A/B (memory_search as overflow recall for the evicted tail). As it stands the store (20 entries) is a strict subset of the hot cache. (raised 2026-05-29)

- Haiku/judge inter-run variance: mean ranges 2.97-4.85; recurred in Phase 59 (rich-topic spread 0.79–1.19, both variance-dominated at n=5). Cross-model judge / judge re-calibration remains a standing lever (deferred). (raised 2026-05-27, reconfirmed 2026-05-28)
- ~~Memory venv broken (recurring Phases 56-58): `make test` halted at `test_memory.sh`.~~ **RESOLVED 2026-05-28.** Root cause was twofold: (1) the optional `sqlite-vec` dep was absent from the (healthy, uv-built, Py3.13) venv — the `libpython3.11.dylib` symptom was stale from an older venv; (2) `test_memory.sh` forced `_vec_available=True` and hard-crashed instead of skipping when the *optional* dep was missing. Fixed both: installed `sqlite-vec==0.1.9` locally (full 11/11 run), AND guarded the 4 vec-requiring tests behind a one-time probe so they skip cleanly (FTS5-only 7/7) when sqlite-vec is absent — `make test` can no longer halt on a missing optional dep. (resolved 2026-05-28)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| `eval/ceremony-lift/` | NEW (Phase 86) — the ceremony-lift screen's frozen durable record (19 files, verdicts-only, NO kit component touched): `pre-registration.md` (byte-FROZEN post-9ad62f0; corpus window Ph76-85 + end-commit 5360486, admissibility rule, materiality thresholds, MDE arithmetic, closed verdict menu, Stage-2 parameters), `extract-costs.py` + `test-cost-extractor-control.sh` (13 hand-paired checks), `cost-table.md` (12 sessions: ceremony ~66% adj tokens / ~64% wall; EARLY-EXIT no), `classify-evidence.py` + 5 blind controls vs `controls/answer-key.json`, `corpus-manifest.md` (44 dispatches, anchor==8), `evidence-table.md` + re-execution log (post-correction: 1 outcome-grade / 10 ambiguous-downgrade / 8 consumption-capped / 25 zero-catch; transcript-extracted pre-fix re-execution of the Ph85 catch), `check-verdict-block.sh`, `check-verdicts-only.sh`, `run-exit-criteria.sh` (8/8). Verdicts in the phase article + [[ceremony-step-verdicts]] (high). | 2026-06-10 |
| `eval/install-gap/` | NEW (Phase 85) — the install-gap phase's durable record: `inventory.md` (line-cited hook-shipping paths; `causal-path verdict: confirmed`), `rehearsal.log` + `checkpoint-1.md` (A3 keep-current; backup + tested restore), `drq1-verification.md` (string-keyed dedupe; 2 positive-controlled headless probes) + `checkpoint-2.md` (firing-count: 1), `dogfood-evidence.md` (2 real sessions, pinned schema, A5 liveness-probed zero + provenance note), `assert-global-set.sh` + `assert-edge-screener-registration.sh`, `run-exit-criteria.sh` (9/9). [[install-gap-dir-currency]] (high). | 2026-06-10 |
| `tests/test_install_drift_dircurrency.sh` + install.sh `ship_hook_dirs` + checker directory cells | NEW (Phase 85) — 26th make-test script (7 seeded controls: deleted-dir / stale-file / orphan-flag / synced-0 / synthetic second dir); modules.json top-level `hook_dirs` map replaces `project_local.extra_dirs` (4 consumers migrated); install.sh ships consumer-adjacent hook dirs on global + project-local paths (find-based, empty-glob tolerant); check-install-drift.sh consumer-conditioned directory cells + `orphan:` rows + bash-3.2 `${arr[@]+...}` fix; harness-audit.sh 3 stale 4b predicates fixed (review-gate HIGH). | 2026-06-10 |
| `eval/hook-hygiene/` | NEW (Phase 84) — the hygiene phase's durable record: `capture-diagnosis.md` (live-probed PostToolUse shape — NO exit-code field, NO failure-event delivery; branch verdicts post-commit=REDESIGN / detect-loop=UPSTREAM), `eval-diff.md` (post-instrument baseline: zero flips; 2 explained init_git flips), `coverage-matrix.md` (6 roots × 11 hooks registration + copy-currency + the session-start.d live-repair addendum), `check-matrix.sh` (positive-control-gated), `rehearsal.log` (sandbox rehearsal + T4b execution + 11-ghost DEREG assertions + post-T4b Stop-hook trim + fresh-session handoff), `run-exit-criteria.sh` (10 criteria, N/A-upstream rule for 3-5; 10/10). [[hook-registration-hygiene]] (high). | 2026-06-10 |
| `tests/test_eval_hermeticity.sh` + `tests/test_fixture_provenance.sh` + `tests/test_lifecycle_hooks_firing.sh` + `tests/fixtures/real-events/` | NEW (Phase 84) — 23rd-25th make-test scripts (Makefile-wired; +25 tests): hermetic sandbox leak test + seeded-leak self-check; byte-for-byte real-event captures with provenance sidecars (the fixture-circularity antidote); 16/16 firing tests over the redesigned post-commit (4-matrix markers + parser edges + 120s recency + failed-commit). `scripts/eval-runner.sh` now hermetic (CLAUDE_PROJECT_DIR=$WORK_DIR in run_hook) + lifecycle init_git support. The kit repo now self-consumes (gitignored .claude/settings.json + .claude/hooks/). | 2026-06-10 |
| `eval/prune-on-value/` | NEW (Phase 83) — the subtraction phase's durable record: `verdict-table.md` (6 closed-enum rows + arming procedures + checkpoint decisions + close-out matrix), `liveness-grep.log` (7 DISCOVERED roots + removal-set-exclusion liveness + 23 DEREG assertions), `arming-runs.log` (couldnt-fire/didnt-fire evidence incl. the live fastembed verification), `check-artifacts.sh` + `run-exit-criteria.sh` (the spec's 10 criteria, 10/10). Also: `Makefile` gained `make audit` (harness-audit wire-in), audit-log model field cut, 3 orphan companions cut, fastembed live in the memory venv. [[prune-on-value-subtraction]] (high). | 2026-06-09 |
| `eval/qa-sweep/` | NEW (Phase 82) — the sweep's durable record: `verification-matrix.md` (8 area rows + `## Controls` + `## Fixes`; enum clean/defect-found/deferred/instrument-dead — verified-clean mechanically distinguishable from never-checked), `candidates.md` (58 from the 8-agent fan-out), `repro-runs.log` (orchestrator-executed evidence), `run-exit-criteria.sh` (the spec's 10 exit criteria, 10/10). [[qa-verification-sweep]] (high). | 2026-06-09 |
| `tests/test_manifest_freshness.sh` + `tests/test_scripts_smoke.sh` | NEW (Phase 82) — 21st + 22nd make-test scripts (Makefile-registered; MANIFEST staleness + scripts smoke). Plus test_companions.sh Directions C+D and 3 new pipe tests in test_tooluse_hooks.sh (17/17, the reviewer gap closed inline). make test now ~480 assertions. | 2026-06-09 |
| `templates/.claude/hooks/*` (7 field/path-fixed) + `scripts/check-install-drift.sh` (pass 2b) + `scripts/register-settings.py` (cmd_mcp `--modules-json`) + `templates/.claude/skills/MANIFEST` (regenerated, 122 checksums) | Phase 82 fixes — canonical `.tool_input // .input` parse + per-hook path normalization ([[hook-event-shape-normalization]]); drift compares any kit-shipped hook PRESENT in the installed root ([[drift-compare-installed-presence]]); session-start `[nana:enforce]` marker advisory; 11 stale `~/.claude` runtime hook copies refreshed. | 2026-06-09 |
| `scripts/check-assumption-ledger.sh` + `tests/test_assumption_ledger.sh` | NEW (Phase 81) — the deterministic NO-LLM ledger validator (bash/awk, 4 modes `--schema/--revisit/--gate/--append-only` + `--selftest`; awk state-flag + END-flush, EOF-boundary asserted; carries the canonical `## Ledger schema` block = THE single schema source). `--append-only` guards per-block row-count non-decreasing (within-block row deletion caught, not just whole-block removal — the reviewer MEDIUM fix). Test = 22 firing assertions (flags blank-revisit/no-positions/non-monotonic/schema-missing, passes conformant; closing-phase-filled→silent differential; 4 schema-consistency assertions) + scripts/+.dev-wiki/ outside-drift coverage note; 20th make-test script (README 19→20). [[assumption-approval-gate]] (high). | 2026-06-09 |
| `.dev-wiki/assumption-ledger.md` | NEW (Phase 81) — the append-only cross-phase ledger (one `## Phase` block per phase, newest at bottom; never rewrite a prior block except to fill a blank `revisit-status:` at debrief). Seeded with the Phase-80 A1-A4 entry (`all_accept:false`, A2 reject). Lives in `.dev-wiki/` (not a skill-managed path) to survive section-rewriting skills; the row is the firing evidence ([[HEU-012]]). | 2026-06-09 |
| `templates/.claude/skills/dev-plan/{assumption-gate.md,assumption-gate-example.md,SKILL.md}` | NEW companions + Step-13 REWRITE (Phase 81) — the assumption gate. assumption-gate.md: naive surfacer (3-6 cost-sorted; Step-10 T0's weakest a required member; top-N cap; empty-set confirm), accept/reject/don't-know via AskUserQuestion, reject→revise+re-surface, don't-know→defend/down-scope else route-to-Blockers+must-revisit, all-accept→warn+track+restate, ledger-append by-path. assumption-gate-example.md frozen (mixed+reject / all-accept+restate). SKILL.md Step 13 REWRITTEN so positions REPLACE approval (legacy prose removed; LOW stale-wording reviewer fix at line 14) + Step-15f Gates line + Direction Gate; 313→312 (≤350). | 2026-06-09 |
| `templates/.claude/skills/dev-debrief/{debrief-finalization.md,SKILL.md}` | Phase 81 — the "Assumption-Ledger Revisit" forcing-function (non-numbered heading; invisible to test_step_numbering). Fills the closing-phase revisit-status (held/bit/open), re-scans prior open/late-bite rows (the cascade pattern), surfaces a bit→suggests a decision-confidence review (no auto-mutate), enforces via `check-assumption-ledger.sh --revisit`. Wired from Step 21; runs in Lite too. NO new hook. | 2026-06-09 |
| `eval/assumption-screen/` | NEW (Phase 80) — the frozen assumption-surfacer **silent-class** screen (repo-only, sibling to `eval/amplifier/`; NOT wired into install.sh/Makefile/make test/make eval). `spike/` (T1 GATE → GO: non-circular outcome-determined ground truth constructible for ≥3 phases). `pre-registration.md` + `.prereg-commit` (`86d8584` ⊂ HEAD, ancestor-guarded; two-track verdict + variance-derived bar + the `surfacer ≈ baseline ⇒ 5th-null` possibility, frozen BEFORE runs). `surfacer.md` (naive baseline + scope-anchored/framing) + `coverage-check.sh` (`--selftest`). `fixtures/` (3 REAL silent cases mcp-cwd/line-cap/cascade + cost-sort-adversarial + negative) + `checks/`. `check.sh` (cloned from `anchor-screen/`, **NO LLM in scoring**, `--selftest`) + `runs/` (50 = 5×2×n5) + `verdicts/summary.md` (leak audit + per-case classification). `screen-record.md` (**`^PROGRAM-VERDICT: INSTRUMENT-DEAD`** — clean-context control FAILED: workflow subagents inherited working-knowledge; SURFACER leaked documented answers; the amplifier-null caught in the act; clean signal DEGENERATE; Phase-81 = ship the SIMPLEST gate). [[assumption-surfacer-completeness-screen]] (high). | 2026-06-09 |
| `scripts/check-install-drift.sh` | NEW (Phase 76) — deterministic fail-open installed-copy-drift comparator (bash+jq, NO LLM). Comparison set from `modules.json` (installed skills + `scope:global` hooks + managed rules-dir glob) minus a pinned bounded 3-entry exclusion allow-list (`rules/nana-personal.md`, `rules/py-session-state.md`, `settings.json` [merged]); not-installed skill dirs SKIPPED (no partial-install false positives). Modes: report (exit 0 synced / 1 drift), `--count` (always exit 0), `--excludes`; installed-root override via arg or `$NANA_INSTALLED_ROOT` (hermetic tests never touch real `~/.claude`). 7 RED-first tests in `test_install.sh` (120 passed). [[installed-copy-drift-guard]] (high). | 2026-05-31 |
| `templates/.claude/hooks/session-start.sh` | Phase 76 — added a kit-repo-scoped `[nana:drift]` advisory block (2nd inline advisory, sibling to the Phase-75 delivery-divergence detector; now **108 lines**). Gate compares **canonical physical paths** (git-root vs `~/.claude/.nana-dev-kit-path` resolved via `pwd -P`) — a string compare silently never fired under macOS `/var`→`/private/var`. Fail-open, once/session. 4 firing tests in `test_harden.sh` (22 passed); firing-coverage **21/21** (no denominator churn — inline, not a curator). Phase 75 — also the fail-open delivery divergence-detector branch ([[delivery-commit-verification]]). | 2026-05-31 |
| `install.sh` | Phase 76 — `--status` gained an "Install drift" line via `scripts/check-install-drift.sh` (counts differing kit files vs the installed `~/.claude`; "drift: none" when synced, "unknown" if the comparator is missing). | 2026-05-31 |
| `templates/.claude/skills/dev-debrief/delivery-flow.md` (+ executor-prompt.md #11, SKILL.md Step 18) | Phase 75 — SECONDARY fix: D3 verifies the commit landed (exit status + HEAD advanced + clean tree; hook-aborted commit → surface loudly, no push/gate-mark, STOP) + **gate-after-commit ordering** — executor writes the delivery gate UNCHECKED, D3 flips it post-verified-commit. Gate-state follows git-state. | 2026-05-31 |
| `templates/.claude/hooks/py-review-stop.sh` | NEW (Phase 74) — gated command Stop hook replacing the looping `py-review-stop-prompt.md` prompt hook. jq fail-open + `stop_hook_active` loop-guard + `.py`-change planning-guard + exit-2 review checklist (mirrors check-tests-were-run.sh). Firing-tested 3 paths in test_long_cadence_hooks.sh; modules.json/settings.json command-typed; firing-coverage floor 21. | 2026-05-30 |
| `/Users/jwang/edge-screener` (EXTERNAL repo) | NEW (Phase 73) — the cross-session-persistence **substrate**: a real stock-screener consuming project, scaffolded with the full real harness (26 skills, hook tree, settings, rules, pre-commit, CI), own `.dev-wiki/` (ruler-first 4-phase roadmap, Phase 1 active). Committed `5cceac6` (183 files), toolchain green. The screener build proceeds in ITS own wiki; the deferred cross-session measurement rides along once it has real multi-session history. | 2026-05-30 |
| `eval/amplifier/xsession-screen/` | NEW (Phase 77) — the frozen cross-SESSION retention-headroom screen (repo-only, sibling to `anchor-screen/`+`retention-screen/`; NOT wired into install.sh/Makefile/make test/make eval). `residual-audit.sh` = deterministic provenance absence-grep over the OFF corpus (code+tests+git-MESSAGES; `git log -p` EXCLUDED so the stripped substrate is not resurrected from history; NO LLM; `--selftest` both-ways control pair + substrate-strip + superseded + unresolved-at-HEAD + gitmsg channel-gating). `assert-subject-untouched.sh` (read-only guard). `token-list.tsv` (14 operative discriminators) + `pre-registration.md` committed BEFORE the run (`.prereg-commit` = `21a6c52` ⊂ HEAD, ancestor guard). `residual.md` (the GATE result: **0/14**, all RECOVERABLE:tree). `screen-record.md` (`^PROGRAM-VERDICT: TERMINATE` + no-harness-value disclaimer + git-log-as-channel caveat + scope note). **TERMINATE** (residual 0 < floor 3 ⇒ T3 ablation SKIPPED-BY-GATE). Edge-screener byte-identical pre/post. [[cross-session-retention-headroom-screen]] (high). | 2026-06-04 |
| `eval/amplifier/retention-screen/` | NEW (Phase 71) — the frozen cross-boundary retention-headroom screen (repo-only, 79 files, NOT wired into install.sh/Makefile/make test/make eval). `check.sh` clones `../anchor-screen/check.sh` (`run_check`/`aggregate`/`stability`/`verify_pins` byte-VERBATIM); only `diff_verdict` + `--diff` new (differential OFF×ON over two n=5 aggregates: DEGENERATE/HAS-HEADROOM/INERT[same-clause]/UNSTABLE; NO LLM in scoring). `leak-check.sh` + `leak-vocab.txt`, `assert-off-on-isolation.sh` (OFF is an exact byte-prefix of ON). `pre-registration.md` committed `896e096` BEFORE runs (ancestor of verdict commit `d7067e6`). `screen-record.md` (`^PROGRAM-VERDICT: TERMINATE-by-summary-robustness` + no-harness-value disclaimer + dead-`.session-anchor` finding). `transcripts/`, `summaries/`, `prompts/`, `checks/`, `probes/`, `runs/` ×40, `verdicts/` ×8 (3 controls + 3 candidates). **TERMINATE-by-summary-robustness** (native compaction summary RETAINS decisions; bare opus honors them 5/5 in OFF) | 2026-05-30 |
| `eval/amplifier/anchor-screen/` | NEW (Phase 70) — the frozen single-decision anchor-headroom screen (repo-only, NOT wired into install.sh/Makefile/make test/make eval). `check.sh` = deterministic named-clause consensus-by-clause checker (≥4/5 same clause-id; `--selftest` 13/13 both directions; `--aggregate`/`--stability`/`--verify-pins`; NO LLM in the scoring path). `leak-check.sh` + `leak-vocab.txt` (no harness-rule vocab smuggled into OFF prompts). `pre-registration.md` (3 controls + 4 candidates, each base-model + shasum-pinned no-tools OFF prompt + named-clause check; committed `be96783` BEFORE runs). `screen-record.md` (^PROGRAM-VERDICT: TERMINATE + no-harness-value disclaimer). `verdicts/` ×7, `prompts/` ×7, `checks/` ×7, `fixtures/`, `runs/` ×30. **PROGRAM-VERDICT: TERMINATE** (all 4 candidates DEGENERATE 5/5) | 2026-05-30 |
| `eval/amplifier/survey-real-transcripts.sh` + `real-transcript-survey.md` | NEW (Phase 69) — read-only survey of the ruler over the pinned 8 real transcripts; `--selfcheck` RUNS the ruler on planted `surfaced.jsonl` (positive control), shasum-proves read-only, reproduces 8/8 `surfaced=false`. Frozen record: separate escalation/in-boundary/raw/parse_errors columns + sourced OFF/ON provenance + paraphrase spot-check. Repo-only probe (NOT a make-test gate) | 2026-05-30 |
| `eval/amplifier/measurability-gate.sh` + `VALID-MEASUREMENT.md` | NEW (Phase 69) — **the Phase-70 trigger**: deterministic MEASURABLE/NOT-MEASURABLE/NO-DATA predicate (≥2-ON ∧ ≥2-OFF in-boundary events ∧ OFF-vs-ON differential; planted-fixture shasums excluded; threshold read from the doc). Prints **NOT-MEASURABLE** now; `--selftest` falsifiable both ways. Doc: operational degeneracy/headroom criterion + dual-failure pre-mortem + Phase-70 experiment + no-harness-value disclaimer. Reviewer 10/10 | 2026-05-30 |
| `eval/amplifier/emit-proxy-vector.sh` | NEW (Phase 68) — **the ruler** (Frontier 0): deterministic READ-ONLY emitter (bash+jq). Ingests a REAL transcript jsonl + final git/test state → 4-group proxy-vector (mechanical/interaction/enforcement/ground_truth). Absent enforcement.log ⇒ `null` sentinel (not 0); corrupt line ⇒ `parse_errors`; NO `verdict` field, NO LLM. Repo-only (NOT in install.sh) | 2026-05-29 |
| `tests/test_amplifier_emitter.sh` + `eval/amplifier/{SCHEMA-NOTES.md,schema-probe.sh,fixtures/}` | NEW (Phase 68) — 14 assertions (control pair FLIPS detector + ≥1 interaction proxy, phrase-outside-escalation=false, sentinel, parse_errors, read-only shasum-proven). schema-probe = T1 feasibility gate (GO on real transcript); SCHEMA-NOTES = frozen schema + pinned predicates; 5 control fixtures. 19th make-test script | 2026-05-29 |
| `tests/test_hook_firing_coverage.sh` | NEW (Phase 67) — the **coverage gate**: RED-first meta-test making the functional-smoke invariant machine-checkable. Denominator = union of `jq '.hooks[]\|select((.type//"command")!="prompt")\|.script'` + every `extra_dirs/*.sh` curator; invocation-anchored coverage via `# fires:` declarations (NOT bare grep); un-gameable (pinned exemption allow-list by membership+size + denominator floor + permanent negative control). RED run surfaced the true 10-hook gap | 2026-05-29 |
| `tests/test_long_cadence_hooks.sh` + `tests/test_tooluse_hooks.sh` | NEW (Phase 67) — side-effect-asserting firing tests in mktemp-d + HOME-override isolation. long-cadence: pre/post-compact, session-stop, check-tests allow/skip (8 tests); tooluse: scan-secrets, block-dangerous-bash, auto-ruff-format, stale-queue, post-commit, context-size-check (12 tests). 16th + 17th make-test scripts | 2026-05-29 |
| `scripts/signal-richness-probe.sh` + `tests/test_signal_richness_probe.sh` | NEW (Phase 66) — read-only deterministic gate: "is the enforcement-firing scorer buildable yet?". Classifies records (new/legacy/debrief/malformed), predicate ≥2 hooks ∧ ≥1 block over schema_version-bearing records after tuple-dedup → SCOREABLE/NOT-SCOREABLE/NO-DATA/CORRUPT. The committed park-trigger; 14 tests. Live log → NOT-SCOREABLE | 2026-05-29 |
| `templates/.claude/hooks/audit-log.sh` + `tests/test_audit_log.sh` | Phase 66 — KEEP disposition: hardened raw-`printf $FILE_PATH` → `jq --arg` (fixed latent JSONL-injection; mkdir fail-open) + NEW 6-test functional-smoke (record shape + injection-safety + fail-open) the eval lacked; 15th make-test script | 2026-05-29 |
| `templates/.claude/hooks/{enforce-spec,enforce-loop,enforce-memory,dev-wiki-scope-check,detect-loop,check-tests-were-run}.sh` | Phase 65 — hardened inline-duplicated `log_firing` (fail-open, exit-neutral under set -e, jq --arg controlled-vocab, atomic >>) → `.dev-wiki/enforcement.log`; retrofit of the 3 existing loggers fixed their JSON-injection + truncation-race bugs | 2026-05-29 |
| `tests/test_firing_log.sh` | NEW (Phase 65) — 21 tests: per-hook well-formed record, fail-open (chmod-000 + stubbed-date) preserving exit 2 on all 4 blocking hooks, injection/exfiltration-safe, static 6-copy byte-equality; 13th make-test script | 2026-05-29 |
| `eval/comparison/methodology.md` | Phase 65 — RETIRED tombstone (A-vs-C confounded apparatus removed; A-vs-B clean note kept). `eval/reasoning/README.md` demoted to calibration-only/never-a-gate. | 2026-05-29 |
| `modules.json` | Single canonical scope-tagged `hooks` array (17 project + 1 global) + top-level `hook_dirs` map (script → companion dirs; Phase 85) — hook source of truth; the Phase-76 comparator reads both for the drift comparison set | 2026-06-10 |

## Session Journal (last 5)

- [2026-06-10] [[2026-06-10-phase-86-ceremony-lift-measurement-complete|Phase 86 complete — Ceremony Lift Measurement: stage-1 screen + checkpoint verdicts (4 decided, 2 to stage-2/follow-on); review gate caught the retrofit class against the frozen pre-registration]] -- 8/8 tasks. T1 pre-registration FROZEN (9ad62f0, survived 11 commits). T2-T3 cost extractor (13-check control; DISCOVERY: subagent cost recovery silently zero — both marker forms missed, control strengthened) → cost table 12 sessions (~66% adj / ~64% wall, EARLY-EXIT no). T4-T6 five blind tabulator controls → 44-dispatch manifest (anchor==8) → evidence table + re-execution log (flagship: Ph85 review-gate catch admitted via transcript Edit-call reverse-application, 4/4). T7 verdicts: dev-plan=trim, spec=ambiguous-stage-2, approach-rev=keep, plan-rev=keep-by-immateriality, review-gate=underpowered-keep-direction (2nd live catch during this debrief), debrief-capture=trim; STAGE-2 go, follow-on. T8 8/8 exit criteria + verdicts-only check. Review gate 6/10 revise: CRITICAL taxonomy-widening fixed inline; A2 STOP fired, accepted (ledger A2 bit, A1/A3/A4/A5 held). READY FOR COMPLETION, delivery gate pending.
- [2026-06-10] [[2026-06-10-phase-85-install-gap-dogfood-complete|Phase 85 complete — Install-Gap Fix + Edge-Screener Dogfood: hook_dirs shipped on every install path, checker directory currency, edge-screener single-registration migration, A5 zero-demand evidence]] -- Five serialized stages, 8/8 tasks. **T1** causal-path verdict CONFIRMED (Phase-82 drift-guided resync via the dir-blind checker shopping list; py-init/ts-init exempt recursive; nana-init exempt no-copy). **T2-T4** modules.json `hook_dirs` map (4 consumers migrated, zero `project_local.extra_dirs` refs remain) + install.sh `ship_hook_dirs` both paths + checker consumer-conditioned directory cells with orphan flagging (test_install_drift_dircurrency.sh, 7 seeded controls → 26 test scripts; DISCOVERY: latent bash-3.2 set-u empty-array crash fixed). **T5** rehearsal GREEN → HARD checkpoint 1 (A1 confirmed; A3 keep-current) → live ~/.claude run (backup + tested restore; post: drift 0, scope:global set equality). **T6** DRQ-1: **string-keyed dedupe** (2 positive-controlled headless probes; A2 bit) → HARD checkpoint 2 → edge-screener migration (settings.json → {}, --project-local install, 17/17 basenames exactly once, each Stop hook fired exactly once). **T7** dogfood 2 real headless sessions (USER OVERRIDE: spec's provenance-noted fallback; 390 tests / 94.44% cov exact baseline; Phase-10 candidate analysis delivered to edge-screener) + A5: liveness-probed ZERO voluntary memory use = genuine demand evidence, FILED. **T8** 9/9 exit criteria, eval zero flips. Review gate 8/10 revise → HIGH (harness-audit 3 stale 4b predicates false-DRIFT) + 2 MEDIUMs fixed inline, re-verified. Ledger revisit: A1 held (resolved confirmed), A2 bit, A3/A4 held. RETRO (81-85): 3 ledger bits in 5 phases, ALL absorbed by pre-declared verification branches; corrections concentrate at gates/checkpoints; instrument-validity = the dominant recurring blocker class. 8/8 tasks ✓, **READY FOR COMPLETION, delivery gate pending.** Next: accept → prune-on-value round 2 or edge-screener Phase 10.
- [2026-06-10] [[2026-06-10-phase-84-hook-registration-hygiene-complete|Phase 84 complete — Hook & Registration Hygiene: post-commit redesigned on event-arrival-as-success, detect-loop filed upstream, 11 ghosts deregistered after 6-root remediation]] -- Instrument-first, capture-anchored. **T1** eval harness hermetic (run_hook exports CLAUDE_PROJECT_DIR=$WORK_DIR; seeded-leak self-check RED-verified; post-instrument baseline: ZERO flips — the Phase-82/83 leak was not load-bearing, supersession noted). **T2** live capture: PostToolUse carries NO exit-code field (tool_response: interrupted/isImage/noOutputExpected/stderr/stdout) AND does NOT fire for failing commands — event arrival IS the success signal; foreign-session events arrived via the ghost registration (machine-wide firing proven live). **T3** post-commit REDESIGN (event-arrival + legacy .exit_code failure guard + *git*commit* prefilter + git-recency ≤120s + project-local OR HOME marker + HEU-002 fail-loud); detect-loop UPSTREAM (counter unimplementable hook-side; hook untouched; platform defect filed; prune candidate). **T4a/T4b** HARD checkpoint → maintainer approved FULL remediate-then-deregister: 6 roots remediated (incl. session-start.d), 11 ghosts deregistered (end-state == modules.json scope:global), survivor smoke green, backup + tested restore, fresh-session check handed off (rehearsal.log). **LIVE REPAIR:** ~/.claude/hooks/session-start.d/ EMPTY → every SessionStart errored machine-wide since 06-09 (5th registered-but-broken instance; install-gap filed). **T5** 10/10 exit criteria; 2 eval scenarios had passed only via the hook's false-fire → init_git:true + eval-runner lifecycle extension (DEPENDENCY). ESCAPE HATCHES: DEPENDENCY + DISCOVERY ×2 + user-reported live repair. Ledger revisit: A1 bit (detect-loop had no recoverable signal — the pre-declared upstream branch absorbed it), A2 open (fresh-session check pending), A3/A4/A5 held. make test 22→25 scripts (+25 tests), make eval 52/52, drift 0, reviewer 9/10 ACCEPT (2 MEDIUM inline, 2 deferred-with-rationale). 6/6 tasks ✓, **READY FOR COMPLETION, delivery gate pending.** Next: accept → next direction open (A5 memory-layer / edge-screener dogfood / install-gap fix).
- [2026-06-09] [[2026-06-09-phase-83-prune-on-value-subtraction|Phase 83 complete — Prune-on-Value Subtraction: 4 of 6 dead-weight zeros were measurement artifacts; 2 cuts, 2 keeps, 2 hardens activated]] -- Evidence-first, verdict-gated, serialized subtraction over the 6 Phase-82-evidenced candidates. T1 installed-surface DISCOVERY (A3 reject vindicated: 4 consuming projects found beyond the assumed roots; edge-analyst even has enforce-memory registered) + removal-sets-first liveness grep (LIVE vs HISTORICAL). T2 hermetic arming: enforce-memory couldnt-fire (marker SHIPPED by install.sh per modules.json — the always-loaded "NOT shipped" claim corrected — and firing LIVE, 69 records in this very session); memory-reinforcement couldnt-fire (A7: no fastembed → reinforce branch unreachable; word-overlap only warns); audit-log-model-field couldnt-fire (plumbing works, CLAUDE_MODEL set by nothing); orphan-companions + harness-audit didnt-fire; memory-mcp-scaffold no-surface. T3 checkpoint (couldnt-fire rows presented as DEFECTS, no cut offered): Jake approved 2 keeps + orphan cut, OVERRODE 3 — model-field cut now, fastembed install now, harness-audit wire-in over cut. T4: 2 cuts one commit each (regenerated-diff ⊆ removal set; 23 installed-surface DEREG assertions across 6 roots) + both hardens IMPLEMENTED (cosine reinforcement verified end-to-end with real nomic embeddings; make audit + functional smoke). T5: 10/10 exit criteria, make test 22 green, make eval 52/52 UNCHANGED, drift 0. Ledger revisit: A2 bit (zeros-measure-demand false for 4/6 — the arming gate caught it), A5 open. Reviewer 9/10 ACCEPT (4 MEDIUM fixed f6c7e8d). ESCAPE HATCHES: USER OVERRIDE x2 (checkpoint), DISCOVERY (scope additions). 5/5 tasks ✓, **READY FOR COMPLETION, delivery gate pending.** Next: accept → /dev-plan Phase 84.
- [2026-06-09] [[2026-06-09-phase-82-qa-verification-sweep|Phase 82 complete — QA & Verification Sweep (ultracode): 58 candidates, 35 fixed, enforcement layer restored from 15-day dormancy]] -- Coverage-defined 8-area sweep (wiring/firing/companions/schema/drift/coverage/docs/usage). **T1** baseline + instrument controls (every clean-vouching checker first caught a seeded synthetic defect). **T2** 8-agent ultracode READ-ONLY fan-out → 58 candidates. **T3** orchestrator-executed verification (NO subagent prose as evidence — Jake's A2 reject held), mid-phase report, the pre-registered >10-confirmed-defects STOP fired → Jake re-scoped (all 4 clusters approved) → 35 serialized sandbox-first fixes. **T4** verification-matrix close-out + 20 deferred filings + 3 orphans to the subtraction list + 10/10 spec exit criteria via run-exit-criteria.sh. **HEADLINES:** enforcement layer silently DORMANT since 05-25 (COMPOUND: `.tool_input` event-shape + missing enforce marker + allowlist bypass + em-dash slug) → restored, enforce-memory's first lifetime firing ([[hook-event-shape-normalization]]); 11 stale project-scoped hook copies in ~/.claude invisible to scope-tagged drift → pass 2b presence-comparison ([[drift-compare-installed-presence]]); MANIFEST regenerated (122 checksums, stale since Phase 63). ESCAPE HATCHES: SECURITY (live enforce-spec gate self-lockout mid-fix — recovered via Bash, gate matcher is Write|Edit only) + DISCOVERY (>10 STOP). First END-TO-END dogfood of the Phase-81 assumption gate (real don't-know + real reject → 2 forced revisions; revisit: A1 bit, A2-A6 held). make test 20→22 scripts (~480 assertions) green, make eval 52/52, drift 0 EXTENDED, reviewer 9/10 ACCEPT findings fixed inline. 4/4 tasks ✓, **READY FOR COMPLETION, delivery gate pending orchestrator commit-verify.** Next: accept → /dev-plan Phase 83.

## Cross-References

- Phases 1-86: 85 completed (all delivery-accepted) + Phase 86 READY FOR COMPLETION (delivery gate pending); Phase 86 — Ceremony Lift Measurement: stage-1 deterministic screen over the ceremony steps + maintainer checkpoint verdicts; STAGE-2 go routed follow-on; VERDICTS-ONLY (no kit component touched, pinned base 5360486).
- **Phase 86 outcome:** the ceremony steps got their first measured disposition ([[ceremony-step-verdicts]], high; apparatus [[ceremony-lift-tiered-screen]], high). Cost: ceremony ~66% of cache-adjusted tokens / ~64% wall-clock across 12 sessions (Ph76-85) — material, no early exit. Evidence (post-correction): 1 outcome-grade-admitted (the Ph85 review-gate catch, fully re-executed via transcript-extracted pre-fix state) / 10 ambiguous-downgrade / 8 consumption-capped / 25 zero-catch. Verdicts: dev-plan-orchestration=trim (ride-alongs to follow-on; assumption-vote gate untouched), spec-generation=ambiguous-stage-2 (structurally stage-1-undecidable), approach-reviewer=keep (5.4%), plan-reviewer=keep-by-immateriality (0.8%), review-gate-reviewer=underpowered-decide-by-arithmetic keep-direction (~600k adj tokens/phase vs the 8-33-phase instrument-dead class; second live catch arrived during the debrief itself), debrief-capture=trim (knowledge-capture half = prime follow-on cut candidate). The review gate convicted the tabulation of taxonomy-widening against the FROZEN pre-registration (the retrofit class; A2 bit, STOP fired + accepted) — the freeze + external reader is the anti-retrofit mechanism, not the freeze alone.
- Phases 1-85: 84 completed (all delivery-accepted) + Phase 85 READY FOR COMPLETION (delivery gate pending); Phase 85 — Install-Gap Fix + Edge-Screener Dogfood: hook_dirs shipped on every install path, the drift checker gained consumer-conditioned directory currency, edge-screener migrated off the Phase-79 hand-patch to a single-registration template-sourced install, A5 zero-demand memory evidence filed
- **Phase 85 outcome:** structural-invariant fix + consuming-project dogfood ([[install-gap-dir-currency]], high). T1 inventory CONFIRMED incident 5's causal path (the Phase-82 drift-guided resync used the dir-blind checker output as its shopping list; py-init/ts-init exempt — recursive copy carries .d/; nana-init exempt — no-copy; the A1 deferred don't-know resolved confirmed at checkpoint 1). T2-T4: modules.json `project_local.extra_dirs` → declared top-level `hook_dirs` map (4 consumers migrated; a project_local-named key driving global-path behavior was the single-source smell); install.sh `ship_hook_dirs` ships consumer-adjacent dirs on BOTH paths (find-based, empty-glob tolerant under set -euo pipefail); check-install-drift.sh consumer-conditioned directory cells + `orphan:` rows (flag, never remove — detect-and-warn charter) + a latent bash-3.2 set-u empty-array crash fixed; 7 seeded controls in the 26th make-test script. T5 checkpoint 1: A3 **keep-current** for the registration-dead ~/.claude copies ([[a3-disposition-keep-current-for-registration-dead]]; disposal routed to the next prune-on-value round with the Phase-82 residue inventory) → live run, drift 0 WITH directory cells, scope:global set equality. T6 DRQ-1 settled empirically ([[drq-1-settings-merge-semantics-are-string-keyed]]): **string-keyed dedupe** — identical command strings dedupe, distinct strings invoking the same script BOTH fire → the migration reframed from correctness fix to hygiene closing a latent class; edge-screener now single-registration template-sourced (union-uniqueness 17/17 basenames, single-firing verified, 390 tests / 94.44% cov exact pre-migration baseline; .bak superseded by tested backup). T7: 2 real headless dogfood sessions (provenance-noted USER OVERRIDE per the spec's fallback) + **A5 evidence: liveness probe exit 0 / DB absent row count 0 → ZERO voluntary memory use = genuine demand evidence, FILED not disposed**. Review gate 8/10 revise: HIGH harness-audit.sh stale-predicate false-DRIFT (instrument-dead class) fixed inline (all 3 stale 4b predicates: ship_hook_dirs + recursive dot-copy alternation, verified DRIFT: none); assert-global-set.sh set-vs-multiset hardened (seeded-duplicate control exits 1); rehearsal fixture deviation RECORDED in checkpoint-1.md. SOFT OBS: check-tests-were-run Read-trigger false positive with real bite (filed to Blockers); harness-audit 4b DRIFT block lacks a seeded control; checker section-2 set has no dedup guard; enforcement.log decision:null (Phase-66 schema-gap echo); headless `claude -p` sandbox probes = reusable platform-semantics measurement method; _CURRENT_STATE 212/100 overage (curation candidate). make test 26 scripts green, make eval 52/52 ZERO flips, 9/9 exit criteria.
- Phases 1-84: 83 completed (all delivery-accepted) + Phase 84 completed (delivery accepted + gate flipped 2026-06-10, e894dff); Phase 84 — Hook & Registration Hygiene: post-commit redesigned on event-arrival-as-success, detect-loop filed upstream, 11 ghost global registrations deregistered after 6-root remediation; the machine-wide SessionStart breakage found and repaired live
- **Phase 84 outcome:** instrument-first, capture-anchored hygiene ([[hook-registration-hygiene]], high). T1 made the eval harness hermetic FIRST (run_hook exports CLAUDE_PROJECT_DIR=$WORK_DIR; seeded-leak self-check RED-verified; post-instrument baseline ZERO flips — the Phase-82/83 leak was not load-bearing for any prior verdict, supersession recorded never rewritten). T2 live capture settled the branches with evidence: PostToolUse events carry NO exit-code field at any level (tool_response: interrupted/isImage/noOutputExpected/stderr/stdout) AND the platform does NOT deliver PostToolUse for failing commands — event arrival IS the success signal; foreign-session events arrived via the ghost registration (machine-wide firing proven, incl. non-kit signal-watch). T3: post-commit **REDESIGN** (event-arrival + legacy `.exit_code` failure guard + `*git*commit*` prefilter + git-recency ≤120s + project-local OR HOME marker + HEU-002 fail-loud; the planned detect-loop jq migration pre-empted); detect-loop **UPSTREAM** (consecutive-failure counter structurally unimplementable hook-side; hook untouched; platform defect + prune-on-value candidate filed). T4a/T4b: the HARD checkpoint approved the FULL remediate-then-deregister package (the A2 deferred don't-know resolved at its declared decision point) — 6 roots remediated (hooks + session-start.d synced, 11/11 project-local registration, ai-game null entry dropped), 11 ghosts deregistered (removed=11, context-size-check intact, non-hook keys byte-identical), survivor smoke green, end-state == modules.json scope:global set; backup + tested restore; fresh-session verification handed to the maintainer. LIVE FINDING: ~/.claude/hooks/session-start.d/ EMPTY while session-start.sh hard-sources 3 modules under set -e → every SessionStart errored machine-wide since 06-09 (5th registered+present+current-but-BROKEN instance; repaired live; install-gap Blocker filed — modules.json declares the dir only under project_local.extra_dirs). Two eval scenarios had passed ONLY via the hook's false-fire → init_git:true + eval-runner lifecycle extension (DEPENDENCY). Post-T4b: 2 Python-workflow Stop hooks trimmed from the kit repo's own settings (bash/markdown repo; logged, reversible). Ledger revisit: A1 **bit** (detect-loop had NO recoverable signal — the pre-declared upstream branch absorbed it; post-commit half held), A2 open (fresh-session check pending), A3/A4/A5 held. SOFT OBS: project-local registration is Python-harness-shaped everywhere (no language tag in modules.json); single-file md5 currency misses directory-level absence; global hooks fire across ALL concurrent sessions (hazard + the reason cleanup mattered); the zsh positive-control hazard bit twice at the gate. make test 25 scripts green (+25 tests), make eval 52/52 unchanged denominator, drift 0, 10/10 exit criteria, reviewer 9/10 ACCEPT.
- Phases 1-83: 82 completed (all delivery-accepted) + Phase 83 completed (delivery accepted + gate flipped fb6d189); Phase 83 — Prune-on-Value Subtraction: 6 evidence-forced verdicts — 2 cuts / 2 keeps / 2 hardens-activated; 4 of 6 Phase-82 dead-weight zeros were measurement artifacts; the couldnt-fire-vs-didnt-fire gate earned its keep
- **Phase 83 outcome:** evidence-first, verdict-gated subtraction ([[prune-on-value-subtraction]], high) over the Phase-82 usage filings. The dominant control (zero may measure broken plumbing, not absent demand) FIRED 4 times: enforce-memory's lifetime zero was the event-shape dormancy — its opt-in marker is SHIPPED by install.sh (modules.json markers list; the always-loaded "NOT shipped" claim was corrected in place) and it now fires live (69 records in the phase's own session) → KEEP, demand question re-filed with real data; memory-reinforcement's 0/55 was a structurally unreachable cosine branch (no fastembed; word-overlap fallback only warns) → harden ACTIVATED (fastembed installed, cosine reinforcement verified end-to-end; install.sh contract unchanged per [[benchmark-only-hybrid-deps]]); audit-log-model-field couldnt-fire (CLAUDE_MODEL set by nothing — upstream-unfixable) → maintainer-override CUT (field-level; audit-log stays per [[audit-log-disposition]]); memory-mcp-scaffold had NO per-project surface (zero .memory DBs across all 5 consuming projects) → KEEP, the kit-side layer-value question = the A5 deferred-don't-know Blocker (must-revisit). True didnt-fire cuts: orphan-companions (3 files, ORPHAN_EXEMPT emptied — Direction C now stricter, 18 installed copies removed across 6 roots); harness-audit OVERRIDDEN to wire-in (`make audit` + functional smoke — it runs correctly and emits the per-hook utilization report this phase produced by hand). Installed surface DISCOVERED not assumed (A3 reject → kit-marker scan found ab-test/stock-screener, edge-analyst, fate, ai-game). Serialized per-candidate commits, regenerated-diff ⊆ removal set, 23 DEREG assertions. Ledger revisit: A2 **bit** instructively (the arming protocol caught the false zeros), A1/A3/A4/A6/A7 held, A5 open. SOFT OBS: filed utilization evidence DECAYS (re-arm before trusting zeros); enforcement.log now accumulates real firing data (cheap demand analytics); the installed footprint (7 roots) exceeds what drift tooling tracks (Phase-76 item B re-trigger approaching); modules.json ships the enforce-memory marker armed-by-default vs the opt-in design intent. Reviewer 9/10 ACCEPT (4 MEDIUM fixed inline). make test 22 green, make eval 52/52 UNCHANGED denominator, drift 0, 10/10 spec exit criteria.
- Phases 1-82: 81 completed (all delivery-accepted) + Phase 82 READY FOR COMPLETION (delivery gate pending orchestrator commit-verify); Phase 82 — QA & Verification Sweep (ultracode): 58 candidates → 35 fixed / 20 deferred-with-filings / 3 orphans; enforcement layer restored from 15-day dormancy; drift 0 under an EXTENDED comparison set; first END-TO-END live dogfood of the Phase-81 assumption gate
- **Phase 82 outcome:** coverage-defined 8-area QA sweep ([[qa-verification-sweep]], high) under the orchestrator-only evidence standard (subagents READ-ONLY candidate-generators — Jake's A2 reject; every verdict an orchestrator-executed deterministic command) + controls-first staging (clean-on-seed = instrument-dead). 58 candidates; the pre-registered >10-confirmed-defects STOP fired → Jake re-scoped (all 4 clusters) → 35 serialized sandbox-first fixes; 20 deferred verdicts FILED as Blockers + 3 orphans to the subtraction list. HEADLINES: (1) the enforcement layer silently DORMANT since 05-25 — COMPOUND (platform `.tool_input` event-shape vs legacy `.input` parsing + missing `~/.claude/enforce` marker + absolute-path allowlist bypass + em-dash slug break); fixed via canonical `.tool_input.X // .input.X // empty` parse + per-hook path normalization + transcript-scanning Stop hooks + phase-number-glob spec lookup; the fixed enforce-spec gate then blocked the orchestrator's OWN mid-phase edits (firing proven live; SECURITY escape hatch: self-lockout recovered via Bash, sandbox-first held) — [[hook-event-shape-normalization]] (high). (2) check-install-drift.sh pass 2b: any kit-shipped hook PRESENT in the installed root is compared regardless of scope tag — 11 pre-Phase-79 project-scoped hooks ran stale in ~/.claude under a "drift 0" report; copies refreshed, ghost REGISTRATIONS deferred to a maintainer call — [[drift-compare-installed-presence]] (high). Architectural: +2 make-test scripts (22 total, ~480 assertions), test_companions Directions C+D, session-start [nana:enforce] advisory, MANIFEST regenerated (122 checksums, stale since Phase 63), register-settings.py cmd_mcp --modules-json single-sourcing. SOFT OBS: no platform-shape canary (next field rename re-creates silent dormancy); enforcement.log lacks run-provenance (observed test contamination); living-doc caps unguarded (_CURRENT_STATE 188/100, _ARCHITECTURE ~102/100); prune-on-value candidates must be re-judged against the RESTORED enforcement layer (enforce-memory's "zero lifetime firings" INVERTED post-fix — disabled, not dead). Reviewer 9/10 ACCEPT, findings fixed inline. make test green, make eval 52/52, 10/10 spec exit criteria.
- Phases 1-81: 80 completed (all delivery-accepted) + Phase 81 READY FOR COMPLETION (delivery gate pending orchestrator commit-verify); Phase 81 — Assumption-Approval Gate: the dev-plan direction gate now takes accept/reject/don't-know POSITIONS on the plan's cost-sorted load-bearing assumptions (positions REPLACE the approval click) + an append-only cross-phase ledger + a dev-debrief revisit forcing-function + a deterministic NO-LLM check; the earned consequence of Phase 80's INSTRUMENT-DEAD; efficacy UNMEASURABLE in-kit (tests assert MECHANICS only)
- **Phase 81 outcome:** ship the SIMPLEST assumption-approval gate ([[assumption-approval-gate]], high) — the earned consequence of Phase 80's INSTRUMENT-DEAD (the scope-anchored surfacer's only edge was a working-knowledge LEAK; the clean signal DEGENERATE — a naive cost-sorted surfacer recovers load-bearing assumptions by reasoning). The dev-plan direction gate's "approve the approach?" step is REPLACED by the maintainer taking accept/reject/don't-know **positions** on the plan's cost-sorted load-bearing assumptions (positions ARE the gate; no unresolved reject/don't-know = direction confirmed; reject→revise+re-surface, don't-know→defend/down-scope else route-to-Blockers+must-revisit), all-accept → warn + track `all_accept:true` + restate (Jake's Q2, NOT a hard block), an append-only `.dev-wiki/assumption-ledger.md` (Phase-80 A1-A4 seed) + a dev-debrief revisit forcing-function (re-scans prior-phase unrevisited rows — the cascade pattern) + a deterministic NO-LLM `scripts/check-assumption-ledger.sh` (4 modes + `--selftest`, bash/awk, the ONE canonical schema source; `--append-only` guards per-block row-count) enforced at debrief finalization. NO new hook ([[HEU-012]]: the debrief-finalization check is the firing point; the ledger ROW is the firing evidence). Efficacy is UNMEASURABLE in-kit (the amplifier-null + Ph66/69/80 representativeness) — tests assert MECHANICS only, no quality delta claimed; the value is the human role-change (rubber-stamp → interrogator). SOFT OBS: live evidence for the gate's own thesis — the agent's OWN buried assumption ("subagents are clean-context", Ph80) was missed by self-check, caught only by an adversarial audit; Phase 81's reviewer (9/10 ACCEPT) then caught a MEDIUM self-check had passed (the `--append-only` row-level wording-vs-impl gap → fixed) + a LOW stale "approach approval" string → both fixed inline. make test "All tests passed", make eval 52/52, `--selftest` exit 0, test_assumption_ledger 22/22, no new hook → firing-coverage/registration unchanged. 4/4 tasks ✓, all 10 spec exit criteria pass. Deferred: a session-start advisory backstop for unrevisited rows (re-trigger: debrief-skip). Parked: the accretion/budget-class residual (needs a CONSUMING-project context). Phase 82 = the first live dogfood of the gate.
- Phases 1-80: 79 completed (all delivery-accepted) + Phase 80 delivery-accepted; Phase 80 — Assumption-Surfacer Completeness Screen: silent-class 50-run screen → PROGRAM-VERDICT INSTRUMENT-DEAD (clean-context control FAILED); the **5th amplifier-null**, now extended from decisions/tooling-correctness to assumption-surfacing
- **Phase 80 outcome:** the dev-plan human gate was proposed to move from approving the APPROACH (a conclusion the maintainer can't evaluate → blind-yes) to taking accept/reject/don't-know positions on the plan's load-bearing ASSUMPTIONS ([[assumption-surfacer-completeness-screen]], high). The plan was interrogated LIVE via that mechanism (the gate eating its own dogfood): Jake A1 accept / **A2 REJECT** ("can't trust the agent-CHOSEN assumption set; solve set-completeness first") / A3 accept / A4 accept — the single A2 reject made it a **SCREEN, not a build**. Pivoted (post-spike) to the **SILENT** class via the project's REAL silent failures (`mcp-cwd`, `line-cap`, `cascade` — the R5 silence-gap), not synthetic plants. **T1 GATE → GO**; prereg `86d8584` committed BEFORE runs, ancestor-guarded; two-track verdict + variance-derived bar + the explicit `surfacer ≈ baseline ⇒ 5th-null` possibility frozen up front. The 50-run screen (5 fixtures × 2 conditions × n5, NO-LLM scoring) returned **`^PROGRAM-VERDICT: INSTRUMENT-DEAD` because a control FAILED**: the workflow subagents ran INSIDE nana-dev-kit and inherited its always-loaded `working-knowledge.md`, which documents all 3 real fixtures' buried assumptions; the SURFACER **leaked** those documented answers — **4/5, 5/5, 5/5** on the real cases and **0/5** on the 2 cases NOT in working-knowledge (NAIVE clean ~0/5). The headline SURFACER>NAIVE (line-cap 5/5 vs 0/5) is a **leak artifact** — **the amplifier-null caught in the act** (Ph70/71/77/78 sharpest form; harness re-presents documented answers). **Informative-null:** the clean signal points **DEGENERATE** — NAIVE recovered **3/4** silent-class assumptions by pure reasoning, the one fully-clean comparison (costsort-adversarial) was BOTH-CATCH; the project's silent failures were silent because nobody **ASKED** at planning time. SOFT OBS: the agent's OWN buried assumption ("subagents are clean-context") was missed by self-check, caught only by an adversarial output audit — external forced checking beats self-triggering interrogation (the handover thesis, observed on the very phase that screens it). **FORWARD (Phase 81):** ship the SIMPLEST gate (naive cost-sorted surfacer + accept/reject/don't-know + the A3 ledger detect-after + the all-accept block), NOT the elaborate scope-anchored machinery (its only edge was the leak). **Residual (parked):** the accretion/budget class (line-cap) is genuinely UNMEASURED — a clean test needs a CONSUMING-project context (Ph66/69 representativeness, re-confirmed: you cannot measure assumption-surfacing inside the project whose working-knowledge documents the answers). Repo-only `eval/assumption-screen/` (NOT wired into install.sh/Makefile/make test/make eval); make test green.
- **Phase 79 outcome:** the 4th dogfood→harden fix ([[harness-hygiene]], high; spec nana:approved) — two bundled kit defects degrading real sessions. **(A)** hooks registered with bare-relative `.claude/hooks/X.sh` 404 under a non-root CWD (edge-screener Stop-hook dogfood; Claude Code does NOT guarantee CWD=project-root) → register-settings.py + template + install.sh emit `${CLAUDE_PROJECT_DIR}/.claude/hooks/X.sh` (project) / `$HOME/...` (global); all 17 hooks `cd "${CLAUDE_PROJECT_DIR:-.}"`; the unconfirmable live check → a committed hermetic wrong-CWD test. **(B)** `working-knowledge.md` 45,217 chars/99 entries tripped the context warning (curator caps count/lines not SIZE) → 4 amplifier mega-entries compressed → 1 + dev-wiki pointers (45,217 → 35,726, no knowledge lost) + a NON-DESTRUCTIVE per-entry char-cap advisory (`WK_MAX_ENTRY_CHARS=1500`, warns never truncates). make test all-passed, make eval 52/52, test_harden 26/26, curation 15/15, firing-coverage 21/21. Deferred: prune-on-value (subtraction framing); edge-screener re-sync (interim hand-patch holds).
- **Phase 78 outcome:** the capability/correctness boundary screen ([[skill-crystallization-headroom-screen]], high; reviewer-hardened C1–C4/M1–M5) — does a candidate tooling artifact embed correctness a bare model CANNOT re-derive from the RECOVERABLE CORPUS (interface + call sites + task) a non-crystallized consuming project would have? Successor to the decision-retention line (Ph70/71/77 all null). NO-LLM `check.sh` (n=5/threshold=4 cloned from anchor-screen), `.offleak` guard, 4 controls, ancestor-guarded prereg `3f6a0cb`. **RESULT: PROGRAM-VERDICT TERMINATE** — both candidates DEGENERATE (edge-eligibility/domain-PIT-survivorship 4/5, nana-drift/general 5/5); controls valid (instrument LIVE). **Headline: a tooling artifact's correctness IS in the goal that motivated it** — the capability→skill crystallization module is DEAD-ON-ARRIVAL, do NOT build it. Surviving avenue stays Ph70 (genuinely proprietary/post-cutoff). Repo-only `eval/amplifier/skill-screen/`, frozen; subjects byte-identical.
- Phases 1-77: 76 completed (all delivery-accepted) + Phase 77 READY FOR COMPLETION (delivery gate pending); Phase 77 — Cross-Session Retention Headroom Screen (audit-gated ablation): residual 0/14 → PROGRAM-VERDICT TERMINATE; the amplifier decision-retention line now closed across all 3 regimes
- **Phase 77 outcome:** the amplifier program's LAST untested regime measured ([[cross-session-retention-headroom-screen]], high) — does the persistent on-disk dev-wiki substrate let a fresh session recover decisions a substrate-free session loses across a real session boundary? Measured on the REAL external edge-screener substrate (Phase 73; decidable-when green at 9 phases / 14 decision articles / 11 journals / 28 commits) via an **audit-gated ablation**: a cheap deterministic T1/T2 residual-audit GATE (`residual-audit.sh`, NO LLM — extract one pre-registered discriminating token per decision pinned to terminal value, absence-grep over OFF corpus = code+tests+git-MESSAGES with `git log -p` EXCLUDED so the stripped substrate is not resurrected; HEAD-resolvability filter; `--selftest` proves the predicate both ways) short-circuits before any OFF/ON run. **RESULT: residual 0/14** — every discriminator RECOVERABLE from **code+tests alone** (gitmsg not even needed), incl. the two residual-FAVORABLE candidates (`Shumway` −30% delisting citation, `Stooq` deferred source). Per the pre-registered ladder residual 0 < floor 3 ⇒ **TERMINATE**; T3 OFF/ON ablation correctly SKIPPED-BY-GATE (L cost not spent — the audit selftest already proves instrument liveness). Anti-retrofit: prereg `21a6c52 ⊂ HEAD`; DISCOVERY = the real `residual.md` moved T1→T2 because the token list must be COMMITTED before the audit (else cherry-picking). **HEADLINE: "a decision that has been implemented IS in the implementation"** — the substrate was never the sole carrier of any decision's operative value. CLOSES the decision-retention line across all 3 regimes (Ph70 DEGENERATE 5/5, Ph71 TERMINATE-by-summary-robustness, Ph77 residual-0 TERMINATE); surviving untested avenue stays the Ph70 proprietary/post-cutoff-retrieval one. Substrate kept on OPERATIONAL grounds; no measured harness-value claim. Caveat (pre-registered): a null residual ≠ substrate worthless — the OFF baseline's decision-rich commit bodies are partly a product of the harness's debrief discipline. Apparatus frozen, repo-only `eval/amplifier/xsession-screen/`; edge-screener byte-identical pre/post. make test green, make eval 52/52, zero surface churn. Un-sampled sliver (Phase-78 candidate, low): process/sequencing-roadmap retention.
- Phases 1-76: 75 completed (all delivery-accepted) + Phase 76 delivery-accepted; Phase 76 — Installed-Copy-Drift Guard: a deterministic fail-open `scripts/check-install-drift.sh` comparator (modules.json comparison set minus a pinned 3-entry exclusion allow-list) surfaced at session-start (kit-repo-scoped, canonical-path gate) + install.sh --status closes the silent installed-copy-drift class that bit Phases 73 & 75
- **Phase 76 outcome:** the 3rd dogfood→harden fix ([[installed-copy-drift-guard]], high; reviewer 9/10) — the kit develops in `templates/.claude` but RUNS from `~/.claude`, so a stale installed copy silently undermined work twice (Phase-73 curator gap; Phase-75 debrief ran the OLD delivery-flow). Detect-and-warn, scope (A) maintainer-side only (symlink/`--link` REJECTED — live-WIP-breakage risk; (B) consuming-project drift deferred). `scripts/check-install-drift.sh` (bash+jq, NO LLM): comparison set from `modules.json` (installed skills + `scope:global` hooks + managed rules-dir glob) minus a pinned bounded 3-entry exclusion allow-list (`rules/nana-personal.md`, `rules/py-session-state.md`, `settings.json`); not-installed skill dirs SKIPPED; installed-root overridable for hermetic tests. Surfaced as a `[nana:drift]` advisory in `session-start.sh` gated to the kit repo via a **canonical-physical-path** compare (a string compare silently never fired under macOS `/var`→`/private/var` — fixed) + an "Install drift" line in `install.sh --status`. **DOGFOOD (headline):** the guard's first firing caught the maintainer's real `~/.claude` 36 files behind `templates/` — including the exact Phase-75 scar (`skills/dev-debrief/delivery-flow.md`); resynced via `install.sh` this session, drift 36 → 0, detect→resync→silent verified end-to-end. make test green (test_install +8 → 120, test_harden +4 → 22), make eval 52/52, firing-coverage 21/21 (no new hook → no count/registration/README churn). The dogfood→harden loop (Phases 74-76) continues to pay.
- Phases 1-75: 75 completed (all delivery-accepted); Phase 75 — Delivery-Commit Verification: a fail-open session-start divergence detector (PRIMARY) + delivery-flow D3 commit self-assert + gate-after-commit ordering (SECONDARY) close the accepted-but-uncommitted divergence the edge-screener dogfood exposed
- **Phase 75 outcome:** the 2nd dogfood→harden fix ([[delivery-commit-verification]], high) — `/dev-debrief` had marked edge-screener Phase 2 `[x] Delivery accepted` + journaled it while the work was NEVER committed (Phase 3 built on the uncommitted tree; gate-state and git-state silently diverged). Reframed from "guarantee the commit fires" (unachievable — committing is an agent action; the D3 commit step was itself skipped) to "make gate-state diverging from git-state impossible to ignore." PRIMARY = a fail-open `session-start.sh` detector (`[nana:recovery]` when active-phase.md shows delivery-accepted for Phase N but `git log` has 0 commits matching `phase[ _-]?N\b`; fires independent of agent adherence, RED-first, 4 tests in test_harden.sh 17/17, firing-coverage 21/21 no churn). SECONDARY = delivery-flow D3 verifies the commit landed + gate-after-commit ordering (executor writes the gate UNCHECKED, D3 flips it post-verified-commit). make test green, make eval 52/52, eval/ git-diff-clean. SOFT OBS (2nd instance): the kit's own /dev-debrief runs the INSTALLED skill, so the `templates/` fix is not live until install.sh re-syncs → strengthens the installed-copy-drift-guard case.
- Phases 1-74: 74 completed (all delivery-accepted); Phase 74 — Harden the Consuming-Project Scaffold Path: fixed the 4 edge-screener-dogfood scaffold defects at the source (reviewer SHIP)
- **Phase 74 outcome:** the dogfood→harden loop ([[harden-consuming-project-scaffold]]) — the first real consuming project (edge-screener) exposed scaffold defects, fixed at the kit source: py-review Stop hook `prompt`→gated command (the planning-loop fix), py-init+ts-init recursive hook copy, pyproject ruff/mypy out-of-box guards, AGENTS.md domain-neutralize; #5 (CWD-coupling) deferred. DISCOVERY: the curator "gap" was a stale-installed-copy artifact (source was correct); T1 removing the last .md hook created a dangling `cp *.md` that T2 fixed. Reviewer SHIP (1 SHOULD-FIX fixed: stale generate-workflow.py refs). Process lesson: isolate review agents that run make/git (one reverted uncommitted settings.json). make test green, make eval 52/52, eval/ untouched.
- Phases 1-73: 73 completed (all delivery-accepted); Phase 73 — Cross-Session Substrate (thin handoff): no nana-dev-kit code — the pivot decision + state records + the external substrate bootstrap (`/Users/jwang/edge-screener` `5cceac6`)
- **Phase 73 outcome:** the amplifier program pivots from measurement to **substrate-construction** ([[cross-session-substrate-stock-screener]]). Cross-session persistence — the one regime the Phase 58–71 campaign never reached — gets a **real, external, unconfounded substrate**: a stock-screener project built with the real harness across many real sessions. Honest goal = an un-foolable backtest validator, not guaranteed alpha; build the ruler (data → engine+controls) before any edge search; "no edge" is a valid result. The cross-session measurement is DEFERRED until the substrate accrues real multi-session history (you can't measure recovery on session 1). First consuming-project dogfood surfaced concrete kit scaffold gaps → a Phase 74 engineering candidate (harden the consuming-project scaffold path). nana-dev-kit → active phase NONE.
- **Phase 72 outcome:** the first "cash the conclusion" harness right-sizing ([[cash-compaction-recovery-subtraction]]) — removed the dead `.session-anchor` recovery machinery (post-compact.sh read-branch + .gitignore entry) after a confirm-truly-dead repo grep. Subtraction over construction: P70/71 measured the recovery pathway headroom-free (native compaction summary is decision-comprehensive), so wiring up the missing writer would add machinery the campaign proved inert. Historical [[hook-reconciliation]] superseded NOT rewritten; gap 4.1 DEFERRED YAGNI. make test "All tests passed" at the unchanged script count (post-compact stays registered → no registration/settings/README drift), make eval 52/52, registration 41/41, settings + firing-coverage green. Jake-chosen (Engineering→Tight subtraction, AUQ 2026-05-30).
- **Phase 71 outcome:** built AND ran the **multi-turn analog of the Phase-70 anchor screen** ([[cross-boundary-retention-headroom-screen]]) — does the harness cross-compaction state machinery RECOVER a counter-default decision a bare agent (residual only) DROPS across a compaction boundary? Cloned the `anchor-screen/` spine into repo-only `eval/amplifier/retention-screen/` (`check.sh` `run_check`/`aggregate`/`verify_pins` byte-VERBATIM, only `diff_verdict` new — differential OFF×ON: DEGENERATE/HAS-HEADROOM/INERT[same-clause]/UNSTABLE; NO LLM in scoring); pre-registration `896e096` BEFORE runs (ancestor of verdict `d7067e6`). Controls validated the instrument (neg→DEGENERATE, **pos→HAS-HEADROOM** — bare model REFUSED to invent an absent sentinel, ON honored the in-context `[HARNESS STATE]` block ⇒ the ON pathway is LIVE, middle→STABLE). Then the deciding finding, across two transcript scales: Claude Code's NATIVE model-authored compaction summary RETAINS explicit decisions (a 356-word summary of a 1,213-word/~12-decision session retained ALL 3 candidates — motivated EUR 8,000 over the EUR 10,000 default, motivated rolling-7-day window, arbitrary `hit_flag` sentinel 9, all grep-verified — and bare opus HONORED them 5/5 in OFF) ⇒ **PROGRAM-VERDICT: TERMINATE-by-summary-robustness** (no candidate reached a lossy boundary; nothing for the harness state to recover in the single-compaction regime). The precise multi-turn analog of Phase 70's "the bare model does it unprompted," extended from reasoning to decision-RETENTION. DISCOVERY: a brittle pre-registered `forbid` fired on the correct answer's explanation → withdrawn INVALID (committed check left unchanged — no retrofit); + a dead `.session-anchor` read-branch in `post-compact.sh` (recorded, NOT fixed — freeze-the-subject). Surviving UNTESTED regimes (need a real multi-session substrate; NO rig authorized): cross-SESSION persistence, cumulative MULTI-compaction degradation, diffuse process discipline. make eval 52/52, make test green at the unchanged 19-script count; frozen cross-compaction machinery git-diff-empty.
- **Phase 70 outcome:** built AND ran the single-decision anchor-headroom screen — the cheapest decisive go/no-go for the amplifier program ([[amplifier-anchor-headroom-screen]]) — **inverting** the Phase-69 handoff (anchor selection is upstream of measurement; predicate-repair-first was structurally blocked on the degenerate OFF ≥ ON anchor). Deterministic consensus-by-clause `check.sh` (≥4/5 same clause-id, NO LLM in scoring), pre-registration committed `be96783` BEFORE runs (anti-retrofit `git merge-base --is-ancestor` holds vs `df45592`). Controls validated the instrument (neg→DEGENERATE, pos→HAS-HEADROOM, middle→STABLE); then **all 4 candidates screened DEGENERATE 5/5** (structuring, UBO, sanctions, engineered-favorable EU AMLR €10k cap) — opus does the AML reasoning unprompted; headroom appeared ONLY on the positive control's fictional fact ⇒ **PROGRAM-VERDICT: TERMINATE** (pre-registered ladder). Finding: single-decision harness headroom lives only in *unknown facts* (a retrieval problem; extends Phase-59). Surviving avenues (decidable-when): retrieval-on-proprietary + long-horizon/process-retention. NO Phase-71 single-decision live run; NO harness-value claim. make eval 52/52, make test green at the unchanged 19-script count; emitter + measurability-gate + eval/comparison|corpus|reasoning git-diff-empty. Delivery accepted, pushed.
- **Phase 69 outcome:** a read-only recon falsified the strategic handover's "run the live off/on experiment, declare the harness verdict" plan BEFORE any spend ([[amplifier-representativeness-audit]]) — the Phase-68 ruler over all 8 real transcripts gives `surfaced=false` ×8 / AUQ-scoped hits=0 (the v1 detector is non-representative: the decision surfaces in prose not AskUserQuestion) AND the same-day-close anchor is degenerate (handled unprompted even in the harness-OFF baseline ⇒ no headroom). Approach A (user-chosen): audit, no live run, no emitter patch. Shipped a re-runnable `measurability-gate.sh` (NOT-MEASURABLE now; the Phase-70 trigger) + `VALID-MEASUREMENT.md`, the frozen `real-transcript-survey.md`, the SCHEMA-NOTES line-50 resolution, and the honest apparatus disposition (ruler not yet a validated gate ⇒ comparison tombstoned, reasoning calibration-only, binary corpus sole gate). Deterministic/read-only/no-LLM; verdict vocabulary about the ANCHOR not harness value. make eval 52/52, make test green at the unchanged 19-script count. Reviewer 10/10.
- **Phase 68 outcome:** built **Frontier 0 of the amplifier vision — the deterministic READ-ONLY ruler** ([[amplifier-measurement-instrument]]) BEFORE any cognitive frontier (the strategic handover ranked the escalation-classifier highest, but it is unfalsifiable without an instrument). `eval/amplifier/emit-proxy-vector.sh` ingests a REAL transcript jsonl + final git/test state and emits a 4-group proxy-vector (mechanical/interaction/enforcement/ground_truth); control-validated via a surfaced/buried minimal pair that FLIPS the detector + ≥1 interaction proxy (`tests/test_amplifier_emitter.sh` 14/14). Substrate = transcript (not `enforcement.log`): closes the Phase-66 firing→action schema-gap + clears the Phase-65 corpus-duplication bar with real trace provenance. NO LLM judge, NO `verdict` field, NO harness claim (instrument-validation only; harness-measurement is Phase 69). New repo-only `eval/amplifier/` tree (8 files, NOT in install.sh). make test 18→19 / make eval 52/52.
- **Phase 67 outcome:** made the functional-smoke invariant ([[functional-smoke-invariant-rule]]; [[HEU-012]]) **machine-checkable** — a RED-first coverage meta-test ([[hook-firing-coverage-gate]]) over the UNION of `.hooks[]` (type-predicate) + `extra_dirs` curators, un-gameable via pinned exemption allow-list + denominator floor + negative control. The RED run surfaced the true gap = **10 untested hooks (not the roadmap's 3)**; all 10 now firing-tested (`test_long_cadence_hooks.sh`, `test_tooluse_hooks.sh`, memory-nudge in `test_harden.sh`). Closed 3 Phase-63 roadmap items with corrections. Tests-only → no registration/settings drift. make test 15→18 / make eval 52/52. Reviewer 9/10 accept (committed + pushed).
- **Phase 66 outcome:** the planned scorer is UNBUILDABLE — the signal-richness gate failed decisively (11 new-format records, 1 hook, 0 blocks, all self-traffic; 2 of 4 blockers structural: representativeness + schema-gap). Shipped the falsification AS A RUNNABLE PROBE ([[park-enforcement-scorer-signal-insufficient]]) and parked the scorer behind it. Redirected to the audit-log disposition → KEEP + harden ([[audit-log-disposition]]; the Phase-64 write-only⇒deadweight rule doesn't apply to a human-facing forensic trail). Reviewer 7/10 → 2 bugs fixed. make test 15 / make eval 52/52.
- **Phase 65 outcome:** built the non-blind eval's measurement SUBSTRATE (the Phase-63 verdict's proposal) — 6 lifecycle hooks emit fail-open `{schema_version,ts,hook,action,reason,phase}` firing records to `enforcement.log` — after an approach-review reversed a scored fixture-replay as corpus-duplication ([[instrument-not-score-enforcement-firing-substrate]]). The scorer is Phase 66 (gated on signal-richness). Executed the [[eval-validity-verdict]] disposition: comparison A-vs-C retired, reasoning demoted to calibration-only. The retrofit fixed 2 latent bugs in the existing loggers; enforcement.log untracked. Safety: all 4 blocking hooks proven to preserve exit 2 under log failure.
- **Phase 62 outcome:** the hot cache now enforces its own integrity invariants deterministically (cap/dedup/well-formedness/atomic-write), replacing fuzzy LLM-executed prose; dedup key corrected slug→proposition-text; policy consolidated to one source of truth. The one affirmative-evidence direction from Phase 61 ([[hot-cache-is-the-effective-retrieval-layer]], [[two-tier-curate-into-hot-cache]]) shipped. Governed by [[harden-hot-cache-curation-deterministic]], [[dedup-key-proposition-not-slug]], [[curator-fail-safe-atomic]], [[extend-wk-prune-not-new-hook]].
- **Phase 61 outcome:** experiment-first A/B → all 5 runtime-retrieval directions CUT; load-bearing positive = [[hot-cache-is-the-effective-retrieval-layer]]. Validates [[memory-architecture-classification]] (strengthen always-loaded `.claude/rules/` activation points) by measurement. Companion to Phase-59's [[cut-active-research-step-2-7]].
- **Roadmap: Phase 57+ Harness Activation — COMPLETE** (Fixes 1–5 all done across P55-60). No residual harness-activation items.
- Next substantive roadmap items: **Phase 63 candidate: hot-cache eviction value-signal** (usage counter empirically inert ⇒ cap-eviction is de-facto recency; decide a real value signal vs. accepting recency explicitly) + distillation QUALITY (what gets written into the cache — needs a non-binary eval method first). Also standing: vector-search-default-on design call; gap 4.1 language-agnostic core (factor py-* out); always-loaded-budget claim research (AGENTS.md:84); repo-hygiene: gitignore `.memory/*.db*` (runtime churn pollutes phase diffs).
- Maintenance (post-Phase-58, commit `74da87a`): memory venv fix — `make test` green end-to-end; [[guard-optional-dep-tests]] (optional-dep tests skip, never assume-and-halt).
- Spec: Phase 55 used USER OVERRIDE (experiment data replaced prescriptive spec)
- Roadmap: [[roadmap-gap-analysis|Engineering Gap Analysis]] -- 1 OPEN gap remains (4.1 language-agnostic core, DEFERRED YAGNI). The dead `.session-anchor` read-branch (recorded Phase 71) was REMOVED in Phase 72 ([[cash-compaction-recovery-subtraction]]).
- Roadmap: [[roadmap-cognitive-enhancement|Cognitive Enhancement Roadmap]] -- 7/7 phases done (all complete)
- Release: v0.5.0 tagged and pushed
- Benchmark: FTS5 recall@5 91.0%, hybrid/turn ~95% estimated (LongMemEval-S)
- Knowledge wiki: wiki/ (cognitive-patterns) -- 11 heuristics + 5 IRON RULES + 4 domain pattern articles, heuristic judge + matcher wired at Step 6.5, counter + lifecycle evolution loop
- Retro: Phases 51-55 clean (0 recurring blockers, 0 reversals, 1 documented user override)
