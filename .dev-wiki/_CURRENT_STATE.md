# Current State: nana-dev-kit

> Last updated: 2026-05-30 by /dev-plan (Phase 70 planned — Single-Decision Anchor-Headroom Screen; direction approved, spec written, 5 tasks, beginning implementation at T1)

## Recommended Next Action

**Implement Phase 70, beginning at T1.** Phase 69 is accepted (committed + pushed; `measurability-gate.sh` = NOT-MEASURABLE). Phase 70 = a single-decision **anchor-headroom screen** — the cheapest decisive go/no-go for the whole amplifier program after five mostly-negative phases. It inverts the Phase-69 handoff order: anchor selection is upstream of measurement, and predicate-repair-first is structurally blocked on the current degenerate anchor (it surfaces in OFF ≥ ON ⇒ any boundary repair collapses to raw-text). The screen asks: does ANY non-commodity single-decision pure-reasoning anchor have real OFF-baseline headroom (the harness-OFF bare subagent OMITS the correct behavior unprompted)? **T1 (FIRST/checkpoint)** builds the deterministic apparatus + pre-registration (committed BEFORE runs); **T2 (load-bearing checkpoint)** runs the 3 controls (any misbehavior ⇒ STOP, instrument broken); **T3** screens ≥3 candidates; **T4** writes the graded `PROGRAM-VERDICT` (CONTINUE→P71 / PARKED / TERMINATE); **T5** regression. NO harness-ON run. Direction approved 2026-05-30 (Approach A via AskUserQuestion). See [[amplifier-anchor-headroom-screen]].

## Active Phase

**[[phase-70-anchor-headroom-screen|Phase 70: Single-Decision Anchor-Headroom Screen]]** (status: active — planned 2026-05-30, beginning implementation)

Entry criteria: MET (Phase 69 delivered — `measurability-gate.sh` = NOT-MEASURABLE; anchor degenerate + detector blind. Anchor selection is upstream of measurement, so the next plannable step is screening for anchor existence — not the gated live run, not predicate-repair-first).
Exit criteria: see contract below + the phase article. (Direction gate ✓ 2026-05-30; delivery gate pending.)

Build AND RUN the cheapest decisive go/no-go for the amplifier-measurement program: screen anchor EXISTENCE — does ANY non-commodity single-decision **pure-reasoning** anchor have real OFF-baseline headroom (the harness-OFF bare subagent OMITS the correct behavior unprompted, so the harness could plausibly help)? Controls-first; all five adversarial-review fixes adopted (the un-fixed design scored 4/10): **(1)** deterministic consensus criterion (named-clause check, HAS-HEADROOM iff FAILS ≥4/5 same clause-id) NOT a judge; **(2)** pure-reasoning anchors only (bare-subagent ≠ harness-off for tool-users); **(3)** drop long-horizon/retention (one-shot subagent can't surface it); **(4)** three controls run FIRST (neg⇒DEGENERATE, pos⇒HAS-HEADROOM, middle⇒UNSTABLE-reproduced; misbehavior⇒STOP); **(5)** graded verdict ladder from pre-registered-before-runs candidates. **NO harness-ON run.** Tooling/docs only under `eval/amplifier/anchor-screen/**` + `specs/` + `.dev-wiki/articles/**`; deterministic; NO harness-value claim (HAS-HEADROOM = lift POSSIBLE never lift exists); make eval frozen at 52, make test green at the unchanged 19 scripts.

Progress: 0% (0/5 tasks, beginning T1). Direction confirmed 2026-05-30 (Approach A via AskUserQuestion); spec `specs/phase-70-anchor-headroom-screen.md` written + reviewed 7/10→fixed. See [[amplifier-anchor-headroom-screen]].

## Active Phase Contract

Phase: 70 - Single-Decision Anchor-Headroom Screen
Tasks: 5 (T1 [M, FIRST/checkpoint] pre-registration + deterministic check apparatus (check.sh --selftest classifies planted PASS/FAIL/HEDGED + --verify-pins; leak-check.sh + leak-vocab.txt; pre-registration.md = 3 controls + ≥3 candidates, each base-model + shasum-pinned OFF prompt + named-clause check) — COMMITTED before runs → T2 [M, load-bearing checkpoint] controls-first run, n=5 bare clean-room subagents per control, neg=DEGENERATE ∧ pos=HAS-HEADROOM ∧ middle=UNSTABLE-reproduced(STABLE) else STOP+INSTRUMENT-BROKEN → T3 [M] screen ≥3 candidates n=5 each, quarantine tool-gap(DISQUALIFIED)/prompt-sensitive(UNSTABLE) → T4 [S] graded PROGRAM-VERDICT (CONTINUE/PARKED/TERMINATE) + decision article + handoff → T5 [S, LAST] regression gate + scope-honesty)
Transition: continue (begin T1)
Abort: if any control misbehaves (negative shows headroom / positive shows degenerate / middle unstable across runs+blind-re-run) → STOP + record FINDING: INSTRUMENT-BROKEN (do NOT screen candidates on a broken instrument). If no deterministic check is writable even for the controls → STOP (method infeasible — a finding). If make eval drifts off 52 OR make test regresses OR a candidate needs the emitter/gate patched to screen → STOP. Before any "termination" language → confirm the engineered-favorable also DEGENERATE, else PARKED. If blocked >3 attempts, mark [blocked:], report, ask skip-or-abort. T1 is FIRST/checkpoint (must COMMIT before T2 — pre-registration precedes runs); T2 is the load-bearing controls checkpoint.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| [[amplifier-anchor-headroom-screen]] (Phase 70) The Phase-69 handoff order is wrong — predicate-repair-first is structurally blocked (the current degenerate anchor surfaces in OFF ≥ ON ⇒ any boundary repair collapses to raw-text). Anchor selection is upstream of measurement, so Phase 70 screens anchor EXISTENCE first: does ANY non-commodity single-decision pure-reasoning anchor have real OFF-baseline headroom? Also the cheapest go/no-go for the whole amplifier program (5 negative phases). User chose Approach A (AskUserQuestion 2026-05-30). Controls-first, all 5 adversarial-review fixes adopted (4/10→fixed): deterministic consensus criterion (not a judge), pure-reasoning only, drop long-horizon, three controls run first, graded pre-registered verdict ladder (CONTINUE/PARKED/TERMINATE). NO harness-ON run; NO harness-value claim. | high | 2026-05-30 |
| [[amplifier-representativeness-audit]] (Phase 69) A read-only recon ran the Phase-68 ruler over all 8 real consuming-project transcripts and FALSIFIED the handover's live-run plan: 8/8 `surfaced=false`, AUQ-scoped hits=0 (decision surfaces in prose not AskUserQuestion — v1 predicate is a structural false-negative), anchor DEGENERATE (look-ahead handled unprompted even in the harness-OFF baseline ⇒ no headroom). User chose Approach A (AskUserQuestion): turn the recon into the deliverable — NO live run, audit-only, do NOT patch the emitter. Ship a re-runnable `measurability-gate.sh` (NOT-MEASURABLE now; the Phase-70 trigger) + honest apparatus disposition (ruler is NOT yet a validated gate ⇒ comparison stays tombstoned, reasoning calibration-only, binary corpus sole gate). | high | 2026-05-29 |
| [[amplifier-measurement-instrument]] (Phase 68) Build Frontier 0 (the measurement instrument / ruler) BEFORE any cognitive frontier — the handover's dependency order was backwards and the escalation-classifier (Frontier 1) is unfalsifiable without a ruler. A deterministic READ-ONLY emitter at `eval/amplifier/emit-proxy-vector.*` ingests a REAL transcript jsonl (substrate, NOT enforcement.log — closes the Phase-66 schema-gap + clears the Phase-65 corpus-duplication bar) and emits a 4-group proxy-vector; validated by a positive/negative CONTROL pair (differential-flip), NO LLM judge (PARK if it can't pass deterministically). Instrument-validation (Phase 68, planted ground truth, n=1) is separated from harness-measurement (Phase 69, real consuming-project, n>1). | high | 2026-05-29 |
| [[hook-firing-coverage-gate]] (Phase 67) Make the functional-smoke invariant machine-checkable — a RED-first meta-test enforcing coverage over the UNION of `.hooks[]` (predicate `(.type//"command")!="prompt"`, NOT literal `command` which matches zero) + `extra_dirs/*.sh` curators; un-gameable via pinned exemption allow-list + denominator floor + negative control; invocation-anchored coverage (bare grep is empirically useless). Fix the GENERATOR, not the 3 known holes. Tests only. | high | 2026-05-29 |
| [[park-enforcement-scorer-signal-insufficient]] (Phase 66) The signal-richness gate FAILED (11 new-format records, 1 hook, 0 blocks, all self-traffic) — park the scorer behind a runnable probe-trigger, NOT a calendar note; 2 of 4 blockers are structural (representativeness: kit's own log ≠ consuming-project work; schema-gap: record = decision, not agent action). Redirect to the audit-log subtraction-test disposition. NO scorer code. | high | 2026-05-29 |
| [[audit-log-disposition]] (Phase 66) KEEP + harden audit-log — it functions + is eval-covered; `.nana/audit.jsonl` is a human-facing forensic trail (which model edited which file when), NOT code-dead deadweight; hardened raw-printf `$FILE_PATH`→`jq --arg` (fixed latent JSONL-injection) + added the functional-smoke the eval lacked. | high | 2026-05-29 |
| [[instrument-not-score-enforcement-firing-substrate]] (Phase 65) Build the enforcement-firing SUBSTRATE, not a scorer — a scored fixture-replay duplicates the corpus `lifecycle` category; the missing ingredient is real trace provenance, so instrument the 6 lifecycle hooks (fail-open) and defer the scorer. | high | 2026-05-29 |
| [[cut-heuristic-scoring-keep-articles]] Cut the never-fired scoring MACHINERY (matcher/judge/counter/lifecycle/dashboard + dev-plan Step 13 sub-items 6-7 + dev-debrief Step 7 heuristic-capture); KEEP the 17 articles + SCHEMA.md (test fixtures + eval/reasoning corpus). | high | 2026-05-29 |

## Blockers and Open Questions
- [planning] [Phase-69] Whether any AUQ event in the real transcripts contains a PARAPHRASE of the look-ahead decision (point-in-time / future-leak) the literal detector misses is resolved at T1 (spot-check). If a paraphrase IS present in an escalation, reframe the finding to "detector phrase-list too narrow" (still no emitter patch this phase — predicate repair is gated Phase-70 work). (raised 2026-05-29)
- [Phase-70 ADDRESSES the anchor-existence item] Of the Phase-69 deferred set, Phase 70 now executes the **anchor-headroom screen** — "a valid non-commodity anchor passing the OFF-baseline headroom screen" — as the upstream go/no-go. The REMAINING items stay DEFERRED + GATED (predicate repair without raw-text collapse, the live off/on run n>1, Frontier 1): predicate-repair + live-run are unblocked only by a Phase-70 `PROGRAM-VERDICT: CONTINUE` (≥1 natural anchor HAS-HEADROOM) AND then `measurability-gate.sh` flipping MEASURABLE. A `PARKED`/`TERMINATE` verdict re-gates them on new priors / a multi-turn substrate (the deferred long-horizon class). (raised 2026-05-29, reframed 2026-05-30)
- RESOLVED (Phase 68): the 3 Phase-68 planning questions — exact transcript schema (T1 GO, no logging-hook pivot), redirect proxy computability (shipped as a structural `unsolicited_human_turns` proxy), deterministic keyword detector sufficiency (passed its planted control). The Phase-69 recon then surfaced that the detector, while correct on planted fixtures, is non-representative on REAL data — now the Phase-69 work. (resolved 2026-05-29)
- [Phase-66 scorer PARKED] The signal-richness gate was checked 2026-05-29 and returned **NOT-SCOREABLE**: only 11 new-format (`schema_version=1`) records in `enforcement.log` — all from one hook (dev-wiki-scope-check), ZERO action-changing `block` firings, all `phase:"65"` intra-phase self-traffic (the 253 enforce-loop + 23 block records are pre-instrumentation legacy format). Unbuildable for 4 reasons, **2 structural**: (1) volume; (2) skew; (3) **representativeness** — the log lives in the kit's OWN `.dev-wiki`, sampling the maintainer editing the kit, never the consuming-project agentic work enforcement governs; (4) **schema-gap** — the record captures the hook's DECISION, not the agent's subsequent ACTION (no firing→response link). `decidable-when`: re-run `scripts/signal-richness-probe.sh` (the committed gate) → SCOREABLE, AND the two structural blockers resolved first (real consuming-project provenance + a record schema that captures agent-response). Not a calendar note — a runnable predicate. See [[park-enforcement-scorer-signal-insufficient]]. (parked 2026-05-29)
- RESOLVED (Phase 64): the 2 Phase-64 design Qs (which working-knowledge entries describe removed-machinery-as-operational; whether Memory Harvest shares counter-write code with Heuristic Capture) — both resolved during Phase 64 execution (semantic prune via curator dogfood; Memory Harvest confirmed separate). (resolved 2026-05-29)
- RESOLVED (Phase 63): cut-vs-fix for the never-fired cognitive system → CUT the scoring machinery (DEADWEIGHT: 18 counters at 0/0, git -S empty ~13 phases), keep the 17 articles as fixtures. Executed in Phase 64. (resolved 2026-05-29)

- ~~Phase 58 residual research delta is +0.5 composite at n=1 (at significance threshold, topic-favorable). Kept Step 2.7 (Checkpoint 2). Phase 59 resolves the keep/trim/cut call.~~ **RESOLVED 2026-05-28 → CUT.** Strengthened with 3 new topics (13 paired runs at n≥3): poor −1.0 real harm, rich 0.0/−0.4 variance-dominated; the +0.5 sits inside the noise band. Feature removed; dev-plan reverts to Phase-55 behavior. (resolved 2026-05-28)

- OPEN: Active research's value on genuinely novel / post-training-cutoff / proprietary topics (weak parametric knowledge — research's theoretical sweet spot) is UNTESTED. Only well-documented domains were measured. A deliberate keep-for-novel-topics-only is a separate user call. (raised 2026-05-28) — Phase 61 reconfirmed: the weak-parametric + properly-absorbed + covered sweet spot remains unmeasured (would need an absorb pipeline + a non-commodity corpus first).

- OPEN: D2 re-test trigger (deferred, concrete numeric trigger not a present feature): if the MCP memory store ever grows past the hot-cache 100-entry cap with valuable DISTINCT entries, re-run the D2 A/B (memory_search as overflow recall for the evicted tail). As it stands the store (20 entries) is a strict subset of the hot cache. (raised 2026-05-29)

- Haiku/judge inter-run variance: mean ranges 2.97-4.85; recurred in Phase 59 (rich-topic spread 0.79–1.19, both variance-dominated at n=5). Cross-model judge / judge re-calibration remains a standing lever (deferred). (raised 2026-05-27, reconfirmed 2026-05-28)
- ~~Memory venv broken (recurring Phases 56-58): `make test` halted at `test_memory.sh`.~~ **RESOLVED 2026-05-28.** Root cause was twofold: (1) the optional `sqlite-vec` dep was absent from the (healthy, uv-built, Py3.13) venv — the `libpython3.11.dylib` symptom was stale from an older venv; (2) `test_memory.sh` forced `_vec_available=True` and hard-crashed instead of skipping when the *optional* dep was missing. Fixed both: installed `sqlite-vec==0.1.9` locally (full 11/11 run), AND guarded the 4 vec-requiring tests behind a one-time probe so they skip cleanly (FTS5-only 7/7) when sqlite-vec is absent — `make test` can no longer halt on a missing optional dep. (resolved 2026-05-28)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| `eval/amplifier/survey-real-transcripts.sh` + `real-transcript-survey.md` | NEW (Phase 69) — read-only survey of the ruler over the pinned 8 real transcripts; `--selfcheck` RUNS the ruler on planted `surfaced.jsonl` (positive control), shasum-proves read-only, reproduces 8/8 `surfaced=false`. Frozen record: separate escalation/in-boundary/raw/parse_errors columns + sourced OFF/ON provenance + paraphrase spot-check. Repo-only probe (NOT a make-test gate) | 2026-05-30 |
| `eval/amplifier/measurability-gate.sh` + `VALID-MEASUREMENT.md` | NEW (Phase 69) — **the Phase-70 trigger**: deterministic MEASURABLE/NOT-MEASURABLE/NO-DATA predicate (≥2-ON ∧ ≥2-OFF in-boundary events ∧ OFF-vs-ON differential; planted-fixture shasums excluded; threshold read from the doc). Prints **NOT-MEASURABLE** now; `--selftest` falsifiable both ways. Doc: operational degeneracy/headroom criterion + dual-failure pre-mortem + Phase-70 experiment + no-harness-value disclaimer. Reviewer 10/10 | 2026-05-30 |
| `eval/amplifier/emit-proxy-vector.sh` | NEW (Phase 68) — **the ruler** (Frontier 0): deterministic READ-ONLY emitter (bash+jq). Ingests a REAL transcript jsonl + final git/test state → 4-group proxy-vector (mechanical/interaction/enforcement/ground_truth). Absent enforcement.log ⇒ `null` sentinel (not 0); corrupt line ⇒ `parse_errors`; NO `verdict` field, NO LLM. Repo-only (NOT in install.sh) | 2026-05-29 |
| `tests/test_amplifier_emitter.sh` + `eval/amplifier/{SCHEMA-NOTES.md,schema-probe.sh,fixtures/}` | NEW (Phase 68) — 14 assertions (control pair FLIPS detector + ≥1 interaction proxy, phrase-outside-escalation=false, sentinel, parse_errors, read-only shasum-proven). schema-probe = T1 feasibility gate (GO on real transcript); SCHEMA-NOTES = frozen schema + pinned predicates; 5 control fixtures. 19th make-test script | 2026-05-29 |
| `tests/test_hook_firing_coverage.sh` | NEW (Phase 67) — the **coverage gate**: RED-first meta-test making the functional-smoke invariant machine-checkable. Denominator = union of `jq '.hooks[]\|select((.type//"command")!="prompt")\|.script'` + every `extra_dirs/*.sh` curator; invocation-anchored coverage via `# fires:` declarations (NOT bare grep); un-gameable (pinned exemption allow-list by membership+size + denominator floor + permanent negative control). RED run surfaced the true 10-hook gap | 2026-05-29 |
| `tests/test_long_cadence_hooks.sh` + `tests/test_tooluse_hooks.sh` | NEW (Phase 67) — side-effect-asserting firing tests in mktemp-d + HOME-override isolation. long-cadence: pre/post-compact, session-stop, check-tests allow/skip (8 tests); tooluse: scan-secrets, block-dangerous-bash, auto-ruff-format, stale-queue, post-commit, context-size-check (12 tests). 16th + 17th make-test scripts | 2026-05-29 |
| `scripts/signal-richness-probe.sh` + `tests/test_signal_richness_probe.sh` | NEW (Phase 66) — read-only deterministic gate: "is the enforcement-firing scorer buildable yet?". Classifies records (new/legacy/debrief/malformed), predicate ≥2 hooks ∧ ≥1 block over schema_version-bearing records after tuple-dedup → SCOREABLE/NOT-SCOREABLE/NO-DATA/CORRUPT. The committed park-trigger; 14 tests. Live log → NOT-SCOREABLE | 2026-05-29 |
| `templates/.claude/hooks/audit-log.sh` + `tests/test_audit_log.sh` | Phase 66 — KEEP disposition: hardened raw-`printf $FILE_PATH` → `jq --arg` (fixed latent JSONL-injection; mkdir fail-open) + NEW 6-test functional-smoke (record shape + injection-safety + fail-open) the eval lacked; 15th make-test script | 2026-05-29 |
| `templates/.claude/hooks/{enforce-spec,enforce-loop,enforce-memory,dev-wiki-scope-check,detect-loop,check-tests-were-run}.sh` | Phase 65 — hardened inline-duplicated `log_firing` (fail-open, exit-neutral under set -e, jq --arg controlled-vocab, atomic >>) → `.dev-wiki/enforcement.log`; retrofit of the 3 existing loggers fixed their JSON-injection + truncation-race bugs | 2026-05-29 |
| `tests/test_firing_log.sh` | NEW (Phase 65) — 21 tests: per-hook well-formed record, fail-open (chmod-000 + stubbed-date) preserving exit 2 on all 4 blocking hooks, injection/exfiltration-safe, static 6-copy byte-equality; 13th make-test script | 2026-05-29 |
| `eval/comparison/methodology.md` | Phase 65 — RETIRED tombstone (A-vs-C confounded apparatus removed; A-vs-B clean note kept). `eval/reasoning/README.md` demoted to calibration-only/never-a-gate. | 2026-05-29 |
| `templates/.claude/hooks/session-start.d/wk-prune.sh` | Phase 62 — extended into the single deterministic hot-cache curator: cap-enforce + exact-proposition dedup + well-formedness whole-file bail + atomic validate-temp→rename (bash-3.2 wrapper + inline python3) | 2026-05-29 |
| `tests/test_working_knowledge_curation.sh` | NEW (Phase 62 T1) — 11 invariant tests (over-cap cull / pinned-immune / exact-dup keeps max uses / distinct-same-slug NOT collapsed / malformed no-op / exactly-100 no-op / idempotent / >30d prune); 13th make-test script | 2026-05-29 |
| `templates/.claude/skills/dev-wiki/working-knowledge-spec.md` | Phase 62 — single source of truth for the cap/dedup/eviction policy; dedup key fixed slug→proposition-text; 3 other touchpoints now reference it | 2026-05-29 |
| `eval/memory-integration/results.md` | Phase 61 A/B record: all 5 runtime-retrieval directions CUT; hot-cache meta-finding + Phase-62 build list | 2026-05-29 |
| `templates/.claude/skills/dev-plan/SKILL.md` | Steps renumbered whole-number gap-free 1..18 (P61 T6); Step 16f-ter now references working-knowledge-spec.md (P62) | 2026-05-29 |
| `modules.json` | Single canonical scope-tagged `hooks` array (17 project + 1 global) — hook source of truth | 2026-05-28 |
| `tests/test_memory.sh` | Vec-requiring tests probe sqlite-vec once + SKIP cleanly (FTS5-only) when absent | 2026-05-28 |

## Session Journal (last 5)

- [2026-05-30] [[2026-05-30-phase-69-amplifier-representativeness-audit-complete|Phase 69 complete — Amplifier Representativeness Audit + Anchor-Validity Verdict (the cheap probe falsified the expensive plan)]] -- the strategic handover framed Phase 69 as "use the Phase-68 ruler, run the live off/on experiment, declare the harness verdict." A **read-only recon at the start of planning falsified that plan before any spend** — and the falsification BECAME the phase. The ruler over all 8 real consuming-project transcripts: **`ground_truth.surfaced=false` on all 8, AUQ-scoped phrase hits=0 on all 8** (the look-ahead decision surfaces in prose/code/eval-frameworks 8–50×, never inside an AskUserQuestion — the v1 predicate is a structural false-negative on real provenance); a paraphrase spot-check of every AUQ event that DID occur (project scoping — phases, tech-stack, horizons, targets) confirmed the strong form: the decision is never escalated, even paraphrased. And the anchor is **degenerate** — look-ahead bias appears even in the harness-OFF baseline (base model handles it unprompted ⇒ no headroom; recurs the Phase-59 commodity-knowledge lesson). User chose **Approach A** at the direction gate (AskUserQuestion): turn the recon into the deliverable — NO live run, audit-only (no emitter patch). **T1** froze the evidence: `survey-real-transcripts.sh` (read-only, shasum-pinned, `--selfcheck` RUNS the ruler on planted `surfaced.jsonl` as a positive control ⇒ 8/8-false is a DATA property not a dead branch) + `real-transcript-survey.md` (separate escalation/in-boundary/raw/parse_errors columns + sourced OFF/ON provenance). **T2** shipped `measurability-gate.sh` — the runnable Phase-70 trigger (Phase-66 probe idiom): NOT-MEASURABLE now, falsifiable BOTH ways (`--selftest` proves a MEASURABLE + a NOT-MEASURABLE scenario), ≥2-ON ∧ ≥2-OFF threshold read from the doc, planted-fixture shasums excluded (content-addressed) — + `VALID-MEASUREMENT.md` (operational degeneracy/headroom criterion + dual-failure pre-mortem + Phase-70 experiment + no-harness-value disclaimer). **T3** resolved SCHEMA-NOTES line-50 + the **honest** apparatus disposition (ruler is NOT yet a validated gate ⇒ comparison stays tombstoned, reasoning calibration-only, binary corpus SOLE gate — no overclaim). **T4** regression: make eval 52/52, make test green at the UNCHANGED 19-script count (probes self-test, not make-test gates → no README bump), all Phase-68 deliverables git-diff-empty, no-LLM clean, disclaimer present. New repo-only `eval/amplifier/` files (survey + gate + 2 docs); emitter + eval/comparison|corpus|reasoning untouched. Reviewer **10/10 accept** (adversarially verified the gate logic empirically: false-GREEN paths closed, planted-shasum exclusion content-addressed, no permanently-RED path, bash-3.2 portable; 2 non-blocking polish suggestions applied). 4/4 tasks ✓, **READY FOR COMPLETION, delivery gate pending**. Next: accept → /dev-plan Phase 70 (gated on the measurability-gate flipping MEASURABLE).
- [2026-05-29] [[2026-05-29-phase-68-amplifier-measurement-instrument-complete|Phase 68 complete — Amplifier Measurement Instrument (Frontier 0, the control-validated ruler)]] -- built **Frontier 0 of the amplifier vision — the deterministic READ-ONLY "ruler"** — BEFORE any cognitive frontier, correcting the strategic handover (which ranked the escalation-classifier / Frontier 1 highest but it is **unfalsifiable without an instrument**; the dependency order was backwards). **T1 (BEHAVIORAL checkpoint, first):** `eval/amplifier/schema-probe.sh` PASSES on a real session transcript — the jsonl distinguishes human turns, AskUserQuestion/escalation events, and tool-uses by structure → **GO, no logging-hook pivot**; `SCHEMA-NOTES.md` froze the proxy→field table + pinned predicates (escalation = AskUserQuestion-only for v1). **T2:** froze the 4-group proxy-vector schema (mechanical / interaction / enforcement / ground_truth) + 5 control fixtures (surfaced/buried **minimal pair** + buried-outside-escalation + corrupt-line + enforcement-with-blocks.log). **T3:** the emitter `eval/amplifier/emit-proxy-vector.sh` (bash+jq, deterministic, read-only) built TDD against `tests/test_amplifier_emitter.sh` → **14/14** — the control pair FLIPS the detector + ≥1 interaction proxy (the rm-rf-guard-probe analog), phrase-outside-escalation reads `false`, absent enforcement.log ⇒ `null` sentinel (NOT silent 0), corrupt line ⇒ `parse_errors`, read-only shasum-proven, real transcript clean. **T4:** wired into `make test` + scope-honesty guards (no-LLM sweep clean, NO `verdict` field — the instrument makes no harness claim). **T5:** regression gate — `make test` 0 FAIL (18→19 scripts), `make eval` 52/52 unchanged, `eval/comparison`+`eval/corpus` untouched, Phase-69 handoff recorded. The substrate choice (transcript, not enforcement.log) closes the parked Phase-66 firing→action schema-gap AND clears the Phase-65 corpus-duplication bar with real trace provenance. NEW repo-only `eval/amplifier/` tree (8 files; NOT in install.sh). DISCOVERY: the `test_templates.sh` README script-count drift-guard required a manual README bump 18→19 (no auto-sync). Instrument-validation (here, planted ground truth, n=1) deliberately separated from harness-measurement (Phase 69, real consuming-project, n>1) — **NO harness verdict claimed**. 5/5 tasks ✓, **READY FOR COMPLETION, delivery gate pending**. Next: accept → /dev-plan Phase 69 (use the ruler for the live off/on harness measurement).
- [2026-05-29] [[2026-05-29-phase-67-hook-firing-coverage-gate-complete|Phase 67 complete — Long-Cadence Hook Firing Tests + Hook Firing-Coverage Gate]] -- made the **functional-smoke invariant machine-checkable** (held only in PROSE for ~26 phases — the same unguarded-convention class that eroded the session-start.sh line-cap 70→137 + caused 4 silent breakages). **T1** built the gate RED-first (`tests/test_hook_firing_coverage.sh`): denominator = UNION of `jq '.hooks[]|select((.type//"command")!="prompt")|.script'` + every `extra_dirs/*.sh` curator (curators register via extra_dirs, not .hooks[]); un-gameable (pinned exemption allow-list by membership+size + denominator-sanity floor + permanent negative control); coverage invocation-anchored via `# fires:` declarations (bare grep proven useless across 3 probes). **The RED run was the headline: the true gap was 10 hooks, not the roadmap's 3** — pre/post-compact, session-stop, scan-secrets, block-dangerous-bash, auto-ruff-format, stale-queue, post-commit, context-size-check, memory-nudge. **T2** filled it GREEN — `test_long_cadence_hooks.sh` (8) + `test_tooluse_hooks.sh` (12) + memory-nudge direct-fire in `test_harden.sh` + 6 `# fires:` retrofits; all 10 now side-effect-asserting in mktemp-d + HOME-override isolation. **T3** sandbox `--project-local` curator-chain regression in `test_install.sh` (fires the copied session-start.sh, asserts the session-start.d chain executes end-to-end at the destination). **T4** full gate + roadmap reconcile: 3 Phase-63 items CLOSED with corrections (gap was 3 not 4; `.venv` already clean; install plumbing already correct → made durable as the chain-fire test). Tests-only, NO new hooks → `test_registration` 41/41 + `test_settings_template` clean (untouched, confirms no drift). `make test` 15→18 scripts (~390→~420 tests) / `make eval` 52/52. Reviewer **9/10 accept**, adversarially confirmed the gate fails closed on a collapsed-classifier bug AND on an uncovered hook. Durable lessons harvested: coverage-gate-as-machine-checkable-invariant (fix the GENERATOR, not the symptom holes); memory-nudge `timeout` portability gap (absent on stock macOS). 4/4 tasks ✓, **delivery pending acceptance**. Next: /dev-plan Phase 68 (a remaining Phase-63 roadmap item).
- [2026-05-29] [[2026-05-29-phase-66-signal-richness-park-audit-complete|Phase 66 complete — Signal-Richness Falsification + Scorer Park + audit-log Disposition]] -- planned AND implemented in one session. The phase invoked to "build the scorer (gated on signal-richness)" **resolved at the falsification checkpoint, not the build**. The gate FAILED decisively: only 11 new-format (schema_version=1) records in enforcement.log, all ONE hook (dev-wiki-scope-check), ZERO block firings, all phase:"65" self-traffic; the 253 enforce-loop + 23 block records are pre-instrumentation LEGACY format. Unbuildable for 4 reasons, **2 STRUCTURAL** (time won't fix): representativeness (the log lives in the kit's OWN .dev-wiki, sampling the maintainer editing the kit, never consuming-project agentic work) + schema-gap (record = hook DECISION, not agent ACTION). So **falsify + park + redirect** (user-approved via a 3-way AskUserQuestion fork at the direction gate): (1) shipped `scripts/signal-richness-probe.sh` — a read-only deterministic gate that re-checks "buildable yet?" in one command (classify new/legacy/debrief/malformed; predicate ≥2 hooks ∧ ≥1 block over schema_version-bearing records after tuple-dedup; SCOREABLE/NOT-SCOREABLE/NO-DATA/CORRUPT); (2) parked the scorer behind that RUNNABLE trigger + the 4-reason verdict (not a calendar note); (3) redirected to the long-open **audit-log disposition → KEEP + harden** (it functions + is eval-covered; .nana/audit.jsonl is a human-facing forensic trail, NOT code-dead — the Phase-64 "write-only⇒deadweight" rule doesn't apply since those counters fed a *dead automated* reader; while keeping, hardened the lone raw-printf $FILE_PATH interpolation to jq --arg, fixing a latent JSONL-injection, + added a functional-smoke the eval lacked). Reviewer **7/10 SHIP-WITH-FIXES** → 2 real bugs fixed + regression-tested (probe crash on a non-object JSON line; audit-log mkdir fail-open violation). make test 13→15 scripts green / make eval 52/52 / test_registration 41/41 / DRIFT none. Net additive, NO scorer code (a scored fixture-replay = corpus-duplication, settled Phase 65). Durable lessons harvested: park-trigger-as-runnable-probe; write-only≠deadweight for human-facing forensic logs; SIGPIPE-under-pipefail in `cmd|grep -q` tests. 4/4 tasks ✓, **delivery pending acceptance**. Next: /dev-plan Phase 67 (a remaining Phase-63 roadmap item).
- [2026-05-29] [[2026-05-29-phase-65-enforcement-trace-instrumentation-complete|Phase 65 complete — Enforcement-Trace Instrumentation + Eval-Apparatus Disposition]] -- planned AND implemented in one session. Built the measurement SUBSTRATE the Phase-63 verdict proposed (NOT a scorer — that's Phase 66). **The build reversed at the direction gate:** an adversarial approach-review killed a scored fixture-replay as **corpus-duplication** (the corpus `lifecycle` category already replays event sequences + toggles the enforce marker; action-delta is weaker than the per-event exit-code it pins) — the distinguishing ingredient of a real-agentic eval is trace PROVENANCE, not fixtures. So: one hardened **fail-open** `log_firing` across 6 lifecycle hooks → `enforcement.log` (retrofit of the 3 existing loggers FIXED their latent JSON-injection + racy `tail-500` truncation); untracked the log (was git-tracked → cp-r churn/leak) + appended ×4 py-init/ts-init sites. Disposition: retired `eval/comparison` A-vs-C to a tombstone, demoted `eval/reasoning` to calibration-only, deleted 2 orphaned self-dialogue dirs. **Safety proof (the headline):** all 4 blocking hooks preserve `exit 2` under both unwritable-log AND failing-`date` — a logging failure cannot turn a block into an allow. 3 planning reviews (approach 4/10→reversed, adversarial-constraints, spec Tier-1 7/10→fixed, plan-review 7/10→fixed) + post-impl code review **9/10 SHIP** (2 findings incorporated: arity-defensive guard + static 6-copy byte-equality test). Net −1,134 lines. make test 13 green / make eval 52/52 / test_firing_log 21/21 / test_registration 41/41. Durable lessons (memory-harvested): fixture-replay ≡ corpus; safe fail-open hook instrumentation; inline-snippet beats sourced-lib for this project's copy-miss failure profile. 4/4 tasks ✓, **delivery pending acceptance**. Next: /dev-plan Phase 66 (build the scorer, gated on signal-richness).
## Cross-References

- Phases 1-68: 68 completed; Phase 69 = implementation complete, delivery gate pending (see index.md)
- **Phase 69 outcome:** a read-only recon falsified the strategic handover's "run the live off/on experiment, declare the harness verdict" plan BEFORE any spend ([[amplifier-representativeness-audit]]) — the Phase-68 ruler over all 8 real transcripts gives `surfaced=false` ×8 / AUQ-scoped hits=0 (the v1 detector is non-representative: the decision surfaces in prose not AskUserQuestion) AND the same-day-close anchor is degenerate (handled unprompted even in the harness-OFF baseline ⇒ no headroom). Approach A (user-chosen): audit, no live run, no emitter patch. Shipped a re-runnable `measurability-gate.sh` (NOT-MEASURABLE now; the Phase-70 trigger) + `VALID-MEASUREMENT.md`, the frozen `real-transcript-survey.md`, the SCHEMA-NOTES line-50 resolution, and the honest apparatus disposition (ruler not yet a validated gate ⇒ comparison tombstoned, reasoning calibration-only, binary corpus sole gate). Deterministic/read-only/no-LLM; verdict vocabulary about the ANCHOR not harness value. make eval 52/52, make test green at the unchanged 19-script count. Reviewer 10/10.
- **Phase 68 outcome:** built **Frontier 0 of the amplifier vision — the deterministic READ-ONLY ruler** ([[amplifier-measurement-instrument]]) BEFORE any cognitive frontier (the strategic handover ranked the escalation-classifier highest, but it is unfalsifiable without an instrument). `eval/amplifier/emit-proxy-vector.sh` ingests a REAL transcript jsonl + final git/test state and emits a 4-group proxy-vector (mechanical/interaction/enforcement/ground_truth); control-validated via a surfaced/buried minimal pair that FLIPS the detector + ≥1 interaction proxy (`tests/test_amplifier_emitter.sh` 14/14). Substrate = transcript (not `enforcement.log`): closes the Phase-66 firing→action schema-gap + clears the Phase-65 corpus-duplication bar with real trace provenance. NO LLM judge, NO `verdict` field, NO harness claim (instrument-validation only; harness-measurement is Phase 69). New repo-only `eval/amplifier/` tree (8 files, NOT in install.sh). make test 18→19 / make eval 52/52.
- **Phase 67 outcome:** made the functional-smoke invariant ([[functional-smoke-invariant-rule]]; [[HEU-012]]) **machine-checkable** — a RED-first coverage meta-test ([[hook-firing-coverage-gate]]) over the UNION of `.hooks[]` (type-predicate) + `extra_dirs` curators, un-gameable via pinned exemption allow-list + denominator floor + negative control. The RED run surfaced the true gap = **10 untested hooks (not the roadmap's 3)**; all 10 now firing-tested (`test_long_cadence_hooks.sh`, `test_tooluse_hooks.sh`, memory-nudge in `test_harden.sh`). Closed 3 Phase-63 roadmap items with corrections. Tests-only → no registration/settings drift. make test 15→18 / make eval 52/52. Reviewer 9/10 accept (committed + pushed).
- **Phase 66 outcome:** the planned scorer is UNBUILDABLE — the signal-richness gate failed decisively (11 new-format records, 1 hook, 0 blocks, all self-traffic; 2 of 4 blockers structural: representativeness + schema-gap). Shipped the falsification AS A RUNNABLE PROBE ([[park-enforcement-scorer-signal-insufficient]]) and parked the scorer behind it. Redirected to the audit-log disposition → KEEP + harden ([[audit-log-disposition]]; the Phase-64 write-only⇒deadweight rule doesn't apply to a human-facing forensic trail). Reviewer 7/10 → 2 bugs fixed. make test 15 / make eval 52/52.
- **Phase 65 outcome:** built the non-blind eval's measurement SUBSTRATE (the Phase-63 verdict's proposal) — 6 lifecycle hooks emit fail-open `{schema_version,ts,hook,action,reason,phase}` firing records to `enforcement.log` — after an approach-review reversed a scored fixture-replay as corpus-duplication ([[instrument-not-score-enforcement-firing-substrate]]). The scorer is Phase 66 (gated on signal-richness). Executed the [[eval-validity-verdict]] disposition: comparison A-vs-C retired, reasoning demoted to calibration-only. The retrofit fixed 2 latent bugs in the existing loggers; enforcement.log untracked. Safety: all 4 blocking hooks proven to preserve exit 2 under log failure.
- **Phase 62 outcome:** the hot cache now enforces its own integrity invariants deterministically (cap/dedup/well-formedness/atomic-write), replacing fuzzy LLM-executed prose; dedup key corrected slug→proposition-text; policy consolidated to one source of truth. The one affirmative-evidence direction from Phase 61 ([[hot-cache-is-the-effective-retrieval-layer]], [[two-tier-curate-into-hot-cache]]) shipped. Governed by [[harden-hot-cache-curation-deterministic]], [[dedup-key-proposition-not-slug]], [[curator-fail-safe-atomic]], [[extend-wk-prune-not-new-hook]].
- **Phase 61 outcome:** experiment-first A/B → all 5 runtime-retrieval directions CUT; load-bearing positive = [[hot-cache-is-the-effective-retrieval-layer]]. Validates [[memory-architecture-classification]] (strengthen always-loaded `.claude/rules/` activation points) by measurement. Companion to Phase-59's [[cut-active-research-step-2-7]].
- **Roadmap: Phase 57+ Harness Activation — COMPLETE** (Fixes 1–5 all done across P55-60). No residual harness-activation items.
- Next substantive roadmap items: **Phase 63 candidate: hot-cache eviction value-signal** (usage counter empirically inert ⇒ cap-eviction is de-facto recency; decide a real value signal vs. accepting recency explicitly) + distillation QUALITY (what gets written into the cache — needs a non-binary eval method first). Also standing: vector-search-default-on design call; gap 4.1 language-agnostic core (factor py-* out); always-loaded-budget claim research (AGENTS.md:84); repo-hygiene: gitignore `.memory/*.db*` (runtime churn pollutes phase diffs).
- Maintenance (post-Phase-58, commit `74da87a`): memory venv fix — `make test` green end-to-end; [[guard-optional-dep-tests]] (optional-dep tests skip, never assume-and-halt).
- Spec: Phase 55 used USER OVERRIDE (experiment data replaced prescriptive spec)
- Roadmap: [[roadmap-gap-analysis|Engineering Gap Analysis]] -- 1 OPEN gap remains (4.1 language-agnostic core)
- Roadmap: [[roadmap-cognitive-enhancement|Cognitive Enhancement Roadmap]] -- 7/7 phases done (all complete)
- Release: v0.5.0 tagged and pushed
- Benchmark: FTS5 recall@5 91.0%, hybrid/turn ~95% estimated (LongMemEval-S)
- Knowledge wiki: wiki/ (cognitive-patterns) -- 11 heuristics + 5 IRON RULES + 4 domain pattern articles, heuristic judge + matcher wired at Step 6.5, counter + lifecycle evolution loop
- Retro: Phases 51-55 clean (0 recurring blockers, 0 reversals, 1 documented user override)
