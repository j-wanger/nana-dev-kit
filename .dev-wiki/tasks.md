# Tasks

> Last updated: 2026-05-19 by /dev-plan (Phase 9)

<!-- phase:phase-01-foundation-and-packaging -->
## Phase 1: Foundation & Packaging

- [x] Create .gitignore with exclusions for settings.local.json, .nana/, .DS_Store, *.pyc, __pycache__/, .venv/ | scope: .gitignore | success: test -f .gitignore && grep -q settings.local.json .gitignore && grep -q '.nana/' .gitignore | size: S
- [x] Verify install.sh idempotency: test expected outputs exist (RED), run install.sh in temp HOME verify 3 files created (GREEN), fix error handling issues (REFACTOR) | scope: install.sh | success: THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/skills/py-init/SKILL.md" && test -f "$THOME/.claude/rules/nana-soul.md" && test -f "$THOME/.claude/.nana-dev-kit-path" && cp "$THOME/.claude/rules/nana-soul.md" /tmp/nana-first && HOME="$THOME" bash install.sh && diff /tmp/nana-first "$THOME/.claude/rules/nana-soul.md" && rm -rf "$THOME" /tmp/nana-first | size: M
- [x] Verify sync-rules.sh correctness: test outputs in temp dir (RED), run sync-rules.sh verify 4 output files with headers and content (GREEN), fix script issues (REFACTOR) | scope: scripts/sync-rules.sh, Makefile | success: TDIR=$(mktemp -d) && echo '# Test' > "$TDIR/AGENTS.md" && bash scripts/sync-rules.sh "$TDIR" "$TDIR" && grep -q 'AUTO-GENERATED' "$TDIR/CLAUDE.md" && grep -q 'Test' "$TDIR/CLAUDE.md" && grep -q 'AUTO-GENERATED' "$TDIR/GEMINI.md" && test -f "$TDIR/.github/copilot-instructions.md" && test -f "$TDIR/.cursor/rules/main.mdc" && rm -rf "$TDIR" | size: M
- [x] Write README.md — concise format with install + usage + 5-layer table: test -f README.md fails (RED), write README (GREEN), trim to 40-50 lines (REFACTOR) | scope: README.md | success: test -f README.md && grep -q 'install.sh' README.md && grep -q 'py-init' README.md && grep -qi 'layer' README.md && [ $(wc -l < README.md) -ge 30 ] | size: M
- [x] Initial git commit with clean project structure: no commit with all artifacts (RED), stage and commit (GREEN) | scope: * | success: git log --oneline -1 && git diff --quiet && git diff --cached --quiet && git ls-files | grep -q install.sh && git ls-files | grep -q README.md | size: S

<!-- phase:phase-02-automated-testing -->
## Phase 2: Automated Testing

- [x] Create tests/helpers.sh — shared assertion functions (assert_eq, assert_file_exists, assert_contains, assert_exit_code) + test summary reporting: bash -n tests/helpers.sh fails (RED), write helper functions (GREEN) | scope: tests/helpers.sh | success: test -f tests/helpers.sh && bash -n tests/helpers.sh | size: S
- [x] Create tests/test_install.sh — install.sh idempotency in temp HOME, verify 3 files created, diff between runs, verify kit path content: bash tests/test_install.sh fails (RED), write tests using HOME=$(mktemp -d) pattern (GREEN), clean up edge cases (REFACTOR) | scope: tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'mktemp' tests/test_install.sh | size: M
- [x] Create tests/test_sync_rules.sh — 4 output files created, AUTO-GENERATED headers, content propagated, Cursor frontmatter, missing AGENTS.md exits non-zero with stderr: bash tests/test_sync_rules.sh fails (RED), write tests in temp dir (GREEN), add error case tests (REFACTOR) | scope: tests/test_sync_rules.sh | success: bash tests/test_sync_rules.sh && grep -q 'AUTO-GENERATED' tests/test_sync_rules.sh | size: M
- [x] Create tests/test_templates.sh — verify {{PACKAGE_NAME}}, {{PROJECT_DESCRIPTION}}, {{PROJECT_NAME}} placeholders exist in template files: bash tests/test_templates.sh fails (RED), write grep-based placeholder checks (GREEN) | scope: tests/test_templates.sh | success: bash tests/test_templates.sh | size: S
- [x] Add make test target and verify all tests pass — Makefile test target runs all test scripts fail-fast: make test fails (RED), add test target (GREEN) | scope: Makefile, tests/* | success: make test | size: S

<!-- phase:phase-03-distribution-and-polish -->
## Phase 3: Distribution & Polish

- [x] Create VERSION file with 0.1.0: test -f VERSION fails (RED), create VERSION containing "0.1.0" (GREEN) | scope: VERSION | success: test -f VERSION && grep -qx '0.1.0' VERSION | size: S
- [x] Harden install.sh edge cases — fix asymmetric error suppression, validate source files exist before copy, report clear errors on failure: add edge-case tests to test_install.sh for missing source file scenario (RED), fix asymmetric 2>/dev/null || true and add source-file existence checks with error messages (GREEN), unify error handling style across all copy operations (REFACTOR) | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh | size: M
- [x] Harden sync-rules.sh edge cases — validate target directory writability, guard against partial writes: add edge-case tests to test_sync_rules.sh for unwritable target scenario (RED), add writability pre-check and consistent error reporting (GREEN), unify error handling style across missing-AGENTS.md and unwritable-dir paths (REFACTOR) | scope: scripts/sync-rules.sh, tests/test_sync_rules.sh | success: bash tests/test_sync_rules.sh | size: M
- [x] Create .github/workflows/kit-ci.yml — shellcheck all .sh files + make test, triggered on push and PR to main: test -f .github/workflows/kit-ci.yml fails (RED), write GitHub Actions workflow with shellcheck + make test (GREEN) | scope: .github/workflows/kit-ci.yml | success: test -f .github/workflows/kit-ci.yml && grep -q 'shellcheck' .github/workflows/kit-ci.yml && grep -q 'make test' .github/workflows/kit-ci.yml | size: M
- [x] Update README.md — add "Upgrading" section documenting re-run install.sh (documentation only, no install.sh code change), trim to stay within line budget: README has no upgrade mention (RED), add upgrade section and trim verbose content (GREEN) | scope: README.md | success: grep -qi 'upgrad' README.md && [ $(wc -l < README.md) -le 55 ] | size: S
- [x] Tag release v0.1.0 — create local annotated git tag (remote push out of scope until GitHub remote configured): no v0.1.0 tag exists (RED), commit all Phase 3 changes and create annotated tag (GREEN) | scope: VERSION, .github/workflows/kit-ci.yml, install.sh, scripts/sync-rules.sh, README.md | success: git tag -l 'v0.1.0' | grep -q 'v0.1.0' | size: S

<!-- phase:phase-04-dev-wiki-and-memory-integration -->
## Phase 4: Dev-Wiki & Memory Integration

- [x] Vendor memory_server from nanaclaw — copy Python source files to memory_server/ (exclude tests/ and __pycache__/), create requirements.txt (required: mcp, pydantic, pyyaml, nanoid, httpx) and requirements-optional.txt (fastembed, sqlite-vec): memory_server/server.py does not exist (RED), copy files excluding tests/__pycache__, create requirements files (GREEN), verify all .py files parse as valid Python (REFACTOR) | scope: memory_server/ | success: test -f memory_server/server.py && test -f memory_server/storage.py && test -f memory_server/requirements.txt && test -f memory_server/requirements-optional.txt && for f in memory_server/*.py; do python3 -c "import ast; ast.parse(open('$f').read())" || exit 1; done | size: M
- [x] Update install.sh to register memory MCP server — copy memory_server/ to ~/.claude/memory_server/ and add mcpServers entry to ~/.claude/settings.json via idempotent python3 JSON merge (ESCAPE HATCH: DEPENDENCY — evolves install-sh-stays-minimal per Phase 4 objective): add test cases to test_install.sh for MCP registration — no settings.json, existing without mcpServers, existing with mcpServers (RED), implement memory_server copy + JSON merge in install.sh (GREEN), unify error handling for JSON merge failures (REFACTOR) | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'mcpServers' tests/test_install.sh | size: M
- [x] Enhance session-start.sh — read .dev-wiki/_CURRENT_STATE.md and .memory/MEMORY.md with graceful silent skip when missing: hook reads only 2 sources (RED), add dev-wiki state + memory snapshot reads with file-existence guards (GREEN) | scope: templates/.claude/hooks/session-start.sh | success: bash -n templates/.claude/hooks/session-start.sh && grep -q 'dev-wiki' templates/.claude/hooks/session-start.sh && grep -q 'memory' templates/.claude/hooks/session-start.sh && TDIR=$(mktemp -d) && cd "$TDIR" && bash "$OLDPWD/templates/.claude/hooks/session-start.sh" && rm -rf "$TDIR" | size: S
- [x] Update /py-init SKILL.md — add /dev-init suggestion in "After scaffolding" section for both new and existing project flows: SKILL.md does not mention dev-init (RED), add suggestion text (GREEN) | scope: templates/.claude/skills/py-init/SKILL.md | success: grep -q 'dev-init' templates/.claude/skills/py-init/SKILL.md | size: S
- [x] Update README.md — add Memory & Dev-Wiki section documenting integration, trim to stay within line budget: README has no memory mention (RED), add section and trim (GREEN) | scope: README.md | success: grep -qi 'memory' README.md && grep -qi 'dev-wiki\|lifecycle' README.md && [ $(wc -l < README.md) -le 65 ] | size: S

<!-- phase:phase-05-memory-bootstrap-and-report -->
## Phase 5: Memory Bootstrap & Package Report

- [x] Bootstrap memory server deps in install.sh — create venv at ~/.claude/memory_server/.venv/, pip install requirements.txt, update mcpServers config to use venv Python path, graceful fallback if venv unavailable: add tests to test_install.sh for venv creation and MCP config pointing to venv Python (RED), implement venv creation + pip install + config update in install.sh (GREEN), unify error handling for venv/pip failures (REFACTOR) | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'venv' tests/test_install.sh | size: M
- [x] Create HTML package report generator — Python script at scripts/generate-report.py that scans project and generates docs/report.html covering all 5 layers, memory system, dev-wiki integration, file tree, workflows, template inventory, test coverage, and dependencies. Add make report target: scripts/generate-report.py does not exist (RED), write generator script and add Makefile target (GREEN), generate initial report (REFACTOR) | scope: scripts/generate-report.py, docs/report.html, Makefile | success: test -f scripts/generate-report.py && test -f docs/report.html && python3 scripts/generate-report.py && grep -qi 'nana dev kit' docs/report.html && grep -q 'report' Makefile | size: M
- [x] Update README.md — note memory deps auto-installed, add make report mention: README has no mention of auto-install (RED), add notes and trim (GREEN) | scope: README.md | success: grep -qi 'auto.*install\|venv\|pip' README.md && grep -qi 'make report' README.md && [ $(wc -l < README.md) -le 65 ] | size: S

<!-- phase:phase-06-ship-and-workflow-assessment -->
## Phase 6: Ship & Workflow Assessment

- [x] Create versioned workflow breakdown generator — Python script at scripts/generate-workflow.py generates docs/workflow.html with end-to-end flows (install→scaffold→develop→lifecycle), layer deep-dives (5 layers with trigger/action/effect), hook system walkthrough (each hook template), template inventory (new vs existing project paths), quality signals (error handling, idempotency, test coverage per component), dependency/integration map, memory+dev-wiki integration, version stamp from VERSION. Add make workflow target. Update README.md with make workflow mention: test -f scripts/generate-workflow.py fails (RED), write generator scanning templates/.claude/ and project files to produce comprehensive HTML (GREEN), add Makefile target and README line (REFACTOR) | scope: scripts/generate-workflow.py, docs/workflow.html, Makefile, README.md | success: test -f scripts/generate-workflow.py && python3 scripts/generate-workflow.py && test -f docs/workflow.html && grep -qi 'workflow' docs/workflow.html && grep -q '0\.' docs/workflow.html && grep -q 'workflow' Makefile && grep -qi 'make workflow' README.md | size: M
- [x] Bump version to 0.2.0 and regenerate reports — update VERSION file, regenerate both docs/report.html and docs/workflow.html with new version stamp: grep -qx '0.2.0' VERSION fails (RED), write 0.2.0 to VERSION and run both generators (GREEN) | scope: VERSION, docs/report.html, docs/workflow.html | success: grep -qx '0.2.0' VERSION && grep -q '0.2.0' docs/report.html && grep -q '0.2.0' docs/workflow.html | size: S
- [x] Commit all work + push to GitHub + tag v0.2.0 — two commits (Phase 5 work, Phase 6 work) for phase attribution, add GitHub remote if needed, push, create annotated tag v0.2.0. CI verification is manual post-push: uncommitted changes exist (RED), git add + commit (two commits) + remote add + push + tag (GREEN) | scope: * | success: git diff --quiet && git diff --cached --quiet && git remote -v | grep -q origin && git tag -l 'v0.2.0' | grep -q 'v0.2.0' | size: S

<!-- phase:phase-07-soul-and-instructions-enhancement -->
## Phase 7: Soul & Instructions Enhancement

- [x] Add "Before acting" and "Memory discipline" sections to nana-soul.md — new "Before acting" section (state assumptions, check memory/context, name alternatives, declare approach) and "Memory discipline" section (when to recall, when to store, compaction anchors). Add surgical discipline bullet to "Work habits". Rename "Review posture" to "Code quality lens". Add top-of-file comment marking global/universal scope. Target ≤60 lines: grep -qi 'before acting' templates/.claude/rules/nana-soul.md returns false (RED), write both sections + rename + bullet + comment (GREEN), trim to stay under 60 lines (REFACTOR) | scope: templates/.claude/rules/nana-soul.md | success: grep -qi 'before acting' templates/.claude/rules/nana-soul.md && grep -qi 'memory discipline' templates/.claude/rules/nana-soul.md && grep -qi 'code quality lens' templates/.claude/rules/nana-soul.md && grep -qi 'every changed line' templates/.claude/rules/nana-soul.md && grep -qi 'global' templates/.claude/rules/nana-soul.md && [ $(wc -l < templates/.claude/rules/nana-soul.md) -le 60 ] | size: M
- [x] Extract "Who you're working with" from template, install via install.sh — remove personal profile section from templates nana-soul.md (template becomes universal). Create templates/.claude/rules/nana-personal.md with the personal content. Update install.sh to copy nana-personal.md alongside nana-soul.md. Update tests: 'jake' NOT in template nana-soul.md (RED), extract to nana-personal.md and update install.sh (GREEN), verify idempotency (REFACTOR) | scope: templates/.claude/rules/nana-soul.md, templates/.claude/rules/nana-personal.md, install.sh, tests/test_install.sh | success: ! grep -qi 'jake' templates/.claude/rules/nana-soul.md && test -f templates/.claude/rules/nana-personal.md && grep -qi 'jake' templates/.claude/rules/nana-personal.md && grep -q 'nana-personal' install.sh && bash tests/test_install.sh | size: M
- [x] Rename "Verification" to "Pre-commit sequence" in AGENTS.md — sharpen name to distinguish operational verification from cognitive quality lens in soul: grep -qi 'pre-commit sequence' templates/AGENTS.md returns false (RED), rename section header (GREEN) | scope: templates/AGENTS.md | success: grep -qi 'pre-commit sequence' templates/AGENTS.md && ! grep -q '## Verification' templates/AGENTS.md | size: S
- [x] Sync nana.instructions.md to match updated nana-soul.md — copy updated body content preserving Copilot applyTo frontmatter: content differs after Tasks 1+2 (RED), update body to match (GREEN) | scope: templates/.github/instructions/nana.instructions.md | success: diff <(tail -n +5 templates/.github/instructions/nana.instructions.md) templates/.claude/rules/nana-soul.md | size: S
- [x] Add protocol assertions + instruction budget regression test — new assertions in test_templates.sh: grep for "Before acting" and "Memory discipline" in nana-soul.md, "Pre-commit sequence" in AGENTS.md, "jake" NOT in template nana-soul.md, wc budget check summing nana-soul.md + nana-personal.md + nana.instructions.md + AGENTS.md ≤ 300: no protocol or budget checks exist (RED), add assertions (GREEN) | scope: tests/test_templates.sh | success: bash tests/test_templates.sh && grep -qE 'Before.acting|Memory.discipline' tests/test_templates.sh && grep -qE 'nana-personal' tests/test_templates.sh && grep -qE 'wc|budget|300' tests/test_templates.sh | size: S

<!-- phase:phase-08-spec-skill -->
## Phase 8: Spec Skill

- [x] Create /spec SKILL.md + spec-reviewer-prompt.md — portable spec skill with 9-section template, two-tier review gate (Tier 0 structural lint inline + Tier 1 semantic subagent review), thinking protocol integration, dev-wiki detection, approval gate, persistence to specs/. Companion spec-reviewer-prompt.md with 6 dimensions (ambiguity, constraint completeness, exit criteria verifiability, checkpoint proportionality, assumption explicitness, self-containment): test -f templates/.claude/skills/spec/SKILL.md fails (RED), write SKILL.md + reviewer prompt (GREEN), trim to budgets (REFACTOR) | scope: templates/.claude/skills/spec/SKILL.md, templates/.claude/skills/spec/spec-reviewer-prompt.md | success: test -f templates/.claude/skills/spec/SKILL.md && [ $(wc -l < templates/.claude/skills/spec/SKILL.md) -le 120 ] && test -f templates/.claude/skills/spec/spec-reviewer-prompt.md && [ $(wc -l < templates/.claude/skills/spec/spec-reviewer-prompt.md) -le 80 ] && grep -qi 'constraints' templates/.claude/skills/spec/SKILL.md && grep -qi 'checkpoints' templates/.claude/skills/spec/SKILL.md && grep -qi 'assumptions' templates/.claude/skills/spec/SKILL.md && grep -qi 'tier 0\|structural.*lint' templates/.claude/skills/spec/SKILL.md && grep -qi 'subagent\|reviewer' templates/.claude/skills/spec/SKILL.md && grep -qi 'dev-wiki\|dev.plan' templates/.claude/skills/spec/SKILL.md && grep -qi 'specs/' templates/.claude/skills/spec/SKILL.md && grep -qi 'ambiguity' templates/.claude/skills/spec/spec-reviewer-prompt.md | size: M
- [x] Backport Constraints/Checkpoints/Assumptions into phase template + update dev-plan Step 6 — add 3 optional H2 sections to ~/.claude/skills/dev-wiki/phase-template.md, add spec-field coverage note to dev-plan Step 6: phase template lacks these sections (RED), add sections + note (GREEN) | scope: ~/.claude/skills/dev-wiki/phase-template.md, ~/.claude/skills/dev-plan/SKILL.md | success: grep -qi 'constraints' ~/.claude/skills/dev-wiki/phase-template.md && grep -qi 'checkpoints' ~/.claude/skills/dev-wiki/phase-template.md && grep -qi 'assumptions' ~/.claude/skills/dev-wiki/phase-template.md && grep -qi 'constraints' ~/.claude/skills/dev-plan/SKILL.md && grep -qi 'checkpoints' ~/.claude/skills/dev-plan/SKILL.md && grep -qi 'assumptions' ~/.claude/skills/dev-plan/SKILL.md | size: M
- [x] Update install.sh to copy spec skill + add tests — install.sh copies templates/.claude/skills/spec/ to ~/.claude/skills/spec/. Add assert_file_exists for spec SKILL.md in test_install.sh. Add spec content assertions in test_templates.sh: install.sh doesn't reference spec (RED), add copy + assertions (GREEN) | scope: install.sh, tests/test_install.sh, tests/test_templates.sh | success: grep -q 'spec' install.sh && bash tests/test_install.sh && bash tests/test_templates.sh && grep -qi 'spec' tests/test_templates.sh | size: S
- [x] Update README with /spec mention — add /spec to After scaffolding list: README has no /spec mention (RED), add mention (GREEN) | scope: README.md | success: grep -qi '/spec' README.md && [ $(wc -l < README.md) -le 65 ] | size: S

<!-- phase:phase-09-file-lifecycle-reference -->
## Phase 9: File Lifecycle Reference

- [ ] Create file-lifecycle.md + remove PROJECT_STATE.md orphan — routing table at templates/.claude/rules/file-lifecycle.md (~30 lines) with 4 categories (user, agent, skill, hook) + decision routing section. Remove PROJECT_STATE.md read from session-start.sh and reference from /spec SKILL.md. Update install.sh to copy file-lifecycle.md: test -f templates/.claude/rules/file-lifecycle.md fails (RED), write routing table + remove orphan + update install.sh (GREEN), trim to ≤35 lines (REFACTOR) | scope: templates/.claude/rules/file-lifecycle.md, templates/.claude/hooks/session-start.sh, templates/.claude/skills/spec/SKILL.md, install.sh | success: test -f templates/.claude/rules/file-lifecycle.md && [ $(wc -l < templates/.claude/rules/file-lifecycle.md) -le 35 ] && grep -qi 'decision routing' templates/.claude/rules/file-lifecycle.md && grep -qi 'AGENTS.md' templates/.claude/rules/file-lifecycle.md && grep -qi 'memory_store' templates/.claude/rules/file-lifecycle.md && ! grep -q 'PROJECT_STATE' templates/.claude/hooks/session-start.sh && ! grep -q 'PROJECT_STATE' templates/.claude/skills/spec/SKILL.md && grep -q 'file-lifecycle' install.sh | size: M
- [ ] Update tests — add file-lifecycle.md copy assertion to test_install.sh, content assertions to test_templates.sh, update budget test to include file-lifecycle.md in wc sum: no file-lifecycle assertions exist (RED), add assertions (GREEN) | scope: tests/test_install.sh, tests/test_templates.sh | success: bash tests/test_install.sh && bash tests/test_templates.sh && grep -qi 'file-lifecycle' tests/test_templates.sh | size: S
- [ ] Commit + push to GitHub | scope: * | success: git diff --quiet && git diff --cached --quiet | size: S
