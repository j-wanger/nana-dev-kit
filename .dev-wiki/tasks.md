# Tasks

> Last updated: 2026-05-25 by /dev-debrief (Phase 40 completed)

<details>
<summary>Phases 1-38 (all completed, 220 tasks, collapsed)</summary>

<!-- gate-log:phase-33 spec=approved approach=approved plan-review=approved tasks=approved -->



<!-- phase:phase-01-foundation-and-packaging -->
## Phase 1: Foundation & Packaging

- [x] Create .gitignore with exclusions for settings.local.json, .nana/, .DS_Store, *.pyc, __pycache__/, .venv/ | scope: .gitignore | success: test -f .gitignore && grep -q settings.local.json .gitignore && grep -q '.nana/' .gitignore | size: S
- [x] Verify install.sh idempotency: test expected outputs exist (RED), run install.sh in temp HOME verify 3 files created (GREEN), fix error handling issues (REFACTOR) | scope: install.sh | success: THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/skills/py-init/SKILL.md" && test -f "$THOME/.claude/rules/nana-soul.md" && test -f "$THOME/.claude/.nana-dev-kit-path" && cp "$THOME/.claude/rules/nana-soul.md" /tmp/nana-first && HOME="$THOME" bash install.sh && diff /tmp/nana-first "$THOME/.claude/rules/nana-soul.md" && rm -rf "$THOME" /tmp/nana-first | size: M
- [x] Verify sync-rules.sh correctness: test outputs in temp dir (RED), run sync-rules.sh verify 4 output files with headers and content (GREEN), fix script issues (REFACTOR) | scope: scripts/sync-rules.sh, Makefile | success: TDIR=$(mktemp -d) && echo '# Test' > "$TDIR/AGENTS.md" && bash scripts/sync-rules.sh "$TDIR" "$TDIR" && grep -q 'AUTO-GENERATED' "$TDIR/CLAUDE.md" && grep -q 'Test' "$TDIR/CLAUDE.md" && grep -q 'AUTO-GENERATED' "$TDIR/GEMINI.md" && test -f "$TDIR/.github/copilot-instructions.md" && test -f "$TDIR/.cursor/rules/main.mdc" && rm -rf "$TDIR" | size: M
- [x] Write README.md — concise format with install + usage + 5-layer table: test -f README.md fails (RED), write README (GREEN), trim to 40-50 lines (REFACTOR) | scope: README.md | success: test -f README.md && grep -q 'install.sh' README.md && grep -q 'py-init' README.md && grep -qi 'layer' README.md && [ $(wc -l < README.md) -ge 30 ] | size: M
- [x] Initial git commit with clean project structure: no commit with all artifacts (RED), stage and commit (GREEN) | scope: * | success: git log --oneline -1 && git diff --quiet && git diff --cached --quiet && git ls-files | grep -q install.sh && git ls-files | grep -q README.md | size: S

<!-- phase:phase-02-automated-testing -->
## Phase 2: Automated Testing

- [x] Create tests/helpers.sh — shared assertion functions (assert_eq, assert_file_exists, assert_contains, assert_exit_code) + test summary reporting: bash -n tests/helpers.sh fails (RED), write helper functions (GREEN) | scope: tests/helpers.sh | success: test -f tests/helpers.sh && bash -n tests/helpers.sh | size: S
- [x] Create tests/test_install.sh — install.sh idempotency in temp HOME, verify 3 files created, diff between runs, verify kit path content: bash tests/test_install.sh fails (RED), write tests using HOME=$(mktemp -d) pattern (GREEN), clean up edge cases (REFACTOR) | scope: tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'mktemp' tests/test_install.sh | size: M
- [x] Create tests/test_sync_rules.sh — 4 output files created, AUTO-GENERATED headers, content propagated, Cursor frontmatter, missing AGENTS.md exits non-zero with stderr: bash tests/test_sync_rules.sh fails (RED), write tests in temp dir (GREEN), add error case tests (REFACTOR) | scope: tests/test_sync_rules.sh | success: bash tests/test_sync_rules.sh && grep -q 'AUTO-GENERATED' tests/test_sync_rules.sh | size: M
- [x] Create tests/test_templates.sh — verify {{PACKAGE_NAME}}, {{PROJECT_DESCRIPTION}}, {{PROJECT_NAME}} placeholders exist in template files: bash tests/test_templates.sh fails (RED), write grep-based placeholder checks (GREEN) | scope: tests/test_templates.sh | success: bash tests/test_templates.sh | size: S
- [x] Add make test target and verify all tests pass — Makefile test target runs all test scripts fail-fast: make test fails (RED), add test target (GREEN) | scope: Makefile, tests/* | success: make test | size: S

<!-- phase:phase-03-distribution-and-polish -->
## Phase 3: Distribution & Polish

- [x] Create VERSION file with 0.1.0: test -f VERSION fails (RED), create VERSION containing "0.1.0" (GREEN) | scope: VERSION | success: test -f VERSION && grep -qx '0.1.0' VERSION | size: S
- [x] Harden install.sh edge cases — fix asymmetric error suppression, validate source files exist before copy, report clear errors on failure | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh | size: M
- [x] Harden sync-rules.sh edge cases — validate target directory writability, guard against partial writes | scope: scripts/sync-rules.sh, tests/test_sync_rules.sh | success: bash tests/test_sync_rules.sh | size: M
- [x] Create .github/workflows/kit-ci.yml — shellcheck all .sh files + make test | scope: .github/workflows/kit-ci.yml | success: test -f .github/workflows/kit-ci.yml && grep -q 'shellcheck' .github/workflows/kit-ci.yml && grep -q 'make test' .github/workflows/kit-ci.yml | size: M
- [x] Update README.md — add "Upgrading" section | scope: README.md | success: grep -qi 'upgrad' README.md && [ $(wc -l < README.md) -le 55 ] | size: S
- [x] Tag release v0.1.0 | scope: VERSION, .github/workflows/kit-ci.yml, install.sh, scripts/sync-rules.sh, README.md | success: git tag -l 'v0.1.0' | grep -q 'v0.1.0' | size: S

<!-- phase:phase-04-dev-wiki-and-memory-integration -->
## Phase 4: Dev-Wiki & Memory Integration

- [x] Vendor memory_server from nanaclaw | scope: memory_server/ | success: test -f memory_server/server.py && test -f memory_server/requirements.txt | size: M
- [x] Update install.sh to register memory MCP server | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'mcpServers' tests/test_install.sh | size: M
- [x] Enhance session-start.sh — read .dev-wiki/_CURRENT_STATE.md | scope: templates/.claude/hooks/session-start.sh | success: bash -n templates/.claude/hooks/session-start.sh && grep -q 'dev-wiki' templates/.claude/hooks/session-start.sh | size: S
- [x] Update /py-init SKILL.md — add /dev-init suggestion | scope: templates/.claude/skills/py-init/SKILL.md | success: grep -q 'dev-init' templates/.claude/skills/py-init/SKILL.md | size: S
- [x] Update README.md — add Memory & Dev-Wiki section | scope: README.md | success: grep -qi 'memory' README.md && [ $(wc -l < README.md) -le 65 ] | size: S

<!-- phase:phase-05-memory-bootstrap-and-report -->
## Phase 5: Memory Bootstrap & Package Report

- [x] Bootstrap memory server deps in install.sh | scope: install.sh, tests/test_install.sh | success: bash tests/test_install.sh && grep -q 'venv' tests/test_install.sh | size: M
- [x] Create HTML package report generator | scope: scripts/generate-report.py, docs/report.html, Makefile | success: test -f scripts/generate-report.py && test -f docs/report.html | size: M
- [x] Update README.md — note memory deps auto-installed | scope: README.md | success: grep -qi 'auto.*install\|venv\|pip' README.md | size: S

<!-- phase:phase-06-ship-and-workflow-assessment -->
## Phase 6: Ship & Workflow Assessment

- [x] Create versioned workflow breakdown generator | scope: scripts/generate-workflow.py, docs/workflow.html, Makefile, README.md | success: test -f scripts/generate-workflow.py && test -f docs/workflow.html | size: M
- [x] Bump version to 0.2.0 and regenerate reports | scope: VERSION, docs/report.html, docs/workflow.html | success: grep -qx '0.2.0' VERSION | size: S
- [x] Commit all work + push to GitHub + tag v0.2.0 | scope: * | success: git tag -l 'v0.2.0' | grep -q 'v0.2.0' | size: S

<!-- phase:phase-07-soul-and-instructions-enhancement -->
## Phase 7: Soul & Instructions Enhancement

- [x] Add "Before acting" and "Memory discipline" sections to nana-soul.md | scope: templates/.claude/rules/nana-soul.md | success: grep -qi 'before acting' templates/.claude/rules/nana-soul.md && [ $(wc -l < templates/.claude/rules/nana-soul.md) -le 60 ] | size: M
- [x] Extract "Who you're working with" from template, install via install.sh | scope: templates/.claude/rules/nana-soul.md, templates/.claude/rules/nana-personal.md, install.sh, tests/test_install.sh | success: ! grep -qi 'jake' templates/.claude/rules/nana-soul.md && test -f templates/.claude/rules/nana-personal.md | size: M
- [x] Rename "Verification" to "Pre-commit sequence" in AGENTS.md | scope: templates/AGENTS.md | success: grep -qi 'pre-commit sequence' templates/AGENTS.md | size: S
- [x] Sync nana.instructions.md to match updated nana-soul.md | scope: templates/.github/instructions/nana.instructions.md | success: diff <(tail -n +5 templates/.github/instructions/nana.instructions.md) templates/.claude/rules/nana-soul.md | size: S
- [x] Add protocol assertions + instruction budget regression test | scope: tests/test_templates.sh | success: bash tests/test_templates.sh | size: S

<!-- phase:phase-08-spec-skill -->
<!-- gates: spec=8/10 approach=yes plan-review=7/10 tasks=yes -->
## Phase 8: Spec Skill

- [x] Create /spec SKILL.md + spec-reviewer-prompt.md | scope: templates/.claude/skills/spec/SKILL.md, templates/.claude/skills/spec/spec-reviewer-prompt.md | success: test -f templates/.claude/skills/spec/SKILL.md && test -f templates/.claude/skills/spec/spec-reviewer-prompt.md | size: M
- [x] Backport Constraints/Checkpoints/Assumptions into phase template + update dev-plan Step 6 | scope: ~/.claude/skills/dev-wiki/phase-template.md, ~/.claude/skills/dev-plan/SKILL.md | success: grep -qi 'constraints' ~/.claude/skills/dev-wiki/phase-template.md | size: M
- [x] Update install.sh to copy spec skill + add tests | scope: install.sh, tests/test_install.sh, tests/test_templates.sh | success: grep -q 'spec' install.sh && bash tests/test_install.sh | size: S
- [x] Update README with /spec mention | scope: README.md | success: grep -qi '/spec' README.md | size: S

<!-- phase:phase-09-file-lifecycle-reference -->
<!-- gates: spec=9/10 approach=yes plan-review=n/a(2-tasks) tasks=yes -->
## Phase 9: File Lifecycle Reference

- [x] Create file-lifecycle.md + remove PROJECT_STATE.md orphan | scope: templates/.claude/rules/file-lifecycle.md, templates/.claude/hooks/session-start.sh, install.sh | success: test -f templates/.claude/rules/file-lifecycle.md && grep -q 'file-lifecycle' install.sh | size: M
- [x] Update tests | scope: tests/test_install.sh, tests/test_templates.sh | success: bash tests/test_install.sh && bash tests/test_templates.sh | size: S
- [x] Commit + push to GitHub | scope: * | success: git diff --quiet | size: S

<!-- phase:phase-10-memory-lifecycle-convergence -->
<!-- gates: spec=8/10 approach=SKIPPED plan-review=SKIPPED(post-hoc=8/10) tasks=SKIPPED -->
## Phase 10: Memory Lifecycle Convergence

- [x] Remove MEMORY.md from session-start + update soul + file-lifecycle | scope: templates/.claude/hooks/session-start.sh, templates/.claude/rules/nana-soul.md, templates/.claude/rules/file-lifecycle.md | success: ! grep -q 'MEMORY.md' templates/.claude/hooks/session-start.sh | size: M
- [x] Verify tests + commit + push | scope: tests/test_templates.sh, * | success: bash tests/test_templates.sh && git diff --quiet | size: S

<!-- phase:phase-11-process-hardening -->
<!-- gates: spec=n/a(process-hardening) approach=yes plan-review=yes tasks=yes -->
## Phase 11: Process Hardening

- [x] Add pre-flight gate verification to implementation-guide.md | scope: ~/.claude/skills/dev-plan/implementation-guide.md | success: grep -qi 'gate.*unchecked.*STOP\|refuse.*unchecked' ~/.claude/skills/dev-plan/implementation-guide.md | size: M
- [x] Add gate-compliance audit to dev-debrief retro-check | scope: ~/.claude/skills/dev-debrief/SKILL.md | success: grep -qi 'gate.*compliance\|gate.*audit' ~/.claude/skills/dev-debrief/SKILL.md | size: M
- [x] Add gate-check reminder to session-start.sh | scope: templates/.claude/hooks/session-start.sh | success: grep -q 'gate-check\|unchecked.*gate' templates/.claude/hooks/session-start.sh | size: S
- [x] Add regression test + budget assertion | scope: tests/test_templates.sh | success: bash tests/test_templates.sh | size: S
- [x] Commit + push + regenerate reports | scope: docs/report.html, docs/workflow.html | success: git diff --quiet | size: XS

<!-- phase:phase-12-soul-enhancement-memory-harvest -->
<!-- gates: spec=9/10 approach=yes plan-review=pending tasks=yes -->
## Phase 12: Soul Enhancement & Memory Harvest

- [x] Compress soul + add Voice & presence section — free >=3 lines via compression (remove 3 redundant "What to avoid" bullets), add 5-bullet Voice & presence section. Sync nana.instructions.md. TDD: soul missing "Voice" section (RED), compress + add + sync (GREEN), verify reads coherently (REFACTOR) | scope: templates/.claude/rules/nana-soul.md, templates/.github/instructions/nana.instructions.md | success: grep -qi 'Voice.*presence' templates/.claude/rules/nana-soul.md && [ $(wc -l < templates/.claude/rules/nana-soul.md) -le 60 ] && diff <(tail -n +5 templates/.github/instructions/nana.instructions.md) templates/.claude/rules/nana-soul.md | size: M
- [x] Create memory-harvest companion + wire into dev-debrief — Write memory-harvest.md (~40 lines): extraction categories, memory_store output, 100-entry ceiling, stale removal. Add Step 4.7 to SKILL.md (~3 lines). Add step to executor-prompt.md. TDD: memory-harvest.md doesn't exist (RED), write companion + wire (GREEN), verify no overlap with Step 5 (REFACTOR) | scope: ~/.claude/skills/dev-debrief/memory-harvest.md, ~/.claude/skills/dev-debrief/SKILL.md, ~/.claude/skills/dev-debrief/executor-prompt.md | success: test -f ~/.claude/skills/dev-debrief/memory-harvest.md && grep -qi 'memory.harvest' ~/.claude/skills/dev-debrief/SKILL.md && grep -qi 'memory_store' ~/.claude/skills/dev-debrief/memory-harvest.md && grep -qi 'memory.harvest' ~/.claude/skills/dev-debrief/executor-prompt.md | size: M
- [x] Add spec-existence check to dev-plan pre-checks — New Step 0.6: standard ceremony checks specs/<phase-slug>.md or phase article ## Formal Spec. If neither: STOP. Lite: skip. TDD: no spec check exists (RED), add Step 0.6 (GREEN) | scope: ~/.claude/skills/dev-plan/SKILL.md | success: grep -qi 'spec.*exist\|spec.*check\|No spec found' ~/.claude/skills/dev-plan/SKILL.md | size: S
- [x] Add thinking-protocol T0 to dev-plan Step 6 — Inline check: challenge frame, read subtext, delay commitment. Conversational output only. TDD: no thinking protocol reference in Step 6 area (RED), add T0 check (GREEN) | scope: ~/.claude/skills/dev-plan/SKILL.md | success: grep -qi 'thinking.protocol\|challenge.*frame' ~/.claude/skills/dev-plan/SKILL.md | size: S
- [x] Add soul ceiling test + regression check — Assert soul <=60 lines in test_templates.sh. Run full suite. TDD: no soul ceiling test (RED), add assertion (GREEN) | scope: tests/test_templates.sh | success: bash tests/test_templates.sh && grep -qi 'soul.*60\|60.*soul\|wc.*nana-soul' tests/test_templates.sh | size: S
- [x] Commit + push + regenerate reports — make report && make workflow, git commit, git push. TDD: reports outdated (RED), regenerate + commit + push (GREEN) | scope: docs/report.html, docs/workflow.html | success: git diff --quiet && git diff --cached --quiet | size: XS

<!-- phase:phase-13-final-polish-and-ship -->
<!-- gates: spec=8/10(revised-to-accept) approach=yes plan-review=7/10(revised) tasks=yes -->
## Phase 13: Final Polish & Ship

- [x] [S] Add H8+H9 to soul + sync — Add 2 verbatim lines to Thinking protocol: "Before searching, name what you already know — then construct targeted queries from it, not generic topic keywords." and "Check adjacent domains: upstream causes, downstream effects, parallel developments." Sync nana.instructions.md. TDD: soul missing H8/H9 (RED), add + sync (GREEN), verify exactly 59 lines (REFACTOR) | scope: templates/.claude/rules/nana-soul.md, templates/.github/instructions/nana.instructions.md | success: grep -qi 'targeted queries' templates/.claude/rules/nana-soul.md && [ $(wc -l < templates/.claude/rules/nana-soul.md) -eq 59 ] && diff <(tail -n +5 templates/.github/instructions/nana.instructions.md) templates/.claude/rules/nana-soul.md | size: S
- [x] [M] Make personal profile template + conditional install — Replace templates/.claude/rules/nana-personal.md with generic placeholder (no Jake content). Update install.sh to conditionally copy (skip if existing). Write Jake's 7-line version to ~/.claude/rules/nana-personal.md locally. Update tests. TDD: template has 'jake' (RED), replace template + update install.sh + write local (GREEN), verify idempotency (REFACTOR) | scope: templates/.claude/rules/nana-personal.md, install.sh, tests/test_install.sh, tests/test_templates.sh | success: ! grep -qi 'jake' templates/.claude/rules/nana-personal.md && grep -qi 'conditional\|skip.*exist\|-f.*personal' install.sh && bash tests/test_install.sh | size: M
- [x] [S] Raise SKILL.md ceiling — Change complex-orchestration cap from 250 to 350 in self-check-checklist.md. TDD: grep shows 250 (RED), change to 350 (GREEN) | scope: ~/.claude/skills/dev-plan/self-check-checklist.md | success: grep -q 'complex.*350\|≤350' ~/.claude/skills/dev-plan/self-check-checklist.md | size: S
- [x] [S] Version bump + reports + tests — Write 0.3.0 to VERSION, regenerate reports, run make test. TDD: VERSION shows 0.2.0 (RED), update + regen + test (GREEN) | scope: VERSION, docs/report.html, docs/workflow.html | success: grep -qx '0.3.0' VERSION && make test | size: S
- [x] [XS] Commit + tag v0.3.0 + push — Annotated tag, push with tags. TDD: no v0.3.0 tag (RED), commit + tag + push (GREEN) | scope: * | success: git diff --quiet && git tag -l 'v0.3.0' | grep -q 'v0.3.0' | size: XS

<!-- phase:phase-14-adversarial-thinking-and-review -->
<!-- gates: spec=8/10 approach=yes plan-review=8/10 tasks=yes -->
## Phase 14: Adversarial Thinking & Review

- [x] [S] T0 Rewrite — force non-vacuous output in dev-plan Step 6: grep -qi 'weakest.*assumption' fails (RED), rewrite Step 6 T0 with output-format requirements: name weakest assumption + what breaks, identify alternative framing, state what info would change recommendation, add non-vacuity gate ~15 lines (GREEN) | scope: ~/.claude/skills/dev-plan/SKILL.md | success: grep -qi 'weakest.*assumption\|what breaks' ~/.claude/skills/dev-plan/SKILL.md | size: S
- [x] [M] Adversarial constraint generation — spec Step 2.5 + companion: test -f adversarial-constraints-prompt.md fails (RED), create adversarial-constraints-prompt.md (~40-50 lines) with clean-context subagent prompt for constraints with falsifiability tests, edge cases, scope risks + insert Step 2.5 in spec SKILL.md ~20 lines (GREEN), verify SKILL.md ≤ 350 lines (REFACTOR) | scope: templates/.claude/skills/spec/SKILL.md, templates/.claude/skills/spec/adversarial-constraints-prompt.md | success: test -f templates/.claude/skills/spec/adversarial-constraints-prompt.md && grep -qi 'adversarial.*constraint\|clean.context.*subagent' templates/.claude/skills/spec/SKILL.md && [ $(wc -l < templates/.claude/skills/spec/SKILL.md) -le 350 ] | size: M
- [x] [M] Install + tests + verification: grep -q 'adversarial-constraints-prompt' install.sh fails (RED), add copy line to install.sh + test assertions in test_install.sh and test_templates.sh (GREEN), run full suite (REFACTOR) | scope: install.sh, tests/test_install.sh, tests/test_templates.sh | success: grep -q 'adversarial-constraints-prompt' install.sh && make test && [ $(wc -l < templates/.claude/rules/nana-soul.md) -le 60 ] | size: M

<!-- phase:phase-15-wire-the-lifecycle -->
<!-- gates: spec=7/10(revised-to-accept) approach=yes plan-review=7/10(revised) tasks=yes -->
## Phase 15: Wire the Lifecycle

- [x] [M] Import dev-wiki skills — copy 6 dirs from ~/.claude/skills/{dev-wiki,dev-check,dev-debrief,dev-init,dev-plan,dev-scan} into templates/.claude/skills/. TDD: test -d templates/.claude/skills/dev-plan fails (RED), cp -r all 6 dirs (GREEN), verify cross-references with grep for companion file paths (REFACTOR) | scope: templates/.claude/skills/dev-*/ | success: ls -d templates/.claude/skills/dev-wiki templates/.claude/skills/dev-check templates/.claude/skills/dev-debrief templates/.claude/skills/dev-init templates/.claude/skills/dev-plan templates/.claude/skills/dev-scan && [ $(find templates/.claude/skills/dev-* -name "*.md" | wc -l) -ge 40 ] | size: M
- [x] [M] Import knowledge-wiki skills — copy 11 dirs from ~/.claude/skills/{knowledge-wiki,wiki-absorb,wiki-add,wiki-bootstrap,wiki-consolidate,wiki-health,wiki-index,wiki-init,wiki-query,wiki-reorg,wiki-registry} into templates/.claude/skills/. TDD: test -d templates/.claude/skills/wiki-query fails (RED), cp -r all 11 dirs (GREEN), verify cross-references (REFACTOR) | scope: templates/.claude/skills/wiki-*/, templates/.claude/skills/knowledge-wiki/ | success: ls -d templates/.claude/skills/knowledge-wiki templates/.claude/skills/wiki-absorb templates/.claude/skills/wiki-add templates/.claude/skills/wiki-bootstrap templates/.claude/skills/wiki-consolidate templates/.claude/skills/wiki-health templates/.claude/skills/wiki-index templates/.claude/skills/wiki-init templates/.claude/skills/wiki-query templates/.claude/skills/wiki-reorg templates/.claude/skills/wiki-registry && [ $(find templates/.claude/skills/wiki-* templates/.claude/skills/knowledge-wiki -name "*.md" | wc -l) -ge 45 ] | size: M
- [x] [S] Generate MANIFEST — create templates/.claude/skills/MANIFEST with sorted file listing + md5 checksums. Depends: tasks 1+2. TDD: test -f templates/.claude/skills/MANIFEST fails (RED), generate with find+sort+md5 (GREEN) | scope: templates/.claude/skills/MANIFEST | success: test -f templates/.claude/skills/MANIFEST && [ $(wc -l < templates/.claude/skills/MANIFEST) -gt 100 ] | size: S
- [x] [L] Refactor install.sh — module-group architecture with --all/--core-only/--no-python/--dry-run flags, dependency validation (exit non-zero on missing prereqs), directory-based iteration. Depends: tasks 1+2. TDD: bash install.sh --dry-run fails (RED), implement flag parsing + module definitions + iteration loop (GREEN), verify idempotency + existing tests pass (REFACTOR) | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'dev-plan' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh --core-only && test ! -d "$THOME/.claude/skills/dev-plan" && test -f "$THOME/.claude/rules/nana-soul.md" && rm -rf "$THOME" && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh --no-python && test ! -d "$THOME/.claude/skills/py-init" && test -d "$THOME/.claude/skills/dev-plan" && rm -rf "$THOME" | size: L
- [x] [M] Add PreCompact hook — pure bash, reads _CURRENT_STATE.md + tasks.md + active-phase.md, outputs structured summary for context injection. TDD: test -f templates/.claude/hooks/pre-compact.sh fails (RED), write hook with graceful skips (GREEN), test against fixture with output validation (REFACTOR) | scope: templates/.claude/hooks/pre-compact.sh | success: bash -n templates/.claude/hooks/pre-compact.sh && mkdir -p /tmp/test-pc/.dev-wiki && printf '## Active Phase\n**Phase 15** (status: active)\n' > /tmp/test-pc/.dev-wiki/_CURRENT_STATE.md && (cd /tmp/test-pc && bash "$OLDPWD/templates/.claude/hooks/pre-compact.sh") 2>/dev/null | grep -qi 'phase' && rm -rf /tmp/test-pc | size: M
- [x] [S] Enhance session-start.sh — extract active task topic from dev-wiki state, output memory_search guidance line. TDD: grep -q 'memory_search' templates/.claude/hooks/session-start.sh fails (RED), add topic extraction + guidance output (GREEN) | scope: templates/.claude/hooks/session-start.sh | success: bash -n templates/.claude/hooks/session-start.sh && grep -q 'memory_search' templates/.claude/hooks/session-start.sh | size: S
- [x] [M] Update test suite — test_install.sh: flag combinations (--all, --core-only, --no-python), negative assertions, new skill dir presence. test_templates.sh: spot-check imported skills. TDD: new assertions fail (RED), add test cases (GREEN), verify make test <30s (REFACTOR) | scope: tests/test_install.sh, tests/test_templates.sh | success: make test && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -d "$THOME/.claude/skills/dev-plan" && test -d "$THOME/.claude/skills/wiki-query" && rm -rf "$THOME" | size: M

<!-- phase:phase-16-enforce-the-loop -->
<!-- gates: spec=approved approach=yes plan-review=7/10(revised) tasks=yes -->
## Phase 16: Enforce the Loop

- [x] [M] enforce-spec.sh — PreToolUse hook for spec enforcement: `bash -n templates/.claude/hooks/enforce-spec.sh` fails (RED), implement: parse JSON stdin via Python for file_path, check .claude/enforce marker, path allowlist, active-phase.md gate check, specs/<slug>.md validity check with `grep -qE '^\- \[ \] `.+`'`, exit 2 if no approved spec (GREEN), verify allowlist covers all meta paths (REFACTOR) | scope: templates/.claude/hooks/enforce-spec.sh | success: `bash -n templates/.claude/hooks/enforce-spec.sh && echo '{"tool_name":"Write","input":{"file_path":"src/main.py"}}' | bash templates/.claude/hooks/enforce-spec.sh; [ $? -eq 0 ] && T=$(mktemp -d) && mkdir -p "$T/.claude/rules" "$T/.dev-wiki" && printf 'Phase: 99 - test\n' > "$T/.claude/rules/active-phase.md" && touch "$T/.claude/enforce" && echo '{"tool_name":"Write","input":{"file_path":"src/x.py"}}' | (cd "$T" && bash "$OLDPWD/templates/.claude/hooks/enforce-spec.sh") 2>/dev/null; [ $? -eq 2 ] && rm -rf "$T"` | size: M
- [x] [M] enforce-loop.sh — Stop hook for loop enforcement: `bash -n templates/.claude/hooks/enforce-loop.sh` fails (RED), implement: check .claude/enforce + .dev-wiki existence, read active phase slug from active-phase.md, run file-existence exit criteria from specs/<slug>.md, count open tasks in active phase section, check debrief journal entry via git log + glob, advisory stdout for debrief/open-tasks (GREEN), verify no false blocking on empty state (REFACTOR) | scope: templates/.claude/hooks/enforce-loop.sh | success: `bash -n templates/.claude/hooks/enforce-loop.sh && echo '{}' | bash templates/.claude/hooks/enforce-loop.sh; [ $? -eq 0 ] && T=$(mktemp -d) && mkdir -p "$T/.claude/rules" "$T/.dev-wiki" "$T/specs" && printf 'Phase: 99 - test\n' > "$T/.claude/rules/active-phase.md" && printf '- [ ] \x60test -f '"$T"'/missing-file\x60\n' > "$T/specs/phase-99-test.md" && touch "$T/.claude/enforce" && echo '{}' | (cd "$T" && bash "$OLDPWD/templates/.claude/hooks/enforce-loop.sh") 2>/dev/null; [ $? -eq 2 ] && rm -rf "$T"` | size: M
- [x] [M] test_enforce.sh — 10 enforcement test cases: `bash tests/test_enforce.sh` fails (RED), implement fixture-based tests with temp dirs: (a) allow no .dev-wiki, (b) allow allowlisted path, (c) block no spec, (d) block stub spec, (e) allow valid spec, (f) allow gate marked [x], (g) deliverable-check pass, (h) deliverable-check fail, (i) debrief advisory output, (j) marker absent = allow. Wire into Makefile test target (GREEN), verify all 10 pass deterministically (REFACTOR) | scope: tests/test_enforce.sh, Makefile | success: `bash tests/test_enforce.sh && grep -q 'test_enforce' Makefile` | size: M
- [x] [M] install.sh — hooks module + enforce marker: `bash install.sh --dry-run 2>&1 | grep -q 'enforce-spec'` fails (RED), implement: add hooks to dev-wiki module group (requires core), create ~/.claude/hooks/ dir, copy enforce-*.sh, JSON merge for hooks section in settings.json (append to arrays, don't overwrite), create .claude/enforce marker in target project dir (GREEN), verify idempotency — run twice, diff (REFACTOR) | scope: install.sh | success: `bash install.sh --dry-run 2>&1 | grep -q 'enforce-spec' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/hooks/enforce-spec.sh" && test -f "$THOME/.claude/enforce" && rm -rf "$THOME"` | size: M
- [x] [S] test_install.sh — enforcement hook assertions: enforce hook assertions fail (RED), add test cases: --all includes hooks + marker, --core-only excludes hooks + marker, hooks JSON registration in settings.json (GREEN) | scope: tests/test_install.sh | success: `grep -q 'enforce' tests/test_install.sh && bash tests/test_install.sh` | size: S
- [x] [S] session-start.sh — enforcement status report: `grep -q 'enforce' templates/.claude/hooks/session-start.sh` fails (RED), add: check .claude/enforce existence, report "[enforcement] active" or "[enforcement] inactive (touch .claude/enforce to enable)" (GREEN) | scope: templates/.claude/hooks/session-start.sh | success: `bash -n templates/.claude/hooks/session-start.sh && grep -q 'enforce' templates/.claude/hooks/session-start.sh` | size: S

<!-- phase:phase-17-harden -->
<!-- gates: spec=approved approach=yes plan-review=7/10(revised) tasks=yes -->
## Phase 17: Harden

- [x] [M] detect-loop.sh — pure bash PostToolUse hook: `bash -n templates/.claude/hooks/detect-loop.sh` fails (RED), implement: parse command+exit_code from JSON stdin via grep/sed, append to .loop-state file, check last 3 lines for consecutive identical failed commands, emit advisory warning to stderr if loop detected, exit 0 always (GREEN), verify no false positives on alternating commands (REFACTOR) | scope: templates/.claude/hooks/detect-loop.sh | success: `bash -n templates/.claude/hooks/detect-loop.sh && T=$(mktemp -d) && mkdir -p "$T/.claude" && touch "$T/.claude/enforce" && printf '{"tool_name":"Bash","tool_input":{"command":"false"},"stdout":"","stderr":"err","exit_code":1}' | (cd "$T" && bash "$OLDPWD/templates/.claude/hooks/detect-loop.sh"); [ $? -eq 0 ] && rm -rf "$T"` | size: M
- [x] [M] session-start.sh enhancements — memory nudge + working-knowledge pruning + loop state clear: `grep -q 'sqlite3' templates/.claude/hooks/session-start.sh` fails (RED), implement: sqlite3 query of memory.db entry count with 2s timeout + cooldown file check, working-knowledge pruning ([uses:1] + >30d + not [pinned] entries moved to .stale-queue max 5), clear .loop-state file if present (GREEN), verify graceful skip when sqlite3/DB unavailable (REFACTOR) | scope: templates/.claude/hooks/session-start.sh | success: `bash -n templates/.claude/hooks/session-start.sh && grep -q 'sqlite3' templates/.claude/hooks/session-start.sh && grep -q 'stale-queue\|stale.queue' templates/.claude/hooks/session-start.sh && grep -q 'loop-state\|loop.state' templates/.claude/hooks/session-start.sh` | size: M
- [x] [M] test_harden.sh + Makefile — 8 fixture tests: `bash tests/test_harden.sh` fails (RED), implement fixture-based tests: (a) detect-loop exits 0 on non-Bash tool, (b) detect-loop exits 0 on successful command, (c) detect-loop warns on 3 identical failures, (d) detect-loop no warn on 2 identical failures, (e) session-start clears loop state, (f) session-start skips memory nudge when no DB, (g) working-knowledge pruning moves stale entry, (h) working-knowledge pruning respects pinned. Wire into Makefile test target (GREEN), verify all 8 pass deterministically (REFACTOR) | scope: tests/test_harden.sh, Makefile | success: `bash tests/test_harden.sh && grep -q 'test_harden' Makefile` | size: M
- [x] [S] install.sh — detect-loop.sh hook copy + PostToolUse registration: `bash install.sh --dry-run 2>&1 | grep -q 'detect-loop'` fails (RED), add detect-loop.sh to hooks module copy list + add PostToolUse JSON registration in settings.json merge (GREEN) | scope: install.sh | success: `bash install.sh --dry-run 2>&1 | grep -q 'detect-loop' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/hooks/detect-loop.sh" && rm -rf "$THOME"` | size: S

<!-- phase:phase-18-spec-devplan-ux -->
<!-- gates: spec=8/10(revised) approach=yes plan-review=9/10 tasks=yes -->
## Phase 18: Spec/Dev-Plan UX Unification

- [x] [S] Routing investigation + decision article — RED: `test -f .dev-wiki/articles/decisions/spec-routing-investigation.md` fails. GREEN: investigate /spec routing by checking installed skill file structure at ~/.claude/skills/spec/, testing user-side `/spec` vs agent-side `Skill(skill="spec")`, document findings as decision article. | scope: .dev-wiki/articles/decisions/spec-routing-investigation.md | success: test -f .dev-wiki/articles/decisions/spec-routing-investigation.md && grep -qi 'routing\|discovery\|Skill.*tool' .dev-wiki/articles/decisions/spec-routing-investigation.md | size: S
- [x] [M] Create spec-auto-invoke.md companion file — RED: `test -f templates/.claude/skills/dev-plan/spec-auto-invoke.md` fails. GREEN: write ~30-40 line companion defining: user notification, Skill(skill="spec", args=...) invocation, three terminal states (approved→restart Step 1, rejected→abort, failed→abort with fallback), restart protocol (re-read _CURRENT_STATE.md + tasks.md + new spec, continue from Step 1). REFACTOR: verify no circular invocation path, ensure restart instructions unambiguous. | scope: templates/.claude/skills/dev-plan/spec-auto-invoke.md | success: test -f templates/.claude/skills/dev-plan/spec-auto-invoke.md && grep -qi 'Skill.*spec\|invoke.*spec' templates/.claude/skills/dev-plan/spec-auto-invoke.md && grep -qi 'abort\|terminal' templates/.claude/skills/dev-plan/spec-auto-invoke.md && grep -qi 'Step 1' templates/.claude/skills/dev-plan/spec-auto-invoke.md | size: M
- [x] [S] Modify SKILL.md Step 0.6 — replace STOP with companion reference. Depends: Task 2. RED: `grep -q 'Run.*\/spec.*first.*STOP' templates/.claude/skills/dev-plan/SKILL.md` (STOP behavior exists). GREEN: replace Step 0.6 STOP block with ~3-line pointer to spec-auto-invoke.md. | scope: templates/.claude/skills/dev-plan/SKILL.md | success: grep -q 'spec-auto-invoke' templates/.claude/skills/dev-plan/SKILL.md && ! grep -q 'Run.*\/spec.*first.*STOP' templates/.claude/skills/dev-plan/SKILL.md && [ $(wc -l < templates/.claude/skills/dev-plan/SKILL.md) -le 350 ] | size: S
- [x] [S] Update install.sh — copy companion file in dev-wiki module. Depends: Task 2. RED: `bash install.sh --dry-run 2>&1 | grep -q 'spec-auto-invoke'` fails. GREEN: add spec-auto-invoke.md to dev-plan skill copy in dev-wiki module. | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'spec-auto-invoke' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/skills/dev-plan/spec-auto-invoke.md" && rm -rf "$THOME" | size: S
- [x] [M] Test updates + full verify. Depends: Tasks 2-4. RED: `grep -q 'spec.auto.invoke\|auto_invoke_spec' tests/test_templates.sh` fails. GREEN: add test assertions: companion file exists in templates/, SKILL.md references it, SKILL.md ≤350 lines, install.sh copies it. REFACTOR: run make test, verify all tests pass (115+ existing + new assertions). | scope: tests/test_templates.sh, tests/test_install.sh | success: grep -q 'spec.auto.invoke\|auto_invoke_spec' tests/test_templates.sh && make test | size: M

<!-- phase:phase-19-memory-wiki-bridge -->
<!-- gates: spec=8/10(revised) approach=8/10 plan-review=8/10 tasks=yes -->
## Phase 19: Memory-Wiki Bridge

- [x] [M] Channel 1 — memory-bridge.md companion + dev-plan SKILL.md pointer: write memory-bridge.md (~30-40 lines) with store protocol, category="custom", tags, content format, budget guard via memory_stats, fail-open (RED), add 2-3 line pointer in SKILL.md after Step 8a before Step 8b (GREEN), verify SKILL.md ≤350 lines and companion reads coherently standalone (REFACTOR) | scope: templates/.claude/skills/dev-plan/memory-bridge.md, templates/.claude/skills/dev-plan/SKILL.md | success: test -f templates/.claude/skills/dev-plan/memory-bridge.md && grep -q 'memory-bridge\.md' templates/.claude/skills/dev-plan/SKILL.md && [ $(wc -l < templates/.claude/skills/dev-plan/SKILL.md) -le 350 ] && grep -q 'bridge-decision' templates/.claude/skills/dev-plan/memory-bridge.md && grep -q 'memory_stats' templates/.claude/skills/dev-plan/memory-bridge.md | size: M
- [x] [S] Channel 2 — spec SKILL.md inline memory-store: add 3-5 lines after Step 6 (persist) with memory_store call, category="custom", tags=["bridge-decision"], content format, fail-open wrap (RED→GREEN) | scope: templates/.claude/skills/spec/SKILL.md | success: grep -q 'memory_store' templates/.claude/skills/spec/SKILL.md && grep -q 'bridge-decision' templates/.claude/skills/spec/SKILL.md && [ $(wc -l < templates/.claude/skills/spec/SKILL.md) -le 350 ] | size: S
- [x] [S] Channel 3 — wiki-query SKILL.md inline memory-search: add ~5-8 lines in Step 1 after search pre-pass with memory_search(question, limit=5), include as ### Memory Results in analyst Runtime Context, silent skip if unavailable/empty (RED→GREEN) | scope: templates/.claude/skills/wiki-query/SKILL.md | success: grep -q 'memory_search' templates/.claude/skills/wiki-query/SKILL.md && grep -q 'Memory Results' templates/.claude/skills/wiki-query/SKILL.md && [ $(wc -l < templates/.claude/skills/wiki-query/SKILL.md) -le 350 ] | size: S
- [x] [S] Tests — test_templates.sh assertions for all channels: add assertions for memory-bridge.md exists, SKILL.md references it, spec has memory_store, wiki-query has memory_search/Memory Results, all line counts ≤350 (RED→GREEN). Depends: Tasks 1-3. | scope: tests/test_templates.sh | success: grep -q 'memory.bridge\|memory-bridge' tests/test_templates.sh && make test | size: S

<!-- phase:phase-20-eval-harness -->
<!-- gates: spec=approved approach=yes plan-review=yes tasks=yes -->
## Phase 20: Eval Harness

- [x] [L] Eval runner infrastructure + bootstrap scenario — Create scripts/eval-runner.sh with jq check, scenario discovery, HOME isolation, fixture staging from scenario.json manifests, binary scoring, category breakdown, and report output. Create eval/corpus/ skeleton and one bootstrap hook scenario. RED: test -f scripts/eval-runner.sh fails. GREEN: Implement runner with jq check, scenario iteration, per-scenario tmpdir isolation (HOME=$(mktemp -d), trap cleanup EXIT), fixture staging from manifest, execution, pass/fail scoring, category averages, summary report with Score: line. Create eval/corpus/hook-enforce-spec-no-marker/ as bootstrap. REFACTOR: Verify runner handles empty corpus gracefully, verify cleanup runs on error. | scope: scripts/eval-runner.sh, eval/corpus/hook-enforce-spec-no-marker/ | success: test -f scripts/eval-runner.sh && bash -n scripts/eval-runner.sh && bash scripts/eval-runner.sh 2>&1 | grep -qE 'Score: [0-9]' && bash scripts/eval-runner.sh 2>&1 | grep -q 'hook' | size: L
- [x] [S] Hook contract schemas — Derive JSON input schemas from field-access patterns in each hook script. Create 4 schema files in eval/schemas/. RED: test -d eval/schemas fails. GREEN: Read each hook in templates/.claude/hooks/ to identify parsed fields. Write eval/schemas/pre-tool-use.json, post-tool-use.json, stop.json, session-start.json. REFACTOR: Validate each schema with jq empty; verify field names match grep of actual hook scripts. | scope: eval/schemas/ | success: test -d eval/schemas && [ $(ls eval/schemas/*.json 2>/dev/null | wc -l) -ge 4 ] && jq empty eval/schemas/*.json | size: S
- [x] [M] Hook fidelity corpus — Create 7+ additional hook scenarios (8 total). Cover 5 hooks: enforce-spec (block no spec, allow valid spec, allow gate marked), enforce-loop (deliverable pass, deliverable fail), detect-loop (3 identical failures warning, no warn on 2), session-start (populated dev-wiki), pre-compact (active phase output). Depends: Tasks 1+2. RED: [ $(find eval/corpus -name 'scenario.json' | wc -l) -ge 8 ] fails. GREEN: Create 7 scenario directories with scenario.json manifests and fixture files. REFACTOR: Run full eval, verify all hook scenarios pass, no false positives. | scope: eval/corpus/hook-*/ | success: [ $(find eval/corpus/hook-* -name 'scenario.json' | wc -l) -ge 8 ] && bash scripts/eval-runner.sh 2>&1 | grep -q 'hook.*[0-9]' | size: M
- [x] [M] Skill artifact validators + corpus — Create bash validators in eval/validators/ for spec, phase-article, decision-article contracts. Create 5+ scenarios with synthetic exemplars (valid + invalid). Depends: Task 1. RED: test -d eval/validators fails. GREEN: Write eval/validators/validate-spec.sh, validate-phase-article.sh, validate-decision-article.sh. Create 5 scenario directories. REFACTOR: Verify validators reject known-bad exemplars, accept known-good ones. | scope: eval/validators/, eval/corpus/skill-*/ | success: [ $(find eval/corpus/skill-* -name 'scenario.json' | wc -l) -ge 5 ] && bash -n eval/validators/validate-spec.sh && echo '' | bash eval/validators/validate-spec.sh; [ $? -ne 0 ] && bash scripts/eval-runner.sh 2>&1 | grep -q 'skill.*[0-9]' | size: M
- [x] [S] Lifecycle compliance corpus — Create 3+ scenarios testing end-to-end hook chain behavior. Depends: Tasks 1+2. RED: [ $(find eval/corpus/lifecycle-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ] fails. GREEN: Create 3 scenario directories with complete project fixtures. Each runs multiple hooks in sequence. | scope: eval/corpus/lifecycle-*/ | success: [ $(find eval/corpus/lifecycle-* -name 'scenario.json' | wc -l) -ge 3 ] && bash scripts/eval-runner.sh 2>&1 | grep -q 'lifecycle.*[0-9]' | size: S
- [x] [S] Documentation + Makefile + final verify — Create eval/README.md. Add eval target to Makefile. Verify all exit criteria pass. Depends: Tasks 1-5. RED: test -f eval/README.md fails. GREEN: Write README, add eval: target to Makefile. Run make eval and make test. | scope: eval/README.md, Makefile | success: test -f eval/README.md && grep -q '^eval[[:space:]]*:' Makefile && make eval && make eval 2>&1 | grep -qE 'Score: [0-9]' && make test | size: S

<!-- phase:phase-21-eval-expansion -->
<!-- gates: spec=7/10(revised-to-accept) approach=8/10 plan-review=7/10(revised) tasks=yes -->
## Phase 21: Eval Expansion

- [x] [M] Runner context category — add `context)` case to eval-runner.sh with checks array (`file_exists`, `section_present`, `hook_output`), CONTEXT_TOTAL/CONTEXT_PASSED counters, context report line. TDD: grep -q 'context)' scripts/eval-runner.sh fails (RED), implement context case branch with check-type dispatch + counters + report line (GREEN), verify existing 18 scenarios still pass (REFACTOR). | scope: scripts/eval-runner.sh | success: grep -q 'context)' scripts/eval-runner.sh && grep -q 'CONTEXT_TOTAL' scripts/eval-runner.sh && make eval 2>&1 | grep -qE 'Score: 18/18' | size: M
- [x] [S] Prompt validator + scenario — validate-prompt.sh checks numbered checklist + format directive presence. TDD: test -f eval/validators/validate-prompt.sh fails (RED), write validator + create skill-prompt-valid scenario (GREEN), verify validator accepts py-review-stop-prompt.md (REFACTOR). | scope: eval/validators/validate-prompt.sh, eval/corpus/skill-prompt-* | success: test -f eval/validators/validate-prompt.sh && bash -n eval/validators/validate-prompt.sh && bash eval/validators/validate-prompt.sh templates/.claude/hooks/py-review-stop-prompt.md | size: S
- [x] [M] PostToolUse hook scenarios — 6 scenarios covering audit-log (write+skip), auto-ruff (py-file+no-ruff-fallback), scan-secrets (file+no-gitleaks-fallback). Fixture JSON uses `{"input":{"file_path":"..."}}`. TDD: count < 6 (RED), create 6 scenario dirs with manifests + fixtures (GREEN), verify all pass with make eval (REFACTOR). | scope: eval/corpus/hook-audit-*, eval/corpus/hook-auto-ruff-*, eval/corpus/hook-scan-* | success: [ $(find eval/corpus -type d -name 'hook-audit-*' -o -name 'hook-auto-ruff-*' -o -name 'hook-scan-*' | wc -l) -ge 6 ] && make eval 2>&1 | grep -qE 'Score.*100' | size: M
- [x] [M] PreToolUse + Stop hook scenarios — 7 scenarios: block-dangerous-bash (rm-rf, git-push-force, chmod-777, git-reset-hard, safe-command) with `{"input":{"command":"..."}}`, check-tests-were-run (tests-present, no-tests) with `{"tool_uses":[...]}`. TDD: count < 7 (RED), create 7 scenario dirs (GREEN), verify all pass (REFACTOR). | scope: eval/corpus/hook-block-*, eval/corpus/hook-check-tests-* | success: [ $(find eval/corpus -type d -name 'hook-block-*' -o -name 'hook-check-tests-*' | wc -l) -ge 7 ] && make eval 2>&1 | grep -qE 'Score.*100' | size: M
- [x] [M] Context scenarios — 4 scenarios: soul-sections (Voice & presence, Thinking protocol sections present), file-lifecycle (routing table sections), session-start-guidance (dev-wiki output), rules-installed (nana-soul.md + file-lifecycle.md exist after install). Depends Task 1. TDD: no context-* dirs (RED), create 4 scenario dirs with checks arrays (GREEN), verify context category 100% (REFACTOR). | scope: eval/corpus/context-* | success: [ $(find eval/corpus -type d -name 'context-*' | wc -l) -ge 3 ] && make eval 2>&1 | grep -qi 'context' | size: M
- [x] [M] Lifecycle + docs + final verify — 2-3 new lifecycle scenarios (multi-hook chains), update eval/README.md with context category docs, verify total >= 30 scenarios. Depends Tasks 1-5. TDD: total < 30 (RED), create lifecycle scenarios + update README (GREEN), verify make eval 100% + make test passes (REFACTOR). | scope: eval/corpus/lifecycle-*, eval/README.md, Makefile | success: [ $(find eval/corpus -name 'scenario.json' | wc -l) -ge 30 ] && make eval 2>&1 | grep -qE 'Score.*100' && make test | size: M

<!-- phase:phase-22-session-start-refactor -->
<!-- gates: spec=8/10 approach=yes plan-review=7/10(revised) tasks=yes -->
## Phase 22: Session-Start Refactor + v0.4.0 Ship

- [x] Extract session-start modules: test session-start.d/wk-prune.sh exists (RED), create session-start.d/ with two module files (each defines one function, no top-level side effects), update session-start.sh to source and call functions via HOOK_DIR resolution (GREEN), verify behavioral equivalence with test_harden.sh and wc -l ≤ 70 (REFACTOR) | scope: templates/.claude/hooks/session-start.sh, templates/.claude/hooks/session-start.d/* | success: test -f templates/.claude/hooks/session-start.d/wk-prune.sh && bash -n templates/.claude/hooks/session-start.d/wk-prune.sh && test -f templates/.claude/hooks/session-start.d/memory-nudge.sh && bash -n templates/.claude/hooks/session-start.d/memory-nudge.sh && [ $(wc -l < templates/.claude/hooks/session-start.sh) -le 70 ] && grep -c 'source.*session-start\.d/' templates/.claude/hooks/session-start.sh | grep -q '^2$' && bash tests/test_harden.sh | size: M
- [x] Fix scan-secrets BSD grep + update eval fixture: grep -q '\\x27' scan-secrets.sh succeeds (RED), replace \x27 with quote-break pattern '"'"' in grep pattern, update eval fixture to use single quotes (GREEN) | scope: templates/.claude/hooks/scan-secrets.sh, eval/corpus/hook-scan-secrets-pattern/* | success: ! grep -q '\\x27' templates/.claude/hooks/scan-secrets.sh && test -d eval/corpus/hook-scan-secrets-pattern && grep -rq "'" eval/corpus/hook-scan-secrets-pattern/ && make eval 2>&1 | grep -qE 'Score.*100' | size: S
- [x] Update gap analysis: grep for CLOSED Phase 19 fails (RED), close gaps 1.3 (Phase 19), 3.3 (Phase 19), 4.2 (Phase 20), mark 4.4 PARTIAL (Phase 19), update header date (GREEN) | scope: .dev-wiki/articles/roadmap-gap-analysis.md | success: grep -c 'CLOSED.*Phase' .dev-wiki/articles/roadmap-gap-analysis.md | grep -qE '^[4-9]' | size: S
- [x] Test updates: grep for session-start.d in test_templates.sh fails (RED), add file existence + bash -n assertions for wk-prune.sh and memory-nudge.sh (GREEN). Depends: Tasks 1, 2. | scope: tests/test_templates.sh | success: grep -q 'session-start\.d' tests/test_templates.sh && make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S
- [x] v0.4.0 bump + tag + push: grep -qx '0.4.0' VERSION fails (RED), write 0.4.0 to VERSION, commit, tag v0.4.0, push (GREEN). Depends: Tasks 1-4. | scope: VERSION | success: grep -qx '0.4.0' VERSION && make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S

<!-- phase:phase-23-bug-fixes-readme -->
<!-- gates: spec=8/10 approach=8/10 plan-review=n/a tasks=yes -->
## Phase 23: Bug Fixes + README Rewrite

- [x] [S] Register pre-compact.sh in settings.json — add PreCompact entry with nested format. RED: jq -e '.hooks.PreCompact' fails. GREEN: Add PreCompact entry. | scope: templates/.claude/settings.json | success: jq -e '.hooks.PreCompact' templates/.claude/settings.json && jq -e '.hooks.PreCompact[0].hooks[0].command' templates/.claude/settings.json | grep -q 'pre-compact' | size: S
- [x] [M] Add pre-compact.sh to install.sh dev-wiki module — global copy + PreCompact JSON merge + idempotency verify. RED: bash install.sh --dry-run | grep 'pre-compact' fails. GREEN: Add to dev-wiki hooks copy + JSON merge. REFACTOR: Run twice, diff settings.json. | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'pre-compact' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/hooks/pre-compact.sh" && jq -e '.hooks.PreCompact' "$THOME/.claude/settings.json" && HOME="$THOME" bash install.sh && rm -rf "$THOME" | size: M
- [x] [S] Fix memory-harvest.md API mismatches — rewrite to match memory-bridge.md. RED: grep -q 'category.*lesson' (present). GREEN: Replace categories, API shape, params. | scope: templates/.claude/skills/dev-debrief/memory-harvest.md | success: ! grep -q 'category.*lesson\|category.*constraint\|confidence' templates/.claude/skills/dev-debrief/memory-harvest.md && grep -q 'category.*custom' templates/.claude/skills/dev-debrief/memory-harvest.md && grep -q 'trust' templates/.claude/skills/dev-debrief/memory-harvest.md | size: S
- [x] [M] Rewrite README.md — v0.4.0, 90-100 lines. RED: grep -qi 'dev-plan' README.md fails. GREEN: Rewrite with 7 sections. REFACTOR: Verify claims map to installed files. | scope: README.md | success: grep -qi 'enforcement' README.md && grep -qi 'eval' README.md && grep -qi 'dev-plan' README.md && grep -qi 'dev-init' README.md && [ $(wc -l < README.md) -ge 70 ] && [ $(wc -l < README.md) -le 120 ] | size: M
- [x] [S] Update tests — PreCompact + README + memory-harvest assertions. Depends: 1-4. RED: grep -q 'PreCompact' tests/test_install.sh fails. GREEN: Add assertions. | scope: tests/test_install.sh, tests/test_templates.sh | success: grep -q 'PreCompact' tests/test_install.sh && grep -q 'memory.harvest\|memory-harvest' tests/test_templates.sh && make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S
- [x] [S] Commit + push. Depends: 1-5. | scope: * | success: git diff --quiet && make test | size: S

<!-- phase:phase-24-dx-hook-performance -->
<!-- gates: spec=7/10(revised) approach=8/10 plan-review=7/10(revised) tasks=yes -->
## Phase 24: DX + Hook Performance

- [x] [M] Migrate 6 hooks to jq — add jq guard + replace python3 -c with jq -r | scope: templates/.claude/hooks/{audit-log,auto-ruff-format,block-dangerous-bash,scan-secrets,enforce-spec,check-tests-were-run}.sh | success: ! grep -r 'python3 -c' templates/.claude/hooks/{audit-log,auto-ruff-format,block-dangerous-bash,scan-secrets,enforce-spec,check-tests-were-run}.sh && grep -q 'command -v jq' templates/.claude/hooks/audit-log.sh && grep -q 'command -v jq' templates/.claude/hooks/block-dangerous-bash.sh && make eval 2>&1 | grep -qE 'Score.*100' | size: M
- [x] [S] install.sh Getting Started output — add 3-path guide | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -qi 'dev-init' | size: S
- [x] [S] README Requirements — add before Quick Start | scope: README.md | success: grep -qi 'requirements' README.md && grep -qi 'jq' README.md | size: S
- [x] [S] Update tests — jq assertions in test_templates.sh. Depends: 1-3. | scope: tests/test_templates.sh | success: make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S
- [x] [S] Commit + push. Depends: 1-4. | scope: * | success: git diff --quiet && make test | size: S

<!-- phase:phase-25-postcommit-hook -->
<!-- gates: spec=approved approach=yes plan-review=yes tasks=yes -->
## Phase 25: PostCommit Hook

- [x] [M] post-commit.sh + settings.json -- Create PostToolUse hook: jq fail-open guard, JSON parsing for command/exit_code, git commit detection (skip --amend/--fixup/--squash), write one-line JSON to .dev-wiki/.pending-commit, emit [dev-wiki:post-commit]. All paths exit 0 (advisory only). Add PostToolUse Bash matcher to settings.json. | RED: test -f templates/.claude/hooks/post-commit.sh fails | GREEN: Implement hook (~40-60 lines) with jq fail-open guard (command -v jq || exit 0) + settings.json entry | REFACTOR: Verify fast-path exit for non-commit, --amend skip, all paths exit 0, .pending-commit JSON validity | scope: templates/.claude/hooks/post-commit.sh, templates/.claude/settings.json | success: test -f templates/.claude/hooks/post-commit.sh && bash -n templates/.claude/hooks/post-commit.sh && grep -q 'pending-commit' templates/.claude/hooks/post-commit.sh && grep -q 'dev-wiki:post-commit' templates/.claude/hooks/post-commit.sh && grep -q 'command -v jq' templates/.claude/hooks/post-commit.sh && jq -e '.hooks.PostToolUse[] | select(.hooks[].command | test("post-commit"))' templates/.claude/settings.json | size: M
- [x] [S] session-start.sh stale .pending-commit handling -- Inline check (~5 lines). If .dev-wiki/.pending-commit exists, warn and delete. | RED: grep -q 'pending-commit' templates/.claude/hooks/session-start.sh fails | GREEN: Add check block after dev-wiki state section | scope: templates/.claude/hooks/session-start.sh | success: grep -q 'pending-commit' templates/.claude/hooks/session-start.sh && bash -n templates/.claude/hooks/session-start.sh | size: S
- [x] [S] install.sh hook copy + registration -- Add post-commit.sh to dev-wiki hooks module, register PostToolUse Bash in JSON merge. | RED: bash install.sh --dry-run 2>&1 | grep -q 'post-commit' fails | GREEN: Add to hooks copy + JSON merge | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'post-commit' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/hooks/post-commit.sh" && jq -e '.hooks.PostToolUse[] | select(.hooks[].command | test("post-commit"))' "$THOME/.claude/settings.json" && rm -rf "$THOME" | size: S
- [x] [M] eval scenarios -- 3 scenarios: hook-post-commit-detected (commit success -> .pending-commit written), hook-post-commit-non-commit (fast-path skip), hook-post-commit-amend-skip (--amend -> no .pending-commit). Depends: Task 1. | RED: [ $(find eval/corpus/hook-post-commit-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ] fails | GREEN: Create 3 scenario dirs with manifests + fixtures | REFACTOR: Verify make eval 100% | scope: eval/corpus/hook-post-commit-*/ | success: [ $(find eval/corpus/hook-post-commit-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ] && make eval 2>&1 | grep -qE 'Score.*100' | size: M
- [x] [S] tests + final verify -- test_templates.sh + test_install.sh assertions for hook, settings.json, install copy. | RED: grep -q 'post.commit\|post-commit' tests/test_templates.sh fails | GREEN: Add assertions | scope: tests/test_templates.sh, tests/test_install.sh | success: make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S

<!-- phase:phase-26-memory-harness-hardening -->
<!-- gates: spec=7/10(revised) approach=8/10 plan-review=8/10 tasks=yes -->
## Phase 26: Memory & Harness Hardening

- [x] [M] Memory-bridge auto-supersede — raise ceiling 100→500, add auto-supersede step between budget guard and store: search existing bridge-decisions for same phase-slug, store new entry, memory_forget highest-scoring conflict with superseded_by. Include response schema docs (result["memory"]["id"]), 10-call cap, 1 supersede per decision max: grep -q 'memory_forget' memory-bridge.md fails (RED), rewrite companion with ceiling 500, new Step 1.5 auto-supersede (GREEN), verify 0-results path and supersession path both documented (REFACTOR) | scope: templates/.claude/skills/dev-plan/memory-bridge.md | success: grep -q 'ceiling.*500\|500.*ceiling' templates/.claude/skills/dev-plan/memory-bridge.md && grep -q 'memory_forget' templates/.claude/skills/dev-plan/memory-bridge.md && grep -q 'superseded_by' templates/.claude/skills/dev-plan/memory-bridge.md | size: M
- [x] [S] Memory-harvest supersede — raise ceiling 100→500, add supersede logic for corrections that reverse prior corrections via memory_forget: grep -q 'memory_forget' memory-harvest.md fails (RED), update ceiling, add supersede step before storing corrections (GREEN) | scope: templates/.claude/skills/dev-debrief/memory-harvest.md | success: grep -q 'ceiling.*500\|500.*ceiling' templates/.claude/skills/dev-debrief/memory-harvest.md && grep -q 'memory_forget' templates/.claude/skills/dev-debrief/memory-harvest.md | size: S
- [x] [M] Session-start crash recovery — add ~12-line block: stat mtime (macOS/Linux), git log timestamp, dual-condition check with case-insensitive Debrief grep, advisory [recovery] output: grep -q '\[recovery\]' session-start.sh fails (RED), add crash recovery block after dev-wiki state section guarded by test -f .dev-wiki/_CURRENT_STATE.md (GREEN), verify graceful exit on no .dev-wiki, no git repo, empty git log (REFACTOR) | scope: templates/.claude/hooks/session-start.sh | success: grep -q '\[recovery\]' templates/.claude/hooks/session-start.sh && bash -n templates/.claude/hooks/session-start.sh && grep -qi 'debrief' templates/.claude/hooks/session-start.sh && grep -q 'stat.*-f\|stat.*-c' templates/.claude/hooks/session-start.sh | size: M
- [x] [M] Cross-skill reference validation — new test function in test_templates.sh: grep all ~/.claude/skills/ absolute path references from skill files, map to templates/ paths, check file existence, output file:line for broken refs. Fix any broken refs found: grep -q 'cross.skill.*ref\|skill.*reference' test_templates.sh fails (RED), add test_cross_skill_references function, wire into test runner (GREEN), run and fix any broken references discovered (REFACTOR) | scope: tests/test_templates.sh, templates/.claude/skills/ | success: grep -q 'cross.skill.*ref\|skill.*reference' tests/test_templates.sh && bash tests/test_templates.sh | size: M
- [x] [XS] README Windows note — add one line to Requirements section: grep -qi 'WSL\|windows' README.md fails (RED), add Windows note after existing Requirements line (GREEN) | scope: README.md | success: grep -qi 'WSL' README.md && grep -qi 'windows' README.md | size: XS
- [x] [S] Eval scenarios for crash recovery — 2 scenarios: (a) commits newer than state with no debrief → recovery message, (b) commits newer but debrief exists → no recovery message. Depends: Task 3: find eval/corpus/hook-session-start-* -name 'scenario.json' | wc -l returns <3 (RED), create 2 scenario dirs with manifests, fixtures, expected output checks (GREEN) | scope: eval/corpus/hook-session-start-*/ | success: [ $(find eval/corpus/hook-session-start-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ] && make eval 2>&1 | grep -qE 'Score.*100' | size: S

<!-- phase:phase-27-dx-ship -->
<!-- gates: spec=9/10 approach=9/10 plan-review=8/10(revised) tasks=yes -->
## Phase 27: DX + Ship

- [x] [S] README refresh — fix stale numbers: grep -q '43-scenario' README.md fails (RED), update 5 stale numbers (43 eval scenarios x2, 28 hook fidelity, 160 tests, 43 eval in testing section), verify all other factual claims (GREEN), REFACTOR: n/a | scope: README.md | success: grep -q '43-scenario' README.md && grep -q '43 eval' README.md && grep -q '28)' README.md && grep -q '160 tests' README.md && make test | size: S
- [x] [S] README staleness regression tests — add dynamic count assertions: grep -qE 'readme.*drift|readme.*stale|README.*accuracy' tests/test_templates.sh fails (RED), add test function: compute actual eval count via find, extract README claim via grep, compare; same for test script count; fail loudly if pattern not found (GREEN), REFACTOR: n/a | scope: tests/test_templates.sh | success: grep -qE 'readme.*drift|readme.*stale|README.*accuracy' tests/test_templates.sh && bash tests/test_templates.sh | size: S
- [x] [XS] install.sh summary polish — update echo lines to list all 5 global hooks: grep -q 'detect-loop' install.sh summary section fails (RED), update hook description lines to cover enforce-spec, enforce-loop, detect-loop, post-commit, pre-compact (GREEN), REFACTOR: n/a | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -qi 'hook' && make test | size: XS
- [x] [S] VERSION bump + tag + push — bump and ship: grep -qx '0.5.0' VERSION fails (RED), write 0.5.0 to VERSION, run make test && make eval, commit, tag v0.5.0, push (GREEN), REFACTOR: n/a | scope: VERSION | success: grep -qx '0.5.0' VERSION && make test && make eval && git tag -l 'v0.5.0' | grep -q 'v0.5.0' | size: S

<!-- phase:phase-28-dx-discoverability -->
<!-- gates: spec=9/10(revised) approach=yes plan-review=8/10 tasks=yes -->
## Phase 28: DX Discoverability

- [x] [S] Assertion inventory — build change manifest: grep all 11 hook scripts for echo/printf output lines, grep all eval/corpus scenarios for stdout_contains/stderr_contains, grep test scripts for output assertions. Write mapping to .dev-wiki/.hook-prefix-inventory.md (scratch file). Verifies exactly which eval scenarios need updates. RED: no inventory exists. GREEN: run grep commands, compile mapping table. REFACTOR: review mapping for completeness, verify no hooks missed. | scope: templates/.claude/hooks/*.sh, eval/corpus/*/scenario.json, tests/*.sh | success: test -f .dev-wiki/.hook-prefix-inventory.md && grep -c 'nana:' .dev-wiki/.hook-prefix-inventory.md | grep -qE '^[1-9]' | size: S
- [x] [M] Hook prefix normalization + eval updates: modify all 11 hooks to use [nana:<hook>] prefix. Three categories: silent (jq guard only: audit-log, auto-ruff-format), semantic trigger (post-commit: keep [dev-wiki:post-commit], add [nana:post-commit]), user-facing (8 hooks: add [nana:<hook>] prefix per mapping table, session-start gets [nana:gate], [nana:memory], [nana:recovery], [nana:pending], [nana:enforce]). Update eval assertions across scenario files. Depends: Task 1. RED: grep -rL '\[nana:' templates/.claude/hooks/*.sh | wc -l returns 11. GREEN: edit all 11 hooks + eval scenarios. REFACTOR: make test && make eval. | scope: templates/.claude/hooks/*.sh, eval/corpus/hook-detect-loop-3-failures/scenario.json, eval/corpus/hook-session-start-populated/scenario.json, eval/corpus/hook-session-start-recovery-detected/scenario.json, eval/corpus/lifecycle-session-enforcement-status/scenario.json | success: grep -rL '\[nana:\|dev-wiki:' templates/.claude/hooks/*.sh | wc -l | grep -q '^0$' && make test && make eval | size: M
- [x] [M] install.sh --status: add --status flag handler. Dynamic filesystem checks: count skills, hooks, rules, memory venv, VERSION, enforcement marker. Grouped output. Add test assertions. RED: bash install.sh --status fails/no output. GREEN: add --status case (~40-60 lines). REFACTOR: verify output matches filesystem, add test assertions in test_install.sh. | scope: install.sh, tests/test_install.sh | success: bash install.sh --status 2>&1 | grep -q 'skills' && bash install.sh --status 2>&1 | grep -q 'hooks' && grep -q 'status' tests/test_install.sh && make test | size: M
- [x] [S] MANIFEST enrichment: append # descriptions section. One line per skill: # <skill-dir>: <first sentence from SKILL.md>. Existing checksum lines untouched. Add test assertion. RED: grep -c '^# [a-z]' templates/.claude/skills/MANIFEST returns 0. GREEN: extract descriptions, append section. REFACTOR: n/a. | scope: templates/.claude/skills/MANIFEST, tests/test_templates.sh | success: grep -c '^# [a-z]' templates/.claude/skills/MANIFEST | grep -qE '^[1-9][0-9]' && make test | size: S
- [x] [S] Session-start kit summary line: add [nana:kit] dynamic summary line as final output. Counts skills/hooks, checks memory, reads VERSION. <=120 chars. Depends: Task 2. RED: grep -q '\[nana:kit\]' templates/.claude/hooks/session-start.sh fails. GREEN: add dynamic count block (~8-10 lines). REFACTOR: verify output <=120 chars. | scope: templates/.claude/hooks/session-start.sh, tests/test_templates.sh | success: grep -q '\[nana:kit\]' templates/.claude/hooks/session-start.sh && bash -n templates/.claude/hooks/session-start.sh && make test | size: S
- [x] [S] Final verify: run full test + eval suite. Verify all 6 spec exit criteria. RED: not all verified in one pass. GREEN: run each criterion. REFACTOR: review for regressions. | scope: tests/, eval/, install.sh, templates/.claude/hooks/, templates/.claude/skills/MANIFEST | success: make test && make eval && bash install.sh --status 2>&1 | grep -q 'skills' && grep -rL '\[nana:\|dev-wiki:' templates/.claude/hooks/*.sh | wc -l | grep -q '^0$' && grep -c '^# [a-z]' templates/.claude/skills/MANIFEST | grep -qE '^[1-9][0-9]' && grep -q '\[nana:kit\]' templates/.claude/hooks/session-start.sh | size: S

<!-- phase:phase-29-v051-grade-push -->
<!-- gates: spec=7/10(revised) approach=yes plan-review=8/10(revised) tasks=yes -->
## Phase 29: v0.5.1 Grade Push

- [x] Root-skip fix — add `id -u` guard to 2 writability tests: `grep -q 'id -u' tests/test_sync_rules.sh` fails (RED), add skip guard before each writability test (GREEN), verify skip message is descriptive (REFACTOR) | scope: tests/test_sync_rules.sh | success: `grep -q 'id -u' tests/test_sync_rules.sh && make test` | size: S
- [x] Dev-plan companion extraction — Step 3 to companion: `test -f templates/.claude/skills/dev-plan/scope-exploration-spec.md` fails (RED), extract lines 166-177 to companion, replace with 2-line Read pointer (GREEN), verify cross-skill ref test passes + SKILL.md ≤330 (REFACTOR) | scope: templates/.claude/skills/dev-plan/SKILL.md, templates/.claude/skills/dev-plan/scope-exploration-spec.md | success: `test -f templates/.claude/skills/dev-plan/scope-exploration-spec.md && [ $(wc -l < templates/.claude/skills/dev-plan/SKILL.md) -le 330 ] && bash tests/test_templates.sh` | size: M
- [x] /nana skill — in-session skill discovery reading MANIFEST: `test -f templates/.claude/skills/nana/SKILL.md` fails (RED), create SKILL.md (~30-40 lines) reading MANIFEST via kit path marker, parsing descriptions, grouping by module (GREEN), verify standalone readability (REFACTOR) | scope: templates/.claude/skills/nana/SKILL.md | success: `test -f templates/.claude/skills/nana/SKILL.md && grep -q 'MANIFEST' templates/.claude/skills/nana/SKILL.md && grep -q 'nana-dev-kit-path' templates/.claude/skills/nana/SKILL.md && [ $(wc -l < templates/.claude/skills/nana/SKILL.md) -ge 20 ] && [ $(wc -l < templates/.claude/skills/nana/SKILL.md) -le 45 ]` | size: M
- [x] /memory-consolidate skill — Claude-powered memory consolidation: `test -f templates/.claude/skills/memory-consolidate/SKILL.md` fails (RED), create SKILL.md (~30-40 lines) with search strategy, cluster identification, merge/forget, 10-merge cap, dry-run (GREEN), verify standalone readability (REFACTOR) | scope: templates/.claude/skills/memory-consolidate/SKILL.md | success: `test -f templates/.claude/skills/memory-consolidate/SKILL.md && grep -q 'memory_search' templates/.claude/skills/memory-consolidate/SKILL.md && grep -q 'memory_forget' templates/.claude/skills/memory-consolidate/SKILL.md && [ $(wc -l < templates/.claude/skills/memory-consolidate/SKILL.md) -ge 20 ] && [ $(wc -l < templates/.claude/skills/memory-consolidate/SKILL.md) -le 45 ]` | size: M
- [x] Spec provenance marker — add `<!-- nana:approved -->` to /spec Step 6: `grep -q 'nana:approved' templates/.claude/skills/spec/SKILL.md` fails (RED), modify Step 6 to prepend marker as first line (GREEN) | scope: templates/.claude/skills/spec/SKILL.md | success: `grep -q 'nana:approved' templates/.claude/skills/spec/SKILL.md && [ $(wc -l < templates/.claude/skills/spec/SKILL.md) -le 130 ]` | size: S
- [x] Enforcement hardening — provenance OR check + event logging: `grep -q 'nana:approved' templates/.claude/hooks/enforce-spec.sh` fails (RED), implement provenance OR + JSON logging to .dev-wiki/enforcement.log with tail -n 500 truncation in both hooks (GREEN), verify backward compat + truncation (REFACTOR) | scope: templates/.claude/hooks/enforce-spec.sh, templates/.claude/hooks/enforce-loop.sh | success: `grep -q 'nana:approved' templates/.claude/hooks/enforce-spec.sh && grep -q 'enforcement.log' templates/.claude/hooks/enforce-spec.sh && grep -q 'enforcement.log' templates/.claude/hooks/enforce-loop.sh && grep -q 'tail.*500' templates/.claude/hooks/enforce-spec.sh && make eval` | size: M
- [x] Tests + MANIFEST + README — integration: `grep -q 'manifest_freshness' tests/test_templates.sh` fails (RED), add freshness test + assertions, update MANIFEST, add README note (GREEN), verify make test && make eval (REFACTOR) | scope: tests/test_templates.sh, templates/.claude/skills/MANIFEST, README.md | success: `grep -q 'manifest_freshness' tests/test_templates.sh && grep -q '# nana:' templates/.claude/skills/MANIFEST && grep -q '# memory-consolidate:' templates/.claude/skills/MANIFEST && grep -qi 'consolidat' README.md && make test && make eval` | size: M

<!-- phase:phase-30-data-driven-report-generators -->
<!-- gates: spec=7/10(revised) approach=yes plan-review=n/a(4-tasks) tasks=yes -->
## Phase 30: Data-Driven Report Generators

- [x] [M] generate-report.py updates — LAYERS 5→7, WORKFLOWS v0.5.0, categorize_files() adds Eval/Specs, replace count_tests() with test_start counting, read MANIFEST: grep -q '7\.' scripts/generate-report.py on LAYERS fails (RED), update all hardcoded content (GREEN), regenerate + verify no stale strings (REFACTOR) | scope: scripts/generate-report.py, docs/report.html | success: `! grep -q '5-Layer' docs/report.html && grep -q '7-Layer\|7 Layer' docs/report.html && ! grep -q 'MEMORY.md' docs/report.html && ! grep -q 'make.*test' scripts/generate-report.py` | size: M
- [x] [L] generate-workflow.py updates — subtitle 7-Layer, rewrite 5 flows, add Enforcement + Memory Bridge sections with diagrams, parse settings.json hooks dynamically, read MANIFEST for template purposes, document global hooks: grep -q '5-Layer' scripts/generate-workflow.py succeeds (RED), rewrite flows + add sections + dynamic hooks + MANIFEST (GREEN), regenerate + verify section headers + no stale strings (REFACTOR) | scope: scripts/generate-workflow.py, docs/workflow.html | success: `! grep -q '5-Layer' docs/workflow.html && grep -q '7-Layer\|7 Layer' docs/workflow.html && ! grep -q 'MEMORY.md' docs/workflow.html && grep -q '<h[23].*[Ee]nforcement' docs/workflow.html && grep -q '<h[23].*[Mm]emory.*[Bb]ridge' docs/workflow.html` | size: L
- [x] [M] Staleness regression test — Depends Tasks 1-2. grep -q 'report_staleness' tests/test_templates.sh fails (RED), add test function (GREEN), run and verify (REFACTOR) | scope: tests/test_templates.sh | success: `grep -q 'report_staleness' tests/test_templates.sh && make test` | size: M
- [x] [S] Final verify + README fix — Depends Tasks 1-3. Fix README stale test counts, full verify. | scope: README.md | success: `make test && make eval && grep -q '175\|17[0-9]' README.md` | size: S

<!-- phase:phase-31-integration-eval-memory-gating -->
<!-- gates: spec=7/10(revised) approach=yes plan-review=8/10 tasks=yes -->
## Phase 31: Integration Eval + Memory Gating

- [x] [M] Lifecycle eval scenario: `test -f eval/corpus/lifecycle-full-phase-cycle/scenario.json` fails (RED), create scenario.json with 4 steps (session-start -> enforce-spec allow -> post-commit detect -> enforce-loop pass) + fixture files: active-phase.md (Phase: 31, Gates: [x] spec), spec.md (with <!-- nana:approved --> provenance), current-state.md (minimal active phase), tasks.md (one open task), enforce marker, deliverable.py (GREEN), verify make eval includes new scenario and passes (REFACTOR) | scope: eval/corpus/lifecycle-full-phase-cycle/ | success: `test -f eval/corpus/lifecycle-full-phase-cycle/scenario.json && make eval 2>&1 | grep -q 'lifecycle-full-phase-cycle.*PASS'` | size: M
- [x] [M] enforce-memory.sh + session-start.sh: `test -f templates/.claude/hooks/enforce-memory.sh` fails (RED), create enforce-memory.sh (~40-50 lines): jq fail-open guard, CI bypass ($CI=true), opt-in check (~/.claude/enforce-memory), JSON parse file_path, path allowlist (copy enforce-spec.sh lines 38-42 verbatim), gate check (.claude/.memory-consulted), enforcement.log event writing with tail -n 500, [nana:enforce-memory] stderr message. Add `rm -f .claude/.memory-consulted` to session-start.sh next to .loop-state cleanup (GREEN), verify bash -n passes, block/allow paths work via inline smoke test (REFACTOR) | scope: templates/.claude/hooks/enforce-memory.sh, templates/.claude/hooks/session-start.sh | success: `bash -n templates/.claude/hooks/enforce-memory.sh && grep -q 'memory-consulted' templates/.claude/hooks/session-start.sh && grep -q 'nana:enforce-memory' templates/.claude/hooks/enforce-memory.sh && T=$(mktemp -d) && mkdir -p "$T/.claude" "$T/.dev-wiki" && touch "$T/.claude/enforce-memory" && echo '{"tool_name":"Write","input":{"file_path":"src/x.py"}}' | (cd "$T" && HOME="$T" bash "$OLDPWD/templates/.claude/hooks/enforce-memory.sh") 2>/dev/null; [ $? -eq 2 ] && touch "$T/.claude/.memory-consulted" && echo '{"tool_name":"Write","input":{"file_path":"src/x.py"}}' | (cd "$T" && HOME="$T" bash "$OLDPWD/templates/.claude/hooks/enforce-memory.sh") 2>/dev/null; [ $? -eq 0 ] && rm -rf "$T"` | size: M
- [x] [S] install.sh registration (depends: Task 2): `bash install.sh --dry-run 2>&1 | grep -q 'enforce-memory'` fails (RED), add enforce-memory.sh to dev-wiki hooks module: copy to ~/.claude/hooks/, chmod +x, dry-run output line, Python JSON merge for PreToolUse (flat {matcher: 'Write|Edit', command} format), create ~/.claude/enforce-memory marker (GREEN) | scope: install.sh | success: `bash install.sh --dry-run 2>&1 | grep -q 'enforce-memory' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -f "$THOME/.claude/hooks/enforce-memory.sh" && test -f "$THOME/.claude/enforce-memory" && grep -q 'enforce-memory' "$THOME/.claude/settings.json" && rm -rf "$THOME"` | size: S
- [x] [M] Eval scenarios for enforce-memory (depends: Task 2): `[ $(find eval/corpus/hook-enforce-memory-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ]` fails (RED), create 3-4 scenario dirs: hook-enforce-memory-block (marker + no .memory-consulted -> exit 2), hook-enforce-memory-allow (marker + .memory-consulted present -> exit 0), hook-enforce-memory-inactive (no marker -> exit 0), optionally hook-enforce-memory-no-jq if PATH manipulation works (GREEN), verify make eval 100% (REFACTOR) | scope: eval/corpus/hook-enforce-memory-*/ | success: `[ $(find eval/corpus/hook-enforce-memory-* -name 'scenario.json' 2>/dev/null | wc -l) -ge 3 ] && make eval 2>&1 | grep -qE 'Score.*100'` | size: M
- [x] [S] Tests + final verify (depends: Tasks 1-4): `grep -q 'enforce.memory\|enforce-memory' tests/test_templates.sh` fails (RED), add test assertions: enforce-memory.sh exists + bash -n, session-start.sh has memory-consulted, install.sh has enforce-memory + PreToolUse, jq guard pattern present. test_install.sh: hook copied + marker created + JSON registered (GREEN), run make test && make eval (REFACTOR) | scope: tests/test_templates.sh, tests/test_install.sh | success: `make test && make eval 2>&1 | grep -qE 'Score.*100'` | size: S

<!-- phase:phase-32-longmemeval-s-benchmark -->
<!-- gates: spec=approved approach=yes plan-review=yes tasks=yes -->
## Phase 32: LongMemEval-S Memory Benchmark

- [x] [M] Dataset exploration + scaffold: `test -f benchmark/longmemeval.py` fails (RED), download LongMemEval-S dataset, inspect JSON structure (confirm session ID format, gold answer fields, category taxonomy), validate `memory_server/storage.py` importable via sys.path, create `benchmark/` dir with skeleton `longmemeval.py` implementing `--smoke-test` flag: downloads 3 questions, indexes sessions, queries, asserts recall > 0. Hardcode DATASET_REVISION constant. Add `benchmark/data/` and `benchmark/results.json` to .gitignore (GREEN), verify `python3 benchmark/longmemeval.py --smoke-test` exits 0 (REFACTOR) | scope: benchmark/longmemeval.py, .gitignore | success: `test -f benchmark/longmemeval.py && python3 -c "import ast; ast.parse(open('benchmark/longmemeval.py').read())" && grep -q 'benchmark/results' .gitignore && grep -q 'benchmark/data' .gitignore && grep -qE 'REVISION|revision' benchmark/longmemeval.py` | size: M
- [x] [L] Full benchmark implementation (depends: Task 1): `python3 benchmark/longmemeval.py --help 2>&1 | grep -q 'dry-run'` fails (RED), implement full 500-question benchmark: per-question DB isolation (tempfile), FTS5 search with verbatim question text, recall@5 and recall@10 scoring per category and overall, hybrid mode detection (fastembed + sqlite-vec availability check), hybrid search path with embedding generation, --dry-run N flag (runs N real questions with full pipeline), JSON results output to benchmark/results.json, human-readable summary to stdout, progress reporting every 50 questions. Add `benchmark/README.md` with usage instructions and interpretation guide (GREEN), verify --dry-run 3 exits 0 and produces valid output (REFACTOR) | scope: benchmark/longmemeval.py, benchmark/README.md | success: `python3 benchmark/longmemeval.py --smoke-test && python3 benchmark/longmemeval.py --dry-run 3 && test -f benchmark/README.md && python3 -c "import ast; ast.parse(open('benchmark/longmemeval.py').read())"` | size: L
- [x] [S] README + final verify (depends: Tasks 1-2): `grep -q 'recall@5' README.md` fails (RED), add Memory Benchmark section to README.md with recall numbers placeholder, retrieval unit caveat, baseline comparison note, link to benchmark/README.md for details. Run make test to verify no regressions (GREEN) | scope: README.md | success: `grep -q 'recall@5\|LongMemEval' README.md && make test` | size: S

<!-- phase:phase-33-hybrid-retrieval-benchmark-memory-server-fixes -->
<!-- gates: spec=approved approach=approved plan-review=approved tasks=approved -->
## Phase 33: Hybrid Retrieval Benchmark + Memory Server Fixes

- [x] [M] Fix _sanitize_fts_query in storage.py — switch from token-level filtering to char-level stripping with FTS5 keyword removal. RED: python3 behavioral test asserting +, -, ^ stripped fails. GREEN: rewrite using re.sub(r'[^\w\s]', ' ', query), normalize whitespace, strip FTS5 keywords (AND/NOT/NEAR as standalone tokens), preserve existing OR-join semantics. REFACTOR: verify edge cases (C++ -> C known limitation, user's -> user s, standalone AND stripped). | scope: memory_server/storage.py | success: python3 -c "import sys; sys.path.insert(0,'memory_server'); from storage import _sanitize_fts_query; r=_sanitize_fts_query('test+foo-bar^baz'); assert '+' not in r and '-' not in r and '^' not in r, f'got: {r}'" && python3 -c "import sys; sys.path.insert(0,'memory_server'); from storage import _sanitize_fts_query; r=_sanitize_fts_query('hello AND world'); assert 'AND' not in r.split(' OR '), f'got: {r}'" | size: M
- [x] [S] Remove benchmark sanitize_query() + verify FTS5 recall doesn't regress. RED: grep -q 'def sanitize_query' benchmark/longmemeval.py succeeds (function still exists). GREEN: remove function, update evaluate_question() to pass raw question text to search_fts (safe because _sanitize_fts_query handles sanitization internally), run --dry-run 10, verify recall@5 >= 91%. Depends: Task 1. | scope: benchmark/longmemeval.py | success: ! grep -q 'def sanitize_query' benchmark/longmemeval.py && python3 benchmark/longmemeval.py --smoke-test | size: S
- [x] [M] Install hybrid deps + implement hybrid indexing in benchmark. RED: hybrid mode unavailable or silently degrades to FTS5. GREEN: pip install fastembed + sqlite-vec into ~/.claude/memory_server/.venv/. In evaluate_question(): move EmbeddingProvider creation before indexing loop, collect all session texts, call embedder.embed_batch(texts), pass embedding=embeddings[i] to each store() call. After indexing, check _vec_available with diagnostic message if False. Verify memories_vec has rows. REFACTOR: --dry-run 3 --mode hybrid produces actual HYBRID results. Depends: Task 2. | scope: benchmark/longmemeval.py | success: python3 benchmark/longmemeval.py --dry-run 3 --mode hybrid 2>&1 | grep -q 'HYBRID Results\|hybrid.*recall' | size: M
- [x] [M] Run full benchmark + update docs. RED: grep -q 'hybrid' benchmark/README.md fails (no hybrid results). GREEN: run --dry-run 50 --mode both as checkpoint, then --full --mode both. Update benchmark/README.md with FTS5 vs hybrid comparison table. Update README.md benchmark section with hybrid numbers. If hybrid recall@5 within 1% of FTS5, note explicitly. Depends: Task 3. | scope: benchmark/README.md, README.md | success: grep -q 'hybrid' benchmark/README.md && grep -qi 'hybrid\|embedding' README.md && make test | size: M

<!-- phase:phase-34-upstream-sync-store-opt-ts-design -->
<!-- gates: spec=approved approach=approved plan-review=approved tasks=approved -->
## Phase 34: Upstream Sync + store() Optimization + TypeScript Design Spec

- [x] [M] Upstream sync + divergence inventory — Read nanaclaw upstream storage.py HEAD from https://github.com/j-wanger/nanaclaw, compare _sanitize_fts_query implementations, create surgical patch at patches/nanaclaw-sanitize-fts.patch, write divergence inventory at .dev-wiki/articles/decisions/nanaclaw-divergence-inventory.md documenting all kit-only vs shared functions. If repo inaccessible: document and skip patch. TDD: test -f patches/nanaclaw-sanitize-fts.patch fails (RED), fetch upstream, diff, create patch + divergence article (GREEN), verify vendored functions survive: grep -q 'def search_hybrid' memory_server/storage.py && grep -q 'def export_memories' memory_server/storage.py (REFACTOR) | scope: patches/*, .dev-wiki/articles/decisions/nanaclaw-divergence-inventory.md | success: (test -f patches/nanaclaw-sanitize-fts.patch || grep -qi 'inaccessible\|skip' .dev-wiki/articles/decisions/nanaclaw-divergence-inventory.md) && test -f .dev-wiki/articles/decisions/nanaclaw-divergence-inventory.md && grep -q 'def search_hybrid' memory_server/storage.py | size: M
- [x] [M] Baseline tests/test_memory.sh — Write threshold equivalence tests before optimization. TDD: test -f tests/test_memory.sh fails (RED), create Python-based test harness with cosine boundaries (0.84, 0.86, 0.91), word-overlap boundaries (0.89, 0.91), FTS5-only mode (_vec_available=False), n=1 edge case (GREEN), wire into Makefile (REFACTOR) | scope: tests/test_memory.sh, Makefile | success: test -f tests/test_memory.sh && bash tests/test_memory.sh && grep -q 'test_memory' Makefile && grep -q 'overlap\|duplicate' tests/test_memory.sh | size: M
- [x] [S] Optimize cosine path in _find_near_duplicate — Replace full scan with vec0 KNN LIMIT 50, apply _cosine_similarity() on candidates only. Depends: Task 2. TDD: baseline tests pass (RED), replace SELECT * full scan with vec0 KNN query targeting memories_vec (GREEN), verify tests still pass (REFACTOR) | scope: memory_server/storage.py | success: bash tests/test_memory.sh && awk '/^def _find_near_duplicate/,/^def [a-z]/' memory_server/storage.py | grep -q 'memories_vec' | size: S
- [x] [S] Optimize word-overlap path in _find_near_duplicate — Replace full scan with FTS5 MATCH LIMIT 50, apply word-overlap on candidates only. Depends: Task 2. TDD: baseline tests pass (RED), replace SELECT * full scan with FTS5 candidate query (GREEN), verify tests still pass, run make test (REFACTOR) | scope: memory_server/storage.py | success: bash tests/test_memory.sh && ! (awk '/^def _find_near_duplicate/,/^def [a-z]/' memory_server/storage.py | grep -q 'SELECT \* FROM memories WHERE active = 1') && make test | size: S
- [x] [M] TypeScript design spec — Write specs/ts-init-design.md mapping all py-init assumptions to TS equivalents. Enumerate: package manager (npm/pnpm/bun), config (tsconfig.json + package.json), project layout, linter/formatter (eslint+prettier vs biome), type checker (tsc), test runner (vitest/jest), pre-commit hooks. Address AGENTS.md template, install.sh module group, CI template, monorepo edge cases. TDD: test -f specs/ts-init-design.md fails (RED), read py-init skill files, enumerate every assumption with TS mapping (GREEN), verify completeness (REFACTOR) | scope: specs/ts-init-design.md | success: test -f specs/ts-init-design.md && [ $(wc -l < specs/ts-init-design.md) -ge 80 ] && grep -q 'tsconfig' specs/ts-init-design.md && grep -q 'package.json' specs/ts-init-design.md && grep -q 'AGENTS' specs/ts-init-design.md | size: M

<!-- gate-log:phase-35 spec=approved approach=8/10 plan-review=8/10 tasks=yes -->
<!-- phase:phase-35-ts-init-implementation -->
<!-- gates: spec=approved approach=8/10 plan-review=8/10 tasks=yes -->
## Phase 35: ts-init Implementation

- [x] [S] AGENTS-ts.md template — adapt from AGENTS.md, swap toolchain sections (uv→pnpm, ruff→biome, mypy→tsc, pytest→vitest), add Build section. RED: test -f templates/AGENTS-ts.md fails. GREEN: Write AGENTS-ts.md. REFACTOR: Verify ≤95 lines. | scope: templates/AGENTS-ts.md | success: test -f templates/AGENTS-ts.md && grep -q 'pnpm' templates/AGENTS-ts.md && grep -q 'biome' templates/AGENTS-ts.md && grep -q 'vitest' templates/AGENTS-ts.md && grep -q 'tsc' templates/AGENTS-ts.md && [ $(wc -l < templates/AGENTS-ts.md) -le 95 ] | size: S
- [x] [M] ts-init/SKILL.md — two-mode skill, node/pnpm prereq check, ES2023 target, ESM default, references scanner.md + transform.md. RED: test -f templates/.claude/skills/ts-init/SKILL.md fails. GREEN: Write SKILL.md (~160-170 lines). REFACTOR: Verify cross-refs + design decisions encoded. | scope: templates/.claude/skills/ts-init/SKILL.md | success: test -f templates/.claude/skills/ts-init/SKILL.md && grep -q 'scanner.md' templates/.claude/skills/ts-init/SKILL.md && grep -q 'transform.md' templates/.claude/skills/ts-init/SKILL.md && grep -qi 'new project\|new-project' templates/.claude/skills/ts-init/SKILL.md && grep -qi 'existing project\|existing-project' templates/.claude/skills/ts-init/SKILL.md && grep -qi 'pnpm' templates/.claude/skills/ts-init/SKILL.md && grep -qi 'es2023\|ES2023' templates/.claude/skills/ts-init/SKILL.md && [ $(wc -l < templates/.claude/skills/ts-init/SKILL.md) -le 200 ] | size: M
- [x] [M] ts-init/scanner.md — 10-dimension feasibility scanner, React/Next.js Biome warning, monorepo blocking detection. RED: test -f templates/.claude/skills/ts-init/scanner.md fails. GREEN: Write scanner.md (~100-110 lines). REFACTOR: Verify React + monorepo paths. | scope: templates/.claude/skills/ts-init/scanner.md | success: test -f templates/.claude/skills/ts-init/scanner.md && grep -qi 'compatible' templates/.claude/skills/ts-init/scanner.md && grep -qi 'upgradeable' templates/.claude/skills/ts-init/scanner.md && grep -qi 'blocking' templates/.claude/skills/ts-init/scanner.md && grep -qi 'react\|next' templates/.claude/skills/ts-init/scanner.md && grep -qi 'monorepo\|turbo\|pnpm-workspace' templates/.claude/skills/ts-init/scanner.md && [ $(wc -l < templates/.claude/skills/ts-init/scanner.md) -le 130 ] | size: M
- [x] [M] ts-init/transform.md — per-dimension upgrade paths for existing projects. RED: test -f templates/.claude/skills/ts-init/transform.md fails. GREEN: Write transform.md (~90-100 lines). REFACTOR: Verify all upgradeable dimensions have a transform. | scope: templates/.claude/skills/ts-init/transform.md | success: test -f templates/.claude/skills/ts-init/transform.md && grep -qi 'biome' templates/.claude/skills/ts-init/transform.md && grep -qi 'vitest' templates/.claude/skills/ts-init/transform.md && grep -qi 'husky\|lint-staged' templates/.claude/skills/ts-init/transform.md && [ $(wc -l < templates/.claude/skills/ts-init/transform.md) -le 120 ] | size: M
- [x] [S] ci-ts.yml template — pnpm/action-setup, biome check, tsc --noEmit, vitest --run. RED: test -f templates/.github/workflows/ci-ts.yml fails. GREEN: Write ci-ts.yml (~50 lines). | scope: templates/.github/workflows/ci-ts.yml | success: test -f templates/.github/workflows/ci-ts.yml && grep -q 'pnpm' templates/.github/workflows/ci-ts.yml && grep -q 'biome' templates/.github/workflows/ci-ts.yml && grep -q 'tsc' templates/.github/workflows/ci-ts.yml && grep -q 'vitest' templates/.github/workflows/ci-ts.yml && grep -qi 'setup-node\|actions/setup-node' templates/.github/workflows/ci-ts.yml | size: S
- [x] [S] install.sh typescript module — add module group, --no-typescript flag, --core-only disables typescript, source validation, MANIFEST ts-init line. Depends: Tasks 1-5. RED: bash install.sh --dry-run 2>&1 | grep -q 'ts-init' fails. GREEN: Add module + flag + MANIFEST. REFACTOR: Verify idempotency + --core-only exclusion. | scope: install.sh, templates/.claude/skills/MANIFEST | success: bash install.sh --dry-run 2>&1 | grep -q 'ts-init' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -d "$THOME/.claude/skills/ts-init" && test -f "$THOME/.claude/skills/ts-init/SKILL.md" && rm -rf "$THOME" && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh --no-typescript && test ! -d "$THOME/.claude/skills/ts-init" && rm -rf "$THOME" && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh --core-only && test ! -d "$THOME/.claude/skills/ts-init" && rm -rf "$THOME" | size: S
- [x] [M] Tests — test_install.sh + test_templates.sh assertions for all ts-init artifacts. Depends: Tasks 1-6. RED: grep -q 'ts-init\|ts_init' tests/test_templates.sh fails. GREEN: Add assertions. REFACTOR: make test passes. | scope: tests/test_install.sh, tests/test_templates.sh | success: grep -q 'ts-init\|ts_init' tests/test_templates.sh && grep -q 'ts-init\|ts_init\|no-typescript' tests/test_install.sh && make test | size: M

<!-- gates: spec=9/10 approach=yes plan-review=8/10 tasks=yes -->
<!-- phase:phase-36-hooks-audit-housekeeping -->
## Phase 36: Hooks Audit & Housekeeping

- [x] Capture hook error evidence + global/kit diff inventory: test no evidence file exists (RED), grep recent session JSONLs + .dev-wiki/enforcement.log + diff -r templates/.claude/hooks ~/.claude/hooks into evidence file with quoted errors OR "no errors found" branch listing what was checked (GREEN) | scope: .dev-wiki/articles/decisions/hook-error-evidence.md | success: test -f .dev-wiki/articles/decisions/hook-error-evidence.md && [ $(wc -l < .dev-wiki/articles/decisions/hook-error-evidence.md) -ge 5 ] | size: S
- [x] Write per-hook reconciliation decision article for 6 global-only hooks: test reconciliation file doesn't exist (RED), write decision article with explicit disposition (backport/delete/tolerate) per hook + rationale (GREEN) | scope: .dev-wiki/articles/decisions/hook-reconciliation.md | success: for h in context-size-check dev-wiki-post-commit dev-wiki-scope-check post-compact session-stop stale-queue; do grep -qE "$h.*(backport|delete|tolerate)" .dev-wiki/articles/decisions/hook-reconciliation.md || exit 1; done | size: S
- [x] Static lint sweep on all kit hooks: lint failures unknown (RED), run bash -n on every hook, verify set -euo pipefail present, verify jq fail-open guard on hooks that use jq per jq-hook-migration decision, record findings inline in commit message and feed flagged hooks into Task 4 (GREEN), document Claude Code stdin contract version checked against (REFACTOR) | scope: templates/.claude/hooks/*.sh | success: for f in templates/.claude/hooks/*.sh; do bash -n "$f" || exit 1; grep -q 'set -euo pipefail' "$f" || exit 1; if grep -q 'jq ' "$f"; then grep -q 'command -v jq' "$f" || exit 1; fi; done | size: M
- [x] Apply targeted hook fixes + backport hooks per Task 2 disposition: hooks fail evidence/lint checks (RED), apply each fix pinned to quoted evidence string or specific lint finding in commit message, backport global-only hooks marked for backport, test each fix in mktemp -d tmpdir before touching live state (GREEN), update install.sh if any new hook registered and update test_install.sh + test_enforce.sh fixtures accordingly (REFACTOR) | scope: templates/.claude/hooks/*.sh, install.sh, tests/test_install.sh, tests/test_enforce.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'enforce-spec' && for f in templates/.claude/hooks/*.sh; do bash -n "$f"; done && make test | size: M→L (USER OVERRIDE)
- [x] README ts-init coverage + stale 201 sweep across repo: README lacks /ts-init + stale count present (RED), add /ts-init line to Quick Start, --no-typescript row to Installer Flags table, TypeScript Quality entry to Skills by Module, Node version line to Requirements derived from ci-ts.yml matrix (cite source in commit), sweep stale 201 references across README + Makefile + install.sh + benchmark/README.md, set actual test count LAST after Task 4 completes (GREEN) | scope: README.md, Makefile, install.sh, benchmark/README.md | success: grep -q '/ts-init' README.md && grep -q 'no-typescript' README.md && grep -q 'TypeScript' README.md && ! grep -rn '\b201\b' README.md Makefile install.sh benchmark/README.md 2>/dev/null | grep -iE 'test|scenario' | size: M
- [x] Submit nanaclaw sanitizer fix as upstream PR: PR URL not recorded (RED), check upstream HEAD applies patch cleanly, clone fork + branch from upstream main + git am patches/nanaclaw-sanitize-fts.patch + push + gh pr create, record URL OR skipped:reason in decision article, verify git log upstream/main..HEAD --name-only shows exactly 1 commit with no nana-dev-kit/ paths (GREEN) | scope: .dev-wiki/articles/decisions/nanaclaw-upstream-pr.md | success: test -f .dev-wiki/articles/decisions/nanaclaw-upstream-pr.md && grep -qE 'https://github.com/.+/pull/[0-9]+|skipped:' .dev-wiki/articles/decisions/nanaclaw-upstream-pr.md | size: S
- [x] TS polish spot-check: ts-init outputs unaudited (RED), read SKILL.md + scanner.md + transform.md + AGENTS-ts.md + ci-ts.yml, identify any issues matching spec's 4-item "surfaced" definition (broken wikilink, stale tool version vs ci-ts.yml matrix, typo, reference to non-existent file/skill), fix only those (GREEN) | scope: templates/.claude/skills/ts-init/*.md, templates/AGENTS-ts.md, templates/.github/workflows/ci-ts.yml | success: NODE_CI=$(grep -oE 'node-version: .[0-9]+' templates/.github/workflows/ci-ts.yml | head -1 | grep -oE '[0-9]+') && NODE_AGENTS=$(grep -oE 'Node\.?(js)?[ .]+[0-9]+' templates/AGENTS-ts.md | head -1 | grep -oE '[0-9]+') && [ "$NODE_CI" = "$NODE_AGENTS" ] | size: S
- [x] Cleanup + MANIFEST regen + full test/eval: .hook-prefix-inventory.md leaks risk + MANIFEST possibly stale (RED), delete .hook-prefix-inventory.md OR add to .gitignore, regenerate templates/.claude/skills/MANIFEST if any skill file under it changed, run make test + make eval (GREEN) | scope: .dev-wiki/.hook-prefix-inventory.md, .gitignore, templates/.claude/skills/MANIFEST | success: (! test -f .dev-wiki/.hook-prefix-inventory.md || grep -q '.hook-prefix-inventory.md' .gitignore) && make test && make eval | size: S

<!-- gate-log:phase-37 direction=approved delivery=approved -->
<!-- phase:phase-37-ceremony-streamlining-autonomous-flow -->
## Phase 37: Ceremony Streamlining & Autonomous Flow

- [x] Extract plan-review Steps 7.5/7.6 to companion: test -f templates/.claude/skills/dev-plan/plan-review-companion.md fails (RED), create companion with current Steps 7.5/7.6 content and replace in SKILL.md with 2-line pointer (GREEN), verify SKILL.md ≤ 315 lines after extraction (REFACTOR) | scope: templates/.claude/skills/dev-plan/SKILL.md, templates/.claude/skills/dev-plan/plan-review-companion.md | success: test -f templates/.claude/skills/dev-plan/plan-review-companion.md && grep -q 'plan-review-companion' templates/.claude/skills/dev-plan/SKILL.md && [ $(wc -l < templates/.claude/skills/dev-plan/SKILL.md) -le 315 ] | size: S
- [x] Rewrite dev-plan ceremony — direction-only approval: grep -q 'agent-internal' templates/.claude/skills/dev-plan/SKILL.md fails (RED), modify SKILL.md: Step 0.6 silent spec invocation via --internal, Step 6.5 agent-internal (incorporate findings no user block), preserve Step 7 as direction gate, Step 7.5 pointer as agent-internal, remove Step 7.6, Step 8f 2-gate template (direction + delivery), Step 9 auto-transition, update Orchestrator Protocol and Hard Gate sections (GREEN), verify ≤ 350 lines and no orphan gate references (REFACTOR) | scope: templates/.claude/skills/dev-plan/SKILL.md | success: grep -q 'agent-internal' templates/.claude/skills/dev-plan/SKILL.md && ! grep -q 'Step 7\.6' templates/.claude/skills/dev-plan/SKILL.md && ! grep -q 'Spec reviewed.*Tier' templates/.claude/skills/dev-plan/SKILL.md && grep -q 'direction' templates/.claude/skills/dev-plan/SKILL.md && grep -q 'delivery' templates/.claude/skills/dev-plan/SKILL.md && [ $(wc -l < templates/.claude/skills/dev-plan/SKILL.md) -le 350 ] | size: M
- [x] Add --internal mode to spec SKILL.md: grep -q '\-\-internal' templates/.claude/skills/spec/SKILL.md fails (RED), add mode detection after Step 1: when args contain "internal", auto-run Steps 2-4 and Tier 0/1, incorporate findings, persist with marker, skip Step 5 user approval (~15 lines), direct /spec unchanged (GREEN) | scope: templates/.claude/skills/spec/SKILL.md | success: grep -q '\-\-internal' templates/.claude/skills/spec/SKILL.md && grep -q 'auto-incorporate\|skip.*approval' templates/.claude/skills/spec/SKILL.md && [ $(wc -l < templates/.claude/skills/spec/SKILL.md) -le 160 ] | size: S
- [x] Create delivery report script: test -f scripts/generate-delivery-report.py fails (RED), create Python script (~200-300 lines): reads git diff + tasks.md + decision articles, runs make test/eval, produces HTML report with phase summary, task completion, files changed, test/eval delta, decisions, scannable layout with inline CSS (GREEN), verify script runs with --help and produces valid HTML on current repo (REFACTOR) | scope: scripts/generate-delivery-report.py | success: test -f scripts/generate-delivery-report.py && python3 scripts/generate-delivery-report.py --help 2>&1 | grep -qi 'usage\|delivery\|report' | size: M
- [x] Modify dev-debrief — delivery gate + auto-commit/push: grep -q 'delivery.*report\|generate-delivery-report' templates/.claude/skills/dev-debrief/SKILL.md fails (RED), add Step 4.8 (generate delivery report), Step 4.9 (present to user: accept/reject/fix), Step 7 (auto-commit all changes + push after acceptance), create companion if needed (GREEN), verify ≤ 350 lines (REFACTOR) | scope: templates/.claude/skills/dev-debrief/SKILL.md, templates/.claude/skills/dev-debrief/delivery-flow.md | success: grep -q 'delivery.*report\|generate-delivery-report' templates/.claude/skills/dev-debrief/SKILL.md && grep -q 'auto.*commit\|auto-commit' templates/.claude/skills/dev-debrief/SKILL.md && [ $(wc -l < templates/.claude/skills/dev-debrief/SKILL.md) -le 350 ] | size: M
- [x] Update gate enforcement — 2-gate model: grep -q 'Spec reviewed' templates/.claude/skills/dev-plan/implementation-guide.md (old 5-gate reference exists) (RED), update implementation-guide.md pre-flight (2 gates: direction + delivery), update dev-wiki-hooks.md gate references, update task-schema.md gate log format and example, update dev-debrief gate compliance audit references (GREEN), verify no orphan 5-gate references across modified files (REFACTOR) | scope: templates/.claude/skills/dev-plan/implementation-guide.md, .claude/rules/dev-wiki-hooks.md, templates/.claude/skills/dev-plan/task-schema.md, templates/.claude/skills/dev-debrief/SKILL.md | success: ! grep -q 'Spec reviewed.*Tier\|Tasks approved by user' templates/.claude/skills/dev-plan/implementation-guide.md && grep -q 'direction' templates/.claude/skills/dev-plan/implementation-guide.md && grep -q 'direction=\|delivery=' templates/.claude/skills/dev-plan/task-schema.md | size: M
- [x] Tests + MANIFEST + final verify: grep -q 'plan-review-companion' templates/.claude/skills/MANIFEST fails (RED), regenerate MANIFEST, add test assertions for new companion files + --internal mode + delivery report + 2-gate model + auto-commit references in test_templates.sh (GREEN), run make test && make eval, verify all pass (REFACTOR) | scope: templates/.claude/skills/MANIFEST, tests/test_templates.sh, tests/test_install.sh | success: grep -q 'plan-review-companion' templates/.claude/skills/MANIFEST && grep -q 'delivery.report\|delivery-report' tests/test_templates.sh && make test && make eval 2>&1 | grep -qE 'Score.*100' | size: M

<!-- gate-log:phase-38 spec=approved(internal) approach=approved plan-review=8/10(accepted) tasks=approved -->
<!-- phase:phase-38-install-integrity-functional-verification -->
## Phase 38: Install Integrity & Functional Verification

- [x] [S] Add 5 skills to install.sh — grep -q 'memory-consolidate' install.sh fails (RED), add nana+memory-consolidate to CORE_SKILLS, add py-lint+py-review+py-test to PYTHON_SKILLS (GREEN) | scope: install.sh | success: bash install.sh --dry-run 2>&1 | grep -q 'nana' && bash install.sh --dry-run 2>&1 | grep -q 'py-lint' && bash install.sh --dry-run 2>&1 | grep -q 'memory-consolidate' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -d "$THOME/.claude/skills/nana" && test -d "$THOME/.claude/skills/py-lint" && test -d "$THOME/.claude/skills/memory-consolidate" && rm -rf "$THOME" | size: S
- [x] [S] Fix MultiEdit matchers in all code sites — test $(grep -c "Edit|Write'" install.sh) -eq 0 fails (RED), update all 7 upsert matcher strings from Write|Edit / Edit|Write to Write|Edit|MultiEdit in global hooks (~line 467: enforce-spec, enforce-memory, dev-wiki-scope-check, stale-queue) and project-local hooks (~line 127: audit-log, auto-ruff-format, scan-secrets). Also update 3 display strings in the --project-local summary output (GREEN) | scope: install.sh | success: test $(grep -c "Edit|Write'" install.sh) -eq 0 && grep -c "Write|Edit|MultiEdit" install.sh | grep -qE '^[7-9]' | size: S
- [x] [S] Fix dev-wiki-scope-check.sh field path — grep -q '.tool_input.file_path' shows bug present (RED), change .tool_input.file_path to .input.file_path (GREEN) | scope: templates/.claude/hooks/dev-wiki-scope-check.sh | success: grep -q '.input.file_path' templates/.claude/hooks/dev-wiki-scope-check.sh && ! grep -q '.tool_input.file_path' templates/.claude/hooks/dev-wiki-scope-check.sh | size: S
- [x] [S] Regenerate MANIFEST — grep -q 'nana/SKILL.md' templates/.claude/skills/MANIFEST fails (RED), regenerate with checksums + descriptions for all skills (GREEN). Depends: Task 1. | scope: templates/.claude/skills/MANIFEST | success: grep -q 'nana' templates/.claude/skills/MANIFEST && grep -q 'py-lint' templates/.claude/skills/MANIFEST && grep -q 'memory-consolidate' templates/.claude/skills/MANIFEST | size: S
- [x] [M] Add functional verification tests — new assertions fail (RED), add ~15-20 tests to test_install.sh: (a) --core-only installs nana but NOT py-lint, (b) --no-python excludes py-lint/py-review/py-test, (c) pipe fixture JSON through installed enforce-spec.sh verify exit code, (d) pipe fixture JSON through installed scope-check.sh verify .input.file_path parsed, (e) verify companion files (spec/adversarial-constraints-prompt.md, dev-plan/memory-bridge.md), (f) verify MCP cwd. Note: no eval scenario updates needed — confirmed no existing scope-check eval scenarios (GREEN). Depends: Tasks 1-4. | scope: tests/test_install.sh | success: make test && make eval 2>&1 | grep -qE 'Score.*100' | size: M
- [x] [S] Document PostToolUse inconsistency + final verify — add to Known Issues in _ARCHITECTURE.md: 3 PostToolUse hooks use .input.file_path (audit-log, auto-ruff, scan-secrets), 2 use .tool_input (post-commit, stale-queue), needs live verification (GREEN). | scope: .dev-wiki/_ARCHITECTURE.md | success: grep -qi 'PostToolUse.*field.*inconsisten\|field.*path.*PostToolUse' .dev-wiki/_ARCHITECTURE.md && make test && make eval 2>&1 | grep -qE 'Score.*100' | size: S

</details>

<!-- gate-log:phase-39 direction=approved delivery=accepted -->
## Phase 39: Resilience & Health Probes

- [x] [M] Session-start.sh MCP block rewrite (health probe + jq migration): grep 'python3 -c' session-start.sh succeeds (RED), rewrite MCP block lines 86-103: replace python3 JSON parsing with jq for settings.json reads (.mcpServers.memory.command, .mcpServers.memory.cwd), keep $MCP_CMD -c "import memory_server" functional check, add sqlite3 DB entry count with graceful fallback, emit 3-state output: "[nana:memory] server healthy (N entries)" / "[nana:memory] server broken (<reason>)" / "[nana:memory] not configured", add jq fail-open guard at MCP block entry (GREEN), verify behavioral equivalence with tmpdir test: create fixture settings.json, pipe through both old python3 and new jq extractions, compare outputs (REFACTOR) | scope: templates/.claude/hooks/session-start.sh | success: ! grep 'python3 -c' templates/.claude/hooks/session-start.sh && grep -q 'jq' templates/.claude/hooks/session-start.sh && grep -q 'memory.*healthy' templates/.claude/hooks/session-start.sh && grep -q 'memory.*broken' templates/.claude/hooks/session-start.sh && grep -q 'memory.*configured' templates/.claude/hooks/session-start.sh && bash -n templates/.claude/hooks/session-start.sh | size: M

- [x] [S] context-size-check.sh jq migration: grep -q 'python3' context-size-check.sh succeeds (RED), replace python3 -c "import sys,json; print(json.load(sys.stdin).get('transcript_path',''))" with jq -r '.transcript_path // empty', add jq fail-open guard (command -v jq || exit 0), move INPUT= assignment after guard (GREEN) | scope: templates/.claude/hooks/context-size-check.sh | success: grep -q 'jq' templates/.claude/hooks/context-size-check.sh && ! grep -q 'python3' templates/.claude/hooks/context-size-check.sh && grep -q 'command -v jq' templates/.claude/hooks/context-size-check.sh && bash -n templates/.claude/hooks/context-size-check.sh | size: S

- [x] [M] PostToolUse field path live verification + fix: grep -c '.tool_input' templates/.claude/hooks/{post-commit,stale-queue}.sh returns 2 while grep -c '.input.file_path' templates/.claude/hooks/{audit-log,auto-ruff-format,scan-secrets}.sh returns 3 — inconsistency confirmed (RED), create temporary diagnostic hook that dumps raw stdin JSON to /tmp/.posttooluse-dump, register for Write|Edit PostToolUse, trigger Write tool use, read dump to determine actual field path. Normalize ALL PostToolUse Edit/Write hooks to the verified canonical path. Add defensive fallback (jq '.input.file_path // .tool_input.file_path // empty') as transitional guard. Update eval/schemas/post-tool-use.json and eval fixtures to match (GREEN), verify make eval 100% after fixes (REFACTOR) | scope: templates/.claude/hooks/post-commit.sh, templates/.claude/hooks/stale-queue.sh, templates/.claude/hooks/audit-log.sh, templates/.claude/hooks/auto-ruff-format.sh, templates/.claude/hooks/scan-secrets.sh, eval/schemas/post-tool-use.json, eval/corpus/hook-* | success: make eval 2>&1 | grep -qE 'Score.*100' | size: M

- [x] [M] /init router skill: test -f templates/.claude/skills/init/SKILL.md fails (RED), create SKILL.md (~30-50 lines): detect language markers (pyproject.toml/setup.py/setup.cfg → Python; package.json/tsconfig.json → TypeScript), handle polyglot (both detected → present choice to user), handle empty (no markers → prompt user for language), dispatch via Skill(skill="py-init") or Skill(skill="ts-init"), include brief section headers: Detection, Routing, Fallback (GREEN), verify standalone readability and no wikilink assumptions (REFACTOR) | scope: templates/.claude/skills/init/SKILL.md | success: test -f templates/.claude/skills/init/SKILL.md && grep -qi 'py-init\|python' templates/.claude/skills/init/SKILL.md && grep -qi 'ts-init\|typescript' templates/.claude/skills/init/SKILL.md && grep -qi 'pyproject\|package.json' templates/.claude/skills/init/SKILL.md && [ $(wc -l < templates/.claude/skills/init/SKILL.md) -le 60 ] | size: M

- [x] [S] install.sh + MANIFEST update: bash install.sh --dry-run 2>&1 | grep -q '  init' fails (RED), add 'init' to CORE_SKILLS array in install.sh, regenerate MANIFEST with checksums + descriptions for all skills including init (GREEN). Depends: Task 4. | scope: install.sh, templates/.claude/skills/MANIFEST | success: bash install.sh --dry-run 2>&1 | grep -q 'init' && THOME=$(mktemp -d) && HOME="$THOME" bash install.sh && test -d "$THOME/.claude/skills/init" && rm -rf "$THOME" && grep -q '# init:' templates/.claude/skills/MANIFEST | size: S

- [x] [M] Tests + eval scenarios: new test assertions fail (RED), add test_templates.sh assertions: context-size-check has jq + no python3, session-start has 3-state health probe + no python3 -c, init/SKILL.md exists with markers, MANIFEST has init description. Add test_install.sh assertions: init skill installed after --all. Add eval scenarios: hook-context-size-jq (UserPromptSubmit hook parses transcript_path via jq), hook-session-start-memory-healthy (configured MCP → healthy output), hook-session-start-memory-broken (bad CWD → broken output). Run make test + make eval (GREEN), verify all new + existing tests pass with no regressions (REFACTOR). Depends: Tasks 1-5. | scope: tests/test_install.sh, tests/test_templates.sh, eval/corpus/hook-context-size-*, eval/corpus/hook-session-start-memory-* | success: make test && make eval 2>&1 | grep -qE 'Score.*100' | size: M

<!-- gate-log:phase-40 direction=approved delivery=pending -->
## Phase 40: install.sh Extraction & Anti-Pattern Hardening

- [x] [S] PostToolUse normalization completion — add `.tool_input.X // .input.X // empty` fallback to stale-queue.sh (file_path field) and post-commit.sh (command field). Update eval fixtures if any reference these fields. | RED: `grep -c 'tool_input.*//.*input' templates/.claude/hooks/stale-queue.sh` returns 0 | GREEN: update jq expressions in both hooks to use fallback pattern matching audit-log/auto-ruff/scan-secrets | REFACTOR: verify make eval 100% | scope: templates/.claude/hooks/stale-queue.sh, templates/.claude/hooks/post-commit.sh, eval/corpus/hook-post-commit-*, eval/corpus/hook-stale-queue-* | success: `grep -q 'tool_input.*//.*input\|input.*//.*tool_input' templates/.claude/hooks/stale-queue.sh && grep -q 'tool_input.*//.*input\|input.*//.*tool_input' templates/.claude/hooks/post-commit.sh && make eval 2>&1 | grep -qE 'Score.*100'` | size: S

- [x] [S] Phase article housekeeping — delete not-started Phase 12 duplicate (phase-12-soul-enhancement-and-memory-keeper.md), rename Phase 22 canonical (phase-22-session-start-refactor-v040.md → phase-22-session-start-refactor.md, delete old), fix Phase 24 status from "READY FOR COMPLETION" to "completed". Add /init to install.sh Getting Started output + README Quick Start. | RED: `test -f .dev-wiki/articles/phases/phase-12-soul-enhancement-and-memory-keeper.md` succeeds (duplicate exists) | GREEN: delete duplicates, rename, fix status, add /init mentions | REFACTOR: verify index.md links still resolve | scope: .dev-wiki/articles/phases/phase-12-*.md, .dev-wiki/articles/phases/phase-22-*.md, .dev-wiki/articles/phases/phase-24-*.md, install.sh, README.md | success: `! test -f .dev-wiki/articles/phases/phase-12-soul-enhancement-and-memory-keeper.md && ! test -f .dev-wiki/articles/phases/phase-22-session-start-refactor-v040.md && grep -q 'completed' .dev-wiki/articles/phases/phase-24-dx-hook-performance.md && grep -q '/init' README.md && bash install.sh --dry-run 2>&1 | grep -qi 'init'` | size: S

- [x] [M] modules.json manifest — create declarative JSON manifest at repo root defining all 5 module groups. Each module: name, depends_on (array), skills (array of dir names), hooks (array of {event, matcher, script}), flags (which CLI flags enable/disable). Include MCP server config in core module. Validate: parseable by both jq and Python, skill dirs match templates/.claude/skills/ filesystem, hook scripts match templates/.claude/hooks/ filesystem. | RED: `test -f modules.json` fails | GREEN: create modules.json with all 5 modules (core, python, typescript, dev-wiki, knowledge-wiki), add test assertions validating filesystem consistency | REFACTOR: verify jq and python3 can both parse it | scope: modules.json, tests/test_install.sh | success: `test -f modules.json && jq empty modules.json && python3 -c "import json; json.load(open('modules.json'))" && jq -r '.modules[].skills[]' modules.json | while read s; do test -d "templates/.claude/skills/$s" || (echo "missing: $s" && exit 1); done` | size: M

- [x] [M] scripts/register-settings.py — extract the duplicated inline Python into a standalone script. Subcommands: `hooks` (reads hook definitions from modules.json or stdin, upserts into settings.json with nested schema, handles flat-to-nested migration, ghost cleanup) and `mcp` (registers MCP memory server with correct CWD). Independently testable with --dry-run flag. | RED: `test -f scripts/register-settings.py` fails | GREEN: create script (~100-120 lines) with argparse CLI, upsert function (from existing install.sh Python), ghost cleanup logic, MCP registration | REFACTOR: verify `python3 scripts/register-settings.py hooks --help` works, test upsert idempotency in tmpdir | scope: scripts/register-settings.py | success: `test -f scripts/register-settings.py && python3 scripts/register-settings.py hooks --help 2>&1 | grep -qi 'usage\|settings' && python3 scripts/register-settings.py mcp --help 2>&1 | grep -qi 'usage\|mcp'` | depends: T3 | size: M

- [x] [M] install.sh refactor — replace all inline Python with calls to scripts/register-settings.py, replace hardcoded skill arrays with jq reads from modules.json. Project-local path: call register-settings.py with project-local hook definitions. Global path: call register-settings.py for hooks + MCP. Keep bash-native operations (file copies, venv setup, flag parsing, --status, --dry-run). Target: < 320 lines, zero inline Python. | RED: `grep -c "python3 -c" install.sh` returns 3 (three inline Python blocks) | GREEN: replace all 3 with register-settings.py calls, read skill lists from modules.json via jq | REFACTOR: run full existing test suite (make test) to verify functional equivalence, verify idempotency (run twice, diff) | scope: install.sh | success: `[ $(wc -l < install.sh) -lt 320 ] && ! grep -q "python3 -c" install.sh && make test` | depends: T3, T4 | size: M

- [x] [S] Functional smoke invariant + integration checklist — add to spec SKILL.md: "When a spec includes a new hook, skill, or install.sh change, exit criteria MUST include a functional test (pipe real input, check output)." Add integration checklist to dev-plan implementation-guide.md: verify registration in settings.json AND install.sh, verify functional test exists. | RED: `grep -qi 'functional.*smoke\|functional.*test.*required' templates/.claude/skills/spec/SKILL.md` fails | GREEN: add rule (~3-5 lines) to spec SKILL.md Step 4 and checklist (~5 lines) to implementation-guide.md | scope: templates/.claude/skills/spec/SKILL.md, templates/.claude/skills/dev-plan/implementation-guide.md | success: `grep -qi 'functional.*test' templates/.claude/skills/spec/SKILL.md && grep -qi 'registration.*verif\|functional.*test' templates/.claude/skills/dev-plan/implementation-guide.md` | size: S

- [x] [M] Tests + MANIFEST + final verify — add register-settings.py tests (upsert idempotency, flat-to-nested migration, ghost cleanup, MCP registration in tmpdir). Add modules.json filesystem consistency test (every skill dir exists, every hook script exists). Verify install.sh functional equivalence with all flag combinations. Update MANIFEST. Run make test && make eval. | RED: `grep -q 'register.settings\|register-settings' tests/test_install.sh` fails | GREEN: add test functions, update MANIFEST, run full verify | REFACTOR: verify all tests pass, no regressions | scope: tests/test_install.sh, tests/test_templates.sh, templates/.claude/skills/MANIFEST | success: `make test && make eval 2>&1 | grep -qE 'Score.*100' && grep -q 'register-settings' tests/test_install.sh` | depends: T1-T6 | size: M
