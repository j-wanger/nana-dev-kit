# Active Phase Context

Phase: 112 — OS-sandbox bash filesystem isolation. BUILT + ADVERSARIALLY REVIEWED 2026-06-27, 6/6 tasks [x] — app suite 242→274 green (incl. all live e2e under the sandbox), tsc + `npm run build` + `cargo check` all exit 0; READY FOR COMPLETION (delivery gate flips after the commit lands + the maintainer's standing live window-drive check). BUILD phase (SECURITY). Closed the documented Ph111 residual: string-gating arbitrary bash is INCOMPLETE BY NATURE (`python3 -c` / `node -e` / `base64 -d` / env indirection evade a literal-text pattern). A per-command OS-sandbox enforcement layer BELOW the Ph111 string-gate, macOS-first (seatbelt via `/usr/bin/sandbox-exec`), at the bash EXECUTION site for BOTH adapters through ONE guarded chokepoint. SEAM (T1/P6 CORRECTION): the seam is `createAgentSession({customTools:[createBashToolDefinition(cwd,{spawnHook})]})` — NOT `baseToolsOverride` (that's on the low-level AgentSession ctor + replaces the whole base set); the synchronous spawnHook is DOWNSTREAM of `pi.on('tool_call')` so the gate + Ph110/111 visibility provably see the ORIGINAL command. Vercel's bash = our own `execSync` (vercel-adapter.ts), routed through `runSandboxedBash`. NO `types.ts` change; the gate VERDICT-LOOP core stays UNCHANGED. T1 SPIKE proved A1/A4/A5 HELD (18 evasion vectors blocked at the syscall layer, inheritance transitive, STRICT viable — only 2 mach names needed). ADVERSARIAL REVIEW (6 finders×2 refuters + 1 re-run): 2 confirmed (canonicalize off-by-one `/w`→`/`=whole-FS → basename; `(subpath "/")` over-grant → reject `/`) + 3 dismissed-but-hardened (PATH-shadow → absolute path; strict unix-socket Docker-daemon-socket exfil → DROPPED unix-socket from strict; hardlink → seatbelt blocks the `ln`) + overclaim fixes (Pi command-match TESTED, integrity `~/.npm`/`.cache`/`.cargo` caches per A2, no-bypass grep broadened, header over-claims tightened) — all fixed, +10 regression tests. Decision [[os-sandbox-bash-fs-isolation]] (high); the named follow-on of [[host-gate-out-of-workspace-hardening]]. Umbrella spec specs/gui-harness-architecture.md (rides; Ph108-111 precedent, ADR-named).
Objective: confine DIRECT bash file-WRITES to the workspace at the OS layer on darwin — new pure `app/src/gate/sandbox/seatbelt.ts` (`isSandboxAvailable` cached; `escapeSBPL`+char-reject; `buildProfile` strict+integrity, `$TMPDIR` pinned from the TRUSTED host env; `wrapBashArgv` argv-form; `runSandboxedBash` single chokepoint) wired into Pi (`baseToolsOverride`) + Vercel (`runBash`), no-op off-darwin (Ph111 string-gate remains).
Scope: app/src/gate/sandbox/**, app/src/gate/** (confirm channel only), app/src/engine/{pi,vercel}/**, app/tests/**, specs/gui-harness-architecture.md (read-only). OUT (→Ph113): Linux landlock/bwrap + Windows job-objects; daemon/XPC-mediated writes (`defaults`/`launchctl`/`osascript`) + filesystem READ exfil; signed/notarized bundle + A3 keyring residual + rollback; command palette (axis 3); Claude Agent SDK adapter; live window-drive felt-quality check.
DECISIONS LOCKED (assumption gate): A1 write-deny INHERITS to subprocesses (accept, T1 proves first); A2 default-deny-write (accept, T1 sets the minimal allowlist); A3 C1-PRESERVE (confirmable out-of-ws bash writes stay → approval threads the target into `extraWrites`, approve-then-succeed, OS-enforced); A4 Pi `baseToolsOverride` binds + tool_call gate still sees the ORIGINAL command (accept, fallbacks BashSpawnHook→BashOperations→gate-modify); A5 STRICT (also tighten network+mach; STOP-escalate to integrity-only if T1 shows strict unusable).
T1 is the FRONT-LOADED de-risking gate: prove (a) python/node/base64/env-indirection evasions DENIED, (b) confinement INHERITS to child processes, (c) symlink-traversal DENIED, (d) `defaults write` ESCAPES (positive control = the residual), (e) legit ws/$TMPDIR write + read ALLOWED, (f) strict network/mach allowlist viable for a basic shell+DNS, (g) Pi seam substitutes bash AND the gate sees the original command, (h) a DENY surfaces as a legible tool_result error.
Key constraints:
- HONEST RESIDUALS (do NOT over-claim "residual closed"): exit criteria = "DIRECT bash file-writes confined on darwin", NOT "residual closed". Daemon/XPC writes + full filesystem READ remain even in strict mode; `sandbox-exec` is Apple-deprecated-but-functional (Chromium-seatbelt precedent); non-darwin = Ph111 string-gating ONLY; the allowlist can't enumerate every tool cache (tune as dogfooding surfaces breaks).
- The gate VERDICT-LOOP core ([[gate-confirm-approve-loop]] ConfirmationBroker/confirmingGate, key-store hard-deny) stays UNCHANGED — T4 adds an approved-targets channel beside it, not inside it. Per-command TS keeps it engine-neutral ([[engine-adapter-in-process-gate]]); the wrapper NEVER leaks into the tool-call surface (A4).
- Untrusted model-controlled paths (workspace root + approved-targets) reach SBPL → `escapeSBPL` + char-rejection is load-bearing; an over-tight strict profile that breaks normal dev work is a regression (the Ph111 alert-fatigue lesson, applied to OS enforcement).
Exit criteria: a bash file-write outside the workspace is OS-DENIED even when the Ph111 string-gate misses it (python/node/base64/env-indirection probe), inheriting to child processes, on darwin; legit ws + $TMPDIR writes + reads still work; approved out-of-ws write succeeds via the per-command `extraWrites` channel (C1-preserve); the gate verdict-loop core UNCHANGED; deterministic execute-under-profile test asserts the block (skip-loudly off-darwin); provider-roundtrip e2e hard-fails the evasion vector on darwin (`existsSync(outside)===false` ship-blocking); inert-render/CSP/capability-guard/gate-confirm NO regression; full app suite green + tsc + npm run build + cargo check all exit 0; + adversarial pre-commit review confirmed-findings fixed.
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip. T1 STOP-and-escalate: if inheritance fails OR the Pi seam isn't substitutable through the fallbacks OR strict is unusable for a basic shell → pivot / fall back to integrity-only, do NOT build on a false foundation.
Gates:
- [x] spec — umbrella spec specs/gui-harness-architecture.md (nana:approved; Ph108-111 precedent — Surface + Success Vision + the inert/gate constraints govern this slice; no separate phase spec). ADR-named, so enforce-spec's specs/phase-112-*.md glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (assumption positions taken; ledger Phase-112 block appended, all_accept:false — A3/A5 rejects resolved by revision; --gate 112 exit 0)
- [x] Delivery accepted (post-implementation report 2026-06-27; commit lands the BUILT + adversarially-reviewed phase — full gate 274/274 + tsc/build/cargo exit 0; the maintainer's live window-drive felt-quality check is deferred per Ph109-111 precedent)
