# Active Phase Context

Phase: 96 — Consumer Re-sync Rollout (consolidate-to-local + live `--update` across all 7)
Objective: realize the banked sandbox-only Phase-93 `--update` value LIVE across all 7 consuming projects, after adding a one-shot `install.sh --migrate-to-local` that consolidates `settings.json`-resident kit-hook registrations into gitignored `settings.local.json`. End state: every consumer on ONE canonical topology (`settings.local.json`), reconciled to the current kit hook set, `detect-loop` deregistered with NO ghost, signal-watch + aml-casework armed.
Scope: install.sh (new `--migrate-to-local`; existing `--update` UNCHANGED) + tests/test_install_update.sh + scripts/check-install-drift.sh + scripts/register-settings.py (reuse only) + eval/consumer-resync/** + README/MANIFEST/AGENTS + .dev-wiki/**; LIVE writes to 7 external consumer repos (signal-watch, edge-screener, aml-substrate, edge-analyst, ai-game, fate, aml-casework). OUT: in-place two-file `--update` (A1 reject); memory-layer; arming ai-game/fate; disarming anyone; user-owned ~/.claude/rules/; frozen apparatus.
Topology: settings.local.json = ready-3 (signal-watch greenfield, edge-screener, aml-substrate) via `--update`; settings.json = Group-B 4 (edge-analyst, ai-game, fate, aml-casework) via `--migrate-to-local` then `--update`. cut_hooks=["detect-loop"]; context-size-check FLAG-but-leave (global scope).
Key constraints: consolidate-to-local NOT in-place (A1); rails back up BOTH settings files before any removal + survivor smoke + revert-on-failure (A2); controls-first — seeded controls (settings.json detect-loop removed; settings.json kit-regs relocated) gate the build, clean-on-seed=instrument-dead STOP (A3); verify-by-FIRING not presence (HEU-012, bitten 5×); idempotent (re-run no-op); arming DECOUPLED — `.claude/enforce` touched only for signal-watch + aml-casework (A5); every live write dry-run-reviewed + git-snapshot-first (ai-game's 73 dirty files committed/stashed first, A4); a consumer with an unmodeled drift class STOPs for re-modeling.
Exit: tests/test_install_update.sh PASS (new fixture + idempotency + both seeded controls); `--migrate-to-local --dry-run`/real correct + idempotent; both-file backup round-trips + survivor smoke; check-install-drift --consumer flags settings.json kit-regs; make test + make eval 50/50 + kit drift 0; ALL 7 live: settings.local-only + detect-loop ghost-free + post-drift clean + idempotent + smoke, evidence table recorded; signal-watch + aml-casework armed, ai-game + fate unarmed, 3 prior-armed unchanged; Phase-93 deferred filing RESOLVED; `## Phase 96` window-events; eval/consumer-resync/run-exit-criteria.sh ALL-PASS.
Abort: HARD checkpoint after T3 before ANY live write (no live execution on direction-gate authority); per-consumer dry-run review before each apply; a destructive migration failing survivor smoke auto-reverts from the both-file backup; >3 attempts on a task → ask user skip|abort.
Spec: specs/phase-96-consumer-resync-rollout.md (nana:approved). Decision: [[consumer-resync-rollout]] (high). Ledger: Phase 96 (all_accept:false — A1 reject→consolidate, A2/A3/A4 accept, A5 reject→arm signal-watch+aml-casework).
Status: IMPLEMENTATION COMPLETE 2026-06-21 — 6/6 tasks [x]. Built install.sh --migrate-to-local (consolidate settings.json-topology onto settings.local.json; --update UNCHANGED), controls-first (test_install_update.sh 52/52), HARD checkpoint passed, applied LIVE to all 7 consumers. run-exit-criteria.sh ALL-PASS: every consumer 17-hook kit set in settings.local only, settings.json kit-clean, detect-loop ghost-free, drift CLEAN; armed={signal-watch,edge-screener,aml-substrate,edge-analyst,aml-casework} unarmed={ai-game,fate}. make test PASS, make eval 50/50, kit drift 0. Resolves the Phase-93 deferred live-application filing. In-kit refinement: check-install-drift --consumer excludes global-scope kit hooks from the cut flag. Delivery gate pending.
Gates:
- [x] Direction confirmed by user (assumption positions taken 2026-06-21; A1/A5 reject resolved into plan, no unresolved reject/don't-know; ledger Phase 96)
- [ ] Delivery accepted (post-implementation report)
