# Active Phase Context

Phase: 70 - Single-Decision Anchor-Headroom Screen
Status: PLANNED (5 tasks, direction approved 2026-05-30 via AskUserQuestion; spec `specs/phase-70-anchor-headroom-screen.md` written + reviewed 7/10→fixed). Beginning implementation at T1.
Objective: Build AND RUN the cheapest decisive go/no-go for the whole amplifier-measurement program (Phases 65-69 mostly negative; Phase 69 found the live off/on run premature — anchor degenerate + detector blind). Screen anchor EXISTENCE: does ANY non-commodity single-decision PURE-REASONING anchor have real OFF-baseline headroom (the harness-OFF bare subagent OMITS the correct behavior unprompted)? This INVERTS the Phase-69 handoff order — anchor selection is upstream of measurement; predicate-repair-first is structurally blocked (the current anchor surfaces in OFF ≥ ON ⇒ any boundary repair collapses to raw-text). Produce a frozen, pre-registered, per-anchor record + a graded program verdict (CONTINUE/PARKED/TERMINATE). NO harness-ON run. See [[amplifier-anchor-headroom-screen]].
Approach (controls-first; all 5 adversarial-review fixes adopted, un-fixed design scored 4/10): (1) DETERMINISTIC consensus criterion NOT a judge — named-clause regex/structural check, HAS-HEADROOM iff FAILS ≥4/5 with the SAME clause-id, DEGENERATE iff PASSES ≥4/5, else UNSTABLE/quarantine (guards the asymmetric false-HAS-HEADROOM = false-continue risk; the Phase-58 n=1 false-positive scar); (2) PURE-REASONING anchors only (bare-subagent ≠ harness-off for tool-users — the eval/comparison A-vs-C confound); (3) DROP long-horizon/retention (one-shot subagent can't surface multi-turn degradation — deferred to a multi-turn substrate); (4) THREE controls run FIRST (negative=look-ahead⇒DEGENERATE, positive=unknowable-fact⇒HAS-HEADROOM, middle=known-partial⇒UNSTABLE reproduced on blind re-run; any misbehavior⇒STOP instrument-broken); (5) GRADED verdict ladder (natural HAS-HEADROOM⇒CONTINUE to P71; only engineered-favorable⇒PARKED; even engineered DEGENERATE⇒TERMINATE).
Scope: eval/amplifier/anchor-screen/** (check.sh + leak-check.sh + leak-vocab.txt + fixtures/ + pre-registration.md + verdicts/ + screen-record.md), specs/, .dev-wiki/articles/** (decision amplifier-anchor-headroom-screen.md). Do NOT touch eval/amplifier/emit-proxy-vector.sh (frozen), eval/amplifier/measurability-gate.sh, or any CODE under eval/comparison|corpus|reasoning. No hooks/modules.json/settings.json/install.sh; nothing wired into make test/make eval.
Key constraints: deterministic consensus criterion (named clause-id, ≥4/5), NEVER a raw LLM-judge score. Pre-registration is load-bearing: candidate list + frozen shasum-pinned OFF prompts + per-anchor checks + base-model: field COMMITTED before any run — **T1 commits SEPARATELY before T2/T3 produce verdicts** so `git merge-base --is-ancestor <prereg> <first-verdict>` passes (a whole-phase single commit defeats pre-registration). n=5 exactly. Each OFF prompt leak-checked vs committed leak-vocab.txt (no harness rule vocab smuggling the answer). Pure-reasoning only; tool-gap candidates quarantined. Null is graded (downgrade-to-PARKED, not auto-terminate); "termination" requires the engineered-favorable also DEGENERATE; NO retry-until-green, threshold un-loosenable post-hoc. NO harness-value claim (HAS-HEADROOM = lift POSSIBLE never lift exists; disclaimer + no machine verdict token). make eval frozen 52; make test green at UNCHANGED 19 scripts (anchor-screen NOT a make-test gate → no README bump).
Exit: check.sh --selftest green (classifies planted PASS/FAIL/HEDGED + verifies prompt-shasum pins) + leak-check.sh green; pre-registration commit is ancestor of the first verdict commit; controls-first checkpoint passes (neg=DEGENERATE, pos=HAS-HEADROOM, middle=STABLE) OR records FINDING: INSTRUMENT-BROKEN; ≥6 verdict files each with anchored ^verdict: token; single anchored ^PROGRAM-VERDICT: (CONTINUE|PARKED|TERMINATE); no harness_lift=/VERDICT: harness token + "lift is possible" disclaimer present; emitter + measurability-gate + eval/comparison|corpus|reasoning git-diff-empty; make eval 52/52; make test green at 19 scripts; decision article names the Phase-71-or-disposition handoff.
Abort rule: if any control misbehaves (negative shows headroom / positive shows degenerate / middle unstable across runs+blind-re-run) ⇒ STOP + record instrument-broken (do NOT screen candidates on a broken instrument). If no deterministic check is writable even for the controls ⇒ STOP (method infeasible — a finding). If make eval drifts off 52 OR make test regresses OR a candidate needs the emitter/gate patched to screen ⇒ STOP. Before any "termination" language ⇒ confirm the engineered-favorable also DEGENERATE, else PARKED. If blocked >3 attempts, mark [blocked:], report, ask skip-or-abort. T1 is FIRST/checkpoint; T2 is the load-bearing controls checkpoint.

Result: IMPLEMENTATION COMPLETE — PROGRAM-VERDICT: TERMINATE (controls validated the instrument; all 4 candidates incl. the engineered-favorable screened DEGENERATE; headroom appeared ONLY on the positive control's fictional fact). Single-decision anchor measurement closed; no Phase-71 live run. Surviving avenues (retrieval-on-proprietary-facts, long-horizon/process-retention) named with decidable-whens. make eval 52/52, make test green at 19, frozen artifacts untouched, 12/12 exit battery.

Gates:
- [x] Direction confirmed by user (Approach A — anchor-headroom screen; chosen via AskUserQuestion 2026-05-30)
- [x] Delivery accepted (post-implementation report — accepted 2026-05-30; commit + push + debrief)
