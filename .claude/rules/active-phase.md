# Active Phase Context

Phase: 115 — Conversation memory (persist + restore the chat thread). DELIVERED + debriefed 2026-06-30, 5/5 tasks [x] (READY FOR COMPLETION). Standard ceremony. Outcome: app suite 374/374 + npm run build + cargo check exit 0. T5 adversarial review (2 finders — security + correctness, orchestrator-verified) caught + fixed 6 confirmed findings incl. 1 HIGH: saveConversation's byte-backstop could prune a non-empty thread to [] then overwrite good history with "[]" (silent total loss when one message > MAX_BYTES) → fixed (keep ≥1 message, skip-write-on-quota never clobbers prior, whole-body fail-soft). Also fixed: tool-status finalize (no restored spinner), details redactDeep, toolCalls-element shape guard, empty-root guard, MAX_BYTES→256KB. Delivery gate pending — D3 flips it after the commit lands; then /dev-plan Phase 116 (candidates: live window-drive validation / renderer-trust hardening / blocking out-of-ws READ / multi-workspace localStorage LRU).
Objective: close dogfood gap #2 — persist a bounded, structurally-redacted, per-workspace conversation to localStorage so the chat thread survives an app restart AND swaps correctly on a workspace change (also fixing the stale cross-workspace leak the Ph114 respawn introduced: the surface never remounts — same BridgeClient, no key — so the prior workspace's thread stays visible against the new workspace's fresh gate). Restore/swap is DISPLAY-ONLY (zero engine sends).
Scope: app/src/ui/conversation-store.ts (NEW), app/src/ui/chat-runtime.ts, app/src/App.tsx, app/tests/ui/**, app/src/** (review fixes only, T5), .claude/rules/active-phase.md.
Key constraints: redact at the PERSISTENCE BOUNDARY via a NEW structured walker redactForPersist (NOT the private truncating render projections) — redactSecrets on ALL string leaves incl. nested tool output + details.diff; STRIP the write content body → '[N bytes]'; finalize done:false turns. Restore/swap DISPLAY-ONLY (pure setMessages, ZERO engine sends — no sendPrompt/respondGate/engine_send). Workspace source is an OPTIONAL useChatRuntime param → absent disables persistence (bare adapters/offline unaffected). Fail-soft setItem (try/catch + prune-retry then skip). SECURITY (the constant, UNCHANGED): host gate + inert-render + redact rail + no-bypass invariant UNCHANGED; out-of-ws READ stays the Ph112 residual; the store is NOT gate-load-bearing so renderer-owned persistence doesn't widen the renderer-trust gap.
Exit criteria: persists+restores keyed by workspace root (restart round-trip live-drive-only, maintainer-verified); serialized string carries NO seeded known-secret; per-workspace keying (no cross-leak); workspace change SWAPS the thread (no stale leak); restore/swap zero engine sends; bound + prune-oldest + quota/shape fail-soft; done:false finalized; newConversation clears store + entry; full app suite + npm run build + cargo check exit 0 + adversarial review findings fixed.
Residuals (accepted): A1 — redactSecrets misses short/plaintext secrets + the content-strip is write-only (edit new_string redacted-not-stripped) — same content the live DOM already showed, the delta is durability; live restart-restore is live-drive-only (Tauri); multi-workspace localStorage quota has NO cross-workspace LRU eviction (past ~15-20 large active workspaces new saves fail-soft/skip, never corrupt); a turn interrupted by a workspace change persists its "workspace changed" error marker (honest interrupted-turn signal, intended).
Decision: [[conversation-memory-persistence]] (high). Rides umbrella spec specs/gui-harness-architecture.md (Ph108-114 precedent, ADR-named).
Abort rule: a task failing 3× → mark [blocked] + ask the maintainer (skip or abort), never silently skip.
Gates:
- [x] spec — rides umbrella spec specs/gui-harness-architecture.md (nana:approved; Ph108-114 precedent, ADR-named — no separate phase spec)
- [x] Direction confirmed by user (assumption positions taken 2026-06-30; A1 don't-know RESOLVED via down-scope 'strip high-risk bodies'; A2-A5 accept; ledger Phase-115 all_accept:false appended; --gate 115 exit 0)
- [x] Delivery accepted (post-implementation report 2026-06-30; commit lands the BUILT + reviewed work)
