# Active Phase Context

Phase: 69 - Amplifier Measurement: Representativeness Audit + Anchor-Validity Verdict
Status: COMPLETE (4/4 tasks [x], all 14 exit criteria green, reviewer 10/10; delivery accepted 2026-05-30, committed + pushed to main). Next: /dev-plan Phase 70 — gated on measurability-gate.sh flipping MEASURABLE (predicate repair → valid non-commodity anchor → the live off/on run).
Objective: Using the validated Phase-68 ruler (`eval/amplifier/emit-proxy-vector.sh`) against REAL consuming-project transcripts, deliver the honest answer the deferred live run cannot yet give — the ruler's ground-truth detector is non-representative on real data AND the same-day-close anchor is degenerate for measuring harness lift — and ship a committed, re-runnable measurability gate that currently BLOCKS (NOT-MEASURABLE) and is the trigger any future live off/on experiment (Phase 70) must flip. NO live run. Audit-only — do NOT patch the emitter. See [[amplifier-representativeness-audit]].
Recon (this session, read-only, the basis for Approach A): the ruler over all 8 real transcripts ⇒ `ground_truth.surfaced=false` on all 8, AUQ-scoped phrase hits=0 on all 8 (decision surfaces in prose/code 8–50×, never in an AskUserQuestion — the v1 predicate is a structural false-negative); the look-ahead concept appears even in the harness-OFF baseline (base model handles it unprompted ⇒ no headroom ⇒ degenerate anchor); the 8 transcripts are not a clean off/on pair (eval vs build vs full-harness), proxies confounded + direction-ambiguous. The cheap probe falsified the expensive live-run plan before any spend.
Scope: eval/amplifier/** (survey-real-transcripts.sh + real-transcript-survey.md, measurability-gate.sh + VALID-MEASUREMENT.md, SCHEMA-NOTES.md line-50 resolution), specs/, .dev-wiki/articles/** (decision amplifier-representativeness-audit.md, phase-63-remediation-roadmap.md). Do NOT touch eval/amplifier/emit-proxy-vector.sh (frozen) or any CODE under eval/comparison|corpus|reasoning.
Key constraints: deterministic-only no-LLM in any path; read-only wrt the real transcripts (shasum-prove) AND the emitter (git-diff-empty); verdict vocabulary is about the ANCHOR/INSTRUMENT never harness value (disclaimer present + no machine verdict token); measurability-gate falsifiable in BOTH directions (--selftest: a MEASURABLE + a NOT-MEASURABLE scenario; pinned ≥2-ON ∧ ≥2-OFF threshold; planted-fixture shasum exclusion) and currently NOT-MEASURABLE; the 2 probes self-test, NOT make-test gates (no README script-count bump); honest apparatus disposition (comparison tombstoned, reasoning calibration-only, binary corpus sole gate — NOT "ruler supersedes"); make eval frozen at 52.
Exit: survey+record reproduce 8/8 surfaced=false with positive-control (ruler RUN on surfaced.jsonl→true) + raw-phrase/parse_errors/escalation_count columns; gate prints NOT-MEASURABLE + --selftest green; VALID-MEASUREMENT defines headroom/degeneracy + dual-failure pre-mortem + Phase-70 experiment; SCHEMA-NOTES line-50 resolved; roadmap records the apparatus disposition; emitter + Phase-68 deliverables git-diff-empty + no eval/comparison|corpus|reasoning code edits; no-LLM + disclaimer + no verdict-token; make eval 52/52; make test green at UNCHANGED count; decision article + roadmap name Phase-70 deferred items.
Abort rule: if a real transcript shows surfaced=true (detector fires on real data) ⇒ STOP + re-derive (the audit premise is wrong; recon shows 0/8 so unlikely). If the AUQ-event text DOES contain a paraphrase of the decision ⇒ reframe to "detector phrase-list too narrow" + note it (still no emitter patch this phase). If apparatus disposition surfaces genuine residual value in eval/reasoning ⇒ record keep-as-calibration, don't force retirement. If make eval drifts off 52 OR make test regresses OR a probe needs the emitter patched to pass ⇒ STOP. If blocked >3 attempts, mark [blocked:], report, ask skip-or-abort. T1 is FIRST/checkpoint.

Gates:
- [x] Direction confirmed by user (Approach A — audit + anchor-validity verdict; chosen via AskUserQuestion 2026-05-29)
- [x] Delivery accepted (post-implementation report — 2026-05-30; reviewer 10/10; accepted → committed + pushed to main)
