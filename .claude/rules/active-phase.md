# Active Phase Context

Phase: 104 — Emerging-Agent-Tooling Landscape Survey (frontier pillar, GitHub-trending discovery). COMPLETED — delivered + accepted 2026-06-22 (4/4 tasks; do NOT re-open). MEASUREMENT phase — the frontier pillar RE-AIMED (the discovery survey Ph97/98 skipped). VERDICT: LARGE value-delta (NOT the null) — the post-cutoff retrieval sweet spot (Ph59 exception) DEMONSTRATED; ~65/65 named repos ground-truth-verified. SHIPS NOTHING. Apparatus gitignored companion/research/frontier-landscape/. Decision [[frontier-landscape-survey]] (medium). NEXT (maintainer's call): (a) adopt-scout follow-on on the agent-security-scanner / code-KG-MCP categories (they rhyme with the kit's boundary-validator bet); (b) a recurring discovery trend-scan (A3 deferred); (c) a working-knowledge amendment to the Ph59 post-cutoff claim.
Objective: Survey GitHub for EMERGING/TRENDING agent / harness / tools / framework repos (last ~6mo, by traction) and produce a LANDSCAPE-AWARENESS map — orientation on where the OSS agent ecosystem is heading, NOT an adopt-list, NOT a positioning verdict. The discovery survey the maintainer envisioned but Ph97/98 (lab-primary positioning) never did. Lands on Ph59's carved-out exception — post-cutoff signal beyond parametric recall (cutoff Jan-2026, today Jun-2026).
Scope: companion/research/frontier-landscape/** + .dev-wiki/** + .claude/rules/active-phase.md. OUT: any kit code/config change; any ADOPTION of a surfaced advancement (separate gated adopt-scout follow-on); a positioning verdict / re-score of the Ph97 frozen rule; a recurring trend-scan capability (A3 one-shot-first); specs/ (the gitignored method-note IS the spec, Ph97 precedent).
Output = BOTH LAYERS (maintainer-revised A4): a pattern/capability cluster SPINE (appearing / gaining momentum / fading) + a repo-evidence APPENDIX (every entry cited source+date+traction).
Key constraints:
- Blind baseline committed BEFORE any web search (the anti-retrofit seal + the value-measurement control — phase value = the measured DELTA; ~0 delta = the pre-registered INFORMATIVE NULL "recoverable from parametric knowledge," Ph59-consistent). No search against an unsealed baseline.
- Cited-or-omit: every repo/claim traces to a fetched source + date + traction signal — no parametric memory presented as discovered.
- Recency positive-control ([[HEU-012]]): a known-established repo MUST NOT pass the emerging filter, or the filter is dead → STOP and fix before synthesis.
- Landscape-awareness ONLY — orientation, not adopt-ranking, not a positioning verdict (the two lenses the maintainer rejected).
- SHIPS NOTHING: the map lives in gitignored companion/; the kit is unchanged.
Exit criteria: T1 method-note.md (gitignored spec) + blind-baseline.md (parametric-memory map, committed BEFORE search); T2 candidates.md (cited rows, source+date+traction) + recency positive-control PASS; T3 landscape-map.md (cluster spine + repo-evidence appendix, every cluster claim cited); T4 delta.md (value-delta vs blind baseline = genuinely-new vs already-known, OR the informative null + caveats); make test PASS, make eval 50/50, drift 0 (ships nothing); git ls-files companion/ empty.
Abort rule: if the recency positive-control fails (a known-established repo classifies as emerging) → STOP, fix the trending filter before synthesis (clean-on-control = instrument-dead). If the value-delta is ~0 → record the informative null, do NOT inflate known repos into discoveries.
Health (entry): nana-dev-kit make test PASS, make eval 50/50, drift 0; companion/ gitignored + untracked.
Spec: the gitignored companion/research/frontier-landscape/method-note.md (Ph97 precedent — apparatus is private; no specs/*.md file). Decision: [[frontier-landscape-survey]] (medium).
Gates:
- [x] spec — the gitignored method-note serves as the spec (Ph97 precedent; written at T1, BEFORE search)
- [x] Direction confirmed by user (gate closed 2026-06-22, ledger Phase-104; A1 elicit-novel-signal DON'T-KNOW→blind-baseline control [~0=informative null]; A2 landscape-awareness, A3 one-shot, A4 both-layers, A5 lightweight-method-note all ACCEPT)
- [x] Delivery accepted (post-implementation report 2026-06-22; verdict = LARGE value-delta / post-cutoff retrieval sweet spot DEMONSTRATED, maintainer-accepted "Yes"; ~65/65 repos ground-truth-verified; SHIPS NOTHING)
