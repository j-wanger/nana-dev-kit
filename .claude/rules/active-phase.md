# Active Phase Context

Phase: 101 — Harder-corpus contract-vs-spec fidelity screen (edge-screener). PLANNED 2026-06-22 (5 tasks L/M/M/M/M; status active; direction gate closed). MEASUREMENT phase — the harder-corpus follow-on routed forward from Phase 100's ceiling-saturated null. Reuses the Phase-100 harness adapted to an edge-screener workspace.
Objective: test whether a contract that names the integrity invariant keeps a delegated opencode worker more on-outcome than a prose spec or a bare prompt, on edge-screener FUNCTION stubs hard enough that a bare prompt actually FAILS for an integrity reason (no-lookahead decision-lag / cost application / PIT survivorship) — scored by HELD-OUT un-foolable integrity tests (deterministic PRIMARY) + a cross-model judge (descriptive SECONDARY). A clean "no discriminating corpus constructible" is a valid result.
Scope: companion/research/contract-screen-hard/** (apparatus, GITIGNORED, like Ph97/98/100); .dev-wiki/** + .claude/rules/active-phase.md. OUT: any kit change (ships NOTHING — check-fidelity.py already shipped Ph100); mutating /Users/jwang/edge-screener (per-run copies only); reusing Ph100 analyze.py verbatim (judge-primary → forked analyze-hard.py); pillar-2 worker fleet; any scripts/modules.json/hook/test/Makefile change.
Key constraints: FAILURE-MODE STABILITY ≥2/N same integrity assertion. DET-PRIMARY held-out test + judge SECONDARY via forked analyze-hard.py [det-primary/MIN_CORPUS=3/n=3 — NOT Ph100 analyze.py]; judge-pass/test-fail = FAIL. HELD-OUT VALIDATED vs seeded known-wrong (MUST fail the canonical bug) + seeded real reference (MUST pass) — clean-on-seed aborts the task, all-clean aborts the phase ([[HEU-012]]). HELD-OUT TRULY UNREACHABLE (grep the whole tree); scored only in a separate copy. CONTRACT NAMES THE INVARIANT, NOT THE TEST LITERALS (leakage diff). INFORMATION PARITY (strip asserts). CONTRACT-ARM FLOOR GUARD (≥1 in-band task contract>bare by the effect form for a VERDICT, else null). REAL EDGE-SCREENER NEVER MUTATED (per-run copies + checksum before/after). BYTE-FROZEN before scored runs; pilot precedes the freeze. SHIPS NOTHING.
Exit: pilot.md per-task held-out passes-reference + fails-known-wrong + bare-failing-assertion + disposition (in-band|floor|ceiling); ≥3 in-band (else informative null); held-out-unreachable grep + contract-leakage diff + info-parity recorded; analyze-hard.py --selftest exit 0; shasum -c .frozen OK BEFORE any scored run; results.md = in-band tasks × 3 arms × n=3 with per-run fields + bare integrity-failure evidence; verdict.md = mechanical det-primary verdict (+ contract-floor disposition) OR informative null, claim scoped + worker pinned; /Users/jwang/edge-screener checksum identical before/after + git clean; make test PASS, make eval 50/50, drift 0; companion/ untracked.
Abort: clean-on-seed (a held-out scorer that passes its seeded known-wrong) OR an un-closable held-out leak OR opencode can't run in an edge-screener copy → STOP, never ship a leaked/vacuous/non-runnable screen. <3 in-band tasks → record the informative null + route to close-out (NOT a forced verdict). >3 attempts on a task → ask user skip|abort.
Spec: specs/phase-101-harder-corpus-screen.md (nana:approved 2026-06-22). Decision: [[harder-corpus-screen]] (medium). Ledger: Phase 101 (all_accept:false — A1 accept, A2 reject→from-scratch stub, A3 accept, A4 accept).
Gates:
- [x] spec — specs/phase-101-harder-corpus-screen.md (nana:approved 2026-06-22, dev-plan --internal)
- [x] Direction confirmed by user (gate closed 2026-06-22, ledger Phase-101, all_accept:false — A1 accept, A2 reject→from-scratch FUNCTION stub, A3 accept, A4 accept)
- [ ] Delivery accepted
