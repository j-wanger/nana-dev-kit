# Active Phase Context

Phase: 99 — Direction Dashboard (render-only, dashboard-first). COMPLETED + DELIVERY ACCEPTED 2026-06-22 (work commit 46473a7; 4/4 tasks; review gate CLEAN + 1 LOW [esc→html.escape] fixed inline).
Objective: Make design directions legible at human pace by rendering the `/dev-plan` DIRECTION GATE as a static, scannable HTML page (recommendation + options + cost-sorted assumptions + orienting context) — so the maintainer reviews visually instead of parsing a fast in-session text stream, then answers the gate in-session. Pillar 3 (the dashboard) of the rung-C contract-driven-delegation program, delivered FIRST; pillars 1–2 (contract-fidelity harness, downstream/local workers) are LATER phases.
Scope: scripts/generate-direction.py + Makefile `direction` target + docs/direction.html (4th member of the existing docs/ HTML-generator family, house-style match); a direction-brief schema (`.dev-wiki/direction-brief.json`); tests/test_direction_dashboard.sh + fixtures (registered in `make test`); a minimal dev-plan integration (`templates/.claude/skills/dev-plan/direction-brief.md` companion + a reference from the assumption-gate step). OUT: the contract-fidelity harness + workers/opencode (later pillars); ANY interactive click-to-decide round-trip (browser→session — A1 boundary); an always-on live SERVER (A2 down-scope → Blockers); refactoring the 3 existing generators; any modules.json/hook change.
Key constraints: RENDER-ONLY — the page is read, decisions stay in-session (A1). CONTROLS-FIRST / [[HEU-012]] — tests assert the rendered HTML CONTAINS the brief content (recommendation + every option label + every assumption text), NOT mere file-existence; a seeded MALFORMED brief (bad JSON / missing required field) MUST make the generator fail loud (non-zero + stderr) — clean-on-seed = dead instrument. MATCH the surrounding code — self-contained generator in the established house style; do NOT add a shared helper used by one caller, do NOT rewrite the other three. JUDGMENT not measurement — human-facing presentation value is unmeasurable in-kit (Ph59/80 are model-facing); shipped on the maintainer's stated need; tests assert mechanics only. PARITY — dev-plan companion ships in templates/ (synced to ~/.claude via install --update).
Exit: `bash tests/test_direction_dashboard.sh` passes (content-presence + recommended-highlight + seeded-malformed fail-loud control); `make direction` writes docs/direction.html from `.dev-wiki/direction-brief.json` (recommendation + option cards [recommended highlighted] + cost-sorted assumptions table + orienting context); Phase-99 dogfood brief renders; dev-plan `direction-brief.md` companion exists + referenced; `make test` ALL-PASS, `make eval` 50/50, drift 0; A2 live-server Blockers re-trigger filed.
Abort: a generator that emits a page on malformed input (clean-on-seed) → STOP, fix to fail-loud, never ship a dashboard that silently renders garbage. >3 attempts on a task → ask user skip|abort.
Spec: specs/phase-99-direction-dashboard.md (nana:approved 2026-06-22). Decision: [[direction-dashboard]] (medium→high at close). Ledger: Phase 99 (all_accept:false — A1/A3/A4 accept, A2 don't-know→down-scoped to generator).
Gates:
- [x] spec — specs/phase-99-direction-dashboard.md (nana:approved 2026-06-22, dev-plan --internal)
- [x] Direction confirmed by user (assumption positions taken 2026-06-22; A1 accept, A2 don't-know→down-scoped to generator + live-server→Blockers, A3 accept, A4 accept; ledger Phase 99; all_accept:false)
- [x] Delivery accepted (post-implementation report 2026-06-22; work commit 46473a7; review gate CLEAN + 1 LOW fixed inline)
