# Active Phase Context

Phase: 64 - Cut heuristic scoring machinery + self-dialogue
Status: COMPLETE (4/4 tasks [x]; all exit criteria met; delivery accepted 2026-05-29; committed). Next session: /dev-plan Phase 65. Top item of the Phase-63 remediation roadmap — DONE.
Objective: Remove the never-fired heuristic SCORING machinery (matcher → judge → counter-update → lifecycle → dashboard + dev-plan Step 13 sub-items 6-7 + dev-debrief Step 7 heuristic-capture) and the net-neutral self-dialogue step (dev-plan Step 11), KEEPING the 17 wiki/heuristics/*.md articles (live fixtures + eval/reasoning corpus). Batched: both force dev-plan renumbers on the same surface (one pass, not two).
Scope: templates/.claude/skills/dev-plan/**, templates/.claude/skills/dev-debrief/**, templates/.claude/skills/MANIFEST, scripts/heuristic-dashboard.py, tests/test_heuristic_evolution.sh, tests/test_templates.sh, Makefile, eval/corpus/**, .claude/rules/working-knowledge.md, .dev-wiki/**, wiki/**, .claude/rules/active-phase.md
Key constraints: Cut machinery, keep knowledge-fixtures (articles are test deps). Walk UPSTREAM (cut dev-debrief producer + dev-plan consumer). test_step_numbering.sh checks HEADINGS only — sweep sub-letters 13x/15x/16x(incl 16f-ter)/17x + inline "Step N" prose MANUALLY. Eval total is DYNAMIC (eval-runner counts scenario.json) → 52/52 with ZERO literal edits. Hot cache is curator-owned — verify with a dogfood no-op, no [pinned] removed.
Exit: 8 files absent; no dangling deleted-basename ref; test_step_numbering + test_companions + test_registration green; make eval 52/52; make test green (suite −1); retained articles cite no deleted machinery; curator dry-run on edited cache = byte-identical no-op.
Abort rule: if a deletion would orphan an install-time registration (modules.json/MANIFEST/settings.json) not in the cleanup, OR an eval scenario turns out to test a RETAINED component, OR the hot-cache curator dry-run is NOT a no-op after the prune — STOP and report. The renumber is the principal risk: test_step_numbering.sh is the gate but ignores sub-lettered refs, so sweep 16x→15x manually.

Outcome: removed 8 files (matcher/judge/counter/lifecycle/dashboard + self-dialogue-prompt + heuristic-capture + test_heuristic_evolution) + 2 eval scenarios + 5 stale hot-cache entries; net −457 lines. Double-renumber (dev-plan 12-18→11-17, dev-debrief 8-26→7-25 incl debrief-finalization companion) — review caught a systematic sed gap (singular-only match missed bare `#### 16a` sub-headings + plural `Steps N-M`); all fixed + completeness-grep-verified. make test green (12 scripts) / make eval 52/52 / test_step_numbering gap-free / curator dogfood no-op. 17 articles + eval/reasoning fixture untouched.

Gates:
- [x] Direction confirmed by user (approach approved 2026-05-29 via dev-plan direction gate: heuristic-machinery cut + self-dialogue removal, batched renumber)
- [x] Delivery accepted (delivery gate — accepted 2026-05-29; reviewer caught the renumber sed-gap, fixed inline + re-verified)
