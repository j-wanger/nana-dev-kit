# Active Phase Context

Phase: 100 — Contract-vs-Spec Fidelity Screen (rung-C pillar-1). COMPLETED + DELIVERED 2026-06-22 (5/5 tasks; work commit 42a6db6 = plan+T1; close-out commit pending). MEASUREMENT phase (reframed at the gate from "build the spine" — A1/A2 came back don't-know, resolvable only by running real workers; rung-C analogue of the amplifier-screen lineage Ph70/71/77/78/80). VERDICT (frozen rule, instrument sha-intact): 60 runs (3 arms × 4 tasks × n=5), det AND judge HONORED rate = 1.00 EVERYWHERE → **A1 AMPLIFIER-NULL → COLLAPSE-TO-RUNNER-ONLY** (capable opencode worker honors outcomes unprompted; contract adds no fidelity over bare → ship check-fidelity.py as a runner, do NOT ship a contract schema); **A2 DETERMINISTIC SUFFICES in the wild** (judge proven live — caught the gaming + non-exponential seeded controls — but 0 real det-passing violations). Limitation: ceiling-saturated corpus (no bare-arm failure headroom) → harder-corpus screen routed forward. Leak-probe deviation: probe-by-guess infeasible; isolation certified by opencode's hard external_directory block (isolation-evidence.txt). Shipped: scripts/check-fidelity.py + tests/test_contract_fidelity.sh (in make test) + make check-fidelity. Apparatus + verdict gitignored companion/research/contract-screen/.
Objective: build the deterministic fidelity-check (scripts/check-fidelity.py — the scoring instrument AND the one shippable artifact; dual-use so building it precommits to NEITHER A1 outcome) + a calibrated marked neural-judge arm, then score a pre-registered controls-first 3-arm screen (bare-prompt / spec / contract) on real opencode workers (opencode run --pure --format json, v1.17.3) in a non-leaking workspace outside the repo tree. The verdict decides whether a contract artifact ships. A NULL (contract adds nothing → collapse to runner-only; deterministic suffices → drop the judge) is a VALID success.
Scope: scripts/check-fidelity.py + tests/test_contract_fidelity.sh + Makefile SHIPPED; companion/research/contract-screen/** apparatus GITIGNORED (pre-registration, runner, dual scorer, results, verdict — like Ph97/98); .dev-wiki/** + .claude/rules/active-phase.md. OUT: the contract-schema SHIPPED shape (a verdict OUTPUT — ship in-phase if conclusive else route to a build phase); pillar-2's 24/7 opencode fleet; any neural judge in the shipped check; modules.json/hook/settings changes; any commit/push of companion/research/.
Key constraints: NON-LEAKING workspace + leak-probe K≥4 AT-CHANCE-OR-VOID (worker+judge see ONLY the variant input, outside the repo tree; at/above the frozen threshold → instrument-dead/void — the Ph80 leak hazard). BYTE-FROZEN before runs (sha256 .frozen; verdict reads the frozen rule, no post-hoc edits). CONTROLS-FIRST / [[HEU-012]] — per-guardrail seeded VIOLATED fixture it MUST fail (a guardrail with no failing fixture = rejected, clean-on-seed = dead) + a malformed contract rejected loud + a GAMING fixture (det PASS, judge MUST flag a citable defect) + JUDGE-FALSIFIABILITY (stable-across-repeats + citable defect). INFRA-FAIL ≠ fidelity-0. CHECK-DETERMINISM + NO-LLM-IMPORT (AST-scanned). EFFECT-FLOOR (non-overlapping ranges OR delta ≥ k·pooled-SD) + MIN-CORPUS (below → underpowered/pilot-scoped, routed forward). APPARATUS GITIGNORED; ship ONLY the deterministic check.
Exit: bash tests/test_contract_fidelity.sh passes (HONORED→0, per-guardrail VIOLATED→non-zero naming the guardrail, malformed→non-zero+stderr, determinism, AST no-LLM-import, offline runtime control); pre-registration byte-frozen before runs (sha256==.frozen, gitignored, info-parity note); leak-probe within the chance band; gaming control (det PASS, judge flags a citable defect) + repeat-variance floor; results.md = 3 arms × n≥3 with per-run fields + leak-probe-score (structural scan); verdict.md cites the frozen rule + per-arm distributions + the effect form + the schema disposition; make test PASS, make eval 50/50, drift 0; companion/ untracked.
Abort: a check that emits a clean verdict on malformed input (clean-on-seed) OR an un-closable leak (leak-probe at/above the void threshold) → STOP; never report a leaked or vacuous result as a verdict. >3 attempts on a task → ask user skip|abort.
Spec: specs/phase-100-contract-fidelity-screen.md (nana:approved 2026-06-22). Decision: [[contract-fidelity-screen]] (medium). Ledger: Phase 100 (all_accept:false — A1/A2 don't-know→measure, A3 reject, A4 accept, R1 reject, R2/R3 accept).
Gates:
- [x] spec — specs/phase-100-contract-fidelity-screen.md (nana:approved 2026-06-22, dev-plan --internal)
- [x] Direction confirmed by user (assumption positions taken 2026-06-22; gate closed, ledger Phase-100; all_accept:false — A1/A2 don't-know→measure, A3 reject, A4 accept, R1 reject, R2/R3 accept)
- [x] Delivery accepted (2026-06-22; verdict AMPLIFIER-NULL → collapse-to-runner-only + deterministic-suffices; maintainer review in-session; all 5 exit criteria met, make test PASS, eval 50/50, drift 0)
