# Active Phase Context

Phase: 105 — Code-Retrieval Adopt-Scout (code-KG / tool-reach). COMPLETED — planning-stage informative null, banked 2026-06-23 (do NOT re-open). The Ph104 NEXT option (a) adopt-scout, maintainer-aimed at Category 2 (code-knowledge-graph / codebase-retrieval as MCP). RESOLVED AT PLANNING: three controls-first adversarial workflows converged on a predicted null BEFORE any measurement run (the rigorous direction-setting WAS the measurement — burden-of-proof-on-the-feature). VERDICT: do NOT adopt. SHIPS NOTHING. Decision [[code-retrieval-adopt-scout]] (high). Evidence gitignored companion/research/code-retrieval-scout/evidence.md.
Objective: Evaluate whether the kit should adopt a code-knowledge-graph / codebase-retrieval capability (Ph104 Category 2). Answer the adoption question with evidence, not plausibility.
Verdict detail: Two framings, each killed by adversarial review. (1) Vendor a code-KG-MCP screened on a consuming repo (aml-substrate) = NON-VIABLE: the pre-built index is an answer-cache for the scored transitive-closure relations (PASS baked into the apparatus); the positive control is NON-CONSTRUCTIBLE (a determined OFF agent's 0.13s stdlib-ast BFS recovers callers on the 26K-LOC first-party repo); the syntactic candidate code-KGs are informationally DOMINATED by the mypy already installed in the substrate .venv on the only hard cases (method dispatch). (2) Reframe — surface already-present type-aware tooling ("are agents under-using mypy?") = the fair Rung-1 existence-only test predicts DEGENERATE; this is the amplifier-null hypothesis class, terminated 4x here (Ph59/70/77/78); the dispatch-hard corpus is only PARTIAL (~1-2 cross-module cases; registry dispatch both mypy-unresolvable AND prose-leaked).
Generalizes: harness headroom does NOT live in re-presenting code structure / tool-choice the strong model recovers from the repo already in its context. Ph59 boundary (option (c) delivered): local code-structure retrieval is NOT in the post-cutoff carve-out Ph104 demonstrated — recoverable in-context + dominated by installed type-aware tooling; the carve-out is for facts derivable from NO fair corpus.
Scope: companion/research/code-retrieval-scout/** + .dev-wiki/** + .claude/rules/{active-phase,working-knowledge}.md. OUT: any kit code/config change; an actual adoption; a measurement run (pre-empted by the planning-stage null).
Key constraints (the controls that earned their keep):
- Orchestrator-verifies ([[HEU-012]]): subagent empirical claims (scale, leak, mypy-inversion, name-collisions) were re-run by the orchestrator before acting — first-party 26K LOC not 1.79M, _ARCHITECTURE.md leaks dependency rows, mypy/dmypy installed, 12 to_dict defs / 21 sites.
- Can't-measure-clean-context-in-kit ([[can't-measure-clean-context-in-kit]]): even aml-substrate carries kit rules that can leak "prefer type-resolution over grep" — the venue-leak gate (Ph80) is load-bearing for any future revisit.
- Burden-of-proof-on-the-feature: a predicted null + no measured lift => subtract the idea; do NOT spend an L-run on a determined outcome.
Exit criteria (MET): adopt/no-adopt verdict reached (no-adopt); decision article [[code-retrieval-adopt-scout]] + working-knowledge amendment (incl. the Ph59 post-cutoff boundary, option (c)) written; ZERO kit code change; make test PASS, make eval 50/50, drift 0; git ls-files companion/ empty.
Health: nana-dev-kit make test PASS, make eval 50/50, drift 0; companion/ gitignored + untracked.
NEXT (maintainer's call): the frontier pillar is now mapped (Ph97/98 positioning, Ph104 discovery, Ph105 adopt-scout = null). Candidates: (a) the surviving untested avenue — a screen on genuinely PROPRIETARY/POST-CUTOFF correctness derivable from no fair corpus (where headroom plausibly lives, the one frontier Ph104 lit up); (b) a recurring discovery trend-scan (Ph104 A3 deferred); (c) return to the rung-C contract-delegation program (Ph99 pillars). No phase is queued.
Spec: none — this resolved at the planning gate; the decision article + evidence record ARE the deliverable (no specs/*.md; no measurement run to pre-register).
Gates:
- [x] Direction confirmed by user (4 AskUserQuestion rounds, 2026-06-23, ledger Phase-105: Category-2 over Category-1 → measurement-screen + null-is-valid + aml-substrate → reframe to tool-reach after the code-KG screen was found rigged → BANK THE NULL after the harden probe showed the 4x-terminated amplifier-null class + thin corpus)
- [x] Delivery accepted (planning-stage null banked by the maintainer 2026-06-23; verdict = do NOT adopt; SHIPS NOTHING)
