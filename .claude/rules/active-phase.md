# Active Phase Context

Phase: 116 — YouTube→wiki grounded acquisition core (rung-B foundation). BUILT 2026-06-30, 6/6 tasks [x]. Standard ceremony. Outcome: local gitignored Python apparatus under companion/research/youtube/ (10 modules + 6 test files, 53/53 pytest green; git check-ignore confirms nothing shippable). Adversarial review = 5-lens finder workflow (22 findings, 5 HIGH) + a focused re-attack; ALL orchestrator-verified by running the code + fixed with 16 regression tests. HIGH fixes: casefold (probe survived non-ASCII/German ß), tilde-fence-safe neutralize + line-anchored never-raise parse, refuse-clobber unparseable files, per-quote timestamps from the segment→time map, vacuous E2 test. The re-attack caught the punctuation-strip fix re-opening the anti-fabrication invariant to cross-sentence splices → REVERTED to casefold + curly/dash-fold (anti-fabrication holds). Delivery gate pending maintainer acceptance; then commit.
Objective: Build the acquisition + grounded-extraction CORE of the Phase-97 rung-B standing research pipeline, specialized to YouTube, as a manual-trigger Python apparatus under gitignored `companion/research/youtube/`. Turns one Pi/agent-setup video into a grounded, provenance-carrying concept set (each concept tied to a verbatim, mechanically-verified transcript quote + timestamp), written ready to feed a Pi-setups wiki via the EXISTING `wiki-add`→`wiki-absorb` skills. NOT a shipped kit skill (local-apparatus home, per the Ph97 precedent); continuous half (RSS/scheduler/dedup-watermark/Telegram digest) deferred to nanaclaw.
Loop: acquire (captioned-first; uncaptioned → dead-letter) → chunk (~1000-tok / 15% overlap, segment→time map) → extract (Claude structured, extraction-by-selection: verbatim source_quote + ≤40-word claim) → ground + consolidate.
Scope: companion/research/youtube/** (NEW, gitignored), .claude/rules/active-phase.md, .dev-wiki/** (planning artifacts). NO shipped-kit/app/templates/install.sh/security/engine change.
Key constraints (LOAD-BEARING INVARIANT = the grounding rail, T4): every source_quote mechanically verified ∈ its chunk before it can enter a concept file; planted fabricated quote REJECTED. 8 adversarial-review-caught constraints: C1 normalization SYMMETRY + a calibration probe that ABORTS on broken normalization (else a silent drop reads as 'thin video'); C2 merge keeps quotes IMMUTABLE + re-verifies the MERGED set; C3 atomic write + regression gate on grounded-quote count + completion marker; C4 neutralize [[/leading---/backticks in quotes at write; E1 empty-200 + sub-200-word → dead-letter; E2 segment→time map survives chunking; S1 pinned tool version + pre-flight cost ceiling; S2 structured immutable `verified` field + README instructs the hand-off to PRESERVE provenance. Live network/API = manual-drive (residential IP); deterministic tests on fixtures + a mocked Anthropic client; deps in a companion-local gitignored venv (NEVER install.sh).
Exit criteria: acquire dead-letters empty-200 + sub-floor (above-floor success control passes); chunk >1 overlapping ~1000-tok window + segment→time map + no dropped text; extract (mocked) schema-valid concept (verbatim quote + ≤40-word claim + timestamp), skips malformed, cost ceiling aborts; ground REJECTS planted fabricated quote + PASSES genuine + probe ABORTS on broken normalization + consolidate dedups to ≥1 immutable grounded quote with re-verified merged set; end-to-end fixtures → concept file w/ frontmatter provenance + structured immutable verified field, quotes markdown-neutralized, regression gate refuses degraded clobber without --force (positive write control passes), full run drops completion marker; README documents trigger + provenance-preserving wiki-absorb hand-off; full companion pytest suite exit 0 + git check-ignore confirms gitignored; live multi-video first-run manual-drive (deferred).
Residuals (accepted): A1 grounding catches FABRICATED not MISREAD quotes (routed to human wiki-review; extraction-by-selection shrinks surface; NO AML-tier reconciliation); A2 whisper deferred (uncaptioned dead-letter, rate measured at first live run); live first-run manual-drive.
Decision: [[youtube-grounded-acquisition]] (high). Spec specs/phase-116-youtube-grounded-acquisition.md (nana:approved). Rung-B foundation of [[frontier-positioning-sweep]] (Ph97); grounding inherits Ph97 verify-by-re-fetch ([[qa-verification-sweep]], [[HEU-012]]).
Abort rule: a task failing 3× → mark [blocked] + ask the maintainer (skip or abort), never silently skip. CHECKPOINT: STOP + report after T4 (the load-bearing grounding-rail proof).
Gates:
- [x] spec — specs/phase-116-youtube-grounded-acquisition.md (nana:approved 2026-06-30; adversarial constraints + Tier-1 8/10-revise incorporated)
- [x] Direction confirmed by user (assumption positions taken 2026-06-30; A1 accept, A2 accept, A3 reject→Standard ceremony; ledger Phase-116 all_accept:false; --gate 116 exit 0)
- [x] Delivery accepted (post-implementation report 2026-06-30; commit 1dd0132 — dev-wiki record + spec; apparatus stays local/gitignored)
