# Active Phase Context

Phase: 111 — Typed Artifact Fidelity (typed-details structured diff) + host-gate out-of-workspace hardening. BUILT + ADVERSARIALLY REVIEWED 2026-06-27, 5/5 tasks [x] + 3 review fixes — app suite 157→201 green (incl. all live e2e), tsc + `npm run build` + `cargo check` all exit 0; READY FOR COMPLETION (delivery gate flips after the commit lands + the maintainer's deferred live window-drive felt-quality check). BUILD phase. The de-risked Ph110 follow-on: thread Pi's typed `details` (normalized to a neutral `{diff?: string}` at the adapter) ADDITIVELY through the EXISTING engine-neutral layers so an edit renders a STRUCTURED canonical DiffView — the typed path WINS over Ph110's `looksLikeDiff` heuristic. A SECURITY scope-amendment (ledger A5, maintainer-directed "fold a gate fix into Phase 111") rode alongside: the regression baseline exposed a REAL host-gate out-of-workspace coverage gap. Decisions [[typed-artifact-fidelity]] (high) + [[host-gate-out-of-workspace-hardening]] (high). Umbrella spec specs/gui-harness-architecture.md Rev 2 (rides; Ph108/109/110 precedent).
Objective: render a real STRUCTURED diff from Pi's typed `details` (T1 adapter `extractToolDetails` → T2 reduction threads it → T3 router typed-branch-first wins over the heuristic); CLOSE the host-gate out-of-workspace gap deterministically (T4, reordered FIRST). Felt-quality/diff-legibility ships on the maintainer's delivery judgment (Ph59/80 carve-out); tests assert MECHANICS only.
Scope: app/src/engine/{pi/pi-adapter.ts,types.ts}, app/src/ui/{runtime.ts,artifact-feed.ts}, app/src/gate/host-gate.ts, app/tests/**, specs/gui-harness-architecture.md (read-only). OUT (→Ph112): OS-sandbox bash-fs isolation (the string-gating residual's real fix); structured TestResults (needs a test-runner tool); signed/notarized bundle + A3 keyring residual + rollback; command palette (axis 3); Claude Agent SDK adapter; live window-drive felt-quality check.
BUILT (5/5): T4 host-gate base-policy COVERAGE extended (default-branch PATH_ARG_KEYS isOutsideWorkspace + bash-branch extractBashWriteTargets + DEV_SINK exemption; verdict-loop core UNCHANGED; 24 gate cases). T1 `extractToolDetails` neutral-`{diff}` normalization + A1 LIVE SPIKE HELD (details.diff on the LOCAL subscribe path; pi.on('tool_result') fallback not needed). T2 SurfaceToolCall.details? threaded (additive, JSON round-trip). T3 artifact-feed typed-diff branch FIRST (wins over the heuristic — Pi's diff is a header-less +/-/space format the heuristic misses). T5 deterministic integration e2e + the full gate.
KEY FINDING: Pi's `EditToolDetails.diff` = line-number-prefixed +/-/space, NO `---/+++/@@` header → Ph110's looksLikeDiff MISSES it; the typed path is strictly BETTER, not just a refactor.
ADVERSARIAL PRE-COMMIT REVIEW (6 finders × 2 refuters, 8 raised → 2 confirmed + 1 self-verified → fixed): a `/dev/null` over-block (`2>/dev/null` forcing a confirm-hold — alert-fatigue regression) → DEV_SINK exemption; a `>|` force-clobber write-evasion (`echo x >| /outside` wrote un-gated) → regex `>>?\|?`. 4 review-regression tests added. The Ph108 "empirically un-bypassable" over-claim corrected (memory + journal).
HONEST RESIDUAL: string-gating arbitrary bash is INCOMPLETE BY NATURE — python -c / node -e / base64 / env indirection still evade. The COMPLETE fix is OS-sandboxing bash's fs to the workspace → Phase 112. This does NOT claim bash is bulletproof.
Key constraints:
- HIGHEST blast radius = app/src/engine/types.ts (the `tool-result` EngineEvent variant + the new additive optional `details?:{diff?:string}` — imported by BOTH adapters + the reduction + the host + the bridge). Vercel/noop/host are the "consumers you didn't touch": additive-optional + JSON-serializable, NO change beyond type-checking (tsc-verified).
- SECURITY: `details.diff` is untrusted model-adjacent content → redactSecrets() + ~16KB cap at the adapter boundary + the inert React-string path (DiffView already inert). The gate VERDICT-LOOP core ([[gate-confirm-approve-loop]] ConfirmationBroker/confirmingGate, key-store hard-deny) stays UNCHANGED — T4 extends base-policy COVERAGE only; out-of-workspace writes are confirmable, not hard-denied.
- Hardening a confirmation gate must measure the FALSE-POSITIVE rate on common idioms — an over-broad deny is a security regression via desensitization (alert fatigue), not just a UX nit.
- "Owned not adopted": DiffView already consumes a diff string — do NOT add Vercel AI Elements (Ph109/110 finding). Typed view routing lives in the UI (artifact-feed keyed on toolName + details), NOT in the adapter (A4).
Exit criteria (ALL PROVEN 2026-06-27 except the deferred felt-quality call): typed structured diff renders (adapter→reduction→router→DiffView, NOT the heuristic); typed path wins when output isn't diff-shaped, details-absent/Vercel degrade cleanly; provider-roundtrip e2e deterministic (out-of-workspace security invariant ship-blocking, no flake); inert-render/CSP/capability-guard/gate-confirm NO regression; full app suite 201 green (was 157); npm run build exit 0; cargo check exit 0; + adversarial pre-commit review confirmed-findings fixed. Felt-quality/legibility + the live window-drive = the maintainer's delivery-time judgment (deferred).
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip.
Gates:
- [x] spec — umbrella spec specs/gui-harness-architecture.md (nana:approved, Rev 2; Ph108/109/110 precedent — Surface + Success Vision + the inert-render constraint govern this slice; no separate phase spec). ADR-named, so enforce-spec's specs/phase-111-*.md glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (assumption ledger Phase-111 block: A1 accept-spike-mitigated→HELD, A2/A3/A4 accept, A5 SECURITY-amendment accept; all_accept:true)
- [x] Delivery accepted (post-implementation report 2026-06-27; commit lands the BUILT + adversarially-reviewed phase; the live window-drive felt-quality check is deferred to the maintainer per Ph109/110 precedent)
