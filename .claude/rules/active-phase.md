# Active Phase Context

Phase: 117 — YouTube apparatus first-contact (derisk the mock-vs-real gap + harden for real tracks). BUILT + T5 RAN 2026-06-30 (6/6 tasks [x]); delivery gate pending maintainer acceptance. Standard ceremony. Smoke-first; the full frozen live measurement is Phase 118. BUILT: telemetry (usage/cost + $2/video kill-switch), reformatting-vs-fabrication-vs-splice drop diagnosis, acquisition hardening (no-blind-retry + 200-empty + exhaustive taxonomy incl. YouTubeRequestFailed/PoTokenRequired), harness117 smoke runner + circuit-breaker (endpoint-suspect streak) + CLI; grounding rail BYTE-UNCHANGED (git-diff-verified); companion pytest 53→78 green (18 build + 7 adversarial-review-fix). A T4-T6 adversarial review (4-lens, orchestrator-verified per HEU-012) caught 2 HIGH + MED, ALL fixed inline + regression-tested. T5 smoke drive RAN on THIS machine (residential Rogers IP — the earlier 'IP-blocked cloud env' claim was WRONG); maintainer directed Claude-as-extractor (no API key). RESULT (video 04EL2_Llenc, quarantined): 95% grounding (21/22), 1 splice-suspect drop, 0 fabrication, ~$0.02-0.05/video. HEADLINE: modern auto-captions are PUNCTUATED → the punctuation-over-drop dominant risk did NOT materialize; the real A1 surface is ASR word-errors that ground faithfully. VERDICT (Claude-as-evaluator): GO to Phase 118 (feasibility confirmed, n=1) with the A1 concern RE-ORIENTED to ASR errors; rail-normalization change NOT indicated. See .dev-wiki/phase-117/results.md. NEXT: maintainer accepts delivery → commit (dev-wiki record; apparatus stays gitignored) → /dev-plan Phase 118 (frozen measurement) or /dev-debrief.
Objective: Before the full frozen live measurement (Phase 118), cheaply derisk the dominant risk that the Phase-116 apparatus — proven only on a mock that grounds BY CONSTRUCTION (test mock returns words[:6] verbatim from the chunk) — over-drops genuine quotes on real Sonnet-4.6 output + real (often unpunctuated auto-caption) tracks. Add minimum telemetry (usage/cost capture + $2/video kill-switch; per-reason drop surfacing; reformatting-vs-fabrication drop diagnosis), harden the acquisition path the mock never exercised, keep verbatim transcript text inside the gitignored boundary (ToS), run 1-2 real quarantined videos manual-drive, then DECIDE go/no-go on Phase 118 + the hardening list. Feasibility gates the measurement; the measurement gates the headroom screen (NOT this phase).
Loop context: rides the Ph116 acquire→chunk→extract→ground+consolidate apparatus (companion/research/youtube/, gitignored). This phase wraps telemetry + hardening AROUND the frozen rail and runs first-contact on real video.
Scope: companion/research/youtube/** (telemetry in extract.py/ingest.py; hardening in acquire.py; NEW harness117.py + tests; verbatim-quote sidecars under gitignored run117/), .dev-wiki/** + .dev-wiki/phase-117/ (aggregates ONLY — never verbatim quotes), .claude/rules/active-phase.md, specs/phase-117-*.md. NO shipped-kit/app/templates/install.sh change.
LOAD-BEARING INVARIANTS: (1) RAIL BYTE-FREEZE — ground.py/normalize.py/consolidate.py/schema.py byte-unchanged (git diff --quiet asserts); all telemetry/diagnosis/hardening lives OUTSIDE the rail. (2) ToS CONTAINMENT — no committed file (esp. tracked .dev-wiki/phase-117/) carries a verbatim source_quote; verbatim text only under gitignored run117/ (grep + git check-ignore gate).
Key constraints (adversarial-pass-caught, folded in): reformatting-vs-fabrication drop split makes the drop-rate interpretable (else silent over-drop reads as "thin video" — C1/Ph80); a real-model adversarial fixture pins keep/drop BEFORE the live run; fetch_segments re-raises block/no-caption WITHOUT the blind retry (one endpoint hit; true first exception surfaces) + 200-empty soft-block detection + exhaustive taxonomy (AgeRestricted); $2/video kill-switch from captured usage + preflight chunk-count print; inter-fetch cooldown + circuit-breaker (halt on IpBlocked / 2 consecutive endpoint-errors); >1 probe-abort = brittleness STOP; smoke videos QUARANTINED from any Phase-118 corpus (no freeze contamination); manual-drive residential IP + ANTHROPIC_API_KEY (deterministic tests on fixtures + mocked client, no network).
Exit criteria: pytest ≥53 green + git diff --quiet on the 4 rail files; usage/cost/killswitch tests green; adversarial-fixture + drop-diagnosis tests green (reformatted → present-modulo-punctuation/ground, fabrication → absent-even-stripped); no-blind-retry + empty200 + taxonomy tests green; pacing + circuit + probe-abort-tripwire green; git check-ignore run117/ + grep gate (no verbatim source_quote in .dev-wiki/phase-117/); the smoke drive writes run117/telemetry.jsonl rows (usage/cost/chunks/outcome_class + drop histogram) with a STOP-after-first-video; results.md records real cost + drop split + go/no-go + the recommended Phase-118 pre-reg params. Live smoke drive = manual-drive (maintainer).
Recommended Phase-118 pre-reg params (frozen at the 118 freeze, not now): search-query corpus frame; A2 whisper-pull-forward threshold 30%; A1 solo + MANDATORY clean-context LLM inversion cross-check run OUTSIDE the kit; cost stops $2/video + $10/batch + 60-call ceiling; tamper-evident anchor (git ancestry + SHA-256 of fetched IDs); overall PASS/FAIL truth table.
Decision: [[youtube-liverun-derisk]] (high). Spec specs/phase-117-youtube-liverun-derisk.md (nana:approved). Rung-B first-contact of [[youtube-grounded-acquisition]] (Ph116) / [[frontier-positioning-sweep]] (Ph97); grounding inherits Ph97 verify-by-re-fetch ([[qa-verification-sweep]], [[HEU-012]]).
Residuals (accepted): A1 misread + A2 dead-letter RATES are FIRST-MEASURED here but only DIRECTIONALLY on 1-2 videos (the population estimate is Phase 118); whisper deferred; the live smoke drive is manual-drive.
Abort rule: a task failing 3× → mark [blocked] + ask the maintainer (skip or abort), never silently skip. CHECKPOINTS: STOP + report after T4 (the ToS-containment + rail-byte-freeze proof, before any live fetch) AND after the FIRST smoke video (before the second — a systematic bug must not burn both; a reformatting-dominated drop IS the go/no-go signal).
Gates:
- [x] spec — specs/phase-117-youtube-liverun-derisk.md (nana:approved 2026-06-30; adversarial pre-registration design workflow incorporated)
- [x] Direction confirmed by user (assumption positions taken 2026-06-30; A1/A2/A3/A5/A6 accept, A4 reject→search-query frame; ledger Phase-117 all_accept:false)
- [x] Delivery accepted (2026-06-30 — maintainer accepted the T5 result + GO verdict; /dev-debrief; apparatus stays gitignored, commit lands the dev-wiki record)
