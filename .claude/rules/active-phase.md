# Active Phase Context

Phase: 108 — GUI Dev-Harness v1 Thin Slice. ACTIVE — BUILT 2026-06-26, 8/8 tasks [x], 14/14 machine-checkable exit criteria proven; DELIVERY GATE PENDING. BUILD phase, THE PIVOT: re-platformed nana-dev-kit into a GUI-primary, model-agnostic desktop dev harness per the APPROVED spec specs/gui-harness-architecture.md (nana:approved Rev 2). Decisions [[engine-adapter-in-process-gate]] + [[provider-defaults-to-local-model]] + [[second-adapter-vercel-ai-sdk]] (all high); ledger Phase-108 A1/A2/A3/A4 all held (none bit). App lives in the new app/ dir.
Objective: a running v1 thin slice (minimum daily loop + security rails, security/gate FIRST) — a Tauri app embedding a model-agnostic engine IN-PROCESS behind an app-owned EngineAdapter, the security gate at the in-process tool-call site. Pi adapter primary; Vercel AI SDK = the proven second adapter (substituted for the spec's Claude Agent SDK by maintainer override, local-backend default). Python MCP memory server mounted UNCHANGED via MCP stdio.
Scope: app/** (Tauri Rust shell + TS/React surface, engine adapters, gate, checkpoint, control, memory mount, tests) + specs/gui-harness-architecture.md (read-only reference). OUT: rewriting the MCP memory server; any external wire-protocol/server boundary.
Status (BUILT): A1 (Pi gate un-bypassable) EMPIRICALLY validated — a real local model's bash rm + out-of-workspace write denied through BOTH adapters before execution. A2 (MCP server ports as-is) validated — round-trip failure was a MOUNT bug, server untouched. A3 (keyring-rs) resolved→accept; residual = signed/notarized-bundle entitlements (Phase-109/bundle-time). 63/63 tests green incl. live; tsc+vite+cargo all exit 0 (binary 23.5MB).
Exit criteria: the 14 machine-checkable spec criteria PASS (criterion #1 Tauri-bundle = compile-proven; visual window-launch is the maintainer's delivery-time check). UI/felt quality UNMEASURABLE in-kit (Ph59/80) — ships on maintainer judgment at delivery; tests assert MECHANICS only.
Discovered → Phase 109+: Claude Agent SDK adapter (once an Anthropic API key exists); felt-quality + real-branding pass; signed/notarized bundle + opt-in auto-update + one-click rollback (folds in the A3 bundle-time keyring residual); daily-driver dogfood on the live repo (now unblocked — all security rails pass).
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip. (N/A — all tasks complete.)
Gates:
- [x] spec — approved spec is specs/gui-harness-architecture.md (nana:approved 2026-06-26, Rev 2). ADR-style slug (not phase-numbered), so enforce-spec's `specs/phase-108-*.md` glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (assumption positions taken 2026-06-26: A1/A2/A4 accept, A3 keyring-rs deferred don't-know→resolved-accept; ledger Phase-108 appended + revisit-status filled)
- [x] Delivery accepted (post-implementation report 2026-06-26)
