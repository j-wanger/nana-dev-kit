# Active Phase Context

Phase: 65 - Enforcement-Trace Instrumentation + Eval-Apparatus Disposition
Status: COMPLETE (4/4 tasks [x]; reviewer 9/10 SHIP; delivery accepted 2026-05-29; committed). Net −1,134 lines. Next session: /dev-plan Phase 66 (build the scorer, gated on signal-richness).
Objective: Build the missing measurement substrate for a future non-blind agentic eval — structured FAIL-OPEN firing records from the 6 lifecycle-enforcement hooks → .dev-wiki/enforcement.log — so a Phase-66 scorer can measure "did-a-component-fire-and-change-an-action" from observed data. Plus the eval-apparatus disposition. NO scorer this phase. (A scored fixture-replay was reversed at the direction gate as corpus-duplication: the corpus `lifecycle` category already replays event sequences + toggles the enforce marker — see [[instrument-not-score-enforcement-firing-substrate]].)
Scope: templates/.claude/hooks/**, .gitignore, templates/.claude/skills/py-init/**, templates/.claude/skills/ts-init/**, tests/test_firing_log.sh, Makefile, eval/comparison/**, eval/reasoning/README.md, eval/reasoning/with-self-dialogue-*/, .claude/rules/**
Instrumented set (committed): harden+retrofit enforce-spec/enforce-loop/enforce-memory; wire dev-wiki-scope-check/detect-loop/check-tests-were-run. OUT: block-dangerous-bash (safety-class, corpus-covered, secret-leak surface), post-commit (trigger), cognitive-readiness (nudge), the scorer (Phase 66), audit-log/.nana/audit.jsonl.
Key constraints: log is exit-code-neutral under set -euo pipefail (`|| true`; a log failure must NOT abort a hook before its decision — full disk could let rm -rf through); jq --arg ONLY, NEVER raw path/command (secret-exfiltration → controlled-vocab reasons); atomic single >> append, truncation OFF the hot path (existing read-modify-write races); untrack enforcement.log (gitignore + git rm --cached + append at ALL 4 py-init/ts-init sites — no templates/.gitignore); gate OR (.dev-wiki OR .claude/enforce — not AND, else detect-loop silenced); make eval stays 52/52; demote≠delete≠retire; no deletion without a full-tree ref-sweep.
Exit: 6 hooks emit jq-valid records; fail-open proven (chmod-000 AND stubbed jq/date); injection+exfiltration safe; enforcement.log untracked + ignored ×4 sites; test_firing_log green + in make test; test_enforce green; comparison A-vs-C retired (tombstone, A-vs-B kept); reasoning README calibration-only (ablation section kept) + test_templates green; with-self-dialogue dirs gone; make eval 52/52; make test + test_registration + test_settings_template green; referential integrity intact; no scorer shipped.
Abort rule: T1 fail-open unmeetable → STOP (unsafe substrate). Deletion would dangle a working/active-knowledge link OR a test depends on a target → STOP, reconcile. Instrumentation moves make eval off 52 / regresses test_enforce decisions → revert. Do NOT reintroduce a scored fixture eval (settled = corpus-duplication). Blocked >3 → mark [blocked:], report, ask skip-or-abort. T1 is FIRST/falsification checkpoint — prove fail-open before propagating in T2.

Gates:
- [x] Direction confirmed by user (approach approved 2026-05-29 via dev-plan direction gate: "Build eval + disposition" → revised to instrument-substrate-not-scorer after the approach-review corpus-duplication finding; reconfirmed)
- [x] Delivery accepted (post-implementation report — 2026-05-29; reviewer 9/10 SHIP, 2 findings incorporated)
