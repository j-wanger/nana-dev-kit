# Active Phase Context

Phase: 68 - Amplifier Measurement Instrument (real-transcript proxy emitter, control-validated)
Status: COMPLETE (5/5 tasks [x], delivery accepted 2026-05-29, committed + pushed to main 434212a). T1 GO (schema-probe.sh passes on a real transcript — no logging-hook pivot). Emitter eval/amplifier/emit-proxy-vector.sh built TDD; tests/test_amplifier_emitter.sh 14/14 — control pair FLIPS detector + ≥1 interaction proxy, phrase-outside-escalation=false, absent enforcement.log⇒null sentinel, corrupt line⇒parse_errors, read-only shasum-proven. make test 18→19 scripts (0 FAIL; README hand-bumped for the test_templates drift-guard), make eval 52/52, eval/comparison+corpus untouched. Next: /dev-plan Phase 69 — use the validated ruler for the real harness measurement (live off/on run on a real consuming-project, n>1, the verdict; representativeness this phase deliberately did not provide).
Objective: Build Frontier 0 of the amplifier vision — a deterministic READ-ONLY "ruler" (eval/amplifier/emit-proxy-vector.sh) that reads a REAL Claude Code session transcript jsonl + final git/test state and emits a 4-group proxy-vector JSON (mechanical / interaction / enforcement / ground_truth), control-validated via a positive/negative pair (differential FLIP). The instrument the escalation layer (Frontier 1) needs; the handover ranked Frontier 1 highest but it is unfalsifiable without this ruler. Validates the INSTRUMENT only — NO harness verdict, NO live run, NO n>1, NO LLM in the scoring path. See [[amplifier-measurement-instrument]].
Scope: eval/amplifier/** (emit-proxy-vector.sh, schema-probe.sh, SCHEMA-NOTES.md, fixtures/), tests/test_amplifier_emitter.sh, Makefile, README.md (script-count drift-guard), specs/, .dev-wiki/articles/**. Do NOT touch eval/comparison/ (Phase-65 tombstone) or eval/corpus/ (make eval stays 52).
Key constraints: scoring path 100% deterministic (no LLM/embedding/fuzzy); read-only instrument (shasum inputs, no .dev-wiki/.nana writes); ground-truth detector scoped to escalation EVENT boundaries (AskUserQuestion-only for v1); missing source ⇒ explicit sentinel never silent 0; malformed jsonl skip-and-count; proxy schema frozen at exactly 4 groups; NO verdict/grade field. Substrate = transcript: closes Phase-66 firing→action schema-gap + clears Phase-65 corpus-duplication bar. Adding a make-test script bumps the README script count the test_templates drift-guard checks (no auto-sync — hand-bump).
Exit: emitter exists; control pair flips detector + ≥1 interaction proxy; buried-phrase-outside-escalation=false; absent enforcement.log⇒sentinel; corrupt line⇒parse_errors; read-only proven; no LLM ref + no verdict field in eval/amplifier; make test green at new count; make eval 52/52; no edits to eval/comparison|corpus; Phase-69 handoff recorded. ALL MET.
Abort rule: T1 finds transcript lacks distinguishable human-turn/AskUserQuestion/tool-use fields ⇒ STOP, pivot to logging-hook capture (did NOT trigger — GO). Controls can't pass deterministically ⇒ PARK (committed runnable check), NO LLM judge (did NOT trigger). make eval off 52 OR make test regresses ⇒ revert.

Gates:
- [x] Direction confirmed by user (validate-instrument-first + screener same-day-close ground truth, approved 2026-05-29)
- [x] Delivery accepted (post-implementation report — 2026-05-29; spec 8/10 + plan 7/10 reviewers, findings folded; accepted → committed + pushed to main 434212a)
