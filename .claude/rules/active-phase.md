# Active Phase Context

Phase: 67 - Long-Cadence Hook Firing Tests + Hook Firing-Coverage Gate
Status: IMPLEMENTATION COMPLETE (4/4 tasks [x]). The RED-first gate revealed the TRUE gap = 10 untested hooks (not the roadmap's 3) — all 10 now firing-tested (test_long_cadence_hooks.sh + test_tooluse_hooks.sh + memory-nudge in test_harden.sh) via 6 `# fires:` retrofits. Coverage gate GREEN; sandbox install --project-local curator-chain fire added; 3 Phase-63 roadmap items CLOSED with corrections. make test 15→18 / make eval 52/52 / test_registration 41/41 / test_settings_template clean. Reviewer 9/10 accept, all findings incorporated. DELIVERY GATE PENDING acceptance → then commit + push. Next: /dev-plan Phase 68 (a remaining Phase-63 roadmap item; fresh small candidate = memory-nudge `timeout` macOS-portability fix).
Objective: Make "every registered hook has a real firing test" machine-checkable (the functional-smoke invariant is held only in prose today), then fill the gaps it exposes. RED-first meta-test (tests/test_hook_firing_coverage.sh) reports the authoritative untested set before any gap is filled. Closes 3 Phase-63 roadmap items with corrections (gap was 3 not 4; .venv already clean; install plumbing already correct). See [[hook-firing-coverage-gate]]. TESTS ONLY — no new hooks, no modules.json/settings.json change.
Scope: tests/test_hook_firing_coverage.sh, tests/test_long_cadence_hooks.sh, tests/test_install.sh, tests/test_firing_log.sh, Makefile, .dev-wiki/articles/phase-63-remediation-roadmap.md, .gitignore
Key constraints: Denominator = UNION of `jq '.hooks[]|select((.type//"command")!="prompt")|.script'` + every extra_dirs/*.sh (≈21) — literal type=="command" matches ZERO and falsely greens. Coverage is invocation-anchored, NOT bare-filename grep (empirically useless — every hook name appears in registration tests). Un-gameable: pinned exemption allow-list (membership+size) + denominator-sanity floor + a negative control (bogus uncovered hook ⇒ FAIL). Firing tests assert load-bearing SIDE-EFFECTS (not exit-code-only), in mktemp-d + HOME-override isolation. make eval stays 52; test_registration + test_settings_template stay green untouched.
Exit: coverage meta-test GREEN; long-cadence side-effect tests (pre/post-compact + session-stop) GREEN; installer fires the copied session-start.d chain at a sandbox dest; make test green at new count; make eval 52/52; repo .dev-wiki/.nana clean post-test; roadmap marks 3 items CLOSED.
Abort rule: meta-test can't separate untested from justified-exempt without an unmaintainable allow-list → STOP, redesign the coverage signal. make eval off 52 OR test_registration/test_settings_template regresses → revert. Firing test must mutate the kit's own .dev-wiki/.nana to pass → STOP, fix isolation. Blocked >3 → mark [blocked:], report, ask skip-or-abort. T1 is FIRST/checkpoint (report the untested count; if >6, report feasibility before writing all).

Gates:
- [x] Direction confirmed by user (approach approved 2026-05-29 — "Both: tests + coverage gate")
- [x] Delivery accepted (post-implementation report — 2026-05-29; reviewer 9/10 accept, all findings fixed; accepted → committed + pushed to main)
