# Active Phase Context

Phase: 98 — Frontier Watch (rung-B) — the recurring external-signal watch standing up the Ph97 verdict's rung-B charter. IMPLEMENTATION COMPLETE 2026-06-21 (3/3 tasks). READY FOR COMPLETION — delivery gate PENDING (status stays active; do NOT auto-transition).
Objective: Stand up a re-runnable, frozen, controls-validated TWO-SPEED tripwire watch on the agent-tooling frontier — a WEEKLY broad-source/narrow-signal fast scan (primary) + a MONTHLY deep full re-derivation backstop + on-hit escalation to the frozen Ph97 instrument (`pre-registration.md` sha256 600e1c9f). Watches the two Ph97 resolvers: (1) **B5 boundary-validator** — a 2nd primary lab ships a value-capturing GA deterministic boundary-validator (like OpenAI's auto-Pydantic tool-arg validation), OR OpenAI's regresses/deprecates → either → DIFFERENTIATED; (2) the **COMMODITIZED early-warning** — ANY lab ships value-capturing, default-on B1 (opinionated blocking CONTENT, not just the hook surface) or B4 (enforced assumption/plan-adjudication as a configurable default) → moves the CORE toward COMMODITIZED → reopens shrink. DETECTS-AND-FLAGS only; executes NO cut.
Scope: companion/research/watch/** (gitignored, local-only — NEVER shipped) + .dev-wiki/** + .claude/rules/active-phase.md. OUT: any kit code/config change; any shrink/cut (gated to a later phase IF a tripwire fires); editing the frozen Ph97 `pre-registration.md`/`verdict.md` (read-only escalation target); specs/ (the gitignored frozen watch-charter IS the spec — Ph97 deviation precedent).
Key constraints: watch-charter FROZEN (SHA256-attested) before the T2 detector (anti-retrofit); controls-first — the detector MUST flag a seeded tripwire AND read QUIET on attach-surface churn (clean-on-seed = dormant watch = instrument-dead, [[HEU-012]]); broad SOURCES / narrow SIGNAL (won't fire on the fast attach-surface layer that doesn't move the verdict — the Ph97 half-life finding applied PER-TIER: fast cadence on the cheap scan, rare on the expensive re-derivation); DON'T re-run the sweep now (Ph97 read 2026-06-21 is the baseline); escalation re-runs the FROZEN rule (A3 accept), never a fresh one mid-stream; reads DECLARED/productized frontier only (honesty bound inherited from Ph97).
Exit: companion/ gitignored + `git ls-files companion/` empty; `watch-charter.md` frozen (SHA256 `.frozen`) with both tripwires + falsifiable observables + broad source list + two-speed cadence + escalation rule, referencing `pre-registration.md` 600e1c9f + the convergence-map baseline; `run-watch.sh --selftest` flags both seeded tripwires + reads QUIET on attach-churn + baseline controls; weekly+monthly trigger wired (scheduled routine OR dated Blockers tripwire); ZERO kit code/config change; make test PASS + drift 0.
Abort: a detector that can't flag a seeded tripwire (clean-on-seed) → STOP, fix the instrument, NEVER ship a dormant watch; >3 attempts on a task → ask user skip|abort.
Spec: companion/research/watch/watch-charter.md (the frozen instrument, gitignored — deliberate deviation, apparatus is private). Decision: [[frontier-watch]] (high). Ledger: Phase 98 (all_accept:false; A1 reject→broad-source, A2 reject→two-speed weekly+monthly, A3 accept→re-run frozen).
Status: IMPLEMENTATION COMPLETE 2026-06-21 (3/3 tasks; delivery gate pending). watch-charter.md FROZEN (.frozen 8ec4af7b); run-watch.sh --selftest PASS (both seeded tripwires FIRE, both controls QUIET, charter-drift guard exit 3); STOOD-UP-NOT-YET-FIRED with the Ph97 read as baseline; dated Blockers tripwire = the committed scheduling fallback (/schedule trigger OFFERED, maintainer to confirm). A macOS awk `\|`-alternation classifier bug found+fixed inside the apparatus (caught by the controls-first seeded-positives — clean-on-seed, HEU-012). ZERO kit change; make test PASS, drift 0, git ls-files companion/ empty. Keeps the Ph97 differentiated-leaning verdict fresh; both arms of the Ph92 shrink question already agree NO-shrink — this watch is the standing early-warning, not a re-open.
Gates:
- [x] spec — the frozen gitignored watch-charter (companion/research/watch/watch-charter.md, SHA256-attested 8ec4af7b) IS the spec for this phase (Ph97 gitignored-deviation precedent; substance approved at the direction/assumption gate; no specs/ file by design — apparatus is private)
- [x] Direction confirmed by user (assumption positions taken 2026-06-21; A1/A2 reject→revised two-speed broad-source, A3 accept; ledger Phase 98; all_accept:false)
- [ ] Delivery accepted (post-implementation report)
