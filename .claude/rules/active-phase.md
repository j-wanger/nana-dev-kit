# Active Phase Context

Phase: 66 - Signal-Richness Falsification + Scorer Park + audit-log Disposition
Status: IMPLEMENTATION COMPLETE (4/4 tasks [x]). Probe shipped + scorer parked + audit-log KEPT+hardened. Reviewer 7/10 SHIP-WITH-FIXES → 2 real bugs fixed + regression-tested (probe non-object crash; audit-log mkdir fail-open). make test 15 green / make eval 52/52 / test_registration + test_settings_template green / DRIFT none. Net additive, NO scorer code. DELIVERY GATE PENDING acceptance → then commit + push. Next: /dev-plan Phase 67 (a remaining Phase-63 roadmap item).
Objective: The planned "build the scorer" was GATED on a signal-richness checkpoint that FAILED decisively (measured 2026-05-29). So the phase is falsify + park + redirect (user-approved): (1) ship scripts/signal-richness-probe.sh — a read-only deterministic gate that re-checks "is the scorer buildable yet?" in one command; (2) park the scorer behind that runnable trigger + record the 4-reason verdict; (3) redirect build effort to the long-open audit-log disposition. NO scorer code. See [[park-enforcement-scorer-signal-insufficient]].
The falsification: only 11 new-format (schema_version=1) records in enforcement.log — ALL from one hook (dev-wiki-scope-check), ZERO block firings, all phase:"65" self-traffic. The 253 enforce-loop + 23 block records are PRE-instrumentation LEGACY format (no schema_version). Unbuildable for 4 reasons, 3 not fixed by time: volume / skew / REPRESENTATIVENESS (log lives in the kit's OWN .dev-wiki — samples the maintainer editing the kit, never consuming-project agentic work) / SCHEMA GAP (record captures the hook's DECISION, not the agent's subsequent ACTION — no firing→response link).
Scope: scripts/signal-richness-probe.sh, tests/test_signal_richness_probe.sh, tests/test_audit_log.sh, Makefile, templates/.claude/hooks/{audit-log,dev-wiki-scope-check}.sh, modules.json, templates/.claude/settings.json, install.sh, templates/.claude/rules/file-lifecycle.md, README.md, eval/README.md, self-test.md, scripts/harness-audit.sh, tests/test_{install,templates}.sh, eval/corpus/hook-audit-log-*/, eval/corpus/lifecycle-full-session-flow/, .dev-wiki/**, .claude/rules/**
Key constraints: NO silent scorer rebuild — probe COUNTS/CLASSIFIES existing records read-only (never writes/truncates enforcement.log), no ON/OFF harness run, no graded eval scenario added. Signal predicate over schema_version-bearing records ONLY (≥2 distinct hooks AND ≥1 block, after full-{hook,ts,action,reason}-tuple dedup) — rejects the one-hook/zero-block sample. schema_version (NOT phase) is the new/old discriminator. Three distinct negatives: NO-DATA (absent/empty) ≠ NOT-SCOREABLE ≠ CORRUPT (>50% lines unparseable). Park trigger = the probe's PASS condition (runnable, not a calendar note) + names the 2 structural blockers as preconditions. audit-log: it FUNCTIONS (eval-covered) — keep/cut is a subtraction-test on consumer-value (human forensic trail, no code consumer), NOT a broken-hook fix; reconcile ALL coupled surfaces atomically (regenerate settings.json via make template, never hand-edit); don't reintroduce its raw-$FILE_PATH surface into the firing log. make eval: 52 if audit-log kept / 50 if cut (dynamic count — assert the new number).
Surfaced findings (spec adversarial review): suspected double-emit (every multi-event new-format ts appears exactly 2× byte-identical — ambiguous at second resolution; fix ONLY if a clear double-call, else caveat — no dev-wiki-scope-check refactor); dev-debrief writes a DIFFERENT schema {event:debrief} into the same log (off-roster, classify don't choke); test_firing_log already proves all 6 hooks emit when fired (silence = no cause, not broken).
Exit: probe emits the 4-state verdict + real log → NOT-SCOREABLE + functional-smoke test green and in make test (14 scripts); scorer parked (verdict + both structural blockers + runnable trigger, NO scorer code); audit-log DECIDED (verdict + named human consumer + raw-path handling) + executed (keep: tests/ smoke + doc reconcile; cut: full ref-sweep + 2 scenarios deleted → make eval 50 + lifecycle rewire + no dangling refs); make test green / make eval at expected count / test_registration + test_settings_template green; referential integrity intact.
Abort rule: probe can't deterministically separate new-format from legacy → STOP (park unfounded — re-examine the Phase-65 record format). audit-log CUT would dangle a working/active-knowledge link OR a test depends on the target → STOP, reconcile (or choose KEEP). make eval off the expected count / test_registration regresses → revert. Do NOT reintroduce a scored fixture eval. Blocked >3 → mark [blocked:], report, ask skip-or-abort. T1 is FIRST/falsification-formalization checkpoint.

Gates:
- [x] Direction confirmed by user (falsify + park + redirect approved 2026-05-29 via dev-plan direction gate, after the signal-richness gate failed decisively; redirect target = audit-log, probe = script+test)
- [x] Delivery accepted (post-implementation report — 2026-05-29; reviewer 7/10 SHIP-WITH-FIXES, 2 bugs fixed + regression-tested; accepted)
