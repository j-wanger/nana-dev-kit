# Active Phase Context

Phase: 110 — Dogfood UX Pass (Tool-Call Visibility + Artifact Fidelity + Branding). BUILT + ADVERSARIALLY REVIEWED 2026-06-27, 8/8 tasks [x] + 6 review findings fixed — app suite 108→157 green (1 live-flaky e2e passes on retry — model non-determinism, →Ph111), tsc + vite build + cargo check all exit 0; READY FOR COMPLETION (delivery gate flips after the live window-drive + maintainer acceptance). BUILD phase. The first dogfood-driven UX pass: close Ph109's live-drive #1 gap — allowed tool calls render NAME-ONLY because the Pi adapter stubs args:{} (pi-adapter.ts:205) + result:{isError} (:213) and the reduction's SurfaceToolCall drops the output (runtime.ts:38-42), while Pi's event stream ALREADY carries the real args (ToolExecutionStartEvent.args) + output (ToolExecutionEndEvent.result) + partial streaming (ToolExecutionUpdateEvent.partialResult) + typed details (ToolResultEvent.details), and the Ph109 UI components (ToolCallView/DiffView/TestResults/TerminalOutput) already render them inertly — the components were built ahead of the data. A single vertical thread, source→surface. Decision [[tool-call-visibility-thread]] (high). Umbrella spec specs/gui-harness-architecture.md Rev 2 (rides; Ph108/109 precedent).
Objective: thread real tool args/output/typed details through the EXISTING engine-neutral layers ADDITIVELY (no engine-type reshape) so the chat shows WHAT ran + its output; make multi-step tool-loops legible (per-step tool-progress streaming); ship a typed-or-generic artifact panel; replace the placeholder Tauri icon with a real Nana icon. Felt-quality/legibility ships on the maintainer's judgment at delivery (Ph59/80 carve-out); tests assert MECHANICS only.
Scope: app/src/engine/{pi/pi-adapter.ts,vercel/vercel-adapter.ts,types.ts}, app/src/ui/{runtime.ts,chat-binding.ts,tool-call-view.tsx,artifact-feed.ts}, app/src/App.tsx, app/src-tauri/{icons/**,tauri.conf.json}, app/assets/**, app/tests/**, specs/gui-harness-architecture.md (read-only). OUT (→Ph111): signed/notarized bundle + A3 keyring residual + one-click rollback; command palette (axis 3); Claude Agent SDK adapter; pin the flaky live e2e.
Order: T1 adapter forwards real args/output/details (front-loaded spike; resolves A1/A3) → T2 engine-neutral SurfaceToolCall+reduction carry them (additive, JSON-serializable) → T3 inline render through redactSecrets on the inert path (SECURITY-bearing, A2) → T4 tool-progress streaming legibility → T5 typed artifact routing in the UI keyed on tool name (generic-text fallback) → T6 wire the live artifact panel into App.tsx → T7 Nana icon → T8 integration + full-suite/build/cargo gate.
BUILT (8/8, all RED→GREEN→VERIFY + full-suite regression each): T1 `mapPiStreamEvent` pure seam forwards real args + capped output (~16KB) + additive `isError` — the typed `details` capture was DEFERRED (T5 routes on toolName+output instead). T2 SurfaceToolCall gained args?/output?/isError?, reduction threads them, JSON round-trip asserted. T3 chat-binding renders real argsText (bash→command, else JSON) + result via redactSecrets()+truncate; gate-denial affordance preserved (isError=denied only); inert/CSP/key-store rails green. T4 additive `tool-progress` event (tool_execution_update.partialResult) → in-flight streaming (host relays verbatim; no exhaustiveness guard to break). T5 ui/artifact-feed.ts pure router (edit/write whose output looks-like-diff→diff, else→terminal; redacted). T6 ArtifactPanel in App.tsx surface__side; useChatRuntime returns {runtime,artifacts}. T7 dependency-free zlib PNG generator → 1024² Nana mark → `tauri icon` regen (icon.png 96308f3b→7045a15d ≠ stock placeholder). T8 e2e reduction test + the full gate.
ADVERSARIAL PRE-COMMIT REVIEW (6 finders × 2 refuters, 15 raised → 6 confirmed → all fixed; before the debrief commit): (HIGH boundary-1) Pi's real tool_execution_end.result / tool_execution_update.partialResult are AgentToolResult WRAPPER objects {content:[{text}],details}, NOT strings — the adapter forwarded them raw, so the LIVE app would have rendered `{"content":[…]}` JSON noise + the 16KB cap never fired + edit→diff never routed; ALL 142 tests used string fixtures and masked it. FIXED: extractToolText() joins content[].text, redacts-then-caps, used for result+partial (resolves the A1/A3 "does the live local path populate" question — it does, in wrapper form; typed `details` confirmed-available → Ph111). (HIGH security-1) redactSecrets missed AWS-secret (40 base64 w/ '/') + GCP AIza keys that now reach the DOM via gate-allowed `bash cat ~/.aws/credentials`/`env`; (MED tests-1) the same catch-all OVER-redacted git SHAs / sha256 digests. FIXED: specific AWS/GCP/JWT patterns + a base64 catch-all that EXEMPTS hash digests, both-directions pinned in tests/security/redact.test.ts. (LOW artifacts-2) looksLikeDiff false-positived on prose bullets → tightened to require diff-header structure; (LOW reduction-3 / security-2) artifact text + cap-boundary now bounded/redacted at the adapter. 9 findings correctly refuted (e.g. duplicate-React-key → benign append-only render). A stray agent-written dupkey-repro.test.tsx was removed.
UNVERIFIED (deferred to delivery, Ph109 precedent): only the LIVE window-drive felt-quality check remains the maintainer's call — the likeliest functional live failure (the Pi wrapper shape) is now CLOSED by boundary-1 + the full-Pi-path regression test. Felt quality/legibility = the maintainer's judgment.
Discovered → Phase 111: (1) TYPED test/diff-from-structured-details — wire Pi's ToolResultEvent typed `details` (EditToolDetails.diff, etc.) so the panel's diff/test views are structured, not the current diff-shape heuristic + generic-text fallback; (2) signed/notarized bundle + A3 keyring residual + one-click rollback; (3) command palette (axis 3); (4) Claude Agent SDK adapter; (5) pin the flaky live e2e.
Key constraints:
- HIGHEST blast radius = app/src/engine/types.ts (EngineEvent + NormalizedToolCall imported by BOTH adapters + the reduction + the host + the bridge). The Vercel adapter + the host/bridge JSON line protocol are the "consumers you didn't touch" — the new tool-progress union variant MUST be handled there too AND every payload MUST stay JSON-serializable (the bridge relays EngineEvent verbatim → enrichment auto-flows, but only if serializable).
- SECURITY: real tool output is untrusted model-adjacent content — every new field routes through redactSecrets() + stays on the inert React-string path (never raw HTML) + is size-capped at the boundary (~16KB). The inert-render/CSP/key-store tests stay green AND gain assertions for the new args/output fields.
- The gate CORE ([[gate-confirm-approve-loop]] ConfirmationBroker/confirmingGate, key-store hard-deny) stays UNCHANGED — this is a visibility/render pass, not a verdict-path change.
- "Owned not adopted": DiffView/TestResults/TerminalOutput/ToolCallView already exist — do NOT add Vercel AI Elements (forces Tailwind/shadcn, Ph109 finding). Typed view routing lives in the UI (mapToArtifact keyed on tool name), NOT in the adapter (A4-defended — routing-in-adapter pushes UI vocabulary backwards across the Ph108 invariant + duplicates across both adapters).
Knowledge gaps: no prior art on tool-output truncation/size-capping (T1/T3 introduce a documented cap); details-on-LOCAL-path unverified (T1 spike resolves; ledger A1/A3 revisit-status: open).
Exit criteria (ALL PROVEN 2026-06-27): pi-event-mapping + ui (runtime/chat-stream/chat-binding/artifact-feed/artifact-panel/tool-visibility/tool-visibility-e2e) + tests/security + adapter-contract + branding tests PASS; an allowed tool call renders real args + output (adapter→reduction→UI); tool-progress streaming; a typed-or-generic artifact panel; a real Nana icon; inert-render/CSP/capability-guard/gate-confirm NO regression; full app suite 157 green (was 108; 1 live-flaky e2e passes on retry); npm run build exit 0; cargo check exit 0; + adversarial pre-commit review (6 confirmed findings fixed, incl. the HIGH live-engine wrapper-shape bug the 142-test suite masked). Felt-quality/legibility + the live window-drive = the maintainer's delivery-time judgment (deferred).
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip.
Gates:
- [x] spec — umbrella spec specs/gui-harness-architecture.md (nana:approved, Rev 2; Ph108/109 precedent — Surface + Success Vision + the inert-render constraint govern this slice; no separate phase spec). ADR-named, so enforce-spec's specs/phase-110-*.md glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (assumption ledger Phase-110 block appended 2026-06-27: A1/A2/A3 accept, A4 don't-know→defended-accept; all_accept:false)
- [x] Delivery accepted (post-implementation report 2026-06-27; commit lands the BUILT + adversarially-reviewed phase. Live window-drive felt-quality check deferred to the maintainer per Ph109 precedent.)
