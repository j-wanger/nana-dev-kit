# Active Phase Context

Phase: 106 — Project-State Dashboard + Act-from-Page Decision Gate. ACTIVE — planned 2026-06-23, 0/7 tasks. BUILD phase, rung-C pillar 3 extended (the dashboard pillar the maintainer started at Ph99). Extend Ph99's render-only direction dashboard into (1) an on-demand `make dashboard` project-state monitoring page and (2) an OPT-IN act-from-page direction gate (the maintainer decides ON a served dashboard that drives /dev-plan). Decision [[project-state-dashboard]] (high); spec specs/phase-106-project-state-dashboard.md; ledger Phase-106 (all_accept:true).
Objective: ship the monitoring page + the opt-in act-from-page gate, with AskUserQuestion as the default + fail-open fallback, exactly ONE gitignored repo write, no daemon, no new gate semantics, eval 50 stays 50.
Mechanism: the file:// sandbox can't write a repo file, so the gate spins up a tiny 127.0.0.1 EPHEMERAL server that serves the live dashboard, accepts ONE POST /decision, atomically writes .dev-wiki/decision-response.json, then EXITS; the orchestrator blocks on a run_in_background watch (NEVER foreground sleep — blocked in this env, would self-brick future gates, Ph82 class) and ingests via ONE deterministic validate-decision-response.py shared by server+ingest (no neural judge at the boundary). The SERVER owns the single timeout (writes a {status:timeout} sentinel). Three stdlib-only scripts: generate-dashboard.py (imports the direction render fns), validate-decision-response.py, decision-server.py (single-accept latch + atomic mkstemp→fsync→os.replace + Origin/Host/Content-Length/chunked guards). Architecture/Decisions panes CUT to a STATUS+DIRECTION floor (subtraction test).
Scope: scripts/{generate-dashboard,validate-decision-response,decision-server}.py; tests/{test_dashboard,test_decision_server,test_dashboard_roundtrip}.sh + tests/fixtures/{dashboard-*,decision-response.*}; Makefile; .gitignore; README.md; templates/.claude/skills/dev-plan/{direction-brief,assumption-gate,SKILL}.md + MANIFEST; docs/dashboard.html; .dev-wiki/** + .claude/rules/active-phase.md. OUT: any daemon; Architecture/Decisions panes; a YAML/list parser; a shared-scaffold refactor of the 5 generators (themes diverge → 6th); new gate semantics / eval scenario / hook registration / settings.json change; editing the live .dev-wiki living docs.
Key constraints:
- Safe-by-default: opt-in via `.dev-wiki/act-from-page` (absence = today's AskUserQuestion path, server NEVER spawned); fail-open on every failure branch; the act-from-page channel is ADDITIVE, never the sole path ([[HEU-012]]: don't self-brick the gate — Ph82).
- Deterministic validator at the boundary: validate-decision-response.py is RUN by both the server POST and the dev-plan ingest (the orchestrator runs it, never eyeballs nonce/coverage/position).
- Render-only: the dashboard never writes the living docs; static page (interactive=False) emits NO form; tests run on FIXTURES never the always-loaded live docs ([[can't-measure-clean-context-in-kit]] / Ph80 leak).
- Structural staleness: gitignore the response + pre-launch rm + 128-bit nonce + fresh-nonce watcher predicate + consume-once; nonce OPTIONAL only at render/ingest (legacy-skip).
- Presentation value is UNMEASURABLE in-kit (Ph59/80 carve-out) — ships on stated need, tests assert MECHANICS only; A1 (round-trip removes friction) is the weakest assumption + cheaply reversible (rm the marker).
Exit criteria: make test green at 33 registered scripts (README matches, MANIFEST fresh); make dashboard exit 0 → docs/dashboard.html (exit 0 on a nonce-less brief too); the 3 controls-first tests pass incl. every seeded-defect control firing; validate-decision-response.py imported by the server AND invoked by the dev-plan ingest; the default no-marker path provably never spawns the server; eval 50/50; drift 0; zero settings.json change.
Abort rule: if a task fails 3× (RED+GREEN+VERIFY shared counter), mark [blocked: what failed] in tasks.md and ask the maintainer (skip or abort) — do NOT silently skip.
Gates:
- [x] Direction confirmed by user (4 AskUserQuestion rounds 2026-06-23: direction=build-dashboard over Ph105 a/b/c → persistence=on-demand → drive=act-from-page → channel=ephemeral-server; then "Continue"; ledger Phase-106 positions appended, all_accept:true, no unresolved reject/don't-know)
- [x] Delivery accepted (2026-06-23, autonomous run per the maintainer's standing waiver — make test 33-scripts PASS, eval 50/50, drift 0, zero settings.json change; a 12-agent adversarial review caught + fixed 5 findings incl. a MEDIUM dead-control (mutation-verified))
