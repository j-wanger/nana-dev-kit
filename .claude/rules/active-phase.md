# Active Phase Context

Phase: 102 — Pillar 2: contract-driven downstream-worker LOOP (edge-screener). PLANNED 2026-06-22 (5 tasks, ~0%; direction gate closed; spec nana:approved). MEASUREMENT phase — rung-C culmination (pillar 2). NOT YET IMPLEMENTED.
Objective: Build and measure a contract-driven iteration LOOP — a delegated opencode worker re-implements a hard edge-screener function, the contract's deterministic VISIBLE guardrails score it (`check-fidelity.py`), rich feedback (failing assertion + counterexample) on fail, retry up to a capped budget; a HELD-OUT generative integrity test validates the converged impl. Measure whether the loop (a) RECOVERS the Phase-101 single-shot FLOOR (decision-lag — the worker couldn't implement the subtle pending-lag in one shot even when told the invariant) VIA FEEDBACK not mere resampling, and (b) produces TRUTHFUL work vs GAMES the visible guardrails (visible-pass but held-out-FAIL). Connects pillar 1 (the check) + pillar 3 (the dashboard) + pillar 2 (the loop).
Scope: companion/research/contract-loop/** (apparatus, GITIGNORED, like Ph97/98/100/101); .dev-wiki/** + .claude/rules/active-phase.md. OUT: any kit change (SHIPS NOTHING — check-fidelity.py already shipped Ph100); mutating /Users/jwang/edge-screener (per-run copies only); a 24/7 fleet / reusable iteration framework / Claude-Agent worker arm; any scripts/modules.json/hook/test/Makefile change.
Key constraints:
- FEEDBACK ≠ RESAMPLING (adversarial-added 4th arm) — a BEST-OF-N control (N independent attempts, NO feedback, take the best) runs at the loop's N; headline feedback claim = contract-loop − best-of-N, NOT − single-shot. Best-of-N terminal selection pinned in the freeze (first to full-visible-pass, else highest pass-count, ties→first).
- 4 ARMS single-shot / best-of-N / contract-loop / bare-loop, byte-identical except the invariant-naming line (recorded diff). DETERMINISTIC-PRIMARY (held-out = ground truth).
- VISIBLE loop-gate ≠ HELD-OUT validation, gaming DETECTABLE IN PRINCIPLE: a seeded GAMER MUST pass-ALL-visible + FAIL-held-out per task (else redundant held-out → task dropped); a seeded HONORED passes both; every visible check FAILS ≥1 seeded defect ([[HEU-012]]). All-dead → abort.
- HELD-OUT NEVER LEAKS into the worker session — separate file-tree, not transitively reachable from the visible-check imports (grep the tree), + a per-run grep of the full feedback transcript AND the shared prompt voids the run on any held-out name/path/expected-literal hit.
- RE-VERIFY the decision-lag single-shot FLOOR still exists (≥3 seeds) BEFORE the freeze — no floor to recover → STOP + no-floor null.
- The worker CANNOT edit/weaken the scoring (visible checks + held-out scored EXTERNALLY; editable surface = the target function only; counterexample inputs DISJOINT from held-out inputs; scoring copy rebuilt from the frozen contract each iteration).
- TERMINAL-FAILURE BUCKETED (converging-truncated / stalled / regressing via per-iteration visible-pass-count delta); infra-fail (opencode error/no-edit/early-done-with-visible-fail) excluded + re-rolled, NOT scored as floor.
- PINNED EFFECT FLOOR (non-overlapping intervals OR delta ≥ k·spread) below which = "directional/underpowered, routed forward" — no tiny-n over-read.
- BYTE-FROZEN pre-registration before scored runs; pilot + controls + floor-recheck precede the freeze. REAL EDGE-SCREENER NEVER MUTATED (per-run copies + checksum before/after). SHIPS NOTHING; apparatus gitignored; claim scoped + worker pinned (opencode/big-pickle, this held-out probe).
Exit: T1 controls recorded (seeded gamer passes-visible+fails-held-out, honored passes both, each visible check reds on a seeded defect, held-out unreachable, arm-parity one-line diff); T2 pilot.md (decision-lag single-shot fail-rate ≥3 seeds — floor still exists OR no-floor null; pilot-loop iterations are real retries; transcript leak-grep clean); T3 analyze-loop.py --selftest exit 0 + shasum -c .frozen OK BEFORE any scored run; T4 results.md = decision-lag + ≥1 task × 4 arms × n≥3 with per-iteration trace + terminal/held-out verdicts + run-status + bucket, leak-grep clean; T5 verdict.md = mechanical per-arm-with-intervals (floor-recovery + feedback lift contract-loop−best-of-N + iterations-to-converge + buckets + gaming rate; below effect floor = directional/underpowered) OR no-floor null, claim scoped + worker pinned; /Users/jwang/edge-screener checksum identical before/after + git clean; make test PASS, make eval 50/50, drift 0; companion/ untracked.
Abort: clean-on-seed (a held-out that passes its seeded gamer, or a visible check that reds on no seeded defect) → STOP, never ship an undetectable-gaming screen. No separable visible/held-out split for any task → floor-recovery only / informative null. decision-lag no longer floors single-shot → no-floor null + close-out. An un-closable held-out leak OR opencode can't iterate (blind restart) → STOP + re-scope. >3 attempts on a task → ask user skip|abort.
Spec: specs/phase-102-contract-loop.md (nana:approved 2026-06-22). Decision: [[contract-loop]] (medium). Ledger: Phase 102 (all_accept:true — A1 rich feedback, A2 visible+held-out, A3 3 arms [+best-of-N adversarial-added], A4 opencode loop).
Gates:
- [x] spec — specs/phase-102-contract-loop.md (nana:approved 2026-06-22, dev-plan --internal)
- [x] Direction confirmed by user (gate closed 2026-06-22, ledger Phase-102, all_accept:true — A1/A2/A3/A4 accept)
- [ ] Delivery accepted (post-implementation report)
