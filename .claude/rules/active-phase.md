# Active Phase Context

Phase: 107 — Decisioning Cockpit (dashboard-as-primary gate). ACTIVE — planned 2026-06-23, 0/5 tasks. BUILD phase, rung-C pillar 3 extended again (the dashboard pillar from Ph99/Ph106). The maintainer ran the Ph106 dashboard and found it UNUSABLE ("the UI is terrible, it doesn't possess enough information for anyone to even make a decision, not usable at all"). Rebuild it into a usable decisioning surface. Decision [[decisioning-cockpit]] (high); spec specs/phase-107-decisioning-cockpit.md; ledger Phase-107 (all_accept:true).
Objective: rebuild docs/dashboard.html into a tabbed DECISIONING COCKPIT (Status | Decide | Workflow) that becomes the PRIMARY /dev-plan direction-gate surface — served live at each gate by the existing ephemeral decision-server — with per-option REASONING+CONSEQUENCES laid out for comparison, a LOUD stale-brief guard, and the harness Workflow page re-rendered NATIVELY into the shared visual system. AskUserQuestion stays the fail-open fallback. Reverses Ph106's STATUS+DIRECTION-floor subtraction on stated need.
Direction (locked across 3 AskUserQuestion rounds 2026-06-23): (1) refine-the-dashboard (maintainer rejected the 4 abstract Ph107 forks A/B/C/D); (2) DASHBOARD-AS-PRIMARY gate over on-demand+freshness-fix — FLIP Ph106's AskUserQuestion-default/opt-in; (3) re-render workflow NATIVELY over iframe.
Mechanism: extend the brief schema (optional per-option reasoning/consequences in generate-direction.py, backward-compatible) + a deterministic is_stale() phase guard; rebuild generate-dashboard.py render_dashboard into a tabbed cockpit (frontend-design visual pass; interactive gates the form); refactor generate-workflow.py to expose content FRAGMENTS the cockpit composes (its standalone docs/workflow.html keeps building); the existing decision-server auto-upgrades (it serves render_dashboard interactive=True); flip the dev-plan Step 13 companion flow to cockpit-by-default-for-interactive + fail-open-on-every-branch.
Scope: scripts/{generate-direction,generate-dashboard,generate-workflow,decision-server,validate-decision-response}.py; tests/{test_dashboard,test_decision_server,test_dashboard_roundtrip,test_scripts_smoke}.sh + tests/fixtures/dashboard-brief.{cockpit,stale}.json; Makefile; README.md; docs/{dashboard,workflow}.html; templates/.claude/skills/dev-plan/{direction-brief,assumption-gate,SKILL}.md + MANIFEST; .dev-wiki/** + .claude/rules/active-phase.md. OUT: any daemon; a shared-scaffold refactor that UNIFIES the 5 generators (import fns / compose fragments only — each still renders independently); a YAML/list parser; new gate SEMANTICS (positions stay accept/reject/don't-know, ledger row = sole firing evidence); a new eval scenario; a settings.json change / hook registration; editing the live .dev-wiki living docs in tests (tests run on FIXTURES).
Key constraints:
- DON'T SELF-BRICK THE GATE ([[HEU-012]], Ph82 class): dashboard-as-default MUST fail open to AskUserQuestion on EVERY failure branch (spawn fail, render/import error, validator error, timeout sentinel, headless/no-browser). The default flips; the fallback never disappears — "is the fallback reachable" is a TEST not a claim. (A2, the HARD constraint.)
- NEVER foreground sleep (blocked in this env — would self-brick future gates): the orchestrator waits on a run_in_background watch; the SERVER owns the single bounded timeout ({status:timeout} sentinel).
- STALE BRIEF IS NEVER SILENTLY DECIDABLE: phase-mismatch/malformed/empty → a loud banner + NO submittable form on the served page; defense-in-depth, the validator rejects a wrong-phase/wrong-nonce POST (4xx, no write). A fresh, phase-matching brief is the ONLY decidable state. make dashboard (static) still exits 0 on a nonce-less/legacy brief.
- interactive=False → the rendered HTML contains NO form and the render path performs NO .dev-wiki write.
- ONE gitignored repo write (.dev-wiki/decision-response.json, via the server only); keep the 5 generators SEPARATE (import/compose, don't unify themes); iframe fallback is SHIP-BLOCKING (locked OUT at the gate → escalate, never ship autonomously); stdlib-only; HTML-escape all brief text; 127.0.0.1 + port-0 only.
- UI quality is UNMEASURABLE in-kit (Ph59/80 carve-out): ships on the maintainer's stated need + judgment at the DELIVERY gate; tests assert MECHANICS only (option-scoped reasoning/consequences container, stale-banner-no-form, native workflow fragments not iframe, served-path stale GET/POST, fail-open reachability).
Exit criteria: make test green at 33 registered scripts (README matches, MANIFEST fresh); make dashboard exits 0 → docs/dashboard.html as a tabbed cockpit (Status|Decide|Workflow), exit 0 on a nonce-less and on a stale brief (stale → loud banner, NOT a silent decidable form); per-option reasoning+consequences render option-scoped; the Workflow tab carries native re-rendered content (≥2 distinct section markers, no iframe) AND generate-workflow.py still builds docs/workflow.html standalone; the stale-brief guard fires on a seeded phase-mismatch fixture; the served path falls open to AskUserQuestion on spawn-fail/timeout/headless (asserted by a test); validate-decision-response.py still the shared boundary; make eval unchanged (verify the count, Ph106 records 52); drift 0; zero settings.json change.
Abort rule: if a task fails 3× (RED+GREEN+VERIFY shared counter), mark [blocked: what failed] in tasks.md and ask the maintainer (skip or abort) — do NOT silently skip. If the T3 workflow fragment seam forces a deep restyle that risks the standalone output → STOP and escalate (iframe was locked OUT).
Gates:
- [x] Direction confirmed by user (3 AskUserQuestion rounds 2026-06-23: refine-the-dashboard → dashboard-as-primary over on-demand+freshness → native-workflow-render over iframe; remaining load-bearing assumptions stated + accepted; ledger Phase-107 appended, all_accept:true, no unresolved reject/don't-know; spec Tier-1 reviewed 7/10→revised)
- [x] Delivery accepted (2026-06-23 — maintainer accepted the cockpit at the delivery gate; make test green, eval 50/50, zero settings.json change; a 7-invariant adversarial review returned SHIP, 2 LOW fixed inline. Note: maintainer's installed ~/.claude lags by 3 dev-plan companions until `install.sh` re-sync — the live dashboard-primary flow activates then.)
