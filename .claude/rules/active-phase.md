# Active Phase Context

Phase: 109 — Felt-Quality Surface + Branding. ACTIVE — BUILT 2026-06-26, 6/6 tasks [x] (T6 webview↔engine bridge discovered mid-phase, maintainer-approved); app tests 63→108 green, tsc+vite+cargo check all exit 0; READY FOR COMPLETION, DELIVERY ACCEPTED ("I'll do the verification later, anything feels off can be fixed next phase") — the delivery gate flips after the commit lands. BUILD phase, the drivable felt-quality slice of THE PIVOT: Ph108's placeholder App.tsx became a real assistant-ui surface wiring the 5-axis thesis (axes 4/1/5/2; axis 3 deferred A5) to the existing engine-neutral reduction, plus the minimum context-assembly seam + a Rust-spawned Node-sidecar bridge that makes the harness genuinely drivable. Decisions [[felt-quality-surface]] (updated BUILT) + [[webview-engine-bridge]] + [[gate-confirm-approve-loop]] (all high); ledger Phase-109 all held (A1 open→held: spike clean; none bit). Umbrella spec specs/gui-harness-architecture.md Rev 2.
Objective: turn the placeholder app/ surface into a real, drivable assistant-ui desktop surface that makes Ph108's rails FELT, plus the minimum context-assembly seam (workspace AGENTS.md/CLAUDE.md + .claude/rules/*.md auto-loaded per turn, loud-on-missing; A2) + the webview↔engine bridge. Felt-quality/joy ships on the maintainer's judgment at delivery (Ph59/80 carve-out); tests assert MECHANICS only.
Scope: app/src/{ui,host,gate/confirm,context}/**, app/src/engine/adapter.ts (additive systemContext), app/src/App.tsx + styles.css, app/src-tauri/src/lib.rs (Node-sidecar spawn/proxy), app/package.json, app/tests/**, specs/gui-harness-architecture.md (read-only). OUT: command palette (axis 3); Claude Agent SDK adapter; signed/notarized bundle (Phase 110+).
Status (BUILT): A1 spike CLEAN (useExternalStoreRuntime + custom convertMessage, zero engine reshape; AI-SDK fallback never needed; clone-on-event is the #1 detail). T3 real host-owned approve-loop (ConfirmationBroker + confirmingGate on the existing setToolCallGate seam; SECURITY: only the literal "requires explicit human confirmation" marker is confirmable, key-store denials stay HARD-denied, tested vs the REAL gate). T6 Rust-spawned Node sidecar + JSON line protocol + BridgeClient-as-EngineAdapter (webview manifest stays core:default + CSP connect-src 'self'; esbuild ESM — CJS fails on pi-coding-agent export-map). app 108/108 green; inert-render/CSP/capability guard still green. UNVERIFIED: the LIVE end-to-end window round-trip (maintainer's deferred launch check; likeliest failure = Rust spawn + NANA_ENGINE_HOST_JS env-path under real tauri dev).
Exit criteria: ui/chat-stream + context/assembly + ui/gate-confirm + ui/artifacts-revert PASS, inert-render green, full suite green, npm run build exit 0 — ALL PROVEN. Criterion #1 window-LAUNCH + felt-quality/joy = the maintainer's delivery-time checks (the live round-trip is deferred).
LIVE-DRIVE VERIFIED (dogfood 2026-06-27): the maintainer launched `npm run app` and drove a real prompt→response round-trip — the full bridge works (webview → Rust spawn → node sidecar → local model → back). THE GATE WORKS LIVE (a destructive request held + the approve/deny dialog fired — the security spine through the GUI, confirmed). Launch blocker fixed: cargo wasn't on the login-shell PATH → the `app` script now prepends $HOME/.cargo/bin. Two quick UX fixes shipped same session: a Stop button (composer Cancel → bridge → host abort) + a "working…" activity indicator.
Discovered → Phase 110 (dogfood-driven UX pass is the headline): (1) TOOL-CALL VISIBILITY — the #1 gap: allowed tool calls render NAME-ONLY because the Pi adapter forwards args:{} (tool_execution_start) + result:{isError} (tool_execution_end), not the real command/output; thread the real args+output through adapter→reduction→UI (SurfaceToolCall currently drops the result — Ph108 reduction) so the chat shows WHAT ran + its output; (2) multi-step tool-loop legibility (a long local-model run looked frozen — the Stop button + indicator help, but per-step progress/streaming would help more); (3) real app icon + branding; (4) richer artifact pipeline (live tool-results → typed diff/test/terminal views); (5) signed+notarized bundle (folds host-sidecar production bundling + the A3 keyring residual + one-click rollback); (6) pin the flaky live e2e; (7) command palette (axis 3); (8) Claude Agent SDK adapter (key-gated). Local-model latency (~4 min for a 5-tool-call loop) is the MODEL not the harness — the lever is making the wait legible + interruptible, not faster.
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip. (N/A — all tasks complete.)
Gates:
- [x] spec — umbrella spec specs/gui-harness-architecture.md (nana:approved, Rev 2; A4 — Surface + Success Vision + Exit criterion #1 govern this slice; no separate phase spec). ADR-named, so enforce-spec's specs/phase-109-*.md glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (ledger Phase-109 appended 2026-06-26: A2 reject→revised [minimum context-assembly into scope]; A1/A3/A4/A5 accept; revisit-status filled, A1 open→held)
- [x] Delivery accepted (post-implementation report 2026-06-26; commit f45f462. Live end-to-end window round-trip + felt-quality = maintainer's DEFERRED launch checks per "I'll do the verification later, anything feels off can be fixed next phase")
