# Active Phase Context

Phase: 113 — Felt-Quality Build — Axis 3: Command Palette + Keyboard Control. BUILT + ADVERSARIALLY REVIEWED 2026-06-27, 6/6 tasks [x] — app suite 274→310 green, tsc + `npm run build` + `cargo check` all exit 0; READY FOR COMPLETION (delivery gate flips after the commit lands + the maintainer's standing live window-drive check). OUTCOME: T2 spike resolved conversation state = UI-SIDE (both adapters stateless across sendPrompt) → new-conversation is a store-clear, NO host reset, HostInbound UNCHANGED (no-bypass invariant strongest). Adversarial review (3 focused finders) caught 1 CONFIRMED felt-safety footgun the 307 tests missed — `approve-gate` was the palette's index-0 default when a gate was held, so Cmd+K→Enter could approve a held destructive action; FIXED by a `dangerous` flag (palette EXCLUDES approve-gate → reachable only via the modal button + deliberate Cmd+Enter; deny stays, safe) + 2 hardenings (Esc preventDefault only when it stops; palette restores prior focus on close) + 3 regression tests. No-bypass + inert/XSS lenses CLEAN. revertLast double-revert is idempotent (checkpoint.revert re-writes the same pre-edit bytes — non-bug). BUILD phase (FELT-QUALITY) — a deliberate pivot back to felt-quality after the Ph108-112 security-hardening run. Builds the LONE unbuilt north-star control/joy axis (axis 3 — "every action reachable by button, shortcut, or palette search", specs/gui-harness-architecture.md:84; axes 1/2/4/5 shipped Ph109-111). Today the app has ZERO keyboard shortcuts and the gate/revert/stop are mouse-only; a daily-driver GUI meant to replace a terminal must be keyboard-competitive. Hand-rolled, NO `cmdk` dependency (Ph109 "owned not adopted"; ~6 static commands don't justify a runtime dep + supply-chain surface in a strict-CSP, sandbox-hardened app). Decision [[axis3-command-palette]] (high); rides umbrella spec specs/gui-harness-architecture.md (Ph108-112 precedent, ADR-named). Builds on [[felt-quality-surface]] (Ph109, axis-defining), [[tool-call-visibility-thread]] (Ph110), [[typed-artifact-fidelity]] (Ph111).
Objective: a small PURE engine-neutral command registry (`app/src/ui/commands.ts`, outlives the palette UI) + a custom Cmd+K command palette (`command-palette.tsx`) + a keyboard shortcut layer (`use-command-shortcuts.ts`), dispatching EXISTING actions (stop/approve/deny/revert/focus-composer) + the ONE genuinely-missing action (new-conversation). Order: T1 pure registry (the ctx source) → T2 new-conversation + surface runtime control state on `useChatRuntime` (host-side vs UI-side conversation-state spike) → T3 standalone palette UI (jsdom) → T4 standalone shortcut hook → T5 the SINGLE App.tsx integration + no-bypass invariant → T6 full gate + focused adversarial review + residuals + BUILT.
Scope: app/src/ui/** (commands.ts, command-palette.tsx, use-command-shortcuts.ts, chat-runtime.ts, engine-bridge.ts, index.ts), app/src/App.tsx, app/src/styles.css, app/src/host/engine-host.ts (benign `reset` only, if host-side), app/tests/**. OUT (→ a future security phase): Vercel bash output redaction; cross-platform OS-sandbox (Linux landlock/bwrap + Windows job-objects). OUT (deferred): the live window-drive felt-quality check (Ph109-112 precedent).
DECISIONS LOCKED (assumption gate, ledger Phase-113 all_accept:true): A1 = build axis 3 NOW as the bet that supply creates demand (the spec's "used weekly" test spec:80 has never run — live drive deferred 4x; honest residual, not evidence); A2 = custom hand-rolled, NO cmdk; A3 = new-conversation is the one new action; A4 = mechanics-only tests, felt-quality on maintainer judgment.
Key constraints:
- SECURITY (the constant): NO new privileged host path — the palette re-dispatches existing bridge/runtime actions through existing channels. The gate VERDICT-LOOP core (ConfirmationBroker/confirmingGate/key-store hard-deny) + inert-render + strict CSP stay UNCHANGED. Command titles render INERT (model-influenced file paths).
- Two felt-safety footguns are task-bound: bare-Enter must NOT auto-approve a held destructive gate (T4); single-key shortcuts must NOT fire while the composer (assistant-ui `ComposerPrimitive.Input` — a textarea/contenteditable, NOT a bare `<input>`) is focused (T4).
- No-bypass invariant (T5, the Ph112 precedent pattern): the engine-host `HostInbound` union is UNCHANGED (prompt|gate-verdict|revert|interrupt) OR grew by EXACTLY ONE benign non-gate `reset` — no gate/approval message type added/altered.
Exit criteria: Cmd+K opens a custom palette that filters by query and dispatches the EXISTING action surface by search + keyboard shortcut, approve/deny present ONLY when a gate is held; a pure node-testable command registry is the durable core; the no-bypass invariant holds; bare-Enter never auto-approves a held gate; single-key shortcuts never fire in a textarea/contenteditable composer; command titles inert; inert-render/CSP/gate-confirm NO regression; full app suite green + `npm run build` + `cargo check` exit 0; + focused adversarial review confirmed-findings fixed. Felt-quality / joy + control + the live window-drive = the maintainer's deferred judgment (Ph59/80 carve-out).
Abort rule: a task failing 3× → mark [blocked] in tasks.md + ask the maintainer (skip or abort), never silently skip. T2 spike: if conversation state is host-side, the `reset` message MUST stay benign (no gate/approval semantics) or fall back to a UI-side clear.
Gates:
- [x] spec — umbrella spec specs/gui-harness-architecture.md (nana:approved, Rev 2 — Success Vision's five axes govern this slice; axis 3 is the unbuilt one; Ph108-112 precedent). ADR-named, so enforce-spec's specs/phase-113-*.md glob can't auto-match; this marker records the genuinely-satisfied spec gate.
- [x] Direction confirmed by user (assumption positions taken 2026-06-27; ledger Phase-113 block appended, all_accept:true — A1-A4; --gate 113 exit 0)
- [x] Delivery accepted (post-implementation report 2026-06-27; maintainer accepted + chose commit-to-main — full gate 310/310 + tsc/build/cargo exit 0 + adversarial review confirmed-footgun fixed; the live window-drive felt-quality check stays the maintainer's deferred call per Ph109-112 precedent)
