# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 71 - Cross-Boundary Retention Headroom Screen

### Cross-Boundary Retention Screen
from: [[decision:cross-boundary-retention-headroom-screen]]
retrieved: 2026-05-30

- The cross-compaction retention value of the harness state machinery has NEVER been measured — prior eval tests only PLUMBING (hook exits 0, advisory fires), never whether a constraint is RETAINED vs re-derived. Phase 71 is that first measurement.
- Confound core: Claude Code's own compaction summary is NOT a blank slate (the one real corpus summary is 11,470 chars and references phases/active-phase/tasks.md unprompted) — so the screen must isolate the INCREMENT the harness adds; OFF MUST be a model-authored summary (real mechanism), never a hand-truncation (which starves OFF and inflates headroom).
- Headroom (if any) lives in MOTIVATED counter-default decisions (derivable from dropped premises) — distinguishing genuine process-retention from the Phase-70 "headroom-only-on-unknowables" result, which would otherwise let the screen declare false victory by rediscovering its own prior negative. The pinned rationale-probe litmus + human authorization gate CONTINUE.
- Discovered latent finding: post-compact.sh READS .claude/.session-anchor but nothing in the repo WRITES it (dead recovery branch) — recorded for a future phase, NOT fixed here (freeze-the-subject: editing the machinery under measurement invalidates the screen).

### Parent Screen (Phase 70 method, ported)
from: [[decision:amplifier-anchor-headroom-screen]]
retrieved: 2026-05-30

- The check.sh run_check/aggregate/verify_pins + consensus-by-clause (≥4/5 same clause-id, NO LLM in scoring) port VERBATIM; only diff_verdict + --diff/--stability are new. Pre-registration committed in a SEPARATE ancestor commit before verdicts (git merge-base --is-ancestor anti-retrofit). Controls-first; the bare-derivation gate disqualifies model-default candidates before spend.
