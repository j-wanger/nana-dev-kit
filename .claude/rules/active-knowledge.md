# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 77 - Cross-Session Retention Headroom Screen (audit-gated ablation)

### Prior regime nulls (Ph70 / Ph71)
from: [[amplifier-anchor-headroom-screen]] | [[cross-boundary-retention-headroom-screen]]
retrieved: 2026-06-04

- Ph70: bare claude-opus-4-8 does the domain reasoning UNPROMPTED; single-decision harness headroom lives ONLY in unknown/proprietary facts (a retrieval problem) — all 4 anchors DEGENERATE 5/5, TERMINATE.
- Ph71: the native compaction summary is decision-comprehensive WITHIN a session — single-session retention headroom is NULL (TERMINATE-by-summary-robustness). The surviving untested regime is cross-SESSION (native summary dies, on-disk substrate persists) — this phase.
- The frozen `retention-screen/` apparatus (consensus-by-clause check.sh, leak-check, assert-off-on-isolation, NO-LLM, ancestor-guarded pre-reg) is the reusable instrument — clone byte-verbatim where the cross-session boundary permits.

### Campaign method invariants
from: [[amplifier-anchor-headroom-screen]]
retrieved: 2026-06-04

- Deterministic scoring only — NO LLM judge (the load-bearing guard against false-HAS-HEADROOM).
- Controls must validate the instrument, else a null is INSTRUMENT-DEAD, not real (positive control gates the null).
- Pre-registration committed BEFORE runs (anti-retrofit `git merge-base --is-ancestor`); cheapest-check-before-expensive-work (the residual audit gates whether T2 is built at all).

### This phase's honest prior + caveat
from: [[cross-session-retention-headroom-screen]]
retrieved: 2026-06-04

- Cross-session is likely degenerate too (git-log+code+tests carry the decisions); the deliverable is an un-foolable verdict either way.
- A null residual ≠ substrate worthless — its value may have precipitated into git via debrief discipline; record that caveat so the verdict is not over-read.
