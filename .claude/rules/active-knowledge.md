# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 83 - Prune-on-Value Subtraction

### Zero-classification hazard (dominant control)
from: [[HEU-012]]
retrieved: 2026-06-09

- A zero can measure broken plumbing, not absent demand (enforcement dormant 15 days; memory CWD mis-pointed ~34 phases): couldnt-fire-when-armed = DEFECT finding, never a demand-evidence cut.
- Arm preconditions in mktemp -d sandboxes only (never live state); assert allow AND block paths; Phase-82 zeros measure demand post-restoration.

### Gate evidence + execution rules
from: [[decision:prune-on-value-subtraction]]
retrieved: 2026-06-09

- Live install lacks fastembed (verified 2026-06-09): storage.py reinforces only at cosine >0.90 — unreachable without embeddings; word-overlap fallback only WARNS → candidate 2's 0/55 is largely pre-classified couldnt-fire → keep/harden, not cut.
- Installed surface is DISCOVERED not assumed (A3 reject → A6): kit-marker scan of this machine; every cut's liveness grep covers ALL discovered roots.
- Settings merge is add/update-only — no deregistration mechanism exists: cuts perform sandbox-rehearsed jq surgery on live settings, own ghosts ONLY (the other 10 stay under the Phase-82 drift filing), survivor functional smoke + revert-on-failure.
- Phase-72 liveness semantics: alive = LIVE reference outside the removal set; HISTORICAL dev-wiki records legitimately name removed things and are NEVER rewritten (supersede instead).

### Binding priors + evidence standards
from: [[decision:audit-log-disposition]] + [[decision:memory-architecture-classification]] + [[decision:single-source-scope-tagged-hook-registration]] + [[decision:qa-verification-sweep]]
retrieved: 2026-06-09

- Candidate 4 = model field only (audit-log itself KEEP); candidate 3 = scaffold-shipping only (A5 down-scoped — the kit-side memory-layer question is deferred must-revisit); modules.json is the single hook source — make template regenerates, never hand-edit generated artifacts.
- Matrix rows are the evidence; enforcement.log has a provenance hazard — never a cut's sole anchor; frozen apparatus (eval/qa-sweep/** beyond the documented denominator change, eval/amplifier/**, eval/assumption-screen/**) read-only.
- An enforce-memory cut removes its 3 eval scenarios (52→49): the spec authorizes an EXPLAINED denominator change, not any specific cut; no candidate cut empties a settings matcher group.
