# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 85 - Install-Gap Fix + Edge-Screener Dogfood

### Install paths + checker blindness (line-cited at plan time)
from: [[decision:install-gap-dir-currency]]
retrieved: 2026-06-10
- install.sh:14 set -euo pipefail; GLOBAL path (:288-309) copies only scope:global hooks FLAT (:298-302, zero extra_dirs); --project-local reads extra_dirs at :72, copies at :88-93 via `cp "$HOOKS_SRC/$d"/*.sh` — an empty/no-match glob ABORTS under set -e.
- check-install-drift.sh pass 2b (:103-108) uses find -maxdepth 1 — structurally blind to subdirectories; diff loop (:119-132) emits missing:/differs: rows; cmp -s is content-only (:128); orphan semantics decided: FLAG as orphan: row, never remove (charter :101 "never ADD").
- py-init SKILL.md:108 + ts-init SKILL.md:145 cp -R recursive (carries session-start.d) — consuming-project scaffold is NOT the gap; leading hypothesis (T1 verifies): incident 5 = Phase-82 drift-guided resync using checker output as shopping list; install.sh global cannot have shipped session-start.sh (scope:project, modules.json:51).
- Copied .d files get no chmod — they are SOURCED, exec bit not load-bearing; mode-drift exemption pinned in the inventory.

### Registration surface + edge-screener migration
from: [[decision:install-gap-dir-currency]]
retrieved: 2026-06-10
- register-settings.py: project-local commands default ${CLAUDE_PROJECT_DIR}/.claude/hooks (:65), global ~/.claude/hooks (:69); UPSERT-ONLY — cannot dedupe edge-screener's hand-patch; --project-local writes .claude/settings.local.json (install.sh:69); CWD-relative — assert project root before writing.
- edge-screener: kit hooks hand-patched in .claude/settings.json, NO settings.local.json exists, .bak is the Phase-79 rollback (poisoned post-migration — explicit disposition required); its session-start.d/ already populated + verified identical to templates.
- ~/.claude/hooks/session-start.sh is registration-dead (zero refs in ~/.claude/settings.json; live registrations all project-local); ship-vs-dispose decided at checkpoint 1.
- DRQ-1 (settings.json vs settings.local.json duplicate-hook merge) has NO record anywhere — resolve empirically in task 6; STOP and re-present if not double-fire.

### Prior-phase end-states to preserve
from: [[decision:hook-registration-hygiene]] + [[decision:prune-on-value-subtraction]]
retrieved: 2026-06-10
- Phase-84 end-state: ~/.claude/settings.json kit hooks == modules.json scope:global set (context-size-check only); basename-normalize over mixed command forms; backup + tested restore + HARD checkpoint before live settings edits; positive control before any matrix/extraction output counts (zsh word-splitting hazard).
- Phase-83 couldnt-fire trap: a memory-layer zero counts as demand evidence ONLY with a liveness probe (server-start cmd + exit code + DB row count); fastembed lives only in the maintainer venv; edge-screener has no .memory/.
- A5 ledger row (Phase 83, revisit-status: open) in .dev-wiki/assumption-ledger.md — this phase files evidence, NOT the disposition.
