# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->
## Phase: 69 - Amplifier Representativeness Audit + Anchor-Validity Verdict

### The recon falsified the live-run plan — audit, don't measure
from: [[amplifier-representativeness-audit]]
retrieved: 2026-05-29

- A read-only recon ran the Phase-68 ruler over all 8 real consuming-project transcripts BEFORE committing to the handover's live off/on run. Result: `ground_truth.surfaced=false` on all 8, AUQ-scoped phrase hits=0 on all 8 — the look-ahead decision surfaces in assistant prose/code/eval-frameworks (8–50× raw), NEVER inside an AskUserQuestion. The v1 AUQ-only predicate is a STRUCTURAL false-negative on real provenance (the SCHEMA-NOTES line-50 "revisit in Phase 69" caveat, now confirmed).
- The anchor is DEGENERATE: the look-ahead concept appears even in the harness-OFF baseline transcripts ⇒ the base model handles it unprompted ⇒ no headroom ⇒ nothing for the harness to add. (Recurs the Phase-59 commodity-knowledge lesson: retrieval/harness lift doesn't pay where parametric knowledge is already strong.)
- So Approach A (user-chosen via AskUserQuestion): turn the recon INTO the deliverable. NO live run (premature around a degenerate, detector-invisible anchor). Audit-only — do NOT patch the emitter (predicate repair is the deferred Approach-C work, gated by the measurability predicate).

### Verdict-as-runnable-predicate + the honesty rails
from: [[amplifier-representativeness-audit]] + [[park-enforcement-scorer-signal-insufficient]]
retrieved: 2026-05-29

- Ship `measurability-gate.sh` (the Phase-66 runnable-probe idiom, NOT a calendar note): classifies MEASURABLE/NOT-MEASURABLE/NO-DATA over a transcript set; encodes representativeness (≥2 distinct ON ∧ ≥2 distinct OFF in-boundary events ∧ an OFF-vs-ON differential; planted-fixture shasums EXCLUDED; threshold read from VALID-MEASUREMENT.md). Runs NOW → NOT-MEASURABLE. It is the Phase-70 trigger — only a flip to MEASURABLE unblocks the expensive live run.
- The gate must be falsifiable in BOTH directions (`--selftest` asserts a MEASURABLE and a NOT-MEASURABLE scenario) — guards against vacuously-RED-forever (blocks the program) AND trivially-GREEN (greenlights a bad experiment).
- 8/8-false must be attributable to DATA not a dead detector: the survey's positive-control row RUNS the ruler on the planted `surfaced.jsonl` (→true). Report `escalation_count` SEPARATELY from in-boundary-match so "0 escalations" ≠ "N escalations, 0 matches" (5/8 real transcripts have escalation_count≥1).
- Honesty rails: verdict vocabulary is about the ANCHOR/INSTRUMENT, never harness value (disclaimer + no machine verdict token). Apparatus disposition is HONEST — the ruler is NOT yet a validated gate (NOT-MEASURABLE), so comparison stays tombstoned, reasoning stays calibration-only, the binary corpus stays the SOLE gate; "retired-for-good" waits for the gate to flip. make eval frozen at 52; read-only; deterministic.
