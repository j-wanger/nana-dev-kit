# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 84 - Hook & Registration Hygiene

### Defect diagnosis + fixture circularity (verified at gate)
from: [[decision:hook-registration-hygiene]]
retrieved: 2026-06-09

- post-commit.sh:21 reads top-level `.exit_code // empty` → fast-path exit 0 at :24-26; detect-loop.sh:34-35 grep/sed-extracts from raw JSON, exits 0 at :38-40 when absent; both check ONLY $HOME/.claude/enforce (post-commit:10, detect-loop:27).
- Pinned real events carry tool_response:{stdout,stderr} with NO exit-code field at ANY level — T2 capture settles the branch (remap/redesign/upstream) before any fix.
- eval-runner run_hook (:72-82) overrides HOME per-invocation but never CLAUDE_PROJECT_DIR → the hooks' `cd "${CLAUDE_PROJECT_DIR:-.}"` escapes sandboxes into the live repo (T1 fixes FIRST).
- test_tooluse_hooks.sh + test_firing_log.sh PASS TODAY while the hooks are dormant — hand-written fixtures are live circularity evidence, never sufficient alone.

### Registration surface + A2 deferral (gate-time scan)
from: [[decision:hook-registration-hygiene]]
retrieved: 2026-06-09

- Live ~/.claude/settings.json: 12 kit-owned commands (context-size-check the only legitimate global) in MIXED forms (7 `bash ~/.claude/hooks/X.sh`, 5 absolute) — extraction/surgery must normalize by basename.
- Scan: edge-analyst + edge-screener 11/11 local; stock-screener/ai-game/fate 3/11; ai-game has a literal null command entry; ~/.claude/enforce armed globally → enforcement in the 3 partial roots currently runs via the ghosts.
- A2 is DEFERRED don't-know (ledger revisit-status open): NO live settings execution on direction-gate authority; the T4a hard checkpoint decides with the completed matrix; remediate-then-deregister is the prepared default.

### Execution discipline
from: [[decision:hook-registration-hygiene]] + [[HEU-012]]
retrieved: 2026-06-09

- Zsh hazard (gate-time self-caught): unquoted $VAR lists don't word-split in zsh — a checker loop produced a false 0/11-everywhere matrix; every matrix/extraction script needs a positive control (a known non-zero cell) before its output counts.
- 6 eval corpus scenarios reference the two hooks (hook-post-commit-{detected,amend-skip,non-commit}, hook-detect-loop-{3-failures,2-no-warn}, lifecycle-full-phase-cycle) — the flip-explanation surface; legacy fallback retention keeps the 52 denominator.
