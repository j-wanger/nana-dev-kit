# Active Knowledge
<!-- Phase-scoped. Auto-generated by /dev plan. Cleared on phase change. -->
<!-- DO NOT EDIT MANUALLY — regenerate with /dev plan -->

## Phase: 88 - Trim Follow-On Round

### Verified apparatus + evidence facts
from: [[decision:trim-follow-on-round]]
retrieved: 2026-06-11
- c2 is a FUNCTION inside eval/ceremony-lift/stage2/run-exit-criteria.sh (~line 31) — the tightening edits the runner itself; no standing check pins the 3 routed checkers' own bytes (A2 spike-defended).
- session-start.sh:110 couples detect-loop state-clear AND enforce-memory marker-clear in ONE rm line — both removal sets carry the split pre-listed.
- enforcement.log: JSON-lines, ISO ts, schema_version-1 records carry a phase field; live count 385 (drifted from the spec's stale 69) — artifacts pin the count the filter actually produced, never 69.
- The log schema is decision-only (no agent-action capture) — `undecidable-on-this-evidence` is the pre-stated fallback for block→follow-through reconstruction.

### Gate-narrowed scope + claim ceiling
from: [[decision:trim-follow-on-round]] + specs/phase-88-trim-follow-on.md
retrieved: 2026-06-11
- A4/A6 REJECTS: memory-layer disposition + bridge/harvest writer trims DEFERRED; strand 2 = working-knowledge seeding + journal prose only; enforce-memory disposes against a LIVE layer.
- Trims are reversible trim-trials — revert SHA + trigger + observation window + Blockers filing recorded BEFORE the commit ships; zero cuts/trims is a valid outcome.
- Phase-87 verdicts stand as recorded under the old checker versions — never re-graded.

### Method: Phase-83 extended + HEU-012/HEU-007
from: [[decision:prune-on-value-subtraction]] + [[HEU-012]] + [[HEU-007]]
retrieved: 2026-06-11
- Verdict-table-first; couldnt-fire vs didnt-fire before any verdict on a load-bearing zero (couldnt-fire = DEFECT, never a demand-evidence cut).
- Serialized per-candidate commits; basename-normalized own-ghosts deregistration; survivor smoke; regenerated-diff ⊆ removal set.
- Verify firing not presence — deregistration held to the same standard: the ghost sweep counts only after a seeded-ghost positive control passes.
- check-tests-were-run harden = HEU-007 dual-condition: trigger keys on Edit/Write of code files AND retains tests-not-run — the Read false-positive dies without raising false negatives.
