// Placeholder shell. The assistant-ui custom runtime + AI Elements surface
// (T6) binds here, pointed at an EngineAdapter's event stream.
export function App() {
  return (
    <main className="app">
      <h1>Nana Dev-Harness</h1>
      <p>v1 thin slice — the engine adapter + in-process gate wire in here.</p>
    </main>
  );
}
