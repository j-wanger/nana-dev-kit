{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window. The capability manifest is THE security boundary (an over-broad fs/shell grant chained with an XSS is RCE), so it starts empty-of-broad-grants and is audited + justified grant-by-grant in T6. No fs/shell/http capability is granted here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
