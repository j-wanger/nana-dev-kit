# Spec: Phase 90 — Fable-5 Distillation (codifiable lift + design recovery)

> Status: nana:approved 2026-06-13 (direction gate closed; A1 accept-pilot-gated, A2/A3/A4 accept).
> Ceremony: standard. This spec records the contract agreed at the direction gate; implementation
> followed inline (the work is low-count, high-blast-radius edits the maintainer reviews at the delivery gate).

## Objective

Make future opus-4-8 sessions behave more like the removed fable-5 at assumption-surfacing, frame-challenging, and interaction clarity — by (a) lifting the few *checkable* forcing-functions fable used and opus lacks, and (b) recovering, via system design (context-shaping), the "intrinsic" reframe behaviors a rules file cannot. NOT by adding more prose opus already complies-with-in-form.

## Scope

- `~/.claude/rules/nana-soul.md` + `templates/.claude/rules/nana-soul.md` (Track A, parity)
- `~/.claude/skills/dev-plan/SKILL.md` + `templates/.claude/skills/dev-plan/SKILL.md` (Track B1, parity)
- `eval/fable-distill/**` (research artifact + Track B2 staged mechanism + pilot protocol)
- dev-wiki lifecycle artifacts; memory-prune renumber 90→91

OUT: deploying the framing subagent (pilot-gated); building I2 reveal-banking (deferred); editing `nana-personal.md` (user-owned); the trim-trial commits or the Phases-90-93 window-events obligation; any measured-lift claim (unmeasurable in-kit).

## Assumptions (direction-gate positions)

- **A1 (accept, pilot-gated):** clean-context forcing functions recover reframe-depth — pilot in a consuming project before wiring as default.
- **A2 (accept):** the 5 lines are genuinely absent and, being checkable, executed not rubber-stamped — confirmed via absence-diff before edit.
- **A3 (accept):** ships on judgment; no A/B proof of lift; exit = applied + self-consistent + parity, not a measured delta.
- **A4 (accept):** cleared to edit the global soul file directly; maintainer reviews at the delivery gate.

## Exit criteria (machine-checkable)

1. nana-soul.md (both copies) IDENTICAL and each of the 5 forcing-functions present exactly once.
2. dev-plan SKILL.md (both copies) IDENTICAL and contains the reframe-absorption beat.
3. `eval/fable-distill/` contains findings.md, framing-subagent-prompt.md, pilot-protocol.md.
4. framing subagent is NOT referenced as a default step in dev-plan SKILL.md (pilot-gated).
5. No memory-prune artifact still claims Phase 90 (renumbered to 91).
6. Decision article + spec persisted; active-phase.md reflects Phase 90.
