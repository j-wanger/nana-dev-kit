# Answer tokens for the eligibility candidate — the inclusive-through-d implementation must NOT be
# stated in R_A (only the GOAL is). Also the hidden test's fixture literals. Matched case-insensitively.
removed_today
inclusive through
inclusive-through
add back
added back
add the removed
members | removed
max(as_of
removed effective exactly
AAA
BBB
CCC
