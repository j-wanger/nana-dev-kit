# Memory-Layer Demand Evidence — Phase 89 close-out (A4/A6 deferred round input)

Per the pre-registered admissibility pins. Substrate: the 3 edge-screener evidence sessions
(post-resync, liveness-probed live layer — couldnt-fire excluded). Kit-side numbers reported
separately below (informational: the kit is not the demand substrate, Ph80 leak class).
Classification: orchestrator-executed pinned rules over transcripts; zero calls needed no
tie-breaks.

## Edge-screener demand tallies (the substrate)

hook-prompted: 0
rules-instructed: 0
spontaneous: 0
instructed-with-readback: 0
couldnt-find: 0
writer writes: 0
writer read-backs: 0
end-of-round DB row count: 0

Notes the disposition round needs:
- The zero is THREE-WAY clean: no hook coercion occurred (enforce-memory fired-allow on every
  write, no nudge issued), the always-loaded rules instruction (session-start broad
  memory_search per nana-soul.md) was NOT followed in any session (the voluntary-skip pattern,
  consistent with [[mandatory-automatic-voluntary]]), and no spontaneous use occurred.
- couldnt-find: 0 because no attempt was made at all — the gate-time deferred-tool caveat
  (a scratch probe declared the tool unavailable rather than searching) therefore never
  activated here; it remains a standing caveat, not an observed event.
- The pinned multi-session CONTINUITY case ran (session 2 depended on session 1's paused
  planning state) and was served ENTIRELY by the .dev-wiki file substrate with zero
  memory-layer involvement — the strongest single data point: the layer's design case arose
  naturally and the file layer absorbed it.
- writer writes 0 because session 1 stopped at the direction gate BEFORE dev-plan Step 15a-bis
  (the bridge writer) — the writer step was structurally unreached in this round's es
  sessions, NOT declined; recorded as unreached, never as no-demand for the writer.

## Kit-side (informational, separate per pin 1; session ef772719)

hook-prompted: 1 (enforce-memory BLOCKED a Write → the session ran memory_search + touched the
marker — block follow-through COMPLY; the Phase-83 distribution gets its first post-restoration
block datapoint)
rules-instructed: 0 (the session-start broad search was skipped here too until the hook forced it)
spontaneous: 0
instructed-with-readback: 0
writer writes: 2 (dev-plan 15a-bis bridge stores — the A6-kept writer is LIVE in standard-ceremony
kit planning)
writer read-backs: 1 (the hook-prompted memory_search returned both same-session bridge entries;
same-session read-back, weaker than cross-session — stated)
end-of-round DB row count: 89 (kit store)

## Pointer

Filed for the deferred A4/A6 round (Blockers entry in `.dev-wiki/_CURRENT_STATE.md` cites this
file). Disposition belongs to that round, not Phase 89.
