# Liveness Probe — read-only, T4 (VALID fixture)

## Probe record

memory server import probe (read-only, never a store): python -m memory_server import check
exit code: 0

## Marker states

marker states: ~/.claude/enforce-memory=present; <project>/.claude/enforce-memory=absent

## DB baseline

before-round DB row count: 11
