#!/usr/bin/env bash
# Fixture placeholder. The check-currency.sh selftest OVERWRITES this file with the live kit
# template (templates/.claude/hooks/check-tests-were-run.sh) in a mktemp staging copy, so the
# pass fixture stays current as the template evolves. Never asserted against directly.
exit 0
