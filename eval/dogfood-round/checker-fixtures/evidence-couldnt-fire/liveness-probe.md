# Liveness Probe — read-only, T4 (VALID fixture: couldnt-fire variant)

## Probe record

memory server import probe attempted; venv import failed before the probe could run.
couldnt-fire: recorded explicitly per pre-registration — demand strand BLOCKED, never a silent zero.

## Marker states

marker states: ~/.claude/enforce-memory=present; <project>/.claude/enforce-memory=absent

## DB baseline

before-round DB row count: 11
