# active-knowledge.md reader/writer surface enumeration (Phase 88 T1)

Re-derivation: `grep -rn 'active-knowledge' templates/ scripts/ tests/ Makefile modules.json`
Extracted 2026-06-11 at repo base 7e9c56f. Verdict question (gate A5): do readers degrade
gracefully on file-absence, so the ride-along trim can be a clean single-commit deletion?

## Surfaces, classified

| # | surface | role | on-absence behavior | trim impact |
|---|---|---|---|---|
| 1 | dev-plan SKILL.md 15f-bis + 15f-ter | WRITER (the trim target) | n/a | removed by the trim |
| 2 | dev-plan artifact-writer-prompt.md §7-8 | WRITER (dispatched copy of #1) | n/a | removed by the trim |
| 3 | wiki-query SKILL.md Step 8a (lines 208-223) | SECOND WRITER — promotes working-knowledge entries; "if absent, CREATE with header" | RESURRECTS THE FILE | MUST join the removal set, else the trim is incomplete |
| 4 | dev-debrief SKILL.md Step 9 (193-205) | reader (activation-quality scoring) | "If ... does not exist, skip this step" — GUARDED | becomes permanently-skipped; step text removed by the debrief-capture trim anyway |
| 5 | dev-debrief SKILL.md Step 19 + active-knowledge-transition.md | lifecycle (carry-forward + delete at phase close) | "If no ... file, skip entirely" — GUARDED | becomes dead lifecycle; companion joins removal set (test_companions.sh update required) |
| 6 | dev-debrief executor-prompt.md:61 | reader (activation quality) | "If ... exists" — GUARDED | text removed with #4 |
| 7 | dev-check SKILL.md:63 (S10) + :120 remediation | validator (phase-match check) | "skip if absent" — GUARDED | S10 becomes a no-op; row may be cleaned in-commit (S; optional) |
| 8 | dev-plan compaction-anchors-spec.md (incl. :76) | spec/doc | explicit "absent at recovery → Skip" row — GUARDED | doc cleanup in trim commit |
| 9 | dev-wiki SKILL.md:108,117; size-budgets.md:22; active-knowledge-spec.md | spec/doc references | descriptive only | active-knowledge-spec.md joins removal set; others get line cleanup |
| 10 | dev-init templates AGENTS.md:16 ("skip if missing") + project-CLAUDE.md:32 | consuming-project session protocol | GUARDED in template text | template line cleanup; already-initialized projects keep stale copies (point-in-time, filed per spec) |
| 11 | tests/test_working_knowledge_curation.sh:236 | fixture STRING `[[active-knowledge:phase-45]]` | not a file reader | unaffected |

## Verdict-relevant summary (A5: HELD, with one mechanism note)

- Graceful-on-absent VERIFIED for every runtime reader (#4-#8, #10): each guards with an
  explicit skip-if-absent. No reader errors on deletion — the clean single-commit deletion
  mechanism stands.
- ONE coupling the gate's A5 framing missed, caught here at T1 as designed: wiki-query Step 8a
  is a second WRITER that re-creates the file on its promotion path. The active-knowledge
  ride-along removal set = dev-plan 15f-bis/15f-ter + artifact-writer §7-8 + wiki-query 8a
  + active-knowledge-transition.md (companion; test_companions.sh row) + active-knowledge-spec.md
  + doc-line cleanup (#7-#10). Single commit remains feasible — the set is bigger than
  "stop writing the file," not differently shaped.
- The CURRENT phase's own .claude/rules/active-knowledge.md (Phase 88's) is regenerated by the
  live installed skill until the trim ships; the trim commit deletes the repo-side writer —
  the file itself dies at the next phase transition (Step 19 deletes it; nothing recreates).
