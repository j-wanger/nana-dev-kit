# Outcome-Determined Ground-Truth Labels (Phase 80, T1 GREEN)

Three real past phases. Each label asserts an assumption the EARLIER phase's plan made, certified
load-bearing by a LATER MECHANICAL signal (R1–R4 of `circularity-predicate.md`) — never by post-hoc
"this mattered" judgment. Quotes are verbatim from the record.

---

## L1 — Phase 58 (active web-research injection into planning)

- **Assumption X (from the Phase-58 plan):** "Injecting actively-researched findings into dev-plan (the
  Step 2.7 web-research step) improves plan quality; the measured +0.5 composite is a real, bankable
  effect worth keeping."
- **Certifying signal — R4 (empirical contradiction) + R2 (reversal) + R1 (feature removed):**
  > "research injection into planning was CUT (Phase 59 — measured net-negative; reverses Phase 58's Step
  > 2.7) … Phase 59 strengthened it (3 new topics, 13 paired runs at n≥3 …): the +0.5 did NOT replicate —
  > all n≥3 deltas were ≤0 … ⇒ feature CUT"
- **Load-bearing because:** the whole Phase-58 deliverable rested on X; X was false (the +0.5 sat inside
  the n=5 noise band), and being wrong about X = shipping a net-negative feature. Source:
  `[[decision:measure-residual-research-delta]]`, Phase 59 results.

## L2 — Phase 50 (scenario-020 wrongness / length-as-driver)

- **Assumption X (from the Phase-50 plan):** "The model's 8/9-wrong result on scenario 020 reflects a
  genuine reasoning weakness the harness should correct."
- **Certifying signal — R4 (empirical contradiction):**
  > "Clean baseline solves scenario 020 correctly (5/5/5). The '8/9 wrong' finding (Phase 50) was from
  > IRON-RULES-injected conditions — IRON-005 biases toward dependency upgrade over capacity multiplier"
- **Load-bearing because:** X mislocated the defect (in the model, not in the injected IRON-RULES). Being
  wrong about X sent effort at the wrong target; Phase 53 reattributed it. Source: `[[active-knowledge:phase-53]]`.

## L3 — Phase 47 (self-dialogue / devil's-advocate in dev-plan)

- **Assumption X (from the Phase-47 plan):** "A self-dialogue / devil's-advocate step citing IRON RULES
  will improve reasoning quality."
- **Certifying signal — R4 (empirical contradiction):**
  > "devil's advocate with IRON RULE citations does not improve reasoning quality. Inline is net negative
  > (adds hedging), subagent is net neutral"
- **Load-bearing because:** X was the phase's premise; it was false, and the pivot ("next lever is
  heuristic *selection* not *application*") only happened once X was contradicted. Source:
  `[[journal:2026-05-27-phase-47-self-dialogue-in-dev-plan-complete]]`.

---

## Sparsity check
≥1 non-circular label per phase for 3 phases ✓ (R4 present in all three; R1/R2 also in L1). Many more
exist in the record (Ph48 trace-collection cut→Ph64; Ph49 conditional-injection zero-delta; the Ph70/71/77
amplifier TERMINATEs) — labels are NOT sparse. The corpus is rich in R4 contradictions because this project
debriefs negative results explicitly.

## Circularity self-audit (against the predicate)
- C1 (judgment without signal): NOT triggered — each label cites a verbatim R-signal that NAMES the
  contradicted assumption.
- C3 (no textual anchor in P's own plan): each X is anchored in the earlier phase's own deliverable/premise.
- C2 (blind baseline leaks outcome): tested separately — see `blind-baseline-*.md` (clean-context pass).
