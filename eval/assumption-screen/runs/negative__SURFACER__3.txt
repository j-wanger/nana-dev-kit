- [scope:call-sites/grep-completeness] HIGH — if there are call sites beyond the three named files (e.g. dynamic dispatch, getattr('calc_total'), string-based references, other modules, or callers in another package), the rename leaves dangling references that fail at runtime, not at test time. ASSUMPTION: the three files (billing.py, invoice.py, report.py) plus the one unit test are the COMPLETE set of references to calc_total; no other module imports or calls it, and it is never referenced by string/reflection.
- [scope:function-definition-site] HIGH — the plan lists three CALL sites and one test but never names where calc_total is DEFINED; if the definition isn't renamed (or is in a file not in scope), every renamed call breaks. ASSUMPTION: the function's definition site is known and included in the edit set, and `compute_total` is not already a name in use (no collision/shadowing at the definition's namespace).
- [scope:public-api-surface] HIGH — if calc_total is part of a public/exported API (imported by code OUTSIDE this repo, re-exported in __init__, or named in __all__), a pure in-repo rename silently breaks external consumers that tests can't see. ASSUMPTION: calc_total is private/internal to this codebase; no external caller, plugin, serialized reference, or cross-repo import depends on the name.
- [scope:test-coverage] HIGH — Success is defined ONLY as 'tests pass, no behavior change'; if the single referenced test does not actually exercise all three call sites (or coverage is thin), passing tests will NOT detect a missed/broken reference. ASSUMPTION: the test suite's pass/fail signal genuinely covers every renamed call site, so 'tests pass' is a valid completeness proxy.
- [scope:test-references] HIGH — the plan assumes exactly ONE unit test references the old name; if other tests, fixtures, mocks, or parametrize strings also reference calc_total, they break. ASSUMPTION: only one test file/reference uses the old name and updating it is sufficient.
- [framing] MEDIUM — 'no behavior change' assumes the rename is purely lexical; if calc_total is referenced via metaprogramming, monkeypatching, decorators keyed on __name__, logging that emits the function name, or serialization, the NAME is itself behavior and changing it changes output. ASSUMPTION: the function name is not load-bearing at runtime (no introspection on __name__).
- [scope:test-runner-environment] MEDIUM — 'Success: tests pass' silently assumes a working, installed test runner and environment that can collect and execute these tests. ASSUMPTION: the test harness is installed, runnable, and currently green BEFORE the change (otherwise 'tests pass' is meaningless as a gate).
- [framing] LOW — the entire task assumes this rename is worth doing (calc_total → compute_total carries no semantic value beyond naming preference). ASSUMPTION: the rename is a desired, sanctioned change and not churn that costs review/merge-conflict overhead exceeding its benefit.
- [framing] LOW — assumes a rename is the right substrate vs. an IDE/automated refactor tool (LSP rename, ruff/rope) that would guarantee completeness across the codebase. ASSUMPTION: a manual three-file edit is as safe as a refactor-aware tool here, i.e. the codebase is small/simple enough that manual edits won't miss references a tool would catch.
