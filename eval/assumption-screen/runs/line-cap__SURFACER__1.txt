- [scope:session-start.sh] session-start.sh must actually FIRE at session start as a registered SessionStart hook — a registered-but-dormant hook passes a file-presence test and ships broken; if it never fires, the memory-health line never appears and Success is unreachable. COST: total feature no-op, silently. (HIGHEST: the working-knowledge cascade-failure anti-pattern shows 3 prior instances of hooks silently unwired for 8-38 phases.)
- [scope:MCP-memory-DB-path] The DB the probe reads must be at the path the probe checks. Working-knowledge explicitly records this was BROKEN: Claude Code ignores settings.json cwd and uses the project directory, so the DB lives at <project_root>/.memory/memory.db, NOT ~/.claude/.memory/ — the old memory-nudge.sh health probe checked the WRONG PATH (and wrong column is_active vs active), making Phase 19-48 entries irrecoverable. ASSUMPTION the plan silently makes: probe reads the correct path. If wrong, the probe reports 'broken/not-configured' on a healthy store (or vice-versa) — a confidently WRONG health line, which is worse than none. COST: actively misleading every session.
- [scope:MCP-memory-DB-schema] The probe must query the correct column/schema (active, not is_active) and the sqlite3 entry-count query must match the live table shape; a wrong column name silently returns a false health verdict. COST: inaccurate health line, directly violates Success ('accurate').
- [scope:MCP-memory-server] The memory MCP server (memory_server, python -m memory_server) must be installed, importable, and runnable for 'healthy' to be a meaningful state; if the server import-check is the thing being probed it must itself be wired (install.sh import-check). COST: probe cannot distinguish 'server absent' from 'server broken'.
- [scope:settings.json] The probe reads server config from settings.json (.mcpServers.memory via jq); settings.json must exist, be valid JSON, and contain the memory MCP block at the expected key path. COST: probe crashes or reports not-configured spuriously. Note: working-knowledge warns settings.json cwd is IGNORED by Claude Code — so config presence != working store.
- [scope:jq] jq must be installed and on PATH — it is the hook's hard dependency for parsing settings.json; hooks are fail-open (exit 0) so a missing jq yields a silent skip, not an error. COST: silent no-op, Success unmet without signal.
- [scope:session-start.sh-line-budget] session-start.sh is under a documented line cap (70 lines, Phase 22) that already eroded to 137 lines once with NO test catching it; adding a probe + nudge grows it further. ASSUMPTION: the additions stay within budget OR a test asserts the cap. COST: silent bloat regression; also feeds the broader context-size warning the kit just fought (Ph79).
- [scope:memory-nudge.sh] If the nudge reuses/extends the existing memory-nudge.sh, that script is the very component working-knowledge flags as having the wrong-path + wrong-column bug — reusing it without fixing inherits the defect. COST: ships a known-broken probe as 'new'.
- [scope:hook-firing-test] A functional firing test (pipe a real SessionStart event, assert the health line is emitted with the right verdict for healthy/broken/not-configured states) must exist — the kit's functional-smoke invariant requires it; presence tests are insufficient. ASSUMPTION the plan omits: such a test is written. COST: undetected breakage (the recurring failure class).
- [scope:hook-message-prefix] The emitted line must use the [nana:memory] sub-prefix convention session-start.sh already uses, else it's inconsistent/unparseable by downstream tooling. COST: low — cosmetic/convention drift.
- [framing] This problem may already be SOLVED. Working-knowledge describes session-start.sh ALREADY having a '3-layer MCP health probe (jq config read, import check, sqlite3 entry count)' emitting a 3-state output healthy/broken/not-configured — i.e. the plan re-builds existing functionality. Load-bearing: is there net-new value here, or is this re-presenting a capability that already ships? (The amplifier program's repeated finding: NULL headroom in re-presenting what already exists.)
- [framing] Is a per-session memory-health line worth its always-loaded context cost? It runs every session forever; the kit just spent Phase 79 fighting context-size bloat. Cost-of-error of the FEATURE itself: permanent context tax on every session for a status line that's actionable only when broken. Subtraction test: does a STARTUP probe earn its place vs. a fail-loud check only when memory is actually used?
- [framing] 'Accurate' is the Success criterion but accuracy is exactly what historically failed (wrong path/column gave confidently-wrong verdicts). Load-bearing: the substrate (probe-by-path-inspection) is the same substrate that produced the prior false readings — is path/column inspection the right approach, or should the probe do a real round-trip (store+search a probe entry) to verify the store actually works end-to-end rather than inferring health from file/config existence?
- [framing] MCP memory is classified as the VOLUNTARY layer (nudges only) in the kit's 5-layer memory architecture; investing hook complexity to surface its health may be misaligned with its tier — the documented insight is 'strengthen existing always-loaded .claude/rules/ activation points, don't add hooks (hooks can be unwired — 3 cascade failures).' Load-bearing: is a hook the right activation substrate at all?
