- [framing] The three named files (billing.py, invoice.py, report.py) are the COMPLETE set of call sites — no dynamic/indirect references (getattr, string-based dispatch, reflection, __getattr__, plugin registries) reference calc_total by name; if any exist, the rename silently breaks them at runtime with no test signal. WORST cost: silent production breakage that passes the suite.
- [framing] The function is referenced ONLY inside this codebase — no external/downstream consumer imports calc_total as a public API (other packages, serialized configs, entry-point manifests, docs/CLI). If it is part of a public surface, a pure rename is a breaking API change, not a no-op. High cost: breaks external callers invisibly.
- [scope:calc_total definition site] The definition of calc_total lives in exactly one place and that file is in the rename set — the plan lists three CALL SITES but never names where the function is DEFINED. If the definition file is unlisted, the rename either misses the def (NameError everywhere) or is incomplete. High cost: total breakage. Assumption: the def location is known and included.
- [framing] "Three call sites" is the actual, verified count — the number came from a real search (grep/IDE), not an estimate. If a 4th call site exists (or the count predates recent commits), it is left calling the old name. High cost: runtime NameError on an untested path. Assumption: call-site enumeration is exhaustive and current.
- [scope:unit test] Exactly ONE unit test references the old name, and that test is the only test coupled to it — no other test, fixture, mock, or patch target (e.g. mock.patch('module.calc_total')) hardcodes the string. If others exist, "tests pass" is false post-rename or, worse, a stale mock makes tests pass while masking a real break. High cost: false green. Assumption: only one test references calc_total and it is the one being updated.
- [scope:test suite / CI] The test suite actually EXERCISES all three call sites and the renamed function — "tests pass, no behavior change" is only a valid success signal if those paths are covered. If billing/invoice/report code paths are untested, a passing suite proves nothing about the rename's safety. High cost: "success" is meaningless. Assumption: the suite covers each call site OR success criteria add coverage/grep checks.
- [scope:test runner] A working, runnable test suite exists and passes BEFORE the change (green baseline). "Tests pass" as success presumes they passed pre-rename; if the baseline is red or the runner is mis-wired, the success check is uninterpretable. Medium cost: cannot distinguish rename breakage from pre-existing failure. Assumption: green pre-change baseline is established.
- [framing] This is a PURE rename with no name collision — compute_total is not already defined/imported anywhere in scope (no existing compute_total, no shadowing, no import clash). If the new name already exists, the rename silently rebinds or shadows it. Medium-high cost: subtle behavior change despite "no behavior change" claim. Assumption: compute_total is an unused identifier project-wide.
- [framing] The rename is the actual goal and is worth doing — a function rename touching three files and a test earns its churn (readability/consistency) versus the merge-conflict / blame-noise / review cost it imposes. Low-medium cost if wrong: pure overhead with no payoff. Assumption: the naming improvement justifies the change.
- [scope:string/log references] calc_total appears nowhere as a STRING literal that matters — log messages, error strings, metric/telemetry names, feature-flag keys, or docstrings that downstream tooling keys on. A code-symbol rename that misses string occurrences either leaves stale strings (cosmetic) or breaks string-keyed lookups (functional). Low-medium cost: stale references or broken keyed lookups. Assumption: no behavior-bearing string references to the old name exist.
- [scope:import statements] Each of the three call-site files imports the function in a way the rename must follow (from x import calc_total, import x then x.calc_total, or `as` aliasing) — the rename must update import bindings, not just call expressions. If an `import ... as calc_total` alias exists, the call site need not change at all and a naive call-site edit breaks it. Low-medium cost: incomplete/incorrect edits. Assumption: import/alias forms are accounted for at every site.
