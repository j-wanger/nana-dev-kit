- [scope:test suite / CI] The test suite must actually exercise calc_total's behavior so that "tests pass" is a real safety net — if the function is untested (the "one unit test" only checks the name binding, not the computation) a behavior change could slip through silently. COST: the success criterion "no behavior change" is unverified and a regression ships green.
- [scope:call sites — completeness of "three"] All references to calc_total must be exactly the three call sites named (billing.py, invoice.py, report.py) plus the one test — there must be no additional, dynamic, or out-of-tree references. COST: a missed reference (e.g. getattr/string dispatch, a 4th module, __all__ export, a caller in another repo/service) raises NameError at runtime after the old name is gone — a hard production break, the worst outcome here.
- [scope:definition site] calc_total must be defined in exactly one place and that definition is included in the rename — the plan names three CALL sites but never names where the function is DEFINED. COST: if the definition isn't renamed, every call site breaks; if there are multiple definitions/overloads, partial rename produces inconsistent dispatch.
- [scope:name uniqueness / collision] `compute_total` must not already exist (as a function, import, attribute, or method) in any of the touched files or their namespaces. COST: a silent shadow/override — the new name binds to or clobbers an existing symbol, changing behavior despite the "pure rename" intent.
- [scope:call sites — invocation form] calc_total must be invoked only by direct name, not via string/reflection (getattr, __dict__, dispatch table, decorator registry, serialized config, log/metric keys). COST: a textual rename leaves string-keyed lookups pointing at the dead name → runtime KeyError/NameError that no compile step catches.
- [scope:public API / external consumers] calc_total must not be part of any public/exported surface (package __all__, importable by external modules, plugins, or downstream packages). COST: renaming an exported symbol is a breaking API change for consumers outside the three files — out of scope but high cost if true.
- [framing] The measured/claimed effect must hold: a name-only edit must genuinely be behavior-preserving — i.e. there is no metaprogramming, caching keyed on the old name, or test asserting on the function's __name__/repr that the rename would flip. COST: "no behavior change" is falsified by an edit assumed inert.
- [scope:test reference count] There must be exactly ONE test referencing the old name as the plan states — if more tests (or fixtures/parametrize ids/mocks patching 'module.calc_total') reference it, updating only one leaves the rest red or, worse, mock-patching a now-nonexistent attribute. COST: broken or misleadingly-passing tests.
- [framing] This rename is worth doing at all — a pure cosmetic rename earns its risk (every changed line traces to a real readability/consistency need). COST: spending review/regression-risk budget on a zero-functional-value change; the subtraction test may say skip it.
- [framing] Text-rename is the right substrate vs. an IDE/AST-aware refactor — a grep/sed-style rename is acceptable because the codebase is small and the name is unambiguous. COST: if the change is done textually it can hit substrings or comments/docstrings/unrelated identifiers containing `calc_total`; an AST/symbol-aware rename would be the safer approach if the name is not unique-as-a-token.
