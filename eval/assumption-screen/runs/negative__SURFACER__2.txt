- [scope:call-site enumeration] The plan claims exactly three call sites (billing.py, invoice.py, report.py). HIGH cost: if any call site is missed, the rename produces a NameError at runtime or a silent call to a now-deleted/wrong symbol. ASSUMPTION: those three files are the COMPLETE set of references to `calc_total` — no other module, dynamic call (getattr/string-based dispatch), `__all__` export, or re-export references the old name.
- [scope:calc_total definition site] The plan renames the function but never names where `calc_total` is DEFINED — it only lists call sites. HIGH cost: if the definition is not also renamed (and the plan implicitly assumes it is), every call site breaks; if the definition lives in one of the three files, the file list conflates definition with call. ASSUMPTION: the definition site is known, will be renamed, and is distinct from / accounted within the three listed files.
- [scope:test suite] Success criterion is "tests pass." HIGH cost: if the existing test suite does not actually exercise the renamed function (low coverage, skipped tests, or tests that pass regardless), "tests pass" gives false assurance of "no behavior change." ASSUMPTION: the test suite is green BEFORE the change, runs, and meaningfully covers the call paths through `calc_total`.
- [scope:the one referencing unit test] The plan asserts exactly ONE unit test references the old name. HIGH cost: if more than one test (or a fixture, mock, patch target like `patch('module.calc_total')`, or parametrized name) references it, those break and the rename is incomplete. ASSUMPTION: exactly one test references `calc_total` and no string-based mock/patch targets the old name.
- [scope:public API surface] HIGH cost: if `calc_total` is part of the package's public API (imported by external callers, documented, or exported), an in-repo-only rename silently breaks downstream consumers the plan never sees. ASSUMPTION: `calc_total` is purely internal — no external/import-by-name consumers outside these files.
- [framing] The whole effort assumes a pure rename has no behavioral effect. ASSUMPTION: nothing keys off the function's name itself — no logging/metrics by function name, no introspection (`__name__`, decorators, serialization, registry-by-name, RPC/dispatch table) that would change behavior when the name changes.
- [scope:no behavior change] MEDIUM cost: the rename touches only an identifier, so behavior should be unchanged — but this holds only if no signature, default args, or logic is incidentally edited. ASSUMPTION: the change is identifier-only; no body or signature is touched.
- [scope:VCS / single-author working tree] MEDIUM cost: a cross-file rename assumes no concurrent edits or merge conflicts on these files. ASSUMPTION: the working tree is current and no one else is mid-edit on `calc_total`.
- [framing] ASSUMPTION: this rename is worth doing at all — `compute_total` is a genuine readability/consistency improvement over `calc_total`, not bikeshedding that incurs churn (diff noise, review time, broken open branches) for negligible benefit.
- [scope:naming collision] LOW cost: `compute_total` must not already exist in any of the touched modules' namespaces. ASSUMPTION: the new name is free — no existing `compute_total` symbol it would shadow or collide with.
