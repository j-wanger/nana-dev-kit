- [scope:call-sites-grep] All references to `calc_total` are confined to the three named files (billing.py, invoice.py, report.py) plus the one unit test — COST: if any other caller, import, dynamic dispatch, or string-based reference exists outside this set, it silently breaks at runtime after the rename (highest cost: undetected production breakage).
- [scope:dynamic-references] `calc_total` is never invoked indirectly — no getattr/string-name lookup, no serialization/config that stores the function name, no `__all__` export, no monkeypatch — COST: a grep-based rename misses these and tests still pass while a runtime path 500s.
- [scope:test-coverage] The single referencing unit test actually exercises the renamed function on every call path the three sites use — COST: if coverage is partial, 'tests pass' is a false green and behavior changes go undetected; this is the load-bearing safety net of the whole plan.
- [scope:test-suite] The test suite is green BEFORE the rename and is the sole verification — COST: if it was already failing/flaky, 'tests pass' as success criterion is unverifiable and the no-behavior-change claim rests on nothing.
- [scope:function-definition] There is exactly one definition of `calc_total` (not multiple, not shadowed, not re-exported under an alias) — COST: if two definitions or an alias exist, renaming one leaves a stale name and either a NameError or a divergent code path.
- [scope:name-collision] `compute_total` is not already defined/imported in any of the three files — COST: collision shadows or overwrites an existing symbol, introducing a behavior change the plan claims cannot happen.
- [scope:import-statements] No file imports `calc_total` by name (`from x import calc_total`) such that the import line also needs updating — COST: missed import edit yields ImportError at module load, breaking files the plan didn't intend to touch.
- [scope:no-comment-coupling] Docstrings, log messages, error strings, or metrics/telemetry keys referencing the literal 'calc_total' are out of scope — COST: low, but stale references mislead operators/dashboards even though execution is unaffected.
- [framing] A pure rename is worth doing at all — the readability/consistency benefit of `compute_total` over `calc_total` exceeds the churn, review, and merge-conflict cost of touching four files.
- [framing] Rename is the right substrate — the naming inconsistency is the actual problem, versus a deeper smell (e.g., the function does more than 'compute total') that a rename only papers over.
- [framing] 'No behavior change' is the correct success bar — there is no in-flight branch, no external consumer, and no API/serialized contract for which renaming the symbol IS a behavior change.
